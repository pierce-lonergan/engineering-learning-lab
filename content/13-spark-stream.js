/* Structured Streaming (SPARK-STREAM) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"SPARK-STREAM","front":"The core model of Structured Streaming (the \"unbounded table\")","back":"A stream is modeled as an <em>unbounded input table</em> to which new rows are appended over time. A query is the <em>same</em> declarative DataFrame/SQL you'd write on a static table; the engine runs it <em>incrementally</em>, converting it into a plan that updates a <em>result table</em> as new input rows arrive, and writes deltas to the sink. You write a batch query; the engine makes it streaming."},
  {"sec":"SPARK-STREAM","front":"Micro-batch vs continuous processing — latency and semantics","back":"<strong>Micro-batch</strong> (default) discretizes the stream into small jobs; latency is typically ~100ms+ with exactly-once guarantees. <strong>Continuous processing</strong> (experimental since 2.3) runs long-lived tasks polling sources to reach ~1ms latency but offers only <em>at-least-once</em> and supports a restricted set of ops (map-like projections/filters, no aggregations/joins). Most production uses micro-batch."},
  {"sec":"SPARK-STREAM","front":"What does a trigger control, and what are the trigger types?","back":"The trigger sets <em>when</em> the next micro-batch fires. Options: default (fire ASAP when previous batch completes), <code>processingTime=\"N seconds\"</code> (fixed cadence), <code>Trigger.Once</code> / its successor <code>Trigger.AvailableNow</code> (process all available data then stop — batch-like, used for scheduled runs), and <code>Trigger.Continuous(\"1 second\")</code> (continuous mode checkpoint interval). <code>AvailableNow</code> respects rate limits across multiple internal batches; <code>Once</code> does one batch."},
  {"sec":"SPARK-STREAM","front":"What problem does a watermark solve, and how is it defined?","back":"A watermark bounds how long the engine waits for late event-time data, so it can (a) <em>drop</em> data later than the threshold and (b) <em>evict</em> old state/finalize windows. Define with <code>withWatermark(\"eventTime\", \"10 minutes\")</code>. The watermark = <code>max(event_time seen) − threshold</code>, and it only moves forward (monotonic). Without it, stateful aggregations must keep state forever."},
  {"sec":"SPARK-STREAM","front":"How is the watermark computed across multiple partitions/streams?","back":"Spark tracks the max observed event time per micro-batch globally, subtracts the delay threshold, and the watermark advances only at batch boundaries. With multiple input streams the global watermark is the <em>minimum</em> of the per-stream watermarks (the slowest stream gates eviction). You can change this to <code>max</code> via <code>spark.sql.streaming.multipleWatermarkPolicy=max</code>, at the risk of dropping more data from the lagging stream."},
  {"sec":"SPARK-STREAM","front":"The three output modes and what each emits","back":"<ul><li><strong>Append</strong> (the system default): only new, finalized rows are written and never changed — valid only when rows won't be updated later (e.g. windowed aggregations with a watermark, or stateless queries).</li><li><strong>Update</strong>: only rows that changed since the last batch are written — the natural mode for continuously-changing aggregations; pair it with an upsert-capable sink to avoid stale/duplicate rows downstream.</li><li><strong>Complete</strong>: the entire result table is rewritten every batch — requires an aggregation and retains all groups in state.</li></ul>"},
  {"sec":"SPARK-STREAM","front":"Why can't you use append mode on a streaming aggregation without a watermark?","back":"In append mode a row is emitted <em>once</em> and is immutable. An aggregation's result keeps changing as new rows arrive, so the engine can only emit a group's final value when it's certain no more input can affect it — that certainty comes from the <em>watermark</em> passing the window end. Without a watermark there's no finalization point, so Spark rejects append mode on aggregations."},
  {"sec":"SPARK-STREAM","front":"Which output modes do aggregation vs non-aggregation queries support?","back":"<ul><li><strong>Non-aggregation</strong> (map/filter/select): append and update; <em>not</em> complete (complete requires aggregation).</li><li><strong>Aggregation with watermark + event-time window</strong>: append, update, complete.</li><li><strong>Aggregation without watermark</strong>: update and complete only (state is unbounded).</li></ul>Complete mode always materializes all groups, so it only suits aggregations with bounded cardinality."},
  {"sec":"SPARK-STREAM","front":"What exactly is stored in a Structured Streaming checkpoint?","back":"The checkpoint directory holds: <code>offsets/</code> (the write-ahead log of source offsets planned for each batch), <code>commits/</code> (which batches completed), <code>sources/</code> metadata, <code>state/</code> (the keyed state store snapshots + deltas for stateful ops), and the query <code>metadata</code> (the stream's id). It must live on durable, atomically-renamable storage (HDFS/S3-with-care). Changing the query plan can make a checkpoint incompatible."},
  {"sec":"SPARK-STREAM","front":"How does Structured Streaming achieve end-to-end exactly-once?","back":"Three ingredients: (1) <strong>replayable sources</strong> that expose offsets and can re-read a range (Kafka, file source); (2) <strong>checkpointing with a WAL</strong> that records the exact offset range of each batch <em>before</em> processing; (3) <strong>idempotent or transactional sinks</strong> that key on batch id so a re-run of the same batch overwrites rather than duplicates. On failure the batch replays from the logged offsets and re-writes deterministically."},
  {"sec":"SPARK-STREAM","front":"The state store: what is it and how is it made fault-tolerant?","back":"Stateful operators (aggregations, dedup, stream-stream joins, <code>flatMapGroupsWithState</code>) keep keyed state in a <em>state store</em> partitioned by the grouping key (default partitions = <code>spark.sql.shuffle.partitions</code>, fixed at query start). Each batch writes a delta and periodic snapshots to the checkpoint <code>state/</code> dir; on recovery the store is rebuilt from snapshot+deltas. Default backend is in-memory+HDFS; RocksDB backend offloads state off-heap for large state."},
  {"sec":"SPARK-STREAM","front":"Why is spark.sql.shuffle.partitions effectively immutable for a stateful stream?","back":"State is hash-partitioned by key into N store partitions, and that N is baked into the checkpoint. Changing <code>spark.sql.shuffle.partitions</code> on restart would re-hash keys to different partitions, so they couldn't find their existing state — recovery would be wrong. You must pick it up front (sized for peak state), and you can't repartition state without state-store rescaling features or a fresh checkpoint."},
  {"sec":"SPARK-STREAM","front":"Stream-stream joins: why do both sides need watermarks + time constraints?","back":"To join two streams the engine must buffer rows from each side until a potential match could still arrive. Without bounds, both buffers grow forever. A <em>watermark on both sides</em> plus a <em>time-range join condition</em> (e.g. event times within an interval) lets Spark drop buffered rows past the watermark and emit/finalize joins. Outer joins additionally need the watermark to know when to emit unmatched (null-padded) rows."},
  {"sec":"SPARK-STREAM","front":"foreachBatch: what does it give you that built-in sinks don't?","back":"<code>foreachBatch((df, batchId) => …)</code> hands you each micro-batch as a normal (batch) DataFrame, so you can use batch-only sinks, write to multiple destinations, do upserts/MERGE, or call arbitrary APIs. To keep exactly-once you must make the write <em>idempotent using <code>batchId</code></em> (e.g. dedupe or transactionally commit per batch id), because <code>foreachBatch</code> can re-invoke the same batch id after failure."},
  {"sec":"SPARK-STREAM","front":"Dropping duplicates and bounding its state","back":"<code>dropDuplicates(cols)</code> keeps every seen key in state forever. Use <code>dropDuplicatesWithinWatermark</code> (3.5+) or include the event-time column under a watermark, so the dedup state is evicted once the watermark passes — otherwise the state store grows without bound and OOMs. The watermark turns unbounded dedup into a bounded sliding window of recent keys."},
  {"sec":"SPARK-STREAM","front":"State eviction timing: late data vs window finalization","back":"A windowed aggregation's state for a window is kept until <code>watermark > window_end</code> (plus allowed lateness already folded into the threshold). At that point, in append mode the result is emitted and the state is dropped; any input arriving afterward for that window is <em>silently discarded</em> as late. Picking the watermark threshold trades completeness (catch late data) against state size and output latency."}
);

Object.assign(GLOSSARY_DATA, {
  "unbounded-table-model": {"term":"Unbounded Table Model","group":"SPARK-STREAM","aliases":["unbounded table","result table","incremental query model"],"eli5":"Treat the never-ending stream as a table that keeps growing, and run an ordinary SQL query that the engine keeps re-running on just the new rows.","memory":"Stream = table that never stops growing; your query is just a SELECT the engine re-runs on the delta.","full":"<p>Structured Streaming's foundational abstraction: the input stream is an <em>unbounded input table</em>, your query produces a <em>result table</em>, and each trigger appends new input rows and incrementally updates the result. The same DataFrame/Dataset/SQL API works on both static and streaming data — the planner detects a streaming source and compiles an incremental execution plan instead of a one-shot one.</p><p>This is what lets you reason about correctness as if it were a batch query: the semantics are defined as \"what a batch query over all data seen so far would produce.\" The engine's job is to realize those semantics incrementally and fault-tolerantly via state stores, watermarks, and checkpoints, rather than asking you to hand-write streaming operators (as DStreams did).</p>","trap":"Candidates conflate it with the old DStream/RDD micro-batch API. Structured Streaming is a declarative DataFrame engine with Catalyst optimization and incrementalization; DStreams were imperative RDD transforms with no event-time/watermark support.","related":["micro-batch-execution","watermark","output-modes"]},
  "micro-batch-execution": {"term":"Micro-Batch Execution","group":"SPARK-STREAM","aliases":["micro-batch","MicroBatchExecution","continuous processing"],"eli5":"By default Spark fakes a stream by running a fast little batch job over each new chunk of data, again and again.","memory":"\"Micro-batch = many tiny batch jobs.\" Default = correctness (exactly-once); continuous = speed (at-least-once).","full":"<p>The default engine (<code>MicroBatchExecution</code>) discretizes the stream into a sequence of small deterministic batch jobs. Each batch: reads a committed offset range from the source, runs the incremental plan, updates state, writes to the sink, and commits. This gives exactly-once and full operator support (aggregations, joins, arbitrary stateful ops) at the cost of ~hundreds-of-ms latency floor driven by job-scheduling overhead and the trigger interval.</p><p><strong>Continuous processing</strong> (experimental, since 2.3) is the alternative engine: long-running tasks poll the source and write records with ~1ms latency, checkpointing offsets asynchronously. It only guarantees at-least-once and supports a narrow operator set (projection/selection/map-like, no shuffles, aggregations, or joins), so it's rarely used in practice.</p>","trap":"Saying continuous mode gives exactly-once. It does not — only at-least-once — and it can't do aggregations/joins, so it's a niche tool, not a drop-in latency upgrade.","related":["unbounded-table-model","trigger","checkpointing","exactly-once"]},
  "trigger": {"term":"Trigger","group":"SPARK-STREAM","aliases":["triggers","Trigger.AvailableNow","Trigger.Once","processingTime trigger"],"eli5":"The trigger is the clock that decides when Spark starts processing the next chunk.","memory":"Once = one big gulp; AvailableNow = many polite sips then stop; processingTime = metronome; default = as-fast-as-possible.","full":"<p>A trigger policy determines when each micro-batch fires. <strong>Default</strong>: as soon as the prior batch finishes (back-to-back). <strong><code>processingTime(\"N seconds\")</code></strong>: fixed wall-clock cadence; if a batch overruns the interval, the next fires immediately (no batch skipping, but cadence drifts). <strong><code>Trigger.Once</code></strong>: process all currently available data in a single batch, then stop — good for cost-efficient scheduled jobs but it ignores source rate limits (one giant batch). <strong><code>Trigger.AvailableNow</code></strong> (3.3+): same \"process everything then stop\" intent but splits work into multiple rate-limited batches, so it's the preferred replacement for <code>Once</code>. <strong><code>Trigger.Continuous(\"1 second\")</code></strong>: selects the continuous engine and sets its checkpoint interval.</p>","trap":"Recommending <code>Trigger.Once</code> for a large backlog — it can blow memory because it ignores <code>maxOffsetsPerTrigger</code>/<code>maxFilesPerTrigger</code>. Use <code>AvailableNow</code> (3.3+), which honors those limits across internal batches.","related":["micro-batch-execution","checkpointing"]},
  "event-time-window": {"term":"Event-Time Window","group":"SPARK-STREAM","aliases":["windowed aggregation","tumbling window","sliding window","session window"],"eli5":"Group events by the time they actually happened (a timestamp in the data), not when Spark received them.","memory":"Window by when-it-happened (event time), finalize when the watermark clears the window end.","full":"<p>Windowing aggregates by <em>event time</em> — a timestamp carried in the record — using <code>window(col(\"ts\"), \"10 minutes\")</code> for tumbling, an additional slide argument for sliding (overlapping) windows, or <code>session_window(...)</code> for gap-based sessions. Because grouping is by event time, out-of-order and late arrivals land in their correct window regardless of processing time, which is the whole point of event-time semantics.</p><p>Each window key holds running aggregation state in the state store. That state is retained until the watermark passes the window's end, then (in append mode) the final result is emitted and the state evicted. Sliding windows multiply state because each event belongs to multiple overlapping windows; session windows have variable length and merge adjacent events within the gap.</p>","trap":"Confusing event time with processing time. Tuning a watermark only makes sense for event-time windows; a processing-time grouping has no late data and no watermark.","related":["watermark","state-store","output-modes"]},
  "output-modes": {"term":"Output Modes","group":"SPARK-STREAM","aliases":["append mode","update mode","complete mode","outputMode"],"eli5":"Choose whether the sink gets only brand-new rows (append), only changed rows (update), or the whole answer every time (complete).","memory":"Append = new+final only; Update = the diff; Complete = full table every time (and unbounded state).","full":"<p><strong>Append</strong> (the system default) writes only rows that are final and will never change; legal for stateless queries and for watermarked windowed aggregations (which become final when the watermark clears the window). <strong>Update</strong> writes only rows whose value changed in this batch — ideal for upsert sinks and the natural mode for ongoing aggregations. <strong>Complete</strong> rewrites the entire result table every batch and therefore requires an aggregation and retains all groups in state forever (only safe for bounded-cardinality keys, e.g. a dashboard of per-country counts).</p><p>The legal mode depends on the query: complete needs an aggregation; append on an aggregation needs a watermark to define finality; non-aggregation queries can't use complete. The engine validates this at query start and throws if you pick an unsupported combination.</p>","trap":"Picking <em>update</em> mode with a non-idempotent sink (e.g. plain file append) — you'll get duplicate/partial rows. Update is meant for sinks that can upsert on a key. Also: complete mode silently keeps unbounded state, a memory time-bomb on high-cardinality aggregations.","related":["watermark","event-time-window","state-store"]},
  "stream-stream-join": {"term":"Stream-Stream Join","group":"SPARK-STREAM","aliases":["stream-stream join","streaming join","time-range join"],"eli5":"Joining two live streams: Spark buffers rows from each side and matches them within a time window so the buffers don't grow forever.","memory":"Two streams → two watermarks + a time-range predicate, or the buffers (and your heap) grow without bound.","full":"<p>Joining two streaming DataFrames requires buffering rows from each side in the state store until a match could still arrive. To keep that buffer bounded you must provide a <em>watermark on both inputs</em> and a <em>time-range condition</em> in the join (e.g. <code>rightTs BETWEEN leftTs AND leftTs + interval</code>). The engine then evicts buffered rows once the watermark guarantees no future row could match them.</p><p>Inner joins only need bounds to limit state. Outer joins (left/right/full) additionally rely on the watermark to decide <em>when</em> to emit unmatched rows with nulls — it can't declare \"no match\" until the watermark proves no match will ever come, so outer-join output for unmatched rows is delayed until that point.</p>","trap":"Expecting outer-join null rows immediately. They're emitted only after the watermark passes the join's time bound, so unmatched results lag. And missing a watermark on either side makes the join state unbounded.","related":["watermark","state-store","event-time-window"]}
});
