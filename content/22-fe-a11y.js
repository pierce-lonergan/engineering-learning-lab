/* Accessibility (a11y) (FE-A11Y) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"FE-A11Y","front":"What does POUR stand for, and what are the WCAG conformance levels?","back":"<strong>POUR</strong> = the four WCAG principles: <ul><li><strong>Perceivable</strong> — info presentable in ways users can perceive (text alternatives, captions, contrast)</li><li><strong>Operable</strong> — UI operable by all (keyboard, enough time, no seizure triggers)</li><li><strong>Understandable</strong> — readable, predictable, input assistance</li><li><strong>Robust</strong> — works with current/future assistive tech (valid markup, name/role/value)</li></ul>Levels: <strong>A</strong> (must), <strong>AA</strong> (the real-world legal/contractual target — ADA case law, EN 301 549, Section 508 all align here), <strong>AAA</strong> (enhanced; <em>not</em> required wholesale — WCAG itself says don't mandate AAA site-wide). <code>WCAG 2.1</code> (2018) added mobile/low-vision/cognitive SC; <code>2.2</code> (Oct 2023) added focus-not-obscured, dragging alternatives, target-size 24×24px, accessible authentication, and <strong>removed 4.1.1 Parsing</strong>."},
  {"sec":"FE-A11Y","front":"State the 5 Rules of ARIA.","back":"<ul><li><strong>1.</strong> <em>Don't use ARIA if a native HTML element/attribute gives you the semantics and behavior for free.</em> A real <code>&lt;button&gt;</code> beats <code>&lt;div role=\"button\"&gt;</code>.</li><li><strong>2.</strong> Don't change native semantics unless you must — never <code>&lt;h1 role=\"button\"&gt;</code>; wrap instead: <code>&lt;h1&gt;&lt;button&gt;…&lt;/button&gt;&lt;/h1&gt;</code>.</li><li><strong>3.</strong> All interactive ARIA controls must be <strong>keyboard-operable</strong> (focusable + key handlers).</li><li><strong>4.</strong> Don't put <code>role=\"presentation\"</code>/<code>aria-hidden=\"true\"</code> on a <em>focusable</em> element — you create a focusable phantom screen readers skip.</li><li><strong>5.</strong> Every interactive element needs an <strong>accessible name</strong>.</li></ul><em>ARIA changes how the a11y tree is read — it adds NO behavior. <code>role=\"button\"</code> doesn't make Enter/Space work; you write that JS yourself.</em>"},
  {"sec":"FE-A11Y","front":"How is an element's accessible name computed? Give the precedence order.","back":"Per the <strong>Accessible Name and Description Computation</strong> (accname spec), in order: <ul><li><strong>1.</strong> <code>aria-labelledby</code> (concatenates referenced elements' text) — wins over everything</li><li><strong>2.</strong> <code>aria-label</code></li><li><strong>3.</strong> host-language / native labeling: <code>&lt;label for&gt;</code> (and wrapping label), <code>&lt;legend&gt;</code>, <code>&lt;img alt&gt;</code>, <code>&lt;caption&gt;</code></li><li><strong>4.</strong> element's own text content / subtree (for buttons, links, headings, cells)</li><li><strong>5.</strong> <code>title</code> attribute (last-resort fallback only)</li></ul><strong>Gotchas:</strong> <code>aria-labelledby</code> can reference <em>hidden</em> elements and overrides visible text — a sighted user and SR user can hear different things. <code>aria-label</code> is silently ignored on non-interactive generic elements (<code>span</code>, <code>div</code> with no role). The <strong>description</strong> (<code>aria-describedby</code>, or fallback <code>title</code>) is a separate string announced <em>after</em> the name."},
  {"sec":"FE-A11Y","front":"tabindex: explain 0, -1, and why positive values are an anti-pattern.","back":"<ul><li><code>tabindex=\"0\"</code> — inserts a non-natively-focusable element into the <strong>natural DOM tab order</strong>. Use for custom widgets (<code>div role=\"button\"</code>).</li><li><code>tabindex=\"-1\"</code> — <strong>programmatically focusable</strong> (<code>.focus()</code>) but NOT in tab order. Essential for <em>roving tabindex</em>, moving focus to a dialog/heading, or focusing error summaries.</li><li><code>tabindex=\"1\"+</code> (positive) — <strong>anti-pattern</strong>. Creates a separate priority tab sequence that jumps ahead of all <code>0</code>/native elements, decoupling tab order from visual/DOM order. Unmaintainable, and one stray positive value poisons the whole page's order.</li></ul><strong>Rule:</strong> fix tab order by <em>fixing DOM order</em> (or avoiding CSS that reorders interactive content, like flex/grid <code>order</code> or <code>flex-direction: row-reverse</code>), never with positive tabindex."},
  {"sec":"FE-A11Y","front":"What is a focus trap and how do you implement an accessible modal dialog?","back":"A <strong>focus trap</strong> confines keyboard focus inside an open modal so Tab/Shift+Tab cycle within it and never reach the inert background. Implementation: <ul><li>On open: save the <strong>previously focused element</strong>, then move focus into the dialog (first focusable element, or the dialog/heading via <code>tabindex=\"-1\"</code>).</li><li>Wrap Tab on the last focusable → first, Shift+Tab on first → last.</li><li><code>Escape</code> closes; mark the background <code>inert</code> so neither Tab nor the SR virtual cursor can reach it.</li><li>On close: <strong>restore focus</strong> to the saved trigger element.</li><li>Markup: <code>role=\"dialog\"</code> (or <code>alertdialog</code>) + <code>aria-modal=\"true\"</code> + <code>aria-labelledby</code> pointing at the title.</li></ul><strong>2026 note:</strong> native <code>&lt;dialog&gt;.showModal()</code> gives the trap, top layer, an inert backdrop, and Esc for free — prefer it. The <code>inert</code> attribute is now Baseline across evergreen browsers. <em>Note WCAG 2.4.3 Focus Order governs <strong>logical</strong> order, not trapping per se; a trap with no Esc/close would itself fail 2.1.2 No Keyboard Trap.</em>"},
  {"sec":"FE-A11Y","front":"Why must focus be visible, and what changed in WCAG 2.2 around focus?","back":"Keyboard and switch users navigate by a <strong>visible focus indicator</strong>; removing it (<code>outline: none</code> with no replacement) is a WCAG <strong>2.4.7 Focus Visible (AA)</strong> failure and strands users. <strong>Never</strong> blanket-remove outlines — replace with a clear custom indicator and use <code>:focus-visible</code> so the ring shows for keyboard but not mouse clicks. WCAG 2.2 added: <ul><li><strong>2.4.11 Focus Not Obscured (Minimum) — AA</strong>: the focused element must not be <em>entirely</em> hidden by sticky headers/cookie banners (2.4.12 Enhanced, which requires it be fully visible, is AAA).</li><li><strong>2.4.13 Focus Appearance — AA</strong>: minimum size/contrast of the indicator (shipped as AA in 2.2, despite being AAA in an early draft).</li></ul><strong>Gotcha:</strong> <code>:focus-visible</code> heuristics are browser-driven; for custom widgets you <code>.focus()</code> programmatically, you often still want a real visible ring, so don't assume <code>:focus-visible</code> always fires."},
  {"sec":"FE-A11Y","front":"Skip links and landmarks: what problem do they solve?","back":"<strong>Skip links</strong> (\"Skip to main content\") are the first focusable element, visually hidden until focused (<em>not</em> <code>display:none</code>/<code>visibility:hidden</code>, which remove from tab order — use a clip/off-screen technique that becomes visible on <code>:focus</code>). They let keyboard/SR users bypass repeated nav blocks (WCAG <strong>2.4.1 Bypass Blocks, A</strong>). The target needs <code>tabindex=\"-1\"</code> so focus actually lands and SR reading resumes there. <strong>Landmarks</strong> — <code>&lt;header&gt;</code>(banner), <code>&lt;nav&gt;</code>(navigation), <code>&lt;main&gt;</code>(main), <code>&lt;aside&gt;</code>(complementary), <code>&lt;footer&gt;</code>(contentinfo), <code>&lt;section aria-label&gt;</code>(region) — let SR users jump between page regions via landmark navigation (rotor / D key). <strong>Gotchas:</strong> <code>banner</code>/<code>contentinfo</code> map only when <code>&lt;header&gt;</code>/<code>&lt;footer&gt;</code> are page-level (not nested in <code>&lt;article&gt;</code>/<code>&lt;section&gt;</code>); one <code>&lt;main&gt;</code> per page; multiple <code>&lt;nav&gt;</code>s each need a distinguishing <code>aria-label</code>; don't over-label (<code>aria-label=\"navigation\"</code> on a <code>&lt;nav&gt;</code> is redundant — the role already says \"navigation\")."},
  {"sec":"FE-A11Y","front":"What is the accessibility tree and how does it relate to the DOM?","back":"The browser builds the <strong>accessibility tree</strong> from the DOM + CSS — a parallel structure of nodes each exposing <strong>role, name, state, value</strong> — and exposes it to platform a11y APIs (UIA/IAccessible2 on Windows, AX on macOS, AT-SPI on Linux), which screen readers consume. Key transforms: <ul><li><code>display:none</code> / <code>visibility:hidden</code> / <code>aria-hidden=\"true\"</code> → node <strong>pruned</strong> (and its descendants for aria-hidden).</li><li>ARIA roles/states override the native role/state mapping.</li><li>Generic <code>div</code>/<code>span</code> with no role collapse to nothing meaningful.</li><li>Visual-only CSS (pseudo-element <code>content</code>) is only inconsistently exposed — don't rely on it for meaning.</li></ul><strong>Gotcha:</strong> CSS can silently break semantics — e.g. <code>display:contents</code> historically dropped the element's box <em>and</em> its semantics from the tree (now fixed in modern engines for most elements), and flex/grid <code>order</code> reordering desyncs visual order from DOM order (which drives tab order)."},
  {"sec":"FE-A11Y","front":"Color contrast: what are the WCAG 2.x thresholds and the common traps?","back":"WCAG <strong>1.4.3 Contrast (Minimum) — AA</strong>: <ul><li><strong>4.5:1</strong> for normal text</li><li><strong>3:1</strong> for <em>large text</em> (≥18pt ≈24px, or ≥14pt ≈18.66px bold)</li></ul><strong>1.4.11 Non-text Contrast — AA</strong>: <strong>3:1</strong> for UI component boundaries (input borders, focus rings) and meaningful graphics. AAA (1.4.6) raises text to <strong>7:1</strong> (4.5:1 large). <strong>Traps:</strong> <ul><li>Contrast is computed from <strong>relative luminance</strong>, not perceived brightness — yellow-on-white fails despite looking different.</li><li>Text over images/gradients must meet contrast at the worst pixel.</li><li><code>opacity</code> and semi-transparent overlays change effective contrast — test the composited color.</li><li>Placeholder text is real text and must pass.</li><li>Disabled controls and pure decoration are exempt.</li></ul><em>WCAG 3 is exploring <strong>APCA</strong> (perceptual, polarity-aware) as a future replacement, but as of 2026 AA still uses the 2.x luminance ratio.</em>"},
  {"sec":"FE-A11Y","front":"ARIA live regions: polite vs assertive, and the activation gotcha.","back":"<strong>Live regions</strong> announce dynamic content changes without moving focus. <ul><li><code>aria-live=\"polite\"</code> — waits until the SR is idle. Use for status, search-results-updated, autosave. <code>role=\"status\"</code> is an implicit polite region (also <code>aria-atomic=\"true\"</code> by default).</li><li><code>aria-live=\"assertive\"</code> — interrupts immediately. Reserve for errors/urgent alerts. <code>role=\"alert\"</code> is an implicit assertive + atomic region.</li></ul>Companions: <code>aria-atomic=\"true\"</code> reads the whole region not just the diff; <code>aria-relevant</code> controls which mutation types announce. <strong>Critical gotcha:</strong> the live region container <em>must exist in the DOM (empty) before</em> you inject text — SRs register the region when it renders. Creating the element <em>and</em> its text in the same tick is frequently missed. Don't toggle <code>display:none</code>; inject text into a persistent region. Avoid spamming assertive — interruptions are hostile."},
  {"sec":"FE-A11Y","front":"Accessible forms: label association, error messaging, and required-field signaling.","back":"<ul><li><strong>Label:</strong> every control needs a programmatic name. Prefer <code>&lt;label for=\"id\"&gt;</code> (or wrapping). Placeholder is <strong>not</strong> a label — it vanishes on input and often fails contrast.</li><li><strong>Errors:</strong> set <code>aria-invalid=\"true\"</code> on the field and link the message with <code>aria-describedby=\"err-id\"</code> so the SR reads the error <em>as part of the field</em>. Don't rely on color alone (1.4.1 Use of Color).</li><li><strong>Error summary:</strong> on submit, render a focusable summary (link list) and move focus to it; activating an item focuses the field.</li><li><strong>Required:</strong> use <code>required</code>/<code>aria-required=\"true\"</code>; signal it with more than asterisk-color alone.</li><li>Group radios/checkboxes in <code>&lt;fieldset&gt;</code>+<code>&lt;legend&gt;</code>.</li></ul><strong>Gotcha:</strong> dynamically appearing errors should be in a live region (<code>role=\"alert\"</code>) <em>or</em> reached via focus — otherwise an SR user submits and hears nothing. Don't put the error in <code>aria-describedby</code> only and never announce it on a no-page-reload submit."},
  {"sec":"FE-A11Y","front":"alt text decision tree: when is alt=\"\", a description, or no img right?","back":"<ul><li><strong>Informative image</strong> (conveys content): concise <code>alt</code> describing the <em>information</em>, not \"image of\". A chart needs the data/trend, with a long description nearby.</li><li><strong>Functional image</strong> (inside a link/button): alt describes the <em>action/destination</em> (\"Search\"), not the picture.</li><li><strong>Decorative</strong> (eye-candy, redundant to adjacent text): <code>alt=\"\"</code> (empty, present) so SRs skip it. A <em>missing</em> alt is different — SRs may read the filename/URL.</li><li><strong>Redundant text in image</strong> (e.g. logo with company name): alt = the text.</li><li><strong>Complex</strong> (infographic): short alt + long description via adjacent visible text or <code>aria-describedby</code>.</li><li>CSS background images convey nothing to SRs — only use for true decoration.</li></ul><strong>Gotcha:</strong> <code>alt=\"\"</code> is the canonical \"decorative\" idiom (<code>role=\"presentation\"</code>/<code>role=\"none\"</code> on an <code>&lt;img&gt;</code> also works); but an empty alt on a <em>functional</em> linked image leaves the link with no accessible name."},
  {"sec":"FE-A11Y","front":"axe / automated testing vs manual testing — what can each actually catch?","back":"<strong>Automated tools</strong> (<code>axe-core</code>, Lighthouse, Pa11y, the <code>jest-axe</code>/Playwright integrations) catch roughly <strong>30–40%</strong> of WCAG issues: missing alt, missing labels, contrast (for solid colors), invalid/abstract ARIA, duplicate IDs, missing <code>lang</code>. They are <em>deterministic and cheap</em> — wire them into CI per-component. <strong>They cannot judge:</strong> <ul><li>whether alt text is <em>meaningful</em></li><li>logical reading/tab order and focus management</li><li>whether a live region actually announces</li><li>keyboard operability of custom widgets</li><li>whether the visible focus indicator is adequate</li><li>cognitive load / sensible error recovery</li></ul><strong>Senior answer:</strong> automated = a regression net for the easy ~40%; the rest requires <em>manual keyboard-only testing</em> and a <em>real screen reader</em> (NVDA+Firefox/Chrome, VoiceOver+Safari, JAWS+Chrome). \"axe passes\" ≠ accessible."},
  {"sec":"FE-A11Y","front":"Screen readers: name the major ones and the virtual cursor / forms mode concept.","back":"Major SRs: <strong>NVDA</strong> (free, Windows, pairs with Firefox/Chrome), <strong>JAWS</strong> (paid, Windows, enterprise default), <strong>VoiceOver</strong> (macOS/iOS, built-in, pairs with Safari), <strong>TalkBack</strong> (Android), <strong>Narrator</strong> (Windows). <strong>Browse/virtual-cursor mode</strong> (NVDA/JAWS): the SR builds a virtual buffer of the page; arrow keys read linearly and single keys (H=headings, K=links, D=landmarks, F=form fields) jump by element type — keystrokes are <em>intercepted</em> by the SR. <strong>Focus/forms/applications mode</strong>: when focus enters an editable field or a <code>role=\"application\"</code> widget, the SR <em>passes keystrokes through</em> to the control so you can type/use arrow keys. (VoiceOver has no separate forms mode — it uses the VO cursor + Quick Nav instead.) <strong>Senior gotcha:</strong> custom widgets must drive this — a custom combobox/grid needs correct roles so the SR auto-switches modes; wrong roles leave users stuck in browse mode unable to operate it."},
  {"sec":"FE-A11Y","front":"prefers-reduced-motion: what is it and how do you honor it correctly?","back":"<code>@media (prefers-reduced-motion: reduce)</code> is a CSS media query reflecting an OS-level \"reduce motion\" setting. It serves WCAG <strong>2.3.3 Animation from Interactions (AAA)</strong> and protects users with vestibular disorders from large parallax/zoom/slide motion that can cause nausea. <strong>Correct pattern:</strong> treat motion as an <em>enhancement</em> — default to minimal motion, or gate large-motion animations so they're disabled/replaced with a cross-fade under <code>reduce</code>: <code>@media (prefers-reduced-motion: reduce){ *{animation-duration:.01ms!important; transition-duration:.01ms!important;} }</code> as a blunt fallback. <strong>Gotchas:</strong> don't kill <em>all</em> feedback (a subtle opacity fade is fine and aids comprehension); JS animations (<code>requestAnimationFrame</code>, GSAP, Web Animations API) must check <code>window.matchMedia('(prefers-reduced-motion: reduce)').matches</code> and listen for changes — CSS-only handling misses them. Also relevant: <strong>2.2.2 Pause, Stop, Hide (A)</strong> for auto-playing motion >5s, and <strong>2.3.1 Three Flashes (A)</strong> for seizure safety."},
  {"sec":"FE-A11Y","front":"\"It works with a mouse but a keyboard user is stuck.\" Diagnose the likely causes.","back":"Senior triage for keyboard inoperability: <ul><li><strong>Non-native interactive element</strong> — a <code>&lt;div&gt;</code>/<code>&lt;span&gt;</code> with a click handler but no <code>tabindex=\"0\"</code>, no <code>role</code>, no Enter/Space handler. Fix: use a real <code>&lt;button&gt;</code>.</li><li><strong>Click-only handlers</strong> — <code>onClick</code> without keydown handling on custom widgets (real buttons get key→click synthesis for free; divs don't).</li><li><strong>Focus lost on DOM update</strong> — removing the focused node (route change, modal close) drops focus to <code>&lt;body&gt;</code>; the user is teleported to the top. Manage focus on transitions.</li><li><strong>Hidden-but-focusable</strong> — a visually hidden menu still in tab order, or <code>aria-hidden</code> on a focusable element.</li><li><strong>Keyboard trap</strong> — a custom modal that traps but never wires Esc/close (a 2.1.2 failure).</li><li><strong>Positive tabindex</strong> scrambling order.</li><li><strong>Custom dropdown/menu</strong> missing arrow-key roving tabindex per the WAI-ARIA APG pattern.</li></ul>"},
  {"sec":"FE-A11Y","front":"Roving tabindex vs aria-activedescendant for composite widgets.","back":"Composite widgets (menu, toolbar, listbox, grid, tabs) should be <strong>a single tab stop</strong>, with arrow keys moving an internal selection. Two patterns from the WAI-ARIA APG: <ul><li><strong>Roving tabindex:</strong> exactly one child has <code>tabindex=\"0\"</code> (the active one), all others <code>tabindex=\"-1\"</code>. On arrow press you move the <code>0</code> and call <code>.focus()</code> on the new active child. DOM focus genuinely moves. Simple, robust, works everywhere.</li><li><strong>aria-activedescendant:</strong> the <em>container</em> keeps DOM focus and carries <code>aria-activedescendant=\"id-of-active-option\"</code>, which you update on arrow keys. The visual/SR \"active\" item moves without real focus moving. Good for editable comboboxes (the input keeps focus while you arrow through options).</li></ul><strong>Tradeoff:</strong> roving moves real focus (CSS <code>:focus</code> works, but you must manage it on every nav); activedescendant keeps one focus target but the active option must be a descendant of the focused element (or its <code>aria-owns</code> subtree) and you style it manually. Both leave Tab to exit the whole widget."}
);

Object.assign(GLOSSARY_DATA, {
  "pour": {"term":"POUR (WCAG principles)","group":"FE-A11Y","eli5":"The four big ideas behind WCAG: content must be Perceivable, Operable, Understandable, and Robust.","memory":"POUR your accessibility into the page.","full":"<p><strong>POUR</strong> organizes all WCAG success criteria under four principles: <strong>Perceivable</strong> (users can sense the content — text alternatives, captions, contrast), <strong>Operable</strong> (they can drive the UI — keyboard, enough time, no seizure triggers), <strong>Understandable</strong> (predictable behavior, readable text, input help), and <strong>Robust</strong> (parses and exposes name/role/value to current and future assistive tech).</p><p>Conformance is graded <strong>A / AA / AAA</strong>; <strong>AA</strong> is the de-facto legal and contractual target worldwide (ADA case law, Section 508, EN 301 549).</p>","trap":"Thinking AAA is the goal — WCAG itself says don't require AAA across a whole site; AA is the target and AAA is selective enhancement.","worked":"1.1.1 (alt text)=Perceivable; 2.1.1 (keyboard)=Operable; 3.3.1 (error identification)=Understandable; 4.1.2 (name/role/value)=Robust.","related":["wcag-levels","name-role-value"]},
  "wcag-levels": {"term":"WCAG levels A / AA / AAA & versions","group":"FE-A11Y","eli5":"Three strictness tiers of accessibility rules, with 2.1 and 2.2 adding newer criteria.","memory":"A = essential, AA = the law, AAA = aspirational.","full":"<p>Each WCAG success criterion is tagged <strong>A</strong> (minimum, blocking), <strong>AA</strong> (the standard real target), or <strong>AAA</strong> (enhanced, situational). Legal frameworks reference <strong>AA</strong>.</p><p><strong>2.1</strong> (2018) added mobile, low-vision and cognitive criteria; <strong>2.2</strong> (Oct 2023) added focus-not-obscured, dragging alternatives, target size 24×24, accessible authentication, and notably <em>removed</em> 4.1.1 Parsing.</p>","trap":"Believing 4.1.1 Parsing (valid HTML) is still a conformance requirement — it was removed in WCAG 2.2 because browsers and AT handle malformed markup robustly.","worked":"Target Size (Minimum) 2.5.8 is AA in 2.2 → tap targets must be ≥24×24 CSS px (or have adequate spacing/exceptions).","related":["pour"]},
  "aria-rules": {"term":"The 5 Rules of ARIA","group":"FE-A11Y","eli5":"Guidelines that boil down to: use real HTML first, and ARIA only changes what screen readers hear, not how things behave.","memory":"\"No ARIA is better than bad ARIA.\"","full":"<p>The five rules: (1) prefer a native element; (2) don't override native semantics; (3) all interactive ARIA must be keyboard-operable; (4) don't hide focusable elements with <code>role=presentation</code>/<code>aria-hidden</code>; (5) every interactive element needs an accessible name.</p><p>The deepest point: <strong>ARIA adds zero behavior</strong>. <code>role=\"button\"</code> on a div changes the announced role but provides no focusability, no Enter/Space activation, no click — you supply all of it in JS. Misused ARIA actively misleads users, hence \"no ARIA is better than bad ARIA.\"</p>","trap":"Assuming adding role=\"button\" or aria-expanded makes a div behave like a button or actually expand something — ARIA never wires behavior.","worked":"<code>&lt;div role=\"button\" tabindex=\"0\"&gt;</code> still needs <code>keydown</code> handling for Enter and Space to fire the action.","related":["accessible-name","accessibility-tree","name-role-value"]},
  "accessible-name": {"term":"Accessible name computation","group":"FE-A11Y","eli5":"The single label a screen reader announces for a control, picked by a strict priority order.","memory":"Labelledby ▸ label ▸ native ▸ text ▸ title.","full":"<p>Defined by the <strong>accname</strong> spec, name resolution precedence is: <code>aria-labelledby</code> → <code>aria-label</code> → host-language labeling (<code>&lt;label for&gt;</code>, <code>img alt</code>, <code>&lt;legend&gt;</code>, <code>&lt;caption&gt;</code>) → element text content/subtree → <code>title</code> (last-resort fallback).</p><p><code>aria-labelledby</code> can reference multiple (even hidden) elements and concatenates them, overriding visible text — powerful but a source of name/visible-text mismatches. The <em>description</em> (<code>aria-describedby</code>) is a separate string announced after the name.</p>","trap":"Putting aria-label on a plain <span> or <div> (no role) and expecting it to be announced — it's ignored on generic non-interactive elements.","worked":"<code>&lt;button aria-label=\"Close\"&gt;&lt;svg.../&gt;&lt;/button&gt;</code> → name is \"Close\" even though the button has only an icon.","related":["aria-rules","aria-describedby","name-role-value"]},
  "name-role-value": {"term":"Name, Role, Value (WCAG 4.1.2)","group":"FE-A11Y","eli5":"Every UI control must tell assistive tech what it is, what it's called, and its current state.","memory":"Robust = expose Name/Role/Value.","full":"<p>WCAG <strong>4.1.2</strong> requires every interactive component to expose a <strong>role</strong> (button, checkbox…), an <strong>accessible name</strong>, and its <strong>states/values</strong> (<code>aria-checked</code>, <code>aria-expanded</code>, <code>aria-selected</code>, <code>value</code>) — and to notify when they change.</p><p>Native elements provide all three for free; custom widgets must replicate them with ARIA <em>and</em> keep state attributes in sync with the visual state on every interaction.</p>","trap":"Setting aria-expanded once and never updating it as the disclosure toggles — stale state is worse than none.","worked":"A custom toggle: <code>&lt;button role=\"switch\" aria-checked=\"false\"&gt;</code> → flip <code>aria-checked</code> to \"true\" in the click handler.","related":["accessible-name","accessibility-tree","live-region"]},
  "focus-management": {"term":"Focus management & focus trap","group":"FE-A11Y","eli5":"Deliberately moving and restoring keyboard focus so users don't get lost when the UI changes.","memory":"Move in, trap, Esc, restore out.","full":"<p>When UI appears/disappears (modals, route changes, expanding panels), you must <strong>move focus</strong> to the new content and <strong>restore</strong> it afterward, or keyboard/SR users get stranded at <code>&lt;body&gt;</code>. A <strong>focus trap</strong> cycles Tab within an open modal and marks the rest of the page <code>inert</code>.</p><p>Save the trigger element on open, move focus into the dialog, loop Tab at the edges, close on Esc, and return focus to the saved trigger on close. Native <code>&lt;dialog&gt;.showModal()</code> handles most of this. A trap with no Esc/close path is itself a WCAG 2.1.2 keyboard-trap failure.</p>","trap":"Trapping focus but forgetting to restore it to the trigger on close — the user resumes from the top of the page.","worked":"On modal close: <code>previouslyFocused?.focus()</code> where <code>previouslyFocused = document.activeElement</code> captured at open time.","related":["focus-visible","skip-link","tabindex"]},
  "focus-visible": {"term":":focus-visible & visible focus","group":"FE-A11Y","eli5":"A clear outline showing where keyboard focus is, shown for keyboard users but not on mouse clicks.","memory":"Never `outline:none` without a replacement.","full":"<p>WCAG <strong>2.4.7 (AA)</strong> requires a visible focus indicator. The CSS <code>:focus-visible</code> pseudo-class lets the browser apply the ring only when focus came from the keyboard (heuristic), avoiding the old habit of removing outlines because they showed on mouse click.</p><p>WCAG 2.2 strengthened this with <strong>2.4.11 Focus Not Obscured (Minimum, AA)</strong> — sticky headers must not fully cover the focused element — and <strong>2.4.13 Focus Appearance (AA)</strong> — minimum indicator size/contrast.</p>","trap":"Global `*:focus{outline:none}` to 'clean up' the design — it silently breaks keyboard navigation for everyone.","worked":"<code>:focus-visible{ outline:2px solid #1a73e8; outline-offset:2px; }</code> plus <code>:focus:not(:focus-visible){ outline:none; }</code> for mouse.","related":["focus-management","tabindex","skip-link"]},
  "tabindex": {"term":"tabindex (0 / -1 / positive)","group":"FE-A11Y","eli5":"Controls whether and how an element can receive keyboard focus.","memory":"0 = add to order, -1 = focus by script only, positive = never.","full":"<p><code>tabindex=\"0\"</code> adds a non-focusable element to the natural tab order; <code>tabindex=\"-1\"</code> makes it focusable only via <code>.focus()</code> (for roving tabindex, error summaries, moving focus to headings/dialogs); positive values create a separate priority order and are an anti-pattern.</p><p>Fix tab order by fixing DOM order, never with positive tabindex — one positive value reorders the whole page unpredictably.</p>","trap":"Using tabindex='1','2','3' to control order — it decouples tab order from DOM/visual order and is unmaintainable.","worked":"Roving tabindex in a toolbar: active button <code>tabindex=\"0\"</code>, the rest <code>tabindex=\"-1\"</code>; arrow keys move the 0 and call <code>.focus()</code>.","related":["focus-management","focus-visible","aria-rules"]},
  "live-region": {"term":"ARIA live region (polite / assertive)","group":"FE-A11Y","eli5":"A page area that announces its content changes to screen readers without moving focus.","memory":"Polite waits, assertive interrupts; create the region empty first.","full":"<p><code>aria-live=\"polite\"</code> (also <code>role=\"status\"</code>) announces when the screen reader is idle — for status/results/autosave. <code>aria-live=\"assertive\"</code> (also <code>role=\"alert\"</code>) interrupts immediately — reserve for errors. <code>aria-atomic</code> reads the whole region; <code>aria-relevant</code> filters which mutations announce. Both <code>status</code> and <code>alert</code> default to <code>aria-atomic=\"true\"</code>.</p><p>The region container must exist in the DOM <em>before</em> you inject text, or the announcement is commonly missed; inject into a persistent region rather than creating element + text together.</p>","trap":"Creating the live region and its message in the same render tick — screen readers register the region first, so the first message is frequently dropped.","worked":"Persistent <code>&lt;div aria-live=\"polite\" class=\"sr-only\"&gt;&lt;/div&gt;</code> in the DOM; later set <code>.textContent = '3 results found'</code> to announce.","related":["aria-describedby","name-role-value"]},
  "aria-describedby": {"term":"aria-describedby & accessible forms","group":"FE-A11Y","eli5":"Links a form field to its hint or error text so the screen reader reads them together.","memory":"Name = what it is; describedby = extra detail/error.","full":"<p><code>aria-describedby</code> points a control at the IDs of elements providing supplementary text (hints, error messages), announced <em>after</em> the accessible name. For errors, pair it with <code>aria-invalid=\"true\"</code> so the SR reads the field as invalid and speaks the linked message.</p><p>Combined with proper <code>&lt;label&gt;</code> association, <code>required</code>/<code>aria-required</code>, and <code>&lt;fieldset&gt;&lt;legend&gt;</code> grouping, this makes forms operable and recoverable for SR users.</p>","trap":"Using a placeholder as the field's label — it disappears on input, often fails contrast, and isn't a reliable accessible name.","worked":"<code>&lt;input aria-invalid=\"true\" aria-describedby=\"email-err\"&gt; &lt;span id=\"email-err\"&gt;Enter a valid email&lt;/span&gt;</code>","related":["accessible-name","live-region","name-role-value"]},
  "skip-link": {"term":"Skip link & landmarks","group":"FE-A11Y","eli5":"A 'skip to main content' link and HTML regions that let keyboard/SR users jump past repeated navigation.","memory":"First focusable link + <main> target with tabindex=-1.","full":"<p>A <strong>skip link</strong> is the first focusable element, visually hidden until focused, satisfying WCAG <strong>2.4.1 Bypass Blocks (A)</strong>. Its target (usually <code>&lt;main&gt;</code>) needs <code>tabindex=\"-1\"</code> so focus lands and SR reading resumes there.</p><p><strong>Landmarks</strong> (<code>header</code>/banner, <code>nav</code>, <code>main</code>, <code>aside</code>/complementary, <code>footer</code>/contentinfo) let SR users jump between page regions; banner/contentinfo only map when header/footer are page-level, and multiple same-type landmarks need distinguishing <code>aria-label</code>s.</p>","trap":"Hiding the skip link with display:none (removes it from tab order entirely) instead of an off-screen technique that becomes visible on :focus.","worked":"<code>&lt;a href=\"#main\" class=\"skip\"&gt;Skip to content&lt;/a&gt; … &lt;main id=\"main\" tabindex=\"-1\"&gt;</code>","related":["focus-management","focus-visible","tabindex"]}
});
