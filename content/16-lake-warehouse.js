/* Cloud Data Warehouses (Snowflake) (LAKE-WAREHOUSE) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"LAKE-WAREHOUSE","front":"Snowflake's three decoupled architecture layers","back":"Snowflake splits into three independently scalable layers: <strong>(1) cloud storage</strong> — columnar micro-partitions in S3/GCS/Azure Blob; <strong>(2) compute</strong> — independent virtual warehouses (MPP clusters) that read from storage; <strong>(3) cloud services</strong> — the always-on brain handling metadata, query optimization, transactions, security, and the result cache. The key win is that storage and compute scale and bill separately, and the services layer is shared/multi-tenant."},
  {"sec":"LAKE-WAREHOUSE","front":"Why does decoupling storage and compute matter (vs. shared-nothing like classic Redshift)?","back":"In a classic shared-nothing MPP (original Redshift dense-compute nodes), data is sharded onto each node's local disk, so storage and compute scale together — adding compute forces a redistribution/resize, and concurrent workloads fight over the same nodes. Decoupling stores data once in object storage and lets <strong>many virtual warehouses</strong> read the same dataset concurrently with zero contention. You scale compute up/down without moving data, and pay for storage independently of how much compute you run."},
  {"sec":"LAKE-WAREHOUSE","front":"What is a Snowflake micro-partition?","back":"The immutable storage unit Snowflake writes data into: contiguous columnar files holding ~<strong>50–500 MB of <em>uncompressed</em> data</strong> (the on-disk compressed file is far smaller — roughly up to ~16 MB), typically tens of thousands of rows. They are created automatically (you don't manage them), stored compressed and columnar, and each carries metadata (per-column min/max, distinct counts, nulls) used for pruning. Because they're immutable, updates/deletes write new micro-partitions — which is exactly what makes Time Travel and zero-copy cloning cheap."},
  {"sec":"LAKE-WAREHOUSE","front":"How does micro-partition pruning work?","back":"The cloud services layer keeps per-column min/max (and other) metadata for every micro-partition. At query time it evaluates predicates against that metadata and <strong>skips entire micro-partitions</strong> that cannot contain matching rows, so the warehouse never reads them. This is metadata-driven I/O elimination — analogous to Parquet row-group/column statistics pruning, but applied automatically across the whole table at the service layer rather than per-file."},
  {"sec":"LAKE-WAREHOUSE","front":"Automatic clustering and the clustering key","back":"Pruning is only effective when data is physically co-located by the columns you filter on. Defining a <strong>clustering key</strong> tells Snowflake the desired sort order; the <strong>automatic clustering</strong> background service then continuously re-sorts micro-partitions to maintain it as data changes — no manual <code>VACUUM</code>/re-sort. It's a serverless, credit-billed background process. Use it on large tables where the natural ingestion order doesn't match the filter columns; it's overkill (and costly) on small or well-ordered tables."},
  {"sec":"LAKE-WAREHOUSE","front":"What is a virtual warehouse, and scale-up vs. scale-out?","back":"A virtual warehouse is a named, on-demand MPP compute cluster (T-shirt sizes XS→6XL; each step ~doubles credits/hr and node count). <strong>Scale up</strong> = bigger size for heavier single queries (more CPU/RAM/local SSD cache). <strong>Scale out</strong> = a multi-cluster warehouse (Enterprise Edition+) that spins up additional same-size clusters to absorb <em>concurrency</em> (more simultaneous queries), then suspends them when load drops. Up for query complexity, out for query count."},
  {"sec":"LAKE-WAREHOUSE","front":"Snowflake's per-second billing and auto-suspend","back":"Warehouses bill compute credits <strong>per second with a 60-second minimum</strong> each time the warehouse resumes/starts. <code>AUTO_SUSPEND</code> idles the warehouse after N seconds of inactivity (you stop paying for compute); <code>AUTO_RESUME</code> restarts it on the next query. Storage is billed separately (flat per-TB/month on compressed bytes). The cloud services layer is free up to 10% of daily warehouse compute credits, billed only above that."},
  {"sec":"LAKE-WAREHOUSE","front":"Three cache layers in Snowflake","back":"<ul><li><strong>Result cache</strong> (services layer): identical query + unchanged underlying data returns the stored result with no warehouse needed; valid ~24h, reset on each reuse up to a 31-day max.</li><li><strong>Local disk / warehouse cache</strong>: micro-partition data cached on the warehouse's local SSD; lost on suspend/resize.</li><li><strong>Metadata cache</strong>: min/max/row-count stats let some queries (e.g. <code>COUNT(*)</code>, <code>MIN</code>/<code>MAX</code> on numeric/date columns) answer from metadata alone, no warehouse.</li></ul>"},
  {"sec":"LAKE-WAREHOUSE","front":"What is zero-copy cloning and why is it instant?","back":"<code>CREATE TABLE t_clone CLONE t</code> (also works on schemas/databases) creates a new named object that initially <strong>shares the same underlying micro-partitions</strong> as the source — only metadata pointers are copied, so it's near-instant and adds no storage. Because micro-partitions are immutable, writes to either side create new micro-partitions (copy-on-write); you only pay storage for the divergence. Used for instant prod-to-dev copies, pre-deployment snapshots, and what-if testing."},
  {"sec":"LAKE-WAREHOUSE","front":"What is Time Travel and what enables it?","back":"Time Travel lets you query, clone, or restore a table/schema/database <strong>as of a past point</strong> using <code>AT</code>/<code>BEFORE</code> (timestamp, offset, or statement id), and <code>UNDROP</code> dropped objects. It works because the old immutable micro-partitions aren't deleted until retention expires. Retention is <strong>1 day by default</strong> (0–90 on Enterprise+, 0–1 on Standard) via <code>DATA_RETENTION_TIME_IN_DAYS</code>. After Time Travel expires, permanent objects pass to <strong>Fail-safe</strong> (7 non-configurable days, recoverable only by Snowflake support)."},
  {"sec":"LAKE-WAREHOUSE","front":"Fail-safe vs. Time Travel — what's the difference?","back":"<strong>Time Travel</strong> is user-accessible self-service recovery within the configurable retention window (default 1 day). <strong>Fail-safe</strong> is a fixed <strong>7-day</strong> period <em>after</em> Time Travel expires (permanent tables only) where data is retained for disaster recovery and is recoverable <strong>only via Snowflake support</strong>, not by you. Both consume billable storage, which is why long retention on high-churn tables can balloon storage cost. Transient/temporary tables have no Fail-safe."},
  {"sec":"LAKE-WAREHOUSE","front":"How do you isolate workloads (ETL vs. BI vs. data science) in Snowflake?","back":"Spin up <strong>separate virtual warehouses</strong> over the same shared storage — e.g. a large <code>ETL_WH</code>, a multi-cluster <code>BI_WH</code> for dashboards, an <code>ADHOC_WH</code> for analysts. Because compute is decoupled, a heavy load job can't starve dashboards: each warehouse has its own CPU/RAM/cache. This is impossible in a single shared-nothing cluster, where all workloads contend for the same nodes."},
  {"sec":"LAKE-WAREHOUSE","front":"Snowflake vs. BigQuery compute model","back":"Snowflake gives you <strong>provisioned, user-managed virtual warehouses</strong> — you pick a size, control suspend/resume, and own concurrency tuning. BigQuery is <strong>serverless</strong>: there are no clusters to manage; queries consume <strong>slots</strong> (units of parallelism) either on-demand (billed per TB scanned) or via capacity/Editions reservations with autoscaling slots. Snowflake = explicit compute knobs + per-second credit billing; BigQuery = no infra, but you trade direct control for automatic slot allocation."},
  {"sec":"LAKE-WAREHOUSE","front":"Snowflake vs. Redshift RA3 — is RA3 the same decoupling?","back":"RA3 nodes (with <strong>Redshift Managed Storage</strong>, hot data on local SSD + cold on S3) decouple storage from compute so you can resize compute without a full data reshuffle and pay for storage separately — closing much of the gap with Snowflake. But the cluster is still a <strong>provisioned, fixed set of nodes</strong> you manage; concurrency isolation comes via <strong>Concurrency Scaling</strong> or <strong>Redshift Serverless</strong> rather than Snowflake's first-class many-independent-warehouses model. RA3 is decoupled storage; it is not Snowflake's services-layer-mediated multi-cluster design."},
  {"sec":"LAKE-WAREHOUSE","front":"Why are Snowflake updates/deletes more expensive than appends?","back":"Micro-partitions are <strong>immutable</strong>, so a DML that touches even one row must rewrite the entire affected micro-partition(s) as new files (copy-on-write), and the old versions linger for Time Travel + Fail-safe. Wide updates that hit many partitions rewrite a lot of data and inflate storage. This is why bulk/append patterns (and clustering to localize updates) are favored, and why high-churn small-update workloads are an anti-pattern for any immutable-columnar warehouse."},
  {"sec":"LAKE-WAREHOUSE","front":"What runs in the cloud services layer specifically?","back":"The always-on, multi-tenant coordination layer: <strong>query parsing & optimization</strong>, <strong>micro-partition metadata & pruning stats</strong>, <strong>transaction management</strong> (ACID, MVCC over immutable files), <strong>the result cache</strong>, <strong>authentication/RBAC/security</strong>, and <strong>infrastructure management</strong> (warehouse provisioning). It needs no virtual warehouse to run — metadata-only queries and result-cache hits return without resuming compute. It's billed only when it exceeds 10% of daily warehouse credits."}
);

Object.assign(GLOSSARY_DATA, {
  "three-layer-architecture": {"term":"Snowflake Three-Layer Architecture","group":"LAKE-WAREHOUSE","aliases":["three-layer architecture","decoupled architecture","storage compute services","snowflake architecture"],"eli5":"Snowflake splits a warehouse into three parts that scale on their own: where data lives, the engines that crunch it, and the brain that coordinates everything.","memory":"Storage / Compute / Services = the disk, the engines, and the brain. Data is rented once, engines are rented by the second.","full":"<p>Snowflake's design separates <strong>storage</strong> (immutable columnar micro-partitions in cloud object storage), <strong>compute</strong> (independent virtual warehouses — MPP clusters that read from storage), and <strong>cloud services</strong> (an always-on, multi-tenant layer for metadata, optimization, transactions, security, and the result cache). Each layer scales and bills independently.</p><p>The architectural breakthrough is that data lives <em>once</em> in object storage and any number of warehouses can read it concurrently without contention — you scale compute without moving data, and run isolated workloads on a shared dataset. This is what classic shared-nothing MPP (data pinned to each node's local disk) cannot do.</p>","trap":"Don't say 'shared-nothing.' Snowflake is a hybrid: shared-disk storage (one copy in object store) + shared-nothing compute (each warehouse is its own MPP cluster). Calling it purely shared-nothing or purely shared-disk is wrong.","worked":"ETL_WH (large) loads into a table at 2am; at 9am a BI multi-cluster warehouse and an analyst's ADHOC_WH both query the same table — three warehouses, one copy of data, zero contention.","related":["virtual-warehouse","micro-partition","cloud-services-layer","storage-compute-separation"]},
  "micro-partition": {"term":"Micro-partition","group":"LAKE-WAREHOUSE","aliases":["micro-partitions","micropartition","micro partition"],"eli5":"Snowflake automatically slices your table into small, immutable columnar files, each tagged with stats so the engine can skip the ones it doesn't need.","memory":"Think Parquet row-group + footer stats, but Snowflake-managed, automatic, and immutable. 50–500MB is the UNCOMPRESSED size the optimizer can skip wholesale.","full":"<p>A micro-partition is Snowflake's automatic storage unit: a contiguous, compressed, <strong>columnar</strong> file holding ~50–500 MB of <em>uncompressed</em> data (the compressed on-disk file is far smaller, on the order of ~16 MB), typically tens of thousands of rows. You never create or manage them. Each carries rich metadata — per-column min/max, distinct counts, null counts — tracked in the cloud services layer.</p><p>They are <strong>immutable</strong>: any update or delete writes new micro-partitions rather than mutating in place (copy-on-write). That immutability is the foundation for cheap Time Travel, Fail-safe, and zero-copy cloning, and the metadata is the foundation for partition pruning. The trade-off: small frequent updates rewrite whole partitions and accumulate versioned storage.</p>","trap":"The 50–500 MB figure is <em>uncompressed</em>; the compressed on-disk file is much smaller (~16 MB-ish). Saying micro-partitions are '50–500 MB compressed' is the common interview slip. And they're immutable — a one-row UPDATE rewrites the entire micro-partition(s) it lands in, not just the row.","worked":"A 10 TB events table becomes ~hundreds of thousands of micro-partitions; a query filtered to one day reads only the partitions whose ts min/max overlap that day.","related":["partition-pruning","time-travel","zero-copy-cloning"]},
  "virtual-warehouse": {"term":"Virtual Warehouse","group":"LAKE-WAREHOUSE","aliases":["virtual warehouse","warehouse","compute warehouse","multi-cluster warehouse"],"eli5":"An on-demand compute cluster you name and size; you can run many of them over the same data, and they bill only while running.","memory":"Up for complexity, Out for concurrency. A warehouse is a meter that only ticks while it's spinning — suspend it and the bill stops.","full":"<p>A virtual warehouse is a Snowflake MPP compute cluster (sizes XS→6XL, each step roughly doubling credits/hr, node count, and resources). <strong>Scale up</strong> (bigger size) adds CPU/RAM/local-SSD cache for heavier individual queries; <strong>scale out</strong> (multi-cluster warehouse, an Enterprise Edition+ feature) adds more same-size clusters to absorb <em>concurrency</em>, auto-spinning clusters up under load and suspending them when idle.</p><p>Billing is per-second compute credits (60-second minimum each time it starts), with <code>AUTO_SUSPEND</code>/<code>AUTO_RESUME</code> so you pay only while active. Because each warehouse is independent and reads the shared storage, warehouses provide <strong>workload isolation</strong> — separate warehouses for ETL, BI, and ad-hoc never contend for the same compute.</p>","trap":"A bigger warehouse does NOT add concurrency — it makes single queries faster. To handle more simultaneous queries you need a multi-cluster (scale-out) warehouse, which requires Enterprise Edition, not just a larger size. Confusing the two is the classic interview miss.","worked":"Dashboards timing out at peak: switching BI_WH from a single Large to a multi-cluster (min 1, max 4) Medium fixes queueing better than jumping to XL.","related":["storage-compute-separation","three-layer-architecture","result-cache"]},
  "storage-compute-separation": {"term":"Separation of Storage and Compute","group":"LAKE-WAREHOUSE","aliases":["storage compute separation","decoupled storage and compute","disaggregated storage compute"],"eli5":"Data sits once in cloud object storage and any number of compute clusters can read it, so you scale (and pay for) each side on its own.","memory":"Data is the building; warehouses are tenants who rent compute by the second. Add tenants without rebuilding the building.","full":"<p>Snowflake stores table data once in cloud object storage (S3/GCS/Azure Blob) and lets independent virtual warehouses read it on demand. Storage is billed flat per compressed TB/month; compute is billed per-second only while warehouses run. You can resize, add, or suspend compute without moving or re-sharding data.</p><p>This is the defining difference from classic shared-nothing MPP, where data is pinned to each node's local disk so compute and storage scale together and concurrent workloads fight over the same nodes. Decoupling enables many-warehouses-on-one-dataset, instant elasticity, and per-workload cost attribution.</p>","trap":"RA3/Redshift Managed Storage and BigQuery also decouple storage from compute — decoupling alone isn't unique to Snowflake. The differentiator is Snowflake's first-class 'many independent warehouses concurrently on one dataset with full isolation,' not merely that storage and compute have separate bills.","worked":"Tripling analyst headcount adds an ADHOC_WH (more compute credits) but changes the storage bill by $0 — the dataset isn't copied.","related":["three-layer-architecture","virtual-warehouse"]},
  "zero-copy-cloning": {"term":"Zero-Copy Cloning","group":"LAKE-WAREHOUSE","aliases":["zero copy cloning","clone","snowflake clone","zero-copy clone"],"eli5":"Cloning a table just copies pointers to the same underlying files, so it's instant and free until you start changing data.","memory":"It's a Git branch for your data: copy-on-write, instant, shares blocks until you mutate. 'Zero-copy' = pointers, not bytes.","full":"<p><code>CREATE TABLE t_clone CLONE t</code> (also for schemas/databases) creates a new named object whose metadata points at the <strong>same immutable micro-partitions</strong> as the source. No data is physically copied, so the clone is near-instant and initially consumes no extra storage. Time Travel offsets can clone an object as of a past moment too.</p><p>Because micro-partitions are immutable, subsequent writes to either the clone or the source create <em>new</em> micro-partitions (copy-on-write); you only pay storage for the divergence. This makes clones ideal for instant prod→dev copies, pre-deploy snapshots, and isolated what-if testing without doubling storage.</p>","trap":"Clones aren't free forever — once you (or the source) modify data, the changed micro-partitions diverge and bill as new storage. Also, a clone is a point-in-time snapshot: it does NOT stay in sync with the source after creation.","worked":"Clone a 20 TB prod database to dev in seconds; the dev clone costs storage only for the micro-partitions dev actually rewrites.","related":["micro-partition","time-travel","storage-compute-separation"]},
  "time-travel": {"term":"Time Travel","group":"LAKE-WAREHOUSE","aliases":["time travel","snowflake time travel","data retention"],"eli5":"Query or restore a table as it looked in the past, because Snowflake keeps the old immutable file versions for a retention window.","memory":"Undo button for data, powered by keeping old immutable partitions. Default 1 day; then 7 days of Fail-safe you can't touch yourself.","full":"<p>Time Travel lets you query (<code>AT</code>/<code>BEFORE</code> with a timestamp, time offset, or statement id), clone, or restore a table/schema/database to a prior state, and <code>UNDROP</code> dropped objects. It works because superseded micro-partitions are retained — not deleted — until the retention window lapses. Retention is set by <code>DATA_RETENTION_TIME_IN_DAYS</code>: <strong>default 1 day</strong>, up to 90 on Enterprise+ (max 1 on Standard).</p><p>After Time Travel expires, permanent objects pass into <strong>Fail-safe</strong> (a fixed 7-day, Snowflake-support-only recovery period) before permanent deletion. Both windows consume billable storage, so long retention on high-churn tables drives up storage cost.</p>","trap":"Default retention is only 1 day (and 0 is allowed). People assume 90 — that's the Enterprise <em>max</em>, not the default, and Standard edition caps at 1. Fail-safe's 7 days are non-configurable and NOT self-service.","worked":"Accidental <code>DELETE</code> at 10:00; at 10:30 run <code>INSERT INTO t SELECT * FROM t BEFORE(STATEMENT => '<id>')</code> or <code>CREATE TABLE t_restored CLONE t AT(OFFSET => -1800)</code>.","related":["micro-partition","zero-copy-cloning","fail-safe"]},
  "fail-safe": {"term":"Fail-safe","group":"LAKE-WAREHOUSE","aliases":["fail-safe","failsafe","fail safe"],"eli5":"A fixed 7-day grace period after Time Travel ends where only Snowflake support can recover your data, for disaster recovery only.","memory":"Time Travel = your undo button; Fail-safe = Snowflake's fire-extinguisher behind glass. 7 fixed days, support-only.","full":"<p>Fail-safe is a <strong>non-configurable 7-day</strong> period that begins <em>after</em> the Time Travel retention window expires, applying to permanent tables (transient and temporary tables have no Fail-safe). During it, historical data is retained solely so Snowflake can recover from extreme operational failures — it is <strong>not</strong> self-service and requires a support request, with recovery taking hours to days.</p><p>Fail-safe storage is billable, so combining long Time Travel retention with high-churn tables compounds storage cost: every superseded micro-partition is held through both windows.</p>","trap":"Fail-safe is NOT a backup or a queryable history you can access — you cannot <code>SELECT ... AT</code> into Fail-safe. It's disaster-recovery-only and only for permanent objects; transient/temporary tables skip it entirely.","worked":"A permanent table with 1-day Time Travel that's dropped is self-recoverable for 1 day; on day 2–8 only Snowflake support can retrieve it; after that it's gone.","related":["time-travel","micro-partition"]},
  "cloud-services-layer": {"term":"Cloud Services Layer","group":"LAKE-WAREHOUSE","aliases":["cloud services layer","services layer","global services","snowflake brain"],"eli5":"The always-on coordinator that plans queries, holds metadata, runs transactions and security, and serves the result cache — no warehouse required.","memory":"The brain that never sleeps. If a query needs only stats or a cached answer, the engines stay off and you pay (almost) nothing.","full":"<p>The cloud services layer is Snowflake's multi-tenant control plane: query parsing and optimization, micro-partition metadata and pruning statistics, transaction/ACID management (MVCC over immutable files), authentication and RBAC, infrastructure provisioning, and the <strong>result cache</strong>. It runs continuously and is shared across accounts.</p><p>Crucially, it can answer some requests with <em>no</em> virtual warehouse: result-cache hits and metadata-only queries (e.g. <code>COUNT(*)</code>, <code>MIN</code>/<code>MAX</code> on numeric/date columns, <code>SHOW</code>) return without resuming compute. Billing is free up to 10% of daily warehouse credits, then charged on the excess.</p>","trap":"It's not 'free' unconditionally — services compute is billed only when it exceeds 10% of the day's warehouse credits (heavy use of clones, many small queries, or aggressive metadata churn can push you over). Also note metadata MIN/MAX shortcuts apply to numeric/date types, not arbitrary strings.","worked":"<code>SELECT COUNT(*) FROM huge_table</code> returns instantly from metadata with the warehouse suspended; rerunning an identical analytical query returns from the result cache with zero compute.","related":["partition-pruning","result-cache","three-layer-architecture"]}
});
