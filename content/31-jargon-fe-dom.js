/* Frontend jargon & acronyms — clickable reference (FE-DOM, auto-assembled) */
Object.assign(GLOSSARY_DATA, {
  "cssom": {"term":"CSSOM","group":"FE-DOM","aliases":["CSS Object Model"],"eli5":"The browser's in-memory tree of all the CSS rules and computed styles, the CSS counterpart to the DOM, which gets combined with the DOM to figure out how to render the page.","memory":"CSSOM = CSS Object Model — the DOM, but for your styles.","full":"<p><strong>CSSOM (CSS Object Model)</strong> is the browser's structured, in-memory representation of the CSS associated with a document: stylesheets, their rules, selectors, declarations, and the computed style of every element. It is the styling counterpart to the <code>DOM</code> (Document Object Model), which represents the HTML structure. Together they are combined to produce the render tree.</p><p>The CSSOM is defined by a family of W3C/WHATWG specifications (the \"CSS Object Model\" and \"CSSOM View\" specs, developed through the 2010s) that standardize the JavaScript APIs for reading and manipulating styles — for example <code>document.styleSheets</code>, <code>CSSRule</code>/<code>CSSStyleRule</code>, <code>element.style</code>, and the read-only <code>getComputedStyle()</code>. CSSOM View additionally standardizes geometry/scrolling APIs like <code>getBoundingClientRect()</code>, <code>scrollTop</code>, and <code>window.matchMedia</code>.</p><p>Its purpose is twofold: internally it lets the engine resolve the cascade and compute final styles; externally it gives scripts a programmatic way to inspect and change styling. CSSOM remains fundamental today because it sits on the critical rendering path — the browser builds the DOM from HTML and the CSSOM from CSS <em>in parallel</em>, merges them into the render tree, then runs layout (reflow) and paint. Understanding it explains why CSS is render-blocking (an incomplete CSSOM can't be safely used), and why reading layout properties like <code>offsetHeight</code> after writing styles forces a synchronous reflow (\"layout thrashing\").</p>","worked":"<p>Read computed styles through the CSSOM (returns the resolved value, not just what you wrote):</p><ul><li><code>const color = getComputedStyle(el).color; // e.g. \"rgb(0, 0, 0)\"</code></li></ul><p>Manipulate stylesheets directly via the CSSOM:</p><ul><li><code>document.styleSheets[0].insertRule(\".btn { color: red }\", 0);</code></li></ul><p>Triggering layout thrash — a write then a read forces synchronous reflow:</p><ul><li><code>el.style.width = \"200px\"; const h = el.offsetHeight; // forces reflow to compute h</code></li></ul>","teach":"<p>At its core, the CSSOM is just a tree of objects that represents your CSS the same way the DOM is a tree of objects that represents your HTML. When the browser reads <code>&lt;p&gt;Hello&lt;/p&gt;</code>, it builds a DOM node for that paragraph. When it reads <code>p { color: blue }</code>, it builds CSSOM objects for that rule. Neither one alone can draw the page — the browser has to <em>combine</em> them to know \"this paragraph node should be blue.\"</p><div class=\"analogy\">Picture assembling IKEA furniture. The DOM is the box of physical parts — the shelves, screws, panels (your content). The CSSOM is the instruction booklet — which part goes where, what color, how it's oriented (your styling). You can't have a finished bookshelf with only parts and no instructions, or only instructions and no parts. The browser merges the two into the \"render tree\" — the actual assembled thing it's about to show you.</div><p>The browser builds both trees <strong>in parallel</strong> as bytes stream in, which is why CSS being \"render-blocking\" makes sense: if it painted using a half-built CSSOM, an element might flash one color then change when the rest of the rules arrive (that's FOUC). So it waits until the CSSOM is complete enough to be safe.</p><p>The CSSOM also has a public JavaScript side. <code>getComputedStyle(el)</code> asks the CSSOM, \"after the entire cascade, what is this element's <em>final</em> color/width/font?\" — the resolved answer, not just the inline value you set. You can even mutate stylesheets through it with APIs like <code>insertRule</code>. The catch: reading geometry (<code>offsetHeight</code>, <code>getBoundingClientRect</code>) right after writing styles forces the browser to stop and recompute layout immediately so it can give you an accurate number. Do that in a loop and you get \"layout thrashing\" — many forced reflows that tank performance. The fix is to batch all your reads, then all your writes.</p><div class=\"key\">CSSOM is the DOM's styling twin: a live object tree of all your CSS plus every element's computed style. The browser merges DOM + CSSOM into the render tree before painting, which is why CSS is render-blocking and why interleaving style writes with layout reads causes reflow thrash.</div>","related":["fouc","fout"]},
  "bom": {"term":"BOM","group":"FE-DOM","aliases":["Browser Object Model","Byte Order Mark"],"eli5":"In frontend usage, BOM (Browser Object Model) refers to the browser-provided objects beyond the document — like window, location, navigator, history, and screen — that let scripts interact with the browser itself rather than the page content.","memory":"Browser Object Model — the 'window' and friends (location, navigator, history) that wrap the browser around the DOM.","full":"<p><strong>BOM (Browser Object Model)</strong> is an informal term for the collection of browser-supplied JavaScript objects that represent the browser environment surrounding a page, as opposed to the page's content. Its root is the global <code>window</code> object, under which sit <code>location</code> (the current URL), <code>navigator</code> (info about the browser/device), <code>history</code> (session navigation), <code>screen</code> (display metrics), and timing functions like <code>setTimeout</code>. The DOM (Document Object Model) handles the page's content tree; the BOM handles everything <em>around</em> it.</p><p>Unlike the DOM, the BOM was never a single formally chartered specification — it grew organically from the objects early browsers (notably Netscape) exposed in the 1990s, and the term \"Browser Object Model\" is a descriptive convention rather than an official standard's name. Over time the pieces it refers to were standardized individually: <code>window</code>, <code>location</code>, <code>history</code>, and others are now defined in the WHATWG HTML and related standards.</p><p>The concept is still used because the objects it names are indispensable: reading or changing the URL (<code>location.href</code>), navigating history (<code>history.pushState</code> for single-page-app routing), feature/capability detection via <code>navigator</code>, and opening windows all live here. Note the acronym is overloaded — \"BOM\" can also mean <em>Byte Order Mark</em>, the special character at the start of some Unicode text files; context disambiguates which is meant.</p>","worked":"<p>Common Browser Object Model interactions:</p><ul><li><code>window.location.href = 'https://example.com/next';</code> — navigate</li><li><code>history.pushState({}, '', '/dashboard');</code> — SPA route change without reload</li><li><code>navigator.onLine</code> — boolean connectivity hint</li><li><code>const id = setTimeout(() =&gt; doThing(), 500);</code> — schedule work</li></ul><p>None of these touch page elements — they manipulate the browser context itself, which is what makes them \"BOM\" rather than \"DOM.\"</p>","teach":"<p>At its core, the BOM is just the set of JavaScript handles the browser gives you to control <em>itself</em> — the window, the address bar, the back button — rather than the contents of the page.</p><p>Beginners often blur the DOM and the BOM together because both are reached through JavaScript. The clean distinction: the <strong>DOM</strong> is the document — the tree of <code>&lt;div&gt;</code>s, text, and attributes that make up the page. The <strong>BOM</strong> is the browser wrapped around that document — the tab, its URL, its history, the device info.</p><div class=\"analogy\">Picture reading a book on an e-reader. The DOM is the <em>text on the page</em> — the words, paragraphs, and chapter headings you can highlight and edit. The BOM is the <em>e-reader device</em> itself — the page-turn buttons (history), the \"which book and page am I on\" indicator (location), the battery and model info (navigator). The book is the content; the device is the environment around it.</div><p>Historically the BOM wasn't designed as one tidy spec. Early browsers in the 1990s just exposed useful objects on the global <code>window</code>, and the name \"Browser Object Model\" was coined afterward to describe that grab-bag. The individual pieces were later folded into formal standards (the WHATWG HTML standard defines <code>window</code>, <code>location</code>, <code>history</code>, and so on).</p><p>You use it constantly. Single-page apps lean on <code>history.pushState</code> to change the URL without a full reload. Redirects use <code>location.href</code>. Feature detection reads <code>navigator</code>. One trap: \"BOM\" is also short for <em>Byte Order Mark</em>, an invisible Unicode character at the start of some files — so always read the context to know which BOM someone means.</p><div class=\"key\">BOM (Browser Object Model) = the browser-environment objects rooted at <code>window</code> (<code>location</code>, <code>navigator</code>, <code>history</code>, <code>screen</code>, timers) — the browser around the page, as opposed to the DOM's page content. Beware the homonym: BOM can also mean Byte Order Mark.</div>","related":["whatwg","w3c"]},
  "dom": {"term":"DOM","group":"FE-DOM","aliases":["Document Object Model"],"eli5":"The browser's live, in-memory tree of objects representing every element of an HTML page, which JavaScript can read and change.","memory":"DOM = Document Object Model — the page as a tree of objects.","full":"<p>The <strong>DOM</strong> (Document Object Model) is a programming interface that represents an HTML or XML document as a tree of nodes — elements, attributes, and text — each exposed as an object that scripts can read and manipulate. When the browser parses HTML, it constructs this live tree in memory; mutating the DOM (e.g., <code>element.textContent = '...'</code>) changes what the user sees. The DOM is the API surface for <code>document.querySelector</code>, event listeners, <code>tabindex</code>, and essentially all client-side page manipulation.</p><p>The DOM was standardized by the <strong>W3C</strong>, beginning with DOM Level 1 in 1998, through Levels 2 and 3, after the chaotic 'DOM Level 0' era of incompatible browser APIs in the 1990s. Stewardship later moved largely to the <strong>WHATWG</strong>, which maintains the DOM as a continuously updated <em>Living Standard</em> alongside HTML. Milestones include standardized events, namespaces, and modern APIs like <code>querySelector</code>, <code>classList</code>, and the <code>MutationObserver</code>.</p><p>Its purpose is to give programs a structured, language-neutral way to access and transform documents. It remains the foundation of all front-end web development: frameworks like React build a <em>virtual DOM</em> abstraction on top of it, and the browser derives the <strong>accessibility tree</strong> from the DOM plus CSS, exposing roles, names, and states to assistive-technology APIs (UIA, IAccessible2, AX). Understanding the DOM is prerequisite to understanding rendering, events, accessibility, and most performance work.</p>","worked":"<p>Reading and mutating the live tree:</p><ul><li><code>const el = document.querySelector('#title');</code></li><li><code>el.textContent = 'Updated!'; // the page visibly changes</code></li><li><code>el.setAttribute('tabindex', '0'); // joins the natural tab order</code></li></ul><p>The HTML <code>&lt;h1 id=\"title\"&gt;Hello&lt;/h1&gt;</code> becomes a node object; the browser later builds the accessibility tree from this DOM node plus its CSS.</p>","teach":"<p>At its core, the DOM is just your HTML turned into a tree of <em>live objects</em> in the browser's memory — and because it's live, changing an object changes the screen.</p><div class=\"analogy\">Think of the HTML file as a recipe written on paper, and the DOM as the actual cake the browser baked from it, sitting on the counter. You can't re-taste the paper — but you <em>can</em> add frosting to the real cake, and everyone sees it. JavaScript edits the cake (the DOM), not the recipe (the source HTML).</div><p>Every tag becomes a <strong>node</strong>: <code>&lt;div&gt;</code> is an element node, the text inside it is a text node, its <code>class</code> is an attribute. These nest exactly like the HTML, forming a tree with <code>document</code> at the root. Parent/child/sibling relationships in that tree are how you navigate (<code>el.parentNode</code>, <code>el.children</code>).</p><p>What makes the DOM powerful is that it's a two-way mirror. You <em>read</em> it (<code>document.querySelector('button')</code> finds a node), and you <em>write</em> it (<code>node.textContent = 'Hi'</code> updates the page instantly). You attach event listeners to nodes to react to clicks, and you set properties like <code>tabindex</code> to splice an element into the keyboard tab order.</p><p>Two things build directly on the DOM and are worth knowing. First, the <strong>accessibility tree</strong>: the browser takes your DOM plus its CSS and derives a parallel tree of roles, names, and states that it hands to screen readers — so semantic, correct DOM = accessible app. Second, frameworks like React keep a lightweight <em>virtual</em> copy of the DOM, diff it on each render, and apply only the minimal real-DOM changes, because touching the real DOM is comparatively expensive.</p><div class=\"key\">The DOM is the page as a live, scriptable tree of objects. Editing the DOM updates the screen; the accessibility tree and frameworks' virtual DOMs are both built on top of it.</div>","related":["jsx","prototype-pollution"]},
  "ime": {"term":"IME","group":"FE-DOM","aliases":["Input Method Editor","Input Method"],"eli5":"A software component that lets you type characters that aren't on your keyboard — like Chinese, Japanese, or Korean — by composing them from multiple keystrokes.","memory":"IME = Input Method Editor: you 'edit' an input by composing it before it commits.","full":"<p>An <strong>IME (Input Method Editor)</strong> is an operating-system component that translates a sequence of keystrokes into characters that can't be typed with a single key press. It is essential for languages with thousands of characters — Chinese, Japanese, Korean (collectively <em>CJK</em>) — where a user types a phonetic or structural sequence (e.g. pinyin, romaji) and the IME offers candidate characters to choose from. The text being assembled is called the <strong>composition</strong>, and it isn't 'final' input until the user confirms it.</p><p>IMEs predate the web — they emerged in the era of early CJK computing as the only practical way to enter these scripts on a keyboard with a limited key count. On the web platform, the relevant standardization came through the DOM, which exposes <strong>composition events</strong> (<code>compositionstart</code>, <code>compositionupdate</code>, <code>compositionend</code>) so JavaScript can tell when the user is mid-composition versus done. These events were specified as part of the DOM Level 3 Events work at the W3C and are supported across all modern browsers.</p><p>The purpose is correctness: an <code>input</code> or <code>keydown</code> handler that fires on every keystroke will see meaningless intermediate state during composition. For a search-as-you-type box, that means firing queries against half-formed pinyin. IMEs remain universally used because they are the only viable way to enter CJK and many other scripts — there is no replacement — so front-end code that handles live input (autocomplete, validation, rich editors) must respect composition state by checking <code>event.isComposing</code> or waiting for <code>compositionend</code>.</p>","worked":"<p>A type-ahead handler that skips queries while a CJK user is still composing:</p><ul><li><code>&lt;input id=\"q\" type=\"search\" autocomplete=\"off\"&gt;</code></li></ul><p>And the listener logic (escaped):</p><ul><li><code>const el = document.getElementById('q');</code></li><li><code>let composing = false;</code></li><li><code>el.addEventListener('compositionstart', () =&gt; { composing = true; });</code></li><li><code>el.addEventListener('compositionend', () =&gt; { composing = false; runQuery(el.value); });</code></li><li><code>el.addEventListener('input', (e) =&gt; { if (e.isComposing || composing) return; runQuery(el.value); });</code></li></ul><p>Without the <code>isComposing</code> guard, typing the pinyin for a single Chinese word could fire several useless requests before the character is even chosen.</p>","teach":"<p>At its core, an IME is just <strong>a translator that sits between your keyboard and your text field</strong>, turning a stream of simple keystrokes into characters your keyboard could never hold directly.</p><p>Think about how many distinct characters Chinese has — tens of thousands. You obviously can't have a key for each. So instead you type a <em>recipe</em>: the phonetic spelling (pinyin) of the word. The IME watches that recipe build up, shows you a little popup of candidate characters that match, and only when you pick one does the real character land in the box. Everything before that pick is provisional.</p><div class=\"analogy\">It's like ordering at a deli counter. You don't get your sandwich the instant you say the first word — you describe what you want ('turkey... no wait, ham... on rye'), and only when you say 'that's it' does the order get made. If the kitchen started cooking on every word you uttered, you'd get three wrong sandwiches before the right one. The IME's composition phase is you still talking; <code>compositionend</code> is you saying 'that's it.'</div><p>For front-end developers this matters enormously, because the browser fires <code>input</code> events <em>during</em> composition. If your search box queries the server on every <code>input</code>, a CJK user triggers a burst of garbage requests against half-finished phonetics. The fix is to listen for the composition events: set a flag on <code>compositionstart</code>, clear it and run your real logic on <code>compositionend</code>, and inside your <code>input</code> handler bail out early when <code>event.isComposing</code> is true.</p><p>This is also why thoughtful UIs reserve vertical space for the candidate popup and result list — so the layout doesn't jump (a CLS, or Cumulative Layout Shift, problem) as composition and results appear.</p><div class=\"key\">An IME composes multi-keystroke characters before committing them. Treat the composition phase as 'not final yet' — guard live-input logic with <code>isComposing</code> / <code>compositionend</code> so CJK users don't trigger work on half-typed text.</div>","related":["html","css"]},
  "utf": {"term":"UTF","group":"FE-DOM","aliases":["Unicode Transformation Format","UTF-8","UTF-16"],"eli5":"A family of encodings that turn Unicode characters (every letter, symbol, and emoji) into the bytes a computer actually stores and transmits.","memory":"UTF = Unicode Transformation Format: it 'transforms' abstract characters into concrete bytes.","full":"<p><strong>UTF (Unicode Transformation Format)</strong> is the family of standard encodings that map Unicode <em>code points</em> — the abstract numbers assigned to every character in the world's writing systems, plus symbols and emoji — onto sequences of bytes. The two most important members are <strong>UTF-8</strong> (a variable-width encoding using 1 to 4 bytes per character) and <strong>UTF-16</strong> (using 2 or 4 bytes). UTF-8 is backward-compatible with ASCII: the first 128 code points encode as a single byte identical to their ASCII value.</p><p>Unicode itself was developed starting around 1987–1991 to replace the chaotic patchwork of incompatible regional character sets. UTF-8 was designed by Ken Thompson and Rob Pike in 1992 and became the dominant text encoding of the web. It is maintained jointly through the Unicode Consortium and aligned with ISO/IEC 10646. Over the decades UTF-8 grew from a clever Unix trick into the overwhelming default — the modern web is encoded in UTF-8 the vast majority of the time, and HTML5 recommends it explicitly.</p><p>Its purpose is unambiguous, interoperable text exchange: every system can encode and decode the same bytes back into the same characters, regardless of language. UTF remains universally used because it solves the fundamental problem perfectly — UTF-8 in particular is compact for Western text, lossless for everything else, and self-synchronizing. For front-end work it underpins everything from declaring <code>&lt;meta charset=\"utf-8\"&gt;</code> to correctly handling multi-byte characters (emoji, CJK from an IME) where one visible character may be several code units, which is why naive <code>string.length</code> can surprise you.</p>","worked":"<p>Declare the document encoding as the very first thing in <code>&lt;head&gt;</code> so the browser parses bytes correctly:</p><ul><li><code>&lt;!doctype html&gt;</code></li><li><code>&lt;html&gt;</code></li><li><code>&lt;head&gt;</code></li><li><code>&nbsp;&nbsp;&lt;meta charset=\"utf-8\"&gt;</code></li><li><code>&nbsp;&nbsp;&lt;title&gt;Café — 日本語 — 🎉&lt;/title&gt;</code></li><li><code>&lt;/head&gt;</code></li></ul><p>In JavaScript, beware code-unit vs character counting: <code>\"🎉\".length</code> is <code>2</code> (a surrogate pair in UTF-16), while <code>[...\"🎉\"].length</code> is <code>1</code>, because the spread iterator walks code points, not UTF-16 code units.</p>","teach":"<p>At its core, UTF is just <strong>a rulebook for turning characters into bytes and back without ever losing or scrambling them</strong>.</p><p>Computers don't store letters — they store numbers (bytes). Unicode is the giant master list that says 'the letter A is number 65, the emoji 🎉 is number 127881,' and so on for every script on Earth. But a list of numbers isn't enough; you still have to decide <em>how to write those numbers down as bytes</em>. That's what a UTF encoding is: the writing-down scheme. UTF-8 says small numbers (plain ASCII) take one byte, and bigger numbers take two, three, or four bytes in a clever pattern that a decoder can always parse unambiguously.</p><div class=\"analogy\">Think of Unicode as a worldwide phone book assigning everyone a unique number, and UTF-8 as the postal format for writing those numbers on envelopes. Short local numbers get a short envelope; international ones get a longer one. Because the format is self-describing, the mail carrier (the decoder) always knows where one address ends and the next begins, even reading a stream of envelopes with no gaps.</div><p>Why does a front-end developer care? Three reasons. First, <code>&lt;meta charset=\"utf-8\"&gt;</code> must come early, or the browser may guess wrong and render mojibake (garbled accented characters). Second, multi-byte characters break naive assumptions: an emoji or many CJK characters occupy two UTF-16 'code units' in JavaScript, so <code>\"🎉\".length === 2</code>. If you slice a string in the middle of such a character, you corrupt it. Third, this connects directly to IME input — when CJK users compose text, you're handling exactly these multi-byte characters, so encoding correctness and character-vs-code-unit awareness matter together.</p><div class=\"key\">UTF encodings map Unicode code points to bytes. UTF-8 is the web's default — ASCII-compatible, variable-width, lossless. Always declare it, and remember that one visible character can be more than one code unit when you measure or slice strings.</div>","related":["ime","html"]}
});
