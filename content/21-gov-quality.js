/* Data Quality Dimensions & Validation (GOV-QUALITY) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"GOV-QUALITY","front":"The six core data-quality dimensions","back":"<ul><li><strong>Accuracy</strong> — values match the real-world entity they describe.</li><li><strong>Completeness</strong> — no missing values where data is expected.</li><li><strong>Consistency</strong> — the same fact agrees across systems/rows.</li><li><strong>Timeliness/freshness</strong> — data is recent enough for its use.</li><li><strong>Validity</strong> — values conform to format/type/range rules.</li><li><strong>Uniqueness</strong> — no unwanted duplicate entities.</li></ul>The senior framing: a dimension is only useful when it has a <em>measurable</em> check and a threshold. (Some frameworks add integrity/relevance, but these six are the common core.)"},
  {"sec":"GOV-QUALITY","front":"Validity vs. accuracy — why they are NOT the same","back":"<strong>Validity</strong> = conforms to the rule (format, type, range, allowed set). <strong>Accuracy</strong> = matches reality. A row can be <em>valid but wrong</em>: <code>'1900-01-01'</code> is a valid date but a wrong birthdate; a real-but-mistyped email passes a regex yet bounces. Validity is cheap and checkable in-pipeline; accuracy usually needs a trusted reference/source-of-truth and is the hardest dimension to measure."},
  {"sec":"GOV-QUALITY","front":"Why is accuracy the hardest dimension to test in a pipeline?","back":"Every other dimension is checkable against the data itself or another column/system. Accuracy needs an external <em>ground truth</em> — a system of record, a reference dataset, or human verification — which is often unavailable, stale, or the very thing you're building. Practitioners proxy it with reconciliation against a trusted source, control totals, and anomaly detection rather than a direct equality assertion."},
  {"sec":"GOV-QUALITY","front":"Consistency: the two flavors you should distinguish","back":"<ul><li><strong>Cross-system / referential consistency</strong> — the same entity agrees across tables/services (e.g., order count in the warehouse equals the OLTP source; every <code>order.customer_id</code> exists in <code>customers</code>).</li><li><strong>Intra-record / logical consistency</strong> — fields agree within a row (<code>ship_date >= order_date</code>, <code>state</code> matches <code>zip</code>).</li></ul>Cross-system consistency is usually enforced by reconciliation jobs; intra-record by row-level assertions."},
  {"sec":"GOV-QUALITY","front":"How do you actually measure timeliness/freshness?","back":"Freshness is a property of the <em>pipeline</em>, not a single row. Track <strong>data lag</strong> = (now − max event/processing timestamp) and alert when it exceeds an SLA (e.g., <code>max(updated_at)</code> &lt; 2h old). Distinguish <em>event-time</em> freshness (latest business event) from <em>load-time</em> freshness (last successful run). dbt's <code>source freshness</code> and Soda freshness checks both compare a timestamp column to <code>now()</code>."},
  {"sec":"GOV-QUALITY","front":"Uniqueness: deduping a stream vs. a batch table","back":"<strong>Batch</strong>: assert a primary/business key is unique (<code>count(*) == count(distinct key)</code>), or grain test on a composite key. <strong>Stream</strong>: exactly-once is hard, so you enforce uniqueness with idempotency keys + dedupe windows (e.g., keep first by <code>event_id</code> within a watermark). The interview point: \"unwanted\" duplicates — legitimate repeated events (two logins) are not a quality defect; restating the <em>grain</em> is what defines a duplicate."},
  {"sec":"GOV-QUALITY","front":"What does 'validation as code' mean and where do you run it?","back":"Quality checks are declarative, versioned assertions living in the repo (Great Expectations expectation suites, dbt tests, Soda checks) and executed in CI/CD and in the running pipeline — not manual SQL spot-checks. They run at <strong>boundaries</strong>: at <em>ingestion</em> (validate raw against the contract before it lands) and at <em>transformation</em> outputs (validate each model/mart). This makes quality reviewable, diffable, and gated like application code."},
  {"sec":"GOV-QUALITY","front":"Great Expectations vs. dbt tests vs. Soda — when to reach for each","back":"<ul><li><strong>dbt tests</strong> — SQL-native, live with your models; great for warehouse transforms (<code>unique</code>, <code>not_null</code>, <code>relationships</code>, <code>accepted_values</code> + generic/singular + packages like <code>dbt_expectations</code>).</li><li><strong>Great Expectations</strong> — Python-native expectation library that validates pandas/Spark DataFrames and files, well-suited to ingestion before the warehouse; produces Data Docs.</li><li><strong>Soda</strong> — SodaCL YAML checks + a metrics/monitoring service, strong for scheduled checks and anomaly detection across sources.</li></ul>"},
  {"sec":"GOV-QUALITY","front":"dbt's four built-in generic tests","back":"<code>unique</code>, <code>not_null</code>, <code>accepted_values</code>, and <code>relationships</code> (referential integrity to another model). Beyond these you write either a <em>generic test</em> (parameterized, reusable) or a <em>singular test</em> (a one-off SQL query that must return zero rows to pass). The mental model: <strong>a dbt test passes when its query returns no failing rows.</strong>"},
  {"sec":"GOV-QUALITY","front":"Circuit-break vs. warn-and-continue on a failed check","back":"<strong>Circuit-break (blocking / error)</strong>: stop the pipeline so bad data never reaches downstream consumers — use at trust boundaries and for critical marts. <strong>Warn-and-continue</strong>: log/alert but let data flow — use for low-severity or noisy checks. dbt encodes this with test <code>severity: error | warn</code> (plus <code>error_if</code>/<code>warn_if</code> thresholds). Staff-level answer: severity should map to <em>blast radius</em>, and warn-only checks must still page someone or they rot."},
  {"sec":"GOV-QUALITY","front":"The shift-left / 'test your data like your code' principle","back":"Push quality checks as far <em>upstream</em> as possible — validate at the source/ingestion boundary and in CI on every model change, rather than discovering bad data in a dashboard. Cheaper to fix early, and it stops a bad row from contaminating dozens of downstream models. Concretely: data contracts at producers, expectations at ingestion, dbt tests in PR CI, and freshness/volume monitors in prod."},
  {"sec":"GOV-QUALITY","front":"What is the cost of bad data (the three buckets)?","back":"<ul><li><strong>Bad decisions</strong> — wrong KPIs/dashboards drive wrong business actions.</li><li><strong>Broken models</strong> — ML trained/served on bad features silently degrades (garbage-in).</li><li><strong>Eroded trust</strong> — once stakeholders catch one wrong number, they stop trusting the platform and revert to spreadsheets.</li></ul>Trust is the asymmetric one: slow to build, instantly lost, and the reason quality is a platform concern, not a team chore."},
  {"sec":"GOV-QUALITY","front":"Data quality SLA vs. SLO vs. SLI — applied to a dataset","back":"<strong>SLI</strong> = the measured indicator (e.g., % rows passing validity, freshness lag in minutes, null rate). <strong>SLO</strong> = the internal target (\"freshness &lt; 30 min for 99% of hours\"; \"&lt;0.1% null on <code>user_id</code>\"). <strong>SLA</strong> = the externally-committed promise to consumers with consequences. Same observability discipline as service reliability, but the indicators are <em>data</em> properties (freshness, completeness, volume), not request latency."},
  {"sec":"GOV-QUALITY","front":"Anomaly / volume checks vs. explicit assertions","back":"<strong>Explicit assertions</strong> encode a <em>known</em> rule (\"<code>amount</code> &gt; 0\"). <strong>Anomaly checks</strong> catch the <em>unknown</em>: row-count drops, schema drift, distribution shifts, a null rate that suddenly triples — flagged against history rather than a fixed threshold. You need both: assertions for invariants you can name, anomaly detection for the failures you can't enumerate in advance (the classic \"upstream silently changed\")."},
  {"sec":"GOV-QUALITY","front":"Why a regex 'valid email' check can lull you into false confidence","back":"It only measures <strong>validity</strong> (format), which says nothing about <strong>accuracy</strong> (does the address belong to this user, does it deliver) or <strong>uniqueness</strong> (same email on 10k accounts) or <strong>completeness</strong> (the rows where email is silently NULL). The trap is reporting \"99.9% valid\" as if it were \"99.9% good.\" A quality program must cover the dimensions that matter for the use case, not just the cheapest one to check."}
);

Object.assign(GLOSSARY_DATA, {
  "data-quality-dimensions": {"term":"Data Quality Dimensions","group":"GOV-QUALITY","aliases":["data quality dimension","quality dimensions","dimensions of data quality"],"eli5":"A small set of named, measurable ways data can be good or bad — so 'is this data good?' becomes several specific, testable questions.","memory":"Order them A-C-C-T-V-U: Accuracy, Completeness, Consistency, Timeliness, Validity, Uniqueness.","full":"<p>The standard six: <strong>accuracy</strong> (matches reality), <strong>completeness</strong> (nothing missing), <strong>consistency</strong> (agrees across systems and within a row), <strong>timeliness/freshness</strong> (recent enough), <strong>validity</strong> (conforms to format/type/range/allowed-set rules), and <strong>uniqueness</strong> (no unwanted duplicates). Some frameworks add integrity (referential) or relevance.</p><p>The senior move is to bind each dimension to a concrete <em>metric</em> and a <em>threshold</em> on a specific column or grain, then turn it into an automated assertion. 'High quality' is meaningless; 'null rate on <code>user_id</code> &lt; 0.1% and freshness &lt; 30 min' is enforceable.</p>","trap":"Listing dimensions is table stakes. Interviewers probe whether you can turn one into a check with a threshold and the right severity — and whether you know accuracy is the one you usually can't directly test.","worked":"For an <code>orders</code> mart: validity = <code>status in (...)</code>; completeness = <code>order_id not null</code>; uniqueness = <code>order_id</code> distinct; consistency = warehouse order count reconciles to OLTP; freshness = <code>max(created_at)</code> within 1h; accuracy = revenue total reconciles to the billing system.","related":["validity-vs-accuracy","completeness","timeliness-freshness","uniqueness","consistency"]},
  "validity-vs-accuracy": {"term":"Validity vs. Accuracy","group":"GOV-QUALITY","aliases":["validity","valid but wrong","format validity"],"eli5":"Validity is 'follows the rules'; accuracy is 'is actually true' — a value can follow every rule and still be a lie.","memory":"Valid = passes the bouncer; Accurate = actually who the ID says they are.","full":"<p><strong>Validity</strong> checks conformance to a defined rule: data type, format/regex, range, or an allowed-value set. It's cheap, deterministic, and checkable inside the pipeline with no external reference. <strong>Accuracy</strong> checks correspondence to the real world, which requires a trusted source of truth, reconciliation, or human verification.</p><p>The gap between them is where most embarrassing data incidents live: a perfectly valid <code>'1970-01-01'</code> default that's actually a missing timestamp, or a regex-valid email that bounces. Most quality tooling overwhelmingly tests validity because it's automatable, so teams over-report quality by conflating 'valid' with 'good.'</p>","trap":"Saying a regex/format/not-null suite 'ensures the data is correct.' It ensures validity only. If pressed for accuracy you must invoke an external reference or reconciliation — there's no in-table assertion for truth.","worked":"A vendor sends <code>country = 'XX'</code> for unknown. It passes <code>not_null</code> and even an <code>accepted_values</code> list that includes 'XX' — fully valid — but it's inaccurate as a country. Catching it needs a business rule or reconciliation, not a format check.","related":["data-quality-dimensions","validation-as-code","assertion-vs-anomaly"]},
  "completeness": {"term":"Completeness","group":"GOV-QUALITY","aliases":["missing values","null rate","coverage"],"eli5":"Are the values that should be there actually there — across columns, rows, and whole partitions.","memory":"Three grains: cells (nulls), rows (missing entities), batches (volume drop).","full":"<p>Completeness operates at three grains and seniors should name all three: <strong>column</strong> (null/blank rate on a field that should be populated), <strong>row</strong> (expected entities present — e.g., every active customer has a row), and <strong>partition/volume</strong> (today's load has roughly the expected number of rows; a 90% row-count drop is a completeness failure even if every present row is perfect).</p><p>The subtlety is <em>expected</em> emptiness: a nullable optional field at 40% null may be fine; a key field at 0.5% null may be a sev-1. Completeness checks therefore need a column-aware threshold, and volume/row-count anomaly detection covers the 'whole upstream feed silently stopped' case that per-row nulls never catch.</p>","trap":"Only checking column null rates. The classic prod incident is a partition that loaded 10% of normal volume — every row valid, completeness catastrophically failed. Volume/row-count checks are part of completeness.","worked":"Soda: <code>missing_count(user_id) = 0</code> (column) plus <code>row_count between 800000 and 1200000</code> (volume) plus a dbt <code>relationships</code> test ensuring every order's customer exists (row-level coverage).","related":["data-quality-dimensions","assertion-vs-anomaly","data-quality-slo"]},
  "timeliness-freshness": {"term":"Timeliness / Freshness","group":"GOV-QUALITY","aliases":["freshness","data lag","staleness","data latency"],"eli5":"Is the data recent enough to be useful, measured by how far behind 'now' the newest record is.","memory":"Two clocks: did the loader run (load-time) AND did the world's data move (event-time)? Stale needs only one to fail.","full":"<p>Freshness is a pipeline property measured as <strong>lag</strong> = (current time − latest relevant timestamp). Two clocks matter: <em>event-time</em> freshness (the latest real-world event represented) and <em>load/processing-time</em> freshness (when the pipeline last succeeded). A pipeline can run on time yet be stale because the <em>source</em> stopped emitting — only event-time freshness catches that.</p><p>Freshness is the most consumer-visible dimension (a stale dashboard is obvious) and the natural anchor for a data SLA. dbt <code>source freshness</code> (<code>warn_after</code>/<code>error_after</code> on a <code>loaded_at_field</code>) and Soda freshness checks both compare a timestamp column to <code>now()</code> and threshold the gap.</p>","trap":"Equating 'the job ran successfully' with 'the data is fresh.' If upstream stopped emitting, the job happily loads zero new rows — green pipeline, stale data. Always check freshness against an event/source timestamp, not job status.","worked":"dbt source (v1.9+ nests this under <code>config:</code>): <code>freshness: {warn_after: {count: 1, period: hour}, error_after: {count: 6, period: hour}}</code> with <code>loaded_at_field: _ingested_at</code>. Errors block downstream models when lag exceeds 6h.","related":["data-quality-dimensions","data-quality-slo","shift-left"]},
  "consistency": {"term":"Consistency","group":"GOV-QUALITY","aliases":["cross-system consistency","reconciliation","referential consistency"],"eli5":"The same fact agrees everywhere it appears — across systems, and within a single record.","memory":"Two questions: do the systems agree (reconcile), and does the row agree with itself (row rules)?","full":"<p>Two scopes. <strong>Cross-system consistency</strong>: an entity matches across stores — warehouse totals reconcile to the OLTP source, the lake copy equals the streaming copy, referential integrity holds across tables. This is enforced by reconciliation/control-total jobs because the systems can't share a constraint. <strong>Intra-record (logical) consistency</strong>: fields agree within one row — <code>ship_date >= order_date</code>, <code>discount <= price</code>, <code>state</code> matches <code>zip</code> — enforced by row-level assertions.</p><p>In a denormalized lakehouse you also fight <em>copy</em> consistency: the same dimension materialized into many marts drifts. The fix is a single conformed source and tests asserting marts agree with it, not re-deriving the value independently in each model.</p>","trap":"Treating consistency as just foreign-key checks. The senior version is reconciliation: warehouse revenue must tie out to the billing system to a tolerance — a thing no in-table constraint can express.","worked":"Singular dbt test: warehouse <code>sum(amount)</code> for yesterday must be within 0.1% of a control-total table pulled from the source system; returns rows (fails) when the tolerance is breached.","related":["data-quality-dimensions","validity-vs-accuracy","assertion-vs-anomaly"]},
  "uniqueness": {"term":"Uniqueness","group":"GOV-QUALITY","aliases":["deduplication","duplicates","grain test","primary key test"],"eli5":"Each real-world thing appears exactly once at the grain you promised — no unwanted duplicates.","memory":"No grain → no duplicate. Define 'one row per X' first; only then can a second row be 'unwanted.'","full":"<p>Uniqueness is defined relative to the table's <strong>grain</strong> (one row per what?). The check is that the business/primary key — possibly composite — has no repeats: <code>count(*) = count(distinct key)</code>, or dbt's <code>unique</code> / a <code>unique_combination_of_columns</code> test. The word that matters is <em>unwanted</em>: legitimate repeated events (a user logging in twice) aren't defects; a duplicate only exists once you've declared the grain.</p><p>In streaming, true uniqueness is unattainable cheaply, so you approximate with idempotency keys and dedupe windows (keep first by <code>event_id</code> within a watermark). Duplicates are insidious because they silently inflate counts and double-count revenue while every individual row looks valid.</p>","trap":"Asserting <code>unique</code> on the wrong key (e.g., a surrogate that's unique by construction) so the test always passes and never guards the real business grain. Test the business key, or a composite, not the auto-increment id.","worked":"Fact table grain = one row per (<code>order_id</code>, <code>line_item_id</code>). dbt's <code>dbt_utils.unique_combination_of_columns</code> on those two columns fails the build when a re-run double-loads a partition.","related":["data-quality-dimensions","completeness","validation-as-code"]},
  "validation-as-code": {"term":"Validation as Code","group":"GOV-QUALITY","aliases":["data tests","expectations","assertions","great expectations","dbt tests","soda"],"eli5":"Quality checks are versioned, declarative assertions in your repo that run automatically — you test data like you test code.","memory":"GE = DataFrames/files (Python, often at ingestion). dbt = SQL models (at transform). Soda = scheduled monitoring (YAML + service).","full":"<p>Instead of ad-hoc SQL spot-checks, quality rules are codified as suites that live next to the pipeline, get code-reviewed, and run in CI and in production. <strong>dbt tests</strong> are SQL-native and pass when their query returns zero failing rows (built-ins: <code>unique</code>, <code>not_null</code>, <code>accepted_values</code>, <code>relationships</code>; plus generic and singular tests). <strong>Great Expectations</strong> validates pandas/Spark DataFrames and files against an expectation suite and renders Data Docs. <strong>Soda</strong> uses SodaCL YAML checks with a metrics/monitoring service.</p><p>Running checks at <em>boundaries</em> — ingestion and each transformation output — is what makes the data 'tested like code': diffable, reviewable, blocking in CI, and tied to the artifact that produced the data.</p>","trap":"Describing tools without their boundary. 'We use Great Expectations' is weak; 'GE at the raw-ingestion boundary before the warehouse, dbt tests on every model in PR CI, Soda for scheduled freshness/anomaly in prod' shows you place checks deliberately.","worked":"PR opens → CI runs <code>dbt build</code> (models + tests) on a slim/CI dataset; any <code>severity: error</code> test failure fails the check and blocks merge — the exact 'test your data like your code' loop.","related":["shift-left","circuit-break-vs-warn","assertion-vs-anomaly","data-quality-dimensions"]},
  "shift-left": {"term":"Shift-Left Data Quality","group":"GOV-QUALITY","aliases":["shift left","test your data like your code","data contracts"],"eli5":"Catch bad data as early and as upstream as possible — at the producer and in CI — instead of in a dashboard at the end.","memory":"A bad row caught at ingestion is a retry; caught in the CEO's dashboard it's an incident. Push checks toward the producer.","full":"<p>Shift-left moves validation toward the data's origin: <strong>data contracts</strong> at producers (a schema + semantic guarantees the producer commits to), expectations at the ingestion boundary, dbt tests in PR CI on every model change, and freshness/volume monitors in prod. The economics mirror software testing — a defect caught at ingestion costs a retry; the same defect caught after it fan-outs into 30 downstream models costs a multi-team backfill and a trust hit.</p><p>The complementary idea is the <em>boundary</em>: the highest-leverage place to assert is where untrusted data first enters a trusted zone, because a single check there protects everything downstream. Shifting left also stops 'silent' upstream changes (a renamed column, a unit change) from propagating before anyone notices.</p>","trap":"Confusing shift-left with 'more tests everywhere.' It's specifically about <em>earlier/upstream</em> placement and producer-side contracts — not just adding assertions to the final mart where the damage is already done.","worked":"A producer's data contract declares <code>amount</code> is cents (int, &gt;0). A schema/contract check at ingestion rejects a release that switches to dollars-as-float, before it silently 100x's a downstream revenue metric.","related":["validation-as-code","circuit-break-vs-warn","cost-of-bad-data","timeliness-freshness"]},
  "circuit-break-vs-warn": {"term":"Circuit-Break vs. Warn-and-Continue","group":"GOV-QUALITY","aliases":["fail the pipeline","blocking checks","severity error vs warn","quarantine"],"eli5":"On a failed check you either stop the pipeline so bad data can't move, or you let it flow and just alert — and severity should match the blast radius.","memory":"Error = stop the world; Warn = tell someone. Pick by blast radius, and never let 'warn' mean 'nobody looks.'","full":"<p><strong>Circuit-break</strong> (blocking) halts the run so downstream consumers never see bad data — correct at trust boundaries and for business-critical marts. <strong>Warn-and-continue</strong> logs/pages but lets data flow — appropriate for low-severity or noisy checks where stopping causes more harm than the defect. dbt expresses this with test <code>severity: error | warn</code> and <code>error_if</code>/<code>warn_if</code> count thresholds; GE/Soda have equivalent fail vs. warn actions and orchestrators gate on the result.</p><p>A mature middle path is <strong>quarantine</strong>: route failing rows to a side table and pass the clean remainder, so one poison record doesn't block an entire load. The staff-level judgment is mapping severity to <em>blast radius</em> — and ensuring warn-only checks still alert a human, or they silently rot into noise.</p>","trap":"Making everything blocking (one flaky check halts the whole platform and people start ignoring/forcing it) or making everything warn (alert fatigue, bad data ships anyway). The skill is per-check severity tied to downstream impact.","worked":"dbt: <code>not_null</code> on <code>order_id</code> is <code>severity: error</code> (blocks the mart); a <code>warn_if: '>50'</code> threshold on a soft enum keeps the build green but emits a warning the on-call triages.","related":["validation-as-code","shift-left","cost-of-bad-data","data-quality-slo"]},
  "data-quality-slo": {"term":"Data Quality SLAs / SLOs / SLIs","group":"GOV-QUALITY","aliases":["data sla","data slo","data sli","quality slo"],"eli5":"Apply reliability-engineering targets to datasets: measure an indicator, set an internal target, and commit an external promise.","memory":"SLI you measure, SLO you target, SLA you promise. For data the metric is freshness/completeness/volume, not request latency.","full":"<p>Same SLI/SLO/SLA hierarchy as service reliability, but the indicators are <em>data</em> properties. <strong>SLI</strong>: the measurement — freshness lag, % rows passing validity, null rate, row-count vs. expected. <strong>SLO</strong>: an internal target with a window — \"freshness &lt; 30 min for 99% of business hours,\" \"<code>user_id</code> null rate &lt; 0.1%.\" <strong>SLA</strong>: the externally-committed version with consequences for consumers/teams.</p><p>This reframes quality as an observability problem with error budgets: each violation burns budget and triggers action, rather than firefighting. The hard part is choosing indicators that map to consumer pain (a fraud model cares about freshness and completeness of a few features) instead of vanity metrics like 'overall quality score,' which no consumer can act on.</p>","trap":"Reporting a single rolled-up 'data quality score.' Seniors define <em>per-dataset, per-dimension</em> SLOs tied to specific consumer needs — an aggregate score hides the one freshness breach that broke the model.","worked":"Checkout-events dataset SLO: 99% of hours have freshness &lt; 15 min and row_count within ±20% of the 4-week seasonal baseline; breaches burn an error budget reviewed weekly.","related":["timeliness-freshness","completeness","circuit-break-vs-warn","cost-of-bad-data"]},
  "cost-of-bad-data": {"term":"Cost of Bad Data","group":"GOV-QUALITY","aliases":["bad data cost","garbage in garbage out","data trust"],"eli5":"Bad data is expensive in three ways: wrong decisions, broken ML models, and lost trust — and trust is the one you can't easily buy back.","memory":"Three D's: bad Decisions, broken moDels, lost trust. Trust is the one with no rollback.","full":"<p>Three buckets. <strong>Bad decisions</strong>: incorrect KPIs/dashboards steer real business actions (mis-priced, mis-targeted, mis-forecast). <strong>Broken models</strong>: ML silently degrades on bad features — garbage-in propagates to training and serving, and the failure is quiet until metrics drift. <strong>Eroded trust</strong>: the asymmetric cost — one publicly-wrong number and stakeholders abandon the platform for shadow spreadsheets, undoing months of adoption.</p><p>This is the business case that justifies the whole quality program: validation-as-code, SLOs, and circuit-breakers exist to bound these costs. The staff framing is that trust is the platform's actual product — and unlike a latency regression, it doesn't recover the moment you ship a fix.</p>","trap":"Pitching quality as a purely technical cleanliness goal. Interviewers want the business framing: which decisions and models break, and the trust dynamic that makes prevention worth more than the engineering it costs.","worked":"A unit change upstream (dollars→cents) inflates a revenue feature 100x: the exec dashboard misleads a pricing decision, the LTV model retrains on corrupted features, and once finance spots it they rebuild the metric in a spreadsheet and stop trusting the warehouse for a quarter.","related":["shift-left","circuit-break-vs-warn","data-quality-slo","validity-vs-accuracy"]},
  "assertion-vs-anomaly": {"term":"Assertions vs. Anomaly Detection","group":"GOV-QUALITY","aliases":["anomaly detection","volume checks","schema drift","distribution shift"],"eli5":"Assertions test rules you can name; anomaly checks catch the failures you couldn't have predicted by comparing to history.","memory":"Assertions = the rules you can write down. Anomaly = 'this looks different from yesterday.' Known unknowns vs. unknown unknowns.","full":"<p><strong>Assertions/expectations</strong> encode <em>known</em> invariants: <code>amount > 0</code>, key is unique, enum in set. Deterministic, explainable, and the backbone of validation-as-code. <strong>Anomaly detection</strong> catches the <em>unknown</em> by trending metrics over history — sudden row-count drops, schema drift (a new/renamed/retyped column), null-rate spikes, distribution shifts — and flagging deviation rather than a fixed threshold.</p><p>You need both. The most damaging incidents are usually things no one wrote an assertion for: an upstream team silently changed a column's meaning. Anomaly/volume/schema-change monitors (Soda anomaly checks, freshness/volume monitors, contract/schema enforcement) are the safety net for the long tail of 'we didn't know to test for that.'</p>","trap":"Relying only on hand-written assertions. They can't cover failures you didn't anticipate — and 'an upstream change we never tested for' is the most common real incident. Pair assertions with volume/schema/distribution anomaly monitoring.","worked":"Every <code>amount > 0</code> assertion passes, but row count is 8x normal because a join fan-out duplicated rows — only an anomaly/volume check against the historical baseline catches it.","related":["validation-as-code","completeness","validity-vs-accuracy","data-quality-slo"]}
});
