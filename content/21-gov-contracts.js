/* Data Contracts & Schema Evolution (GOV-CONTRACTS) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"GOV-CONTRACTS","front":"What is a data contract, and what does it actually contain?","back":"An explicit, versioned agreement between a data <strong>producer</strong> and its <strong>consumers</strong> that goes beyond schema to cover four things: <strong>schema</strong> (field names/types/nullability), <strong>semantics</strong> (what each field <em>means</em>, units, enums, PII tags), <strong>quality guarantees</strong> (freshness, completeness, uniqueness, accepted ranges), and <strong>SLAs/SLOs</strong> (delivery time, availability, support/deprecation policy). It is checked into version control and enforced in CI, not a wiki page."},
  {"sec":"GOV-CONTRACTS","front":"What core problem does a data contract solve? (the \"shift left\" pitch)","back":"It <strong>shifts breakage left</strong>: a producer can no longer <em>silently</em> change or break a dataset and have downstream pipelines, dashboards, and ML features discover it hours later in production. The contract turns an implicit, discovered-at-runtime coupling into an explicit, tested-at-build-time agreement — breaking changes fail the producer's CI/deploy instead of a consumer's 3am page."},
  {"sec":"GOV-CONTRACTS","front":"Who owns a data contract — producer or consumer? Why is that the load-bearing principle?","back":"The <strong>producer owns the contract</strong>. The team emitting the data is accountable for honoring the schema and quality promises and for managing deprecation. This flips the usual reactive model (consumers reverse-engineering and defensively coding around an upstream table). Producer ownership is what makes enforcement meaningful — the contract gates <em>their</em> deploy, so the cost of a breaking change lands on the team that can prevent it."},
  {"sec":"GOV-CONTRACTS","front":"What is a schema registry and what is its role in enforcing contracts?","back":"A central service that stores versioned schemas (e.g., Avro/Protobuf/JSON Schema) keyed by subject and, on each register call, <strong>checks the new version against the subject's configured compatibility mode</strong>, rejecting incompatible ones. Producers register/validate before publishing; consumers fetch the writer's schema by ID to deserialize. It is the enforcement point that prevents an incompatible schema from being published. Confluent Schema Registry is the canonical example; its default mode is <code>BACKWARD</code>."},
  {"sec":"GOV-CONTRACTS","front":"Define BACKWARD compatibility. Who is protected and what changes are allowed?","back":"<strong>New schema can read data written with the old schema.</strong> Protects <em>consumers</em>: you upgrade readers first, and they can still process old in-flight/historical data. Allowed changes: <strong>delete a field</strong> (new reader just ignores it), and <strong>add an optional field with a default</strong> (the new reader supplies the default for old records lacking it). It is the registry's default because you usually want to roll out new consumer code against an existing backlog (e.g., rewinding to the start of a topic)."},
  {"sec":"GOV-CONTRACTS","front":"Define FORWARD compatibility. Who is protected and what changes are allowed?","back":"<strong>Old schema can read data written with the new schema.</strong> Protects <em>producers</em>/old readers: you upgrade the writer first and existing consumers keep working without redeploying. Allowed changes: <strong>add a field</strong> (old readers ignore unknown new fields), and <strong>delete a field that had a default</strong>. Useful when many consumers can't all upgrade in lockstep with the producer. Note: under Avro/Confluent, FORWARD even permits adding a <em>required</em> field — the inverse of BACKWARD."},
  {"sec":"GOV-CONTRACTS","front":"Define FULL compatibility and when you'd require it.","back":"<strong>FULL = both BACKWARD and FORWARD</strong>: new reads old AND old reads new. The only changes that survive both directions are <strong>adding or removing an optional field that has a default</strong>. Require it when you cannot control deployment order between producer and consumers (independent teams, no coordinated release) — it lets either side upgrade in any order. The tradeoff is the strictest, most limiting set of allowed evolutions."},
  {"sec":"GOV-CONTRACTS","front":"BACKWARD vs FORWARD: which to choose, and the mnemonic for deployment order?","back":"Choose by <strong>who you upgrade first</strong>. <strong>BACKWARD</strong> → upgrade <em>consumers</em> first (new reader handles old data); the registry default. <strong>FORWARD</strong> → upgrade <em>producer</em> first (old readers tolerate new data). Mnemonic: BACKWARD lets you look <em>back</em> at old data with new code; FORWARD lets old code look <em>forward</em> at new data. If you can't dictate order, use FULL."},
  {"sec":"GOV-CONTRACTS","front":"Adding a field: when is it a safe change vs a breaking change?","back":"It depends on the mode. Under <strong>BACKWARD</strong> (the default), adding a field is safe only if it is <strong>optional with a default</strong> — a new reader hitting old data fills the default. Adding a <strong>required field with no default is breaking under BACKWARD</strong> (deserialization of old records fails). Under <strong>FORWARD</strong>, adding even a required field is allowed because old readers ignore it. The interview reflex: \"add a field\" is not automatically safe — say <em>optional + default</em>, and name the compatibility mode."},
  {"sec":"GOV-CONTRACTS","front":"Which schema changes are almost always breaking (under BACKWARD/FULL)?","back":"<ul><li><strong>Remove or rename a field</strong> a consumer reads (rename = remove + add).</li><li><strong>Change a field's type</strong> / re-type it incompatibly.</li><li><strong>Narrow a type</strong> (e.g., <code>long</code>→<code>int</code>, shrinking an enum's allowed symbols, or making a nullable field non-null) — old data may hold values the new type can't represent.</li><li><strong>Add a required field without a default.</strong></li><li>Tighten a constraint (NOT NULL, stricter regex/range) that existing data violates.</li></ul>"},
  {"sec":"GOV-CONTRACTS","front":"Why is widening a type usually safe but narrowing usually breaking?","back":"<strong>Widening</strong> (<code>int</code>→<code>long</code>, <code>float</code>→<code>double</code>, adding enum symbols, making a column nullable) means every existing value still fits the new domain — readers lose nothing. <strong>Narrowing</strong> (<code>long</code>→<code>int</code>, removing enum symbols, NOT NULL on a nullable column) shrinks the domain, so historical values may now be unrepresentable or rejected. Direction-of-domain-change is the rule: grow the set = safe, shrink the set = breaking."},
  {"sec":"GOV-CONTRACTS","front":"How is renaming a field handled safely if a registry treats it as remove+add?","back":"You can't atomically rename under most compatibility modes. The safe pattern is a <strong>two-phase / expand-then-contract migration</strong>: (1) add the new field as optional+defaulted and dual-write both old and new; (2) migrate all consumers to read the new field; (3) only after the deprecation window, remove the old field. Avro <strong>field aliases</strong> can sometimes let a new reader map an old name (alias support is optional per implementation), but the dual-write migration is the general answer."},
  {"sec":"GOV-CONTRACTS","front":"What is contract testing in CI, and what should it gate?","back":"An automated check in the <strong>producer's CI pipeline</strong> that diffs the proposed schema against the registered/published version under the agreed compatibility mode and <strong>fails the build on a breaking change</strong>. It can also run producer-generated sample data against consumer-declared expectations (consumer-driven contracts). The point: the breaking change is caught at PR/merge time by the team causing it, not in production by the team consuming it."},
  {"sec":"GOV-CONTRACTS","front":"What does \"enforcement at the pipeline boundary\" mean in practice?","back":"The contract is validated at the <strong>edge where data crosses team ownership</strong> — the ingestion/publish step — not deep inside transformations. Concretely: a schema-registry check on produce, plus a validation/quarantine gate (e.g., Great Expectations / dbt tests / a write-time schema check) that <strong>rejects or dead-letters non-conforming records</strong> before they land in the published table. Enforcing at the boundary keeps bad data out of the contract-governed asset entirely rather than letting it propagate."},
  {"sec":"GOV-CONTRACTS","front":"Data contract vs. a plain schema / a dbt test — what's the senior distinction?","back":"A schema is <em>structure only</em>; a dbt/Great Expectations test is <em>one quality assertion</em>. A <strong>data contract is the governed, versioned bundle</strong>: structure + semantics + the quality assertions + SLAs + an owner + a compatibility/deprecation policy, enforced at the producer boundary with CI gating. Tests and schemas are <em>mechanisms a contract uses</em>; the contract is the accountable agreement that says who promises what and what happens when it's violated."}
);

Object.assign(GLOSSARY_DATA, {
  "data-contract": {"term":"Data Contract","group":"GOV-CONTRACTS","aliases":["data contracts","contract"],"eli5":"A versioned, enforced promise from the team producing a dataset to the teams consuming it about what the data will look like and how good it will be.","memory":"A contract is a schema with a lawyer, an SLA, and an owner.","full":"<p>A data contract is an explicit, versioned agreement between a data <strong>producer</strong> and its <strong>consumers</strong> spanning four dimensions: <strong>schema</strong> (fields, types, nullability), <strong>semantics</strong> (meaning, units, enum domains, PII classification), <strong>quality guarantees</strong> (freshness, completeness, uniqueness, value ranges), and <strong>SLAs/SLOs</strong> plus a deprecation policy. It lives in source control and is enforced in CI and at the publish boundary.</p><p>Its purpose is to <strong>shift breakage left</strong>: convert an implicit coupling that breaks silently in production into an explicit agreement that fails the producer's build. The producer owns it, so the cost of a breaking change lands on the team that can prevent it.</p>","trap":"Calling any Avro/JSON schema a \"data contract.\" A schema is just the structure; a contract adds semantics, quality SLOs, an accountable owner, a compatibility policy, and CI/boundary enforcement. No enforcement and no owner = not a contract.","worked":"Orders team publishes <code>orders_v2</code>: schema in git, <code>amount_cents</code> documented as integer USD cents (not dollars), guarantees 99.9% rows within 15 min and <code>order_id</code> unique, 30-day deprecation notice — CI rejects any PR that removes a field consumers read.","related":["producer-owns-the-contract","schema-registry","schema-evolution","contract-testing","boundary-enforcement","data-quality-slo"]},
  "producer-owns-the-contract": {"term":"Producer-Owns-the-Contract Principle","group":"GOV-CONTRACTS","aliases":["producer ownership","producer owns the contract","shift left"],"eli5":"The team that emits the data is the one accountable for not breaking it, so the contract gates their deploy, not the consumer's runtime.","memory":"The one who emits it owns it — the cost of breaking lands where it can be prevented.","full":"<p>The principle that accountability for honoring schema, quality, and deprecation belongs to the <strong>producer</strong>. This inverts the default reactive pattern where consumers reverse-engineer an upstream table and defensively code around it, only to be paged when it silently changes.</p><p>Producer ownership is what makes a contract enforceable: because the contract check runs in the producer's CI and blocks <em>their</em> merge, the friction of a breaking change is felt by the team with the power to avoid it or to coordinate a proper deprecation.</p>","trap":"Saying the data platform/governance team owns contracts. Central teams provide the registry, tooling, and policy, but a contract owned by everyone is owned by no one — accountability has to sit with the producing team or enforcement has no teeth.","related":["data-contract","contract-testing","boundary-enforcement"]},
  "forward-compatibility": {"term":"Forward Compatibility","group":"GOV-CONTRACTS","aliases":["forward","forward compatible","forward mode"],"eli5":"Old schema can read new data, so you can upgrade the producer first and not-yet-upgraded consumers keep working.","memory":"FORWARD = old code reads data sent FORWARD from the future → upgrade producer first.","full":"<p><strong>A consumer using the old schema can deserialize data written with the new schema.</strong> It protects <em>producers/old readers</em>: you upgrade the writer first and existing consumers keep functioning without an immediate redeploy, ignoring fields they don't know about.</p><p>Permitted changes: <strong>add a field</strong> (including a required one under Avro/Confluent) and <strong>delete a field that has a default</strong>. Deployment order: <strong>upgrade the producer first</strong>. Useful when many heterogeneous consumers can't all redeploy in lockstep with the producer.</p>","trap":"Thinking forward lets you safely remove any field. FORWARD only tolerates ADDED fields (old readers ignore them) and removal of fields that had a DEFAULT. Removing a no-default field a consumer needs still breaks readers.","worked":"Producer adds <code>fraud_score</code> to the event. An old consumer still on the previous schema deserializes the message and simply ignores the unknown field — forward-compatible, no consumer redeploy needed.","related":["backward-compatibility","full-compatibility","schema-evolution","schema-registry"]},
  "full-compatibility": {"term":"Full Compatibility","group":"GOV-CONTRACTS","aliases":["full","full compatible","full mode"],"eli5":"Both directions hold — new reads old and old reads new — so producers and consumers can upgrade in any order.","memory":"FULL = upgrade in ANY order, but you can only touch optional-with-default fields.","full":"<p><strong>FULL = BACKWARD ∧ FORWARD.</strong> A new reader handles old data and an old reader handles new data, so deployment order between producer and consumers is unconstrained. Use it when independent teams own producer and consumers and you can't coordinate a release sequence.</p><p>The price is the strictest evolution set: essentially the only safe change is <strong>adding or removing an optional field that has a default</strong>. <code>FULL_TRANSITIVE</code> enforces this against all historical versions, not just the previous one.</p>","trap":"Defaulting everything to FULL because it sounds safest. FULL is the most restrictive — it blocks legitimate evolutions like adding a required field. Pick the mode that matches your real deployment-order constraint, not the strictest one reflexively.","worked":"Two unrelated teams consume <code>payments</code> on their own release cadence. Setting the subject to FULL means either side can deploy first; the producer's only safe move is adding/removing optional defaulted fields.","related":["backward-compatibility","forward-compatibility","schema-evolution","schema-registry"]},
  "contract-testing": {"term":"Contract Testing in CI","group":"GOV-CONTRACTS","aliases":["contract testing","contract tests","consumer-driven contracts","ci enforcement"],"eli5":"An automated CI check that diffs your proposed schema against the published one and fails the build if the change would break consumers.","memory":"Contract testing = the registry compatibility check, but run in CI so the build goes red before the bad schema ships.","full":"<p>Automated verification in the <strong>producer's CI pipeline</strong> that compares the candidate schema to the registered/published version under the agreed compatibility mode and <strong>fails the build on a breaking change</strong>. It can extend to <strong>consumer-driven contracts</strong>, where producer sample output is validated against expectations declared by each consumer.</p><p>This is the mechanism that operationalizes \"shift left\": the breaking change is caught at PR/merge time by the producing team, not discovered in production by a downstream team. Combined with the registry, it gives both build-time and runtime enforcement.</p>","trap":"Treating green unit tests as contract coverage. Unit tests verify internal logic; only a compatibility/contract check against the consumer-facing schema catches the cross-team break. A passing test suite says nothing about whether you just broke a downstream consumer.","related":["data-contract","producer-owns-the-contract","boundary-enforcement","schema-registry"]},
  "boundary-enforcement": {"term":"Pipeline-Boundary Enforcement","group":"GOV-CONTRACTS","aliases":["enforcement at the boundary","pipeline boundary","quarantine","dead-letter","write-time validation"],"eli5":"Validate and reject non-conforming data right where it crosses team ownership, so bad records never land in the governed table.","memory":"Stop bad data at the door (dead-letter it), not three joins deep where it's already poisoned ten dashboards.","full":"<p>Enforcing the contract at the <strong>edge where data crosses ownership</strong> — the ingest/publish step — rather than deep inside transformations. Mechanisms: a schema-registry check on produce, plus a validation gate (Great Expectations, dbt tests, write-time schema checks) that <strong>rejects, quarantines, or dead-letters</strong> records violating schema or quality rules before they reach the published asset.</p><p>Enforcing at the boundary keeps the contract-governed table clean by construction; enforcing late lets bad data propagate into downstream models and dashboards before anyone notices, which is far costlier to unwind.</p>","trap":"Logging/alerting on violations but still letting the bad row through. Real enforcement is a blocking gate — reject or quarantine — not an observability dashboard you read after the fact.","worked":"Ingestion validates each event against the registered schema and a freshness/uniqueness check; a row with a malformed <code>amount_cents</code> is routed to a dead-letter topic and the published <code>orders</code> table stays conformant.","related":["data-contract","contract-testing","producer-owns-the-contract","data-quality-slo"]}
});
