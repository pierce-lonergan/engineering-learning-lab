/* Pythonic Idioms, Iterators & Generators (PY-IDIOMS) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"PY-IDIOMS","front":"Generator expression vs list comprehension: when does the memory difference actually matter?","back":"A list comprehension <code>[x*x for x in it]</code> materializes the whole list eagerly; a generator expression <code>(x*x for x in it)</code> is lazy and holds <strong>constant memory</strong>, producing one value per <code>__next__</code>. Use the genexp when you only iterate once and feed a consumer like <code>sum()</code>, <code>any()</code>, or a file write — e.g. <code>sum(x*x for x in big)</code> never builds the list. Reach for the list when you need indexing, <code>len()</code>, or multiple passes (a genexp is exhausted after one)."},
  {"sec":"PY-IDIOMS","front":"When you call a function whose body contains <code>yield</code>, what actually happens?","back":"Nothing runs. Calling a generator function returns a <strong>generator object</strong> immediately; the body executes only when you advance it with <code>next()</code> or iterate. Execution runs to the first <code>yield</code>, suspends with all local state frozen, and resumes there on the next advance. This lazy-start is why side effects inside a generator don't fire until consumption."},
  {"sec":"PY-IDIOMS","front":"Iterable vs iterator: what's the protocol distinction?","back":"An <strong>iterable</strong> implements <code>__iter__</code> and returns a fresh iterator (a list, dict, str). An <strong>iterator</strong> implements both <code>__iter__</code> (returning <code>self</code>) and <code>__next__</code>, which yields the next value or raises <code>StopIteration</code>. Key consequence: a list can be looped many times because each <code>for</code> calls <code>__iter__</code> for a new cursor; an iterator (or generator) is one-shot and stays exhausted once <code>StopIteration</code> fires."},
  {"sec":"PY-IDIOMS","front":"How does a <code>for</code> loop use <code>StopIteration</code>, and why should you never let it leak out of a generator?","back":"<code>for</code> calls <code>iter(obj)</code> once, then <code>next()</code> repeatedly, stopping when <code>StopIteration</code> is signalled. The trap: under PEP 479 (default since Python 3.7) a <code>StopIteration</code> raised <em>inside</em> a generator body is converted to <code>RuntimeError</code> rather than silently ending the generator. So calling <code>next(inner)</code> unguarded inside your generator can blow up — wrap it in <code>try/except StopIteration</code> and <code>return</code> to stop cleanly."},
  {"sec":"PY-IDIOMS","front":"EAFP vs LBYL — give the canonical example and when EAFP genuinely wins.","back":"<strong>LBYL</strong> (look before you leap) checks first: <code>if key in d: d[key]</code>. <strong>EAFP</strong> (easier to ask forgiveness) just tries: <code>try: d[key] except KeyError: ...</code>. EAFP is more Pythonic because the happy path is uncluttered, it avoids duplicating the work the operation already does, and exceptions are cheap <em>when not raised</em>. The stronger argument is atomicity for <strong>external/shared</strong> state — a file, a DB row, or a dict mutated by another thread can change in the <strong>TOCTOU</strong> window between an LBYL check and the use, which EAFP eliminates by acting on the real result."},
  {"sec":"PY-IDIOMS","front":"Why is <code>os.path.exists()</code> before opening a file an anti-pattern?","back":"It's LBYL with a real race window: the file can be deleted or have its permissions changed between the check and the <code>open()</code>, and the check duplicates work <code>open()</code> already does. The EAFP form <code>try: open(p) except (FileNotFoundError, PermissionError):</code> acts on the actual outcome of the operation, not a stale prediction. This generalizes to any external resource (network, DB rows) where another process can intervene."},
  {"sec":"PY-IDIOMS","front":"What contract does a context manager's <code>__exit__</code> have, and how do you suppress an exception?","back":"<code>__enter__</code> runs on entry (its return value binds to <code>as x</code>); <code>__exit__(exc_type, exc_val, exc_tb)</code> runs on exit <strong>whether or not</strong> an exception occurred, guaranteeing deterministic cleanup. If <code>__exit__</code> returns a <em>truthy</em> value, the exception is <strong>swallowed</strong>; returning <code>None</code>/falsy re-raises it. That truthy-return suppression is the classic gotcha — accidentally returning <code>True</code> hides errors."},
  {"sec":"PY-IDIOMS","front":"Write a context manager with <code>@contextlib.contextmanager</code> and explain where cleanup goes.","back":"<code>@contextmanager</code> turns a generator into a CM: code before <code>yield</code> is setup, the yielded value is the <code>as</code> target, and code <strong>after</strong> <code>yield</code> is teardown.<ul><li>Put teardown in a <code>finally:</code> block — otherwise an exception in the body is re-raised at the <code>yield</code> and skips cleanup that follows it.</li><li>It's single-use; for reusable/reentrant CMs write a class with <code>__enter__</code>/<code>__exit__</code>.</li></ul>"},
  {"sec":"PY-IDIOMS","front":"Starred assignment: what do <code>first, *rest = seq</code> and <code>*init, last = seq</code> bind?","back":"The starred name absorbs a <strong>list</strong> of the leftover items (always a list, even if empty). <code>first, *rest = [1,2,3]</code> gives <code>first=1, rest=[2,3]</code>; <code>*init, last = [1,2,3]</code> gives <code>init=[1,2], last=3</code>; <code>a, *mid, b = [1,2,3,4]</code> gives <code>mid=[2,3]</code>. At most one star is allowed, and it works on any iterable, so it's the idiomatic way to peel head/tail without slicing indices."},
  {"sec":"PY-IDIOMS","front":"Why prefer <code>enumerate(seq, start=1)</code> and <code>zip()</code> over manual index loops?","back":"<code>enumerate</code> yields <code>(index, value)</code> pairs lazily, avoiding the error-prone <code>range(len(seq))</code> + index pattern, and <code>start</code> sets the first index. <code>zip()</code> walks multiple iterables in lockstep and <strong>stops at the shortest</strong> — silently truncating mismatched lengths. To catch length mismatches use <code>zip(a, b, strict=True)</code> (Python 3.10+), which raises <code>ValueError</code>; use <code>itertools.zip_longest</code> to pad instead."},
  {"sec":"PY-IDIOMS","front":"What is Python's truthiness protocol, and what's the danger with <code>if not x:</code>?","back":"<code>bool(x)</code> calls <code>__bool__</code>; if absent it falls back to <code>__len__</code> (empty container is falsy); with neither, the object defaults to truthy. Falsy values include <code>0</code>, <code>0.0</code>, <code>''</code>, <code>[]</code>, <code>{}</code>, <code>None</code>, and <code>False</code>. The gotcha: <code>if not x:</code> conflates 'empty' with 'absent' — for a value that could legitimately be <code>0</code> or an empty list, test <code>if x is None:</code> explicitly, since <code>0</code> and <code>[]</code> are both falsy but meaningful."},
  {"sec":"PY-IDIOMS","front":"Give the amortized Big-O for the core built-in containers' hot operations.","back":"<ul><li><strong>list</strong>: index/append O(1) amortized, <code>insert(0,...)</code>/<code>pop(0)</code> O(n), <code>x in list</code> O(n).</li><li><strong>dict / set</strong>: lookup, insert, delete O(1) <em>average</em> (hash), O(n) worst case.</li><li><strong>collections.deque</strong>: append/pop at <strong>both</strong> ends O(1); random index O(n).</li></ul>Staff-level point: <code>x in mylist</code> is O(n) — for membership tests on large data, convert to a <code>set</code> first."},
  {"sec":"PY-IDIOMS","front":"Why is building a list then returning it often worse than <code>yield</code> for a streaming pipeline?","back":"Returning a list forces the whole result into memory and blocks until fully computed; a generator with <code>yield</code> streams one item at a time, so a downstream consumer can start work immediately and peak memory stays flat regardless of input size. This is the backbone of composable pipelines — chaining genexps / <code>itertools</code> stages processes huge inputs lazily. The cost: the generator is one-shot and you lose <code>len()</code> / random access."},
  {"sec":"PY-IDIOMS","front":"What does <code>yield from subgen</code> do beyond a manual re-yield loop?","back":"<code>yield from</code> delegates: it yields every item from the sub-iterable, and crucially forwards <code>send()</code>, <code>throw()</code>, and the sub-generator's <strong>return value</strong> (captured via <code>StopIteration.value</code>) back to the delegating generator. A hand-rolled <code>for x in sub: yield x</code> loses the bidirectional plumbing and the return value. It's the idiom for flattening nested generators and composing coroutine-style pipelines."},
  {"sec":"PY-IDIOMS","front":"Dict/set comprehension syntax and the one-pass dedup idiom — what are the forms?","back":"Dict: <code>{k: v for k, v in pairs}</code>; set: <code>{f(x) for x in it}</code>. Braces with a colon give a dict, without a colon give a set (note <code>{}</code> is an empty <em>dict</em>, not a set — use <code>set()</code>). A set comprehension is the idiomatic one-pass dedup; if you need to preserve first-seen order, use <code>dict.fromkeys(it)</code> instead, since dicts have kept insertion order since Python 3.7."}
);

Object.assign(GLOSSARY_DATA, {
  "generator-expression": {"term":"Generator Expression","group":"PY-IDIOMS","aliases":["genexp","generator expression","lazy comprehension"],"eli5":"A comprehension that produces values one at a time on demand instead of building the whole list in memory.","memory":"Parentheses = pipe (flows one at a time); square brackets = bucket (holds everything).","full":"<p>A generator expression has comprehension syntax in parentheses, e.g. <code>(f(x) for x in it if pred(x))</code>, and evaluates to a <strong>generator object</strong>: lazy, single-pass, constant-memory. Each <code>next()</code> runs the loop one more step. When a genexp is the sole argument to a call you can drop the inner parens: <code>sum(x*x for x in nums)</code>.</p><p>Unlike a list comprehension it never materializes all results, so it scales to unbounded or huge streams — but it has no <code>len()</code>, no indexing, and is exhausted after one pass. The iteration variable is scoped to the expression and does <em>not</em> leak into the enclosing namespace (genexps never leaked, even in Python 2; it was list comprehensions that leaked in Py2 and were fixed in Py3).</p>","trap":"Interviewers ask 'what does <code>list(g)</code> return the second time?' — an empty list, because the generator is already exhausted. Re-create the genexp to iterate again.","worked":"<code>total = sum(len(line) for line in open('huge.log'))</code> streams a multi-GB file with flat memory; the list-comp version would try to hold every line length at once.","related":["generator-function","iterator-protocol"]},
  "generator-function": {"term":"Generator Function (yield)","group":"PY-IDIOMS","aliases":["generator","yield","generator function"],"eli5":"A function with yield that pauses and resumes, handing back one value each time you ask for the next.","memory":"yield = 'pause here and hand one out'; the function is a bookmark, not a list.","full":"<p>Any function containing <code>yield</code> is a generator function: calling it returns a generator object without running the body. Each advance runs until the next <code>yield</code>, then <strong>suspends with all locals frozen</strong>, resuming there on the following <code>next()</code>. This makes generators ideal for lazy streaming pipelines and for expressing producer logic without callbacks.</p><p>Generators are also primitive coroutines: <code>gen.send(v)</code> resumes the <code>yield</code> expression with value <code>v</code>, <code>gen.throw(exc)</code> raises inside it, and <code>gen.close()</code> triggers <code>GeneratorExit</code>. A bare <code>return</code> (or running off the end) raises <code>StopIteration</code>, and a <code>return value</code> stashes <code>value</code> in <code>StopIteration.value</code>.</p>","trap":"Under PEP 479 (default since 3.7) a <code>StopIteration</code> raised inside the body becomes a <code>RuntimeError</code> — so an unguarded <code>next(other)</code> inside your generator can crash it instead of ending it.","worked":"<code>def reader(f):\\n    for line in f:\\n        yield line.strip()</code> — yields cleaned lines lazily; the file is read incrementally as the consumer pulls.","related":["generator-expression","iterator-protocol","yield-from","stopiteration"]},
  "stopiteration": {"term":"StopIteration / PEP 479","group":"PY-IDIOMS","aliases":["StopIteration","PEP 479","stop iteration"],"eli5":"The signal an iterator raises to say 'I'm out of items', which the for loop catches silently.","memory":"479: 'StopIteration shouldn't sneak out of a generator' — it gets re-stamped as RuntimeError.","full":"<p><code>StopIteration</code> is how <code>__next__</code> reports exhaustion; <code>for</code> and comprehensions stop on it to terminate. A generator that returns a value places it on <code>StopIteration.value</code>, which <code>yield from</code> surfaces.</p><p><strong>PEP 479</strong> (opt-in via <code>from __future__ import generator_stop</code> in 3.5/3.6, mandatory from 3.7) changed a dangerous behavior: previously a <code>StopIteration</code> that escaped a generator body would silently end the generator, masking bugs. Now it is converted to <code>RuntimeError</code>. So if you call <code>next(inner)</code> inside a generator and want to stop, you must catch <code>StopIteration</code> and <code>return</code> explicitly.</p>","trap":"<code>def g():\\n    while True:\\n        yield next(other)</code> looks like it ends when <code>other</code> ends — pre-3.7 it did; 3.7+ it raises <code>RuntimeError</code>. Wrap <code>next</code> in <code>try/except StopIteration: return</code>.","related":["iterator-protocol","generator-function","yield-from"]},
  "eafp": {"term":"EAFP vs LBYL","group":"PY-IDIOMS","aliases":["EAFP","LBYL","easier to ask forgiveness","look before you leap","try except"],"eli5":"Just try the operation and handle the exception, rather than pre-checking everything that could go wrong.","memory":"LBYL asks 'may I?', then someone changes the answer before you move; EAFP just goes and apologizes if it trips.","full":"<p><strong>EAFP</strong> ('easier to ask forgiveness than permission') wraps the operation in <code>try/except</code>; <strong>LBYL</strong> ('look before you leap') guards it with prior <code>if</code> checks. Python favors EAFP because exceptions are cheap on the non-raising path (raising itself is not free), the happy path stays uncluttered, and it avoids duplicating the work the operation does anyway.</p><p>The deeper reason is <strong>atomicity for external or shared state</strong>: LBYL introduces a TOCTOU (time-of-check-to-time-of-use) window where a file deleted by another process, a row changed in the DB, or a dict mutated by another thread can invalidate the check before you act. EAFP acts on the actual outcome, eliminating that race. (A purely local single-threaded <code>if k in d</code> isn't itself racy, but the EAFP form is still preferred for clarity and speed.)</p>","trap":"Make the <code>try</code> block as narrow as possible and catch a <em>specific</em> exception (<code>KeyError</code>, not bare <code>except:</code>) — a broad try can swallow an error from a different line and mask real bugs.","worked":"EAFP: <code>try: return cache[k]\\nexcept KeyError: cache[k] = compute(k); return cache[k]</code> — no race between a separate <code>in</code> check and the read.","related":["context-manager","truthiness"]},
  "context-manager": {"term":"Context Manager","group":"PY-IDIOMS","aliases":["context manager","with statement","__enter__","__exit__","contextlib","with"],"eli5":"A 'with' block that guarantees setup runs on entry and cleanup runs on exit, even if an error is thrown.","memory":"with = a try/finally with a name — enter sets up, exit always tears down.","full":"<p>The <code>with</code> statement calls <code>__enter__</code> (binding its return to <code>as x</code>) and guarantees <code>__exit__(exc_type, exc_val, exc_tb)</code> runs on exit — normal, <code>return</code>, <code>break</code>, or exception — giving <strong>deterministic, non-GC-dependent cleanup</strong> for files, locks, transactions, and connections. If <code>__exit__</code> returns truthy the exception is suppressed; falsy/<code>None</code> re-raises it.</p><p><code>contextlib.contextmanager</code> lets you write one with a generator: pre-<code>yield</code> = setup, post-<code>yield</code> = teardown (put it in <code>finally</code>). <code>contextlib.ExitStack</code> manages a dynamic number of CMs, <code>contextlib.suppress(Exc)</code> is a clean EAFP ignore, and <code>contextlib.closing</code> wraps objects that only have <code>close()</code>.</p>","trap":"A generator-based CM whose teardown is <em>not</em> in a <code>finally</code> will skip cleanup if the body raises, because the exception is re-thrown at the <code>yield</code>. Also: returning <code>True</code> from <code>__exit__</code> silently eats every exception.","worked":"<code>@contextmanager\\ndef tx(conn):\\n    conn.begin()\\n    try:\\n        yield conn\\n        conn.commit()\\n    except:\\n        conn.rollback(); raise</code>","related":["eafp","generator-function"]},
  "comprehensions": {"term":"Comprehensions (list/dict/set)","group":"PY-IDIOMS","aliases":["list comprehension","dict comprehension","set comprehension","comprehension"],"eli5":"A compact one-expression way to build a list, dict, or set by transforming and filtering an iterable.","memory":"Brackets pick the type: [] list, {} with colon dict, {} without colon set, () lazy generator.","full":"<p>List <code>[f(x) for x in it if p(x)]</code>, set <code>{f(x) for x in it}</code>, dict <code>{k: v for k, v in it}</code> — all eager, building the full container. They read as 'transform then filter' and are faster than equivalent <code>for</code>+<code>append</code> loops because the build loop runs in C and avoids repeated method lookups.</p><p>The loop variable is scoped to the comprehension (no leak in Py3). Beware deeply nested or condition-heavy comprehensions — past two clauses, a plain loop is more readable. For lazy/streaming needs, switch the brackets to parens for a generator expression.</p>","trap":"<code>{}</code> is an empty <strong>dict</strong>, not a set — there is no empty-set literal; use <code>set()</code>. Also a set/dict comprehension silently dedups on value/key, which can drop rows you expected to keep.","worked":"Invert a mapping: <code>{v: k for k, v in d.items()}</code> — but duplicate values collapse, keeping only the last key seen.","related":["generator-expression","container-big-o"]},
  "container-big-o": {"term":"Built-in Container Big-O","group":"PY-IDIOMS","aliases":["list","dict","set","deque","big-o","amortized complexity","collections.deque"],"eli5":"Knowing which container gives O(1) for your hot operation so you don't accidentally write O(n^2) code.","memory":"Ends-fast = deque, lookup-fast = set/dict, index-fast = list, front-ops-slow = list.","full":"<p><strong>list</strong>: append and index O(1) amortized (append occasionally reallocates), but <code>insert(0)</code>/<code>pop(0)</code> and membership <code>x in lst</code> are O(n). <strong>dict</strong>/<strong>set</strong>: average O(1) lookup/insert/delete via hashing, O(n) pathological; dicts preserve insertion order (guaranteed since 3.7). <strong>collections.deque</strong>: O(1) append/pop at both ends (ideal for queues and sliding windows), but O(n) random indexing.</p><p>The staff-interview move is spotting an accidental O(n^2): repeated <code>x in big_list</code> inside a loop, or <code>list.pop(0)</code> as a queue. Convert the list to a <code>set</code> for membership, or use a <code>deque</code> for FIFO.</p>","trap":"'Filter list A against list B' done as <code>[x for x in A if x in B]</code> is O(len(A)*len(B)); making B a <code>set</code> first drops it to O(len(A)).","worked":"Sliding-window max over a stream: a <code>deque</code> of candidate indices gives amortized O(1) per element; a list with <code>pop(0)</code> would be O(n) per step.","related":["comprehensions","truthiness"]},
  "truthiness": {"term":"Truthiness","group":"PY-IDIOMS","aliases":["truthiness","falsy","truthy","__bool__","__len__"],"eli5":"Python treats empty/zero/None as false in conditionals, which is handy but bites when 0 is a real value.","memory":"Empty and zero are 'false'; 'falsy' is not the same as 'None'.","full":"<p>In a boolean context Python calls <code>__bool__</code>, falling back to <code>__len__</code> (length 0 = falsy), and defaults to truthy if neither exists. Built-in falsy values: <code>None</code>, <code>False</code>, <code>0</code>, <code>0.0</code>, <code>0j</code>, <code>''</code>, <code>[]</code>, <code>{}</code>, <code>set()</code>, <code>range(0)</code>. This enables idioms like <code>if items:</code> to mean 'non-empty'.</p><p>The danger is conflating 'empty/zero' with 'missing'. For arguments that can legitimately be <code>0</code>, <code>0.0</code>, or <code>[]</code>, default them to <code>None</code> and test <code>if x is None:</code> — using <code>if not x:</code> would wrongly trigger on the valid zero/empty value. This bug is rampant in config and pandas code.</p>","trap":"<code>def f(timeout=None):\\n    timeout = timeout or 30</code> turns a deliberate <code>timeout=0</code> into 30. Use <code>if timeout is None: timeout = 30</code> instead.","related":["eafp","container-big-o"]},
  "yield-from": {"term":"yield from / Delegation","group":"PY-IDIOMS","aliases":["yield from","delegation","subgenerator"],"eli5":"Re-yield everything from another generator while also passing through send/throw and capturing its return value.","memory":"'yield from' = 'splice this whole sub-stream in here, plumbing and all'.","full":"<p><code>yield from subiter</code> yields every item the sub-iterable produces, but unlike a manual <code>for x in sub: yield x</code> it also <strong>delegates the coroutine channel</strong>: <code>send()</code> and <code>throw()</code> from the caller reach the sub-generator, and the sub-generator's <code>return value</code> becomes the result of the <code>yield from</code> expression (pulled from <code>StopIteration.value</code>).</p><p>It is the idiom for flattening nested generators and for composing generator-based pipelines or legacy coroutines. (Modern async uses <code>async/await</code>, but <code>yield from</code> remains the right tool for plain generator delegation and for collecting a sub-generator's return.)</p>","trap":"<code>result = yield from gen()</code> captures <code>gen</code>'s <code>return</code> value — a manual re-yield loop throws that value away. Interviewers probe exactly this difference.","worked":"<code>def flatten(nested):\\n    for sub in nested:\\n        yield from sub</code> — flattens a list of iterables one level, streaming and lazy.","related":["generator-function","stopiteration"]}
});
