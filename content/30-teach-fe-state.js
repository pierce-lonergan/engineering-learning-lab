/* Teach me from the ground up: FE-STATE (auto-assembled) */
enh({
  "server-state": {"teach":"<p>At its core, <strong>server state</strong> is just data that doesn't really belong to your app — it lives on someone else's computer, and your screen is showing a <em>photocopy</em> of it.</p><h4>The borrowed-book mental model</h4><div class=\"analogy\">Imagine borrowing a library book and photocopying a page to read at home. The moment you walk out the door, the library could update that page — fix a typo, add a correction. Your photocopy is now potentially out of date, and you have no way of knowing without going back to check. That copy is <em>stale the instant you make it</em>.</div><p>That is exactly what happens when you <code>fetch</code> a user profile. The real profile lives in a database. Other users, background jobs, even the same user on another device can change it. Your component is holding a photocopy.</p><h4>Why this is categorically different from client state</h4><p>Let's dissect the formal claim: server state is <em>fetched asynchronously, potentially shared and mutated by other users, and stale the instant you receive it.</em></p><ul><li><strong>Asynchronous</strong> — you don't have it now; you have a <em>promise</em> of it later, plus the in-between states of loading and error.</li><li><strong>Shared and mutated by others</strong> — you are not the only writer, so you can never assume your copy is authoritative.</li><li><strong>Stale on arrival</strong> — even a perfect fetch is a snapshot of one moment in the past.</li></ul><p>Contrast that with <strong>client state</strong> (is the menu open?). Your code is the <em>only</em> writer, so it's always synchronously correct. Opposite ownership, opposite freshness.</p><h4>Why it needs a dedicated library</h4><p>Because the photocopy goes stale, you need machinery: caching (don't re-fetch what you just got), request deduplication (ten components asking for the same user = one network call), background revalidation (quietly check for a fresher copy), retries, and invalidation (mark copies dirty after a write). Hand-rolling all of that in a Redux reducer is how the 2010s frontend drowned in boilerplate.</p><p>The big realization of the early 2020s: <strong>most of what teams stuffed into a global Redux store was mis-handled server state.</strong> Pull it out, hand it to a server-cache library (React Query, SWR, RTK Query), and the manual loading/error/cache code evaporates.</p><h4>What that looks like</h4><p>With React Query you stop writing <em>how to manage a cache</em> and start writing <em>what the data is</em>:</p><ul><li><code>queryKey: ['user', id]</code> — the identity of this resource.</li><li><code>queryFn: () =&gt; fetch(...)</code> — how to fetch it once.</li></ul><p>The library owns the cache, the dedupe, the refetch-on-focus. You just declared identity and a fetcher.</p><div class=\"key\">Server state is a borrowed copy of data whose source of truth lives elsewhere — it goes stale instantly, so describe it (key + fetcher) and let a server-cache library own the freshness, rather than copying it into a store you babysit by hand.</div>"},
  "client-state": {"teach":"<p>At its core, <strong>client state</strong> is just the stuff your UI keeps track of that no server will ever ask about — like whether a dropdown is currently open.</p><h4>The light-switch model</h4><div class=\"analogy\">Think of a light switch on your wall. <em>You</em> flipped it, so you know with absolute certainty whether the light is on or off — you don't have to phone anyone to find out, and nobody else can flip it behind your back. Client state is that switch: your code is the only hand on it, so it is always instantly, perfectly current.</div><p>Examples: the value typed into a text input, the open/closed flag of a modal, which tab is selected, a dark-mode toggle. None of these have a copy living on a backend. There's no source of truth to drift away from, because <em>your code is</em> the source of truth.</p><h4>Why \"synchronous\" is the whole point</h4><p>Recall that server state is asynchronous and stale-on-arrival. Client state is the mirror image: <strong>synchronous</strong> (the value is available the instant you read it) and <strong>always current</strong> (the only writer is you). That difference in ownership and freshness is why these two kinds of state want completely different tools.</p><h4>Scaling it up and down</h4><p>The art of client state is matching the tool to the blast radius:</p><ul><li><strong>Local to one component</strong> → <code>useState</code>. A dropdown's <code>open</code> flag lives right where it's used.</li><li><strong>Shared across a subtree</strong> → Context, or a focused store like Zustand or Jotai.</li><li><strong>Truly global and complex</strong> → Redux Toolkit.</li></ul><p>The trap runs in the <em>up</em> direction. Reaching for a global Redux store to hold a single input's value is <strong>premature globalization</strong>: you add a slice, an action, a selector, and a layer of indirection to manage something one <code>useState</code> handled for free. Global stores earn their keep when state is genuinely shared and its updates are genuinely complex — not before.</p><h4>The cardinal rule: don't mix the two</h4><p>The single most important design move is to keep client state and server state <em>separate</em>. They have opposite freshness (always-current vs stale-on-arrival) and opposite ownership (you vs the backend). If you cram a fetched user list into the same store as your \"is sidebar open\" flag, you inherit the worst of both worlds — manual cache logic for the server half, and a heavyweight store for the trivial half.</p><div class=\"analogy\">It's like storing your house keys and a library book in the same drawer and applying library due-date rules to both. The keys (client state) are always yours and never \"due\"; the book (server state) needs returning and renewing. Different rules, different drawers.</div><div class=\"key\">Client state is UI-owned, synchronous, always-current state that no server cares about — reach for the smallest tool that fits (<code>useState</code> first), and never blend it with server state, which has the opposite ownership and freshness.</div>"},
  "stale-while-revalidate": {"teach":"<p>At its core, <strong>stale-while-revalidate</strong> is just this: show the old data <em>instantly</em>, then quietly go fetch a fresh copy in the background and swap it in if it changed.</p><h4>The leftovers-in-the-fridge model</h4><div class=\"analogy\">You come home hungry. There's food in the fridge from yesterday — maybe not the freshest, but edible <em>right now</em>. You eat it immediately instead of standing at the door waiting for a grocery delivery. Meanwhile, the delivery is on its way; when it arrives you restock. You never went hungry, and you ended up with fresh food anyway. That's SWR: serve the stale leftovers now, restock in the background.</div><p>The alternative — blocking the screen with a spinner on every revisit while you wait for the network — is making yourself starve at the fridge door because the food isn't <em>brand new</em>.</p><h4>Where the name comes from</h4><p>The phrase predates React. It's an HTTP <code>Cache-Control</code> directive from <strong>RFC 5861</strong>: a server can say \"this response is fresh for X seconds, but for Y more seconds after that you may serve it <em>stale</em> while you revalidate in the background.\" React Query and SWR (the library, named after the strategy) brought the same idea into your component tree.</p><h4>The two knobs you must not confuse</h4><p>This is where most engineers stumble, so let's dissect it carefully. There are two independent timers:</p><ul><li><strong><code>staleTime</code></strong> — how long fetched data counts as <em>fresh</em>. While fresh, React Query will <em>not</em> kick off a background refetch. Default is <code>0</code>, meaning data is considered stale immediately.</li><li><strong><code>gcTime</code></strong> (formerly <code>cacheTime</code>) — how long an <em>unused/inactive</em> cache entry survives before it's garbage-collected and forgotten entirely.</li></ul><p>They answer different questions. <code>staleTime</code>: \"may I refetch this in the background?\" <code>gcTime</code>: \"may I throw this cache entry away?\" A query can be stale but still cached (so you serve it then refetch), or fresh but about to be GC'd. They're orthogonal — conflating them is the classic trap.</p><h4>What triggers the background revalidation</h4><p>Once data is past its <code>staleTime</code>, React Query revalidates on any of: window refocus, network reconnect, the query key changing, a stale query mounting again, or an explicit <code>invalidateQueries</code> call.</p><h4>Worked intuition</h4><p>With <code>useQuery({ queryKey: ['todos'], queryFn, staleTime: 30_000 })</code>: for 30 seconds, every revisit serves the cached list with <em>zero</em> network traffic. After 30 seconds it's stale — so the next revisit still shows the cached list instantly, but now also fires a background refetch and swaps in any changes. Fast first paint, eventual consistency, no spinner.</p><div class=\"key\">SWR serves stale-but-instant data and refreshes in the background — and the two timers are independent: <code>staleTime</code> decides when a background refetch is <em>allowed</em>, <code>gcTime</code> decides when an unused cache entry is <em>discarded</em>.</div>"},
  "query-key": {"teach":"<p>At its core, a <strong>query key</strong> is just a label — a name tag you stick on a piece of cached server data so the library knows where to file it and when to reuse it.</p><h4>The coat-check ticket model</h4><div class=\"analogy\">At a coat check, you hand over your coat and get a numbered ticket. The number <em>is</em> the identity of your coat — it's how the clerk finds it again. If two people are somehow given the <em>same</em> number, chaos: one walks off with the other's coat. A query key is that ticket number for cached data. Get the number right and everyone gets their own coat back; let two different requests share a number and you'll hand someone the wrong data.</div><h4>What a key actually is</h4><p>A query key is a <em>serializable</em> array that React Query uses to address a cache entry, like <code>['todos', { status: 'done' }]</code>. Three things happen by key:</p><ul><li><strong>Storage</strong> — results are filed under their key.</li><li><strong>Deduplication</strong> — if ten components ask for the same key at once, they collapse into <em>one</em> in-flight network request, and all share the result.</li><li><strong>Refetch &amp; invalidation</strong> — background revalidation and <code>invalidateQueries</code> target entries by key.</li></ul><p>Object members inside a key are hashed <em>deterministically</em>, so <code>{ a: 1, b: 2 }</code> and <code>{ b: 2, a: 1 }</code> produce the same key — order doesn't matter. Arrays, however, are positional.</p><h4>The one rule that prevents disaster</h4><p>Here's the load-bearing principle: <strong>the key must include every variable the query function depends on.</strong> If <code>queryFn</code> reads <code>userId</code> and <code>tab</code>, then both must be in the key.</p><p>Why? Because React Query identifies cached data <em>only</em> by its key. If you fetch user 42 under the key <code>['user']</code> and later fetch user 99 under the same <code>['user']</code> key, the second request sees a populated cache slot and may serve you user 42's data. That's a <strong>cache collision</strong> — two distinct requests sharing one slot, rendering the wrong thing. It's the coat-check disaster.</p><h4>Getting it right</h4><p>Use <code>['user', userId, { tab }]</code>. Now when <code>userId</code> goes from 42 to 99, or <code>tab</code> changes, the key changes too — so React Query treats it as a genuinely distinct resource, files it separately, and refetches as needed. The key becomes a reactive dependency list: change an input, change the key, get fresh data automatically.</p><div class=\"analogy\">Think of the key like a function's full argument list baked into a memoization cache. Leave out an argument and the memoizer returns a stale result computed for different inputs. Same bug, same fix: capture <em>all</em> the inputs.</div><div class=\"key\">A query key is the unique identity of a cached resource — include <em>every</em> variable the fetch depends on, or two different requests will collide on one cache slot and you'll render the wrong data.</div>"},
  "optimistic-update": {"teach":"<p>At its core, an <strong>optimistic update</strong> is just acting as if your save already worked — updating the screen the instant the user clicks — and quietly undoing it if the server later says no.</p><h4>The restaurant-order model</h4><div class=\"analogy\">A good waiter repeats your order back and writes it down <em>before</em> walking to the kitchen — they act as if it'll be fulfilled. Most of the time it is, and you felt served instantly. But if the kitchen is out of the dish, the waiter comes back and corrects it: \"Sorry, we're out — let me undo that.\" The optimistic part is assuming success; the professionalism is having a clean way to walk it back.</div><p>That walk-back is not optional. An optimistic update <em>without</em> rollback is just a lie your UI tells that it can never retract.</p><h4>The lifecycle, dissected</h4><p>React Query gives you three mutation hooks that map exactly onto the waiter's job:</p><ul><li><strong><code>onMutate</code></strong> (before the request) — do three things in order: <code>cancelQueries</code> to stop any in-flight background refetch, <em>snapshot</em> the current cache with <code>getQueryData</code>, then <code>setQueryData</code> to write your predicted result. Return the snapshot so later hooks can see it.</li><li><strong><code>onError</code></strong> (the kitchen said no) — restore the snapshot: <code>setQueryData(k, ctx.prev)</code>. The optimistic write vanishes as if it never happened.</li><li><strong><code>onSettled</code></strong> (regardless of outcome) — <code>invalidateQueries</code> to refetch and reconcile with the server's <em>real</em> answer, so your guess is replaced by ground truth.</li></ul><h4>Why <code>cancelQueries</code> is the subtle, critical step</h4><p>This is the trap people skip. Imagine a background refetch was already on its way (SWR fired one) when you optimistically wrote your new value into the cache. If you <em>don't</em> cancel it, that in-flight request can resolve <em>after</em> your optimistic write — and since it carries the <em>old</em> server data, it overwrites your fresh optimistic value with stale data. The UI flickers backward.</p><div class=\"analogy\">It's like the waiter writing down your corrected order, but an earlier ticket they forgot to tear up still goes to the kitchen — and the old dish arrives anyway, stomping on your change. <code>cancelQueries</code> is tearing up the stale ticket.</div><h4>When to make this bet</h4><p>Optimistic updates are a wager on success, so use them where success is overwhelmingly likely and instant feedback matters most: toggling a like, checking a checkbox, flipping a setting. For risky or slow writes where failure is common, a plain loading state is honest and safer.</p><div class=\"key\">Optimistic updates write the predicted result into the cache before the server confirms — but they're only safe with the full ritual: <code>cancelQueries</code> + snapshot in <code>onMutate</code>, restore in <code>onError</code>, invalidate in <code>onSettled</code>. Skip the cancel, and a stale in-flight refetch will clobber your update.</div>"},
  "pure-reducer": {"teach":"<p>At its core, a <strong>reducer</strong> is just a function with a very simple job: hand it the current state and a description of something that happened, and it hands you back the new state. <code>(state, action) =&gt; newState</code>. That's the whole shape.</p><h4>The vending-machine model</h4><div class=\"analogy\">Picture a vending machine. Its current state is \"$0.75 inserted.\" You perform an action: \"insert a quarter.\" The machine computes a new state: \"$1.00 inserted.\" Same starting balance plus same coin <em>always</em> gives the same result — it doesn't matter what time it is, what the weather's like, or how many people used it before you. The machine is predictable, repeatable, and has no secret moods. A pure reducer is that vending machine.</div><h4>Let's dissect \"pure\"</h4><p>\"Pure\" is doing a lot of work in that sentence. It bundles three promises:</p><ul><li><strong>Deterministic</strong> — same <code>(state, action)</code> in, same <code>newState</code> out, every time. No <code>Date.now()</code>, no <code>Math.random()</code>, no reading from outside.</li><li><strong>No mutation of arguments</strong> — you never modify the <code>state</code> you were handed. You build and return a <em>new</em> object: <code>return { ...state, items: [...state.items, payload] }</code>.</li><li><strong>No side effects</strong> — no network fetches, no logging to a server, no dispatching other actions. The function does nothing but compute and return.</li></ul><p>Side effects don't disappear — they get <em>pushed out</em> to middleware and thunks, which sit around the reducer and handle the messy outside world. The reducer stays pristine.</p><h4>Why purity is a superpower, not a chore</h4><p>Because a pure reducer is just \"old state + action → new state,\" you get remarkable abilities for free:</p><ul><li><strong>Replayable action logs</strong> — record every action and you can reconstruct any state by re-running them.</li><li><strong>Time-travel debugging</strong> — step backward and forward through state because each step is a pure recomputation.</li><li><strong>Trivial testing</strong> — no mocks, no setup; just <code>expect(reducer(state, action)).toEqual(expected)</code>.</li><li><strong>Safe concurrent rendering</strong> — React can call your logic without fear of hidden side effects firing twice.</li></ul><p>Returning a <em>new</em> immutable object is also what makes change detection cheap: React and Redux compare by reference (<code>prev === next</code>). New object ⇒ reference changed ⇒ re-render. Mutate in place and the reference stays equal — so the UI silently fails to update <em>and</em> time-travel breaks.</p><h4>The Immer footnote</h4><p>Inside Redux Toolkit's <code>createSlice</code>, you'll see mutating-looking code like <code>state.items.push(x)</code>. That isn't a violation — RTK runs your reducer inside <strong>Immer</strong>, which records your \"mutations\" against a draft and produces a brand-new immutable object behind the scenes. The mental model is still pure; Immer just makes the syntax pleasant.</p><div class=\"key\">A reducer is a pure <code>(state, action) =&gt; newState</code> function — deterministic, no argument mutation, no side effects. That purity is exactly what buys you replay, time-travel, easy tests, and reliable reference-based change detection; outside Immer, mutating the state argument silently breaks all of it.</div>"},
  "redux-toolkit": {"teach":"<p>At its core, <strong>Redux Toolkit (RTK)</strong> is just Redux with all the tedious parts done for you — the official, batteries-included way to write Redux so you stop hand-typing action constants and spread pyramids.</p><h4>First, clear up the biggest misconception</h4><p>RTK is <em>not</em> a competing library you can \"skip because we use plain Redux.\" RTK <em>is</em> how the Redux team tells you to use Redux. Saying \"we use plain Redux, not RTK\" in 2026 is like saying \"we write raw SQL strings, not an official, sanctioned query builder the database vendor ships and recommends.\" Hand-written Redux — manual action type constants, giant <code>switch</code> reducers, nested spread operators — is now considered <strong>legacy</strong>. New Redux code should be RTK.</p><div class=\"analogy\">Think of plain Redux as building furniture from raw lumber: you cut every joint, sand every edge, and there's a lot of room to slice your thumb. RTK is the same furniture as a well-designed flat-pack kit — same end result, pre-cut pieces, far fewer ways to hurt yourself. Both produce a Redux store; one just spares you the boilerplate and the bugs.</div><h4>The pieces, in plain terms</h4><ul><li><strong><code>createSlice</code></strong> — the workhorse. You give it a name, initial state, and reducer functions; it generates the matching <em>action creators and reducer together</em>, so you never write action-type strings by hand. Crucially, it runs your reducer bodies inside <strong>Immer</strong>, so you can write mutating-looking code like <code>state.push(a.payload)</code> and still get a correct immutable update.</li><li><strong><code>configureStore</code></strong> — sets up the store with Redux DevTools and thunk middleware already wired, plus dev-only checks that warn if you accidentally mutate state or put non-serializable junk in it.</li><li><strong><code>createAsyncThunk</code></strong> — standard handling for async work (the pending/fulfilled/rejected lifecycle of a fetch).</li><li><strong><code>createEntityAdapter</code></strong> — gives you normalized, pre-built CRUD for collections (add/update/remove by id) so you don't reinvent it.</li><li><strong>RTK Query</strong> — a full data-fetching and caching layer built into RTK, comparable to React Query, for the server-state problem.</li></ul><h4>Why the Immer detail matters</h4><p>Recall that reducers must be pure and must not mutate their arguments. Writing that by hand means endless <code>{ ...state, items: [...state.items, x] }</code> spread pyramids that get unreadable fast. Immer lets you write <code>s.push(a.payload)</code> — which <em>looks</em> like a forbidden mutation but is actually safe, because Immer records your changes against a draft and produces a fresh immutable object. You get readable code <em>and</em> purity. The worked example <code>add: (s, a) =&gt; { s.push(a.payload); }</code> is exactly this: Immer-safe by construction.</p><div class=\"key\">Redux Toolkit is the official, recommended way to use Redux — not an alternative to it. <code>createSlice</code> + Immer kills the action-constant and spread-pyramid boilerplate while keeping reducers pure, and RTK Query even folds in server-state caching. Reach for RTK by default; treat hand-written Redux as legacy.</div>"}
});
