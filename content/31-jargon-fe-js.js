/* Frontend jargon & acronyms — clickable reference (FE-JS, auto-assembled) */
Object.assign(GLOSSARY_DATA, {
  "esm": {"term":"ESM","group":"FE-JS","aliases":["ECMAScript Modules","ES Modules","ES6 Modules"],"eli5":"JavaScript's official, built-in module system using static import/export statements that are resolved before the code runs.","memory":"ESM = ECMAScript Modules — the standard import/export, statically analyzable.","full":"<p><strong>ESM</strong> (ECMAScript Modules) is JavaScript's standardized, native module system, built around the <code>import</code> and <code>export</code> keywords. Its defining property is that it is <em>static</em>: imports and exports are declarations resolved during the parsing/linking phase, <strong>before</strong> any code executes. This enables the module graph (including cyclic dependencies) to be analyzed ahead of time, which in turn enables <strong>tree-shaking</strong> — eliminating unreachable exports from the final bundle.</p><p>ESM was standardized as part of <strong>ECMAScript 2015 (ES6)</strong>, with the host integration (how browsers and Node load modules) finalized afterward. Browsers shipped native support via <code>&lt;script type=\"module\"&gt;</code> in the 2017–2018 window, and Node.js added stable ESM support (with <code>.mjs</code> and <code>\"type\": \"module\"</code>) over the following years. It is the successor to the older <strong>CommonJS</strong> (CJS) <code>require()</code> system that long dominated Node.</p><p>Its purpose is a single, standard, statically-analyzable module format that works across browsers and runtimes. It is the default today: bundlers (Rollup, Vite, esbuild) and test runners (Vitest) lean on ESM's static structure for tree-shaking and fast, correct dependency resolution. Because imports are hoisted and resolved up front, ESM also supports cyclic references more cleanly than CJS, whose <code>require()</code> runs dynamically at call time.</p>","worked":"<p>Static, analyzable imports/exports:</p><ul><li><code>// math.js</code></li><li><code>export function add(a, b) { return a + b; }</code></li><li><code>export function unused() {}</code></li><li><code>// app.js</code></li><li><code>import { add } from './math.js';</code></li></ul><p>Because <code>unused</code> is never imported anywhere, a bundler can <strong>tree-shake</strong> it out — the static <code>import</code>/<code>export</code> graph proves it's unreachable.</p>","teach":"<p>At its core, ESM is just JavaScript's official way to split code into files that explicitly declare what they share (<code>export</code>) and what they need (<code>import</code>) — and crucially, those declarations are figured out <em>before the program runs</em>.</p><div class=\"analogy\">Imagine assembling furniture from a kit. ESM is the kit where the full parts list and assembly diagram are printed on the box <em>before</em> you open it — so you can see in advance exactly which screws you'll use and leave the unused bag in the box. CommonJS is more like rummaging through a junk drawer at each step, grabbing whatever a line of running code happens to ask for. The printed-up-front list is what makes ESM 'static.'</div><p>That static nature is the whole point. Because <code>import</code> and <code>export</code> are resolved during a parse-and-link phase before execution, a tool can read the entire module graph as data. From that it can do <strong>tree-shaking</strong>: 'export <code>add</code> is imported somewhere, but export <code>unused</code> is imported nowhere — drop it.' This dead-code elimination is only safe because the analysis happens statically; you can't reliably do it with code that decides its imports at runtime.</p><p>It also handles <strong>cycles</strong> more gracefully. Since the graph is linked up front (with live bindings between modules), two modules that import each other can be wired together before either runs, rather than one accidentally seeing a half-initialized copy of the other.</p><p>The contrast is CommonJS, whose <code>require()</code> is an ordinary function call that executes at runtime — flexible, but invisible to static analysis. Today ESM is the default everywhere: browsers (<code>&lt;script type=\"module\"&gt;</code>), Node, bundlers, and test runners all build on it.</p><div class=\"key\">ESM is the standard JS module system with static <code>import</code>/<code>export</code> resolved before execution — which is exactly what unlocks tree-shaking and clean cyclic resolution. It replaced CommonJS as the default.</div>","related":["cjs","jsx","ast"]},
  "cjs": {"term":"CJS","group":"FE-JS","aliases":["CommonJS","Common JS"],"eli5":"The older Node.js module system that uses require() to load modules dynamically at runtime and module.exports to expose them.","memory":"CJS = CommonJS — require() runs at call time, the pre-ESM Node default.","full":"<p><strong>CJS</strong> (CommonJS) is the module system that powered <strong>Node.js</strong> for most of its history, based on the <code>require()</code> function for importing and <code>module.exports</code> / <code>exports</code> for exporting. Its defining characteristic is that <code>require()</code> is an ordinary, <em>dynamic</em> function call: it executes at runtime, when the line is reached, and can be called conditionally, in a loop, or with a computed path. This flexibility comes at the cost of static analyzability.</p><p>CommonJS emerged from a community effort (originally 'ServerJS') around 2009 to give server-side JavaScript a standard module format, and Node.js adopted it as its native system. For many years it was the de facto standard for npm packages. Its successor is <strong>ESM</strong> (ECMAScript Modules), standardized in ES2015, which Node now supports alongside CJS.</p><p>Its purpose was to bring modularity to JavaScript outside the browser before the language had a built-in module system. It is still extremely common today because a vast amount of the npm ecosystem ships CJS, and Node continues to support it. A practical consequence appears in tooling: test runners like <strong>Jest</strong> historically transformed CJS through Babel (slower), whereas newer tools like <strong>Vitest</strong> lean on native ESM with a worker pool. Because <code>require()</code> is dynamic, CJS cannot be tree-shaken the way static ESM can.</p>","worked":"<p>CommonJS export and dynamic require:</p><ul><li><code>// math.js</code></li><li><code>module.exports = { add: (a, b) =&gt; a + b };</code></li><li><code>// app.js</code></li><li><code>const { add } = require('./math'); // runs at call time</code></li></ul><p>Because <code>require()</code> is just a function, it can be called conditionally — which static tools can't fully analyze:</p><p><code>if (debug) { require('./extra-logging'); }</code></p>","teach":"<p>At its core, CommonJS is just modules wired together by an ordinary function call: <code>require('./thing')</code> runs when the program reaches that line, fetches the module, and hands you back whatever it assigned to <code>module.exports</code>.</p><div class=\"analogy\">If ESM is a furniture kit with the parts list printed on the box up front, CommonJS is ordering parts one at a time over the phone <em>as you build</em>. You can decide mid-assembly to call and order an extra bracket — totally flexible — but nobody can look at the sealed box beforehand and know exactly which parts you'll end up using. That 'decide as you go' nature is what makes CJS dynamic.</div><p>That dynamism is both its strength and its limitation. Strength: you can <code>require()</code> inside an <code>if</code>, inside a loop, or with a path you compute at runtime. Limitation: a bundler can't reliably prove which exports are unused, so CommonJS resists <strong>tree-shaking</strong> — the dead-code elimination that ESM's static <code>import</code>/<code>export</code> makes safe.</p><p>Historically this also shaped tooling speed. <strong>Jest</strong>, built in the CJS era, typically pipes your code through Babel to transform modules, which adds overhead (though Jest now supports ESM too). <strong>Vitest</strong>, newer, runs native ESM with a worker pool and re-runs only the tests a change affects — generally faster.</p><p>Why does CJS still matter in 2026? Because an enormous fraction of npm packages were published as CommonJS and Node still fully supports it. You'll constantly encounter both formats, and the interop rules between them (a CJS module imported into ESM, default vs named exports) are a frequent source of confusion. Knowing that <code>require()</code> is 'just a runtime function' explains most of CJS's behavior.</p><div class=\"key\">CommonJS is Node's original module system: <code>require()</code> loads modules dynamically at runtime, which is flexible but defeats static tree-shaking. ESM is its statically-analyzable successor, but CJS remains everywhere in the npm ecosystem.</div>","related":["esm","ast"]},
  "tdz": {"term":"TDZ","group":"FE-JS","aliases":["Temporal Dead Zone"],"eli5":"The span between when a let/const variable's scope begins and the line where it's declared, during which touching the variable throws an error instead of giving undefined.","memory":"TDZ = Temporal Dead Zone — declared-but-untouchable until its line runs.","full":"<p>The <strong>TDZ</strong> (Temporal Dead Zone) is the region of a scope between the point where execution enters the scope and the point where a <code>let</code>, <code>const</code>, or <code>class</code> binding is actually declared. During that window the binding <em>exists</em> (it is hoisted to the top of the scope) but is in an 'uninitialized' state — and any attempt to read or write it throws a <code>ReferenceError</code> rather than yielding <code>undefined</code>.</p><p>The TDZ was introduced with <code>let</code> and <code>const</code> in <strong>ECMAScript 2015 (ES6)</strong>, the same release that added block scoping. It exists specifically to fix a long-standing footgun of <code>var</code>: <code>var</code> declarations hoist <em>and</em> initialize to <code>undefined</code>, so reading them before their assignment silently returns <code>undefined</code> and hides bugs. The 'temporal' in the name refers to the fact that whether access is legal depends on <em>when</em> (the order of execution), not <em>where</em> in the source.</p><p>Its purpose is to turn a class of silent, hard-to-diagnose bugs into loud, immediate errors. It remains a permanent feature of the language today (and most linters flag use-before-declaration anyway). Understanding the TDZ explains otherwise-puzzling <code>ReferenceError</code>s and reinforces the best practice of declaring bindings before using them.</p>","worked":"<p>Accessing a <code>let</code> binding before its declaration line:</p><ul><li><code>{</code></li><li><code>&nbsp;&nbsp;console.log(x); // ReferenceError — x is in its TDZ</code></li><li><code>&nbsp;&nbsp;let x = 5;       // declaration ends the TDZ for x</code></li><li><code>&nbsp;&nbsp;console.log(x); // 5 — now safe</code></li><li><code>}</code></li></ul><p>Contrast with <code>var y</code>, where the same early access yields <code>undefined</code> (no TDZ) — silently masking the bug.</p>","teach":"<p>At its core, the TDZ is just a safety rule: from the moment a block starts until the line that <em>declares</em> a <code>let</code>/<code>const</code>, that variable is off-limits — touch it and JavaScript throws instead of pretending it's <code>undefined</code>.</p><p>A common myth is that <code>let</code> and <code>const</code> 'aren't hoisted.' They <em>are</em> hoisted — the engine knows about them at the top of the block — but unlike <code>var</code>, they're not initialized to <code>undefined</code>. They sit in a special uninitialized state, and that state is the Temporal Dead Zone.</p><div class=\"analogy\">Think of a new exhibit at a museum. The display case is already installed and labeled the morning the gallery opens (the variable is hoisted/known), but it's roped off with a 'NOT YET OPEN' sign until the official unveiling at noon (the declaration line). Walk up before noon and a guard stops you cold — a hard error. With <code>var</code>, by contrast, the case is open all day but <em>empty</em>; you reach in and grab nothing (<code>undefined</code>) and don't realize anything's wrong.</div><p>The word 'temporal' is the key insight: legality depends on <em>time</em>, not place. The same line of code reading <code>x</code> is illegal if it runs before <code>let x</code> executes and legal after — even though its position in the file never changed.</p><p>Why was this added? Because <code>var</code>'s silent <code>undefined</code> hid real bugs: you'd misorder your code, read a value too early, get <code>undefined</code>, and chase a mysterious downstream failure. The TDZ converts that into an immediate, pinpointed <code>ReferenceError</code> at the exact offending access — a deliberate trade of 'quietly wrong' for 'loudly stopped.'</p><div class=\"key\">The TDZ is the window between scope entry and a <code>let</code>/<code>const</code> declaration where the binding exists but is untouchable — access throws <code>ReferenceError</code>. It's an intentional ES6 safety feature that replaces <code>var</code>'s silent <code>undefined</code> with a loud error.</div>","related":["esm","cjs","iife"]},
  "ast": {"term":"AST","group":"FE-JS","aliases":["Abstract Syntax Tree"],"eli5":"A tree data structure that represents the grammatical structure of source code, used by compilers, linters, and code-transform tools.","memory":"AST = Abstract Syntax Tree — your code parsed into a tree of meaning.","full":"<p>An <strong>AST</strong> (Abstract Syntax Tree) is a tree representation of the syntactic structure of source code, where each node corresponds to a construct in the language — a function, an expression, a binary operator, a variable declaration. It is 'abstract' because it omits inessential surface details (parentheses, whitespace, exact token punctuation) and captures the meaningful nesting. A <em>parser</em> turns raw text into an AST; tools then read or rewrite that tree.</p><p>The AST is a foundational concept in compiler theory dating back decades, long predating the web. In the front-end world it is the backbone of the JavaScript toolchain: <strong>Babel</strong> (a JS-based compiler with a vast plugin/preset ecosystem like <code>@babel/preset-env</code> and macros) parses code to an AST, lets plugins transform that tree, and regenerates code. ESLint analyzes the AST to find problems; bundlers, codemods (e.g., jscodeshift), and formatters all operate on it. Standardized AST formats like <strong>ESTree</strong> let these tools interoperate.</p><p>Its purpose is to make code <em>programmable as data</em>: instead of brittle text search-and-replace, tools traverse and manipulate a structured tree that understands scope and grammar. It remains central today — every JSX transform, TypeScript type-strip, lint rule, and tree-shake involves AST analysis. (Snapshot tests sometimes assert against serialized config or AST output to catch unintended structural changes.)</p>","worked":"<p>The expression <code>const x = a + 1;</code> parses to (simplified) an AST like:</p><ul><li><code>VariableDeclaration (kind: const)</code></li><li><code>&nbsp;&nbsp;VariableDeclarator</code></li><li><code>&nbsp;&nbsp;&nbsp;&nbsp;id: Identifier (name: 'x')</code></li><li><code>&nbsp;&nbsp;&nbsp;&nbsp;init: BinaryExpression (operator: '+')</code></li><li><code>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;left: Identifier (name: 'a')</code></li><li><code>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;right: NumericLiteral (value: 1)</code></li></ul><p>A Babel plugin can walk this tree and, say, rewrite every <code>BinaryExpression</code> — far safer than regex on text.</p>","teach":"<p>At its core, an AST is just your source code re-expressed as a <em>tree of objects</em>, where the nesting of the tree mirrors the nesting of the code's meaning.</p><div class=\"analogy\">Think of diagramming a sentence in grammar class. 'The cat sat on the mat' becomes a tree: a subject ('the cat'), a verb ('sat'), a prepositional phrase ('on the mat'). The diagram throws away the ink and spacing but keeps the structure — what modifies what. An AST does exactly this for code: <code>a + 1</code> becomes a 'BinaryExpression' node with a left child and a right child. The plus sign's <em>meaning</em> is captured; the literal character is gone.</div><p>Why bother? Because text is a terrible thing to edit programmatically. If you tried to rename a variable with find-and-replace, you'd wreck strings, comments, and unrelated matches. But on an AST, you can say 'find every <code>Identifier</code> node named <code>foo</code> that refers to <em>this specific</em> declaration' — the tree knows scope and grammar, so the transformation is precise.</p><p>This is the engine room of the front-end toolchain. A <strong>parser</strong> reads your text into an AST. Tools then either <em>read</em> it (ESLint walks the tree checking rules: 'is there a node that uses a variable inside its TDZ?') or <em>transform</em> it (Babel plugins rewrite nodes — lowering JSX into function calls, stripping TypeScript types) and a <strong>generator</strong> turns the modified tree back into code. Shared formats like ESTree mean many tools can read the same tree.</p><p>Once you see code as a tree of typed nodes rather than a flat string, a huge amount of tooling stops being magic: linting, codemods, transpilation, minification, and tree-shaking are all just tree traversals and rewrites.</p><div class=\"key\">An AST is source code parsed into a structured tree of grammar nodes, discarding surface syntax. It makes code editable as data — the basis for Babel transforms, ESLint, codemods, and bundlers, all far safer than text manipulation.</div>","related":["jsx","esm","cjs"]},
  "iife": {"term":"IIFE","group":"FE-JS","aliases":["Immediately Invoked Function Expression","Immediately-Invoked Function Expression","self-executing anonymous function"],"eli5":"A function that is defined and called in the same expression, used to create a private scope and capture values immediately.","memory":"IIFE = Immediately Invoked Function Expression — define it and run it on the spot.","full":"<p>An <strong>IIFE</strong> (Immediately Invoked Function Expression, pronounced 'iffy') is a JavaScript function that is executed the moment it is defined. It is written by wrapping a function in parentheses to make it an <em>expression</em> and then immediately calling it: <code>(function(){ ... })()</code> or <code>(() =&gt; { ... })()</code>. Because the function runs right away, any variables declared inside it stay private to its scope and don't leak to the surrounding code.</p><p>The IIFE pattern predates a formal name; the term was popularized by <strong>Ben Alman</strong> around 2010. In the era before ES6 modules and block-scoped <code>let</code>/<code>const</code>, IIFEs were the primary tool for two jobs: creating a <strong>private scope</strong> (the basis of the 'module pattern' for encapsulating state) and <strong>capturing a value per iteration</strong> in a loop, by passing the loop variable as an argument so each iteration closes over its own copy.</p><p>Its purpose is scope isolation and immediate execution. Much of its historical use has been superseded: ES modules give each file its own scope, and <code>let</code> in a <code>for</code> loop creates a fresh binding per iteration (eliminating the classic closure-in-loop bug without an IIFE). Still, IIFEs remain useful today — for example, to use <code>await</code> at top level in environments without top-level await, to run setup code once, or to avoid polluting the global namespace in a plain script.</p>","worked":"<p>The classic loop-closure fix — IIFE capturing <code>i</code> as an argument per iteration:</p><ul><li><code>for (var i = 0; i &lt; 3; i++) {</code></li><li><code>&nbsp;&nbsp;(function(captured) {</code></li><li><code>&nbsp;&nbsp;&nbsp;&nbsp;setTimeout(() =&gt; console.log(captured), 0); // 0, 1, 2</code></li><li><code>&nbsp;&nbsp;})(i);</code></li><li><code>}</code></li></ul><p>Without the IIFE (and with <code>var</code>), all three callbacks would log <code>3</code>. Modern alternative: just use <code>let i</code>, which gives each iteration its own binding.</p>","teach":"<p>At its core, an IIFE is just a function you define and call in the very same breath — a throwaway box that runs once, right now, and then disappears.</p><div class=\"analogy\">Think of it like a sealed scratch-pad. You grab a fresh sheet, scribble some private calculations, read off the one answer you need, then crumple the sheet so no one else can see your scratch work. The function body is the sheet; wrapping it in <code>()()</code> means you use it instantly and the variables inside never touch the desk (the surrounding scope).</div><p>Two parts make it work. First, wrapping a function declaration in parentheses turns it into an <em>expression</em> (the parser otherwise expects a declaration). Second, the trailing <code>()</code> immediately invokes it. So <code>(function(){...})()</code> means 'make this function, then run it now.'</p><p>Historically it solved two problems. The big one was <strong>privacy</strong>: before modules existed, everything in a script shared one global scope, and name collisions were rampant. Running your code inside an IIFE gave it a private bubble — the foundation of the old 'module pattern.' The second was the infamous <strong>loop-closure bug</strong>: with <code>var</code>, all your callbacks share one <code>i</code> and end up logging the final value. By passing <code>i</code> into an IIFE as an argument, each iteration gets its own captured copy.</p><p>Today the language fixed both natively. ES modules give every file its own scope, so you rarely need an IIFE for privacy. And <code>let</code> in a <code>for</code> loop creates a fresh binding each iteration, killing the closure bug without any wrapper. But IIFEs aren't extinct — they're still handy to run one-off setup, to keep a quick script from leaking globals, or to wrap an <code>async</code> body so you can <code>await</code> where top-level await isn't available.</p><div class=\"key\">An IIFE defines and calls a function in one expression to create an instant private scope. It was the pre-module way to isolate variables and capture per-iteration loop values; <code>let</code> and ES modules replaced most uses, but it still serves for one-shot setup and async wrappers.</div>","related":["tdz","esm","cjs"]},
  "lifo": {"term":"LIFO","group":"FE-JS","aliases":["Last In First Out","Last-In-First-Out","Last In, First Out"],"eli5":"An ordering rule where the most recently added item is the first one removed — like a stack of plates.","memory":"Last In, First Out: the last plate you stack is the first one you grab.","full":"<p><strong>LIFO (Last In, First Out)</strong> is an ordering discipline for a collection where the most recently inserted element is the first to be removed. It is the defining behavior of a <em>stack</em> data structure, with two core operations: <em>push</em> (add to the top) and <em>pop</em> (remove from the top). Its counterpart is <strong>FIFO</strong> (First In, First Out), the behavior of a queue.</p><p>LIFO is a foundational concept from computer science dating back to the earliest work on stacks and recursion in the 1950s–60s (the stack as a hardware/software construct is credited to early compiler and ALGOL-era research). In JavaScript it matters most for the <strong>call stack</strong>: when a function is invoked, a frame is <em>pushed</em>; when it returns, its frame is <em>popped</em>. Because the engine is single-threaded and the call stack is synchronous, the last function called is the first to finish — exactly LIFO order. This is why deep or infinite recursion throws <em>\"Maximum call stack size exceeded.\"</em></p><p>The term is still everywhere because it describes the call stack's mechanics, undo/redo histories, backtracking algorithms, and the event-loop model (the call stack must fully drain — pop back to empty — before the engine pulls the next task from the queue). Understanding LIFO is prerequisite to understanding why blocking the call stack freezes the whole page.</p>","worked":"<p>The call stack unwinds in LIFO order — the most recently entered function returns first:</p><p><code>function a() { b(); }</code><br><code>function b() { c(); }</code><br><code>function c() { /* deepest */ }</code><br><code>a();</code></p><p>Push order: <code>a</code> &rarr; <code>b</code> &rarr; <code>c</code>. Pop (finish) order: <code>c</code> &rarr; <code>b</code> &rarr; <code>a</code>. The <strong>last</strong> frame in (<code>c</code>) is the <strong>first</strong> out — pure LIFO.</p>","teach":"<p>At its core, LIFO is just <em>\"the last thing you put down is the first thing you pick up.\"</em> That single rule defines a stack.</p><div class=\"analogy\">Think of a stack of dinner plates in a cupboard. You add clean plates to the top, and when you need one, you take it from the top. The plate that's been sitting there longest — the one at the bottom — is the last one you'll ever touch. You physically can't grab the bottom plate without removing everything above it first. That's Last In, First Out.</div><p>Now map it onto JavaScript's <strong>call stack</strong>. The stack is the engine's bookkeeping for \"which function am I inside right now, and where do I return when it finishes?\" Every function call <em>pushes</em> a new frame on top. When that function returns, its frame <em>pops</em> off, and control resumes in the function underneath.</p><ul><li>Because it's a stack, the <strong>innermost / most recent</strong> call always completes first.</li><li>Because JS is single-threaded, only one frame runs at a time, and the stack must empty completely before the event loop runs the next queued task.</li><li>If you keep pushing without ever popping (runaway recursion), the stack overflows — \"Maximum call stack size exceeded.\"</li></ul><p>This is also why a long synchronous computation \"blocks\" everything: while a frame sits on the stack churning, nothing else — no clicks, no rendering, no timers — can run, because they're all waiting for the stack to drain back down.</p><div class=\"key\">LIFO = last in, first out = a stack. The JS call stack is a LIFO structure: deepest call finishes first, the stack must fully unwind to empty before the next task runs, and never-popping means stack overflow. Contrast with FIFO (queues), which is how the task/callback queue feeds work back in.</div>"},
  "api": {"term":"API","group":"FE-JS","aliases":["Application Programming Interface"],"eli5":"A defined set of functions, objects, or endpoints that one piece of software exposes so other code can use its capabilities without knowing its internals.","memory":"API = Application Programming Interface: the 'interface' you program against, hiding the implementation behind it.","full":"<p>An <strong>API (Application Programming Interface)</strong> is a contract that lets one piece of software talk to another: a named, documented set of functions, objects, events, or network endpoints with defined inputs and outputs. You program <em>against</em> the interface and let the provider handle the implementation behind it. On the front end, 'API' covers both <em>browser/Web APIs</em> exposed to JavaScript (the DOM, <code>fetch</code>, Web Animations API, the Navigation API) and <em>web service APIs</em> reached over the network (REST, GraphQL).</p><p>The concept of an API long predates the web, going back to early software libraries and operating systems where published interfaces let programs reuse shared code. On the web, browser APIs are standardized largely through the W3C and WHATWG, while many newer ones ship under the <em>Baseline</em> initiative that tracks when a feature is interoperable across major browsers. For example, the <strong>Navigation API</strong> (<code>navigation.navigate</code>, the <code>navigate</code> event, <code>currentEntry</code>) is a modern replacement for older history/routing hacks and reached Baseline status in early 2026.</p><p>Its purpose is decoupling and reuse: stable interfaces let independent teams and vendors evolve their internals without breaking callers. APIs remain the backbone of all software — front-end work is largely the art of calling Web APIs and service APIs well. New ones continually supersede old patterns (the Web Animations API and <code>requestAnimationFrame</code> for animation, the Navigation API for routing), and good front-end code checks capability before using them — for instance, animation code should consult <code>window.matchMedia('(prefers-reduced-motion: reduce)')</code> before running motion.</p>","worked":"<p>Using the Web Animations API while respecting a user's reduced-motion preference via the <code>matchMedia</code> API:</p><ul><li><code>const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;</code></li><li><code>if (!reduce) {</code></li><li><code>&nbsp;&nbsp;el.animate([{ opacity: 0 }, { opacity: 1 }], { duration: 300 });</code></li><li><code>}</code></li></ul><p>Here two browser APIs cooperate: <code>matchMedia</code> reports the user's motion preference, and <code>element.animate()</code> (the Web Animations API) runs the transition only when motion is welcome.</p>","teach":"<p>At its core, an API is just <strong>a set of buttons and labels someone hands you so you can use their machine without opening it up.</strong></p><p>You don't need to know how the machine works inside — you only need to know which buttons exist, what each expects you to give it, and what it gives back. That published set of buttons is the interface. When you call <code>fetch(url)</code> in the browser, you're pressing a button on the network API; you don't reimplement TCP and HTTP, you just hand over a URL and get a promise back.</p><div class=\"analogy\">Think of a restaurant menu. The menu is the API: it lists exactly what you can order and what you'll receive, in language you understand. You don't walk into the kitchen, you don't need to know the chef's technique — you just say 'number 7' and a plate arrives. The kitchen can rebuild itself, swap chefs, change suppliers, and as long as 'number 7' still produces the same dish, you, the diner, never notice. That stable menu is what lets the kitchen and the diner evolve independently.</div><p>On the front end you live among two flavors of API. <em>Web APIs</em> are the buttons the browser gives JavaScript — the DOM, <code>fetch</code>, <code>requestAnimationFrame</code>, the Web Animations API, the newer Navigation API for client-side routing. <em>Service APIs</em> are the buttons a server gives your page over the network, like a REST or GraphQL endpoint. Either way the discipline is the same: program to the documented contract, and check that the button exists before pressing it — newer APIs like the Navigation API only recently reached Baseline interoperability (early 2026), so feature-detection and graceful fallback keep your app working everywhere.</p><div class=\"key\">An API is a stable contract you program against, hiding the implementation behind it. Front-end work is mostly calling Web APIs and service APIs — pick the modern one, but feature-detect and respect user settings (like reduced motion) before you use it.</div>","related":["html","css","llm"]}
});
