/* Partitioning & Parallelism (SPARK-PART) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"SPARK-PART","front":"What is a partition in Spark, and how does it relate to parallelism?","back":"A <strong>partition</strong> is the atomic chunk of a distributed dataset (RDD/DataFrame). Spark schedules <strong>exactly one task per partition</strong> within a stage, so the partition count is the upper bound on the parallelism of that stage. A partition is processed by a single task on a single core; to use 200 cores effectively you need at least ~200 partitions in the active stage."},
  {"sec":"SPARK-PART","front":"How many tasks run for a stage with N partitions on a cluster of C cores?","back":"The stage launches <strong>N tasks</strong> total. They run in waves of up to <code>C</code> at a time (one task occupies one core slot). If <code>N &lt; C</code>, some cores sit idle; if <code>N</code> is not a multiple of <code>C</code>, the final wave is partial and those extra cores idle until it finishes. Rule of thumb: target partition count = 2-3x total cores (some go up to 4x) so waves stay balanced and a stray slow task is amortized across the others."},
  {"sec":"SPARK-PART","front":"repartition vs coalesce: what is the core mechanical difference?","back":"<code>repartition(n)</code> always triggers a <strong>full shuffle</strong>, can increase OR decrease partition count, and produces <strong>evenly sized</strong> output partitions (round-robin, or hash when given columns). <code>coalesce(n)</code> avoids a full shuffle by <strong>merging existing partitions, preferring those already co-located on the same executor</strong>; it can only <strong>decrease</strong> the count and may yield <strong>uneven</strong> partitions. Internally <code>repartition(n)</code> is literally <code>coalesce(n, shuffle=true)</code>."},
  {"sec":"SPARK-PART","front":"Why can coalesce(1) before writing be dangerous even though it 'avoids a shuffle'?","back":"<code>coalesce</code> collapses partitions without a shuffle, but that means the <strong>narrow dependency propagates upstream</strong>: the entire upstream computation is forced to run with only 1 task because there is no shuffle boundary to decouple it. So <code>coalesce(1)</code> doesn't just make the write single-threaded — it serializes the whole preceding stage onto one core. If you need parallel computation but a single output file, use <code>repartition(1)</code> (full shuffle, upstream stays parallel) instead."},
  {"sec":"SPARK-PART","front":"What determines the number of partitions when reading files (e.g. Parquet) from storage?","back":"Input splitting is driven by file sizes packed against <code>spark.sql.files.maxPartitionBytes</code> (default <strong>128 MB</strong>), with a per-file overhead of <code>spark.sql.files.openCostInBytes</code> (default <strong>4 MB</strong>) added to each file's estimated size so many tiny files are bin-packed together rather than each getting its own partition. For splittable formats a large file is divided into multiple partitions; non-splittable formats (e.g. gzip text) yield <strong>one partition per file</strong> regardless of size."},
  {"sec":"SPARK-PART","front":"What is partition pruning, and what must be true for it to fire?","back":"<strong>Partition pruning</strong> skips reading entire directory partitions of a partitioned table when a query filters on the <strong>partition column(s)</strong>. It requires (1) the table to be physically partitioned on disk (e.g. <code>PARTITIONED BY (dt)</code> creating <code>dt=2024-01-01/</code> dirs) and (2) a predicate on that column the optimizer can resolve at plan time. It is a metadata/directory-listing operation — pruned partitions are never opened, unlike row-level filtering inside files."},
  {"sec":"SPARK-PART","front":"Partition pruning vs predicate pushdown vs dynamic partition pruning — distinguish them.","back":"<ul><li><strong>Partition pruning</strong>: skip whole on-disk partition directories using a static filter on the partition column.</li><li><strong>Predicate pushdown</strong>: push a filter into the file reader (e.g. Parquet row-group skipping via min/max stats) to avoid decoding rows within a file.</li><li><strong>Dynamic partition pruning (DPP)</strong>: at runtime, derive partition filters for the fact table from the build side of a join (e.g. a dimension filter), pruning fact partitions that no join key can match. Controlled by <code>spark.sql.optimizer.dynamicPartitionPruning.enabled</code> (on by default since Spark 3.0).</li></ul>"},
  {"sec":"SPARK-PART","front":"When does Spark use a hash partitioner vs a range partitioner?","back":"<strong>Hash partitioning</strong> is the default for shuffles behind <code>groupBy</code>, joins, and <code>repartition(col)</code>: partition = <code>hash(key) mod n</code>. It co-locates equal keys but gives no ordering and is skew-prone. <strong>Range partitioning</strong> is used by <code>repartitionByRange</code> and global <code>ORDER BY</code>/<code>sortBy</code>: it samples the data to compute range boundaries so each partition holds a contiguous, roughly equal-count key range — enabling sorted output and more even sizes for skewed key distributions."},
  {"sec":"SPARK-PART","front":"How does Spark's range partitioner decide its boundaries, and what's the catch?","back":"It runs <strong>reservoir sampling</strong> (per-partition sketch) over the input to estimate the key distribution, then computes range bounds so partitions are roughly equal-sized by row count. The catch: it adds an extra <strong>sampling pass/job</strong> over the data before the actual shuffle, and boundaries are only as good as the sample — heavily skewed or clustered keys can still produce uneven ranges."},
  {"sec":"SPARK-PART","front":"What goes wrong with TOO FEW partitions?","back":"<ul><li><strong>Underutilization</strong>: fewer tasks than cores leaves cores idle.</li><li><strong>OOM / spill</strong>: each task must hold a large slice in memory; oversized partitions blow executor memory or spill heavily to disk.</li><li><strong>No straggler smoothing</strong>: one slow/large partition becomes the stage's critical path with nothing to overlap it.</li></ul>"},
  {"sec":"SPARK-PART","front":"What goes wrong with TOO MANY partitions?","back":"<ul><li><strong>Scheduling overhead</strong>: each task has fixed launch/serialization/bookkeeping cost; with millions of tiny tasks this overhead dominates runtime.</li><li><strong>Small-files problem on write</strong>: each output partition becomes a file, flooding storage with tiny files.</li><li><strong>Shuffle amplification</strong>: shuffle blocks = mappers x reducers; many partitions on both sides explode block count and metadata.</li></ul>"},
  {"sec":"SPARK-PART","front":"What is the small-files problem and what typically causes it on write?","back":"Many tiny output files (KB-few MB each) instead of fewer large ones. Causes: a high partition count at write time (one file per partition per write task), over-eager <code>partitionBy</code> on a high-cardinality column, or streaming micro-batches each emitting files. Consequences: bloated metastore/listing metadata, high per-file open cost on subsequent reads, and poor read-parallelism packing. Fixes: <code>repartition</code>/<code>coalesce</code> to a sane count before writing, compaction jobs, or <code>OPTIMIZE</code>/auto-compaction in table formats like Delta/Iceberg."},
  {"sec":"SPARK-PART","front":"What controls the number of partitions produced by a shuffle (groupBy/join), and why does the default cause trouble?","back":"Post-shuffle partition count is governed by <code>spark.sql.shuffle.partitions</code>, default <strong>200</strong>. It's a fixed constant regardless of data size, so it over-partitions small datasets (tiny tasks, small files) and under-partitions huge ones (oversized partitions, spill). It does NOT affect file-read partitioning. <strong>Adaptive Query Execution (AQE)</strong> (<code>spark.sql.adaptive.enabled</code>, on by default since Spark 3.2) mitigates this by coalescing shuffle partitions at runtime based on actual map-output sizes."},
  {"sec":"SPARK-PART","front":"How does AQE change partition counts at runtime?","back":"AQE uses real map-stage output statistics to (1) <strong>coalesce</strong> many small shuffle partitions into fewer right-sized ones targeting <code>spark.sql.adaptive.advisoryPartitionSizeInBytes</code> (default <strong>64 MB</strong>), and (2) <strong>split skewed partitions</strong> via skew-join handling (<code>spark.sql.adaptive.skewJoin.enabled</code>). This makes the static <code>spark.sql.shuffle.partitions=200</code> behave as an upper bound rather than the final count."},
  {"sec":"SPARK-PART","front":"You repartition(col) but one task runs 10x longer than the rest — what is happening and what fixes it?","back":"<strong>Data skew</strong>: hash partitioning sent a disproportionate share of rows (a hot key) to one partition, so its task dominates the stage. Fixes: enable <strong>AQE skew-join</strong> handling; <strong>salt</strong> the hot key (append a random suffix to spread it across partitions, then aggregate in two stages); switch a join with a small side to a <strong>broadcast join</strong>; or use <code>repartitionByRange</code> when the skew is in a sortable key rather than a single hot value. Note: raising <code>spark.sql.shuffle.partitions</code> does NOT help — the hot key still lands in one partition."}
);

Object.assign(GLOSSARY_DATA, {
  "repartition-vs-coalesce": {"term":"repartition vs coalesce","group":"SPARK-PART","aliases":["repartition","coalesce","repartition vs coalesce"],"eli5":"repartition reshuffles everything to get evenly sized partitions; coalesce just glues neighboring partitions together cheaply but only to shrink the count.","memory":"co-ALESCE = COllapse, cheap, only shrinks, can be lumpy. RE-partition = REdistribute, full shuffle, even, any direction.","full":"<p><code>repartition(n)</code> performs a <strong>full shuffle</strong>, redistributing all rows (round-robin, or by hash when given columns). It can <strong>increase or decrease</strong> partition count and yields <strong>evenly sized</strong> partitions. <code>coalesce(n)</code> is a narrow transformation that <strong>merges existing partitions</strong> — preferring those already on the same executor — with <strong>no shuffle</strong>; it can only <strong>decrease</strong> the count and may produce <strong>uneven</strong> partitions. Internally <code>repartition(n) == coalesce(n, shuffle=true)</code>.</p><p>The subtle trap is that <code>coalesce</code>'s narrow dependency <strong>propagates upstream parallelism</strong>: <code>coalesce(1)</code> forces the entire preceding stage to run with one task because there is no shuffle boundary to decouple them. When you need both upstream parallelism and a small output, use <code>repartition</code> (it inserts a shuffle boundary) and accept the shuffle cost.</p>","trap":"\"coalesce avoids a shuffle so it's always cheaper\" — false. coalesce(1) makes your upstream computation single-threaded; repartition(1) keeps it parallel.","worked":"After a heavy filter leaves 200 sparse partitions, coalesce(20) cheaply merges them with no shuffle. But to fix skewed sizes you'd need repartition(20), paying for a shuffle to get even partitions.","related":["partition","shuffle-partitions","small-files-problem","partition-count-tradeoff"]},
  "input-partitioning": {"term":"Input Partitioning (File Splits)","group":"SPARK-PART","aliases":["input partitioning","file splits","maxpartitionbytes","split size"],"eli5":"When Spark reads files, it packs file chunks into partitions up to a byte budget, so file sizes and a config decide how many read tasks you get.","memory":"Read parallelism ~ total bytes / maxPartitionBytes, but gzip pins it to one-file-one-task.","full":"<p>For file sources, the initial partition count is computed by bin-packing file splits against <code>spark.sql.files.maxPartitionBytes</code> (default <strong>128 MB</strong>), with a per-file overhead charge <code>spark.sql.files.openCostInBytes</code> (default <strong>4 MB</strong>) added to each file's estimated size so that many tiny files are packed together rather than each getting its own partition. Splittable formats (Parquet, ORC, uncompressed/splittable text) let one large file become several partitions; non-splittable formats (gzip CSV/JSON) force <strong>one partition per file</strong> regardless of size.</p><p>This is why a single 10 GB gzip file is a performance disaster: it is one non-splittable partition handled by one task, no matter how many cores you have. Re-encoding to a splittable format (or to Parquet) restores read parallelism.</p>","trap":"Interviewers love the gzip trap: a huge .gz file reads on a single core because gzip isn't splittable — no config can parallelize that read.","worked":"Ten 1 GB Parquet files with the 128 MB default produce ~80 read partitions; the same data as one 10 GB gzip produces exactly 1 read partition.","related":["partition","small-files-problem","partition-pruning"]},
  "partition-pruning": {"term":"Partition Pruning","group":"SPARK-PART","aliases":["partition pruning","dynamic partition pruning","dpp","directory pruning"],"eli5":"If a table is laid out in folders by a column, filtering on that column lets Spark skip whole folders without opening any files.","memory":"Pruning = skip folders (partition column). Pushdown = skip row-groups (statistics). DPP = pruning whose filter is discovered at join time.","full":"<p><strong>Partition pruning</strong> eliminates entire on-disk partition <em>directories</em> of a partitioned table when a query predicate constrains the partition column(s). For a table <code>PARTITIONED BY (dt)</code> stored as <code>dt=2024-01-01/</code>, a filter <code>WHERE dt = '2024-01-01'</code> lets the optimizer list and read only that directory. It is a metadata operation (directory listing), distinct from <strong>predicate pushdown</strong>, which skips row-groups <em>inside</em> files using column statistics.</p><p><strong>Dynamic Partition Pruning (DPP)</strong> extends this to joins: when a fact table is partitioned on the join key and the dimension side has a filter, Spark derives a runtime partition filter from the dimension's build side and prunes fact partitions before scanning. It is controlled by <code>spark.sql.optimizer.dynamicPartitionPruning.enabled</code> (on by default since Spark 3.0) and is most valuable in star-schema queries.</p>","trap":"Pruning only fires on the PHYSICAL partition column. Filtering on a non-partition column, or wrapping the partition column in a function (e.g. cast/UDF) that blocks the optimizer, scans every directory.","worked":"A 3-year daily-partitioned table queried for one day touches 1 of ~1,095 directories with pruning; without the partition-column filter it lists and scans all of them.","related":["input-partitioning","partition","hash-vs-range-partitioner"]},
  "hash-vs-range-partitioner": {"term":"Hash vs Range Partitioner","group":"SPARK-PART","aliases":["hash partitioner","range partitioner","hashpartitioner","rangepartitioner","repartitionbyrange"],"eli5":"Hash spreads keys by a hash mod n (fast, unordered, skew-prone); range slices keys into sorted contiguous bands by sampling the data first.","memory":"Hash = mod-the-hash, equal keys together, no order. Range = sample then slice into sorted bands.","full":"<p>The <strong>hash partitioner</strong> assigns each row to <code>hash(key) mod n</code>. It is the default for shuffles behind <code>groupBy</code>, joins, and <code>repartition(col)</code>: it co-locates equal keys but provides no global ordering and is vulnerable to skew when one key (or a poor hash) dominates a partition.</p><p>The <strong>range partitioner</strong> (used by <code>repartitionByRange</code> and global <code>ORDER BY</code>/<code>sortBy</code>) first runs a <strong>reservoir-sampling sketch</strong> to estimate the key distribution, then computes range boundaries so each partition holds a contiguous key band of roughly equal row count. This produces sorted, more evenly sized partitions for skewed distributions, at the cost of an extra sampling job and boundary quality that depends on the sample.</p>","trap":"\"Range partitioning guarantees equal sizes\" — no. It targets equal sizes from a SAMPLE; clustered or skewed keys can still produce lopsided ranges, and it costs an extra pass.","worked":"repartition(8, 'country') hashes — a 'US'-heavy dataset slams most rows into one partition. repartitionByRange(8, 'event_ts') samples timestamps and gives 8 sorted, balanced time bands.","related":["repartition-vs-coalesce","data-skew","partition"]},
  "partition-count-tradeoff": {"term":"Partition Count Tradeoff","group":"SPARK-PART","aliases":["partition count","too few partitions","too many partitions","partition sizing"],"eli5":"Too few partitions starves cores and blows memory; too many drowns you in task overhead and tiny files — aim for a few times your core count.","memory":"Goldilocks: too few = idle cores + OOM; too many = task-overhead + small files. ~2-3x cores, ~100-200 MB each.","full":"<p>Partition count is a balance. <strong>Too few</strong>: fewer tasks than cores leaves cores idle, each task must materialize a large slice (OOM or heavy disk spill), and a single oversized partition becomes an un-overlappable straggler. <strong>Too many</strong>: fixed per-task overhead (launch, serialization, result tracking) dominates, shuffle block count (mappers x reducers) explodes, and writes emit a flood of tiny files.</p><p>Common heuristics: target <strong>2-3x total executor cores</strong> (some go up to 4x) for the active stage so waves stay balanced, and aim for partition sizes in the ~<strong>100-200 MB</strong> range (AQE's advisory post-shuffle default is 64 MB). <strong>AQE</strong> reduces the need to hand-tune by coalescing/splitting shuffle partitions at runtime.</p>","trap":"There is no universal magic number — the right count depends on data size, executor memory, and core count. Quoting \"always 200\" (the stale shuffle default) signals you don't understand the tradeoff.","worked":"60 GB shuffled into the default 200 partitions = ~300 MB each (spill risk). Bump to ~600 partitions (~100 MB each) or let AQE coalesce based on actual sizes.","related":["partition","shuffle-partitions","small-files-problem","data-skew"]},
  "small-files-problem": {"term":"Small-Files Problem","group":"SPARK-PART","aliases":["small files problem","small files","tiny files","compaction"],"eli5":"Writing thousands of tiny files clogs the metastore and makes future reads slow because of per-file overhead.","memory":"1 file per task per partition. High partition count + partitionBy high-cardinality column = file explosion.","full":"<p>The <strong>small-files problem</strong> is the accumulation of many tiny output files (KB to a few MB) instead of fewer large ones. On write, Spark emits roughly <strong>one file per partition per writing task</strong>, so a high partition count, an over-eager <code>partitionBy</code> on a high-cardinality column, or per-micro-batch streaming writes each multiply file count. The damage: inflated directory listing / metastore metadata, high cumulative per-file open cost on reads (related to <code>openCostInBytes</code>), and degraded read packing.</p><p>Mitigations: <code>repartition</code> or <code>coalesce</code> to a sane count <em>before</em> writing (e.g. one file per output partition directory), periodic <strong>compaction</strong> jobs, and table-format features like Delta/Iceberg <code>OPTIMIZE</code> / auto-compaction. For streaming, trigger-interval batching or downstream compaction is standard.</p>","trap":"partitionBy on a high-cardinality column (e.g. user_id) is the classic self-inflicted wound: it creates a directory per value AND a tiny file per writer in each — millions of files. Partition only on low-cardinality, filter-relevant columns.","worked":"Streaming 1-minute batches that each write 200 shuffle partitions create 200 files/minute = ~288k files/day. coalesce before write or run nightly OPTIMIZE compaction.","related":["repartition-vs-coalesce","partition-count-tradeoff","input-partitioning","shuffle-partitions"]}
});
