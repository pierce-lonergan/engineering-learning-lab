/* Frontend jargon & acronyms — clickable reference (FE-HTML, auto-assembled) */
Object.assign(GLOSSARY_DATA, {
  "seo": {"term":"SEO","group":"FE-HTML","aliases":["Search Engine Optimization"],"eli5":"The practice of structuring a site's content and markup so search engines rank and display it well.","memory":"SEO = Search Engine Optimization: making your page easy for Google to find, understand, and rank.","full":"<p><strong>SEO</strong> (Search Engine Optimization) is the practice of improving a website so that search engines like Google can crawl it, understand its content, and rank it highly for relevant queries — driving organic (unpaid) traffic. On the frontend, SEO touches semantic HTML, fast load times, mobile-friendliness, structured data, and metadata such as <code>&lt;meta name=\"description\"&gt;</code> and titles.</p><p>SEO emerged in the mid-1990s alongside the first search engines, and matured dramatically once <strong>Google</strong> (founded 1998) made link-based ranking (PageRank) dominant. Over decades it evolved from keyword-stuffing tricks toward genuine quality signals: relevance, page experience, and increasingly <strong>Core Web Vitals</strong> as ranking inputs.</p><p>Its purpose is visibility — most web traffic begins with a search, so ranking well is commercially critical. It remains central in 2026. The deck highlights two concrete frontend touchpoints: (1) social Open Graph tags use the <code>property</code> attribute (<code>&lt;meta property=\"og:image\"&gt;</code>), distinct from the <code>name</code>-based <code>&lt;meta name=\"description\"&gt;</code> SEO tags; and (2) HTTP <code>301 Moved Permanently</code> redirects are cached aggressively and pass \"link equity,\" so SEO ranking value transfers to the new URL.</p>","worked":"<p>Core SEO metadata, contrasted with social (Open Graph) tags:</p><pre><code>&lt;head&gt;\n  &lt;title&gt;Best Trail Running Shoes (2026 Guide)&lt;/title&gt;\n  &lt;!-- SEO: uses name= --&gt;\n  &lt;meta name=\"description\"\n        content=\"Independent reviews of 2026 trail shoes.\"&gt;\n  &lt;link rel=\"canonical\" href=\"https://ex.com/trail-shoes\"&gt;\n\n  &lt;!-- Social: uses property= (Open Graph), not name= --&gt;\n  &lt;meta property=\"og:title\" content=\"Best Trail Running Shoes\"&gt;\n  &lt;meta property=\"og:image\" content=\"https://ex.com/og.jpg\"&gt;\n&lt;/head&gt;\n\n# Moving a page? A 301 transfers SEO link-equity to the new URL:\n# HTTP/1.1 301 Moved Permanently\n# Location: https://ex.com/trail-shoes-2026</code></pre>","teach":"<p>At its core, SEO is just <strong>making your page easy for a search engine to find, understand, and trust</strong>. A search engine sends out crawlers that read your HTML, build an index of what each page is about, and then decide which pages to show for a given query. SEO is everything you do to help that process go in your favor.</p><div class=\"analogy\">Think of a giant library where the librarian (Google) decides which books go on the front display table. You can't bribe the librarian, but you <em>can</em> make your book easy to shelve: a clear title on the spine, an accurate summary on the back, well-organized chapters, and references from other respected books pointing to yours. SEO is writing your book so the librarian confidently puts it where readers will see it.</div><p>On the frontend, several signals matter. <strong>Semantic HTML</strong> (proper headings, <code>&lt;title&gt;</code>, descriptive <code>&lt;meta name=\"description\"&gt;</code>) tells the crawler what the page is. <strong>Speed and stability</strong> — measured by Core Web Vitals — feed into ranking, because Google favors pages that load fast and don't jank. <strong>Links</strong> from other sites act like votes of confidence.</p><p>The deck calls out two precise details. First, don't confuse the two kinds of <code>&lt;meta&gt;</code>: SEO description tags use the <code>name</code> attribute, while social-share <strong>Open Graph</strong> tags use <code>property</code> (e.g. <code>og:image</code>). Second, when you move a page, a <strong><code>301 Moved Permanently</code></strong> redirect tells search engines the move is permanent — they cache it aggressively and transfer the old page's accumulated ranking value (\"link equity\") to the new URL, so you don't lose your hard-won rankings.</p><div class=\"key\">The takeaway: <strong>SEO is technical hospitality for crawlers</strong> — clean semantic markup, fast stable pages, accurate metadata, and correct redirects so search engines can confidently rank you.</div>","related":["avif","webp"]},
  "html": {"term":"HTML","group":"FE-HTML","aliases":["HyperText Markup Language"],"eli5":"The markup language that defines the structure and meaning of web page content — headings, links, forms, images, and more.","memory":"HTML = HyperText Markup Language: 'markup' tags wrap content to give it meaning.","full":"<p><strong>HTML (HyperText Markup Language)</strong> is the standard markup language for documents meant to be displayed in a web browser. It describes a page's <em>structure and semantics</em> using nested <em>elements</em> denoted by tags — <code>&lt;h1&gt;</code> for a heading, <code>&lt;a&gt;</code> for a link, <code>&lt;button&gt;</code> for a button — each carrying built-in meaning and, often, built-in behavior. The browser parses HTML into the DOM, the live tree that CSS styles and JavaScript manipulates.</p><p>HTML was created by Tim Berners-Lee at CERN around 1990–1991 as part of inventing the World Wide Web. It was standardized through the IETF and then the W3C (HTML 4.01 in 1999), went through an XHTML detour, and was reinvigorated by the WHATWG, whose work produced HTML5 (reaching W3C Recommendation in 2014). HTML is now maintained by the WHATWG as a continuously updated <em>Living Standard</em> rather than numbered versions, adding semantic elements (<code>&lt;nav&gt;</code>, <code>&lt;article&gt;</code>), native form controls, and rich media.</p><p>Its purpose is to give content meaning so that browsers, search engines, and assistive technology can all interpret it correctly. HTML remains the irreplaceable foundation of the web: every page is HTML at the bottom. A core accessibility principle follows directly — <em>prefer native HTML semantics over ARIA</em>: if a real <code>&lt;button&gt;</code> or <code>&lt;label&gt;</code> gives you the role, keyboard behavior, and focus handling for free, you should use it rather than reconstructing that behavior on a <code>&lt;div&gt;</code> with ARIA attributes.</p>","worked":"<p>Native semantics give accessibility and behavior for free. Prefer this:</p><ul><li><code>&lt;button type=\"submit\"&gt;Save&lt;/button&gt;</code></li></ul><p>over this ARIA reconstruction, which must re-implement focusability, Enter/Space activation, and role by hand:</p><ul><li><code>&lt;div role=\"button\" tabindex=\"0\" onclick=\"save()\"&gt;Save&lt;/div&gt;</code></li></ul><p>The native <code>&lt;button&gt;</code> is keyboard-focusable, fires on Enter and Space, exposes the correct role to the accessibility tree, and works without JavaScript — all automatically.</p>","teach":"<p>At its core, HTML is just <strong>labeled boxes inside labeled boxes</strong> — a way of wrapping content in tags that say what each piece <em>is</em>.</p><p>When you write <code>&lt;h1&gt;Welcome&lt;/h1&gt;</code>, you're not just making text big — you're declaring 'this is the top-level heading of the page.' That declaration is meaning, not appearance. Appearance is CSS's job; behavior beyond the built-in is JavaScript's job. HTML's one job is to say what the content means and how it nests. The browser reads your tags and builds the DOM, a tree where each labeled box becomes a node.</p><div class=\"analogy\">Think of HTML like the framing of a house and the labels on every room. The blueprint says 'this is a kitchen, this is a bedroom, this door connects them.' Paint and furniture (CSS) come later; plumbing and electricity that react when you flip a switch (JavaScript) come later still. But the labeled rooms are what make the house usable — a plumber, an inspector, and a guest all rely on the room being correctly labeled a kitchen.</div><p>That 'correct label' idea is the whole reason native HTML beats ad-hoc alternatives. A real <code>&lt;button&gt;</code> already announces itself as a button to screen readers, already responds to Enter and Space, already takes keyboard focus. If you instead style a <code>&lt;div&gt;</code> to look like a button, you've thrown all of that away and must rebuild it with <code>tabindex</code>, key handlers, and ARIA roles — and you'll probably get some of it wrong. Hence the guideline: reach for a native element first, and only add ARIA when no native element expresses what you need.</p><p>HTML also has special elements with no visual output at all, like <code>&lt;script&gt;</code> and <code>&lt;template&gt;</code> nodes — structure that exists in the tree but isn't laid out — which is why some content can sit in the DOM yet never trigger CSS or reflow.</p><div class=\"key\">HTML defines structure and meaning, not looks. Use the most specific native element that fits; it hands you semantics, keyboard support, and accessibility for free that you'd otherwise have to rebuild.</div>","related":["css","api","cms"]}
});
