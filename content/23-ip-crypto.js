/* Crypto & Digital Asset Operations (IP-CRYPTO) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"IP-CRYPTO","front":"How does crypto post-trade differ from equities post-trade? Give the top 4 differences.","back":"<ul><li><strong>No central depository / CCP.</strong> Equities clear through <strong>DTCC</strong> (NSCC nets &amp; novates as the CCP; DTC custodies book-entry). Crypto has <em>no equivalent</em> — settlement is bilateral/on-chain across venues and chains, so there is no single golden record to reconcile against.</li><li><strong>Settlement finality.</strong> Equities: deterministic <strong>T+1</strong> (since <strong>May 28, 2024</strong>) on a fixed cycle. Crypto: <em>probabilistic</em> finality (PoW chains can <strong>reorg</strong>) or deterministic finality (modern BFT/PoS), measured in <strong>confirmations</strong>, not business days.</li><li><strong>Hours.</strong> Crypto is <strong>24/7/365</strong> — no settlement window, no SOD/EOD cutoff, so recon and break-resolution must run continuously, not in a nightly batch.</li><li><strong>Custody is on you.</strong> No DTC book-entry custody; the firm holds keys (<strong>hot vs cold wallets</strong>, MPC/multisig). Plus crypto-only recon dimensions: multiple chains, gas/fees, address management, deposit/withdrawal confirmation counts, reorgs.</li></ul>"},
  {"sec":"IP-CRYPTO","front":"CEX vs DEX — what's the operational difference for a trade-ops / recon engineer?","back":"<ul><li><strong>CEX (centralized exchange)</strong> — e.g. Coinbase, Binance. Custodial: <em>you hold a balance on their books</em>, off-chain order matching, <strong>REST + WebSocket</strong> APIs (some institutional derivatives venues like Coinbase Derivatives / Zero Hash also expose <strong>FIX + drop-copy</strong>). Recon = compare internal fills/positions vs the exchange's trade/balance API, like a classic broker recon. Deposits/withdrawals are the on-chain leg.</li><li><strong>DEX (decentralized exchange)</strong> — e.g. Uniswap. Non-custodial, <em>self-custody</em>, trades execute as <strong>on-chain transactions</strong> (smart contracts / AMM pools). No account API — your 'truth' is the <strong>blockchain itself</strong> (tx hash, logs/events, gas spent, slippage). Recon = parse on-chain events vs internal intent; you must handle reorgs, failed txns that still burn gas, and MEV/slippage.</li></ul>The CEX is API recon; the DEX is <strong>on-chain data engineering</strong>."},
  {"sec":"IP-CRYPTO","front":"Custody: hot wallet vs cold wallet — and why is custody the operational crux of crypto?","back":"<ul><li><strong>Hot wallet</strong> — keys on internet-connected systems. Fast, automatable (deposits/withdrawals/settlement), but the <em>highest-risk attack surface</em>; firms cap balances here.</li><li><strong>Cold wallet</strong> — keys offline (air-gapped / hardware). Secure but slow to mobilize; used as the vault.</li></ul><strong>Why it's the crux:</strong> in equities, <strong>DTC</strong> holds securities in book-entry and settlement is a ledger update you don't operationally own. In crypto there is <em>no depository</em> — <strong>'not your keys, not your coins.'</strong> A key compromise or a fat-fingered withdrawal is <strong>irreversible</strong> (no chargeback, no CCP guarantee). So custody design — key management, withdrawal allow-listing, approval workflows, hot/cold rebalancing — <em>is</em> the post-trade settlement control."},
  {"sec":"IP-CRYPTO","front":"What are MPC and multisig wallets, and why do trading firms use them over a single private key?","back":"Both eliminate the <strong>single point of compromise</strong> of one private key.<ul><li><strong>Multisig (multi-signature)</strong> — an <em>on-chain</em> contract requiring <strong>m-of-n</strong> signatures to move funds (e.g. 2-of-3). Visible on-chain; signing policy enforced by the chain.</li><li><strong>MPC (multi-party computation)</strong> — the private key <em>never exists in one place</em>; n parties each hold a key <em>share</em> and jointly compute a signature without ever reconstructing the full key. Chain-agnostic, looks like a normal single signature on-chain, supports flexible <strong>threshold</strong> policies.</li></ul>For a trading firm this enforces <strong>separation of duties / quorum approval</strong> on withdrawals and settlement — the operational analog of maker-checker controls, but cryptographic and irreversible-by-design."},
  {"sec":"IP-CRYPTO","front":"Explain on-chain finality: probabilistic vs deterministic, and what a 'reorg' means for recon.","back":"<ul><li><strong>Probabilistic finality</strong> (PoW, e.g. Bitcoin) — a block is never 100% final; each additional <strong>confirmation</strong> makes reversal exponentially less likely. You wait <em>N confirmations</em> before treating a deposit as settled.</li><li><strong>Deterministic finality</strong> (modern BFT/PoS) — once a block is finalized by the consensus protocol, it <em>cannot</em> be reverted. Closer to traditional settlement finality.</li></ul><strong>Reorg (chain reorganization):</strong> the network discards a chain tip and adopts a different fork; a transaction you thought settled <em>disappears or moves</em>. Ops impact: a deposit credited too early can be <strong>un-credited</strong>. Defense — <em>require enough confirmations before booking</em>, and treat 'seen in mempool' / '1 conf' as <strong>pending</strong>, never settled. Loosely analogous to an equities <strong>settlement fail</strong>, but it can hit <em>after</em> you booked."},
  {"sec":"IP-CRYPTO","front":"What reconciliation challenges are UNIQUE to crypto (vs an equities clearing-SOD recon)?","back":"<ul><li><strong>No central depository</strong> — no single external 'truth' file; truth is fragmented across each CEX API, each chain, and each custodian.</li><li><strong>Multiple chains</strong> — same asset (e.g. USDC) on Ethereum, Polygon, Solana, etc.; (chain, asset, address) is the real key, not just the symbol.</li><li><strong>Gas / network fees</strong> — the amount that leaves your wallet ≠ the amount that arrives; you must model fees as an <em>expected discrepancy</em>, like commissions in equities.</li><li><strong>Address management</strong> — deposits keyed by address, not by an account id; mis-attributed or reused addresses cause breaks.</li><li><strong>Confirmation lag</strong> — deposits/withdrawals are <strong>pending</strong> until N confirmations; a recon at the wrong instant shows a false break.</li><li><strong>Reorgs</strong> — a previously-matched on-chain settlement can reverse.</li><li><strong>24/7</strong> — no quiet window to snapshot; recon must be continuous.</li></ul>The primitive is the same — internal vs external truth, match on stable IDs, tolerate expected discrepancies — but the <em>identifiers</em> and <em>tolerances</em> are crypto-specific."},
  {"sec":"IP-CRYPTO","front":"How would you reconcile internal trade records against a CEX over its REST/WebSocket API? Outline the approach.","back":"<ol><li><strong>Pull external truth</strong>: paginate the exchange <code>GET /fills</code> (or stream the WS <code>user-trades</code> channel) for the period; persist raw.</li><li><strong>Pull internal</strong>: our order/fill records for the same window.</li><li><strong>Match on stable identifiers</strong>: the exchange <strong>trade id</strong> (best), else <code>(symbol, side, price, qty, exchange_order_id, exec_time±tolerance)</code>.</li><li><strong>Tolerate expected discrepancies</strong>: <em>fees</em>, partial fills, rounding/precision, and <strong>timing</strong> (24/7, no cutoff) — compare on a rolling window, not a calendar day.</li><li><strong>Classify breaks</strong>: external-only (we missed a fill), internal-only (phantom/un-acked order), and <em>amount</em> mismatches.</li><li><strong>Resolve</strong>: auto-correct known classes, route the rest to tooling for manual/bulk correction.</li></ol>Same primitive as a platform-vs-clearing-SOD recon (e.g. Devexperts syncing against Apex SOD files) — just the truth source is an exchange API instead of a clearing SOD file."},
  {"sec":"IP-CRYPTO","front":"Write a pandas reconciliation that matches internal fills against exchange fills on trade_id and flags breaks (including a fee tolerance).","back":"<pre><code>import pandas as pd\n\nm = internal.merge(\n    exchange, on=\"trade_id\",\n    how=\"outer\", suffixes=(\"_int\", \"_ext\"),\n    indicator=True,\n)\n\n# break taxonomy\nm[\"break_type\"] = \"matched\"\nm.loc[m._merge == \"left_only\",  \"break_type\"] = \"internal_only\"   # phantom / unacked\nm.loc[m._merge == \"right_only\", \"break_type\"] = \"exchange_only\"   # missed fill\n\nmatched = m._merge == \"both\"\nqty_break = (m.qty_int - m.qty_ext).abs() > 1e-9\n# tolerate fee/rounding noise on notional\nFEE_TOL = 0.01\nnotional_break = (m.notional_int - m.notional_ext).abs() > FEE_TOL\nm.loc[matched & (qty_break | notional_break), \"break_type\"] = \"amount_mismatch\"\n\nbreaks = m[m.break_type != \"matched\"]\n</code></pre><em>Match on a stable id, tolerate expected discrepancies (fees/rounding), and emit a clean break taxonomy</em> — the same shape works for an on-chain or settlement-file recon. (<code>internal</code> is the left frame, so <code>left_only</code> = internal_only.)"},
  {"sec":"IP-CRYPTO","front":"Why is idempotency critical for crypto order-entry and withdrawals, and how do you implement it? (Stripe/IETF pattern)","back":"Crypto venues are REST + WS over flaky networks and are 24/7; a timeout doesn't tell you if the order/withdrawal landed. <strong>Idempotency lets you retry safely</strong> so you never double-submit (and never double-spend a withdrawal).<br><br>Pattern (IETF / Stripe-style — 409 is the established community/IETF convention): store <code>(caller, idempotency_key) -&gt; request_hash + response + status</code>.<ul><li><strong>Same key + same payload</strong> &rarr; return the <em>original</em> stored response (including the original error).</li><li><strong>Same key + different payload</strong> &rarr; <code>409 Conflict</code>.</li></ul>For exchanges, the practical handle is a <strong>client order id (clOrdID)</strong> you generate and the venue dedupes on. <em>Generate the key before the first send, persist it, and reuse it on every retry</em> — that's what makes retries-with-backoff safe."},
  {"sec":"IP-CRYPTO","front":"What is FIX drop-copy and is it relevant to crypto trade ops?","back":"<strong>FIX</strong> is the tag=value session protocol (<code>35=D</code> NewOrderSingle, <code>35=8</code> ExecutionReport), with sequence numbers per session. A <strong>drop-copy</strong> is a separate FIX session that <em>pushes a copy of every <code>35=8</code> Execution Report</em> to a non-trading consumer — the standard <strong>trade-capture / recon feed</strong>.<br><br>Relevant ExecType (<code>150</code>) values: <code>150=F</code> Trade (fill), <code>150=G</code> Trade Correction, <code>150=H</code> Trade Cancel; gaps are replayed via <strong>ResendRequest</strong> using sequence numbers.<br><br><strong>Crypto relevance:</strong> yes — the <em>institutional</em> crypto-derivatives venues expose FIX + drop-copy (e.g. <strong>Coinbase Derivatives</strong>, <strong>Zero Hash</strong>), so a multi-asset firm gets one consistent capture pipeline. <em>But</em> most spot crypto and prediction venues are REST + WebSocket only, so you build to both."},
  {"sec":"IP-CRYPTO","front":"Stablecoins and tokenized deposits as the on-chain CASH LEG — what's the 2026 trend and the GENIUS Act detail?","back":"The 2026 trend: <strong>stablecoins (e.g. USDC) and tokenized bank deposits are used as the on-chain CASH LEG of settlement</strong> — the money side of delivery-versus-payment moves on-chain, enabling near-instant, 24/7, atomic settlement against the asset leg (<strong>DvP</strong> without a traditional cash wire).<br><br>The key regulatory datapoint: the <strong>OCC GENIUS Act proposal (Mar 2, 2026)</strong> defines <em>'timely' stablecoin redemption</em> as <strong>no later than 2 business days</strong>. Ops impact: you can treat a regulated stablecoin as cash-like, but your liquidity/recon model must assume redemption to fiat can take up to <strong>2 business days</strong> — it is <em>not</em> instantaneous, so the cash leg has its own settlement-timing risk to track."},
  {"sec":"IP-CRYPTO","front":"Prediction markets: how do they settle, and what operational risks are unique to them?","back":"<strong>Binary event contracts</strong> priced <code>$0.00–$1.00</code> (= implied probability); settle to <code>$1</code> if the event occurs, else <code>$0</code>. Settlement is to <strong>EVENT RESOLUTION</strong>, not a price or a delivery.<br><br>Unique ops risks:<ul><li><strong>Oracle / resolution risk</strong> — who declares the outcome, and when? Settlement waits on resolution, which can be delayed or <strong>disputed</strong>.</li><li><strong>Multi-platform arb / recon</strong> — the same event trades on <strong>Kalshi</strong> (CFTC-regulated <strong>DCM</strong>, designated 2020) and <strong>Polymarket</strong> (crypto-native, 2020, Polygon/USDC; became a CFTC-regulated US exchange via Amended Order of Designation <strong>Nov 25, 2025</strong>, after acquiring QCX/QCEX in July 2025); you reconcile positions across venues with potentially different resolution sources.</li></ul>Reg backdrop: the <strong>CFTC Prediction Markets Advisory (Feb 25, 2026)</strong> asserts full DCM authority over insider trading, wash/pre-arranged trades, spoofing/disruptive trading, and fraud/manipulation."},
  {"sec":"IP-CRYPTO","front":"Why does T+1 (May 28, 2024) make automation and STP non-negotiable — and how does that thinking carry to crypto?","back":"On <strong>May 28, 2024</strong> US securities moved <strong>T+2 &rarr; T+1</strong> (equities, corp &amp; muni bonds, UITs, ADRs, ETFs). The intraday post-trade window collapsed to roughly <strong>2–5 hours</strong>, so <em>data management is the #1 challenge</em> and <strong>STP (straight-through processing)</strong> is critical: any unresolved exception becomes a <strong>settlement fail</strong>. (EU &amp; UK align <strong>Oct 11, 2027</strong>.) Affirmation via DTCC <strong>ITP</strong> ('match-to-instruct') must happen fast enough to gate <strong>NSCC/DTC</strong> settlement.<br><br>Carry to crypto: crypto is already the extreme case — <strong>24/7, near-instant on-chain settlement, no window at all</strong>. The same conclusion holds harder: manual recon can't keep up, so breaks must be detected and resolved by <em>automated, continuous</em> tooling. T+1 is the equities preview of the crypto reality."},
  {"sec":"IP-CRYPTO","front":"Write a SQL break report: internal positions vs an exchange balance snapshot, keyed by (chain, asset), tolerating dust.","back":"<pre><code>WITH diff AS (\n  SELECT\n    COALESCE(i.chain, e.chain)  AS chain,\n    COALESCE(i.asset, e.asset)  AS asset,\n    COALESCE(i.qty, 0)          AS internal_qty,\n    COALESCE(e.qty, 0)          AS exchange_qty,\n    COALESCE(i.qty,0) - COALESCE(e.qty,0) AS delta\n  FROM internal_positions i\n  FULL OUTER JOIN exchange_balances e\n    ON i.chain = e.chain\n   AND i.asset = e.asset\n)\nSELECT *,\n  CASE\n    WHEN internal_qty = 0 THEN 'exchange_only'\n    WHEN exchange_qty = 0 THEN 'internal_only'\n    ELSE 'qty_mismatch'\n  END AS break_type\nFROM diff\nWHERE ABS(delta) &gt; 1e-8      -- tolerate dust / rounding\nORDER BY ABS(delta) DESC;\n</code></pre><em>FULL OUTER JOIN on the stable key (chain, asset), a dust tolerance, and a break taxonomy</em> — the on-chain analog of a clearing-SOD recon."},
  {"sec":"IP-CRYPTO","front":"A deposit shows in your wallet but isn't credited to the trader yet. Walk through the correct ops handling.","back":"<ol><li><strong>Don't book on first sight.</strong> A tx in the mempool or with 1 confirmation is <strong>pending</strong>, never settled — booking early risks a <strong>reorg</strong> un-crediting it.</li><li><strong>Wait for N confirmations</strong> per asset/chain policy (BTC more, fast-finality chains fewer); confirmation thresholds are a risk parameter, not a constant.</li><li><strong>Attribute by address</strong> — map the deposit address to the internal account; flag reused/unknown addresses for manual review.</li><li><strong>Net the fee</strong> — for withdrawals, amount-sent ≠ amount-received because of <strong>gas</strong>; reconcile on the on-chain delta, not the requested amount.</li><li><strong>Credit on finality</strong>, then reconcile the on-chain event against the internal ledger.</li><li><strong>Monitor for reorg</strong> after crediting; have a defined reversal/adjustment path since on-chain moves are <em>irreversible</em> and corrections are bookkeeping-only.</li></ol>This is the crypto version of <em>'unaffirmed trade &rarr; potential settlement fail'</em> — the control is confirmation discipline."},
  {"sec":"IP-CRYPTO","front":"How does a payments/ledger/billing engineering background map onto crypto trade-ops automation?","back":"Near 1:1 — the primitives are identical, only the nouns change.<ul><li><strong>Reconciliation against an external truth</strong>: a payments processor settlement file &rarr; <em>exchange API / on-chain data / clearing SOD</em>. Match on stable IDs, tolerate expected discrepancies (chargebacks/fees &rarr; <em>gas/fees/partial fills</em>), unmatched = <strong>breaks</strong>.</li><li><strong>Idempotency keys</strong>: <code>(caller, idempotency_key) -&gt; hash + response + status</code> for safe payment retries &rarr; <em>safe retry of exchange order-entry &amp; withdrawal instructions</em> (client order id).</li><li><strong>Exception handling</strong>: failed-payment workflows &rarr; <em>break / settlement-fail resolution</em>.</li><li><strong>Stack</strong>: Python/Java + Kafka/Spark/Snowflake/AWS is exactly the post-trade data-plumbing toolset; the <strong>ledger</strong> mindset (double-entry, immutable, reconciled) <em>is</em> position-keeping.</li></ul>Interview framing: <em>'I've already built idempotent, reconciled, externally-verified money systems — crypto just swaps the truth source to an exchange API and a blockchain.'</em>"}
);

Object.assign(GLOSSARY_DATA, {
  "hot-vs-cold-wallet": {"term":"Hot vs Cold Wallet","group":"IP-CRYPTO","eli5":"A hot wallet keeps the keys online so you can move crypto fast; a cold wallet keeps them offline so they can't be hacked.","memory":"Hot = connected = quick but exposed; Cold = offline = safe but slow (cold storage = the vault).","full":"<p><strong>Hot wallets</strong> hold private keys on internet-connected systems, enabling automated deposits, withdrawals, and settlement — at the cost of being the highest-risk attack surface, so firms cap balances there. <strong>Cold wallets</strong> keep keys offline (air-gapped or hardware) as the secure vault, mobilized only deliberately.</p><p>Because crypto has <em>no central depository</em> (unlike DTC for equities), the firm itself owns custody, and a key compromise or mistaken withdrawal is <strong>irreversible</strong>. The hot/cold split is the core custody control.</p>","trap":"Thinking cold storage is just 'a backup.' It's an active operational tier — you must rebalance hot&harr;cold to fund trading without over-exposing the hot wallet.","worked":"A market-maker keeps ~2% of inventory in a hot MPC wallet for live settlement and 98% in cold multisig; an automated job tops up the hot wallet when it drops below a floor, behind a quorum approval.","related":["mpc-multisig","custody-crux","reorg-finality"]},
  "mpc-multisig": {"term":"MPC / Multisig","group":"IP-CRYPTO","eli5":"Ways to require several parties to approve before crypto can move, so no single stolen key can drain funds.","memory":"Multisig = m-of-n signatures ON-chain; MPC = key SHARES, full key never assembled (off-chain math, one signature on-chain).","full":"<p><strong>Multisig</strong> uses an on-chain contract requiring <strong>m-of-n</strong> signatures (e.g. 2-of-3) to move funds — the policy is enforced and visible on the blockchain. <strong>MPC (multi-party computation)</strong> splits signing across n parties who each hold a key <em>share</em> and jointly produce a signature <em>without ever reconstructing the full key</em>; it's chain-agnostic and looks like an ordinary single signature on-chain.</p><p>Both remove the single-point-of-compromise and let a firm enforce cryptographic separation of duties / quorum approval on withdrawals — the irreversible-by-design analog of maker-checker controls.</p>","trap":"Conflating them: multisig is visible on-chain and chain-specific; MPC is off-chain key-share math that produces one normal-looking signature and works across chains.","worked":"A 2-of-3 MPC policy: an ops engineer initiates a withdrawal, a risk officer approves; their two shares co-sign and the third (cold backup) stays offline — no single machine ever held the key.","related":["hot-vs-cold-wallet","custody-crux"]},
  "custody-crux": {"term":"Custody as the Ops Crux","group":"IP-CRYPTO","eli5":"In crypto, your firm holds the actual keys, so safeguarding them IS settlement — there's no DTC to do it for you.","memory":"'Not your keys, not your coins' — and there's no chargeback button.","full":"<p>In equities, <strong>DTC</strong> holds securities in book-entry and settlement is a ledger update the firm doesn't operationally own. In crypto there is <em>no depository</em>: the firm holds the private keys, and moving an asset is an irreversible on-chain transaction. A key compromise or fat-fingered withdrawal cannot be reversed and there is no CCP guarantee.</p><p>So custody design — key management (hot/cold, MPC/multisig), withdrawal allow-listing, approval quorums, confirmation discipline — <em>is</em> the post-trade settlement control surface.</p>","trap":"Treating custody as a security/IT concern separate from trade ops. In crypto it's the heart of settlement and recon.","worked":"A withdrawal to a non-allow-listed address is auto-held for manual review; the allow-list + quorum is the firm's substitute for DTC's settlement guarantee.","related":["hot-vs-cold-wallet","mpc-multisig","crypto-recon-challenges"]},
  "reorg-finality": {"term":"Finality & Reorgs","group":"IP-CRYPTO","eli5":"On some chains a 'done' transaction can briefly un-happen, so you wait for confirmations before treating money as settled.","memory":"Probabilistic = wait N confirmations; Deterministic = finalized = locked. Reorg = the tip rewrote itself.","full":"<p><strong>Probabilistic finality</strong> (PoW, e.g. Bitcoin): a block is never absolutely final; each <strong>confirmation</strong> makes reversal exponentially less likely, so you wait N confs. <strong>Deterministic finality</strong> (modern BFT/PoS): once finalized, a block cannot be reverted.</p><p>A <strong>reorg</strong> is when the network discards a chain tip and adopts a different fork, so a transaction thought settled can move or vanish. Ops defense: require enough confirmations before booking; treat mempool/1-conf as <strong>pending</strong>. It's loosely the crypto analog of a settlement fail — but it can hit <em>after</em> you booked.</p>","trap":"Crediting a deposit at 1 confirmation. A reorg can then un-credit it, creating a phantom balance / negative break.","worked":"Policy: BTC deposits credited at 6 confs, an L2/chain with deterministic finality at 1. A recon job re-checks recently-credited txns for reorg before treating them as immutable.","related":["crypto-vs-equities-posttrade","settlement-window-247","crypto-recon-challenges"]},
  "crypto-recon-challenges": {"term":"Crypto Reconciliation Challenges","group":"IP-CRYPTO","eli5":"Crypto recon is harder because there's no single source of truth and money moves across many chains with fees and delays.","memory":"No depository, Many chains, Gas, Addresses, Confirmations, Reorgs, 24/7 — the seven crypto break-makers.","full":"<p>The recon primitive is unchanged — internal vs external truth, match on stable identifiers, tolerate expected discrepancies, unmatched = breaks. What's crypto-specific are the identifiers and tolerances: <em>no central depository</em> (truth is fragmented across CEX APIs + chains + custodians), <em>multiple chains</em> ((chain, asset, address) is the real key), <em>gas/fees</em> (sent ≠ received), <em>address management</em>, <em>confirmation lag</em> (pending vs settled), <em>reorgs</em>, and <em>24/7</em> operation (no snapshot window).</p>","trap":"Matching on symbol alone. USDC on Ethereum vs Polygon are different settlement realities — you must key on (chain, asset, address).","worked":"A withdrawal of 100 USDC arrives as 99.7 — the 0.3 is gas; reconcile on the on-chain delta net of fees, not the requested amount, or you log a false break.","related":["reconciliation-primitive","reorg-finality","settlement-window-247"]},
  "reconciliation-primitive": {"term":"Reconciliation Primitive","group":"IP-CRYPTO","eli5":"Compare your records to an outside source of truth, match on solid IDs, allow for expected differences, and anything left over is a break to fix fast.","memory":"Internal vs Truth &rarr; Match on stable IDs &rarr; Tolerate expected gaps &rarr; Breaks &rarr; Resolve.","full":"<p>Reconciliation compares <strong>internal records vs an external truth source</strong> (clearing/custodian SOD files, exchange/settlement files, on-chain data), <strong>matching on stable identifiers</strong> (provider/trade id, amount, currency, symbol, settlement date), while <strong>tolerating expected discrepancies</strong> (timing gaps, partial fills, fees, chargebacks, rounding). Unmatched records are <strong>breaks</strong>, resolved via manual or bulk correction in tooling.</p><p>It is identical in shape to payments recon (e.g. Devexperts syncing a platform vs Apex clearing SOD), which is why a billing/ledger background transfers directly.</p>","trap":"Treating every mismatch as a real break. Fees, partial fills, and timing differences are <em>expected</em> — bake them in as tolerances or you drown ops in false positives.","worked":"FULL OUTER JOIN internal vs exchange fills on trade_id; flag left_only (phantom), right_only (missed), and notional deltas beyond a fee tolerance.","related":["crypto-recon-challenges","idempotency-pattern","t-plus-one"]},
  "idempotency-pattern": {"term":"Idempotency Key Pattern","group":"IP-CRYPTO","eli5":"Tag each request with a key so retrying after a timeout never double-submits an order or a withdrawal.","memory":"Same key + same body = same answer; same key + different body = 409.","full":"<p>The IETF/Stripe-style pattern stores <code>(caller, idempotency_key) -&gt; request_hash + response + status</code>. A retry with the <strong>same key and same payload</strong> returns the <em>original</em> stored response (including the original error); the <strong>same key with a different payload</strong> returns <strong>409 Conflict</strong>.</p><p>Over flaky, 24/7 crypto REST APIs a timeout doesn't reveal whether the order/withdrawal landed, so this makes retries-with-backoff safe and prevents double-spends. The exchange-side handle is usually a self-generated <strong>client order id</strong> the venue dedupes on.</p>","trap":"Generating the key on retry instead of before the first send. The key must be created once, persisted, and reused on every attempt — otherwise it doesn't dedupe anything.","worked":"Order submit times out; you resend with the same clOrdID/idempotency_key — the exchange returns the original ExecutionReport instead of opening a second position.","related":["reconciliation-primitive","fix-drop-copy","settlement-window-247"]},
  "settlement-window-247": {"term":"24/7 Settlement (No Window)","group":"IP-CRYPTO","eli5":"Crypto trades and settles around the clock, so there's never a quiet period to batch-reconcile.","memory":"No SOD, no EOD, no cutoff — recon runs continuously, not nightly.","full":"<p>Crypto markets run <strong>24/7/365</strong> with on-chain settlement and no fixed window, so there is no start-of-day or end-of-day cutoff to anchor a nightly batch recon. Breaks must be detected and resolved <em>continuously</em>, and confirmation/finality state changes at all hours.</p><p>This is the endpoint of the same trend that T+1 started in equities: as the settlement window shrinks toward zero, automated continuous reconciliation and exception handling become mandatory rather than optional.</p>","trap":"Porting an equities nightly-batch recon design to crypto. There's no quiet snapshot moment — you need streaming/continuous recon.","worked":"Instead of a 5pm batch, a streaming job reconciles fills and on-chain deposits on a rolling window and alerts on breaks any hour of the day or night.","related":["t-plus-one","crypto-recon-challenges","reorg-finality"]},
  "crypto-vs-equities-posttrade": {"term":"Crypto vs Equities Post-Trade","group":"IP-CRYPTO","eli5":"Crypto has no central clearer, settles on-chain at all hours, and makes you hold custody — equities have DTCC, T+1, and book-entry.","memory":"Equities: DTCC + T+1 + book-entry custody. Crypto: no depository + on-chain finality + 24/7 + self-custody.","full":"<p>Equities clear through <strong>DTCC</strong> (NSCC nets/novates/guarantees as the CCP; DTC custodies book-entry) on a deterministic <strong>T+1</strong> cycle, with custody handled for you. Crypto has <em>no CCP or depository</em>: settlement is bilateral/on-chain with <strong>probabilistic or deterministic finality</strong> (and reorg risk), runs <strong>24/7</strong>, and the firm holds its own keys (hot/cold, MPC/multisig). Recon must additionally handle multiple chains, gas, addresses, and confirmation lag.</p>","trap":"Assuming a crypto 'settlement fail' works like equities. There's no CCP to guarantee or buy-in; a reorg or a lost-key event is irreversible and self-inflicted.","worked":"An equity fail is backstopped by NSCC's guarantee and CNS (Continuous Net Settlement); a crypto mis-send has no backstop — the control had to be the confirmation/quorum policy that prevents it.","related":["t-plus-one","reorg-finality","custody-crux","settlement-window-247"]}
});
