/* The Shuffle (SPARK-SHUFFLE) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"SPARK-SHUFFLE","front":"What is a shuffle in Spark?","back":"A <strong>shuffle</strong> is the physical redistribution of data across partitions (and usually across executors/nodes over the network) so that all records sharing a key land in the same partition. It is the boundary between two <em>stages</em> in Spark's DAG: the stage upstream of the shuffle is the <em>map side</em>, the stage downstream is the <em>reduce side</em>."},
  {"sec":"SPARK-SHUFFLE","front":"Which transformations trigger a shuffle?","back":"<strong>Wide transformations</strong> — those whose output partition depends on data from many input partitions. Common ones: <code>join</code> (non-broadcast), <code>groupBy</code>/<code>groupByKey</code>, <code>reduceByKey</code>, <code>aggregateByKey</code>, <code>distinct</code>, <code>repartition</code>, <code>repartitionByRange</code>, and <code>sortBy</code>/global sort. <code>map</code>, <code>filter</code>, <code>select</code>, and <code>union</code> are narrow and never shuffle."},
  {"sec":"SPARK-SHUFFLE","front":"Narrow vs wide dependency — the precise distinction","back":"In a <strong>narrow dependency</strong> each parent partition is consumed by at most one child partition (1:1, or N:1 like <code>coalesce</code>), so the work pipelines within a single task with no data movement. In a <strong>wide dependency</strong> a parent partition feeds <em>many</em> child partitions, forcing a shuffle. Wide dependencies define stage boundaries; the entire chain of narrow transforms collapses into one stage."},
  {"sec":"SPARK-SHUFFLE","front":"What physically happens on the map (write) side of a shuffle?","back":"Each map task applies the partitioner (typically <code>hash(key) % numPartitions</code>) to every record, buffers/sorts per target partition, and writes a single <strong>shuffle data file</strong> plus an <strong>index file</strong> to local disk; records are serialized on write. Spark picks one of three writers (bypass-merge-sort, serialized/unsafe, or the fallback <code>SortShuffleWriter</code>). Output is NOT pushed eagerly — it sits on local disk for reducers to pull."},
  {"sec":"SPARK-SHUFFLE","front":"What physically happens on the reduce (fetch) side of a shuffle?","back":"Each reduce task fetches <em>its</em> partition's blocks from the map outputs across all executors over the network (via the <code>BlockManager</code> or external shuffle service), deserializes them, and merges (aggregating or sorting). The all-to-all M×R fetch pattern — every reducer reading a slice from many map outputs — is why shuffle network cost grows with both map and reduce parallelism. The reduce side can't start until map outputs are materialized."},
  {"sec":"SPARK-SHUFFLE","front":"Why is shuffle the dominant cost in a Spark job?","back":"It is the one operation that simultaneously pays <strong>all three</strong> expensive costs:<ul><li><strong>Serialization/deserialization</strong> of every record crossing the boundary</li><li><strong>Disk I/O</strong> — map outputs are always written to local disk, and reducers spill when they don't fit memory</li><li><strong>Network I/O</strong> — the M×R all-to-all transfer between executors</li></ul>Narrow transforms pay none of these, so minimizing shuffles is the #1 tuning lever."},
  {"sec":"SPARK-SHUFFLE","front":"What does spark.sql.shuffle.partitions control, and what is its default?","back":"It sets the number of <strong>post-shuffle partitions</strong> (reduce-side parallelism) for DataFrame/SQL shuffles like joins and aggregations. Default is <code>200</code>. Note the RDD API uses <code>spark.default.parallelism</code> instead — <code>spark.sql.shuffle.partitions</code> only governs the SQL/DataFrame engine."},
  {"sec":"SPARK-SHUFFLE","front":"Why is the default 200 shuffle partitions often wrong?","back":"It is a fixed constant independent of data size. On a <strong>small</strong> dataset, 200 creates tiny partitions and huge scheduling/task overhead; on a <strong>large</strong> dataset, 200 makes each partition huge, forcing heavy spill and risking OOM. Rule of thumb: target ~100-200 MB per post-shuffle partition. Since Spark 3, <strong>Adaptive Query Execution</strong> can coalesce this number at runtime, mitigating (but not eliminating) the need to tune it."},
  {"sec":"SPARK-SHUFFLE","front":"What is shuffle spill and what triggers it?","back":"When the reduce side's aggregation/sort buffer for a partition exceeds available execution memory, Spark <strong>spills</strong> the sorted in-memory data to local disk, then merges the spilled runs later. Triggered by partitions too large to fit memory — usually from skew or too few shuffle partitions. Watch <em>Shuffle Spill (Memory)</em> and <em>Shuffle Spill (Disk)</em> in the UI; non-trivial disk spill means you should increase partitions or fix skew."},
  {"sec":"SPARK-SHUFFLE","front":"How do you actually reduce shuffle cost (tuning levers)?","back":"<ul><li><strong>Broadcast join</strong> the small side to eliminate the shuffle (<code>spark.sql.autoBroadcastJoinThreshold</code>, default 10 MB)</li><li>Use <code>reduceByKey</code>/aggregation that <strong>map-side combines</strong> instead of <code>groupByKey</code></li><li>Filter and project (<code>select</code> fewer columns) <em>before</em> the shuffle to shrink bytes moved</li><li><strong>Pre-partition</strong> (bucketing) so repeated joins reuse the same layout</li><li>Right-size <code>spark.sql.shuffle.partitions</code> and enable AQE</li></ul>"},
  {"sec":"SPARK-SHUFFLE","front":"Why does groupByKey lose to reduceByKey for aggregations?","back":"<code>reduceByKey</code> applies a <strong>map-side combiner</strong>: it partially aggregates within each map partition before the shuffle, so far fewer bytes cross the network. <code>groupByKey</code> shuffles <em>every</em> raw value to the reducer and can OOM a single reducer when one key has many values. Prefer <code>reduceByKey</code>/<code>aggregateByKey</code> (or DataFrame <code>groupBy().agg()</code>, which combines automatically)."},
  {"sec":"SPARK-SHUFFLE","front":"What is a stage boundary and how does it relate to shuffles?","back":"Spark splits a job into <strong>stages</strong> at every wide dependency: each shuffle creates exactly one boundary. Within a stage, narrow transforms pipeline inside a single task with no materialization. So the number of shuffle/map stages in a query's physical plan equals its shuffle count — counting stages in the UI is a quick proxy for how many shuffles a query pays."},
  {"sec":"SPARK-SHUFFLE","front":"How does data skew interact with the shuffle?","back":"The shuffle sends all records for a key to one reducer. If one key (or a null) dominates, that reducer's partition is enormous: it spills heavily, runs far longer than its peers (straggler), or OOMs — while other tasks sit idle. Symptom in the UI: one task's shuffle-read bytes/duration is orders of magnitude above the median. Fixes: salting the hot key, isolating/filtering nulls, or AQE skew-join handling (<code>spark.sql.adaptive.skewJoin.enabled</code>)."},
  {"sec":"SPARK-SHUFFLE","front":"What does the external shuffle service do, and why does it matter for dynamic allocation?","back":"The <strong>external shuffle service</strong> is a long-lived process (one per node — e.g. inside the YARN NodeManager, or a standalone daemon) that serves map-output shuffle files independently of the executors that wrote them. This lets Spark <strong>remove idle executors</strong> under dynamic allocation without losing their shuffle data, since the service keeps serving fetches. Without it, decommissioning an executor would destroy its shuffle files and force recomputation."},
  {"sec":"SPARK-SHUFFLE","front":"Does repartition vs coalesce both shuffle?","back":"No. <code>repartition(n)</code> always triggers a <strong>full shuffle</strong> to produce <em>n</em> evenly-sized partitions (can increase or decrease count). <code>coalesce(n)</code> only <em>decreases</em> partition count and is a <strong>narrow</strong> transform — it merges adjacent partitions on the same executor with no network movement, but can produce uneven partitions and reduce upstream parallelism. Use <code>coalesce</code> to cheaply shrink output files; use <code>repartition</code> when you need balance or more partitions."},
  {"sec":"SPARK-SHUFFLE","front":"Which shuffle writer does Spark choose, and does it sort within partitions?","back":"Three writers: <strong>BypassMergeSortShuffleWriter</strong> (no map-side combine and partitions &lt; <code>spark.shuffle.sort.bypassMergeThreshold</code>, default 200) writes one file per partition then concatenates — <em>no</em> intra-partition sort; <strong>UnsafeShuffleWriter</strong> (serialized sort on raw bytes) when records can be relocated serialized; and the fallback <strong>SortShuffleWriter</strong>, which sorts/aggregates in memory and spills. All three produce one data + one index file per map task."}
);

Object.assign(GLOSSARY_DATA, {
  "wide-dependency": {"term":"Wide Dependency","group":"SPARK-SHUFFLE","aliases":["wide dependency","wide transformation","shuffle dependency","ShuffleDependency"],"eli5":"A transformation where one input partition feeds many output partitions, so data has to be reshuffled.","memory":"WIDE = one parent fans out WIDE to many children. Narrow = single-file line.","full":"<p>A dependency is <strong>wide</strong> when a single parent partition contributes records to multiple child partitions — the hallmark of operations that regroup by key (<code>join</code>, <code>groupBy</code>, <code>distinct</code>, <code>repartition</code>). Contrast with a <strong>narrow</strong> dependency, where each parent partition feeds at most one child and the work pipelines within a task. Spark's DAG scheduler cuts a new <em>stage</em> at every wide dependency.</p><p>Internally a wide dependency is represented as a <code>ShuffleDependency</code> carrying a partitioner; narrow ones are <code>OneToOneDependency</code>/<code>NarrowDependency</code>. Recognizing which transforms are wide is the core skill: it tells you exactly where shuffles (and thus cost) will appear in a plan before you ever run it.</p>","trap":"<code>coalesce</code> looks like it should shuffle (it changes partition count) but it's narrow — it only merges adjacent partitions. <code>repartition</code> is the wide one. Mixing these two up is a classic miss.","worked":"<code>rdd.map(f)</code> is narrow (partition i → partition i). <code>rdd.reduceByKey(f)</code> is wide: records for key K may start in any of the N input partitions and must converge on one output partition.","related":["shuffle","map-side-reduce-side","shuffle-partitions"]},
  "map-side-reduce-side": {"term":"Map Side / Reduce Side","group":"SPARK-SHUFFLE","aliases":["map side","reduce side","shuffle write","shuffle read","shuffle file"],"eli5":"The map side writes shuffle files to local disk; the reduce side pulls its slice of every file over the network.","memory":"M×R = the all-to-all fan. More reducers and more mappers both multiply network requests.","full":"<p>Every shuffle has two halves. On the <strong>map (write) side</strong>, each task applies the partitioner to its records, buffers/sorts per target partition, serializes, and writes one shuffle <em>data</em> file plus an <em>index</em> file to local disk. Spark chooses among three writers — <code>BypassMergeSortShuffleWriter</code> (no intra-partition sort), <code>UnsafeShuffleWriter</code> (serialized sort), or the fallback <code>SortShuffleWriter</code>. On the <strong>reduce (fetch/read) side</strong>, each task requests its partition's blocks from the map outputs across the cluster, deserializes, and merges (aggregate or sort).</p><p>The fetch pattern is all-to-all: <em>M</em> map outputs and <em>R</em> reducers means on the order of M×R block transfers. This M×R explosion — plus the mandatory disk write on the map side — is the mechanical reason shuffle dominates cost and why it doesn't pipeline across the stage boundary (the reduce side can't start until map outputs are materialized).</p>","trap":"'Does the map side send data to reducers?' No — map output sits on local disk and reducers PULL it. This pull model is what lets the external shuffle service keep serving after the executor dies. (And don't claim shuffle output is 'always sorted' — the bypass-merge writer skips the intra-partition sort.)","worked":"With 500 map tasks and 200 reduce tasks, the reduce side issues on the order of 500×200 = 100,000 block fetches (batched by destination); each reducer reads one slice from all 500 map files.","related":["shuffle","shuffle-partitions"]},
  "shuffle-partitions": {"term":"spark.sql.shuffle.partitions","group":"SPARK-SHUFFLE","aliases":["spark.sql.shuffle.partitions","shuffle partitions","post-shuffle partitions","200 partitions"],"eli5":"The knob that sets how many partitions exist after a SQL/DataFrame shuffle — default 200.","memory":"200 is a magic constant, not a smart default — it ignores your data size entirely.","full":"<p><code>spark.sql.shuffle.partitions</code> sets the post-shuffle (reduce-side) partition count for DataFrame/SQL operations such as joins and aggregations; its default is <strong>200</strong>. It directly determines reduce-side parallelism and the size of each post-shuffle partition. Too few → giant partitions that spill or OOM; too many → tiny tasks dominated by scheduling overhead. A common target is ~100-200 MB per partition.</p><p>It applies only to the SQL/DataFrame engine — RDD shuffles default to <code>spark.default.parallelism</code> instead. Since Spark 3.0, <strong>Adaptive Query Execution</strong> dynamically coalesces post-shuffle partitions based on actual shuffle-output statistics, so the static 200 is increasingly a starting point rather than a hard setting — though for AQE coalescing it acts as the upper bound on the partition count (via <code>spark.sql.adaptive.coalescePartitions.initialPartitionNum</code>, which defaults to this value).</p>","trap":"Candidates set <code>spark.sql.shuffle.partitions</code> and expect RDD jobs to change — they won't; RDDs honor <code>spark.default.parallelism</code>. Also, AQE's coalescing only <em>merges</em> down from this initial count and never coalesces above it — so set it generously and let AQE shrink it (note AQE skew-splitting is a separate path that <em>can</em> create more sub-partitions for hot keys).","worked":"Joining two 500 GB tables with the default 200 gives ~5 GB per post-shuffle partition — massive spill. Bumping to ~5,000 (or enabling AQE) yields ~200 MB partitions that fit memory.","related":["shuffle","shuffle-spill","adaptive-query-execution","map-side-reduce-side"]},
  "shuffle-spill": {"term":"Shuffle Spill","group":"SPARK-SHUFFLE","aliases":["shuffle spill","spill to disk","spill"],"eli5":"When a shuffle partition is too big to aggregate in memory, Spark dumps it to disk and merges later.","memory":"Spill = Spark choosing slow-but-alive (disk) over fast-but-dead (OOM).","full":"<p>Spill happens when the in-memory buffer used to aggregate or sort a shuffle partition exceeds available execution memory. Spark sorts what it has, writes that run to local disk, frees the memory, and continues; at the end it merges the spilled runs. It is a correctness safety valve that prevents OOM, but it adds extra disk writes and reads on top of the shuffle's mandatory I/O.</p><p>The Spark UI reports two numbers: <strong>Shuffle Spill (Memory)</strong> — the in-memory size before spilling — and <strong>Shuffle Spill (Disk)</strong> — the serialized on-disk size. Non-trivial disk spill is a red flag pointing to partitions that are too large: increase <code>spark.sql.shuffle.partitions</code>, fix skew, or grant more execution memory.</p>","trap":"'Spill (Memory)' being large is NOT itself bad — it's the unspilled in-memory footprint; the metric that hurts is 'Spill (Disk)'. Confusing the two leads people to chase the wrong number.","worked":"A skewed join where one reducer gets 8 GB of values but has 2 GB of execution memory will spill ~6 GB to disk, turning a 30-second task into a multi-minute straggler.","related":["shuffle","shuffle-partitions","data-skew","map-side-reduce-side"]},
  "broadcast-join": {"term":"Broadcast Hash Join","group":"SPARK-SHUFFLE","aliases":["broadcast join","broadcast hash join","map-side join","autoBroadcastJoinThreshold"],"eli5":"If one table is small enough, ship a full copy to every executor so the join needs no shuffle at all.","memory":"Broadcast = copy the dwarf to everyone so the giant never has to move.","full":"<p>A broadcast hash join avoids the shuffle by sending a complete copy of the <em>small</em> side to every executor, where it becomes an in-memory hash table; the large side is then joined locally with no data movement. This is the single most effective shuffle-elimination technique. Spark auto-selects it when the estimated small-side size is under <code>spark.sql.autoBroadcastJoinThreshold</code> (default <strong>10 MB</strong>), or you can force it with the <code>broadcast()</code> hint.</p><p>The trade-off: the broadcast table must fit in each executor's memory and the driver must collect it first, so broadcasting something too large causes driver OOM or network storms. AQE can also convert a planned sort-merge join into a broadcast join at runtime if actual statistics show one side is small.</p>","trap":"Forcing <code>broadcast()</code> on a table that's actually large is a classic foot-gun — it OOMs the driver/executors. Also, broadcast only helps when one side is small; it doesn't fix a shuffle between two large tables.","worked":"Joining a 2 TB fact table to a 5 MB dimension table: broadcasting the dimension turns an all-to-all shuffle into a local hash lookup, often cutting runtime by an order of magnitude.","related":["shuffle","wide-dependency","adaptive-query-execution"]},
  "data-skew": {"term":"Data Skew","group":"SPARK-SHUFFLE","aliases":["data skew","skew","skewed key","hot key","straggler"],"eli5":"One key has way more rows than the others, so its reducer becomes a slow, memory-hungry bottleneck.","memory":"Skew = one fat partition holding the whole stage hostage; the cluster is only as fast as its worst reducer.","full":"<p>Because a shuffle routes all records for a key to a single reduce partition, an uneven key distribution makes one (or a few) partitions far larger than the rest. That reducer becomes a <strong>straggler</strong>: it spills heavily or OOMs while every other task finishes and sits idle, so the stage's wall-clock time is hostage to the worst partition. Nulls and default/sentinel values are frequent culprits.</p><p>Diagnose it in the UI by comparing max vs. median task shuffle-read bytes and duration within a stage — a 10-100× gap signals skew. Remedies: <strong>salting</strong> (append a random suffix to the hot key to spread it across reducers, then re-aggregate), isolating/filtering nulls, broadcasting the other side, or enabling AQE skew-join handling (<code>spark.sql.adaptive.skewJoin.enabled</code>), which splits oversized partitions into sub-partitions automatically.</p>","trap":"Adding more <code>shuffle.partitions</code> does NOT fix skew — a single hot key still hashes to exactly one partition no matter how many you have. You need salting or AQE skew handling, not more parallelism.","worked":"A join keyed on <code>country</code> where 60% of rows are 'US': the US reducer processes 600 GB while others handle a few GB. Salting US into US_0..US_99 spreads it across 100 reducers.","related":["shuffle","shuffle-spill","shuffle-partitions","adaptive-query-execution"]},
  "shuffle-writer": {"term":"Shuffle Writer (map-side write path)","group":"SPARK-SHUFFLE","aliases":["shuffle writer","SortShuffleWriter","BypassMergeSortShuffleWriter","UnsafeShuffleWriter","bypass merge sort"],"eli5":"The map-side component that partitions a task's output and writes the one data file plus index file per map task — Spark picks one of three implementations.","memory":"Three writers: Bypass (few partitions, no sort), Unsafe (serialized sort), Sort (fallback, the one that aggregates/spills).","full":"<p>Since the sort-based shuffle replaced hash shuffle, every map task writes exactly <em>one</em> shuffle data file plus an index file (the index records each partition's offset). Spark's <code>SortShuffleManager</code> selects among three writer implementations at runtime. <strong>BypassMergeSortShuffleWriter</strong> is used when there is no map-side combine and the partition count is below <code>spark.shuffle.sort.bypassMergeThreshold</code> (default 200): it opens one file per partition and concatenates them, doing <em>no</em> sort within partitions. <strong>UnsafeShuffleWriter</strong> (serialized/'tungsten' sort) is used when records can be relocated in serialized form and there's no map-side aggregation; it sorts compressed record pointers, not deserialized objects.</p><p><strong>SortShuffleWriter</strong> is the general fallback used when the other two don't apply (e.g. map-side combine is required, or partition count exceeds the bypass threshold). It buffers records in an <code>ExternalSorter</code>, optionally aggregates/sorts, and spills to disk when memory is exceeded. Knowing which path runs explains why 'shuffle output is always sorted' is false and why few-partition shuffles can be cheaper.</p>","trap":"Don't call <code>SortShuffleWriter</code> 'the' shuffle write path — it's the fallback. With &lt;200 partitions and no map-side combine, Spark uses the bypass writer, which does NOT sort within partitions.","worked":"<code>rdd.repartition(50)</code> (no combine, 50 &lt; 200) → BypassMergeSortShuffleWriter, no intra-partition sort. <code>rdd.reduceByKey(f)</code> with 1,000 partitions → SortShuffleWriter, because map-side combine is required.","related":["map-side-reduce-side","shuffle","shuffle-spill"]}
});
