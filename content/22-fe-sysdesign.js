/* Frontend System Design & Patterns (FE-SYSDESIGN) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"FE-SYSDESIGN","front":"Compare CSR, SSR, SSG, and ISR across TTFB, FCP/LCP, and staleness — and how to choose.","back":"<ul><li><strong>CSR (SPA)</strong>: server ships an empty shell + JS bundle. <em>Fast TTFB</em> (static HTML) but <em>slow FCP/LCP</em> — nothing paints until JS downloads, parses, and fetches data. Bad for SEO/link previews; great for app-like dashboards behind auth.</li><li><strong>SSR</strong>: HTML rendered <em>per request</em>. Good FCP and SEO, but <em>TTFB pays</em> the render + data-fetch cost on every hit, and you still ship JS to <strong>hydrate</strong>. Use for personalized, dynamic, SEO-critical pages.</li><li><strong>SSG</strong>: HTML built <em>once at build time</em>, served from CDN — best TTFB/LCP, cache-friendly, but <em>stale</em> until rebuild, and build time scales with page count.</li><li><strong>ISR / on-demand revalidation</strong>: SSG + per-path TTL or webhook invalidation; serves cached HTML and regenerates in the background (<code>stale-while-revalidate</code> semantics). Best of static + freshness.</li></ul><strong>Choose by</strong>: data freshness need, personalization, SEO, and page count. Default to static; reach for SSR only where per-request rendering (personalization/freshness) is truly required."},
  {"sec":"FE-SYSDESIGN","front":"Why is hydration the dominant cost of SSR, and what techniques reduce it?","back":"SSR sends HTML the user can see, but it's <em>inert</em> — React/Vue must re-run the component tree on the client to attach event handlers and rebuild state. This <strong>hydration</strong> ships and executes JS roughly proportional to the whole page, blocking the main thread and hurting <strong>INP</strong> even though pixels appeared early (the <em>uncanny valley</em>: looks ready, isn't interactive).<ul><li><strong>Streaming SSR + Suspense</strong>: flush HTML in chunks (<code>renderToPipeableStream</code>); React 18+ <em>selective hydration</em> prioritizes the part the user interacts with first and hydrates out of order.</li><li><strong>Islands / partial hydration</strong> (Astro): ship static HTML, hydrate only interactive widgets, often lazily (on idle/visible).</li><li><strong>Resumability</strong> (Qwik): serialize listener/state info into HTML so the client <em>resumes</em> instead of re-executing the tree — near-zero startup JS.</li><li><strong>RSC</strong>: Server Components never ship to the client at all; only Client Components hydrate.</li></ul>"},
  {"sec":"FE-SYSDESIGN","front":"What are React Server Components (RSC) and how do they differ from SSR?","back":"<strong>RSC</strong> (stable in React 19, Dec 2024; consumed via frameworks like Next.js App Router) render <em>only on the server</em> and serialize to an <strong>RSC payload</strong> — a component-tree description, <em>not</em> HTML and <em>not</em> hydrated JS. Their code <em>never enters the client bundle</em>.<ul><li><strong>SSR</strong> renders components to HTML <em>then hydrates the same components</em> on the client. RSC = some components are server-only with <em>no</em> client counterpart and no hydration. (They're complementary: a framework can SSR the RSC payload to initial HTML <em>and</em> stream it.)</li><li>Server Components can <code>async/await</code> data directly (DB, fs) — no API round-trip, no client loading waterfall.</li><li><code>'use client'</code> marks the boundary where interactivity (state, effects, handlers) begins.</li><li>Wins: <strong>smaller bundles</strong>, no data-fetch waterfalls, secrets stay server-side. Cost: new mental model, a server/build runtime, and serialization limits (props crossing into Client Components must be serializable — no functions/classes/Dates-as-methods).</li></ul>"},
  {"sec":"FE-SYSDESIGN","front":"Composition over configuration: what's the design failure of a 'god component' with 30 props?","back":"A component that accepts dozens of boolean/enum props (<code>showHeader</code>, <code>variant</code>, <code>withIcon</code>, <code>iconPosition</code>...) suffers <strong>combinatorial prop explosion</strong>: an untestable permutation space, conditional spaghetti inside the implementation, and every new use case forcing an API change to a widely-shared component.<br><strong>Composition</strong> inverts control — expose <em>primitives</em> the caller assembles:<ul><li><strong>Slots / children</strong>: <code>&lt;Card&gt;&lt;Card.Header/&gt;&lt;Card.Body/&gt;&lt;/Card&gt;</code> instead of a <code>header</code> prop.</li><li><strong>Compound components</strong>: share implicit state via context (<code>&lt;Tabs&gt;&lt;Tab/&gt;&lt;TabPanel/&gt;&lt;/Tabs&gt;</code>) so the parent coordinates without prop drilling.</li><li><strong>Render props / headless hooks</strong> (e.g. <code>useCombobox</code>): hand the caller behavior + state and let them own markup.</li></ul>Rule: <em>props for data, composition for structure.</em> If a prop only exists to toggle rendering of a child, make it a child."},
  {"sec":"FE-SYSDESIGN","front":"Controlled vs uncontrolled components — when do you choose each, and what's the gotcha?","back":"<ul><li><strong>Controlled</strong>: value lives in React state; <code>value</code> + <code>onChange</code> on every keystroke. Single source of truth, enables validation/formatting/derived UI, but re-renders on each input and you own all state.</li><li><strong>Uncontrolled</strong>: the DOM holds the value; read it via <code>ref</code> / <code>defaultValue</code>. Less code, fewer re-renders, plays well with non-React form libs. Note: <code>&lt;input type=\"file\"&gt;</code> is <em>always</em> uncontrolled (its value is read-only for security).</li></ul><strong>Gotcha</strong>: passing <code>value={undefined}</code> then later a string flips a field controlled↔uncontrolled, which React warns about and can reset cursor/selection. <em>Always initialize controlled values</em> (<code>value={x ?? ''}</code>). For big forms, libraries like React Hook Form deliberately keep inputs <em>uncontrolled</em> (register via ref) and subscribe surgically to minimize re-renders."},
  {"sec":"FE-SYSDESIGN","front":"Design tokens: what are they and why is a token layer the backbone of a design system?","back":"<strong>Design tokens</strong> are named, platform-agnostic design decisions stored as data (often JSON / the W3C DTCG format), e.g. <code>color.brand.primary</code>, <code>space.4</code>, <code>font.size.body</code>. A build step (Style Dictionary) compiles them to CSS variables, iOS, Android, etc., so <em>one source of truth</em> feeds every platform.<br>The power is <strong>tiered tokens</strong>:<ul><li><strong>Primitive/global</strong>: raw palette (<code>blue-500: #3b82f6</code>).</li><li><strong>Semantic/alias</strong>: intent (<code>color.action.bg → blue-500</code>).</li><li><strong>Component</strong>: <code>button.primary.bg → color.action.bg</code>.</li></ul>Components reference <em>semantic/component</em> tokens, never primitives, so retheming (dark mode, white-label, rebrand) is a value swap with <em>no component changes</em>. Exposing semantic tokens as <code>:root</code> CSS custom properties makes runtime theming a class/<code>data-*</code> attribute toggle."},
  {"sec":"FE-SYSDESIGN","front":"Module Federation micro-frontends: the real tradeoffs.","back":"<strong>Module Federation</strong> (native to Webpack 5 / Rspack; community plugins for Vite via <code>@module-federation/vite</code>) lets a <em>host</em> app load independently-built JS modules from <em>remotes</em> at <strong>runtime</strong>, so teams deploy independently and declare <code>shared</code> deps (e.g. <code>shared: { react: { singleton: true } }</code>) to avoid duplicate downloads.<br><strong>Wins</strong>: team/deploy autonomy, independent release cadence, incremental migration off a monolith.<br><strong>Costs / gotchas</strong>:<ul><li><strong>Version skew</strong>: mismatched shared <code>react</code> must be a <code>singleton</code> or you get two React copies → hook/runtime crashes.</li><li><strong>Bundle bloat</strong>: undisciplined sharing means remotes ship duplicate deps.</li><li><strong>Runtime coupling</strong>: a remote going down can break the host — needs error boundaries + fallbacks and remote version pinning.</li><li>Harder cross-MFE design consistency, shared perf budgets, and end-to-end tracing.</li></ul>Don't adopt for a single team — overhead only pays off at <em>org scale</em>. A monorepo + shared libraries is usually the simpler answer."},
  {"sec":"FE-SYSDESIGN","front":"Offset vs cursor pagination — why does offset break at scale?","back":"<ul><li><strong>Offset</strong> (<code>LIMIT 20 OFFSET 10000</code>): simple, supports jump-to-page and total counts. But the DB still <em>scans and discards</em> the skipped rows → <strong>O(offset)</strong> slowdown on deep pages, and rows inserted/deleted mid-scroll cause <strong>duplicates or skipped items</strong> (the window shifts under you).</li><li><strong>Cursor / keyset</strong> (<code>WHERE id &lt; :lastId ORDER BY id DESC LIMIT 20</code>): uses an indexed sort key as a pointer. <em>Constant time</em> regardless of depth and <strong>stable</strong> against concurrent inserts — ideal for infinite scroll and real-time feeds.</li></ul><strong>Cursor downsides</strong>: no random page jumps, no cheap total count, and the sort key must be unique + stable — use a <em>composite</em> cursor like <code>(created_at, id)</code> to break ties deterministically. Default to cursor for feeds; offset only for small, bounded, page-numbered admin tables."},
  {"sec":"FE-SYSDESIGN","front":"List virtualization (windowing): what problem does it solve and what are the gotchas?","back":"Rendering 10k+ DOM nodes destroys memory, layout, and scroll perf. <strong>Virtualization</strong> renders only the rows in (and just around) the viewport, recycling nodes as you scroll — the list <em>feels</em> infinite but the DOM stays tiny (TanStack Virtual, react-window).<br>Mechanics: a full-height spacer (or absolutely-positioned rows) creates the real scrollbar; visible rows are translated to the correct offset (<code>transform: translateY</code>).<br><strong>Gotchas</strong>:<ul><li><strong>Variable/dynamic row heights</strong> need measurement + estimated sizes, or scroll position jumps.</li><li><strong>Ctrl+F / SEO</strong>: off-screen content isn't in the DOM, so browser find and crawlers miss it.</li><li><strong>a11y</strong>: focus on a row that unmounts is lost; manage <code>aria-rowcount</code>/<code>aria-setsize</code> and focus restoration.</li><li>Pairs with cursor pagination for <em>infinite</em> remote data, not just a big static array.</li></ul>"},
  {"sec":"FE-SYSDESIGN","front":"WebSocket vs SSE vs polling — pick the transport for a real-time feature.","back":"<ul><li><strong>Long polling</strong>: client repeatedly requests and the server holds the response until data exists; simple and works everywhere, but high overhead and latency. Fallback tier only.</li><li><strong>SSE</strong> (<code>EventSource</code>): <em>server→client</em>, one-way, plain HTTP, auto-reconnect with <code>Last-Event-ID</code>, UTF-8 text only. Ideal for notifications, live scores, LLM token streaming. On HTTP/1.1 it consumes one of the ~6 connections per origin; over HTTP/2+ multiplexing removes that cap.</li><li><strong>WebSocket</strong>: full-duplex binary/text over one upgraded TCP connection. Use for <em>bidirectional</em>, low-latency: chat, collaborative editing, multiplayer. Cost: you own reconnection, heartbeat/ping-pong, backpressure, and auth (custom headers aren't allowed by the browser API after upgrade → pass a token via subprotocol or query string), and stateful connections complicate horizontal scaling (sticky sessions / pub-sub fan-out).</li></ul>Rule: <em>server→client only? SSE. Need client→server too? WebSocket.</em> WebTransport (HTTP/3/QUIC) is emerging for unreliable/datagram needs."},
  {"sec":"FE-SYSDESIGN","front":"Optimistic UI: how to do it correctly, and how to handle failure.","back":"<strong>Optimistic UI</strong> applies the expected result <em>immediately</em> (before the server confirms) so the app feels instant, then reconciles with the real response.<br>Correct pattern:<ul><li>Apply the optimistic mutation to a cache and <em>snapshot the previous state</em>.</li><li>On success: replace optimistic data with the server's canonical response (server may assign IDs, timestamps, run server-side validation).</li><li>On error: <strong>roll back</strong> to the snapshot and surface a non-destructive error/retry.</li></ul>Tooling: TanStack Query (React Query) <code>onMutate</code>/<code>onError</code>/<code>onSettled</code>, or React 19's <code>useOptimistic</code> hook.<br><strong>Gotchas</strong>: out-of-order responses (use temp client-generated IDs to map to server IDs), avoid optimism for <em>irreversible</em> or money-moving actions, and de-duplicate when a real-time channel echoes your own mutation back."},
  {"sec":"FE-SYSDESIGN","front":"Design an autocomplete/typeahead widget — list the senior-level concerns.","back":"<ul><li><strong>Debounce</strong> keystrokes (~150–300ms) so you query on a pause, not per character; <em>throttle</em> only if you want periodic in-flight updates.</li><li><strong>Abort stale requests</strong>: an in-flight fetch for an older query must be cancelled (<code>AbortController</code>) so a slow earlier response can't overwrite a newer one (the <em>race / last-write-wins</em> bug); guard by sequence number too.</li><li><strong>Cache</strong> results per query (LRU) — backspacing should be instant; consider prefix-caching.</li><li><strong>Keyboard a11y</strong>: implement the ARIA <code>combobox</code> pattern — <code>role=listbox</code> popup, <code>aria-activedescendant</code> for virtual focus, arrow/Enter/Esc, <code>aria-expanded</code>, and announce result counts via a live region.</li><li><strong>UX</strong>: min query length, loading + empty + error states, highlight match, reserve height to prevent CLS, and handle IME composition (don't fire mid-composition for CJK — listen for <code>compositionend</code>).</li><li>Server: ranking, typo-tolerance, response cap, and rate limiting.</li></ul>"},
  {"sec":"FE-SYSDESIGN","front":"What is a BFF (Backend-for-Frontend) and when does it beat a shared API or GraphQL gateway?","back":"A <strong>BFF</strong> is a thin server <em>owned by the frontend team</em>, typically one per client experience (web, iOS, Android), that aggregates/reshapes downstream microservices into exactly what that UI needs.<br><strong>Why</strong>:<ul><li>Kills <strong>over/under-fetching</strong> and client-side request waterfalls — one round trip, tailored payload.</li><li>Keeps secrets/tokens server-side; centralizes auth, caching, and downstream retries.</li><li>Lets web vs mobile evolve different response shapes without a generic API trying to please both.</li></ul><strong>vs GraphQL gateway</strong>: GraphQL solves over-fetching declaratively and shines for many heterogeneous clients, but adds caching complexity (POST-by-default; mitigate with persisted queries + GET), N+1 risk (need <code>DataLoader</code> batching), and query-cost/depth security concerns. <em>BFF is per-client and imperative; GraphQL is one schema for all clients.</em> Pick BFF when client needs diverge sharply; GraphQL when clients are many and the data graph is richly interrelated. (They compose: a BFF can call a GraphQL gateway.)"},
  {"sec":"FE-SYSDESIGN","front":"Responsive image and asset delivery: the levers that actually move LCP.","back":"<ul><li><strong>Right size</strong>: <code>srcset</code> + <code>sizes</code> so the browser picks a resolution-appropriate file; <code>&lt;picture&gt;</code> with <code>type</code> for <strong>AVIF→WebP→JPEG</strong> fallback (AVIF/WebP typically cut bytes 30–50%).</li><li><strong>Prioritize the hero</strong>: <em>eager</em>-load + <code>fetchpriority=\"high\"</code> the LCP image (and/or a <code>&lt;link rel=preload&gt;</code> matching the chosen source); <code>loading=\"lazy\"</code> only <em>below the fold</em> — never lazy-load the LCP image.</li><li><strong>Prevent CLS</strong>: always set <code>width</code>/<code>height</code> or <code>aspect-ratio</code> so the browser reserves space before load.</li><li><strong>CDN image service</strong>: on-the-fly resize/format/quality by query param + edge caching; serve over <strong>HTTP/2 or HTTP/3 (QUIC)</strong> to remove head-of-line blocking.</li><li>Fonts: <code>font-display: swap</code> (or <code>optional</code>) + preload + subsetting to avoid invisible-text (FOIT) and layout shift.</li></ul>"},
  {"sec":"FE-SYSDESIGN","front":"Feature flags & A/B testing: how do you avoid the flicker and the CLS, and what kinds of flags exist?","back":"Flag types: <strong>release</strong> (kill switch / gradual rollout), <strong>experiment</strong> (A/B), <strong>permission/entitlement</strong>, and <strong>ops</strong> flags — different lifecycles; experiment flags should be short-lived (clean up stale flags or they rot into tech debt and dead-code paths).<br><strong>The flicker problem</strong>: if the client fetches the variant <em>after</em> first paint, users see control then jump to treatment — a <em>flash of original content</em> + CLS that also <em>biases the experiment</em> (exposure timing skews metrics). Fixes:<ul><li><strong>Evaluate server-side</strong> (SSR/edge middleware) so HTML ships already in the assigned variant; pass the bucketing down so the client doesn't re-evaluate.</li><li><strong>Stable bucketing</strong>: hash a sticky user/anonymous ID so a user stays in one variant (consistency &gt; per-request randomness).</li><li>Run an <strong>SRM</strong> (sample-ratio-mismatch) check, fire a clean exposure event, and never branch on a flag inside a hot render path without memoizing the decision.</li></ul>"},
  {"sec":"FE-SYSDESIGN","front":"Performance budgets and the 2026 Core Web Vitals — what do you actually measure and enforce?","back":"<strong>Core Web Vitals</strong>: <code>LCP</code> (≤2.5s, load), <code>CLS</code> (≤0.1, visual stability), and <strong><code>INP</code></strong> (≤200ms, responsiveness) — INP <em>replaced FID</em> as a CWV on <strong>March 12, 2024</strong>. Unlike FID (input delay of the <em>first</em> interaction only), INP measures the <em>full</em> interaction→next-paint latency across <em>all</em> interactions, exposing main-thread blocking and slow event handlers FID missed.<br><strong>Budgets</strong> are enforced limits, not aspirations:<ul><li><strong>Resource budgets</strong>: e.g. critical-path JS &lt; ~150–170KB compressed, total transfer weight, request count, image bytes.</li><li><strong>Metric budgets</strong>: CWV thresholds at the <em>p75</em> of <em>field/RUM</em> data (Chrome UX Report), not just lab Lighthouse.</li><li><strong>Enforcement</strong>: fail CI on bundle regression (size-limit / Lighthouse-CI), track RUM in prod, alert on regressions.</li></ul>Lower INP by code-splitting, breaking up long tasks / yielding (<code>scheduler.yield()</code> where available, else <code>setTimeout</code>/<code>postTask</code>), reducing hydration, and avoiding heavy synchronous work in handlers."},
  {"sec":"FE-SYSDESIGN","front":"Resilience patterns: error boundaries, skeletons, and retries — how do they compose?","back":"<ul><li><strong>Error boundaries</strong> (React class component or <code>react-error-boundary</code>) catch errors thrown during <em>rendering</em>, lifecycle, and child constructors in a subtree and show a fallback instead of unmounting the whole tree. They do <em>NOT</em> catch event handlers, async/Promise code, or errors during SSR — wrap those with <code>try/catch</code> and reset via a <code>resetKeys</code>/key. Nest them so a failing widget doesn't blank the page.</li><li><strong>Skeletons / Suspense fallbacks</strong> reserve layout and reduce perceived latency; size them to the real content to avoid CLS, and avoid spinner-flash for sub-~300ms loads (delay showing the spinner).</li><li><strong>Retries</strong>: only for <em>idempotent</em>/transient failures, with <strong>exponential backoff + jitter</strong> and a cap; don't blindly retry 4xx. Pair with <strong>circuit breaking</strong> and request <strong>timeouts/AbortController</strong> so a dead dependency degrades gracefully (serve cached/stale data) instead of hanging.</li></ul>Compose: timeout → retry-with-backoff → fallback UI → error boundary as the last line."},
  {"sec":"FE-SYSDESIGN","front":"Offline-first & sync: what makes local-first hard, and how is conflict resolved?","back":"<strong>Offline-first</strong> treats the local store as the source of truth and syncs opportunistically. Building blocks: a <strong>Service Worker</strong> (Cache Storage + Background Sync) intercepts requests; <strong>IndexedDB</strong> holds structured data; mutations queue while offline and replay on reconnect.<br>The hard part is <strong>conflict resolution</strong> when two clients edit the same data concurrently:<ul><li><strong>Last-write-wins</strong>: simple, but silently drops one side's edits.</li><li><strong>Operational Transform / CRDTs</strong> (e.g. Yjs/Automerge): structures that <em>merge</em> concurrent edits deterministically without a central lock — the backbone of collaborative editing.</li></ul>Other gotchas: <em>tombstones</em> for deletes, vector clocks / Lamport timestamps to <em>detect</em> concurrency, idempotent replay (client-generated IDs), schema migration of already-cached data, and storage eviction under pressure. Don't go local-first unless offline/collab is a real requirement — it roughly doubles the data-model work."}
);

Object.assign(GLOSSARY_DATA, {
  "hydration": {"term":"Hydration","group":"FE-SYSDESIGN","eli5":"Server-sent HTML looks ready, but the browser still has to re-run the JS to make buttons actually work.","memory":"Dry HTML + JS water = interactive. Too much water = slow.","full":"<p><strong>Hydration</strong> is the client-side process where a JS framework attaches event listeners and rebuilds component state on top of server-rendered HTML, making static markup interactive. The framework re-executes the component tree and reconciles its virtual representation with the existing DOM.</p><p>It's the hidden tax of SSR: the page <em>paints</em> early but isn't interactive until hydration JS downloads, parses, and runs — degrading <strong>INP</strong>. Mitigations: streaming SSR with <em>selective hydration</em>, <em>partial hydration/islands</em>, <em>resumability</em> (Qwik), and <strong>RSC</strong> (Server Components never hydrate).</p>","trap":"Thinking SSR means 'no JS shipped.' SSR usually re-ships the <em>same</em> component JS to hydrate — hydration cost ≈ a full client render.","worked":"Next.js SSR sends product-page HTML; React calls <code>hydrateRoot()</code>, walks the tree, and wires up the Add-to-Cart <code>onClick</code>. Until that finishes, clicking the button does nothing.","related":["ssr","rsc","islands-architecture","core-web-vitals"]},
  "rsc": {"term":"React Server Components (RSC)","group":"FE-SYSDESIGN","eli5":"Some components run only on the server and never send their code to the browser.","memory":"'use client' is the door; everything outside it stays server-side.","full":"<p><strong>RSC</strong> render exclusively on the server and serialize to an <em>RSC payload</em> (a tree description), not HTML and not hydrated JS — their code is excluded from the client bundle. They can fetch data with <code>async/await</code> directly against databases or the filesystem, eliminating client-side fetch waterfalls and keeping secrets server-side.</p><p>Stable in React 19 (Dec 2024), consumed via frameworks like the Next.js App Router. The <code>'use client'</code> directive marks the boundary where interactivity (state/effects/handlers) begins; only Client Components ship and hydrate. Distinct from SSR, which renders components to HTML and then hydrates those same components — RSC and SSR are complementary, not alternatives.</p>","trap":"Confusing RSC with SSR. SSR hydrates; RSC components have no client counterpart and aren't hydrated at all.","worked":"<code>async function Page(){ const posts = await db.posts.find(); return &lt;List posts={posts}/&gt; }</code> — runs on the server; <code>db</code> never reaches the browser bundle.","related":["ssr","hydration","bff"]},
  "ssr": {"term":"Server-Side Rendering (SSR)","group":"FE-SYSDESIGN","eli5":"The server builds the full HTML for each request so the page shows content before JS loads.","memory":"Render on the server, hydrate on the client.","full":"<p><strong>SSR</strong> renders the component tree to HTML <em>per request</em>, giving fast first paint and SEO-friendly markup, then ships JS to <strong>hydrate</strong> that HTML into an interactive app. It suits personalized, dynamic, SEO-critical pages.</p><p>Cost: every request pays render + data-fetch time (TTFB), the server must scale with traffic, and you still pay hydration. <em>Streaming SSR</em> with Suspense (<code>renderToPipeableStream</code>) flushes HTML in chunks and enables selective hydration to soften these costs.</p>","trap":"Assuming SSR is always faster. It improves FCP/SEO but can <em>worsen</em> TTFB vs static, and doesn't remove hydration JS.","worked":"A logged-in dashboard renders user-specific HTML on each request via <code>renderToPipeableStream</code>, streaming the shell first and Suspense-deferred widgets after.","related":["hydration","rsc","csr-spa","ssg"]},
  "csr-spa": {"term":"Client-Side Rendering / SPA","group":"FE-SYSDESIGN","eli5":"The server sends a near-empty page and JavaScript draws everything in the browser.","memory":"Empty shell now, JS paints later.","full":"<p><strong>CSR</strong> ships an HTML shell plus a JS bundle that renders the UI and fetches data in the browser. TTFB is fast (static file) but FCP/LCP lag because nothing meaningful paints until JS loads, parses, and resolves data — a spinner-then-content sequence that also hurts SEO and link previews.</p><p>It shines for app-like, authenticated, highly interactive experiences (dashboards, editors) where SEO doesn't matter and the JS cost amortizes over a long session. Code-splitting and prefetching mitigate the initial bundle cost.</p>","trap":"Using CSR for content/marketing pages that need SEO and fast LCP — that's exactly where it's weakest.","worked":"A Vite + React SPA serves <code>index.html</code> with a <code>&lt;div id=root&gt;</code>; the bundle renders nothing server-side and fetches <code>/api/me</code> after mount.","related":["ssr","ssg","core-web-vitals"]},
  "ssg": {"term":"Static Site Generation (SSG) & ISR","group":"FE-SYSDESIGN","eli5":"Pages are built once ahead of time and served from a CDN — super fast but can go stale.","memory":"Build once, serve everywhere; ISR refreshes in the background.","full":"<p><strong>SSG</strong> pre-renders pages to HTML at build time so they're served from CDN edge caches with the best possible TTFB/LCP. Tradeoffs: <em>staleness</em> (content frozen until rebuild) and build time scaling with page count.</p><p><strong>ISR / incremental revalidation</strong> fixes staleness: cached pages carry a TTL or are invalidated on-demand (webhook), serving the stale copy instantly while regenerating in the background — <code>stale-while-revalidate</code> semantics at the page level.</p>","trap":"Believing static = always up to date. Without ISR/revalidation, an SSG page shows old data until the next full build.","worked":"A blog uses <code>generateStaticParams</code> + <code>revalidate: 60</code>; an edited post is served stale for up to 60s, then regenerated on the next request.","related":["ssr","csr-spa","core-web-vitals"]},
  "design-tokens": {"term":"Design Tokens","group":"FE-SYSDESIGN","eli5":"Named variables for colors, spacing, and type that one source feeds to every platform.","memory":"Primitive → semantic → component; theme by swapping values.","full":"<p><strong>Design tokens</strong> store design decisions as platform-agnostic data (JSON / the W3C DTCG format) — <code>color.brand.primary</code>, <code>space.4</code> — compiled (e.g. Style Dictionary) to CSS variables, iOS, Android, etc. Tiering them into <em>primitive</em> (raw palette), <em>semantic</em> (intent like <code>color.action.bg</code>), and <em>component</em> levels lets components reference intent, so retheming (dark mode, white-label) is a value swap with no component edits.</p><p>At runtime, exposing semantic tokens as <code>:root</code> custom properties makes theme switching a class/data-attribute toggle.</p>","trap":"Letting components reference primitive tokens (<code>blue-500</code>) directly — that defeats theming; always go through semantic aliases.","worked":"<code>--color-action-bg: var(--blue-500);</code> then <code>[data-theme=dark]{ --color-action-bg: var(--blue-400); }</code> — every button retheme without touching button code."},
  "list-virtualization": {"term":"List Virtualization (Windowing)","group":"FE-SYSDESIGN","eli5":"Only render the rows you can see, recycling them as you scroll, so a 10k-row list stays fast.","memory":"Big scrollbar, tiny DOM.","full":"<p><strong>Virtualization</strong> renders only the rows in/near the viewport while a full-height spacer preserves the scrollbar, recycling DOM nodes as the user scrolls (TanStack Virtual, react-window). This keeps memory, layout, and paint cost roughly constant regardless of dataset size.</p><p>Gotchas: variable row heights need measurement/estimation to avoid scroll jumps; off-screen content is missing from the DOM (breaking Ctrl+F and naive SEO); and accessibility requires managing focus on recycled rows plus <code>aria-rowcount</code>/<code>aria-setsize</code>. It pairs with cursor pagination to render virtually-infinite remote data.</p>","trap":"Assuming virtualization helps SEO/find-in-page — off-screen rows aren't in the DOM, so crawlers and Ctrl+F can't see them.","worked":"A 50k-row table renders ~30 visible rows; <code>useVirtualizer({count:50000, estimateSize:()=>40})</code> translates them to the correct <code>transform: translateY</code> offsets.","related":["cursor-pagination","core-web-vitals"]},
  "bff": {"term":"Backend-for-Frontend (BFF)","group":"FE-SYSDESIGN","eli5":"A small server owned by the frontend team that bundles backend calls into exactly what the UI needs.","memory":"One BFF per client experience; reshape, don't expose.","full":"<p>A <strong>BFF</strong> is a per-client edge service (web, iOS, Android each get one) that aggregates and reshapes downstream microservices into UI-tailored payloads, eliminating over/under-fetching and client request waterfalls in a single round trip. It also centralizes auth, token handling, caching, and downstream retries server-side.</p><p>Versus a <strong>GraphQL gateway</strong> (one schema for many clients, declarative field selection but caching/N+1/query-cost complexity), the BFF is per-client and imperative — preferred when web and mobile needs diverge sharply. The two can compose: a BFF may itself call a GraphQL gateway.</p>","trap":"Turning the BFF into a second monolith that owns business logic. It should aggregate/adapt, not become the system of record.","worked":"The mobile BFF merges <code>/user</code>, <code>/orders</code>, and <code>/recommendations</code> into one trimmed JSON response sized for a phone, while the web BFF returns a richer shape.","related":["rsc"]},
  "optimistic-ui": {"term":"Optimistic UI","group":"FE-SYSDESIGN","eli5":"Show the result of an action immediately, then fix it if the server disagrees.","memory":"Assume success, snapshot first, roll back on error.","full":"<p><strong>Optimistic UI</strong> applies the expected outcome to local state before the server confirms, making the app feel instant. The correct pattern snapshots previous state on mutate, replaces the optimistic value with the server's canonical response on success, and rolls back to the snapshot on error — e.g. TanStack Query's <code>onMutate/onError/onSettled</code> or React 19's <code>useOptimistic</code>.</p><p>Gotchas: handle out-of-order responses with client-generated temp IDs, avoid optimism for irreversible/financial actions, and de-duplicate when a real-time channel echoes your own mutation back.</p>","trap":"Forgetting to snapshot/roll back — a failed mutation then leaves the UI showing data the server never accepted.","worked":"Liking a post: increment the count instantly, store the old count; if the <code>POST</code> 500s, restore the old count and show a retry toast.","related":["cursor-pagination","core-web-vitals"]},
  "islands-architecture": {"term":"Islands / Partial Hydration","group":"FE-SYSDESIGN","eli5":"Ship a mostly-static page and only make the few interactive widgets run JS.","memory":"Sea of static HTML, small islands of interactivity.","full":"<p><strong>Islands architecture</strong> (Astro, Fresh) renders the page as static HTML and hydrates <em>only</em> the interactive components (the 'islands'), independently and often lazily (on idle/visible). This slashes shipped JS and improves INP versus full-page hydration, since most of the page never needs client JS.</p><p>Related is <em>resumability</em> (Qwik), which serializes listener/state info into the HTML so the client resumes without re-executing the tree — pushing startup hydration cost toward zero.</p>","trap":"Assuming islands can freely share React state across the page — each island is isolated; cross-island state needs an external store or signals.","worked":"An Astro blog post is pure HTML except a <code>&lt;Search client:visible /&gt;</code> island that only downloads its JS when scrolled into view.","related":["hydration","rsc","ssg","core-web-vitals"]}
});
