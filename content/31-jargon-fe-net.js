/* Frontend jargon & acronyms — clickable reference (FE-NET, auto-assembled) */
Object.assign(GLOSSARY_DATA, {
  "url": {"term":"URL","group":"FE-NET","aliases":["Uniform Resource Locator"],"eli5":"The web address that identifies and locates a resource, including how to reach it (scheme), where it lives (host/path), and extra parameters.","memory":"Uniform Resource Locator — it not only names a resource, it tells you where to LOCATE and fetch it.","full":"<p><strong>What it is.</strong> A <strong>URL (Uniform Resource Locator)</strong> is the standard string that both identifies a web resource and describes how to retrieve it. It bundles a scheme (<code>https</code>), host (<code>example.com</code>), optional port, path (<code>/articles/42</code>), query string (<code>?sort=new</code>), and fragment (<code>#section</code>). A URL is the most common kind of <strong>URI</strong> — the one that includes a <em>location</em>, not just an identifier.</p><p><strong>History / origin.</strong> The URL was specified by <strong>Tim Berners-Lee</strong> and collaborators as part of inventing the World Wide Web around 1994, formalized in early IETF RFCs (notably RFC 1738). The syntax has been refined over the years (RFC 3986 unified URI syntax), and in the browser era the <strong>WHATWG URL Standard</strong> defined exactly how browsers parse and serialize URLs and exposed the <code>URL</code> JavaScript object.</p><p><strong>Purpose &amp; why still used.</strong> URLs are the addressing backbone of the entire web — every link, asset, API endpoint, and route is a URL. In frontend work they're central to client-side routing: <code>history.pushState(state, '', '/path')</code> changes the visible URL and adds a history entry <em>without</em> a full page reload, which is the core trick behind single-page-app navigation. They also matter for accessibility (a missing image <code>alt</code> may cause screen readers to announce the file's URL) and SEO. URLs are not going anywhere — they are the web's universal locator.</p>","worked":"<p>Anatomy of a URL:</p><p><code>https://example.com:443/articles/42?sort=new#comments</code></p><ul><li><code>https</code> — scheme</li><li><code>example.com</code> — host, <code>:443</code> — port</li><li><code>/articles/42</code> — path</li><li><code>?sort=new</code> — query string</li><li><code>#comments</code> — fragment</li></ul><p>SPA routing changes the URL without reloading:</p><p><code>history.pushState({ id: 42 }, '', '/articles/42');</code></p><p>Parsing in JS:</p><p><code>const u = new URL('https://example.com/a?x=1');</code><br><code>u.searchParams.get('x'); // \"1\"</code></p>","teach":"<p>At its core, a URL is just an address that does double duty: it <em>names</em> a resource and tells the browser <em>how and where</em> to go get it. Every link you've ever clicked is one.</p><p>Break it into parts and it stops being mysterious. The <strong>scheme</strong> (<code>https</code>) is the method of access. The <strong>host</strong> (<code>example.com</code>) is which server. The optional <strong>port</strong> is which door on that server. The <strong>path</strong> (<code>/articles/42</code>) is which resource. The <strong>query</strong> (<code>?sort=new</code>) carries parameters. The <strong>fragment</strong> (<code>#comments</code>) points at a spot <em>within</em> the page and is never sent to the server.</p><div class=\"analogy\">A URL is like a full postal address combined with delivery instructions. The scheme is \"by air mail vs ground\"; the host is the city and building; the path is the apartment number; the query is a note on the envelope (\"leave with concierge\"); and the fragment is \"once inside, go to the third shelf\" — an instruction the postal service ignores but you act on after arrival.</div><p>For frontend developers the URL is also the secret to single-page apps. Normally, changing the address bar means a full page reload. But <code>history.pushState(state, '', '/path')</code> lets JavaScript update the visible URL and add a back-button entry <em>without</em> reloading — so your router can swap views instantly while the address still reflects where the user is. That's why SPAs feel fast yet still have shareable, bookmarkable links and a working Back button.</p><p>One subtle accessibility note: when an image lacks an <code>alt</code> attribute, some screen readers fall back to announcing the image's file name or URL — which is why \"no alt\" is worse than an empty <code>alt=\"\"</code> for decorative images. The URL leaks where descriptive text should be.</p><div class=\"key\">A URL is the web's universal address — scheme, host, port, path, query, fragment — that both identifies a resource and says how to fetch it. It's a kind of URI that includes location, and via <code>pushState</code> it's the backbone of client-side routing.</div>","related":["uri","cors","sop"]},
  "uri": {"term":"URI","group":"FE-NET","aliases":["Uniform Resource Identifier"],"eli5":"A string that uniquely identifies a resource — a broad category that includes URLs (which also say where to find it) and URNs (which only name it).","memory":"Uniform Resource Identifier — the umbrella; a URL is a URI that also tells you the Location.","full":"<p><strong>What it is.</strong> A <strong>URI (Uniform Resource Identifier)</strong> is the general, umbrella term for a compact string that identifies a resource. A <strong>URL</strong> is a URI that additionally specifies <em>how to locate</em> the resource (its access method and network location). A <strong>URN</strong> is a URI that names a resource persistently <em>without</em> implying how to find it (e.g. <code>urn:isbn:0-486-27557-4</code>). In short: every URL is a URI, but not every URI is a URL.</p><p><strong>History / origin.</strong> The URI concept was formalized through the IETF as the web matured, with the unifying syntax specified in <strong>RFC 3986</strong> (2005, by Berners-Lee, Fielding, and Masinter), which reconciled the earlier separate notions of URLs and URNs under one generic grammar: <code>scheme:[//authority]path[?query][#fragment]</code>.</p><p><strong>Purpose &amp; why still used.</strong> URI is the precise, spec-level term you'll see in standards, HTTP definitions, and APIs (e.g. XML namespaces and RDF identifiers are URIs that may not be dereferenceable URLs). In everyday frontend conversation people usually just say \"URL,\" but \"URI\" remains the correct word whenever the identifier might not be a fetchable location. It is still very much current — it's the foundational addressing abstraction the rest of the web's identifiers are defined against.</p>","worked":"<p>Distinguishing the family:</p><ul><li><strong>URI (URL):</strong> <code>https://example.com/page</code> — identifies <em>and</em> locates.</li><li><strong>URI (URN):</strong> <code>urn:isbn:0-486-27557-4</code> — identifies a book, but doesn't tell you where to fetch it.</li><li><strong>URI used as a name only:</strong> an XML namespace like <code>http://www.w3.org/1999/xhtml</code> — looks like a URL but is used purely as a unique identifier, not necessarily fetched.</li></ul><p>So: every URL is a URI; a URN is a URI that is not a URL.</p>","teach":"<p>At its core, a URI is just a standardized way to write down \"this particular resource\" as a string. It's the umbrella; URL and URN are two things that live under it.</p><p>The confusion is almost entirely about scope. People say \"URL\" for everything, but the spec draws a careful distinction. A <strong>URI</strong> identifies a resource. If the identifier <em>also</em> tells you how to reach it — the scheme and network location — then it's specifically a <strong>URL</strong> (the L is for Locator). If it only names the thing without saying where it lives, it can be a <strong>URN</strong> (the N is for Name).</p><div class=\"analogy\">Think of identifying a person. \"The individual with passport number X12345\" is a pure <em>identifier</em> — it uniquely picks out one person but doesn't tell you how to reach them; that's URN-like. \"The person at 12 Oak Street, apartment 4\" both identifies <em>and</em> locates them; that's URL-like. Both are ways of referring to the same kind of thing — a \"way to refer to a person\" — which is the URI umbrella.</div><p>Why keep the distinction if everyone just says URL? Because some identifiers genuinely aren't fetchable. XML namespaces and RDF use strings that <em>look</em> like web addresses but are really just globally-unique names — nobody is expected to download them. Calling those \"URIs\" is precise; calling them \"URLs\" implies a location that may not exist.</p><p>In practice as a frontend developer you'll type \"URL\" 99% of the time and be right, because almost every address you handle is a fetchable location. But when you read an HTTP spec, a standards document, or an API that talks about \"resource identifiers,\" the broader word URI is being used deliberately — to include the cases where the string names something without locating it.</p><div class=\"key\">URI is the umbrella term for any resource identifier. A URL is a URI that also tells you where and how to fetch the resource; a URN is a URI that only names it. Every URL is a URI, but not every URI is a URL.</div>","related":["url"]},
  "xhr": {"term":"XHR","group":"FE-NET","aliases":["XMLHttpRequest","XML HTTP Request"],"eli5":"The original browser API that lets JavaScript make HTTP requests in the background and update the page without a full reload.","memory":"XMLHttpRequest — the AJAX-era object; despite the XML name it fetches anything, and fetch() is its modern successor.","full":"<p><strong>What it is.</strong> <strong>XHR (XMLHttpRequest)</strong> is a browser JavaScript API for making HTTP requests programmatically — sending and receiving data from a server without reloading the page. Despite the \"XML\" in the name, it can handle any payload (JSON, text, blobs). It's the technology that originally made <strong>AJAX</strong> (asynchronous page updates) possible.</p><p><strong>History / origin.</strong> XMLHttpRequest originated at <strong>Microsoft</strong>, which shipped it as an ActiveX control in Internet Explorer around 1999/2000 to support Outlook Web Access. Other browsers adopted a native version, and its breakout moment came in the mid-2000s when Google Maps and Gmail showcased AJAX, after which the W3C/WHATWG standardized it. Its modern successor, the promise-based <strong>Fetch API</strong>, arrived in the mid-2010s and is now the default choice.</p><p><strong>Purpose &amp; why still used.</strong> XHR's purpose was background client-server communication for dynamic pages. It's largely superseded by <code>fetch()</code> (cleaner promises, streams, better defaults), but XHR is <em>not</em> dead: it's still the only way to get certain features like fine-grained upload/download <strong>progress events</strong> and synchronous requests, and tons of existing code and libraries use it. It also matters in security/testing contexts: <code>SameSite=Lax</code> cookies are <em>not</em> sent on cross-site subresource/XHR requests (only top-level GET navigations), and tools like <strong>MSW (Mock Service Worker)</strong> intercept real <code>fetch</code>/XHR calls at the network layer so test code runs unmodified.</p>","worked":"<p>Classic XHR request:</p><p><code>const xhr = new XMLHttpRequest();</code><br><code>xhr.open('GET', '/api/data');</code><br><code>xhr.responseType = 'json';</code><br><code>xhr.onload = () =&gt; console.log(xhr.response);</code><br><code>xhr.upload.onprogress = (e) =&gt; console.log(e.loaded, '/', e.total); // progress — fetch can't do this easily</code><br><code>xhr.send();</code></p><p>Modern equivalent with fetch:</p><p><code>const res = await fetch('/api/data');</code><br><code>const data = await res.json();</code></p><p>MSW can intercept either form transparently in tests.</p>","teach":"<p>At its core, XHR is just an object that lets your JavaScript talk to a server in the background and do something with the answer — no page reload required. That single capability is what turned static web pages into dynamic web apps.</p><p>Before XHR, every interaction that needed fresh data meant a full page navigation: click, blank screen, whole page reloads. XHR broke that. Now a script could quietly ask the server \"give me the search results for 'cat'\" and splice them into the existing page. The pattern got the name <strong>AJAX</strong> (Asynchronous JavaScript and XML), and Gmail and Google Maps made it famous — maps you could drag without the page flickering felt like magic at the time.</p><div class=\"analogy\">Think of an old-fashioned restaurant where any change to your order meant getting up and re-seating at a freshly set table (a full reload). XHR is like getting a waiter: you stay seated, hand them a note, they go to the kitchen, and they bring back just the one dish you asked for. The rest of your table — your drink, your conversation — is undisturbed. The page persists; only the requested piece updates.</div><p>The naming is a historical quirk: the \"XML\" in XMLHttpRequest reflects the data format fashionable when Microsoft built it around 2000, but it was always able to carry JSON, plain text, or binary — and JSON quickly won.</p><p>Today you'll mostly reach for <code>fetch()</code> instead: it returns promises, plays nicely with <code>async/await</code>, and supports streaming. So why learn XHR at all? Two reasons. First, capability gaps — XHR still exposes upload/download <strong>progress events</strong> that fetch handles awkwardly, so file-upload bars often still use it. Second, it shows up in security and testing discussions: cross-site XHR subresource requests don't carry <code>SameSite=Lax</code> cookies (a CSRF defense), and mocking tools like MSW intercept both fetch and XHR at the network layer so your real request code runs untouched in tests.</p><div class=\"key\">XHR is the original background-HTTP API that birthed AJAX and dynamic pages. Fetch has mostly replaced it, but XHR survives for progress events and lives on in security (SameSite) and testing (MSW) discussions about real network requests.</div>","related":["cors","sop","url","jsonp"]},
  "mime": {"term":"MIME","group":"FE-NET","aliases":["Multipurpose Internet Mail Extensions","MIME type","media type","content type"],"eli5":"A standardized label (like text/html or application/json) that tells software what kind of data a file or response contains and how to interpret it.","memory":"Multipurpose Internet Mail Extensions — born for email attachments, now the web's 'what kind of thing is this?' label (the Content-Type).","full":"<p><strong>What it is.</strong> A <strong>MIME type</strong> (formally a <em>media type</em>) is a standardized two-part label — <code>type/subtype</code>, e.g. <code>text/html</code>, <code>application/json</code>, <code>image/png</code> — that identifies the format of a piece of data. On the web it's carried in the HTTP <code>Content-Type</code> header and tells the browser how to handle a response: render it, parse it, download it, or execute it.</p><p><strong>History / origin.</strong> MIME (<strong>Multipurpose Internet Mail Extensions</strong>) was created for <em>email</em> in the early 1990s, standardized through the IETF (RFCs 2045–2049), to let messages carry non-ASCII text and attachments like images and audio. The web adopted the same media-type system for HTTP, and the registry of types is maintained by <strong>IANA</strong>. The naming has since shifted toward \"media type,\" though \"MIME type\" remains in common use.</p><p><strong>Purpose &amp; why still used.</strong> MIME types are how the web negotiates and interprets content — every response and many requests declare one. They're also security-relevant: browsers historically <em>sniffed</em> content (guessing the type from the bytes), which attackers abused to make a server's JSON or text response be treated as executable HTML/JS (a classic XSS vector). The header <code>X-Content-Type-Options: nosniff</code> disables that guessing, forcing the browser to trust the declared MIME type. MIME types are fundamental and ubiquitous today — there is no replacement.</p>","worked":"<p>A server declares the type of its response:</p><p><code>Content-Type: application/json; charset=utf-8</code></p><p>Common MIME types: <code>text/html</code>, <code>text/css</code>, <code>application/javascript</code>, <code>image/png</code>, <code>application/json</code>.</p><p>Hardening against MIME sniffing:</p><p><code>X-Content-Type-Options: nosniff</code></p><p>With <code>nosniff</code>, if an API mistakenly serves user-controlled HTML as <code>text/plain</code>, the browser won't \"helpfully\" reinterpret it as <code>text/html</code> and execute embedded scripts — closing a content-type-confusion XSS hole.</p>","teach":"<p>At its core, a MIME type is just a label that answers one question: <em>what kind of data is this?</em> <code>text/html</code> means \"a web page,\" <code>application/json</code> means \"JSON data,\" <code>image/png</code> means \"a PNG image.\" Software reads the label to decide what to do with the bytes.</p><p>This matters because, to a computer, a stream of bytes is ambiguous. The same bytes could be displayed as text, rendered as a page, executed as a script, or saved as a download — and only the label disambiguates. On the web that label travels in the <code>Content-Type</code> header alongside the response.</p><div class=\"analogy\">Think of MIME types as the labels on shipping containers at a port. The container's contents are opaque from outside, but the stenciled label — \"REFRIGERATED FOOD\" vs \"HAZARDOUS CHEMICALS\" vs \"DRY GOODS\" — tells the crane operator exactly how to handle it: which truck, which warehouse, what precautions. Mislabel a hazardous container as dry goods and someone handles it dangerously. That mislabeling is exactly the security problem MIME sniffing creates.</div><p>Here's the twist. Browsers used to <em>second-guess</em> the label by peeking at the bytes — \"this says <code>text/plain</code> but it looks like HTML, I'll render it as HTML.\" Convenient, but dangerous: if an attacker can get user-supplied content echoed back, and the browser reinterprets it as HTML, any <code>&lt;script&gt;</code> inside runs. That's a cross-site scripting (XSS) attack born purely from content-type confusion.</p><p>The fix is the header <code>X-Content-Type-Options: nosniff</code>. It tells the browser: <em>trust my label, don't guess.</em> A response declared <code>application/json</code> will never be executed as a script even if its body happens to look like HTML. It's a small header that closes a whole category of attacks, which is why it's a standard part of any security baseline.</p><div class=\"key\">A MIME type (media type) is the <code>type/subtype</code> label that tells software how to interpret data, carried in <code>Content-Type</code>. Born in 1990s email, now universal on the web — and <code>nosniff</code> stops browsers from dangerously guessing past the declared type.</div>","related":["sop","url","cors"]},
  "jsonp": {"term":"JSONP","group":"FE-NET","aliases":["JSON with Padding","JSON-P"],"eli5":"An old hack that fetched cross-origin data by injecting a &lt;script> tag whose response was a JavaScript function call wrapping the JSON.","memory":"JSON with Padding — pad the JSON inside a function call and sneak it in via a &lt;script> tag, dodging the Same-Origin Policy.","full":"<p><strong>What it is.</strong> <strong>JSONP (JSON with Padding)</strong> is a legacy technique for fetching data from a different origin <em>before</em> CORS existed. Because the Same-Origin Policy blocks cross-origin <code>XMLHttpRequest</code> reads, JSONP exploited a loophole: <code>&lt;script&gt;</code> tags are allowed to load cross-origin. The page injects a script whose URL points at the remote server, and the server responds not with raw JSON but with JSON \"padded\" inside a call to a callback function the page already defined — so loading the script <em>executes</em> that function with the data.</p><p><strong>History / origin.</strong> JSONP emerged in the mid-2000s during the AJAX boom as a community workaround for cross-origin data access. It was never a formal standard — more of a widely-copied pattern — and it was the dominant cross-domain method until CORS shipped in browsers around the turn of the 2010s and superseded it.</p><p><strong>Purpose &amp; why it's (mostly) gone.</strong> Its purpose was purely to defeat the same-origin read restriction for APIs. It is now strongly discouraged and largely obsolete because it is <em>insecure by design</em>: loading remote code via <code>&lt;script&gt;</code> means the remote server can run arbitrary JavaScript in your page, it only supports GET, and it has no proper error handling. CORS replaced it cleanly and safely. JSONP also intersects with modern security policy: a Content Security Policy directive like <code>'strict-dynamic'</code> changes how trusted scripts can load further scripts (and causes browsers to ignore host-source allowlists in supporting browsers), which is exactly the kind of dynamic script-loading surface JSONP relied on and CSP aims to control.</p>","worked":"<p>The page predefines a callback, then injects a script:</p><p><code>function handleData(data) { console.log(data.name); }</code><br><code>const s = document.createElement('script');</code><br><code>s.src = 'https://api.other.com/user?callback=handleData';</code><br><code>document.body.appendChild(s);</code></p><p>The server responds with JS that <em>calls</em> the callback (the \"padding\"):</p><p><code>handleData({ \"name\": \"Ada\" });</code></p><p>Loading the script executes that call. Note the danger: the server could instead return <code>fetch('/steal-cookies')</code> and it would run in your page. The modern, safe equivalent is simply a CORS-enabled <code>fetch('https://api.other.com/user')</code>.</p>","teach":"<p>At its core, JSONP is just a clever abuse of the <code>&lt;script&gt;</code> tag to get data from another website back when the browser flatly refused to let you read cross-origin responses any other way.</p><p>Here's the bind it solved. The Same-Origin Policy stops your JavaScript from <em>reading</em> a response from a different origin via <code>XMLHttpRequest</code>. But the policy has an exception that's older than the policy itself: pages have always been allowed to load <em>scripts</em> from anywhere — that's how you embed third-party libraries and ad code. JSONP slips through that exception.</p><div class=\"analogy\">Imagine a building where you're not allowed to receive mail from outside companies (cross-origin reads are blocked) — but you <em>are</em> allowed to hire outside contractors who walk right in and start working (loading external scripts). JSONP is hiring a \"contractor\" (a remote script) whose entire job is to hand you the data you wanted by physically placing it on your desk (calling your callback function). You got your data — but you also let a stranger walk into your office and do whatever they like.</div><p>Mechanically: you define a function, say <code>handleData</code>, then add a <code>&lt;script src=\"...?callback=handleData\"&gt;</code>. The remote server, instead of returning bare JSON, returns <code>handleData({...})</code> — the JSON \"padded\" inside a call to your function. When the browser runs the downloaded script, it calls <code>handleData</code> with the data. That's the whole trick, and where the \"P\" (padding) comes from.</p><p>The fatal flaw is in that analogy: you are <em>executing</em> whatever the remote server sends. If that server is malicious or compromised, it runs arbitrary code in your page with full access to your users' sessions. JSONP also can't do anything but GET and has no real error handling.</p><p>That's why CORS replaced it: CORS lets you make a normal, safe <code>fetch</code> where the data stays <em>data</em> and the server merely opts in via headers — no remote code execution required. Today JSONP is a historical curiosity, and modern defenses like CSP's <code>'strict-dynamic'</code> exist precisely to rein in the dynamic-script-loading behavior JSONP depended on.</p><div class=\"key\">JSONP smuggled cross-origin data through the one always-allowed cross-origin channel — the <code>&lt;script&gt;</code> tag — by wrapping JSON in a callback call. It worked before CORS but is insecure (it executes remote code) and is now obsolete, replaced by CORS-enabled fetch.</div>","related":["cors","sop","xhr","mime"]},
  "sni": {"term":"SNI","group":"FE-NET","aliases":["Server Name Indication"],"eli5":"SNI is a field in the TLS handshake that tells the server which website hostname you're trying to reach, so a server hosting many sites can present the right certificate.","memory":"Server Name Indication — you 'indicate' the server name up front so the right cert comes back.","full":"<p><strong>SNI (Server Name Indication)</strong> is an extension to the TLS protocol that lets a client announce the hostname it wants to connect to during the very first message of the handshake (the <code>ClientHello</code>). Because a single IP address often hosts many HTTPS sites, the server needs to know <em>which</em> site you want before it can pick the correct certificate. Without SNI, the server would have to guess, and serving one shared certificate per IP doesn't scale to virtual hosting.</p><p>SNI was standardized by the IETF as part of the TLS Extensions work (RFC 3546 in 2003, later folded into RFC 4366 and subsequent TLS extension RFCs). It arrived to solve the practical explosion of name-based virtual hosting on shared infrastructure and CDNs, where IPv4 address scarcity made one-IP-per-site impractical. It is now effectively universal — virtually every modern browser and TLS library sends it.</p><p>Its core trade-off is privacy: historically the SNI hostname travels in <em>plaintext</em> in the ClientHello, so anyone on the network path can see which site you're visiting even though the rest of the connection is encrypted. The 2026-era answer is <strong>ECH (Encrypted Client Hello)</strong>, which wraps the sensitive parts of the ClientHello (including SNI) in an additional layer of encryption keyed to the server, hiding the requested hostname from on-path observers. SNI remains the mechanism that selects the certificate; ECH is the privacy upgrade layered on top.</p>","worked":"<p>Conceptually, the browser's ClientHello carries the hostname so the right cert is served:</p><ul><li><code>ClientHello { ... server_name: \"shop.example.com\" ... }</code> — plaintext SNI</li><li>Server sees <code>shop.example.com</code>, returns the cert for that exact host</li></ul><p>With ECH in 2026, that same <code>server_name</code> field is encrypted, so an eavesdropper only sees a generic public name and cannot tell which site behind the CDN you requested.</p>","teach":"<p>At its core, SNI is just a sticky note you hand the server saying \"I'm here for <em>this</em> website,\" attached to the very first packet of a secure connection.</p><p>When your browser opens an HTTPS connection it does a TLS handshake. The problem: one server (or one CDN edge node) might host thousands of different sites on a single IP address. A certificate proves \"I really am shop.example.com,\" but the server has many certificates and must choose one <em>before</em> it can respond. The chicken-and-egg issue is that encryption hasn't started yet — so how does the server know which cert to send?</p><div class=\"analogy\">Imagine a giant office building with one front door but hundreds of companies inside. You walk up and the receptionist asks, \"Who are you here to see?\" You have to say the company name <em>out loud in the lobby</em> before they hand you that company's badge. SNI is you announcing the company name; the badge is the certificate. The catch: anyone standing in the lobby overhears which company you named.</div><p>That overhearing is the privacy weakness. The hostname in SNI is sent in the clear, so network observers — your ISP, a coffee-shop Wi-Fi sniffer, a censor — can log which sites you visit even though they can't read the page contents.</p><p>The 2026 fix is <strong>ECH (Encrypted Client Hello)</strong>. Instead of saying the company name out loud, you hand the receptionist a <em>sealed envelope</em> that only the building's master key can open. Observers see you walk in but can't tell which company you asked for. SNI still does its job (picking the certificate); ECH just hides the question.</p><div class=\"key\">SNI = \"which site?\" announced in the TLS ClientHello so the server serves the right certificate. It's been plaintext (and thus a privacy leak) since 2003; ECH encrypts it. Selecting the cert is SNI's job; hiding the hostname is ECH's.</div>","related":["coop","coep","sri"]},
  "rfc": {"term":"RFC","group":"FE-NET","aliases":["Request for Comments"],"eli5":"An RFC is a numbered technical document in a long-running series, published by the IETF and related bodies, that defines or describes internet standards and protocols.","memory":"Request for Comments — the numbered docs (RFC 5861, RFC 6749…) that define how the internet works.","full":"<p><strong>RFC (Request for Comments)</strong> is the name of a numbered document series that specifies internet protocols, procedures, and standards. Despite the modest historical name (originally an invitation for feedback), many RFCs are the authoritative, formal definitions of how the internet's technologies work — HTTP, TCP, TLS, OAuth, and countless others are defined in RFCs.</p><p>The RFC series began in 1969, with RFC 1 written by Steve Crocker during the early ARPANET work; the name reflected the collaborative, non-authoritarian spirit of the early network designers. Today the series is overseen by the RFC Editor and produced largely through the <strong>IETF (Internet Engineering Task Force)</strong> and related streams. RFCs are immutable once published — an RFC is never edited in place; instead a new RFC is issued to update or obsolete an earlier one.</p><p>RFCs remain the bedrock of web and network engineering in 2026 because they are the precise, citable source of truth for protocol behavior. Frontend caching strategies cite them directly: <em>stale-while-revalidate</em>, a pattern where a cached response is served immediately even if stale while a fresh copy is fetched in the background, originates in the HTTP <code>Cache-Control</code> extension defined in <strong>RFC 5861</strong>. Likewise OAuth 2.0 is RFC 6749. When developers need the exact, unambiguous semantics of a header or protocol, they read the RFC.</p>","worked":"<p>The <code>stale-while-revalidate</code> directive (RFC 5861) on a cache response header:</p><ul><li><code>Cache-Control: max-age=600, stale-while-revalidate=86400</code></li></ul><p>This means: treat the response as fresh for 600 seconds; after that, for up to 86400 seconds, serve the stale copy instantly while revalidating in the background. The same idea appears in frontend data libraries (e.g., SWR), named after this RFC-defined HTTP behavior.</p>","teach":"<p>At its core, an RFC is just a numbered, permanent spec document — the official rulebook entry for a piece of internet technology.</p><p>The internet is a stack of agreements: how computers address each other, how browsers and servers talk, how connections get encrypted. Those agreements have to be written down precisely, or different implementations won't interoperate. RFCs are where they're written down, each one stamped with a unique number you can cite forever.</p><div class=\"analogy\">Think of RFCs like numbered laws in a legal code. \"RFC 6749\" is as specific as citing a statute by section number — anyone can look up exactly the same text. And like laws, RFCs are never quietly edited; if the rule changes, a <em>new</em> RFC is passed that amends or repeals the old one, but the old number still exists in the record.</div><p>The name is a charming historical artifact. In 1969, the engineers building the ARPANET wrote up their ideas as \"Requests for Comments\" — deliberately humble, an invitation to discuss rather than a decree — and the name stuck even as the series became the formal home of internet standards. The IETF and the RFC Editor steward the series today.</p><p>For frontend work, RFCs come up whenever you touch the network layer. Caching is a great example: the elegant <em>stale-while-revalidate</em> strategy — show the user the cached data instantly, then quietly fetch a fresh copy for next time — isn't a framework invention. It's an HTTP <code>Cache-Control</code> behavior formally defined in RFC 5861. Popular data-fetching libraries even borrowed the name. When you want the exact truth about a header, status code, or protocol step, the RFC is the primary source.</p><div class=\"key\">RFC = a numbered, immutable spec document (series started 1969, RFC 1 by Steve Crocker) produced via the IETF. RFCs formally define internet protocols (HTTP, OAuth/RFC 6749, stale-while-revalidate/RFC 5861). They're the citable source of truth for network behavior.</div>","related":["sso","w3c","whatwg","idn"]},
  "llm": {"term":"LLM","group":"FE-NET","aliases":["Large Language Model"],"eli5":"An AI model trained on huge amounts of text that generates language one token at a time — often streamed to the browser as it's produced.","memory":"LLM = Large Language Model: 'large' (billions of parameters) + 'language model' (predicts the next token).","full":"<p>A <strong>LLM (Large Language Model)</strong> is a machine-learning model — typically a transformer neural network with billions of parameters — trained on vast text corpora to predict and generate natural language. It produces output incrementally as a sequence of <em>tokens</em> (word fragments), which is why LLM responses are commonly <em>streamed</em>: the server emits tokens as they're generated and the UI appends them progressively rather than waiting for the whole answer.</p><p>The modern LLM era was catalyzed by the transformer architecture introduced in the 2017 'Attention Is All You Need' paper, followed by successively larger models through the late 2010s and 2020s that brought conversational AI into mainstream products. From a front-end perspective, the defining trait is the streaming interaction pattern — partial results arriving over a long-lived connection.</p><p>For front-end engineers, LLMs are less about the model internals and more about <em>how their output reaches the page</em>. Token streaming is a canonical use case for server-push transports: technologies like Server-Sent Events (SSE) or WebSockets deliver tokens as they arrive, alongside other streaming UIs such as notifications and live scores. The reason this pattern is 'still used' and growing is responsiveness — streaming lets the user start reading immediately, dramatically improving perceived latency for the inherently sequential, sometimes slow generation of an LLM.</p>","worked":"<p>Consuming a streamed LLM response token-by-token in the browser with the Fetch streaming API:</p><ul><li><code>const res = await fetch('/api/chat', { method: 'POST', body });</code></li><li><code>const reader = res.body.getReader();</code></li><li><code>const dec = new TextDecoder();</code></li><li><code>while (true) {</code></li><li><code>&nbsp;&nbsp;const { value, done } = await reader.read();</code></li><li><code>&nbsp;&nbsp;if (done) break;</code></li><li><code>&nbsp;&nbsp;output.textContent += dec.decode(value, { stream: true });</code></li><li><code>}</code></li></ul><p>Each chunk that arrives is appended immediately, so the answer visibly 'types itself out' instead of appearing all at once after a long wait.</p>","teach":"<p>At its core, an LLM is just <strong>a very sophisticated next-word guesser.</strong></p><p>Given some text, it predicts the most plausible next chunk (a token), appends it, then predicts the next one based on everything so far, and repeats. That's the whole loop. 'Large' refers to the billions of internal parameters it learned from reading enormous amounts of text, which is what makes the guesses good enough to feel like coherent answers. The key behavioral fact for a UI builder is that this happens <em>one token at a time, sequentially</em> — the model literally cannot know the end of its sentence before it writes the middle.</p><div class=\"analogy\">Picture a storyteller improvising a tale out loud. They don't hand you a finished book — they speak it word by word, and you hear each word the instant it's said. If you made them write the entire story silently first and only then read it to you, you'd sit in awkward silence for a long time. Streaming is just letting you hear the storyteller in real time. The LLM is the improviser; SSE or WebSockets are the open microphone carrying each word to your ear as it's spoken.</div><p>This is why LLM front ends almost always <em>stream</em>. The model takes seconds to finish a full answer, but it produces readable text continuously the whole time. So instead of a spinner that finally dumps a wall of text, you open a streaming connection — Server-Sent Events or a WebSocket, or the Fetch streaming reader shown above — and append each token as it lands. The same pattern powers notifications and live sports scores: a long-lived connection where the server pushes small updates as they happen.</p><p>The payoff is perceived performance. Even if total generation time is unchanged, the user starts reading within a fraction of a second, which feels dramatically faster and keeps them engaged through a slow response.</p><div class=\"key\">An LLM generates language token-by-token, so its output is naturally streamed to the browser over a push transport (SSE/WebSocket). Streaming doesn't speed up generation — it slashes <em>perceived</em> latency by letting users read as the answer is produced.</div>","related":["api","html"]}
});
