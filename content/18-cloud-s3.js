/* S3 & Object Storage (CLOUD-S3) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"CLOUD-S3","front":"S3 is object storage, not a filesystem — what does that actually mean?","back":"S3 stores immutable <em>objects</em> (blob + metadata) addressed by a flat <code>bucket</code> + <code>key</code> string. There are no real directories, no in-place mutation (you overwrite the whole object), no atomic rename, and no append. The <code>/</code> in a key is just a character — the console fakes folders by grouping on prefixes via the <code>delimiter</code> parameter."},
  {"sec":"CLOUD-S3","front":"Is a 'folder' in S3 real? What is a prefix?","back":"No. The namespace is <strong>flat</strong> — a prefix is just the leading substring of a key (e.g. <code>year=2026/month=06/</code>). <code>ListObjectsV2</code> with <code>delimiter=/</code> returns <code>CommonPrefixes</code> to simulate a directory tree. This matters for performance: S3 throughput scales <em>per prefix</em>, so your key layout is a performance decision, not cosmetics."},
  {"sec":"CLOUD-S3","front":"What is S3's consistency model today vs historically?","back":"Since December 2020, S3 provides <strong>strong read-after-write consistency</strong> for all GET, PUT, LIST, and DELETE (plus tag/ACL/metadata) operations in <em>all</em> regions, automatically and free. Before that, brand-new-object PUTs were read-after-write but <em>overwrite PUTs and DELETEs were eventually consistent</em>, which forced patterns like EMRFS consistent view and S3Guard (both now obsolete)."},
  {"sec":"CLOUD-S3","front":"Strong consistency removed the need for which old workarounds?","back":"<ul><li><code>EMRFS consistent view</code> (DynamoDB-backed metadata for EMR)</li><li><code>S3Guard</code> (the Hadoop/S3A equivalent)</li><li>Random hash prefixes for staleness avoidance (you may still shard prefixes purely for <em>throughput</em>)</li></ul>Note: strong consistency does <strong>not</strong> mean LIST reflects in-flight multipart uploads, nor does it give you multi-object transactions — that's what Iceberg/Delta provide on top."},
  {"sec":"CLOUD-S3","front":"What does '11 nines of durability' mean, and is availability the same?","back":"Durability is <code>99.999999999%</code> — the probability S3 does <em>not</em> lose an object in a year, achieved by replicating across ≥3 AZs. <strong>Availability is much lower</strong>: S3 Standard targets <code>99.99%</code>, Standard-IA <code>99.9%</code>, One-Zone-IA <code>99.5%</code>. Durability = won't lose it; availability = can reach it right now. One-Zone-IA keeps 11 nines of <em>device</em> durability but loses data if its single AZ is destroyed."},
  {"sec":"CLOUD-S3","front":"Name the S3 storage classes and their core tradeoff.","back":"<ul><li><strong>Standard</strong> — hot, no retrieval fee, multi-AZ</li><li><strong>Standard-IA</strong> — cheaper storage, per-GB retrieval fee, 30-day min, multi-AZ</li><li><strong>One-Zone-IA</strong> — ~20% cheaper than IA, single AZ (re-creatable data only)</li><li><strong>Glacier Instant Retrieval</strong> — archive price, ms access, 90-day min</li><li><strong>Glacier Flexible Retrieval</strong> — mins–12h restore, 90-day min</li><li><strong>Glacier Deep Archive</strong> — cheapest, 12–48h restore, 180-day min</li></ul>"},
  {"sec":"CLOUD-S3","front":"What are the hidden cost traps of Standard-IA / One-Zone-IA / Glacier Instant Retrieval?","back":"Three traps: (1) a <strong>per-GB retrieval fee</strong> — bad for frequently read data; (2) a <strong>minimum storage duration</strong> (30 days IA, 90 days GIR/Glacier Flexible, 180 days Deep Archive) charged even if you delete early; (3) a <strong>128 KB minimum billable object size</strong> — applies to Standard-IA, One-Zone-IA, and Glacier Instant Retrieval (NOT Flexible/Deep Archive), so millions of tiny files bill as if each were 128 KB. These classes only save money for genuinely cold, large objects."},
  {"sec":"CLOUD-S3","front":"How does S3 Intelligent-Tiering work and when is it the right call?","back":"It auto-moves objects between access tiers based on observed access patterns for a small <strong>per-object monitoring fee</strong>, with <em>no retrieval charges</em>. Use it when access patterns are unknown or unpredictable. Note: objects <strong>under 128 KB are never auto-tiered</strong> — they stay at the frequent-access rate and are <em>not</em> charged the monitoring fee. Avoid it for data you already know is cold (manual Glacier/Deep Archive is cheaper since you skip the monitoring fee)."},
  {"sec":"CLOUD-S3","front":"How do S3 Lifecycle rules work, and what's the classic gotcha?","back":"Lifecycle policies (per-prefix/tag) <strong>transition</strong> objects to colder classes or <strong>expire</strong> them after N days from creation. Gotcha: each transition costs a <strong>per-object request fee</strong>, so transitioning millions of tiny objects can cost more than just leaving them in Standard. Always add rules to <strong>abort incomplete multipart uploads</strong> and expire old <strong>noncurrent versions</strong> — these silently accrue cost forever."},
  {"sec":"CLOUD-S3","front":"What is S3's per-prefix performance ceiling?","back":"Each prefix sustains at least <strong>3,500 PUT/COPY/POST/DELETE</strong> and <strong>5,500 GET/HEAD</strong> requests per second, and S3 auto-scales by partitioning on key ranges. To go higher, <strong>spread keys across multiple prefixes</strong> (parallelism multiplies the limit). A hot single prefix throttles with <code>503 SlowDown</code> until S3 splits the partition."},
  {"sec":"CLOUD-S3","front":"Date-partitioned keys can throttle ingest — why, and the fix?","back":"Keys like <code>logs/2026/06/21/...</code> funnel all of today's writes into <em>one</em> prefix, capping you at ~3,500 writes/s and risking <code>503 SlowDown</code>. Fix: add entropy <em>high</em> in the key (e.g. a hash or random shard prefix before the date) so writes fan out across partitions. Strong consistency means the old hash-prefix trick is now purely a throughput concern, not a staleness one."},
  {"sec":"CLOUD-S3","front":"When must you use multipart upload, and what are the limits?","back":"Multipart is <strong>required for objects > 5 GB</strong> (the single-PUT ceiling) and recommended above ~100 MB. Parts are 5 MB–5 GB (last part exempt), up to <strong>10,000 parts</strong>, max object <strong>5 TB</strong>. Parts upload in parallel and the object is invisible until <code>CompleteMultipartUpload</code>. Unfinished uploads keep billable parts in storage indefinitely — always set a lifecycle rule to abort them."},
  {"sec":"CLOUD-S3","front":"How does S3 versioning behave, and what's the cost/perf trap?","back":"Versioning is <strong>bucket-level</strong> and, once enabled, can only be <em>suspended</em>, never disabled. Each overwrite keeps the old version; a DELETE just adds a <strong>delete marker</strong> (the data still bills). Trap: listing over many delete markers and noncurrent versions gets slow and expensive — use lifecycle rules to expire noncurrent versions. Versioning is a prerequisite for cross-region/same-region replication."},
  {"sec":"CLOUD-S3","front":"What is a presigned URL and what are the two expiry gotchas?","back":"A time-limited URL granting <code>GET</code> or <code>PUT</code> on a single object, signed with the <em>creator's</em> credentials — the holder needs no AWS identity. Use it for direct browser upload/download so bytes bypass your app servers. Gotchas: (1) it inherits the <strong>signer's permissions</strong>, not the caller's; (2) the 7-day SigV4 max <strong>only applies with long-term IAM-user keys</strong> — if signed with <em>temporary</em> credentials (an assumed role, Lambda, EC2 instance profile), the URL dies when those credentials expire, often within hours."},
  {"sec":"CLOUD-S3","front":"Why is S3 the backbone of the lakehouse (Parquet/Iceberg)?","back":"S3 gives cheap, durable, infinitely scalable storage that decouples storage from compute — Athena, EMR, Spark, Trino all read the same Parquet without copies. Table formats (<strong>Iceberg</strong>, Delta, Hudi) add a manifest/metadata layer on top of immutable S3 objects to deliver ACID snapshots, schema evolution, and time travel that raw S3 can't. Strong consistency made commit protocols reliable; partition/prefix layout drives both cost (data scanned) and throughput."},
  {"sec":"CLOUD-S3","front":"How do you control access to S3, and which boundary is decisive?","back":"Layers: IAM identity policies, <strong>bucket policies</strong> (resource-based), ACLs (legacy — disabled by default on new buckets via <code>BucketOwnerEnforced</code> since April 2023), and the account/bucket-level <strong>Block Public Access</strong>. BPA <strong>overrides any policy or ACL that would make data public</strong> — it's the decisive guardrail and is ON by default for new buckets. Cross-account access needs an explicit allow on <em>both</em> the IAM and bucket-policy sides."}
);

Object.assign(GLOSSARY_DATA, {
  "bucket-key-object": {"term":"Bucket, Key & Object","group":"CLOUD-S3","aliases":["bucket","object key","s3 object","flat namespace"],"eli5":"S3 is a giant key-value store: a globally-named bucket holds objects addressed by a single string key — folders are an illusion.","memory":"Bucket = the drawer, key = the full label on the item. There are no dividers in the drawer — the labels just happen to share prefixes.","full":"<p>An <strong>object</strong> is an immutable blob plus metadata, stored in a <strong>bucket</strong> (globally unique name) under a <strong>key</strong> (UTF-8 string up to 1024 bytes). The namespace is <strong>flat</strong> — there are no directories; <code>/</code> is an ordinary character. Tools fake a tree by grouping on prefixes with a <code>delimiter</code>.</p><p>Objects are write-whole: you cannot append, edit in place, or atomically rename (a 'rename' is COPY + DELETE). This immutability is exactly why table formats like Iceberg exist — they layer mutability and transactions on top of append-only object storage.</p>","trap":"Interviewers ask 'how do you rename a 10 TB prefix?' Correct answer: you can't cheaply — it's a server-side COPY of every object then DELETE, billed per request and per GB. There is no metadata-only move.","worked":"Key <code>db/orders/dt=2026-06-21/part-0001.parquet</code> is one flat string; a LIST with <code>delimiter=/</code> and <code>prefix=db/orders/</code> returns the common prefix <code>dt=2026-06-21/</code> as a pseudo-folder.","related":["prefix-performance","strong-consistency","lakehouse-on-s3"]},
  "strong-consistency": {"term":"Strong Read-After-Write Consistency","group":"CLOUD-S3","aliases":["s3 consistency","read-after-write","eventual consistency"],"eli5":"After any successful write, every subsequent read anywhere sees the latest data — no stale reads, no waiting.","memory":"Dec 2020 = the day S3Guard died. Consistency is now a property of S3, not your job.","full":"<p>Since December 2020, S3 is <strong>strongly consistent</strong> for PUT, GET, LIST, and DELETE on every object in every region, automatically and free, with no performance penalty. A successful write is immediately visible to all readers, and a LIST immediately reflects added/removed objects. Previously, brand-new-object PUTs were read-after-write but overwrite-PUTs and DELETEs were <em>eventually</em> consistent, which produced 'file not found' / stale-read races in analytics jobs.</p><p>This killed entire workaround categories: EMRFS consistent view and S3Guard (external DynamoDB metadata stores). It does <strong>not</strong> provide transactions, multi-object atomicity, or visibility of in-progress multipart uploads — those still need a table format.</p>","trap":"'Strong consistency means S3 is transactional' — FALSE. It's per-object. Two writers overwriting the same key still last-writer-wins with no atomic compare-and-set; cross-object atomicity requires Iceberg/Delta.","worked":"A Spark job writes <code>part-000.parquet</code> then immediately LISTs the output dir; pre-2020 the LIST might omit the just-written file, breaking the commit. Today the file is guaranteed present.","related":["bucket-key-object","lakehouse-on-s3","versioning"]},
  "durability-availability": {"term":"Durability vs Availability (11 nines)","group":"CLOUD-S3","aliases":["11 nines","99.999999999","durability","availability"],"eli5":"Durability is the odds S3 never loses your object; availability is the odds you can reach it right now — they're different numbers.","memory":"Durability = will it still exist? Availability = can I get it this second? Eleven nines is durability; four nines is availability.","full":"<p>S3 Standard is designed for <strong>99.999999999% (11 nines) durability</strong>, achieved by redundantly storing each object across <strong>three or more Availability Zones</strong>. Statistically that's roughly one object lost per ten million per ten thousand years. <strong>Availability</strong> is a separate, lower SLA — 99.99% for Standard, 99.9% for Standard-IA, 99.5% for One-Zone-IA — describing whether the service can serve a request, not whether data survives.</p><p>One-Zone-IA still offers 11 nines of <em>device</em> durability but stores in a <strong>single AZ</strong>, so an AZ-loss event destroys the data — only acceptable for re-creatable data (e.g. intermediate shuffle output, replicas).</p>","trap":"Candidates conflate the two. If asked 'does 11 nines mean 99.999999999% uptime?' — no, that's durability. Standard availability is 99.99%.","worked":"Put 10 million objects in S3 Standard and you'd expect to lose one roughly every 10,000 years — but you could still get a transient 500/503 (an availability blip) on any request.","related":["storage-classes","lifecycle"]},
  "storage-classes": {"term":"S3 Storage Classes","group":"CLOUD-S3","aliases":["standard-ia","one-zone-ia","glacier","intelligent-tiering","deep archive"],"eli5":"A menu trading storage price for retrieval cost, access latency, and AZ redundancy — pick by how often and how fast you'll read.","memory":"Hot→Cold: Standard → IA → Glacier Instant → Glacier Flexible → Deep Archive. Further right = cheaper to store, pricier/slower to read.","full":"<p>Classes: <strong>Standard</strong> (hot, no retrieval fee); <strong>Standard-IA</strong> (cheaper storage + per-GB retrieval, 30-day min, multi-AZ); <strong>One-Zone-IA</strong> (~20% less than IA, single AZ); <strong>Glacier Instant Retrieval</strong> (archive price, ms reads, 90-day min); <strong>Glacier Flexible Retrieval</strong> (mins–12h restore, 90-day min); <strong>Glacier Deep Archive</strong> (cheapest, 12–48h restore, 180-day min); and <strong>Intelligent-Tiering</strong> (auto-tiers for a monitoring fee, no retrieval charges).</p><p>The decision axes are: access frequency, acceptable retrieval latency, minimum-duration commitment, and whether single-AZ risk is acceptable. IA/Glacier only win for genuinely cold, large objects — small/hot data is cheaper in Standard.</p>","trap":"The 128 KB minimum billable object size applies to Standard-IA, One-Zone-IA, and Glacier Instant Retrieval (NOT Flexible/Deep Archive). Combined with per-GB retrieval and minimum-duration charges, transitioning millions of tiny files to those classes often costs MORE than Standard. Always model total cost including request and retrieval fees.","worked":"7-year compliance logs, read maybe once: Deep Archive. Last 90 days of query-able logs read weekly: Standard or Intelligent-Tiering. Re-derivable Spark scratch: One-Zone-IA.","related":["lifecycle","durability-availability","intelligent-tiering"]},
  "intelligent-tiering": {"term":"S3 Intelligent-Tiering","group":"CLOUD-S3","aliases":["int-tiering","auto tiering"],"eli5":"S3 watches access patterns and moves each object to the cheapest tier automatically, charging a tiny monitoring fee instead of retrieval fees.","memory":"Intelligent-Tiering = 'set it and forget it' insurance against guessing access patterns wrong; you pay a small watcher fee to avoid retrieval surprises.","full":"<p>Intelligent-Tiering moves objects across a frequent-access and infrequent-access tier (and optional archive tiers) based on per-object access in the last 30/90+ days, for a small <strong>per-object monitoring/automation fee</strong>. Crucially it charges <strong>no retrieval fees</strong>, so unexpected reads of 'cold' data are free of penalty — the key advantage over manual IA.</p><p>Best when access patterns are unknown or shifting. Objects <strong>smaller than 128 KB are never auto-tiered</strong> — they stay at the frequent-access rate and are <em>not</em> charged the monitoring fee, so IT silently does nothing for tiny objects. Poor fit too for data you already know is cold, where manual Glacier/Deep Archive is cheaper (you skip the monitoring fee).</p>","trap":"Common myth: 'objects under 128 KB get charged a 128 KB floor in IT.' FALSE — IT doesn't bill a floor or a monitoring fee on sub-128 KB objects; it just never tiers them, so they sit at frequent-access rates getting no benefit. (The 128 KB billing floor is an IA / One-Zone-IA / Glacier-Instant thing.)","worked":"A data lake with mixed hot/cold partitions and unpredictable re-queries: Intelligent-Tiering avoids both retrieval-fee surprises and manual lifecycle guesswork — but objects under 128 KB just sit in frequent access untouched.","related":["storage-classes","lifecycle"]},
  "lifecycle": {"term":"Lifecycle Policies","group":"CLOUD-S3","aliases":["lifecycle rule","transition","expiration","abort multipart"],"eli5":"Rules that automatically move objects to colder classes or delete them after N days — your automated cost-control and retention engine.","memory":"Every bucket needs two janitors: one that aborts stuck multipart uploads and one that sweeps old versions. Forget them and your bill climbs invisibly.","full":"<p>A lifecycle configuration attaches per-bucket rules, scoped by prefix or tag, that <strong>transition</strong> objects to colder storage classes and/or <strong>expire</strong> (delete) them based on age since creation. On a versioned bucket you separately manage <em>noncurrent</em> versions and delete markers.</p><p>Two rules belong in almost every bucket: <strong>AbortIncompleteMultipartUpload</strong> (orphaned parts bill forever otherwise) and <strong>noncurrent-version expiration</strong>. Remember each transition incurs a per-object request fee, so cold-tiering millions of tiny objects can cost more than leaving them — model it.</p>","trap":"People only set transitions and forget that each transition is a billed request and that noncurrent versions + delete markers keep accruing storage. The silent killer is orphaned multipart parts — invisible in a normal LIST, fully billed.","worked":"Rule: keep in Standard 30 days → Standard-IA → Glacier Flexible after 90 → expire at 365; plus AbortIncompleteMultipartUpload after 7 days and expire noncurrent versions after 30.","related":["storage-classes","multipart-upload","versioning"]},
  "prefix-performance": {"term":"Per-Prefix Performance Scaling","group":"CLOUD-S3","aliases":["3500 writes","5500 reads","503 slowdown","hot prefix","key sharding"],"eli5":"S3 throughput limits are per key-prefix, so you scale by spreading keys across many prefixes — a hot single prefix throttles.","memory":"3,500 write / 5,500 read — PER PREFIX. Want 10x? Use 10 prefixes. The limit is a per-partition speed limit, not a bucket cap.","full":"<p>Each S3 prefix sustains at least <strong>3,500 PUT/COPY/POST/DELETE</strong> and <strong>5,500 GET/HEAD</strong> requests per second; S3 auto-partitions the keyspace and you can get effectively unlimited throughput by <strong>parallelizing across prefixes</strong>. A single hot prefix that exceeds the limit returns <code>503 SlowDown</code> until S3 splits that partition (which takes time).</p><p>Layout is therefore a throughput decision. Time-only keys (<code>.../2026/06/21/</code>) funnel a day's writes into one prefix; injecting entropy high in the key fans writes across partitions. Since strong consistency arrived, the old hash-prefix trick is purely about throughput, no longer about avoiding stale reads.</p>","trap":"'S3 has a fixed bucket-wide request limit' — wrong. It's per prefix and auto-scales. The real failure mode is a single hot prefix (often a date) hitting 503 SlowDown before S3 has split the partition.","worked":"Ingesting 20k events/s: keying as <code>{hash2}/dt=2026-06-21/...</code> spreads load across many partitions instead of one date prefix throttling at 3,500/s.","related":["bucket-key-object","strong-consistency","lakehouse-on-s3"]},
  "multipart-upload": {"term":"Multipart Upload","group":"CLOUD-S3","aliases":["mpu","complete multipart","large object upload"],"eli5":"Split a big object into parts, upload them in parallel, then stitch them into one object with a final commit.","memory":"Big files = many parts + one COMPLETE. The invisible bill is the upload you forgot to finish — abort it via lifecycle.","full":"<p>Multipart upload (MPU) breaks an object into parts (5 MB–5 GB each, last part exempt), up to <strong>10,000 parts</strong> and a <strong>5 TB</strong> max object. Parts upload independently and in parallel; the object only becomes visible on <code>CompleteMultipartUpload</code>, which assembles them server-side. It's <strong>required for objects over 5 GB</strong> (the single-PUT ceiling) and recommended above ~100 MB for throughput and retry granularity.</p><p>The cost trap: an MPU that's started but never completed or aborted leaves billable parts in storage that <strong>don't appear in a normal LIST</strong> — only in <code>ListMultipartUploads</code>. This is why an AbortIncompleteMultipartUpload lifecycle rule is mandatory.</p>","trap":"Orphaned multipart parts are the classic 'why is my bucket bigger/costlier than the objects I can see?' mystery. They're invisible to ListObjectsV2 and accrue storage charges indefinitely.","worked":"Uploading a 200 GB Parquet dump as 2,000 × 100 MB parts in parallel finishes far faster than a serial PUT and lets you retry only failed parts; a 7-day abort rule cleans up any that never complete.","related":["lifecycle","bucket-key-object","prefix-performance"]},
  "versioning": {"term":"S3 Versioning","group":"CLOUD-S3","aliases":["object versioning","delete marker","noncurrent version"],"eli5":"Bucket-level setting that keeps every overwrite as a separate version and turns deletes into reversible markers.","memory":"Delete doesn't delete — it lays a tombstone (delete marker) and the old bytes keep billing until lifecycle sweeps them.","full":"<p>Enabled per bucket, versioning preserves prior versions on overwrite and replaces DELETE with a <strong>delete marker</strong> (a tombstone that hides the object but keeps the data). It protects against accidental overwrite/delete and is a <strong>prerequisite for replication</strong> (CRR/SRR). Once enabled it can only be <strong>suspended</strong>, never fully turned off, and existing versions remain.</p><p>The cost/perf trap: noncurrent versions and delete markers keep billing and bloat LIST operations. Pair versioning with lifecycle rules that expire noncurrent versions and clean up expired delete markers, or storage grows unbounded.</p>","trap":"On a versioned bucket, 'I deleted those objects so why am I still paying?' — the data lives on as noncurrent versions behind delete markers. You must expire noncurrent versions explicitly.","worked":"User deletes <code>config.json</code>; S3 adds a delete marker. <code>GET</code> now 404s, but listing versions shows the old object — remove the marker to restore it, or a lifecycle rule expires it after 30 days.","related":["lifecycle","strong-consistency","bucket-key-object"]},
  "lakehouse-on-s3": {"term":"Lakehouse on S3 (Parquet/Iceberg)","group":"CLOUD-S3","aliases":["data lake","iceberg","parquet","delta lake","table format"],"eli5":"S3 is the cheap, durable storage layer; columnar files plus a table format turn it into a queryable, transactional warehouse.","memory":"S3 = the disk, Parquet = the file format, Iceberg = the 'table' that makes immutable files behave like a transactional warehouse.","full":"<p>The lakehouse decouples storage (S3) from compute (Athena, EMR/Spark, Trino, Redshift Spectrum), all reading the same <strong>Parquet/ORC</strong> files with no copies. A <strong>table format</strong> — Iceberg, Delta, or Hudi — adds a metadata/manifest layer over immutable S3 objects to provide ACID snapshots, schema evolution, hidden partitioning, and time travel that raw object storage lacks.</p><p>Two S3 properties make this work: strong consistency (reliable commit protocols and LIST) and per-prefix scaling (partition layout sets both throughput and the bytes Athena scans, which is what you pay for). Good partitioning + columnar pruning is the difference between scanning gigabytes and terabytes.</p>","trap":"'S3 itself gives ACID tables' — no. S3 gives per-object strong consistency only; multi-file atomic commits, snapshot isolation, and time travel come from the table format layered on top.","worked":"An Iceberg table over <code>s3://lake/orders/</code> commits new data files plus a metadata snapshot atomically; Athena queries a consistent snapshot and prunes partitions so it scans only the relevant Parquet, minimizing cost.","related":["strong-consistency","prefix-performance","bucket-key-object","storage-classes"]}
});
