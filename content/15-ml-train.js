/* ML Training Fundamentals (ML-TRAIN) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"ML-TRAIN","front":"Supervised vs unsupervised vs self-supervised learning","back":"<strong>Supervised</strong> learns a mapping from inputs to human-provided labels (classification, regression). <strong>Unsupervised</strong> finds structure in unlabeled data (clustering, PCA). <strong>Self-supervised</strong> manufactures labels from the data itself (predict the masked/next token), giving supervised-style gradients without manual annotation. The embedding models behind RAG are trained self-supervised: contrastive objectives treat (query, relevant passage) as positive pairs and in-batch others as negatives."},
  {"sec":"ML-TRAIN","front":"Why is the test set touched exactly once?","back":"Every time you look at test results and change something, information leaks from test into your decisions, and the score becomes an optimistic estimate of generalization. The <strong>validation set</strong> is the surface you tune against (hyperparameters, early stopping, model choice); the <strong>test set</strong> is opened once at the very end to get an unbiased estimate. Reusing it turns it into a second validation set and you lose your only honest number."},
  {"sec":"ML-TRAIN","front":"What does MSE optimize and when do you use it?","back":"<strong>Mean Squared Error</strong>, <code>mean((y - ŷ)²)</code>, is the loss for regression. Minimizing it makes the model predict the <em>conditional mean</em> E[y|x] (it is also maximum likelihood under Gaussian noise). The square heavily penalizes large errors, so MSE is sensitive to outliers; use <strong>MAE</strong> (predicts the conditional median) or Huber loss when you need robustness."},
  {"sec":"ML-TRAIN","front":"What does cross-entropy optimize?","back":"<strong>Cross-entropy</strong> is the classification loss; the multi-class form is <code>-Σ y·log(ŷ)</code> over class probabilities. Minimizing it maximizes the likelihood of the correct labels and is equivalent to minimizing KL divergence between the true and predicted distributions. It pairs with <code>softmax</code> (multi-class) or <code>sigmoid</code> (binary); the log term blows up as a confident-and-wrong probability approaches 0, giving the steep gradient that drives learning. The InfoNCE loss used to train retrieval embeddings is cross-entropy over in-batch negatives."},
  {"sec":"ML-TRAIN","front":"Gradient descent: SGD vs mini-batch vs full-batch","back":"<ul><li><strong>Full-batch</strong>: gradient over the whole dataset per step — accurate direction, but slow and memory-heavy.</li><li><strong>SGD (stochastic)</strong>: one example per step — noisy, cheap, the noise can help escape sharp minima.</li><li><strong>Mini-batch</strong>: the practical default (e.g. 32–512 examples) — stable gradient estimates plus GPU-friendly vectorization.</li></ul>In practice \"SGD\" almost always means mini-batch SGD."},
  {"sec":"ML-TRAIN","front":"Why is Adam usually the default optimizer?","back":"<strong>Adam</strong> combines momentum (an EMA of past gradients) with per-parameter adaptive learning rates (an EMA of squared gradients), so it needs little LR tuning and converges fast on sparse, noisy, ill-conditioned problems — typical of deep nets. The cost is more memory (two extra state tensors per parameter, roughly tripling optimizer memory vs plain SGD) and that well-tuned SGD-with-momentum sometimes generalizes slightly better on vision. <code>AdamW</code> (decoupled weight decay) is the standard for training transformers."},
  {"sec":"ML-TRAIN","front":"Learning rate: what happens too high vs too low?","back":"The learning rate scales each gradient step. <strong>Too high</strong>: loss oscillates, spikes, or diverges to NaN because steps overshoot the minimum. <strong>Too low</strong>: training crawls, wastes compute, and can stall in a poor region. It is the single most important hyperparameter; standard practice is a warmup then a decay/cosine schedule, often found via an LR-range test."},
  {"sec":"ML-TRAIN","front":"Epoch vs batch size vs iteration","back":"An <strong>epoch</strong> is one full pass over the training set. <strong>Batch size</strong> is the number of examples per gradient step. An <strong>iteration</strong> (step) processes one batch, so <code>iterations_per_epoch = dataset_size / batch_size</code>. Larger batches give smoother gradients and better hardware utilization but often need a higher LR (linear scaling rule) and can generalize slightly worse without tuning."},
  {"sec":"ML-TRAIN","front":"Overfitting vs underfitting — how do you diagnose each?","back":"<strong>Underfitting</strong>: high training error AND high validation error — the model is too simple or undertrained (high bias). <strong>Overfitting</strong>: low training error but a large gap up to validation error — the model memorized noise (high variance). Diagnose by watching the train/val gap over epochs: a widening gap with rising val loss means stop and regularize; both losses high and flat means add capacity or train longer."},
  {"sec":"ML-TRAIN","front":"The bias-variance tradeoff","back":"Expected error decomposes into <strong>bias²</strong> (error from wrong assumptions / too-simple model), <strong>variance</strong> (sensitivity to the particular training sample), and irreducible noise. Simple models = high bias, low variance; complex models = low bias, high variance. The classic U-shaped test-error curve balances them, though heavily overparameterized deep nets can show <em>double descent</em> where error falls again past the interpolation threshold."},
  {"sec":"ML-TRAIN","front":"L1 vs L2 regularization","back":"Both add a penalty on weight magnitude to the loss. <strong>L2 (ridge / weight decay)</strong> adds <code>λ·Σw²</code>, shrinking weights smoothly toward zero — the default for deep nets. <strong>L1 (lasso)</strong> adds <code>λ·Σ|w|</code>, which drives some weights to exactly zero, giving sparse, feature-selecting models. <code>λ</code> trades fit against simplicity; tune it on validation."},
  {"sec":"ML-TRAIN","front":"How does dropout regularize a network?","back":"<strong>Dropout</strong> randomly zeroes a fraction <code>p</code> of activations each training step, so no neuron can rely on a specific co-feature — it forces redundant, robust representations and approximates training an ensemble of subnetworks. It is <em>active only during training</em>. Modern frameworks use <em>inverted dropout</em>: surviving activations are scaled by <code>1/(1-p)</code> at train time, so inference is a plain forward pass with nothing dropped or rescaled. Forgetting to switch to eval mode (so dropout stays on at inference) is a classic bug that silently degrades predictions."},
  {"sec":"ML-TRAIN","front":"What is early stopping and why does it count as regularization?","back":"<strong>Early stopping</strong> monitors validation loss and halts training (keeping the best checkpoint) once it stops improving for a <code>patience</code> number of epochs. It limits how far weights can grow and how much the model fits training noise; for a linear/quadratic-loss model, gradient descent with early stopping is provably close to L2 (ridge) regularization. It is nearly free and one of the most effective regularizers, but it requires a trustworthy validation set."},
  {"sec":"ML-TRAIN","front":"Why does more/cleaner data plus a simpler model often beat a complex one?","back":"Generalization error roughly scales with model capacity over data size, so a complex model on little data overfits, while extra clean data shrinks variance and lets a simpler model win. Simpler models are also cheaper to serve, easier to debug, and less brittle to distribution shift. Label noise caps achievable accuracy regardless of architecture — \"garbage in, garbage out.\" In RAG this is why fixing chunking, dedup, and retrieval quality beats swapping in a bigger generator."},
  {"sec":"ML-TRAIN","front":"What is data leakage and how does it inflate scores?","back":"<strong>Leakage</strong> is when information unavailable at prediction time sneaks into training, making offline metrics look great and production fail. Common forms: fitting a scaler/imputer/vectorizer on the full dataset before splitting, target leakage from a feature derived post-outcome, and temporal leakage from random-splitting time-series. Fix: split first, fit all preprocessing inside cross-validation folds, and split time-ordered data by time."},
  {"sec":"ML-TRAIN","front":"Why use stratified or time-based splits instead of a random split?","back":"A plain random split can leave a rare class under-represented in validation/test or let future rows train a model evaluated on the past. <strong>Stratified</strong> splitting preserves class proportions across splits (vital for imbalanced data); <strong>time-based</strong> splitting trains on the past and tests on the future to respect causality. Grouped splitting keeps all rows of one entity (e.g. a user) in a single split to avoid identity leakage."}
);

Object.assign(GLOSSARY_DATA, {
  "train-val-test-split": {"term":"Train / Validation / Test Split","group":"ML-TRAIN","aliases":["train/validation/test","holdout set","validation set","test set","holdout"],"eli5":"Three separate piles of data: one to learn from, one to tune against, and one you open exactly once to grade yourself honestly.","memory":"Train to learn, validate to choose, test to confess — and you only confess once.","full":"<p>The <strong>training set</strong> fits model parameters; the <strong>validation set</strong> (a.k.a. dev set) selects hyperparameters, architecture, and the early-stopping point; the <strong>test set</strong> gives a single unbiased estimate of generalization and is touched once. Each peek at the test set that influences a decision leaks information and biases the estimate optimistically.</p><p>Use k-fold cross-validation when data is scarce to reduce the variance of the validation estimate, but still keep a final held-out test set. For temporal or grouped data, replace random splitting with time-based or grouped splitting to prevent leakage.</p>","trap":"\"Why not just tune on the test set?\" Because then it becomes a second validation set: you've optimized your decisions against it, so its score no longer estimates generalization.","worked":"100k rows: 80k train, 10k val, 10k test. You try 12 LR/dropout combos, pick the best by val loss, early-stop on val, then evaluate ONCE on test. If test ≪ val, you over-searched the val set.","related":["data-leakage","early-stopping","overfitting-underfitting"]},
  "loss-functions": {"term":"Loss Functions (MSE & Cross-Entropy)","group":"ML-TRAIN","aliases":["loss function","objective function","mse","cross-entropy","log loss","cost function"],"eli5":"A single number that says how wrong the model is right now; training just rolls downhill on it.","memory":"Squared error → mean; cross-entropy → likelihood. Regression squares, classification logs.","full":"<p><strong>MSE</strong> (<code>mean((y-ŷ)²)</code>) is the regression workhorse; minimizing it predicts the conditional mean and corresponds to maximum likelihood under Gaussian noise. Its squared term over-weights outliers, so MAE (conditional median) or Huber are robust alternatives. <strong>Cross-entropy</strong> (multi-class: <code>-Σ y·log ŷ</code>) is the classification loss; minimizing it is maximum-likelihood / minimum-KL between true and predicted label distributions, paired with softmax (multi-class) or sigmoid (binary) outputs.</p><p>The loss must be differentiable to backprop through; metrics you actually care about (accuracy, F1, nDCG, recall@k) are often non-differentiable, so you train on a differentiable surrogate (cross-entropy, InfoNCE) and <em>evaluate</em> on the real metric. Retrieval embedding training uses InfoNCE — cross-entropy over in-batch negatives.</p>","trap":"\"Why not just train on accuracy?\" Accuracy is flat or discontinuous in the weights — zero gradient almost everywhere — so it can't drive gradient descent. You optimize a smooth surrogate and only report accuracy.","worked":"A confident-wrong binary prediction ŷ=0.99 for true y=0 gives loss <code>-log(0.01)≈4.6</code>; a hedged ŷ=0.5 gives <code>-log(0.5)≈0.69</code>. Cross-entropy punishes confident mistakes hard.","related":["gradient-descent","regularization","train-val-test-split"]},
  "gradient-descent": {"term":"Gradient Descent & Optimizers","group":"ML-TRAIN","aliases":["gradient descent","sgd","stochastic gradient descent","mini-batch","adam","adamw","optimizer","momentum"],"eli5":"Nudge every weight a little in the direction that lowers the loss, then repeat thousands of times.","memory":"SGD walks, momentum rolls, Adam rolls with per-leg shock absorbers.","full":"<p>Each step computes the gradient of the loss w.r.t. parameters and moves opposite it, scaled by the <strong>learning rate</strong>. <strong>Full-batch</strong> uses all data (accurate, slow); <strong>SGD</strong> uses one example (noisy, cheap); <strong>mini-batch</strong> (32–512) is the practical default balancing gradient quality and GPU throughput. The noise in SGD is a feature: it helps escape sharp minima and saddle points.</p><p><strong>Momentum</strong> accumulates an EMA of gradients to glide through ravines. <strong>Adam</strong> adds per-parameter adaptive step sizes (EMA of squared gradients) and is the low-tuning default; <strong>AdamW</strong> decouples weight decay and is standard for transformers. Adam stores 2 extra state tensors per parameter (roughly tripling optimizer memory vs SGD), and well-tuned SGD+momentum can edge it on generalization in some vision tasks.</p>","trap":"\"SGD vs mini-batch?\" In modern practice the optimizer everyone calls \"SGD\" IS mini-batch SGD — pure one-sample SGD is almost never used. Don't describe SGD as full-batch.","worked":"1M rows, batch=256 → ~3,900 steps/epoch. Adam tracks m (1st moment) and v (2nd moment) per weight, so a 1B-param model needs ~8GB extra just for optimizer state in fp32.","related":["learning-rate","loss-functions","epoch-batch-size"]},
  "learning-rate": {"term":"Learning Rate","group":"ML-TRAIN","aliases":["learning rate","lr","step size","lr schedule","warmup","cosine decay"],"eli5":"How big a step you take downhill each update — too big and you fly past the valley, too small and you never arrive.","memory":"Too hot it explodes, too cold it freezes — Goldilocks the LR first, everything else second.","full":"<p>The learning rate (LR) multiplies the gradient to set step size and is the highest-leverage hyperparameter. <strong>Too high</strong>: loss oscillates, diverges, or goes NaN from overshoot. <strong>Too low</strong>: training crawls and may stall in a poor region. The usable range often spans orders of magnitude, so it is tuned on a log scale.</p><p>Schedules matter as much as the value: a short <strong>warmup</strong> (ramp from ~0) stabilizes early steps for adaptive optimizers and large batches, followed by <strong>cosine or step decay</strong> to anneal into a flat minimum. With Adam, typical starting LRs are ~1e-3 to ~3e-4; with transformer fine-tuning, often ~1e-5 to ~5e-5 — treat these as task-dependent.</p>","trap":"Batch size and LR are coupled: if you double the batch, you usually need to scale the LR up (the linear scaling rule) or training slows — interviewers probe whether you know they're not independent.","worked":"An LR-range test sweeps LR from 1e-7 to 1 over a few hundred steps; you plot loss vs LR and pick the value about one order of magnitude below where loss starts climbing.","related":["gradient-descent","epoch-batch-size"]},
  "epoch-batch-size": {"term":"Epochs & Batch Size","group":"ML-TRAIN","aliases":["epoch","batch size","iteration","step","mini-batch size","linear scaling rule"],"eli5":"An epoch is one lap over all your data; batch size is how many examples you read before each weight update.","memory":"Epoch = a lap; batch = a sip; step = one swallow. Bigger sips need a bigger learning-rate gulp.","full":"<p>One <strong>epoch</strong> = one full pass over the training set; one <strong>iteration/step</strong> processes one batch; <code>steps_per_epoch = N / batch_size</code>. Too few epochs underfit; too many overfit (use early stopping rather than guessing). <strong>Batch size</strong> trades gradient quality and hardware efficiency against memory and generalization.</p><p>Larger batches give lower-variance gradients and better GPU saturation but need a higher LR (linear scaling rule) and sometimes generalize slightly worse, drifting toward sharp minima. Gradient accumulation simulates a large effective batch when GPU memory is the limit by summing gradients over several micro-batches before stepping.</p>","trap":"\"Bigger batch is strictly better\" is wrong — beyond a point it wastes compute for the same loss and can hurt generalization unless you retune the LR and add warmup.","worked":"GPU fits batch 16 but you want effective 128: accumulate gradients over 8 micro-batches, then call the optimizer step once — same math, 1/8 the peak activation memory.","related":["learning-rate","gradient-descent","overfitting-underfitting"]},
  "overfitting-underfitting": {"term":"Overfitting vs Underfitting & Bias-Variance","group":"ML-TRAIN","aliases":["overfitting","underfitting","bias-variance tradeoff","bias variance","generalization gap","double descent"],"eli5":"Underfitting is a student who didn't study; overfitting is one who memorized the practice exam and bombs the real test.","memory":"UNDERfit = under-studied (both errors high); OVERfit = over-memorized (train low, val high). Gap = variance.","full":"<p><strong>Underfitting</strong> = high train AND high val error (high bias, model too simple/undertrained). <strong>Overfitting</strong> = low train but high val error (high variance, model memorized noise). The <strong>generalization gap</strong> (val − train) is your live diagnostic across epochs. Expected error = bias² + variance + irreducible noise; simpler models trade variance for bias and vice versa.</p><p>The classic remedy for overfitting is more data, regularization, or less capacity; for underfitting, more capacity, better features, or longer training. Note <strong>double descent</strong>: very overparameterized models can re-improve past the interpolation threshold, which is why the naive U-curve intuition partly breaks for large nets.</p>","trap":"A small train/val gap doesn't prove a good model — if BOTH errors are high you're underfitting, not generalizing well. Always check the absolute training error, not just the gap.","worked":"Train acc 99%, val acc 72% → overfit; regularize. Train acc 68%, val acc 67% → underfit; add capacity/features. The first gap is variance, the second floor is bias.","related":["regularization","train-val-test-split","data-leakage"]},
  "regularization": {"term":"Regularization (L1/L2, Dropout, Early Stopping)","group":"ML-TRAIN","aliases":["regularization","l1","l2","weight decay","ridge","lasso","dropout","early stopping"],"eli5":"Anything that stops the model from getting too clever and memorizing the training data.","memory":"L2 squeezes, L1 zeroes, dropout hides, early stopping quits while ahead.","full":"<p>Regularization fights variance by constraining the model. <strong>L2 / weight decay</strong> (<code>λΣw²</code>) shrinks weights smoothly — the deep-learning default. <strong>L1</strong> (<code>λΣ|w|</code>) drives weights to exactly zero, yielding sparse, feature-selecting models. <strong>Dropout</strong> randomly zeroes activations in training to force redundant representations (an implicit ensemble). <strong>Early stopping</strong> halts when val loss stops improving, capping how far the model fits noise.</p><p>These combine and each adds a knob (<code>λ</code>, dropout <code>p</code>, patience) tuned on validation. Data augmentation and getting more clean data are also regularizers in effect. For transformers, AdamW applies <em>decoupled</em> weight decay — equivalent to L2 only for plain SGD, which is exactly why the decoupled form was introduced for adaptive optimizers.</p>","trap":"Dropout and batchnorm behave differently in train vs eval mode — forgetting <code>model.eval()</code> at inference leaves dropout active and uses per-batch stats, silently degrading predictions. Classic production bug. (With inverted dropout the scaling is done at train time, so eval just runs a plain forward pass.)","worked":"Adding L2 λ=1e-4 + dropout p=0.3 + early stopping (patience=5) shrinks a 99/72 train/val split to 92/88 — you traded a little training accuracy for real generalization.","related":["overfitting-underfitting","early-stopping","gradient-descent"]},
  "early-stopping": {"term":"Early Stopping","group":"ML-TRAIN","aliases":["early stopping","patience","best checkpoint","early stop"],"eli5":"Stop training the moment the model stops getting better on validation, and keep the best version you saw.","memory":"Quit while you're ahead — and save the checkpoint from when you WERE ahead, not the last one.","full":"<p>Early stopping watches a validation metric each epoch and halts after it fails to improve for <code>patience</code> consecutive epochs, restoring the best checkpoint. It implicitly limits weight growth and fitting of training noise; for a linear model with quadratic loss, gradient descent with early stopping is provably close to L2 (ridge) regularization, and it's nearly free since you're already evaluating on validation.</p><p>Its quality is only as good as the validation set: a leaky or unrepresentative val set makes early stopping fire at the wrong point. Pair it with a held-out test set so the stopping decision (made on val) doesn't bias your final reported number.</p>","trap":"People restore the LAST epoch instead of the BEST — early stopping requires checkpointing the best-val model, otherwise you ship a slightly-overfit final epoch.","worked":"Val loss bottoms at epoch 18, then drifts up; with patience=5 you stop at epoch 23 and roll back to the epoch-18 weights.","related":["regularization","train-val-test-split","overfitting-underfitting"]},
  "data-leakage": {"term":"Data Leakage","group":"ML-TRAIN","aliases":["data leakage","target leakage","train-test contamination","temporal leakage","leakage"],"eli5":"When the model peeks at information it won't have in production, so it looks great offline and flops live.","memory":"If a feature 'knows the future' or your scaler 'saw the test set,' it's leaking. Split BEFORE you fit anything.","full":"<p>Leakage is any path by which information unavailable at prediction time contaminates training. Common forms: <strong>preprocessing leakage</strong> (fitting a scaler, imputer, PCA, or vectorizer on the full dataset before splitting), <strong>target leakage</strong> (a feature computed from or after the outcome), and <strong>temporal leakage</strong> (random-splitting time-series so the model trains on the future).</p><p>The fix is discipline: split first, then fit every stateful transform inside the training fold only (sklearn <code>Pipeline</code> inside cross-validation does this for you); split time-ordered data by time; and use grouped splits so all rows of an entity stay in one split. Leakage is the #1 cause of an offline-great / online-terrible model.</p>","trap":"The sneakiest leak is fitting <code>StandardScaler</code>/TF-IDF/imputation on all data before the split — it quietly passes test-set statistics into training and inflates every metric. Always fit inside the pipeline.","worked":"Predicting churn with a feature \"days_since_cancellation\" — it's only non-null AFTER the user churned, so AUC hits 0.99 offline and the model is useless in production.","related":["train-val-test-split","overfitting-underfitting"]}
});
