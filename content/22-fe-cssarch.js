/* Modern CSS & Architecture (FE-CSSARCH) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"FE-CSSARCH","front":"Walk through how the cascade resolves two competing declarations. What are the steps, in order?","back":"<p>The cascade is a <strong>multi-stage sort</strong>; the first stage to produce a winner stops the process. In order:</p><ul><li><strong>Origin + importance</strong> — ordered (lowest→highest): UA normal, user normal, author normal, <em>CSS animations</em>, then (importance reverses origins) author important, user important, UA important, and finally <em>CSS transitions</em> at the very top.</li><li><strong>Cascade layers (<code>@layer</code>)</strong> — within an origin, later-declared layers win for normal rules; for <code>!important</code> the layer order is <em>reversed</em>. Unlayered normal rules beat layered ones.</li><li><strong>Specificity</strong> — the (a,b,c) tuple of the winning selector.</li><li><strong>Source order</strong> — last one wins.</li></ul><p>Classic gotcha: <em>origin/importance and layers are sorted before specificity ever enters the picture</em>, so a low-specificity rule in the right origin/layer beats a high-specificity normal rule. Note the asymmetry: a running <strong>animation</strong> overrides normal rules but loses to any <code>!important</code>; a running <strong>transition</strong> overrides <em>everything</em>, including <code>!important</code>.</p>"},
  {"sec":"FE-CSSARCH","front":"How is specificity calculated, and why don't IDs/classes/types ever 'carry'?","back":"<p>Specificity is a 3-component tuple <strong>(a, b, c)</strong> compared left-to-right:</p><ul><li><strong>a</strong> = ID selectors (<code>#id</code>)</li><li><strong>b</strong> = classes, attribute selectors (<code>[type=text]</code>), and pseudo-classes (<code>:hover</code>)</li><li><strong>c</strong> = type selectors (<code>div</code>) and pseudo-elements (<code>::before</code>)</li></ul><p>It is <em>not</em> base-10 — each column is compared independently and <strong>does not overflow</strong>. A single ID <code>(1,0,0)</code> beats <em>any</em> number of classes <code>(0,256,0)</code>. The universal selector <code>*</code> and combinators (<code>&gt;</code>, <code>+</code>, <code>~</code>, descendant) contribute <strong>zero</strong>; <code>:where()</code> forces zero; <code>:is()</code>/<code>:not()</code>/<code>:has()</code> take their <em>most specific</em> argument. Inline <code>style</code> attributes outrank any selector; <code>!important</code> is a separate cascade dimension, not part of the tuple.</p>"},
  {"sec":"FE-CSSARCH","front":"What problem do @layer cascade layers solve, and what's the counterintuitive ordering rule?","back":"<p><code>@layer</code> lets you define <strong>explicit precedence bands</strong> that are sorted <em>before</em> specificity, so you can stop fighting specificity wars and ditch <code>!important</code> hacks. Declaration order establishes priority: <code>@layer reset, base, components, utilities;</code> — <code>utilities</code> wins over <code>components</code> regardless of selector strength.</p><ul><li><strong>Unlayered normal styles win over layered normal styles</strong> — they act as an implicit final (highest-priority) layer.</li><li>For <code>!important</code>, the <em>entire layer order reverses</em>, AND important unlayered styles sit <em>below</em> important layered ones — so an <code>!important</code> rule in an early layer (e.g. <code>reset</code>) can beat an <code>!important</code> rule in <code>utilities</code>.</li></ul><p>Common use: wrap third-party CSS in a low layer (<code>@import url(vendor.css) layer(vendor);</code>) so your authored styles always override it without specificity escalation. Nested layers use dot notation (<code>components.card</code>).</p>"},
  {"sec":"FE-CSSARCH","front":"Custom properties (--var) vs Sass/preprocessor variables — what's the fundamental difference?","back":"<ul><li><strong>Preprocessor vars (<code>$x</code>) are compile-time</strong> — substituted to a literal value before any CSS ships. They don't exist at runtime, can't be read by JS, don't cascade or inherit, and can't respond to media/container queries or state.</li><li><strong>Custom properties (<code>--x</code>) are runtime</strong> — live DOM values that <em>inherit and cascade</em>, are readable/writable from JS (<code>getComputedStyle().getPropertyValue</code> / <code>style.setProperty</code>), and resolve at computed-value time.</li></ul><p>Because they're live and inheriting, custom properties power theming, scoped overrides, and reactive design tokens. The cost: <code>var()</code> resolves per-element at runtime. Key gotcha: an unregistered custom property is <strong>substituted as a token stream then re-parsed</strong> at use, so an invalid <em>resolved</em> value triggers <em>invalid-at-computed-value-time</em> — the property falls back to inherited or initial, not to the prior declaration. Register with <code>@property</code> to get type checking, a real initial value, and animatability.</p>"},
  {"sec":"FE-CSSARCH","front":"Explain var() fallbacks and the difference between the fallback and an invalid value.","back":"<p><code>var(--gap, 16px)</code> — everything after the first comma is the fallback, used <em>only when <code>--gap</code> is not set</em> (or is the guaranteed-invalid value). It is <strong>not</strong> a type guard.</p><ul><li>If <code>--gap</code> <em>is</em> defined but resolves to garbage for the property (e.g. <code>--gap: red</code> used in <code>width</code>), the fallback is <strong>ignored</strong> — you get <em>invalid at computed-value time</em>, which makes the property compute to its <strong>inherited</strong> value (if inherited) or <strong>initial</strong> value.</li><li>Fallbacks nest: <code>var(--a, var(--b, 0))</code> chains. The fallback can itself contain commas (e.g. a <code>font-family</code> list).</li></ul><p>To <em>register</em> a type and a real initial value (so bad values fall back gracefully and the prop can animate), use <code>@property --gap { syntax: '&lt;length&gt;'; initial-value: 16px; inherits: false; }</code>. With registration, an invalid value falls back to the registered <code>initial-value</code> instead of IACVT.</p>"},
  {"sec":"FE-CSSARCH","front":"Container queries: how do @container and container-type work, and why are they not just 'media queries for elements'?","back":"<p>You declare an ancestor as a <strong>query container</strong> via <code>container-type: inline-size</code> (or <code>size</code>), then descendants query <em>that container's</em> size with <code>@container (min-width: 400px) { … }</code>. This enables truly <strong>reusable components</strong> that adapt to their slot, not the viewport.</p><ul><li><code>container-type: inline-size</code> establishes <strong>layout + style + inline-size containment</strong> — the container's <em>inline</em> size no longer depends on its children's content (the usual gotcha: collapsed/zero widths if the element had no intrinsic width). <code>size</code> contains both axes and requires an explicit block-size.</li><li>You cannot query the element's <em>own</em> size — only an <strong>ancestor</strong> container. Name containers with <code>container-name</code> (or the <code>container</code> shorthand) to target a specific one.</li><li><strong>Container query length units</strong> (<code>cqw</code>, <code>cqh</code>, <code>cqi</code>, <code>cqb</code>, <code>cqmin</code>, <code>cqmax</code>) resolve against the nearest matching container.</li></ul>"},
  {"sec":"FE-CSSARCH","front":"Why is mobile-first (min-width) preferred over desktop-first (max-width)?","back":"<p><strong>Mobile-first</strong> writes the base styles for the smallest screen, then <em>layers enhancements upward</em> with <code>@media (min-width: …)</code>. <strong>Desktop-first</strong> starts wide and overrides downward with <code>max-width</code>.</p><ul><li>Mobile-first ships the <em>simplest, lightest</em> rules as the default — better for performance-constrained devices and progressive enhancement.</li><li>Overrides cascade in <strong>ascending</strong> breakpoint order, which reads naturally and avoids the inverted, override-heavy logic of <code>max-width</code> chains.</li><li>Modern <strong>range syntax</strong> (<code>@media (width >= 48rem)</code>) is clearer than min-/max- and avoids the <code>0.02px</code> overlap hack you needed to keep a <code>min-width</code> and a <code>max-width</code> breakpoint from both matching at the same pixel.</li></ul><p>Prefer <code>rem</code>-based breakpoints so they respect the user's root font size (px breakpoints ignore zoom of the root font).</p>"},
  {"sec":"FE-CSSARCH","front":"What are logical properties and what concrete bug do they fix?","back":"<p>Logical properties map to the <strong>writing-mode / direction flow</strong> rather than physical screen sides. <code>margin-inline-start</code>, <code>padding-block</code>, <code>inset-inline-end</code>, <code>border-inline</code>, and <code>inline-size</code>/<code>block-size</code> replace <code>left/right/top/bottom</code> and <code>width/height</code>.</p><ul><li><strong>Inline</strong> = the text-direction axis (horizontal in horizontal writing modes; flips for LTR vs RTL). <strong>Block</strong> = the line-stacking axis (vertical in horizontal writing modes).</li><li>The bug they fix: a hard-coded <code>margin-left</code> on an icon <em>breaks in RTL</em> (Arabic/Hebrew) — <code>margin-inline-start</code> automatically flips to the correct side. One stylesheet, correct in every direction, no <code>[dir=rtl]</code> override sheet.</li></ul><p>Shorthands like <code>inset: 0</code>, <code>margin-block: 1rem 2rem</code> (start then end), and <code>padding-inline</code> keep things terse. Note: a few features (e.g. <code>float</code>) and many third-party libs still assume physical sides, so audit before a full migration.</p>"},
  {"sec":"FE-CSSARCH","front":":is() vs :where() — what's the critical difference, and how does :has() change CSS?","back":"<ul><li><code>:is(a, b, c)</code> takes the <strong>highest specificity of its argument list</strong>. <code>:is(#x, p)</code> contributes <code>(1,0,0)</code>.</li><li><code>:where(a, b, c)</code> always contributes <strong>zero specificity</strong> — ideal for low-priority defaults and design-system base styles that are trivially overridable.</li><li>Both take a <strong>forgiving selector list</strong> — one unsupported/invalid selector is dropped rather than invalidating the whole rule (unlike a bare comma list, where one bad selector kills all).</li></ul><p><code>:has()</code> is the <strong>relational (parent) selector</strong>: <code>.card:has(&gt; img)</code> styles the card based on its descendants/siblings. It enables previously JS-only patterns (<code>form:has(:invalid)</code>, <code>body:has(.modal-open)</code>, <code>label:has(+ input:focus)</code>). Its specificity also takes its <em>most specific</em> argument, like <code>:is()</code>. (Note: <code>:has()</code> itself is <em>not</em> forgiving — an unsupported selector inside it invalidates the <code>:has()</code>.)</p>"},
  {"sec":"FE-CSSARCH","front":"What's the specificity and behavior gotcha of :not()?","back":"<p><code>:not()</code> contributes the <strong>specificity of its most specific argument</strong> (like <code>:is()</code>), <em>not</em> zero. <code>:not(#id)</code> adds <code>(1,0,0)</code> — a frequent source of accidentally-heavy selectors.</p><ul><li>Modern <code>:not()</code> accepts a <strong>selector list</strong>: <code>:not(.a, .b)</code> matches any element that is neither <code>.a</code> nor <code>.b</code> — i.e. it's equivalent to <code>:not(.a):not(.b)</code> (negation distributes over the list). This <em>excludes</em> more elements, not fewer, than people often expect.</li><li>The list form <em>is</em> forgiving, so an invalid argument is dropped rather than killing the rule. (Legacy single-argument <code>:not()</code> was not.)</li></ul><p>Tip: to <em>negate without adding specificity</em>, wrap in <code>:where()</code>: <code>:where(:not(.x))</code> stays at <code>(0,0,0)</code>.</p>"},
  {"sec":"FE-CSSARCH","front":"CSS nesting: what's the rule for an unprefixed nested selector, and where does & matter?","back":"<p>Native CSS nesting (no preprocessor) nests rules inside a parent block. A nested selector is resolved by <strong>prepending the parent</strong>; a selector that <em>doesn't start with <code>&amp;</code></em> gets an <strong>implicit descendant combinator</strong> — so <code>.card { p { … } }</code> behaves like <code>.card :is(p)</code>.</p><ul><li>The <code>&amp;</code> nesting selector references the parent for <strong>compound</strong> targeting: <code>&amp;:hover</code>, <code>&amp;.active</code>, or appended like <code>.theme &amp;</code> to invert context.</li><li>The parent selector is wrapped in <code>:is()</code> when substituted, so nesting can <strong>raise specificity</strong> unexpectedly when the parent selector list contains a high-specificity item (the whole group inherits the most-specific one).</li><li>Unlike Sass, native nesting does <strong>not</strong> support <code>&amp;__element</code> string concatenation — <code>&amp;</code> is a real selector reference, not text glue. <code>&amp;__el</code> is invalid.</li></ul>"},
  {"sec":"FE-CSSARCH","front":"When do you reach for clamp() and why is it better than breakpoint font sizing?","back":"<p><code>clamp(MIN, PREFERRED, MAX)</code> produces a value that scales fluidly between two bounds. For type: <code>font-size: clamp(1rem, 0.5rem + 2vw, 1.5rem)</code> grows with the viewport but never below 1rem or above 1.5rem.</p><ul><li>Replaces a ladder of <code>@media</code> breakpoints with a <strong>single continuous curve</strong> — no jarring jumps at thresholds.</li><li>The preferred term should include a <strong><code>rem</code> component</strong> (not pure <code>vw</code>) so the text still responds to the user's zoom/font-size — a pure-<code>vw</code> size <em>fails WCAG 1.4.4 (Resize Text)</em> because it doesn't scale with the user's font preference.</li><li><code>min()</code> and <code>max()</code> are the one-sided cousins; <code>clamp(a, b, c)</code> is exactly <code>max(a, min(b, c))</code>. Math functions also let you mix units (<code>min(100%, 40rem)</code>).</li></ul>"},
  {"sec":"FE-CSSARCH","front":"Transitions vs animations vs transforms — and what makes a property 'compositor-only'?","back":"<ul><li><strong>Transition</strong> — interpolates between two states triggered by a change (e.g. <code>:hover</code>, a class toggle). One-shot, needs a trigger and a from/to.</li><li><strong>Animation</strong> — <code>@keyframes</code> with multiple stops, runs autonomously, can loop/alternate, no trigger needed.</li><li><strong>Transform</strong> — a <em>property</em> (translate/scale/rotate) you can transition or animate; it doesn't animate by itself.</li></ul><p><strong>Compositor-only</strong> props — chiefly <code>transform</code>, <code>opacity</code>, and <code>filter</code> — can be animated on the <em>compositor thread</em>, skipping layout and paint, so they hold ~60fps even under main-thread load. Animating <code>width</code>, <code>top</code>, or <code>box-shadow</code> instead triggers <strong>layout and/or paint every frame</strong> → jank. The senior move: animate <code>transform: translateX()</code> not <code>left</code>, and fade with <code>opacity</code> not <code>visibility</code>/color.</p>"},
  {"sec":"FE-CSSARCH","front":"What does will-change actually do, and how is it misused?","back":"<p><code>will-change: transform</code> hints the browser to <strong>promote the element to its own compositor layer ahead of time</strong>, so the first frame of an animation isn't stalled by layer creation. It's a <em>performance hint</em>, not a visual style.</p><ul><li><strong>Misuse #1 — leaving it on everything.</strong> Each promoted layer costs GPU memory; <code>will-change</code> on hundreds of elements can <em>degrade</em> performance and exhaust memory.</li><li><strong>Misuse #2 — applying it permanently in CSS.</strong> Best practice is to add it just-in-time (e.g. on <code>:hover</code> or via JS before the animation) and <strong>remove it after</strong>, since the browser already optimizes once an animation is running.</li><li>It has <em>side effects</em>: like <code>transform</code>/<code>opacity&lt;1</code>/<code>filter</code>, promoting an element creates a new <strong>stacking context</strong> and a containing block for <code>position: fixed</code> descendants — not a no-op hint.</li></ul>"},
  {"sec":"FE-CSSARCH","front":"Compare BEM, utility-first (Tailwind), CSS Modules, and CSS-in-JS. What's the runtime-cost gotcha?","back":"<ul><li><strong>BEM</strong> (<code>block__element--modifier</code>) — flat, low-specificity, human-readable classes; scales via discipline but is verbose and has no build-time scoping guarantee (name collisions are on you).</li><li><strong>Utility-first / Tailwind</strong> — compose atomic classes in markup; tiny generated CSS, no naming, but markup churn and weak semantics. v4's <strong>Oxide</strong> engine moves hot paths to Rust and embeds <strong>Lightning CSS</strong>, with CSS-first config (<code>@theme</code> in CSS instead of <code>tailwind.config.js</code>).</li><li><strong>CSS Modules</strong> — class names locally scoped via build-time hashing; zero runtime, framework-agnostic; needs a bundler.</li><li><strong>CSS-in-JS</strong> (styled-components/Emotion) — colocated, dynamic, prop-driven styles; but <strong>runtime libraries serialize and inject styles during render</strong> → per-render cost, hydration complexity, and they don't run in <strong>React Server Components</strong> (the style runtime is client-only).</li></ul><p>The 2026 shift: prefer <strong>zero-runtime</strong> approaches (CSS Modules, Tailwind, or compile-time CSS-in-JS like vanilla-extract / Panda / Linaria) so styling work happens at build time, ships static CSS, and is RSC-compatible.</p>"},
  {"sec":"FE-CSSARCH","front":"prefers-reduced-motion and prefers-color-scheme — how do you implement them correctly?","back":"<p>Both are <strong>user-preference media features</strong> reflecting OS settings.</p><ul><li><code>@media (prefers-reduced-motion: reduce)</code> — for users with vestibular disorders. Don't kill <em>all</em> motion indiscriminately; <strong>replace</strong> large parallax/slide/zoom with subtle cross-fades and disable autoplay/auto-scroll. A robust pattern is to <em>opt in</em>: define motion only inside <code>@media (prefers-reduced-motion: no-preference)</code> so the still version is the default.</li><li><code>@media (prefers-color-scheme: dark)</code> — drives dark mode. Pair with <code>color-scheme: light dark</code> on <code>:root</code> so form controls, scrollbars, and the UA default canvas/text also adapt; the <code>light-dark()</code> color function then lets one declaration supply both values.</li></ul><p>Honoring these is a <strong>WCAG/accessibility</strong> expectation, not a nicety. For toggleable themes, layer a class/<code>data-theme</code> override on top of the media-query default.</p>"},
  {"sec":"FE-CSSARCH","front":"hsl vs oklch — why is oklch the recommended color space in 2026?","back":"<p><code>hsl()</code> is intuitive but <strong>perceptually non-uniform</strong>: equal lightness values across hues look unequal (yellow at 50% L reads far brighter than blue at 50% L), so generated palettes and accessible contrast ramps are unreliable.</p><ul><li><code>oklch(L C H)</code> — <strong>L</strong>ightness (perceptually uniform, 0–1 or 0–100%), <strong>C</strong>hroma (0→unbounded), <strong>H</strong>ue (degrees). Equal L ≈ equal perceived brightness, so lightness ramps and tints/shades stay consistent across hues.</li><li>It's a <strong>wide-gamut</strong> space — can express vivid <em>P3</em> colors that <code>#hex</code>/<code>sRGB hsl</code> physically can't represent (out-of-gamut values are clamped to the display).</li><li>Great for <strong>programmatic theming</strong>: nudge only L for a hover state, only C to desaturate, keeping hue stable.</li></ul><p>Pair with <code>color-mix(in oklch, …)</code> for perceptually even blends. Browser support is broad by 2026, but provide an sRGB fallback (or use <code>@supports</code>) if you must serve very old engines.</p>"}
);

Object.assign(GLOSSARY_DATA, {
  "the-cascade": {"term":"The Cascade","group":"FE-CSSARCH","eli5":"The rulebook CSS uses to pick a winner when several rules try to style the same thing.","memory":"Origin/Importance, Layers, Specificity, Source — 'Old Lions Sit Still'.","full":"<p>The cascade resolves conflicting declarations through an ordered series of tie-breakers: <strong>origin &amp; importance</strong> (UA / user / author, with <code>!important</code> reversing the origin order), then <strong>cascade layers</strong>, then <strong>specificity</strong>, then <strong>source order</strong>. The first stage to break the tie wins.</p><p>Importance and layers are sorted <em>before</em> specificity, so a low-specificity rule in a higher-priority layer or origin can beat a high-specificity one — which is why <code>@layer</code> reduces the need for specificity hacks. Running CSS <strong>animations</strong> override normal rules but lose to <code>!important</code>; running <strong>transitions</strong> override everything.</p>","trap":"Thinking specificity is the first thing checked. Origin/importance and layer ordering are resolved before specificity ever matters.","worked":"Author !important beats author normal; but UA !important beats author !important because importance reverses the origin order. A transition beats even UA !important.","related":["specificity","cascade-layers","custom-properties"]},
  "specificity": {"term":"Specificity","group":"FE-CSSARCH","eli5":"A score for how 'targeted' a selector is, used to break style ties.","memory":"IDs, Classes, Types = (a,b,c) — columns never carry.","full":"<p>Specificity is a three-part tuple <strong>(a=IDs, b=classes/attributes/pseudo-classes, c=types/pseudo-elements)</strong> compared left-to-right. Each column is independent and does not overflow into the next, so one ID always beats any number of classes.</p><p><code>*</code> and combinators count zero; <code>:where()</code> forces zero; <code>:is()</code>/<code>:not()</code>/<code>:has()</code> take their most specific argument. Inline styles outrank selectors, and <code>!important</code> is a separate cascade dimension entirely.</p>","trap":"Treating it as base-10 (e.g. thinking 11 classes beat 1 ID). Columns are compared independently and never carry.","worked":"#nav .link (1,1,0) beats .header .nav ul li a.link (0,2,4) because the first column (1 vs 0) decides it.","related":["the-cascade","is-where","not-selector"]},
  "cascade-layers": {"term":"Cascade Layers (@layer)","group":"FE-CSSARCH","eli5":"Named priority buckets that let you decide which group of styles wins, regardless of selector strength.","memory":"Declare order = priority; unlayered wins; !important reverses.","full":"<p><code>@layer reset, base, components, utilities;</code> establishes precedence bands sorted <em>above</em> specificity. Later-declared layers win for normal declarations, letting you tame third-party CSS (<code>@import url(x) layer(vendor)</code>) and retire <code>!important</code> wars.</p><p>Two twists: <strong>unlayered normal styles beat all layered ones</strong>, and for <code>!important</code> declarations the entire layer order <strong>reverses</strong> (and important unlayered styles sit below important layered ones).</p>","trap":"Assuming a higher-specificity rule in an early layer beats a low-specificity rule in a later layer. Layer order is checked before specificity.","worked":"@layer base { a { color: red !important } } @layer theme { a { color: blue !important } } → red wins, because !important reverses layer order.","related":["the-cascade","specificity","css-in-js-runtime"]},
  "custom-properties": {"term":"Custom Properties (--var)","group":"FE-CSSARCH","eli5":"Live, inheriting CSS variables you can read, change, and theme with at runtime.","memory":"Dashes are dynamic; dollars are dead-at-compile.","full":"<p>Custom properties (<code>--x</code>, read via <code>var(--x, fallback)</code>) are real runtime values that <strong>cascade and inherit</strong>, are scriptable from JS, and resolve at computed-value time. They power theming, scoped overrides, and reactive design tokens.</p><p>Unlike preprocessor variables (<code>$x</code>), which are substituted away at build time, custom properties exist in the live DOM and respond to media/container queries and <code>:hover</code> state. <code>@property</code> can register a type, an initial value, and inheritance so invalid resolutions degrade cleanly and the value can animate.</p>","trap":"Expecting var()'s fallback to catch a *defined-but-invalid* value. It only applies when the property is unset; a bad resolved value triggers invalid-at-computed-value-time instead.","worked":":root { --gap: 1rem } .tight { --gap: .25rem } .box { gap: var(--gap, 0) } — .tight scopes a smaller gap by inheritance.","related":["var-fallback","container-queries","oklch"]},
  "var-fallback": {"term":"var() Fallback & Invalid-at-Computed-Value-Time","group":"FE-CSSARCH","eli5":"The backup value var() uses only when the variable isn't set — not a type checker.","memory":"Unset → fallback; set-but-garbage → IACVT.","full":"<p><code>var(--x, default)</code> supplies <code>default</code> only when <code>--x</code> is missing or the guaranteed-invalid value. If <code>--x</code> exists but resolves to something invalid for the property, the fallback is ignored and the property hits <strong>invalid at computed-value time (IACVT)</strong> — it becomes the <code>inherited</code> value (inherited props) or <code>initial</code> value (non-inherited).</p><p>Register with <code>@property</code> to give a real type and initial value so bad inputs fall back to that initial value (not IACVT) and the property can animate.</p>","trap":"Believing the fallback protects against malformed values. It doesn't — only @property's initial-value provides that safety net.","worked":".x { --w: red; width: var(--w, 10px) } → width is invalid (IACVT) → resolves to initial 'auto', NOT 10px.","related":["custom-properties","oklch"]},
  "container-queries": {"term":"Container Queries (@container)","group":"FE-CSSARCH","eli5":"Lets a component style itself based on the size of its parent slot, not the whole window.","memory":"Query the container, not the viewport.","full":"<p>Marking an ancestor with <code>container-type: inline-size</code> (or <code>size</code>) makes it a query container; descendants then adapt via <code>@container (min-width: 30rem) { … }</code>. This unlocks genuinely reusable components that respond to their context — a card laid out one way in a sidebar and another in a grid.</p><p>Container query units (<code>cqw</code>, <code>cqh</code>, <code>cqi</code>, <code>cqb</code>, <code>cqmin</code>, <code>cqmax</code>) resolve against the nearest matching container. You can only query an <strong>ancestor</strong> container, never the element's own box.</p>","trap":"Forgetting that container-type: inline-size applies inline-size containment, which can collapse the container's width when it was previously sized by its content.","worked":".sidebar { container-type: inline-size } .card { display: grid } @container (min-width: 400px) { .card { grid-template-columns: 1fr 2fr } }","related":["media-queries","logical-properties","custom-properties"]},
  "media-queries": {"term":"Media Queries & Mobile-First","group":"FE-CSSARCH","eli5":"Rules that apply only at certain viewport sizes or user settings, written smallest-screen-first.","memory":"min-width builds up; max-width tears down.","full":"<p>Media queries gate styles on viewport dimensions (<code>@media (width &gt;= 48rem)</code>) and user preferences (<code>prefers-reduced-motion</code>, <code>prefers-color-scheme</code>). <strong>Mobile-first</strong> sets base styles for the smallest screen and enhances upward with <code>min-width</code>, shipping the lightest defaults and cascading naturally.</p><p>Modern <strong>range syntax</strong> (<code>&gt;=</code>, <code>&lt;</code>) replaces min-/max- pairs and removes the 0.02px overlap workaround. Use <code>rem</code> breakpoints so they honor the user's root font size.</p>","trap":"Mixing overlapping min- and max-width breakpoints with the same boundary value, causing both to apply at the exact px and double-styling.","worked":".grid { grid-template-columns: 1fr } @media (width >= 48rem) { .grid { grid-template-columns: repeat(3, 1fr) } }","related":["container-queries","reduced-motion","logical-properties"]},
  "logical-properties": {"term":"Logical Properties","group":"FE-CSSARCH","eli5":"Direction-aware spacing/sizing that automatically flips for RTL and vertical text.","memory":"inline = text axis, block = line-stacking axis.","full":"<p>Logical properties (<code>margin-inline-start</code>, <code>padding-block</code>, <code>inset-inline-end</code>, <code>inline-size</code>) map to the writing-mode flow rather than physical sides. <strong>Inline</strong> follows text direction; <strong>block</strong> follows line stacking.</p><p>They eliminate <code>[dir=rtl]</code> override stylesheets: a single <code>margin-inline-start</code> is correct in LTR, RTL, and vertical writing modes automatically — essential for internationalized UIs.</p>","trap":"Hard-coding margin-left/right on directional UI (icons, badges) and discovering it breaks in Arabic/Hebrew layouts.","worked":".icon { margin-inline-end: .5rem } — sits to the right in LTR, automatically to the left in RTL.","related":["media-queries","container-queries"]},
  "is-where": {"term":":is() vs :where()","group":"FE-CSSARCH","eli5":"Two grouping pseudo-classes — :is() keeps specificity, :where() zeroes it.","memory":"WHERE = zero (W for weightless).","full":"<p>Both accept a forgiving selector list (an unsupported selector is dropped, not fatal). The difference is specificity: <code>:is()</code> takes the <strong>highest specificity in its list</strong>, while <code>:where()</code> always contributes <strong>(0,0,0)</strong>.</p><p><code>:where()</code> is ideal for design-system base styles that should be trivially overridable; <code>:is()</code> is for terse grouping where you still want normal weight. Native CSS nesting wraps the substituted parent in <code>:is()</code>, which is why nesting can raise specificity.</p>","trap":"Using :is(#id, .x) for grouping and being surprised it inherits the ID's (1,0,0) — it takes the *most* specific arg, not an average.","worked":":where(h1, h2, h3) { margin: 0 } — easily overridden later because it adds zero specificity.","related":["specificity","has-selector"]},
  "has-selector": {"term":":has() Relational Selector","group":"FE-CSSARCH","eli5":"Lets a parent style itself based on what's inside (or next to) it — the long-awaited 'parent selector'.","memory":"Element :has(child) → style the parent.","full":"<p><code>:has()</code> selects an element based on its descendants or following siblings: <code>.card:has(img)</code>, <code>form:has(:invalid)</code>, <code>h2:has(+ p)</code>. It replaces patterns that previously required JavaScript, enabling state-driven and structure-driven styling in pure CSS.</p><p>Its specificity follows the <code>:is()</code> rule — it takes the most specific argument. Unlike <code>:is()</code>/<code>:where()</code>, the argument list is <em>not</em> forgiving: an unsupported selector invalidates the whole <code>:has()</code>.</p>","trap":"Assuming :has() is just a child check — it's a full relational query (siblings, deep descendants) and its specificity comes from its argument, not zero.","worked":".gallery:has(> :nth-child(4)) { gap: 2rem } — only widens gap when the gallery has 4+ items.","related":["is-where","specificity"]},
  "not-selector": {"term":":not() Specificity & List Semantics","group":"FE-CSSARCH","eli5":"Negation pseudo-class that surprisingly carries the specificity of its heaviest argument.","memory":":not adds weight; :not(.a,.b) = :not(.a):not(.b).","full":"<p><code>:not()</code> contributes the specificity of its <strong>most specific argument</strong> (like <code>:is()</code>/<code>:has()</code>), not zero — <code>:not(#id)</code> adds <code>(1,0,0)</code>, a common source of accidentally-heavy selectors.</p><p>The modern list form <code>:not(.a, .b)</code> is equivalent to <code>:not(.a):not(.b)</code> — negation distributes, so it <em>excludes</em> elements matching <em>any</em> listed selector. The list form is forgiving (bad args dropped). Wrap in <code>:where(:not(.x))</code> to negate without adding weight.</p>","trap":"Expecting :not() to add zero specificity, or reading :not(.a, .b) as 'not both' — it's 'not either'.","worked":"li:not(.active, .disabled) { color: gray } targets every li that is neither active nor disabled.","related":["specificity","is-where","has-selector"]},
  "compositor-props": {"term":"Compositor-Only Properties","group":"FE-CSSARCH","eli5":"transform, opacity and filter can animate on the compositor thread without re-doing layout or paint, so they stay smooth.","memory":"Animate transform/opacity/filter — everything else janks.","full":"<p>The browser rendering pipeline is layout → paint → composite. <strong>Animating <code>transform</code>, <code>opacity</code>, or <code>filter</code></strong> can run on the compositor thread, skipping layout and paint, which keeps animations at ~60fps even when the main thread is busy.</p><p>Animating geometric/paint properties (<code>width</code>, <code>top</code>, <code>margin</code>, <code>box-shadow</code>) forces layout and/or paint <em>every frame</em>, causing jank. The fix: translate with <code>transform: translateX()</code> instead of animating <code>left</code>, and fade with <code>opacity</code> instead of <code>visibility</code>/color.</p>","trap":"Animating 'left'/'top' for movement — it re-runs layout each frame; transform is the compositor-friendly choice.","worked":"/* janky */ @keyframes a { to { left: 100px } } /* smooth */ @keyframes b { to { transform: translateX(100px) } }","related":["will-change","reduced-motion"]},
  "will-change": {"term":"will-change","group":"FE-CSSARCH","eli5":"A hint telling the browser to prep an element for animation by promoting it to its own layer early.","memory":"Hint, don't hoard — add late, remove after.","full":"<p><code>will-change: transform</code> promotes an element to a dedicated compositor layer ahead of time so the first animation frame isn't stalled by layer creation. It's a performance <em>hint</em>, applied surgically.</p><p>Overusing it backfires: each promoted layer consumes GPU memory, so blanket application degrades performance and can exhaust memory. It also creates a stacking context and a containing block for <code>position: fixed</code> descendants. Best practice: add it just before the animation (e.g. on hover or via JS) and remove it when done.</p>","trap":"Putting will-change: transform on a base rule for hundreds of elements — it costs memory and can make things slower, not faster.","worked":".btn:hover { will-change: transform } /* then JS removes it after the transition ends */","related":["compositor-props"]},
  "css-in-js-runtime": {"term":"CSS-in-JS Runtime Cost","group":"FE-CSSARCH","eli5":"Runtime CSS-in-JS libraries build and inject styles while the page renders, which adds overhead and clashes with Server Components.","memory":"Runtime injects on render; compile-time ships static CSS.","full":"<p>Runtime CSS-in-JS (styled-components, Emotion) serializes styles and injects <code>&lt;style&gt;</code> tags <em>during render</em>, adding per-render cost, bundle weight, and hydration complexity. It also doesn't run in <strong>React Server Components</strong>, whose output is generated on the server without the client-side style runtime.</p><p>The 2026 direction is <strong>zero-runtime</strong>: CSS Modules, Tailwind (Oxide engine + Lightning CSS), or compile-time CSS-in-JS (vanilla-extract, Panda, Linaria) that extract static <code>.css</code> at build time — keeping the colocation/typing benefits while moving the cost out of the render path.</p>","trap":"Choosing styled-components for a Next.js App Router project and hitting Server Component incompatibility plus render-time overhead.","worked":"vanilla-extract: styles authored in TS are extracted to a static .css file at build, so no runtime style engine ships to the client.","related":["cascade-layers","custom-properties"]},
  "reduced-motion": {"term":"prefers-reduced-motion","group":"FE-CSSARCH","eli5":"A media query that respects users who asked their OS to minimize animation.","memory":"Reduce, don't remove — swap big motion for a fade.","full":"<p><code>@media (prefers-reduced-motion: reduce)</code> reflects an OS accessibility setting for users sensitive to motion (vestibular disorders). The goal is to <em>reduce</em>, not blanket-disable: replace large parallax/slide/zoom with subtle cross-fades, and stop autoplay/auto-scroll.</p><p>A robust pattern is to <strong>opt in</strong> to motion: define animations only inside <code>@media (prefers-reduced-motion: no-preference)</code>, so the safe (still) version is the default. Honoring this is a WCAG expectation.</p>","trap":"Treating it as 'turn off all animation' and removing essential motion cues, or ignoring it entirely as a nicety.","worked":"@media (prefers-reduced-motion: no-preference) { .card { transition: transform .3s } }","related":["compositor-props","media-queries"]},
  "oklch": {"term":"oklch() Color","group":"FE-CSSARCH","eli5":"A perceptually uniform, wide-gamut color space where equal lightness looks equally bright across hues.","memory":"OKLCH = Okay, Lightness Chroma Hue — uniform & wide.","full":"<p><code>oklch(L C H)</code> expresses color as perceptual <strong>Lightness</strong>, <strong>Chroma</strong>, and <strong>Hue</strong>. Unlike <code>hsl()</code>, it's perceptually uniform — equal L values look equally bright across hues — so generated palettes, tints/shades, and accessible contrast ramps behave predictably.</p><p>It's also wide-gamut, reaching vivid <strong>P3</strong> colors that sRGB hex/hsl can't represent. Combined with <code>color-mix(in oklch, …)</code>, it's the basis for modern programmatic theming where you nudge a single channel (e.g. L for hover).</p>","trap":"Generating a palette in HSL and assuming equal lightness = equal perceived brightness — yellow at 50% L reads far brighter than blue.","worked":"--brand: oklch(60% 0.15 250); --brand-hover: oklch(55% 0.15 250) — darken by lowering only L, hue/chroma untouched.","related":["custom-properties","var-fallback"]}
});
