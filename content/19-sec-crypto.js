/* Cryptography & TLS (SEC-CRYPTO) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"SEC-CRYPTO","front":"Symmetric vs asymmetric encryption — what's the real tradeoff?","back":"<strong>Symmetric</strong> (e.g. <code>AES</code>) uses one shared secret key for both encrypt and decrypt — fast (GB/s with AES-NI hardware) but you must distribute the key secretly. <strong>Asymmetric</strong> (e.g. <code>RSA</code>, <code>ECC</code>) uses a public/private keypair — solves key distribution and enables signatures, but is orders of magnitude slower. In practice you use asymmetric to <em>establish</em> a symmetric key, then symmetric for the bulk data."},
  {"sec":"SEC-CRYPTO","front":"Why does TLS use BOTH asymmetric and symmetric crypto?","back":"Asymmetric crypto solves key distribution (no pre-shared secret) but is far too slow for bulk traffic. So TLS uses asymmetric crypto only during the handshake to <strong>agree on a fresh symmetric session key</strong> (via ephemeral Diffie-Hellman), then switches to fast symmetric encryption (<code>AES-GCM</code>, <code>ChaCha20-Poly1305</code>) for all application data. Best of both: secure key exchange + fast bulk encryption."},
  {"sec":"SEC-CRYPTO","front":"Hashing vs encryption vs encoding — what's each FOR?","back":"<ul><li><strong>Hashing</strong> (SHA-256): one-way, fixed-size digest. Goal = <em>integrity</em> / fingerprinting. Cannot be reversed.</li><li><strong>Encryption</strong> (AES, RSA): two-way, reversible with a key. Goal = <em>confidentiality</em>.</li><li><strong>Encoding</strong> (base64, URL-encoding): reversible with NO key. Goal = <em>data representation/transport</em>, NOT security.</li></ul>The classic interview trap: base64 is encoding, not encryption — anyone can decode it."},
  {"sec":"SEC-CRYPTO","front":"How does a digital signature work, and what three properties does it give?","back":"The signer hashes the message and signs the digest <strong>with their private key</strong>; verifiers check it with the signer's public key against a fresh hash of the message. This provides <strong>authenticity</strong> (only the private-key holder could sign), <strong>integrity</strong> (tampering breaks the hash match), and <strong>non-repudiation</strong> (the signer can't later deny it). Note the key roles are reversed vs encryption: you sign with the <em>private</em> key, verify with the <em>public</em> key. (Only textbook RSA literally 'encrypts the hash' — RSA-PSS, ECDSA, and EdDSA are distinct signing operations, not encryption.)"},
  {"sec":"SEC-CRYPTO","front":"What is a certificate authority (CA) and the chain of trust?","back":"A <strong>CA</strong> is a trusted third party that signs certificates binding a public key to an identity (e.g. a domain). Your OS/browser ships a set of <strong>trusted root CAs</strong>. A server presents a leaf cert signed by an intermediate, signed by a root. The client walks this <strong>chain of trust</strong> up to a root it already trusts, verifying each signature. No pre-existing trust in the server is needed — only in the root."},
  {"sec":"SEC-CRYPTO","front":"Why is HMAC used instead of a plain hash for message authentication?","back":"A plain hash gives integrity but not authenticity — anyone can recompute <code>SHA-256(message)</code> after tampering. <strong>HMAC</strong> mixes a shared secret key into the hash (<code>HMAC(key, message)</code>), so only parties holding the key can produce or verify the tag. Its nested construction also resists the length-extension attacks that affect naive <code>hash(secret || message)</code> over Merkle–Damgård hashes like SHA-256. Use HMAC for API request signing and webhook verification."},
  {"sec":"SEC-CRYPTO","front":"Encryption at-rest vs in-transit — and why you need both","back":"<strong>In-transit</strong> = TLS on the wire, protecting data moving between services/clients. <strong>At-rest</strong> = encrypting stored data (disk, S3 buckets, DB volumes, backups), often via envelope encryption with a KMS. They defend different threats: in-transit stops network sniffing/MITM; at-rest stops a stolen disk or leaked backup. Neither covers <em>in-use</em> (data decrypted in memory) — that's the domain of confidential computing / enclaves."},
  {"sec":"SEC-CRYPTO","front":"What is envelope encryption and why use it?","back":"Instead of encrypting data directly with a master key, you encrypt each chunk with a per-object <strong>data encryption key (DEK)</strong>, then encrypt the DEK with a <strong>key encryption key (KEK)</strong> held in a KMS/HSM. You store the encrypted DEK next to the ciphertext. Benefits: the KEK never leaves the KMS, you can rotate the KEK without re-encrypting petabytes (just re-wrap DEKs), and you limit blast radius per object."},
  {"sec":"SEC-CRYPTO","front":"Why is key rotation hard, and what does 'rotation' actually mean?","back":"Rotation means retiring an old key and using a new one going forward, while keeping the old key available to <em>decrypt</em> existing ciphertext. The hard part isn't generating a new key — it's tracking which data was encrypted under which key version, re-wrapping/re-encrypting at scale, coordinating rotation across distributed consumers without downtime, and ensuring no consumer pins a now-deleted key. Envelope encryption makes KEK rotation cheap (re-wrap DEKs, not data)."},
  {"sec":"SEC-CRYPTO","front":"Forward secrecy (PFS) — what is it and why does it matter?","back":"<strong>Forward secrecy</strong> means a compromise of the server's long-term private key does NOT let an attacker decrypt past recorded sessions. It's achieved by using <strong>ephemeral Diffie-Hellman</strong> (<code>ECDHE</code>) to derive a unique session key per connection that's discarded afterward — the long-term key only <em>authenticates</em> the handshake, it never encrypts data. TLS 1.3 mandates PFS for the main handshake; static-RSA key exchange (no PFS) was removed."},
  {"sec":"SEC-CRYPTO","front":"What changed in TLS 1.3 vs 1.2 that matters operationally?","back":"<ul><li><strong>1-RTT handshake</strong>, plus optional <strong>0-RTT</strong> resumption — faster connections.</li><li><strong>Forward secrecy mandatory</strong> — static RSA key exchange removed.</li><li>Legacy/weak ciphers cut (no RC4, CBC-mode, MD5, SHA-1, static DH).</li><li>Only AEAD ciphers (<code>AES-GCM</code>, <code>ChaCha20-Poly1305</code>).</li></ul>Operationally: faster, more secure by default — but old clients/middleboxes that don't speak 1.3 can break, and <strong>0-RTT early data is the exception to PFS: it's replayable and not forward-secret</strong>, so use it only for idempotent requests."},
  {"sec":"SEC-CRYPTO","front":"Why must you NEVER roll your own crypto?","back":"Correct cryptographic <em>primitives</em> are the easy part; the failures are in the details — constant-time comparison (timing attacks), nonce/IV reuse (catastrophic for GCM/CTR), padding oracles, weak RNG, and side channels. These bugs are invisible in tests and only found by adversaries. Use vetted, misuse-resistant libraries (libsodium, the platform's TLS stack, a cloud KMS) and let them handle modes, padding, and key handling."},
  {"sec":"SEC-CRYPTO","front":"What is an AEAD cipher and what problem does it solve?","back":"<strong>AEAD</strong> (Authenticated Encryption with Associated Data) — e.g. <code>AES-GCM</code>, <code>ChaCha20-Poly1305</code> — combines confidentiality and integrity in one primitive: it encrypts and produces an authentication tag in a single step. This avoids the error-prone 'encrypt-then-MAC' composition and the padding-oracle attacks of older CBC modes. The catch: AEAD modes are <strong>fatal on nonce reuse</strong> — never reuse a nonce/IV under the same key."},
  {"sec":"SEC-CRYPTO","front":"Why hash passwords with bcrypt/scrypt/Argon2 instead of SHA-256?","back":"SHA-256 is designed to be <em>fast</em>, which lets attackers try billions of guesses per second against a stolen hash. Password hashing needs to be <strong>deliberately slow and memory-hard</strong>: <code>bcrypt</code>, <code>scrypt</code>, and <code>Argon2</code> have a tunable work factor and a built-in per-password salt, so each guess is expensive and rainbow tables are useless. Argon2id is the current OWASP-recommended default (bcrypt remains acceptable where Argon2 isn't available)."},
  {"sec":"SEC-CRYPTO","front":"What is mTLS and where does a data platform use it?","back":"<strong>Mutual TLS</strong> means BOTH sides present and verify certificates — the client authenticates to the server <em>and</em> the server to the client, instead of only the server proving identity. In a data platform it's the backbone of service-to-service authn (service mesh like Istio/Linkerd issues short-lived workload certs, e.g. SPIFFE/SPIRE), replacing shared API keys with cryptographic workload identity and enabling zero-trust networking."},
  {"sec":"SEC-CRYPTO","front":"How much data can RSA actually encrypt directly, and what does that imply?","back":"RSA can only encrypt a payload smaller than its modulus minus padding: <strong>RSA-2048 with OAEP/SHA-256 tops out at ~190 bytes per operation</strong> (256-byte block minus ~66 bytes of padding). That's far too small for real messages, and RSA is also ~100–1000x slower than AES — which is exactly why asymmetric crypto is used to <em>wrap a symmetric key</em>, never to encrypt bulk data directly."}
);

Object.assign(GLOSSARY_DATA, {
  "symmetric-vs-asymmetric": {"term":"Symmetric vs Asymmetric Encryption","group":"SEC-CRYPTO","aliases":["symmetric encryption","asymmetric encryption","public key cryptography","secret key crypto","aes vs rsa"],"eli5":"Symmetric uses one shared key (fast); asymmetric uses a public/private pair (slow) that fixes how you share that key in the first place.","memory":"Symmetric = one key, Symple and Speedy. Asymmetric = Two keys, A-pair.","full":"<p><strong>Symmetric</strong> ciphers like <code>AES</code> use a single secret key for both directions. They're extremely fast (GB/s with AES-NI), but both parties must already share the key — the unsolved 'how do you deliver the key over an untrusted channel' problem. <strong>Asymmetric</strong> (public-key) crypto like <code>RSA</code> and <code>ECC</code> uses a mathematically linked keypair: anything encrypted with the public key only the private key can decrypt, and a signature made with the private key anyone can verify with the public key.</p><p>Asymmetric is ~100-1000x slower, so it's never used for bulk data. The universal pattern: use asymmetric to authenticate and to agree on a fresh symmetric session key, then encrypt all the actual data symmetrically. This is exactly what TLS does.</p>","trap":"Candidates say 'RSA encrypts the data.' In TLS, RSA/ECDHE only protects the key exchange — the data is encrypted with a symmetric session key. Asymmetric crypto is too slow to encrypt payloads directly.","worked":"AES-256-GCM can encrypt 1 GB in well under a second on a laptop; RSA-2048 maxes out at a few thousand operations/sec and (with OAEP/SHA-256) can only encrypt ~190 bytes per operation — which is why it's used to wrap keys, not data.","related":["tls-handshake","digital-signature","forward-secrecy","key-management"]},
  "hashing-vs-encryption-vs-encoding": {"term":"Hashing vs Encryption vs Encoding","group":"SEC-CRYPTO","aliases":["hashing","sha-256","encoding","base64","one-way function","message digest"],"eli5":"Hashing is a one-way fingerprint, encryption is a locked box you can reopen with a key, and encoding is just a different way of writing the same data with no secret at all.","memory":"Hash = Heads-only (one way). Encrypt = has a kEy. Encode = no kEy, just a costume change.","full":"<p><strong>Hashing</strong> (e.g. <code>SHA-256</code>) is a deterministic one-way function producing a fixed-size digest; you can't reverse it, and its job is <em>integrity</em> and fingerprinting. <strong>Encryption</strong> is reversible <em>with a key</em> and provides <em>confidentiality</em>. <strong>Encoding</strong> (base64, hex, URL-encoding) is reversible with <em>no key</em> — it exists to safely represent or transport bytes, and offers zero security.</p><p>Good cryptographic hashes have the avalanche property (one bit flips ~half the output), preimage resistance, and collision resistance. <code>MD5</code> and <code>SHA-1</code> are broken for collision resistance and must not be used for signatures or integrity-against-adversaries.</p>","trap":"'I base64-encoded the token so it's secure.' Base64 is encoding, fully reversible by anyone — it's not security. Equally: never use plain SHA-256 for passwords (too fast).","worked":"A webhook sends a payload plus <code>X-Signature: HMAC-SHA256(secret, body)</code>. The receiver recomputes the HMAC to verify integrity+authenticity. Base64 is only used to wrap the binary signature into an HTTP header — that part is encoding, not the security.","related":["digital-signature","symmetric-vs-asymmetric","password-hashing"]},
  "digital-signature": {"term":"Digital Signature","group":"SEC-CRYPTO","aliases":["digital signature","signing","sign and verify","non-repudiation"],"eli5":"You sign a message with your private key so anyone with your public key can confirm it really came from you and wasn't altered.","memory":"Sign with the key you keep PRIVATE; verify with the key you make PUBLIC. (Encryption is the mirror image.)","full":"<p>To sign, you hash the message and transform that digest with your <strong>private</strong> key; to verify, anyone uses your <strong>public</strong> key plus a fresh hash of the message. Note the inversion from encryption: signing uses the private key, verification the public key. A valid signature proves three things — <strong>authenticity</strong> (only the private-key holder could produce it), <strong>integrity</strong> (any tampering breaks the digest match), and <strong>non-repudiation</strong> (the signer can't credibly deny it).</p><p>Signatures underpin TLS certificates (the CA signs the cert), code signing, JWTs (RS256/ES256), and git commit signing. Because a hash is signed rather than the whole message, signing is cheap regardless of message size. Only textbook RSA is literally 'encrypt the hash with the private key' — RSA-PSS, ECDSA, and EdDSA are dedicated signing algorithms, not encryption run backwards.</p>","trap":"Symmetric MACs/HMAC give integrity+authenticity but NOT non-repudiation — both parties share the key, so either could have produced the tag. Only asymmetric signatures give non-repudiation.","worked":"A JWT signed with ES256: the auth server signs the header.payload with its private key; every downstream service verifies with the public key (from a JWKS endpoint), so no service needs the secret and the issuer can't deny issuing the token.","related":["symmetric-vs-asymmetric","pki-chain-of-trust","hashing-vs-encryption-vs-encoding"]},
  "pki-chain-of-trust": {"term":"PKI, Certificates & Chain of Trust","group":"SEC-CRYPTO","aliases":["pki","certificate authority","ca","chain of trust","x509","root ca","intermediate ca"],"eli5":"Your browser trusts a small set of root authorities, and every website cert traces back to one of them through a chain of signatures.","memory":"Leaf → Intermediate → Root. You only need to trust the Root; the chain vouches for the rest.","full":"<p><strong>PKI</strong> (Public Key Infrastructure) binds public keys to identities via <strong>X.509 certificates</strong> signed by a <strong>Certificate Authority</strong>. Trust is anchored in a set of <strong>root CAs</strong> pre-installed in your OS/browser. A server typically presents a leaf certificate signed by an intermediate CA, which is signed by a root. The client validates each signature up the chain until it reaches a root it already trusts, and also checks the hostname, validity dates, and revocation (CRL/OCSP).</p><p>Compromise or mis-issuance by any CA in the chain undermines trust, which is why mechanisms like Certificate Transparency logs, pinning, and short-lived certs (ACME/Let's Encrypt) exist. Internally, data platforms often run a private CA to issue workload certs for mTLS.</p>","trap":"'Self-signed cert errors' aren't about encryption strength — the encryption is fine; the client just can't validate the chain to a trusted root. Adding the cert to the trust store, not disabling verification, is the fix.","worked":"<code>openssl s_client -connect host:443 -showcerts</code> dumps the leaf + intermediates; the browser appends a trusted root to complete the path. A missing intermediate causes intermittent 'untrusted' errors on clients lacking it cached.","related":["tls-handshake","digital-signature"]},
  "key-management": {"term":"Key Management & Rotation","group":"SEC-CRYPTO","aliases":["key management","kms","key rotation","envelope encryption","dek","kek","hsm"],"eli5":"The crypto math is the easy part — the hard part is storing, rotating, and never leaking the keys, ideally so the master key never leaves a vault.","memory":"DEK encrypts data, KEK encrypts the DEK, KMS guards the KEK. Rotate the KEK = re-wrap, not re-encrypt.","full":"<p>Keys are the crown jewels: leak the key and the encryption is worthless. <strong>Envelope encryption</strong> is the standard pattern — encrypt data with a per-object <strong>data encryption key (DEK)</strong>, then encrypt the DEK with a <strong>key encryption key (KEK)</strong> that lives in a KMS/HSM and never leaves it. You store the wrapped DEK beside the ciphertext.</p><p><strong>Rotation</strong> means moving to a new key version going forward while retaining old versions to decrypt existing data. The difficulty is operational: tracking key-version-per-object, re-wrapping at scale, coordinating across distributed consumers, and never deleting a key still needed for decryption. Envelope encryption makes KEK rotation cheap (re-wrap DEKs, not re-encrypt the data). Use a managed KMS (AWS KMS, GCP KMS, Vault) — don't hand-roll key storage.</p>","trap":"'We rotate keys, but how do you decrypt old data?' If you rotate without versioning and retaining prior keys, you've destroyed your own data. Rotation ≠ delete-the-old-key.","worked":"S3 SSE-KMS: each object gets a unique DEK wrapped by your KMS KEK. Rotating the KEK re-wraps existing DEKs without touching the petabytes of object data; you only re-encrypt the small wrapped keys.","related":["symmetric-vs-asymmetric","tls-handshake"]},
  "forward-secrecy": {"term":"Forward Secrecy (PFS)","group":"SEC-CRYPTO","aliases":["forward secrecy","perfect forward secrecy","pfs","ephemeral diffie-hellman","ecdhe"],"eli5":"Even if someone steals the server's long-term private key tomorrow, they still can't decrypt the traffic they recorded today.","memory":"Ephemeral = throwaway-per-session. Steal the master key later, the thrown-away session keys are already gone.","full":"<p><strong>Forward secrecy</strong> guarantees that compromising a long-term private key does not retroactively expose past sessions. It's achieved with <strong>ephemeral Diffie-Hellman</strong> (<code>ECDHE</code>): each connection derives a unique session key from short-lived DH values that are discarded after the handshake. The long-term certificate key only <em>authenticates</em> the exchange; it never encrypts data and isn't enough to reconstruct any session key.</p><p>Without PFS (e.g. legacy static-RSA key exchange), an attacker who records ciphertext and later steals the private key can decrypt everything historically. TLS 1.3 makes PFS mandatory for the main handshake by removing non-ephemeral key exchange — but 0-RTT early data is exempt: it rides on a resumption secret and is not forward-secret.</p>","trap":"PFS protects against FUTURE key compromise of recorded traffic — it does NOT protect a session whose key is captured live, only applies if the DH params are truly ephemeral (per-connection), and does NOT cover TLS 1.3 0-RTT early data.","worked":"An adversary passively records your TLS traffic for a year, then subpoenas the server's private key. With static RSA they decrypt the whole year; with ECDHE (PFS) they get nothing, because each day's session key was ephemeral and never stored.","related":["tls-handshake","symmetric-vs-asymmetric","key-management"]},
  "aead-cipher": {"term":"AEAD Ciphers & Nonce Reuse","group":"SEC-CRYPTO","aliases":["aead","authenticated encryption","aes-gcm","chacha20-poly1305","nonce","iv reuse"],"eli5":"AEAD encrypts and authenticates in one step, but reusing the same nonce with the same key is catastrophic.","memory":"AEAD = Add Encryption And a Detector (tag) in one. Nonce = use it oNce, literally.","full":"<p><strong>AEAD</strong> (Authenticated Encryption with Associated Data) ciphers like <code>AES-GCM</code> and <code>ChaCha20-Poly1305</code> provide confidentiality <em>and</em> integrity in a single primitive, producing ciphertext plus an authentication tag. This replaced the fragile, easy-to-misorder 'encrypt-then-MAC' composition and eliminated the padding-oracle attacks that plagued CBC mode.</p><p>The critical operational rule: AEAD modes require a <strong>unique nonce/IV per message under a given key</strong>. Reusing a nonce in GCM is catastrophic — it leaks the XOR of the two plaintexts and lets an attacker recover GCM's authentication key (the GHASH key <code>H</code>), enabling universal tag forgery (it does not, however, recover the AES key itself). This is the #1 way teams that 'roll their own' break AEAD. Use a counter or random 96-bit nonce with rekeying limits.</p>","trap":"'We use AES-GCM so we're fine' — but if the nonce is fixed or derived deterministically across messages with the same key, GCM is completely broken. Nonce uniqueness is a hard requirement, not a nicety.","worked":"TLS 1.3 only allows AEAD ciphers and derives a per-record nonce by XOR-ing a sequence number into a base IV — guaranteeing uniqueness without sending the nonce, which is exactly the discipline a hand-rolled implementation forgets.","related":["tls-handshake","symmetric-vs-asymmetric","key-management"]}
});
