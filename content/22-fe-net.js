/* Networking & the Web Platform (FE-NET) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"FE-NET","front":"Safe vs idempotent HTTP methods — and why <code>POST</code> retries are dangerous","back":"<ul><li><strong>Safe</strong> = read-only, no intended server state mutation: <code>GET</code>, <code>HEAD</code>, <code>OPTIONS</code>, <code>TRACE</code>. Caches and prefetchers assume safe methods have no side effects.</li><li><strong>Idempotent</strong> = N identical requests have the same effect as one: <code>GET</code>, <code>HEAD</code>, <code>PUT</code>, <code>DELETE</code>, <code>OPTIONS</code>, <code>TRACE</code> (all safe methods plus <code>PUT</code>/<code>DELETE</code>).</li><li><code>POST</code> is <em>neither</em> safe nor idempotent — that's why a network retry can double-charge a card. The fix is an <strong>idempotency key</strong> the server dedupes on.</li><li><code>PATCH</code> is <em>not</em> guaranteed idempotent (a JSON-Patch <code>add</code> appends each time). <code>CONNECT</code> is neither safe nor idempotent. All safe methods are idempotent; the reverse is false (<code>PUT</code>/<code>DELETE</code> are idempotent but not safe).</li></ul>"},
  {"sec":"FE-NET","front":"301 vs 302 vs 307/308 vs 304 — redirect & cache status codes","back":"<ul><li><strong>301 Moved Permanently</strong>: cached aggressively, SEO link-equity transfers. Historically many clients rewrite the method to <code>GET</code> on follow.</li><li><strong>302 Found</strong> / <strong>303 See Other</strong>: temporary; 303 <em>mandates</em> a <code>GET</code> (the canonical POST-Redirect-GET pattern). Note 302 is <em>also</em> historically method-rewritten to GET by most browsers — which is exactly the ambiguity 307/308 fix.</li><li><strong>307/308</strong>: temporary/permanent that <em>preserve the method and body</em> — the modern, unambiguous choice. Use 308 to permanently move a POST endpoint without it silently becoming a GET.</li><li><strong>304 Not Modified</strong>: revalidation success — empty body, browser serves its cached copy. Returned when <code>If-None-Match</code>/<code>If-Modified-Since</code> match.</li></ul><em>Gotcha:</em> a 301 is sticky — a wrong one is cached by clients for a long time and is painful to undo."},
  {"sec":"FE-NET","front":"401 vs 403 vs 429 — and why the distinction matters for clients","back":"<ul><li><strong>401 Unauthorized</strong> = <em>unauthenticated</em> (misnamed). \"I don't know who you are.\" MUST carry a <code>WWW-Authenticate</code> header. Retrying with credentials may succeed.</li><li><strong>403 Forbidden</strong> = authenticated but <em>not allowed</em>. \"I know who you are; you can't do this.\" Re-auth won't help — don't prompt for a new login.</li><li><strong>429 Too Many Requests</strong> = rate-limited. SHOULD include <code>Retry-After</code> (delta-seconds or HTTP-date). Clients should back off exponentially with jitter.</li></ul><em>Senior tell:</em> some APIs deliberately return <strong>404</strong> instead of 403 to avoid leaking resource existence to unauthorized users."},
  {"sec":"FE-NET","front":"HTTP/1.1 head-of-line blocking & keep-alive vs HTTP/2 multiplexing vs HTTP/3 over QUIC","back":"<ul><li><strong>HTTP/1.1</strong>: one in-flight request per TCP connection (pipelining never shipped usefully). <code>Connection: keep-alive</code> reuses the socket but responses are serialized → <strong>application-layer HOL blocking</strong>. Browsers work around it with ~6 parallel connections per origin (hence domain-sharding hacks).</li><li><strong>HTTP/2</strong>: <em>multiplexes</em> many streams over one TCP connection with binary framing + HPACK header compression + server push (now deprecated/removed in major browsers). Solves <em>app-layer</em> HOL — but a single TCP packet loss stalls <em>all</em> streams (<strong>TCP-layer HOL blocking</strong>).</li><li><strong>HTTP/3</strong>: runs over <strong>QUIC on UDP</strong>. Streams are independent at the transport layer, so a lost packet only stalls its own stream. Also gives 0-RTT/1-RTT setup (TLS 1.3 baked into QUIC) and connection migration across IP changes via connection IDs.</li></ul>"},
  {"sec":"FE-NET","front":"<code>Cache-Control</code>: max-age vs no-cache vs no-store vs immutable","back":"<ul><li><code>max-age=N</code>: fresh for N seconds; served without hitting the network.</li><li><code>no-cache</code>: <em>may</em> store, but MUST revalidate with the origin (via ETag) before reuse. <strong>It does not mean \"don't cache.\"</strong></li><li><code>no-store</code>: never write to any cache — the real \"don't cache\" (use for sensitive data).</li><li><code>immutable</code>: promise the body will <em>never</em> change for its freshness lifetime, so the browser skips revalidation even on explicit reload. Pair with content-hashed filenames (<code>app.a1b2c3.js</code>) + a long <code>max-age=31536000</code>.</li><li><code>private</code> vs <code>public</code>: <code>private</code> blocks shared (CDN) caches; <code>s-maxage</code> overrides <code>max-age</code> for shared caches only.</li></ul>"},
  {"sec":"FE-NET","front":"ETag / If-None-Match revalidation and <code>stale-while-revalidate</code>","back":"<p>An <strong>ETag</strong> is an opaque content fingerprint the server sets. On revalidation the client sends <code>If-None-Match: \"etag\"</code>; the server returns <strong>304 Not Modified</strong> (empty body, saves bandwidth) or 200 with fresh content. <em>Strong</em> ETags guarantee byte-identity; <em>weak</em> (<code>W/</code>) only semantic equivalence (and can't be used for range requests).</p><ul><li><code>stale-while-revalidate=N</code>: serve the stale cached response <em>immediately</em> while asynchronously revalidating in the background — eliminates the latency cliff at expiry.</li><li><code>stale-if-error=N</code>: serve stale content if the origin returns 5xx / is unreachable.</li></ul><em>Gotcha:</em> ETags derived from inode/mtime differ per server in a cluster, breaking 304s behind a load balancer."},
  {"sec":"FE-NET","front":"CORS: simple vs preflighted requests, and what triggers the <code>OPTIONS</code> preflight","back":"<p>CORS is a <em>browser-enforced</em> relaxation of the same-origin policy; the server opts in via headers. A request is <strong>simple</strong> (no preflight) only if its method is <code>GET</code>/<code>HEAD</code>/<code>POST</code> AND it uses only CORS-safelisted headers AND its <code>Content-Type</code> is <code>application/x-www-form-urlencoded</code>, <code>multipart/form-data</code>, or <code>text/plain</code>.</p><ul><li>Anything else — custom headers like <code>X-*</code>, or <code>Content-Type: application/json</code> — triggers a <strong>preflight</strong>: an <code>OPTIONS</code> probe carrying <code>Access-Control-Request-Method/Headers</code>.</li><li>The server answers with <code>Access-Control-Allow-Origin/Methods/Headers</code> and <code>Access-Control-Max-Age</code> (caches the preflight result).</li></ul><em>Gotcha:</em> CORS does not protect the server — the request still <em>arrives and executes</em>; the browser just hides the <em>response</em> from JS.</p>"},
  {"sec":"FE-NET","front":"The CORS wildcard-with-credentials rule","back":"<p>You <strong>cannot</strong> combine <code>Access-Control-Allow-Origin: *</code> with credentials. If the request sends cookies/HTTP-auth (<code>credentials: 'include'</code> or <code>withCredentials</code>), the browser <em>blocks</em> the response unless:</p><ul><li><code>Access-Control-Allow-Origin</code> echoes the <em>exact</em> origin (not <code>*</code>), AND</li><li><code>Access-Control-Allow-Credentials: true</code> is present, AND</li><li><code>Vary: Origin</code> is set so caches don't serve the wrong origin's headers.</li></ul><p>Same applies to <code>Access-Control-Allow-Headers/Methods</code>: with credentials, <code>*</code> is treated as the <em>literal</em> header/method name <code>*</code>, not a wildcard. <em>Practical pattern:</em> maintain an allowlist server-side and reflect the matched origin.</p>"},
  {"sec":"FE-NET","front":"Cookie attributes: HttpOnly, Secure, SameSite (Lax/Strict/None), Domain, Path","back":"<ul><li><strong>HttpOnly</strong>: invisible to <code>document.cookie</code> — mitigates XSS token theft.</li><li><strong>Secure</strong>: only sent over HTTPS.</li><li><strong>SameSite=Lax</strong> (browser default): sent on top-level <em>navigations</em> (GET) but not cross-site subresource/XHR — blocks most CSRF. <strong>Strict</strong>: never on any cross-site request (breaks \"click a link, land logged-in\"). <strong>None</strong>: sent cross-site but <em>requires</em> <code>Secure</code>.</li><li><strong>Domain</strong>: omit it for a host-only cookie (most secure); setting <code>Domain=example.com</code> shares it with subdomains.</li><li><strong>Path</strong>: scopes by URL prefix (weak isolation — not a security boundary; any path can read another via XSS/iframes).</li></ul><em>2026 note:</em> third-party cookies are effectively dead in major browsers; partitioned cookies (<strong>CHIPS</strong>, <code>Partitioned</code> attribute) are the cross-site replacement."},
  {"sec":"FE-NET","front":"<code>fetch()</code> nuances: AbortSignal, credentials, mode, and what counts as an error","back":"<ul><li><strong>Cancellation:</strong> <code>fetch(url, { signal: AbortSignal.timeout(5000) })</code> or an <code>AbortController</code>; aborting rejects with an <code>AbortError</code>. <code>AbortSignal.any([...])</code> combines signals.</li><li><code>credentials</code>: <code>'omit'</code> | <code>'same-origin'</code> (default) | <code>'include'</code> (send cookies cross-origin).</li><li><code>mode</code>: <code>'cors'</code> (default) | <code>'no-cors'</code> (opaque, unreadable response) | <code>'same-origin'</code>.</li><li><strong>Gotcha:</strong> <code>fetch</code> only rejects on <em>network</em> failure or abort — a 404/500 still <em>resolves</em>. You must check <code>res.ok</code>/<code>res.status</code> yourself.</li></ul><em>Also:</em> the body stream can be read once — clone with <code>res.clone()</code> to read twice."},
  {"sec":"FE-NET","front":"REST vs GraphQL — over-fetching, under-fetching, and the real tradeoffs","back":"<ul><li><strong>Over-fetching</strong>: REST endpoint returns more fields than the UI needs. <strong>Under-fetching</strong>: the screen needs data from 3 endpoints → N+1 round trips (the waterfall).</li><li><strong>GraphQL</strong>: one endpoint, client declares the exact shape → solves both. Strongly typed schema + introspection.</li><li><strong>Costs:</strong> HTTP caching is harder (everything is a <code>POST</code> to <code>/graphql</code>; need persisted queries / a normalized client cache like Apollo/urql). Risk of expensive nested queries → need depth/complexity limits. Server-side <strong>N+1</strong> resolver problem solved with <strong>DataLoader</strong> batching.</li></ul><em>2026 lens:</em> REST + sparse-fieldset query params, tRPC, or React Server Components often beat GraphQL for first-party apps where you control both ends."},
  {"sec":"FE-NET","front":"WebSocket vs Server-Sent Events vs long-polling — when to pick which","back":"<ul><li><strong>Long-polling</strong>: client holds a request open until data arrives, then re-requests. Works everywhere; high overhead, latency gap on reconnect.</li><li><strong>SSE</strong> (<code>EventSource</code>): server→client only, over plain HTTP, <em>auto-reconnect</em> + <code>Last-Event-ID</code> resume built in. Great for notifications, live scores, LLM token streaming. Limited to ~6 connections per origin on HTTP/1.1 (a non-issue on HTTP/2+).</li><li><strong>WebSocket</strong>: full-duplex, low-overhead frames after an HTTP <code>Upgrade</code> handshake (101 Switching Protocols). Needed for chat, multiplayer, collaborative editing. But it's a separate protocol — you re-implement reconnect, auth, backpressure, and it doesn't ride HTTP caching/proxies as cleanly.</li></ul><em>Rule of thumb:</em> server→client streaming = SSE; true bidirectional = WebSocket."},
  {"sec":"FE-NET","front":"gzip vs Brotli — compression on the web","back":"<ul><li><strong>gzip</strong> (DEFLATE): universal, fast, decent ratios. Compress on the fly.</li><li><strong>Brotli</strong> (<code>br</code>): ~15-25% smaller than gzip on text/JS at high quality, thanks to a built-in dictionary of common web strings. Quality 1-11; <em>q11 is too slow for dynamic responses</em> — use it for <strong>precompressed static assets</strong> at build time, and a low-q Brotli or gzip for dynamic.</li></ul><p>Client signals support via <code>Accept-Encoding: br, gzip</code>; server picks via <code>Content-Encoding</code> and MUST set <code>Vary: Accept-Encoding</code>. <em>Don't</em> compress already-compressed bytes (JPEG/PNG/WebP/woff2) — wasted CPU, near-zero gain. HTTP/2+ also compress <em>headers</em> separately (HPACK/QPACK).</p>"},
  {"sec":"FE-NET","front":"TLS 1.3 handshake basics & why it's faster","back":"<p><strong>TLS 1.3</strong> cut the handshake to <strong>1-RTT</strong> (vs 2-RTT in 1.2) by sending the ClientHello with a key-share guess immediately. It removed legacy/insecure primitives (static RSA key exchange, CBC, RC4, SHA-1) and mandates <strong>forward secrecy</strong> via ephemeral (EC)DHE — so a stolen server key can't decrypt past captured traffic.</p><ul><li><strong>0-RTT resumption</strong>: returning clients send early data on the first flight — but it's <em>replayable</em>, so only use it for safe/idempotent requests.</li><li><strong>SNI</strong> in ClientHello tells the server which cert to serve (ECH / Encrypted Client Hello hides it in 2026).</li><li>Over QUIC/HTTP3, TLS 1.3 is fused into the transport for a combined 1-RTT (or 0-RTT) connection + crypto setup.</li></ul>"},
  {"sec":"FE-NET","front":"Service worker lifecycle & the Cache API","back":"<p>A service worker is a proxy that intercepts network requests for its scope. Lifecycle: <strong>register → install → (waiting) → activate → fetch</strong>.</p><ul><li><code>install</code>: precache the app shell (<code>caches.open()</code> + <code>cache.addAll()</code>).</li><li><strong>Waiting:</strong> a new SW won't take over until all old tabs close — call <code>self.skipWaiting()</code> + <code>clients.claim()</code> to activate immediately (carefully — it can mix asset versions).</li><li><code>activate</code>: clean up stale caches by version.</li><li><code>fetch</code>: implement strategies — <em>cache-first</em> (static, hashed assets), <em>network-first</em> (HTML), <em>stale-while-revalidate</em> (avatars/API).</li></ul><em>Constraints:</em> HTTPS-only (localhost exempt), no DOM access, must use the async <strong>Cache API</strong> (not blocking storage). This is the engine behind offline-capable <strong>PWAs</strong> + the web app manifest."},
  {"sec":"FE-NET","front":"Why does a custom <code>Authorization</code> header force a CORS preflight, and how do you cut the cost?","back":"<p><code>Authorization</code> (and any non-safelisted header like <code>X-Trace-Id</code>) is <em>not</em> CORS-safelisted, so a cross-origin request carrying it is never \"simple\" → the browser fires an <code>OPTIONS</code> preflight <em>before</em> the real request. That's an extra RTT on the critical path.</p><ul><li><strong>Cache the preflight:</strong> set <code>Access-Control-Max-Age</code> (browsers cap it — Chromium ~2h/7200s, Firefox ~24h/86400s) so repeated calls skip the OPTIONS.</li><li><strong>Same-origin via reverse proxy:</strong> route the API under the app's origin so CORS never engages.</li><li><strong>Move auth into a cookie</strong> (<code>SameSite</code>+<code>HttpOnly</code>) instead of an <code>Authorization</code> header to stay within the safelist — but then guard CSRF.</li></ul><em>Gotcha:</em> the preflight <code>OPTIONS</code> must <em>not</em> require auth itself, or you deadlock."}
);

Object.assign(GLOSSARY_DATA, {
  "idempotent-method": {"term":"Idempotent HTTP method","group":"FE-NET","eli5":"Sending the same request many times leaves the server in the same state as sending it once.","memory":"PUT/DELETE = 'set/erase' — doing it twice changes nothing extra. POST = 'add' — does it again.","full":"<p>An HTTP method is <strong>idempotent</strong> if N identical requests produce the same server-side effect as a single one. <code>GET</code>, <code>HEAD</code>, <code>PUT</code>, <code>DELETE</code>, and <code>OPTIONS</code> are idempotent; <code>POST</code> and (generally) <code>PATCH</code> are not.</p><p>This is what makes safe automatic retries possible: a proxy or client can resend an idempotent request after a timeout without risk. For non-idempotent operations you bolt on an <strong>idempotency key</strong> so the server dedupes replays.</p>","trap":"Thinking idempotent means 'returns the same response.' It's about server <em>state</em>, not the response body — a <code>DELETE</code> returns 200 then 404 but is still idempotent.","worked":"Stripe: POST /charges with header `Idempotency-Key: abc123`. A network retry resends it; Stripe returns the original charge instead of double-billing.","related":["safe-method","cors-preflight"]},
  "safe-method": {"term":"Safe HTTP method","group":"FE-NET","eli5":"A request that only reads — it never changes anything on the server.","memory":"Safe = look, don't touch (GET/HEAD/OPTIONS/TRACE).","full":"<p><strong>Safe</strong> methods (<code>GET</code>, <code>HEAD</code>, <code>OPTIONS</code>, <code>TRACE</code>) are defined as having no intended state-changing effect. Browsers, crawlers, and prefetchers rely on this — they'll happily issue GETs speculatively.</p><p>All safe methods are idempotent, but not all idempotent methods are safe (<code>PUT</code>/<code>DELETE</code> change state yet are idempotent). Never hide a mutation behind a GET (e.g. <code>/deleteUser?id=5</code>) — a prefetch or link-prefetcher can fire it unintentionally.</p>","trap":"Believing 'safe' means secure/authenticated. It only means side-effect-free; it says nothing about auth.","worked":"A classic bug: a link prefetcher (or an antivirus/email scanner) hits GET /logout or GET /delete?id=5 and silently performs the action — the 'don't mutate on GET' lesson.","related":["idempotent-method"]},
  "cors-preflight": {"term":"CORS preflight (OPTIONS)","group":"FE-NET","eli5":"Before a 'fancy' cross-site request, the browser sends a quick OPTIONS to ask the server for permission.","memory":"Non-simple = 'ask first' (OPTIONS), then go.","full":"<p>For cross-origin requests that aren't 'simple' (custom headers, JSON content-type, or methods beyond GET/HEAD/POST), the browser sends a <strong>preflight</strong> <code>OPTIONS</code> carrying <code>Access-Control-Request-Method</code> and <code>-Headers</code>. The server must approve via <code>Access-Control-Allow-Origin/Methods/Headers</code>.</p><p><code>Access-Control-Max-Age</code> caches the approval so subsequent calls skip the extra round-trip (browsers cap it: Chromium ~2h, Firefox ~24h). CORS is enforced entirely by the browser — it gates whether JS can <em>read</em> the response, not whether the request reaches the server.</p>","trap":"Assuming CORS protects your server from malicious requests. The request still arrives and executes; CORS only hides the response from cross-origin JS. CSRF protection is separate.","worked":"fetch('https://api.x.com/v1', { headers: { Authorization: 'Bearer t', 'Content-Type': 'application/json' } }) → browser sends OPTIONS first; server replies with matching Allow-* headers; then the real POST goes.","related":["cors-credentials","samesite-cookie"]},
  "cors-credentials": {"term":"CORS wildcard + credentials rule","group":"FE-NET","eli5":"If a cross-site request carries cookies, the server can't reply with a wildcard '*' — it must name the exact origin.","memory":"Cookies + '*' = blocked. Credentials need a named origin.","full":"<p>When a cross-origin request includes credentials (cookies / HTTP auth via <code>credentials:'include'</code>), the browser will only expose the response if the server returns the <em>exact</em> origin in <code>Access-Control-Allow-Origin</code> plus <code>Access-Control-Allow-Credentials: true</code>. A literal <code>*</code> is rejected (and with credentials, <code>*</code> in Allow-Headers/Methods is treated as the literal name, not a wildcard).</p><p>Because the allowed origin now varies by caller, the server must also emit <code>Vary: Origin</code> so shared caches don't leak one origin's headers to another. The safe pattern is a server-side allowlist that reflects the matched origin.</p>","trap":"Setting `Allow-Origin: *` and `Allow-Credentials: true` together and expecting it to work — the browser silently blocks it.","worked":"Server logic: `if (allowlist.has(req.origin)) { res.set('Access-Control-Allow-Origin', req.origin); res.set('Access-Control-Allow-Credentials','true'); res.set('Vary','Origin'); }`","related":["cors-preflight","samesite-cookie"]},
  "samesite-cookie": {"term":"SameSite cookie attribute","group":"FE-NET","eli5":"A cookie setting that controls whether the browser sends it on requests coming from other sites.","memory":"Lax = links in, XHR out. Strict = nothing cross-site. None = everywhere (but must be Secure).","full":"<p><strong>SameSite</strong> governs cross-site cookie transmission and is the first line of CSRF defense. <code>Lax</code> (the modern default) sends the cookie on top-level GET navigations but withholds it from cross-site subresources and POSTs. <code>Strict</code> withholds it on any cross-site context. <code>None</code> permits cross-site sending but requires the <code>Secure</code> flag.</p><p>Combine with <code>HttpOnly</code> (blocks JS access → mitigates XSS theft) and <code>Secure</code> (HTTPS-only). With third-party cookies phased out, cross-site use now needs partitioned cookies (CHIPS, <code>Partitioned</code>).</p>","trap":"Relying on SameSite=Lax alone to stop CSRF on GET-triggered state changes — Lax still sends cookies on top-level GETs, so don't mutate on GET.","worked":"Set-Cookie: session=abc; HttpOnly; Secure; SameSite=Lax — protects against most CSRF while still letting a user click an email link and land logged in.","related":["cors-credentials","http-cache-control"]},
  "http-cache-control": {"term":"Cache-Control directives","group":"FE-NET","eli5":"Headers that tell browsers and CDNs how long to reuse a response and when to recheck it.","memory":"no-cache = recheck; no-store = never keep; immutable = never recheck.","full":"<p><code>Cache-Control</code> drives HTTP caching. <code>max-age</code>/<code>s-maxage</code> set freshness lifetime (the latter for shared caches). <code>no-cache</code> permits storage but mandates revalidation before reuse; <code>no-store</code> forbids storage entirely. <code>private</code> limits to the browser; <code>public</code> allows CDNs. <code>immutable</code> tells the browser to skip revalidation even on reload.</p><p>The canonical static-asset strategy: content-hashed filenames + <code>max-age=31536000, immutable</code>, and short-lived/revalidated HTML so new hashes are picked up.</p>","trap":"Reading 'no-cache' as 'do not cache.' It means 'cache but always revalidate.' The real don't-cache directive is no-store.","worked":"app.4f9a.js → `Cache-Control: public, max-age=31536000, immutable`; index.html → `Cache-Control: no-cache` (always revalidate via ETag).","related":["etag-revalidation","stale-while-revalidate","http3-quic"]},
  "etag-revalidation": {"term":"ETag / If-None-Match revalidation","group":"FE-NET","eli5":"A fingerprint for a response so the browser can ask 'still the same?' and get a tiny 304 if so.","memory":"ETag out, If-None-Match back → 304 = 'unchanged, use yours.'","full":"<p>The server tags a response with an opaque <strong>ETag</strong>. On revalidation the client sends <code>If-None-Match: \"etag\"</code>; if it still matches, the server returns <strong>304 Not Modified</strong> with an empty body, saving bandwidth. Otherwise it sends a fresh 200 with a new ETag.</p><p>Strong ETags assert byte-identity; weak (<code>W/\"...\"</code>) assert only semantic equivalence (fine for caching, unsafe for range requests). ETags pair with <code>no-cache</code> to give cheap freshness checks.</p>","trap":"Generating ETags from inode/mtime in a multi-server cluster — each node emits a different ETag, so revalidation behind a load balancer never returns 304.","worked":"Req: `If-None-Match: \"v7\"` → Resp: `304 Not Modified` (no body) → browser serves its cached copy instantly.","related":["http-cache-control","stale-while-revalidate"]},
  "http3-quic": {"term":"HTTP/3 over QUIC","group":"FE-NET","eli5":"A newer web protocol that runs on UDP so one lost packet doesn't freeze every other download.","memory":"H2 fixes app HOL; H3/QUIC fixes transport HOL by ditching TCP.","full":"<p><strong>HTTP/3</strong> carries HTTP semantics over <strong>QUIC</strong>, a transport built on UDP with TLS 1.3 fused in. Because each stream is independent at the transport layer, a single lost packet stalls only its own stream — eliminating the <strong>TCP-level head-of-line blocking</strong> that still afflicts HTTP/2.</p><p>QUIC also gives 0/1-RTT connection setup, connection migration (survives a Wi-Fi→cellular IP change via connection IDs), and QPACK header compression. It's widely deployed at major CDNs and browsers in 2026.</p>","trap":"Thinking HTTP/2 multiplexing already solved head-of-line blocking. It solved it at the application layer only; TCP-level HOL remained until HTTP/3.","worked":"On a lossy mobile network, an H2 page stalls all images when one TCP segment drops; the same page over H3/QUIC keeps the other images flowing.","related":["tls13-handshake","http-cache-control"]},
  "tls13-handshake": {"term":"TLS 1.3 handshake","group":"FE-NET","eli5":"The faster, stripped-down secure handshake that sets up encryption in one round trip.","memory":"1.3 = 1-RTT, forward secrecy always, legacy ciphers gone.","full":"<p><strong>TLS 1.3</strong> reduces the handshake to <strong>1-RTT</strong> by having the client send a key-share guess in the ClientHello. It mandates ephemeral (EC)DHE for <strong>forward secrecy</strong> and removes insecure primitives (static RSA key exchange, CBC, RC4, SHA-1).</p><p><strong>0-RTT</strong> resumption lets returning clients send early application data immediately, but that data is replay-vulnerable, so it must be restricted to idempotent requests. SNI selects the cert; ECH (Encrypted Client Hello) hides the hostname. Over QUIC, TLS 1.3 is integrated into the transport handshake.</p>","trap":"Using 0-RTT early data for a non-idempotent POST — an attacker can replay it. 0-RTT is only safe for safe/idempotent requests.","worked":"Returning visitor → 0-RTT resumption sends the GET / request in the very first flight, so the page starts loading with effectively no handshake delay.","related":["http3-quic","idempotent-method"]},
  "sse-vs-websocket": {"term":"SSE vs WebSocket","group":"FE-NET","eli5":"SSE streams data one-way from server to browser; WebSocket is a two-way live channel.","memory":"SSE = server talks (auto-reconnect, plain HTTP). WS = both talk (Upgrade, DIY reconnect).","full":"<p><strong>SSE</strong> (<code>EventSource</code>) is a server→client stream over ordinary HTTP with built-in auto-reconnect and <code>Last-Event-ID</code> resume — ideal for notifications, live feeds, and LLM token streaming. <strong>WebSocket</strong> upgrades the connection (HTTP 101) to a full-duplex frame protocol for true bidirectional, low-latency traffic (chat, multiplayer, collab editing).</p><p>SSE rides HTTP caching/proxies/auth cleanly and is simpler; WebSocket needs you to re-implement reconnect, auth, and backpressure. On HTTP/1.1 SSE shares the ~6-connection-per-origin cap (a non-issue on HTTP/2+).</p>","trap":"Reaching for WebSocket whenever you need 'real-time.' If data only flows server→client, SSE is simpler, auto-reconnects, and works over plain HTTP.","worked":"LLM chat UI streams tokens via SSE (server pushes deltas, browser appends); a multiplayer cursor feature uses WebSocket because both peers send continuously.","related":["http3-quic","fetch-abortsignal"]},
  "fetch-abortsignal": {"term":"fetch AbortSignal","group":"FE-NET","eli5":"A way to cancel an in-flight fetch — for timeouts or when the user navigates away.","memory":"AbortController.signal in, controller.abort() to cancel → AbortError.","full":"<p>The <strong>fetch API</strong> accepts a <code>signal</code> from an <code>AbortController</code>; calling <code>abort()</code> rejects the promise with an <code>AbortError</code> and cancels the underlying request. <code>AbortSignal.timeout(ms)</code> gives a one-liner timeout, and <code>AbortSignal.any([...])</code> composes multiple signals (e.g. timeout OR user-cancel).</p><p>Remember fetch resolves on any HTTP status — a 404/500 is <em>not</em> a rejection — so always inspect <code>res.ok</code>. The body is a single-use stream; <code>res.clone()</code> to read it twice.</p>","trap":"Treating a 500 response as a thrown error. fetch only rejects on network failure or abort; you must check res.ok yourself.","worked":"const c = new AbortController(); fetch(url, { signal: c.signal }); /* on unmount */ c.abort(); — React effect cleanup cancels stale requests.","related":["sse-vs-websocket","rest-vs-graphql"]},
  "rest-vs-graphql": {"term":"REST vs GraphQL","group":"FE-NET","eli5":"REST gives fixed endpoints; GraphQL lets the client ask for exactly the fields it needs.","memory":"REST = over/under-fetch; GraphQL = exact shape, but caching is harder.","full":"<p><strong>REST</strong> exposes resource endpoints; UIs often <em>over-fetch</em> unused fields or <em>under-fetch</em> and waterfall across several endpoints. <strong>GraphQL</strong> uses one typed endpoint where the client specifies the exact response shape, eliminating both — at the cost of harder HTTP caching (POST to /graphql), needing persisted queries and normalized client caches, and guarding against expensive nested queries.</p><p>On the server, GraphQL's resolver <strong>N+1</strong> problem is solved with DataLoader batching. For first-party apps in 2026, REST with sparse fieldsets, tRPC, or React Server Components often win where you own both ends.</p>","trap":"Believing GraphQL is always faster/better. It trades easy HTTP/CDN caching and simplicity for flexibility — overkill when one team controls client and server.","worked":"GraphQL query `{ user(id:1){ name posts{ title } } }` returns exactly that shape in one request; the REST equivalent might be GET /user/1 then GET /user/1/posts.","related":["fetch-abortsignal","http-cache-control"]}
});
