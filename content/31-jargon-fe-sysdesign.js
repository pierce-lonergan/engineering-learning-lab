/* Frontend jargon & acronyms — clickable reference (FE-SYSDESIGN, auto-assembled) */
Object.assign(GLOSSARY_DATA, {
  "csr": {"term":"CSR","group":"FE-SYSDESIGN","aliases":["Client-Side Rendering","Client Side Rendering"],"eli5":"A rendering strategy where the server sends a nearly empty HTML shell plus a JavaScript bundle, and the browser builds the entire UI on the client.","memory":"CSR = Client-Side Rendering — the CLIENT (browser) builds the page from JS.","full":"<p><strong>CSR (Client-Side Rendering)</strong> is the approach where the server returns a minimal HTML document — often little more than an empty <code>&lt;div id=\"root\"&gt;</code> and a <code>&lt;script&gt;</code> tag — and the browser downloads, parses, and executes a JavaScript bundle that constructs the entire DOM and renders the UI on the client. It is the defining technique of the Single-Page Application (<code>SPA</code>).</p><p>CSR rose to prominence in the early-to-mid 2010s with client-side frameworks like AngularJS (2010), React (2013), and Vue, building on the earlier AJAX era that let pages update without full reloads. The model gives a desktop-app-like experience: after the initial load, navigation and updates happen in the browser without round-trips for new HTML pages.</p><p>Its purpose is rich interactivity, smooth in-app navigation, and offloading rendering work to the client (reducing server compute per view). CSR is still widely used — especially for highly interactive, authenticated app \"dashboards\" where SEO and first-paint speed matter less. However, its weaknesses (a slow, blank first paint while the bundle downloads and executes, weaker SEO for crawlers that don't run JS, and large bundles) drove the industry toward <code>SSR</code>, <code>SSG</code>, and the <code>RSC</code> model, which deliver content-first HTML and ship less JavaScript.</p>","worked":"<p>A minimal CSR setup: the server sends an empty shell, and JS mounts the app:</p><ul><li>HTML shipped by server: <code>&lt;div id=\"root\"&gt;&lt;/div&gt;&lt;script src=\"/bundle.js\"&gt;&lt;/script&gt;</code></li><li>Client bundle builds everything: <code>import { createRoot } from \"react-dom/client\";</code></li><li><code>createRoot(document.getElementById(\"root\")).render(&lt;App /&gt;);</code></li></ul><p>Until <code>bundle.js</code> downloads and runs, the user sees a blank page (or a JS-rendered loading spinner) — there is no real content in the initial HTML.</p>","teach":"<p>At its core, CSR means the browser does all the assembly. The server's job shrinks to handing over a near-empty HTML page and a bag of JavaScript. The browser then runs that JavaScript, which programmatically creates every heading, button, and list item and inserts them into the page. Nothing meaningful is visible until that script finishes.</p><div class=\"analogy\">CSR is like getting a flat-pack box of furniture parts delivered to your door. The truck arrives fast — the box is light and the shell of the page loads quickly — but you can't actually use the bookshelf until <em>you</em> assemble it at home. The browser is the person on the living-room floor with the parts and the instructions (the JS bundle), and only once it's done building can you put books on the shelf.</div><p>The big upside is what happens <em>after</em> assembly. Once the app is built in the browser, it behaves like a native application: clicking a link doesn't fetch a whole new HTML page from the server and reload — instead JavaScript swaps out just the part that changed. That makes navigation feel instant and lets you build very interactive, stateful UIs. This is exactly what a Single-Page Application is.</p><p>The downsides are front-loaded. The first visit shows a blank screen (or a spinner) while the bundle downloads and executes — bad for perceived speed and bad for metrics like First Contentful Paint and Largest Contentful Paint. Search-engine crawlers or social link-preview bots that don't fully run JavaScript may see an empty page, hurting SEO. And the bundles can get large. Those pain points are precisely why server-side strategies came back: SSR sends real HTML up front, SSG pre-builds it at build time, and React Server Components ship even less JS. The modern trend is hybrids — CSR for the deeply interactive parts, server rendering for the content-first parts.</p><div class=\"key\">CSR ships an empty shell plus a JS bundle and builds the whole UI in the browser. You get an app-like, smooth-navigating SPA, but at the cost of a slow blank first paint, weaker SEO, and heavy bundles — which is why SSR/SSG/RSC exist as content-first alternatives.</div>","related":["ssr","ssg","spa","rsc","fouc"]},
  "spa": {"term":"SPA","group":"FE-SYSDESIGN","aliases":["Single-Page Application","Single Page App","Single-Page App"],"eli5":"A web app that loads a single HTML page once and then updates the view with JavaScript as you navigate, instead of fetching a fresh full page from the server each time.","memory":"SPA = Single-Page Application — ONE page load, then JS swaps the content.","full":"<p><strong>SPA (Single-Page Application)</strong> is a web-app architecture where the browser loads one HTML document and a JavaScript application, and thereafter all navigation and view changes are handled <em>in the browser</em> by JavaScript — updating the DOM and the URL (via the History API) without triggering full-page reloads from the server. SPAs are the canonical product of client-side rendering (<code>CSR</code>).</p><p>The term and pattern emerged in the mid-to-late 2000s as AJAX made it possible to fetch data without reloading, and it became dominant in the 2010s with frameworks like AngularJS, React, Vue, and their client-side routers (e.g. React Router). The History API (<code>pushState</code>/<code>popState</code>) standardized clean client-side URLs without the older hash-fragment hacks.</p><p>Its purpose is a fast, fluid, app-like experience after the initial load — instant navigation, persistent state across \"pages,\" and rich interactivity — by avoiding repeated server round-trips for full HTML documents. SPAs are still widely used, especially for authenticated dashboards and tools where SEO and first-paint matter less. But the pure-CSR SPA's weaknesses (slow blank first paint, weaker SEO, large bundles, and security nuances — e.g. tokens for a cross-domain SPA are kept short-lived in memory and refreshed via an HttpOnly cookie because any XSS is game-over) pushed the ecosystem toward SSR/SSG/RSC hybrids that pair SPA-like interactivity with server-rendered content.</p>","worked":"<p>In a SPA, the client router intercepts navigation and swaps views without a full reload, updating the URL via the History API:</p><ul><li><code>&lt;Routes&gt;&lt;Route path=\"/\" element={&lt;Home /&gt;} /&gt;&lt;Route path=\"/about\" element={&lt;About /&gt;} /&gt;&lt;/Routes&gt;</code></li><li>Clicking <code>&lt;Link to=\"/about\"&gt;</code> calls <code>history.pushState</code> — no server round-trip for new HTML; only data is fetched as needed.</li></ul><p>Auth note for a cross-domain SPA: keep the access token short-lived in memory and refresh it via an <code>HttpOnly</code> cookie, since any XSS that can read the token is game-over.</p>","teach":"<p>At its core, a SPA is a website that loads <em>once</em> and then never does a traditional full-page reload again. The first request brings down one HTML shell plus a JavaScript app. From then on, when you click around, JavaScript intercepts the navigation, fetches just the data it needs, rewrites the part of the page that changed, and updates the address bar — all without asking the server for a brand-new HTML document.</p><div class=\"analogy\">Think of the difference between flipping through a flip-book and watching a video on a tablet. The classic multi-page website is the flip-book: every \"navigation\" is a whole new printed page slapped in front of you (a full server reload, a flash, a fresh download). A SPA is the tablet app: the screen is already on, and tapping just changes what's displayed instantly — same device, same session, no reboot between views.</div><p>The payoff is a smooth, <strong>app-like feel</strong>: navigation is instant, your scroll position and in-memory state can persist across \"pages,\" and interactions feel snappy because you're not re-downloading and re-parsing a full document each time. This is why complex tools and dashboards are so often SPAs.</p><p>The costs are the same ones CSR has, because a SPA <em>is</em> client-rendered: the first load can show a blank screen while the bundle downloads and runs (slow first paint), search crawlers that don't execute JS may see nothing (weaker SEO), and bundles grow. There's also a notable <strong>security</strong> wrinkle: SPAs often hold auth tokens in JavaScript. For a cross-domain SPA, best practice is to keep the access token short-lived in memory and refresh it through an <code>HttpOnly</code> cookie the JS can't read — because if an attacker lands any XSS, a token sitting in readable JS is instantly stolen. These trade-offs are why modern apps blend SPA interactivity with server rendering (SSR/SSG/RSC): content-first HTML on arrival, SPA-style fluidity after.</p><div class=\"key\">A SPA loads one page and then uses JavaScript (plus the History API) to swap views without full reloads — fluid, app-like, stateful. It inherits CSR's downsides (slow first paint, weak SEO, big bundles) and an XSS-sensitive auth model, which is why server-rendered hybrids now pair SPA interactivity with content-first HTML.</div>","related":["csr","ssr","ssg","rsc"]},
  "mfe": {"term":"MFE","group":"FE-SYSDESIGN","aliases":["Micro-Frontend","Micro Frontend","Microfrontend","Micro-Frontends"],"eli5":"An architecture that splits a large frontend into independently built and deployed pieces (often owned by different teams) that are composed together into one app.","memory":"MFE = Micro-Frontend — microservices, but for the UI: split the frontend into independently shipped pieces.","full":"<p><strong>MFE (Micro-Frontend)</strong> is an architectural style that applies microservices thinking to the browser: a large web application is decomposed into smaller, independently developed, tested, and deployed frontend units — each typically owned by a separate team — which are then composed at runtime (or build time) into a single cohesive application. Pieces may even use different frameworks, communicate via well-defined contracts, and ship on independent release cadences.</p><p>The term gained traction around 2016 and was widely codified by ThoughtWorks (it appeared on their Technology Radar) and articles by practitioners. Composition techniques evolved from server-side includes and iframes to client-side approaches; <strong>Webpack Module Federation</strong> (introduced around 2020 in Webpack 5) became a popular mechanism for runtime sharing of code between independently built bundles, and the Web Components standard is another common integration substrate.</p><p>Its purpose is organizational scalability: it lets many teams work on one big product without a monolithic, lock-step frontend release — enabling independent deployments, autonomy, and incremental migration of legacy stacks. MFEs are still used in 2026 for large enterprise products, but they carry real costs (duplicated dependencies and larger payloads, versioning/contract complexity, harder shared state and consistent UX, and operational overhead). Many teams therefore adopt them selectively, and the trade-off versus a well-structured monorepo/modular monolith is an ongoing design debate.</p>","worked":"<p>With Webpack Module Federation, a host app loads a remotely-built micro-frontend at runtime:</p><ul><li>Remote (team A) exposes a component: <code>exposes: { \"./Cart\": \"./src/Cart\" }</code></li><li>Host (team B) consumes it: <code>remotes: { cartApp: \"cartApp@https://teamA.example.com/remoteEntry.js\" }</code></li><li>In code: <code>const Cart = React.lazy(() =&gt; import(\"cartApp/Cart\"));</code> — team A deploys Cart independently; the host picks it up without rebuilding.</li></ul>","teach":"<p>At its core, a micro-frontend is just the idea of breaking one giant frontend into several smaller frontends that are built and shipped separately, then stitched back together so the user sees one seamless app. It's the same instinct that drove backends from one monolith into many microservices — but applied to the UI layer.</p><div class=\"analogy\">Picture a shopping mall. From the shopper's perspective it's one building with one entrance and a consistent look. But behind the scenes, each store is run by a different company that stocks, staffs, and renovates on its own schedule without asking the mall owner's permission for every change. A micro-frontend architecture is that mall: the search bar, the product grid, the checkout cart, and the recommendations widget might each be a separate \"store\" owned by a different team, deployed independently, yet composed into one storefront.</div><p>The real motivation is <strong>organizational</strong>, not technical elegance. When dozens of engineers across many teams all commit to one frontend codebase that deploys as a single unit, releases become slow and risky — everyone waits on everyone. Micro-frontends let each team own a slice end-to-end and ship it on their own cadence. They also make it possible to migrate a legacy app piece by piece (replace one slice at a time) and even let different slices use different frameworks during a transition.</p><p>The composition can happen different ways: old-school iframes or server-side includes, Web Components, or — popularly — <strong>Webpack Module Federation</strong>, which lets one bundle load code from another independently-built bundle at runtime. But all of this has a price. You can end up shipping the same framework three times (bloated payloads), juggling version contracts between slices, fighting to keep the UX visually consistent, and sharing state across boundaries that don't naturally share state. That's why teams adopt MFEs deliberately, and why a modular monorepo is often the simpler alternative for smaller orgs.</p><div class=\"key\">A micro-frontend splits a big UI into independently built and deployed slices, usually one per team, composed into one app (often via Module Federation or Web Components). It buys team autonomy and incremental migration at the cost of duplicated dependencies, versioning complexity, and harder shared state/consistency.</div>","related":["spa","csr","ssr"]},
  "mvc": {"term":"MVC","group":"FE-SYSDESIGN","aliases":["Model-View-Controller","Model View Controller"],"eli5":"A classic software design pattern that separates an app into three parts — the data (Model), what the user sees (View), and the logic that connects them (Controller).","memory":"MVC = Model-View-Controller: data, display, and the glue between them.","full":"<p><strong>MVC (Model-View-Controller)</strong> is a software architectural pattern that divides an application into three interconnected roles: the <strong>Model</strong> (the data and business rules), the <strong>View</strong> (the presentation/UI the user sees), and the <strong>Controller</strong> (which handles input, updates the model, and selects what view to render). The goal is separation of concerns so that presentation, data, and control logic can evolve independently.</p><p>MVC is one of the oldest UI patterns: it was conceived by Trygve Reenskaug while working on Smalltalk at Xerox PARC in the late 1970s. It spread through desktop GUI toolkits and then became the backbone of server-side web frameworks in the 2000s (Ruby on Rails, ASP.NET MVC, Spring MVC, Django's MVT variant). On the client, early JavaScript frameworks like Backbone.js and AngularJS adopted MVC/MV* flavors, often with two-way data binding between view and model.</p><p>Its purpose is maintainability through clear role boundaries, and it is still extremely common server-side and in many app frameworks. In modern <em>frontend</em> development, however, MVC's two-way data binding became a pain point — cascading, bidirectional updates made it hard to trace how a given state change happened. That frustration directly motivated Facebook's <strong>Flux</strong> (and later Redux), which enforce a strict <em>one-way</em> data flow, and React's unidirectional rendering model. So while MVC remains foundational and widely taught, much of the React ecosystem deliberately moved away from its two-way-binding incarnation.</p>","worked":"<p>A server-side MVC request flow (e.g. Rails/Django style):</p><ul><li><strong>Controller</strong>: receives <code>GET /users/42</code>, asks the model for data: <code>const user = User.find(42);</code></li><li><strong>Model</strong>: <code>User</code> encapsulates the data and rules (e.g. validation, <code>fullName()</code>).</li><li><strong>View</strong>: a template renders the user: <code>&lt;h1&gt;{{ user.fullName }}&lt;/h1&gt;</code>, returned as HTML.</li></ul><p>Contrast with the frontend reaction against two-way MVC binding — Flux/Redux enforce one-way flow: <code>action → reducer → store → view</code>, where the view can never directly mutate state.</p>","teach":"<p>At its core, MVC is just a rule for <em>where different kinds of code are allowed to live</em>. Instead of one big tangled blob, you split an app into three boxes. The <strong>Model</strong> holds the data and the rules about that data (\"a user must have an email\"). The <strong>View</strong> is purely what the user sees — the HTML, the screen. The <strong>Controller</strong> is the middleman: it takes user input (a click, a request), tells the Model to change or fetch something, and picks which View to show.</p><div class=\"analogy\">Think of a restaurant. The <strong>Model</strong> is the kitchen and pantry — where the actual food and the recipes (rules) live. The <strong>View</strong> is the plated dish presented at your table — purely how things look to the customer. The <strong>Controller</strong> is the waiter: they take your order (input), relay it to the kitchen (update the model), and bring back the right plate (choose the view). The waiter never cooks, and the plate never takes orders — each role stays in its lane.</div><p>Why split things up like this? <strong>Maintainability.</strong> If your data rules change, you edit the Model without touching the screen layout. If you want a new look, you change the View without risking the business logic. This separation made MVC the backbone of server frameworks like Rails, Django, and Spring, and it's still everywhere on the backend.</p><p>So why does the React world often avoid it? The trouble showed up in early <em>frontend</em> MVC frameworks that used <strong>two-way data binding</strong>: the view could change the model and the model could change the view, automatically, in both directions. In a small app that's magical; in a big app it becomes a nightmare — a single state change can cascade through bindings and you can't tell <em>what</em> triggered <em>what</em>. That exact pain is what Facebook's Flux (and Redux) were built to solve, by forcing a strict <strong>one-way</strong> data flow: data goes in one direction only, so every change is traceable. React's unidirectional model is, in part, a deliberate reaction against two-way MVC.</p><div class=\"key\">MVC splits an app into Model (data + rules), View (UI), and Controller (input/glue) for clean separation of concerns. Invented at Xerox PARC in the 1970s, it still dominates server-side frameworks — but its two-way-binding frontend variants caused untraceable cascading updates, which is exactly why Flux/Redux and React adopted strict one-way data flow.</div>","related":["spa","csr","ssr"]},
  "w3c": {"term":"W3C","group":"FE-SYSDESIGN","aliases":["World Wide Web Consortium"],"eli5":"The W3C is the international standards organization that develops and publishes many of the open specifications for the web, such as HTML, CSS, accessibility guidelines, and design-token formats.","memory":"World Wide Web Consortium — the 'three Ws' body that standardizes the Web.","full":"<p><strong>W3C (World Wide Web Consortium)</strong> is the international standards body that develops open specifications (called Recommendations) for core web technologies. Member organizations, invited experts, and staff collaborate in working groups to produce vendor-neutral standards that browsers and tools implement.</p><p>The W3C was founded in 1994 by Tim Berners-Lee — the inventor of the World Wide Web — initially hosted at MIT, with co-host institutions in Europe and Asia over time. In 2023 it reorganized into a public-interest non-profit entity. Over its history it has shepherded specifications including CSS, the DOM, SVG, WAI-ARIA, and the WCAG accessibility guidelines (via its Web Accessibility Initiative, WAI). For HTML and DOM, the W3C historically published standards but eventually ceded the living standard to the WHATWG in a 2019 agreement, while continuing to lead in areas like accessibility, CSS, and emerging formats.</p><p>The W3C is still central in 2026 because it provides the neutral, multi-stakeholder process that keeps the web interoperable across competing browser vendors. Modern frontend artifacts reference it directly — for example, the <strong>Design Tokens Community Group (DTCG)</strong> operating under the W3C is producing a standard JSON format for design tokens (named, platform-agnostic design decisions like <code>color.brand.primary</code> or <code>space.4</code>), which design-system tooling compiles into CSS, iOS, Android, and other targets.</p>","worked":"<p>A W3C DTCG design-token file expresses design decisions as platform-agnostic JSON:</p><ul><li><code>{ \"color\": { \"brand\": { \"primary\": { \"$value\": \"#0055ff\", \"$type\": \"color\" } } } }</code></li><li><code>{ \"space\": { \"4\": { \"$value\": \"16px\", \"$type\": \"dimension\" } } }</code></li></ul><p>A build tool compiles those tokens into platform outputs, e.g. a CSS custom property:</p><ul><li><code>:root { --color-brand-primary: #0055ff; --space-4: 16px; }</code></li></ul>","teach":"<p>At its core, the W3C is just the committee where browser makers and web stakeholders sit down together and agree on the rules of the web, so that a page works the same everywhere.</p><p>The web only functions because of shared agreements. If Chrome, Safari, and Firefox each invented their own version of CSS or the DOM, a site that worked in one would break in another. Someone neutral has to write down the spec everyone follows. That someone, for much of the web, is the W3C.</p><div class=\"analogy\">Think of the W3C like the body that standardizes electrical outlets and voltages in a country. No single appliance company gets to decide the plug shape; a standards organization sets it so any lamp fits any socket. The W3C sets the \"sockets\" of the web — the markup, styles, and APIs — so any browser can plug into any page.</div><p>It was founded in 1994 by Tim Berners-Lee, the web's inventor, and runs a deliberate process: ideas mature through working drafts into finalized <em>Recommendations</em>. Some technologies it owns outright (CSS, SVG, accessibility standards via the WAI); for HTML and the DOM it now collaborates with the WHATWG, which maintains those as \"living standards.\"</p><p>The reason it shows up in modern frontend work — like design systems — is that the W3C is where emerging cross-tool formats get blessed. The Design Tokens Community Group under the W3C is standardizing how teams store design decisions as JSON. Instead of every tool inventing its own token format, the DTCG spec gives one shape that Figma, Style Dictionary, and others can read and compile into CSS variables, native styles, and beyond.</p><div class=\"key\">W3C = the standards consortium (founded 1994 by Tim Berners-Lee) that publishes open web specs like CSS, the DOM, and accessibility guidelines, and now incubates formats like the DTCG design-token JSON. It's the neutral process that keeps the web interoperable.</div>","related":["whatwg","rfc","wcag"]},
  "whatwg": {"term":"WHATWG","group":"FE-SYSDESIGN","aliases":["Web Hypertext Application Technology Working Group"],"eli5":"The WHATWG is the organization that maintains the HTML, DOM, Fetch, and several other core web specifications as continuously updated 'living standards' rather than fixed versioned releases.","memory":"Web Hypertext Application Technology Working Group — the browser-vendor group behind the HTML 'living standard'.","full":"<p><strong>WHATWG (Web Hypertext Application Technology Working Group)</strong> is a community that develops and maintains several foundational web specifications, most notably the HTML Standard, the DOM Standard, the Fetch Standard, and others. Its defining philosophy is the <em>living standard</em>: rather than publishing discrete numbered versions, the spec is continuously revised to reflect what browsers actually implement.</p><p>The WHATWG was formed in 2004 by people from browser vendors (notably engineers associated with Apple, Mozilla, and Opera) who were frustrated with the direction of W3C's XHTML-focused work and wanted to evolve HTML pragmatically for web applications. This effort produced what became HTML5. For years HTML was maintained in parallel by both the W3C and WHATWG, which caused divergence; in 2019 the two organizations signed an agreement making the WHATWG the single authoritative maintainer of the HTML and DOM living standards.</p><p>The WHATWG remains the source of truth in 2026 for HTML, DOM, Fetch, URL, Encoding, and related specs precisely because the living-standard model matches the web's continuous evolution — features land in browsers and the spec tracks them, rather than waiting for a monolithic \"HTML6\" release. Frontend developers effectively rely on WHATWG specs every time they use modern DOM APIs, the Fetch API, or HTML parsing semantics.</p>","worked":"<p>The Fetch API, a WHATWG standard, is the canonical way modern frontends make network requests:</p><ul><li><code>const res = await fetch('/api/users', { method: 'GET' });</code></li><li><code>const data = await res.json();</code></li></ul><p>HTML parsing rules — also a WHATWG living standard — define exactly how a browser turns markup like <code>&lt;p&gt;Hello&lt;/p&gt;</code> into a DOM tree, ensuring every browser builds the same structure from the same bytes.</p>","teach":"<p>At its core, the WHATWG is just the group of browser engineers who decided the web's core specs should be a constantly-updated document, not a series of frozen \"versions.\"</p><p>In the early 2000s, HTML's official direction was heading toward XHTML — a stricter, XML-based form that didn't match how real web pages and emerging web <em>applications</em> were being built. A faction of browser-vendor engineers broke away in 2004 to evolve HTML in a more practical direction, and their work became HTML5.</p><div class=\"analogy\">Think of the difference between a printed encyclopedia and a continuously edited wiki. The old model shipped \"HTML 4,\" then someday \"HTML 5,\" like new encyclopedia editions years apart. The WHATWG turned HTML into a wiki-like <em>living standard</em>: when browsers add a capability, the spec is edited to match, immediately. There's no waiting for the next big numbered release.</div><p>This is more than a philosophical quirk — it solved a real interoperability mess. For years both the W3C and WHATWG published HTML, and the two drifted apart, leaving developers unsure which was authoritative. A 2019 agreement settled it: the WHATWG became the single maintainer of the HTML and DOM living standards, while the W3C kept leadership in areas like CSS and accessibility.</p><p>As a frontend developer you depend on WHATWG specs constantly, often without noticing. The exact rules for how a browser parses your markup into a DOM tree, how the <code>fetch()</code> function behaves, how URLs are parsed, how text encodings work — all of these are WHATWG living standards. When two browsers render the same page identically, it's largely because they both implement the same WHATWG description of \"what HTML means.\"</p><div class=\"key\">WHATWG = the browser-vendor-driven group (formed 2004) that maintains HTML, DOM, Fetch, and related specs as continuously-updated living standards. Since 2019 it's the authoritative home of HTML and the DOM; the W3C leads elsewhere (CSS, accessibility).</div>","related":["w3c","rfc","wcag"]},
  "i18n": {"term":"i18n","group":"FE-SYSDESIGN","aliases":["internationalization","internationalisation"],"eli5":"Designing and building software so it can be adapted to different languages and regions without code changes.","memory":"i18n = 'internationalization' — 18 letters between the i and the n. It's preparing the app to be translated.","full":"<p><strong>i18n</strong> is the numeronym for <em>internationalization</em> — \"i\", then 18 letters, then \"n\". It is the engineering practice of building software so that it <em>can</em> be adapted to various languages, scripts, and regional conventions without re-engineering. That means externalizing all user-facing strings, supporting Unicode, handling right-to-left text, and formatting dates, numbers, and currencies in locale-aware ways.</p><p>The numeronym style (and the discipline) became widespread in the software industry from the late 1980s–1990s as companies like Sun, IBM, and others shipped globally; the abbreviation was reportedly popularized at <strong>DEC (Digital Equipment Corporation)</strong>. On the web, standards and APIs such as Unicode, the HTML <code>lang</code> attribute, and the JavaScript <code>Intl</code> object formalized i18n support.</p><p>Its purpose is to make <em>localization</em> (l10n) possible and cheap: do the structural work once (i18n) so each new locale is just translation and configuration (l10n), not a code rewrite. It remains essential in 2026 for any product with a global audience — building i18n in from the start is vastly cheaper than retrofitting it.</p>","worked":"<p>Externalizing strings and using locale-aware formatting (the i18n groundwork):</p><pre><code>// i18n: never hard-code user-facing text; look it up by key\nconst messages = {\n  en: { greeting: 'Hello, {name}!' },\n  fr: { greeting: 'Bonjour, {name} !' }\n}\n\n// Locale-aware formatting via the Intl API\nnew Intl.DateTimeFormat('fr-FR').format(new Date())   // 21/06/2026\nnew Intl.NumberFormat('de-DE',\n  { style: 'currency', currency: 'EUR' }).format(1234.5) // 1.234,50 €\n\n&lt;!-- Declare the document language for the browser/AT --&gt;\n&lt;html lang=\"fr\"&gt;</code></pre>","teach":"<p>At its core, i18n is just <strong>not baking any one language or region into your code</strong>. Instead of writing the words \"Add to cart\" directly in a button, you write a <em>reference</em> to a message, and the actual text comes from a translation table chosen at runtime based on the user's locale. Same idea for dates, numbers, and currency — you ask the platform to format them for the locale rather than assuming \"MM/DD/YYYY\" or \"$\".</p><div class=\"analogy\">Think of building a stage play where none of the lines are painted onto the set. Instead, every spot that needs words has a labeled slot, and you hand the cast a script booklet for whichever country you're touring. Internationalization is building the <em>slotted stage</em>; localization is printing the booklet for France or Japan. If you'd painted the English lines onto the scenery, every new country would mean rebuilding the whole set.</div><p>Concretely, the i18n work includes: pulling every user-facing string into resource files keyed by name; using <strong>Unicode</strong> everywhere so any script renders; supporting right-to-left layouts for Arabic and Hebrew; setting the HTML <code>lang</code> attribute; and using locale-aware APIs like JavaScript's <code>Intl</code> for dates, numbers, and currencies. It also means leaving room in your UI — translated text is often longer than English, so fixed-width buttons break.</p><p>The payoff is the split with <strong>l10n</strong> (localization). i18n is the one-time structural investment; l10n is the per-locale fill-in-the-blanks. Do i18n well and adding Spanish is mostly a translation job, not an engineering project.</p><div class=\"key\">The takeaway: <strong>i18n is preparing your app to speak any language; l10n is actually teaching it one.</strong> Externalize strings, use Unicode and <code>Intl</code>, and design flexible layouts — retrofitting later is painful, so build it in early.</div>","related":["l10n","cjk"]},
  "l10n": {"term":"l10n","group":"FE-SYSDESIGN","aliases":["localization","localisation"],"eli5":"Adapting an already-internationalized product to a specific language and region — translating text and adjusting formats and conventions.","memory":"l10n = 'localization' — l, 10 letters, n. It's the per-locale fill-in: actually translating and adapting.","full":"<p><strong>l10n</strong> is the numeronym for <em>localization</em> — \"l\", 10 letters, \"n\". It is the process of adapting an internationalized product for a <em>specific</em> locale: translating user-facing text, adjusting date/time/number/currency formats, swapping culturally appropriate images or examples, handling sorting and pluralization rules, and respecting local conventions (addresses, units, legal text).</p><p>Like i18n, the abbreviation arose in the software-globalization industry of the late 1980s–1990s as products shipped worldwide. Localization is the downstream counterpart to internationalization: <strong>i18n</strong> makes adaptation <em>possible</em>; <strong>l10n</strong> performs the adaptation for each target market.</p><p>Its purpose is to make a product feel native to each audience — not merely translated, but culturally and conventionally correct. It remains a standard part of shipping global software in 2026, typically supported by translation-management tooling and locale resource files. The clean separation from i18n is what keeps localization affordable: each new locale is mostly translation plus configuration, not code changes.</p>","worked":"<p>Localization = providing locale-specific resources for an i18n-ready app:</p><pre><code>// en-US.json  (one locale's filled-in resources)\n{\n  \"checkout.button\": \"Checkout\",\n  \"cart.items\": \"{count, plural, one {# item} other {# items}}\"\n}\n\n// fr-FR.json  (the localized counterpart)\n{\n  \"checkout.button\": \"Commander\",\n  \"cart.items\": \"{count, plural, one {# article} other {# articles}}\"\n}\n\n// Pluralization rules differ per locale — l10n handles that,\n// often via ICU MessageFormat as shown above.</code></pre>","teach":"<p>At its core, l10n is just <strong>filling in the blanks for one specific place</strong>. Once your app is internationalized (i18n) — all its text externalized, all its formatting locale-aware — localization is the act of actually producing the French, Japanese, or Brazilian-Portuguese version: translating the strings, picking the right currency symbol, choosing images that make sense locally.</p><div class=\"analogy\">If i18n is building a house with empty picture frames on every wall, l10n is the day you hang the actual photos chosen for whoever's moving in. The Tokyo tenant gets different pictures than the Paris tenant — but because the frames (slots) already exist, you never touch the walls. Each new resident is a quick decorating job, not a renovation.</div><p>Good localization is more than word-for-word translation. It includes <strong>pluralization</strong> (English has \"1 item / 2 items,\" but many languages have several plural forms — often handled via ICU MessageFormat), <strong>collation</strong> (alphabetical sort order varies by language), culturally appropriate <strong>imagery and examples</strong>, local <strong>date/number/currency</strong> conventions, and even legal or regulatory text required in a region.</p><p>The reason l10n and i18n are split is economic. If you'd hard-coded English everywhere, every new language would be an engineering project. By doing the structural i18n work once, each additional locale becomes mostly a <em>translation and configuration</em> task — handled by translators and locale files, often through a translation-management system, without developers rewriting code.</p><div class=\"key\">The takeaway: <strong>l10n is the per-locale adaptation that i18n makes possible.</strong> It's not just translating words — it's making the product feel native: correct plurals, formats, sorting, and cultural fit, all slotted into the structure i18n built.</div>","related":["i18n","cjk"]}
});
