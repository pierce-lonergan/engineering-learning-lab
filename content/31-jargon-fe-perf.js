/* Frontend jargon & acronyms — clickable reference (FE-PERF, auto-assembled) */
Object.assign(GLOSSARY_DATA, {
  "fid": {"term":"FID","group":"FE-PERF","aliases":["First Input Delay"],"eli5":"FID (First Input Delay) was the now-retired Core Web Vital that measured only the delay between a user's first interaction and the moment the browser began running its event handler.","memory":"FID = First Input Delay — only the FIRST tap, and only the wait BEFORE the handler starts.","full":"<p><strong>What it is.</strong> First Input Delay (FID) was a Core Web Vital that measured <em>input delay</em> only: the time from a user's <strong>first</strong> interaction (click, tap, key press) until the browser could begin processing that interaction's event handler. Crucially, it measured neither how long the handler ran nor when the screen updated — just the initial wait, and only for the first interaction.</p><p><strong>History / origin.</strong> FID was introduced by Google's Chrome team in the original 2020 Web Vitals / Core Web Vitals launch as the responsiveness metric. It was <strong>replaced by Interaction to Next Paint (INP) as a Core Web Vital on March 12, 2024</strong>, after INP graduated from its experimental period.</p><p><strong>Purpose & why it was replaced.</strong> FID's purpose was to flag pages whose main thread was so busy during load that the first interaction couldn't be handled promptly. But it had two fatal blind spots: it ignored <em>processing time</em> and <em>presentation delay</em>, and it only looked at the <em>first</em> interaction — so a page could score a perfect FID while feeling sluggish on every subsequent click. INP was designed to fix exactly these gaps, and FID is now deprecated; you should optimize for INP instead, though understanding FID explains why INP is defined the way it is.</p>","worked":"<p>FID vs INP on the same interaction:</p><ul><li>A user taps a button at $t=0$. The main thread is busy until $t=120\\text{ms}$, then the handler runs for 200ms, then the paint lands at 350ms.</li><li><strong>FID = 120ms</strong> — only the input delay, only because it's the first interaction. Looks \"good\" (&lt;100ms is great, this is borderline).</li><li><strong>INP ≈ 350ms</strong> — input delay + processing + presentation, captured even on later interactions. Correctly flags this as poor.</li></ul><p>This gap is precisely why FID was retired: it under-reported real sluggishness.</p>","teach":"<p>At its core, FID was just <strong>the wait before the page even started reacting to your first click</strong> — and nothing more. It's worth learning as the metric INP was built to replace, because the contrast teaches you what responsiveness really is.</p><div class=\"analogy\">Picture knocking on a shop door. FID measured only how long until someone inside <em>started walking</em> toward the door — and only for the very first knock of the day. It told you nothing about how long they then took to actually open it, nor how every later knock went. You could have a shopkeeper who springs up instantly (great FID) but then fumbles with the locks for a full minute every single time (terrible real experience).</div><p>FID measured one slice of the interaction pipeline: <em>input delay</em>, the time the main thread was busy and couldn't yet dispatch your event handler. It deliberately stopped there. It never timed the handler running (<em>processing time</em>) or the resulting screen update (<em>presentation delay</em>), and it only ever recorded the <strong>first</strong> interaction in the whole visit.</p><p>That made FID easy to score well on and hard to trust. Pages were often loaded-but-busy at first interaction, so FID caught some real jank — but its narrow scope meant a page could pass FID and still infuriate users. The web-performance community recognized this, built the Event Timing API to measure full interaction latency across the lifecycle, and shipped INP. On March 12, 2024, INP officially took FID's place as the responsiveness Core Web Vital.</p><p>So in 2026, FID is a historical artifact: still occasionally seen in old dashboards and articles, but no longer a Core Web Vital and not something you optimize for. If you find yourself \"fixing FID,\" you're really fixing input delay — one of the three components INP now measures in full.</p><div class=\"key\">FID measured only the input delay of the first interaction — a narrow, easy-to-pass responsiveness proxy. It was replaced by INP on March 12, 2024, which measures full interaction latency across every interaction. Learn FID to understand why INP exists; optimize INP.</div>","related":["inp","cwv","lcp","tbt","rum"]},
  "cwv": {"term":"CWV","group":"FE-PERF","aliases":["Web Vitals"],"eli5":"CWV (Core Web Vitals) is Google's small set of real-user metrics — LCP for loading, CLS for visual stability, and INP for responsiveness — each judged independently at the 75th percentile of field data.","memory":"CWV = Core Web Vitals — the three vital signs of UX: load (LCP), stability (CLS), responsiveness (INP).","full":"<p><strong>What it is.</strong> Core Web Vitals (CWV) is Google's curated set of three user-centric performance metrics meant to capture the essential aspects of page experience: <strong>LCP</strong> (Largest Contentful Paint, loading; good ≤ 2.5s), <strong>CLS</strong> (Cumulative Layout Shift, visual stability; good ≤ 0.1), and <strong>INP</strong> (Interaction to Next Paint, responsiveness; good ≤ 200ms). Each is judged <em>independently</em> at the <strong>75th percentile</strong> of real-user (field) data — there is no single blended CWV score.</p><p><strong>History / origin.</strong> Core Web Vitals were announced by Google in 2020 as a subset of the broader \"Web Vitals\" program, and were rolled into Google Search's page-experience ranking signals starting in 2021. The set has evolved: the responsiveness vital was originally <strong>FID</strong>, which <strong>INP replaced on March 12, 2024</strong>. The metrics are surfaced through field tools (Chrome UX Report, the <code>web-vitals</code> JS library) and lab tools (Lighthouse).</p><p><strong>Purpose & why still used.</strong> CWV exists to give developers a small, standardized, outcome-focused scorecard for user experience, and to give Search a consistent quality signal. It is still very much in use in 2026: the three current vitals (LCP, CLS, INP) remain the page-experience benchmark, each assessed at p75 of field data so that the experience of slower real users — not lab averages — is what counts.</p>","worked":"<p>The current three, with thresholds and what they catch:</p><ul><li><strong>LCP ≤ 2.5s</strong> — loading: when the largest viewport element renders.</li><li><strong>CLS ≤ 0.1</strong> — visual stability: how much content unexpectedly jumps around.</li><li><strong>INP ≤ 200ms</strong> — responsiveness: interaction-to-next-paint (replaced FID on 2024-03-12).</li></ul><p>Scoring nuance: a page \"passes\" CWV only if all three are in the \"good\" band at the <strong>75th percentile</strong> of field data — so a fast median can still fail if the slowest quarter of users have a poor experience. There is no combined number; each verdict stands alone.</p>","teach":"<p>At its core, CWV is just <strong>three vital signs for a web page</strong>, the way a doctor takes pulse, blood pressure, and temperature. Google picked three measurements that, together, summarize whether a page <em>feels</em> good to use: does it load quickly, does it stay still, and does it respond promptly?</p><div class=\"analogy\">A nurse doesn't give you one fused \"health number.\" She reports pulse, blood pressure, and temperature separately, because each catches a different failure and each has its own healthy range. CWV works identically: LCP, CLS, and INP are three independent readings — you can ace two and fail the third, and the failing one tells you exactly what's wrong.</div><p>Each vital maps to a distinct user frustration. <strong>LCP</strong> (≤2.5s) — \"the page took forever to show me anything.\" <strong>CLS</strong> (≤0.1) — \"I went to tap a button and the layout jumped, so I tapped the wrong thing\" (caused by images without dimensions, late-loading ads/fonts). <strong>INP</strong> (≤200ms) — \"I clicked and nothing happened for a beat.\"</p><p>Two methodological points make CWV trustworthy. First, the <strong>75th percentile</strong>: a metric passes only if 75% of real visits hit the \"good\" band. That deliberately weights the experience of users on slower phones and networks, so you can't hide poor UX behind a fast lab machine. Second, <strong>field vs lab</strong>: the official CWV verdict comes from real-user (field/RUM) data — the Chrome UX Report and the <code>web-vitals</code> library — not from a single Lighthouse run. Lab tools are great for debugging and even substitute TBT for INP (since they can't observe real clicks), but they don't decide your CWV pass/fail.</p><p>Finally, CWV is a moving target by design — FID gave way to INP in 2024 as better measurement became possible. The lesson: treat CWV as the current best definition of \"good page experience,\" optimize each vital on its own terms, and always check your p75 field data, not your own fast machine.</p><div class=\"key\">CWV is three independent, user-centric metrics — LCP (load ≤2.5s), CLS (stability ≤0.1), INP (responsiveness ≤200ms) — each scored at the 75th percentile of real-user field data, with no combined score. INP replaced FID on March 12, 2024.</div>","related":["lcp","inp","fid","ttfb","fcp","tbt","rum"]},
  "ttfb": {"term":"TTFB","group":"FE-PERF","aliases":["Time to First Byte"],"eli5":"TTFB (Time to First Byte) measures how long from the start of a request until the browser receives the very first byte of the server's response — a proxy for server/network responsiveness.","memory":"TTFB = Time To First Byte — the wait before any of the response shows up.","full":"<p><strong>What it is.</strong> Time to First Byte (TTFB) is the duration from when the browser initiates a navigation request until it receives the first byte of the response. It rolls up several phases: redirects, DNS lookup, TCP connection, TLS negotiation, the request being sent, and the server's processing time before it starts sending bytes. It is a diagnostic metric, not itself a Core Web Vital, but it is the <em>foundation</em> on which loading metrics are built.</p><p><strong>History / origin.</strong> TTFB is a long-standing web-performance concept, exposed in browsers through the Navigation Timing and Resource Timing APIs (W3C standards). It predates the Core Web Vitals program and has been a staple of performance analysis for many years, available via the <code>PerformanceNavigationTiming</code> interface and most performance tooling.</p><p><strong>Purpose & why still used.</strong> TTFB isolates the <em>server and network</em> portion of load time from the browser-rendering portion, so you can tell whether slowness comes from a slow backend/CDN or from front-end work. It's still essential because it is the first term in LCP's breakdown — $\\text{LCP} = \\text{TTFB} + \\text{resource load delay} + \\text{resource load time} + \\text{element render delay}$ — so a bad TTFB caps how good LCP can ever be. A common pattern: static HTML gives a fast TTFB but FCP/LCP are still slow because nothing paints until JS downloads, parses, and fetches data.</p>","worked":"<p>TTFB sits at the front of the loading waterfall:</p><ul><li>Phases counted: redirect → DNS → TCP connect → TLS → request sent → <em>server thinks</em> → first byte arrives. That arrival is TTFB.</li><li>Everything after (download, parse, render) is <em>not</em> in TTFB.</li></ul><p>Diagnostic example:</p><ul><li><strong>Fast TTFB, slow LCP</strong>: static HTML returns instantly, but the page is a JS shell — nothing meaningful paints until the bundle downloads, parses, and fetches data. Fix the front end, not the server.</li><li><strong>Slow TTFB</strong>: a heavy server-rendered page with an uncached database query — every metric downstream inherits the delay. Add caching/a CDN, optimize the query.</li></ul>","teach":"<p>At its core, TTFB is just <strong>how long you wait before the server says its first word</strong>. Before a single pixel can paint, the browser has to ask a server for the page and get a response back. TTFB times that round trip up to the first byte of the answer.</p><div class=\"analogy\">Imagine ordering at a restaurant. TTFB is the gap between placing your order and the waiter bringing the <em>very first</em> thing — a basket of bread. It doesn't measure how long the full meal takes; it measures how responsive the kitchen is to start delivering at all. A slow first-byte is like a kitchen that takes ten minutes just to acknowledge your order — everything after that is already behind.</div><p>What's hidden inside that wait? Several steps: following any redirects, looking up the domain's IP (DNS), opening a connection (TCP), securing it (TLS), sending the request, and — usually the biggest chunk — the <em>server actually generating the response</em>. If your backend runs an uncached database query or renders a heavy template, that processing time lands squarely in TTFB.</p><p>Why obsess over it? Because TTFB is the <strong>floor</strong> for everything downstream. LCP literally begins with TTFB in its formula, so if your first byte takes 1.5s, your LCP cannot beat 1.5s no matter how perfectly you optimize images. Speeding up TTFB — via CDNs (serving from a server near the user), caching, faster backends, and connection reuse — lifts that floor for every other metric.</p><p>But there's a subtlety: a <em>good</em> TTFB doesn't guarantee a fast page. Single-page apps return a near-empty HTML shell instantly (great TTFB), yet the user stares at a blank screen while JavaScript downloads, parses, and fetches the real data — so FCP and LCP are awful. This is the canonical \"fast TTFB, slow paint\" trap, and it's exactly why server-side rendering exists: move meaningful content into that first response so a fast TTFB actually translates into fast pixels.</p><div class=\"key\">TTFB is the time to the first byte of the server's response — the server-plus-network portion of load, and the first term of LCP. A fast TTFB is necessary but not sufficient; you still have to paint something with those bytes.</div>","related":["lcp","fcp","cwv","rum"]},
  "fcp": {"term":"FCP","group":"FE-PERF","aliases":["First Contentful Paint"],"eli5":"FCP (First Contentful Paint) measures the time until the browser renders the first piece of actual content — any text, image, or non-blank pixel — telling the user something is happening.","memory":"FCP = First Contentful Paint — the first time ANY real content shows up on screen.","full":"<p><strong>What it is.</strong> First Contentful Paint (FCP) is the time from navigation start until the browser renders the <em>first</em> bit of DOM content — any text, image, SVG, or non-white canvas. Unlike LCP (the <em>largest</em> element), FCP marks the <em>earliest</em> moment the user sees something other than a blank screen, signaling that the page is loading.</p><p><strong>History / origin.</strong> FCP is part of the Web Vitals family, exposed via the Paint Timing API (a W3C/WICG standard) through <code>PerformanceObserver</code> entries of type <code>paint</code>. It predates and complements the Core Web Vitals; it is reported by Lighthouse and field tools but is <em>not</em> itself one of the three Core Web Vitals.</p><p><strong>Purpose & why still used.</strong> FCP measures perceived <em>early</em> feedback — how quickly the page reassures the user it's working — and is a key diagnostic between TTFB and LCP. It is still widely used as a supporting metric (e.g. in Lighthouse and CrUX): a fast TTFB but slow FCP/LCP reveals that nothing paints until JS downloads, parses, and fetches data. Techniques like server-side rendering improve FCP/SEO (content arrives in the initial HTML) but can worsen TTFB versus a static shell and don't remove the hydration JS that still gates interactivity.</p>","worked":"<p>FCP vs LCP — same page, two moments:</p><ul><li><strong>FCP</strong>: at 0.8s the header text and logo paint — the user sees <em>something</em>. First contentful paint fires.</li><li><strong>LCP</strong>: at 2.1s the big hero image finally renders — the <em>largest</em> content. LCP fires.</li></ul><p>Diagnostic patterns:</p><ul><li><strong>Fast TTFB, slow FCP</strong>: HTML arrives quickly but is a JS shell — the screen stays blank until the bundle downloads, parses, and fetches data. Render content server-side to fix.</li><li><strong>SSR trade-off</strong>: server-rendering puts real content in the first HTML response → better FCP and SEO, but TTFB may rise (server now does work) and hydration JS still ships.</li></ul>","teach":"<p>At its core, FCP is just <strong>the first sign of life</strong> — the instant the blank white screen turns into <em>any</em> real content. It answers the user's most anxious early question: \"is this thing even working?\"</p><div class=\"analogy\">Think of starting a campfire. FCP is the first visible flame — small, maybe just the kindling catching, but proof the fire is taking. LCP is the moment the big logs are fully ablaze and the fire is doing its job. You want that first flame fast, because a pile of wood that just sits there (a blank screen) makes everyone wonder if it'll ever light.</div><p>Mechanically, the browser fires FCP the moment it paints the first DOM content — a line of text, an image, an SVG. It sits in the loading timeline <em>after</em> TTFB (the bytes have to arrive first) and usually <em>before</em> LCP (the first content paints before the largest content). That position makes FCP a precise diagnostic: if TTFB is fast but FCP is slow, the bytes arrived but the browser couldn't paint — typically because render-blocking CSS/JS is in the way, or because the HTML is an empty shell waiting on JavaScript to build the page.</p><p>That last case is the heart of the SPA problem. A client-rendered app returns minimal HTML, so TTFB looks great, but FCP is delayed until the JS bundle downloads, parses, executes, and fetches data — only then does anything paint. The fix is server-side rendering: send real, content-filled HTML in the first response so the browser paints immediately, boosting FCP and helping SEO. The trade-offs are real, though — SSR makes the server do more work (TTFB can rise versus a static shell), and you still ship hydration JavaScript to make the page interactive, so FCP improving doesn't mean INP is solved.</p><div class=\"key\">FCP is the time to the first painted content — the earliest \"it's working\" signal, sitting between TTFB and LCP. It's a diagnostic, not a Core Web Vital: a slow FCP after a fast TTFB almost always means render-blocking resources or a JS shell with no content to paint.</div>","related":["ttfb","lcp","cwv","rum"]},
  "tbt": {"term":"TBT","group":"FE-PERF","aliases":["Total Blocking Time"],"eli5":"TBT (Total Blocking Time) is a lab metric that sums how long the main thread was blocked by long tasks between FCP and full interactivity, used as a proxy for responsiveness when real interactions can't be measured.","memory":"TBT = Total Blocking Time — add up all the 'main thread was too busy to respond' time.","full":"<p><strong>What it is.</strong> Total Blocking Time (TBT) is a lab performance metric that measures main-thread blocking during page load. It sums, over the window between First Contentful Paint and Time to Interactive, the <em>blocking portion</em> of every <em>long task</em> — a task is \"long\" if it runs more than 50ms, and only the time beyond that 50ms threshold counts as blocking. A high TBT means the main thread was frequently too busy to respond to input.</p><p><strong>History / origin.</strong> TBT was introduced by Google as part of Lighthouse and the Web Vitals tooling to give a <em>lab</em>-measurable stand-in for responsiveness. Because lab tools run without a real human clicking, they cannot measure field metrics like FID or INP directly; TBT was designed to correlate with them using only the load-time main-thread profile.</p><p><strong>Purpose & why still used.</strong> TBT's purpose is to surface main-thread congestion you can fix before shipping — long JavaScript tasks that would make real interactions sluggish. It is still used today: <strong>Lighthouse reports TBT as its responsiveness proxy</strong> in place of INP, which can only be assessed from field/RUM data. Optimizations that lower TBT — code-splitting so the browser only downloads/parses/executes the JS needed for the current route, and moving heavy work to Web Workers — also tend to improve the field INP.</p>","worked":"<p>How TBT counts blocking (only time past 50ms per task):</p><ul><li>A 70ms task → contributes <strong>20ms</strong> of blocking (70 − 50).</li><li>A 200ms task → contributes <strong>150ms</strong>.</li><li>A 40ms task → contributes <strong>0ms</strong> (not a long task).</li><li>TBT = sum of all such contributions between FCP and TTI.</li></ul><p>Fixing it:</p><ul><li><strong>Code-split</strong>: <code>const Chart = React.lazy(() =&gt; import('./Chart'))</code> ships only the current route's JS, shrinking the parse/execute cost and TBT.</li><li><strong>Offload</strong>: run parsing/crypto/large transforms in a Web Worker so those long tasks never block the main thread, lowering both TBT (lab) and INP (field).</li></ul>","teach":"<p>At its core, TBT is just <strong>a tally of how long your JavaScript hogged the main thread during load</strong>. The main thread is the single lane where the browser handles your scripts, layout, painting, <em>and</em> user input. If a long script is running, input has to wait — and TBT adds up all that waiting.</p><div class=\"analogy\">Picture a single checkout lane at a store. Most customers take a few seconds, but occasionally someone has a huge cart that monopolizes the register for minutes. TBT only counts the <em>excess</em> — the time past a reasonable 50-second courtesy limit — for each oversized customer, then sums it. A big TBT means lots of cart-hoggers, and anyone who walks up to ask a quick question (a user clicking) is stuck waiting.</div><p>The 50ms threshold encodes a real rule: a task under 50ms leaves enough headroom for the browser to still feel responsive, so it isn't \"blocking.\" Anything longer steals responsiveness, and only the part beyond 50ms is counted as the blocking contribution. Sum those contributions from FCP (first content) to TTI (fully interactive) and you have TBT.</p><p>Why does TBT exist when we have INP? Because of <em>where</em> you measure. INP needs a real human interacting on a real device — it's a field/RUM metric. But in a lab tool like Lighthouse, running on a CI server with no human, you can't observe real clicks. TBT is computable purely from the main-thread profile during load, and it correlates well with real-world responsiveness, so Lighthouse reports TBT as its INP stand-in. Lower your lab TBT and your field INP usually follows.</p><p>The fixes are the same in both worlds: stop running giant tasks on the main thread. <strong>Code-splitting</strong> ships only the JS the current route needs, so there's less to parse and execute up front. <strong>Web Workers</strong> move heavy work — parsing, crypto, image processing, big data transforms — onto a separate thread entirely, leaving the main thread free for input and rendering. Breaking long tasks into smaller chunks (yielding) directly chips away at the time past 50ms that TBT counts.</p><div class=\"key\">TBT sums the over-50ms blocking time of long main-thread tasks between FCP and TTI. It's the lab proxy Lighthouse uses for responsiveness because INP needs field data. Shrink it by code-splitting and offloading heavy work to Web Workers — which lifts INP too.</div>","related":["inp","fid","lcp","fcp","cwv","rum"]},
  "rum": {"term":"RUM","group":"FE-PERF","aliases":["Real User Monitoring","field data"],"eli5":"RUM (Real User Monitoring) collects performance metrics from actual visitors' real devices, networks, and interactions in production, as opposed to synthetic lab tests on a controlled machine.","memory":"RUM = Real User Monitoring — measure the page on real people, not a lab robot.","full":"<p><strong>What it is.</strong> Real User Monitoring (RUM) is the practice of collecting performance and experience data from <em>real visitors</em> as they use your site in production — their actual devices, networks, geographies, and interaction patterns. It is contrasted with <em>lab</em> (synthetic) testing, where a tool like Lighthouse measures the page under fixed, controlled conditions. RUM data is often called <strong>field data</strong>.</p><p><strong>History / origin.</strong> RUM as a discipline predates Web Vitals and grew out of the broader application-performance-monitoring industry. On the web it's powered by browser APIs (Navigation Timing, Resource Timing, Paint Timing, Event Timing, Long Tasks) plus libraries that report them. For Core Web Vitals specifically, Google supplies field/RUM data through the <strong>Chrome UX Report (CrUX)</strong> and the open-source <strong>web-vitals JavaScript library</strong>.</p><p><strong>Purpose & why still used.</strong> RUM exists because lab numbers can't capture the diversity of the real world — a metric that looks great on a fast developer machine can be terrible on a mid-range phone over a flaky network. This is why all three Core Web Vitals \"good/needs-improvement/poor\" verdicts are assessed at the <strong>75th percentile of field (RUM) data</strong>, not lab numbers. RUM is the authoritative source for CWV pass/fail and remains the standard for understanding true production experience; lab tools (and proxies like TBT) are for debugging.</p>","worked":"<p>RUM vs lab, side by side:</p><ul><li><strong>Lab</strong>: Lighthouse runs the page once on a throttled-but-fixed CPU/network. Reproducible; great for debugging; reports TBT as an INP proxy. But it's one synthetic run.</li><li><strong>RUM / field</strong>: the <code>web-vitals</code> library reports each real user's LCP/CLS/INP; CrUX aggregates them. Captures the real spread of devices and networks.</li></ul><p>The decisive rule: CWV verdicts use the <strong>p75 of field (RUM) data</strong>. So even if your lab LCP is 1.5s, if 25%+ of real users see &gt;2.5s (slow phones, far from your servers), you <em>fail</em> LCP. Minimal collection:</p><ul><li><code>import {onLCP, onINP, onCLS} from 'web-vitals'; onLCP(sendToAnalytics);</code> — beacons each real visit's metric to your backend.</li></ul>","teach":"<p>At its core, RUM is just <strong>measuring your site on the people who actually use it</strong>, instead of on your own fast laptop. The same page is a different experience for a developer on fiber and a shopper on a three-year-old phone with one bar of signal — and RUM captures that whole population, not a single idealized run.</p><div class=\"analogy\">Imagine a restaurant judging its food two ways. The <em>lab</em> way: the head chef tastes one perfect plate in the test kitchen — controlled, repeatable, but just one bite under ideal conditions. The <em>RUM</em> way: surveys from every real diner across a busy night — different tables, different orders, some served cold, some rushed. Only the diner surveys tell you what customers actually experience, and that's what your reputation is built on.</div><p>The technical distinction is <strong>field vs lab</strong>. Lab data (Lighthouse, WebPageTest) runs synthetically under fixed CPU/network throttling: reproducible and perfect for debugging a specific regression, and able to compute proxies like TBT. Field/RUM data comes from instrumenting real sessions — the browser's performance APIs report each visitor's LCP, CLS, and INP, and a tool aggregates them. Google's CrUX is a giant public RUM dataset; the <code>web-vitals</code> library lets you collect your own.</p><p>Why does this distinction decide everything for Core Web Vitals? Because the official verdicts are taken at the <strong>75th percentile of field data</strong>. The p75 choice is deliberate: it means three-quarters of real visits must hit \"good,\" so the slowest quarter — typically your most disadvantaged users — can't be averaged away. A page can look flawless in the lab and still fail CWV if real-world devices and networks drag the p75 past the threshold.</p><p>The practical workflow: use <em>lab</em> tools to find and fix problems (they're fast and reproducible), but trust <em>RUM</em> to tell you whether you actually passed. If lab and field disagree, field wins — it's the real users speaking.</p><div class=\"key\">RUM is performance measured on real users in production (field data), the source of truth for Core Web Vitals, which are judged at the 75th percentile of field data. Lab tools debug; RUM decides. When they conflict, believe the real users.</div>","related":["cwv","lcp","inp","fid","tbt","ttfb","fcp"]},
  "foit": {"term":"FOIT","group":"FE-PERF","aliases":["Flash of Invisible Text"],"eli5":"FOIT (Flash of Invisible Text) is when text stays invisible while a web font is still downloading, because the browser hides the text during the font's 'block period' rather than showing a fallback.","memory":"FOIT = Flash Of Invisible Text — the font isn't ready, so the words just... vanish.","full":"<p><strong>What it is.</strong> Flash of Invisible Text (FOIT) is the rendering behavior where text that's styled with a not-yet-loaded web font is rendered <em>invisibly</em> — the browser reserves the space but paints nothing — during the font's <em>block period</em>. The user sees blank gaps where words should be until the webfont arrives (or the block period times out and a fallback is swapped in). Its sibling is FOUT (Flash of Unstyled Text), where a fallback font shows first and then swaps.</p><p><strong>History / origin.</strong> FOIT became a widespread problem as custom web fonts (via <code>@font-face</code>) grew popular; different browsers historically chose different default behaviors (some blocking indefinitely, some with a timeout). The <code>font-display</code> CSS descriptor — part of the CSS Fonts specification developed under the W3C — was introduced to let authors control this trade-off explicitly, with values <code>auto</code>, <code>block</code>, <code>swap</code>, <code>fallback</code>, and <code>optional</code>.</p><p><strong>Purpose & why it matters today.</strong> FOIT isn't a feature — it's a failure mode to <em>avoid</em>, because invisible text hurts perceived performance and can block users from reading content. The modern remedy is <code>font-display: swap</code> (show fallback immediately, swap to webfont when ready) or <code>optional</code> (use the webfont only if it loads almost instantly), combined with <code>&lt;link rel=\"preload\"&gt;</code> for the font and subsetting to shrink it. This is still standard practice in 2026 to prevent both invisible text (FOIT) and the layout shift that font swaps can cause.</p>","worked":"<p>Causing vs curing FOIT with <code>font-display</code>:</p><ul><li><strong>FOIT (bad)</strong>: <code>@font-face { font-family: Brand; src: url(brand.woff2); font-display: block; }</code> — text is invisible during the block period while <code>brand.woff2</code> loads.</li><li><strong>Cured</strong>: <code>font-display: swap;</code> — a system fallback renders immediately, then swaps to Brand when it loads (no invisible text; small reflow possible).</li><li><strong>Cured, no shift</strong>: <code>font-display: optional;</code> — uses the webfont only if it's ready almost instantly, else sticks with the fallback for this load.</li></ul><p>Plus, in the head: <code>&lt;link rel=\"preload\" as=\"font\" type=\"font/woff2\" href=\"brand.woff2\" crossorigin&gt;</code> and subset the font to the glyphs you use.</p>","teach":"<p>At its core, FOIT is just <strong>the browser choosing silence over a substitute</strong>. When your CSS asks for a custom font that hasn't downloaded yet, the browser has two choices: show the text in a fallback font now, or hide the text and wait. FOIT is the second choice — and the result is blank space where your words should be.</p><div class=\"analogy\">Imagine a singer who refuses to perform until their exact preferred microphone arrives. The audience sits in silence, staring at an empty stage, even though a perfectly good backup mic is right there. That's FOIT: the browser would rather show <em>nothing</em> than show your text in a \"wrong\" font, so the user waits in the dark. FOUT is the opposite singer — they grab the backup mic and start immediately, then switch mics mid-song (a visible swap, but no silence).</div><p>Why is invisible text so bad? It directly hurts perceived performance — a user who can't read anything assumes the page is broken or slow, even if the layout is otherwise ready. And if the network is flaky, the block period can drag, leaving content unreadable for seconds.</p><p>The cure is the <code>font-display</code> descriptor inside <code>@font-face</code>. <code>swap</code> tells the browser: render a fallback font immediately, then swap in the webfont when it loads — you eliminate invisible text entirely, at the cost of a possible reflow when the swap happens. <code>optional</code> is even stricter for performance: use the webfont only if it loads almost instantly, otherwise keep the fallback for this page load — no FOIT and no jarring swap. <code>block</code> is the FOIT-causing value; <code>fallback</code> is a middle ground.</p><p>Two companions complete the fix. <strong>Preload</strong> (<code>&lt;link rel=\"preload\" as=\"font\"&gt;</code>) starts the font download early so the swap happens sooner and the fallback period is short. <strong>Subsetting</strong> strips the font to only the characters you actually use, shrinking the file so it arrives faster. Together — <code>swap</code>/<code>optional</code> + preload + subset — they kill invisible text and minimize the layout shift (which feeds CLS) that a late font swap would otherwise cause.</p><div class=\"key\">FOIT is invisible text during a webfont's block period — the browser hiding words rather than showing a fallback. Fix it with font-display: swap (or optional), preload the font, and subset it, so text is readable instantly and layout stays stable.</div>","related":["lcp","fcp","cwv"]},
  "fout": {"term":"FOUT","group":"FE-PERF","aliases":["Flash of Unstyled Text"],"eli5":"The brief moment where a page shows text in a fallback system font, then swaps it to the real webfont once it finishes downloading.","memory":"FOUT = Flash Of Unstyled Text — you SEE the text (unstyled) first, then it changes.","full":"<p><strong>FOUT (Flash of Unstyled Text)</strong> is the visible phenomenon where the browser renders text immediately using a fallback (usually a local system) font while a custom web font is still downloading, and then re-renders that same text in the web font once it arrives. The swap can shift glyph widths and line breaks, causing a small reflow that contributes to <code>CLS</code> (Cumulative Layout Shift).</p><p>The term was popularized by web typography writers in the late 2000s/early 2010s as web fonts (via <code>@font-face</code>) became mainstream. It is the counterpart to <strong>FOIT (Flash of Invisible Text)</strong>, where the browser instead hides the text entirely during the font's \"block period\" and shows nothing until the web font loads (or a timeout fires). Modern browsers expose direct control over this trade-off through the CSS <code>font-display</code> descriptor (values like <code>swap</code>, <code>block</code>, <code>fallback</code>, <code>optional</code>), which became widely supported across the major engines in the late 2010s.</p><p>The purpose of understanding FOUT is to manage the trade-off between content availability and visual stability: FOUT keeps text readable instantly (great for perceived performance and accessibility) at the cost of a visible swap. It remains relevant today because every site using custom fonts faces this choice; the modern best practice is to deliberately opt into FOUT with <code>font-display: swap</code> (or <code>optional</code> to avoid the swap entirely), pair it with a carefully size-matched fallback font and the <code>size-adjust</code>/<code>ascent-override</code> descriptors to minimize the layout shift, and preload critical fonts.</p>","worked":"<p>Opt into FOUT explicitly so text is never invisible while the web font loads:</p><ul><li><code>@font-face { font-family: \"Inter\"; src: url(/inter.woff2) format(\"woff2\"); font-display: swap; }</code></li><li><code>body { font-family: \"Inter\", system-ui, sans-serif; }</code></li></ul><p>Reduce the swap's layout shift by matching the fallback metrics:</p><ul><li><code>@font-face { font-family: \"Inter\"; size-adjust: 98%; ascent-override: 90%; src: url(/inter.woff2) format(\"woff2\"); }</code></li><li>Preload the file: <code>&lt;link rel=\"preload\" href=\"/inter.woff2\" as=\"font\" type=\"font/woff2\" crossorigin&gt;</code></li></ul>","teach":"<p>At its core, FOUT is just the browser refusing to make you wait to read words. A web font is a file that has to be downloaded over the network, but the text content is already in the HTML. So the browser has a choice: show the text now in whatever font it already has, or hide the text until the fancy font arrives. FOUT is the \"show it now\" choice — you get a <em>flash</em> of the text in an <em>unstyled</em> (fallback) font, then it swaps.</p><div class=\"analogy\">Imagine a wedding invitation that has to go out today, but the calligrapher is stuck in traffic. FOUT is printing the invitation in plain typewriter font so guests can read the date immediately, then mailing a beautiful hand-lettered version that arrives the next day. FOIT is the opposite: you mail an envelope that's blank inside until the calligrapher shows up — readable late, but nobody's left staring at nothing… except they are.</div><p>The reason this matters is twofold. First, <strong>readability</strong>: invisible text (FOIT) is a real accessibility and UX problem — users see a blank gap where content should be. Second, <strong>stability</strong>: when the swap happens, the new font's letters may be wider or taller, so a paragraph might re-wrap and push everything below it down. That jump is measured by Cumulative Layout Shift, one of the Core Web Vitals, and it feels janky.</p><p>You control all of this with one CSS descriptor, <code>font-display</code>. <code>swap</code> forces FOUT (show fallback immediately, swap whenever the font loads). <code>block</code> leans toward FOIT (hide briefly). <code>optional</code> is the cleverest: use the web font only if it's already cached/fast, otherwise just keep the fallback and never swap — zero layout shift. To make the swap nearly invisible, you tune the fallback to match the web font's size using descriptors like <code>size-adjust</code> and <code>ascent-override</code>.</p><div class=\"key\">FOUT means text appears instantly in a fallback font and later swaps to the web font. It's usually the <em>preferred</em> behavior (<code>font-display: swap</code>) because invisible text is worse than a swap — but you should minimize the swap's layout shift by metric-matching your fallback font.</div>","related":["fouc","cssom"]},
  "fouc": {"term":"FOUC","group":"FE-PERF","aliases":["Flash of Unstyled Content"],"eli5":"The brief, ugly moment where a page appears with no styling (raw HTML) before its CSS loads and snaps it into its intended design.","memory":"FOUC = Flash Of Unstyled Content — like FOUT but the whole page, not just text.","full":"<p><strong>FOUC (Flash of Unstyled Content)</strong> is the visual glitch where a browser paints the raw, unstyled HTML of a page for a moment before the CSS has been downloaded and applied, after which the page abruptly \"snaps\" into its styled appearance. Because CSS is render-blocking by design, this normally <em>doesn't</em> happen — but it can appear when stylesheets are loaded late, asynchronously, or via JavaScript, or when style-hiding tricks are used.</p><p>The term dates back to the early 2000s and is most associated with David Hyatt (a browser engineer, then at Netscape/Mozilla and later WebKit), who described the behavior in the context of how browsers decide when to paint. The general rule that emerged — and which browsers still follow — is that <code>&lt;link rel=\"stylesheet\"&gt;</code> and <code>&lt;style&gt;</code> in the <code>&lt;head&gt;</code> block rendering until the CSSOM is built, precisely so that the first paint is already styled and FOUC is avoided.</p><p>Its purpose as a concept is to explain the relationship between the parsing of HTML, the construction of the <code>CSSOM</code>, and the first paint. FOUC remains relevant today because modern patterns reintroduce it: critical-CSS strategies, lazily/async-loaded stylesheets, web components whose styles arrive with JS, and frameworks that inject styles client-side can all flash unstyled content. The standard mitigations are to keep render-blocking CSS in the head, inline critical CSS, avoid hiding content with JS that can fail, and (for components) use techniques that keep markup hidden until styles are ready without leaving content permanently invisible.</p>","worked":"<p>FOUC is avoided by the default behavior — render-blocking CSS in the head:</p><ul><li><code>&lt;head&gt;&lt;link rel=\"stylesheet\" href=\"/styles.css\"&gt;&lt;/head&gt;</code> — the browser won't paint until this CSS is parsed.</li></ul><p>A common way to <em>cause</em> FOUC is loading CSS asynchronously without care:</p><ul><li><code>&lt;link rel=\"stylesheet\" href=\"/styles.css\" media=\"print\" onload=\"this.media='all'\"&gt;</code> — non-blocking, so unstyled HTML may flash first. Pair it with inlined critical CSS in a <code>&lt;style&gt;</code> tag to prevent the flash.</li></ul>","teach":"<p>At its core, FOUC is just the browser showing you the page <em>before</em> it knows what the page is supposed to look like. HTML describes the content (headings, paragraphs, lists); CSS describes the appearance (colors, fonts, layout). These arrive as separate files. If the browser painted the HTML the instant it parsed it, you'd see Times New Roman, black-on-white, everything stacked vertically — and then a fraction of a second later the CSS would arrive and reshape it all. That jarring snap is the Flash of Unstyled Content.</p><div class=\"analogy\">Think of a stage play. The script (HTML) tells the actors what to say and do. The lighting, costumes, and set (CSS) tell us what it all looks like. FOUC is raising the curtain while the actors are standing around in street clothes under fluorescent work-lights — the words are technically all there, but it looks wrong, and then suddenly the real set drops in. Browsers avoid this by keeping the curtain down until the set is ready.</div><p>The way browsers prevent FOUC is by treating CSS as <strong>render-blocking</strong>. When the browser hits a <code>&lt;link rel=\"stylesheet\"&gt;</code> in the <code>&lt;head&gt;</code>, it will not perform that first paint until it has downloaded the CSS and built the CSSOM (the CSS object model). Combining the DOM and CSSOM gives the render tree, and only then does it paint. So a styled-from-the-start experience is actually the <em>default</em>.</p><p>FOUC sneaks back in when you defeat that default — usually for performance. If you load CSS asynchronously to speed up first paint, or if a JavaScript framework injects styles after the HTML is already on screen, or a web component's styles arrive with its script, you can get a flash. The fix is to inline the small amount of \"critical\" CSS needed for the initial view directly in the head (so the first paint is styled), and load the rest asynchronously.</p><div class=\"key\">FOUC is unstyled HTML flashing before CSS applies. It normally doesn't happen because CSS in the head is render-blocking — but async CSS, JS-injected styles, and web components can bring it back. Inline critical CSS to keep the first paint styled.</div>","related":["fout","cssom","csr"]},
  "tti": {"term":"TTI","group":"FE-PERF","aliases":["Time to Interactive","Time-to-Interactive"],"eli5":"A performance metric marking the moment a page has finished loading enough that it reliably responds to user input quickly.","memory":"Time To Interactive — the clock stops when the page can actually be poked without lag.","full":"<p><strong>What it is.</strong> <strong>Time to Interactive (TTI)</strong> is a web performance metric that measures how long it takes a page to become reliably interactive — meaning the page has displayed useful content, event handlers are registered for visible elements, and the main thread is quiet enough that it can respond to user input within a short delay (commonly ~50ms). It is a lab/synthetic metric: it captures the gap between a page that <em>looks</em> ready and one that actually <em>is</em> ready.</p><p><strong>History / origin.</strong> TTI was introduced by the Google Chrome / web performance community and became a headline metric in <strong>Lighthouse</strong> and the Web Vitals family in the late 2010s. It rose to prominence precisely because single-page apps and heavy JavaScript bundles created the now-famous gap where pixels paint quickly but the page is frozen while large scripts parse and execute (often during hydration).</p><p><strong>Purpose &amp; why it changed.</strong> TTI's purpose was to quantify that frozen-but-painted window and pressure teams to ship less main-thread JavaScript. However, TTI proved noisy and hard to interpret, and it has been de-emphasized: Google's Core Web Vitals now favor <strong>INP (Interaction to Next Paint)</strong> for responsiveness and <strong>TBT (Total Blocking Time)</strong> as the lab proxy. TTI is still referenced and still computed by some tools, but the modern responsiveness story is told primarily through INP/TBT rather than TTI alone.</p>","worked":"<p>The classic TTI problem in a server-rendered React app:</p><ul><li>0.8s — HTML arrives, text and buttons <em>paint</em> (looks done).</li><li>0.8s–3.5s — a large JS bundle downloads, parses, and <strong>hydrates</strong>; the main thread is blocked. Clicks do nothing.</li><li>3.5s — hydration finishes, handlers attached → <strong>TTI reached</strong>.</li></ul><p>A user who taps a button at 1.2s gets no response for 2.3s — the gap TTI is designed to expose.</p>","teach":"<p>At its core, TTI is just a stopwatch answering one question: <em>from when the user asked for the page, how long until they can actually use it?</em></p><p>The subtlety is that \"looks ready\" and \"is ready\" are different moments. A browser can paint your headline, buttons, and form in under a second. But those buttons are inert until JavaScript has downloaded, parsed, executed, and wired up their click handlers. In a heavy app — especially one that hydrates server-rendered HTML — that wiring can take several more seconds, during which the main thread is busy and ignores taps.</p><div class=\"analogy\">It's like a shop that flips its window sign to OPEN while the cashier is still in the back counting the till. You walk in, grab items, go to pay — and nobody's at the register. The lights are on, the shelves are stocked, but you can't actually transact yet. TTI measures the moment the cashier finally arrives at the counter.</div><p>To declare TTI reached, tools look for a stretch of time where the main thread is quiet (no long tasks blocking it for, say, 5 seconds) after content has painted, then walk back to the last long task before that quiet window. That's the instant the page stopped being janky and started reliably answering input.</p><p>Why does this matter? Because the gap between paint and interactivity is where users rage-tap, assume the site is broken, and leave. TTI was invented to put a number on that gap so teams would ship less blocking JavaScript.</p><p>One honest caveat: TTI turned out to be jittery — a single late script could swing it wildly — so the industry moved on. Today you'll more often hear about <strong>TBT</strong> (total time the main thread was blocked, a lab signal) and <strong>INP</strong> (real-world measured responsiveness to actual interactions). TTI is the historically important ancestor of those metrics.</p><div class=\"key\">TTI clocks the gap between a page looking done and being usable. It exposed the hydration freeze where painted buttons don't respond — and it's the conceptual parent of today's TBT and INP responsiveness metrics.</div>","related":["hydration"]},
  "avif": {"term":"AVIF","group":"FE-PERF","aliases":["AV1 Image File Format"],"eli5":"A modern, highly efficient image format derived from the AV1 video codec, giving smaller files than JPEG or WebP at similar quality.","memory":"AVIF = AV1 Image File Format: the AV1 video codec, frozen into a single picture.","full":"<p><strong>AVIF</strong> (AV1 Image File Format) is a modern image format that stores still images using the intra-frame compression of the <strong>AV1 video codec</strong>. Because AV1 is a state-of-the-art codec, AVIF typically achieves notably smaller file sizes than JPEG or WebP at comparable visual quality, especially for photographic content, and supports HDR, wide color gamut, transparency, and animation.</p><p>AV1 itself was developed by the <strong>Alliance for Open Media (AOMedia)</strong> — a consortium including Google, Mozilla, Netflix, Amazon, and others — with the codec finalized around 2018; the AVIF image container specification followed around 2019. Browser support rolled out progressively (Chrome and Firefox first, then Safari), and by the mid-2020s AVIF is broadly supported across evergreen browsers.</p><p>Its purpose is to cut image payload — usually the largest contributor to page weight — improving load time and Core Web Vitals. It remains a top choice in 2026 for serving photos, though the tradeoff noted in the deck is real: AVIF <em>encoding</em> is slower and more CPU-heavy than WebP/JPEG. The standard pattern is to offer AVIF first and fall back to WebP/JPEG via the <code>&lt;picture&gt;</code> element.</p>","worked":"<p>Serving AVIF with graceful fallback using <code>&lt;picture&gt;</code>:</p><pre><code>&lt;picture&gt;\n  &lt;source srcset=\"hero.avif\" type=\"image/avif\"&gt;\n  &lt;source srcset=\"hero.webp\" type=\"image/webp\"&gt;\n  &lt;img src=\"hero.jpg\" alt=\"Mountain at dawn\"\n       width=\"1200\" height=\"800\"&gt;\n&lt;/picture&gt;</code></pre><p>The browser picks the first format it supports — AVIF if it can, otherwise WebP, otherwise the JPEG. Explicit <code>width</code>/<code>height</code> reserve space to avoid layout shift.</p>","teach":"<p>At its core, AVIF is just a <strong>single video frame saved as a picture</strong>. Video codecs are extraordinarily good at squeezing detail into few bytes, because compressing thousands of frames is their whole job. AVIF takes the still-image (intra-frame) compression from the AV1 video codec and uses it to store one image — inheriting all that hard-won efficiency.</p><div class=\"analogy\">Think of a master chef who's spent years perfecting how to vacuum-pack a week's worth of meals into a tiny cooler. AVIF asks that same chef to pack just one sandwich. The technique is overkill-good, so the sandwich ends up far smaller and tighter than if a regular cook had wrapped it. The catch: the master's careful packing <em>takes longer to do</em> — that's AVIF's slow, CPU-heavy encode.</div><p>Why does image format matter so much? On most web pages, images are the single biggest chunk of bytes downloaded. Shrink them and the page loads faster, which directly improves <strong>Core Web Vitals</strong> like Largest Contentful Paint. AVIF often beats WebP by a meaningful margin on photos, and WebP itself beats old JPEG — so the size ladder is roughly AVIF &lt; WebP &lt; JPEG.</p><p>But you can't just ship AVIF and hope: a few older browsers or tools may not decode it. That's why the <strong><code>&lt;picture&gt;</code></strong> element exists — you list AVIF first, WebP second, and a JPEG as the universal fallback. The browser walks the list top-to-bottom and uses the first format it understands. You also pair this with <code>srcset</code>/<code>sizes</code> to serve correctly sized versions and <code>width</code>/<code>height</code> to prevent layout shift.</p><div class=\"key\">The takeaway: <strong>AVIF gives the best compression for photos by borrowing the AV1 video codec</strong> — the price is slower encoding, so you generate it once at build time and serve it with a WebP/JPEG fallback.</div>","related":["webp","seo"]},
  "webp": {"term":"WebP","group":"FE-PERF","aliases":["Web Picture format"],"eli5":"A modern image format from Google that compresses photos and graphics smaller than JPEG and PNG while supporting transparency and animation.","memory":"WebP = Web Picture: Google's smaller-than-JPEG image for the web.","full":"<p><strong>WebP</strong> is a modern raster image format that offers both lossy and lossless compression, transparency (alpha channel), and animation in a single format — typically producing smaller files than JPEG (lossy) or PNG (lossless) at equivalent quality. Its lossy mode derives from the intra-frame compression of the <strong>VP8 video codec</strong>.</p><p>WebP was created by <strong>Google</strong> and introduced around 2010, with lossless and alpha-transparency support added a couple of years later. Browser adoption was gradual — Chrome and Android first, with Safari and the broader ecosystem following — and by the late 2010s/early 2020s it became broadly supported across all major browsers, making it a safe default.</p><p>Its purpose is to reduce image payload to speed up page loads. It sits between old formats (JPEG/PNG) and the newest (AVIF) on the compression ladder: WebP usually beats JPEG/PNG, while AVIF usually beats WebP — but WebP encodes faster and has near-universal support, so it's an excellent fallback or default. The deck also flags that WebP bytes are <em>already compressed</em>, so re-compressing them (e.g. with gzip/brotli on the wire) wastes CPU for near-zero gain.</p>","worked":"<p>WebP as the fallback below AVIF, and the \"don't re-compress\" rule:</p><pre><code>&lt;picture&gt;\n  &lt;source srcset=\"photo.avif\" type=\"image/avif\"&gt;\n  &lt;source srcset=\"photo.webp\" type=\"image/webp\"&gt;\n  &lt;img src=\"photo.jpg\" alt=\"\" width=\"800\" height=\"600\"&gt;\n&lt;/picture&gt;\n\n# Server config: skip recompressing already-compressed image bytes\n# (gzip-ing a .webp/.jpg/.png/.woff2 burns CPU for ~0 savings)\ngzip_types text/css application/javascript;  # NOT image/webp</code></pre>","teach":"<p>At its core, WebP is just a <strong>better-squeezed image file built for the web</strong>. It borrows compression tricks from video (the VP8 codec) to make photos smaller than JPEG, and it can also do lossless compression with transparency, so it covers PNG's use cases too — all in one format.</p><div class=\"analogy\">Think of upgrading from a regular suitcase (JPEG/PNG) to a vacuum-seal travel bag (WebP). The same clothes fit into less space, so your luggage is lighter to carry — that's a faster page load. AVIF would be an even tighter vacuum bag, but WebP is the one almost every airline already accepts, which is why it's such a reliable default.</div><p>Two practical points the deck emphasizes. First, <strong>format selection</strong>: you serve WebP via <code>srcset</code>/<code>&lt;picture&gt;</code>, usually as the fallback right below AVIF and above the old JPEG/PNG. Because WebP support is now essentially universal, it's a safe baseline if you don't want to ship AVIF.</p><p>Second, <strong>don't double-compress</strong>. Formats like WebP, JPEG, PNG, and woff2 fonts are <em>already compressed</em> internally. When your server then tries to gzip or brotli them on the way out, it spends CPU re-squeezing bytes that are already near their theoretical minimum — the file barely shrinks, so it's wasted work. You configure your server to apply text compression to CSS/JS/HTML but skip already-compressed binary assets.</p><div class=\"key\">The takeaway: <strong>WebP is the dependable modern image default</strong> — smaller than JPEG/PNG, near-universal support, fast to encode. Serve it via <code>&lt;picture&gt;</code> alongside AVIF, and never gzip already-compressed image bytes.</div>","related":["avif","seo"]},
  "gpu": {"term":"GPU","group":"FE-PERF","aliases":["Graphics Processing Unit"],"eli5":"The specialized chip that renders graphics; browsers offload compositing of layers to it for smooth animations.","memory":"GPU = Graphics Processing Unit: the chip built to push pixels, used to composite browser layers fast.","full":"<p><strong>GPU</strong> (Graphics Processing Unit) is a specialized processor designed for highly parallel work — originally rendering 3D graphics, now also used for compute. In a browser's rendering pipeline, the GPU handles the final <strong>composite</strong> step: stitching together independently-painted layers into the image you see on screen, and doing so fast enough for smooth 60fps animation.</p><p>The term GPU was popularized by <strong>NVIDIA</strong> around 1999 with the GeForce 256, marketed as the first \"GPU,\" though dedicated graphics hardware predates that. In browsers, GPU-accelerated compositing became standard over the 2010s as engines moved to layer-based rendering, letting transforms and opacity changes animate without re-running expensive layout or paint.</p><p>Its relevance to frontend is the rendering pipeline: <code>HTML → DOM</code> and <code>CSS → CSSOM</code> are built in parallel, combined into the render tree, then <em>layout</em> (reflow/geometry) → <em>paint</em> (raster) → <em>composite</em> (GPU layers). Properties like <code>transform</code> and <code>opacity</code> can be composited on the GPU cheaply. But promoting an element to its own layer (e.g. via <code>will-change</code>) costs GPU memory — the deck warns that putting <code>will-change</code> on hundreds of elements can degrade performance and exhaust memory.</p>","worked":"<p>Animating a GPU-friendly property vs. forcing layout:</p><pre><code>/* Cheap: transform animates on the GPU compositor,\n   no layout or paint per frame */\n.card { transition: transform 200ms; }\n.card:hover { transform: translateY(-4px) scale(1.02); }\n\n/* Hint the browser to promote to its own GPU layer —\n   use SPARINGLY; each layer costs GPU memory */\n.modal { will-change: transform, opacity; }\n\n/* AVOID: animating 'top' triggers layout (reflow) every frame */\n.bad { transition: top 200ms; } /* janky */</code></pre><p>Putting <code>will-change</code> on hundreds of elements would exhaust GPU memory and backfire.</p>","teach":"<p>At its core, a GPU is just a <strong>chip with thousands of tiny workers that all paint pixels at once</strong>. A CPU has a few very fast, general-purpose cores; a GPU has a huge number of simpler cores tuned for doing the same operation across millions of pixels in parallel. That makes it perfect for the browser's final job: drawing the composed image to the screen many times per second.</p><div class=\"analogy\">Think of painting a giant mural. A CPU is one brilliant artist who can paint anything but only one stroke at a time. A GPU is an army of a thousand painters, each assigned one small square — together they fill the whole wall almost instantly, as long as the task splits cleanly. Pushing pixels splits perfectly, so the GPU wins.</div><p>To use it, browsers render in stages. First <strong>layout</strong> figures out where every box goes (geometry). Then <strong>paint</strong> fills in colors, text, and borders onto layers (rasterization). Finally <strong>composite</strong> hands those layers to the GPU, which slides, fades, and stacks them into the final frame. The trick to smooth animation is to only change things in that <em>last</em> step.</p><p>That's why <code>transform</code> and <code>opacity</code> are special: animating them only re-composites on the GPU — no layout, no repaint — so they stay buttery at 60fps. Animating something like <code>top</code> or <code>width</code> instead forces <strong>layout (reflow)</strong> every frame, which is expensive and janky.</p><p>You can hint the browser to give an element its own GPU layer with <code>will-change</code>. But every promoted layer consumes <strong>GPU memory</strong>. Sprinkle <code>will-change</code> on hundreds of elements and you exhaust that memory, causing the opposite of what you wanted — slower rendering and stutter.</p><div class=\"key\">The takeaway: <strong>the GPU makes compositing fast, so animate <code>transform</code>/<code>opacity</code> and use <code>will-change</code> sparingly</strong> — layers aren't free, and too many drain GPU memory.</div>","related":["avif","webp"]}
});
