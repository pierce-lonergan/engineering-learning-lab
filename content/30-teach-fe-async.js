/* Teach me from the ground up: FE-ASYNC (auto-assembled) */
enh({
  "event-loop": {"teach":"<p>At its core, the <strong>event loop</strong> is just a tireless dispatcher standing next to a single worker, repeatedly asking: \"Is the worker free? Then here's the next waiting job.\"</p><h4>The setup</h4><p>JavaScript has exactly one worker that actually runs your code — the <strong>call stack</strong>. But programs constantly need to <em>wait</em>: for a timer, a network reply, a click. If the single worker sat idle waiting, your whole app would freeze. So the slow stuff is handed off to the <strong>host</strong> (the browser or Node), and when it's done, the host drops a callback into a queue. The event loop's whole job is to feed those queued callbacks back to the worker whenever it's idle.</p><div class=\"analogy\"><p>Picture a kitchen with one chef (the call stack). Orders come in on tickets. The chef cooks one ticket to completion — no abandoning a half-made dish to start another. A host stands at the pass, and the moment the chef's hands are empty, the host hands over the next ticket. Slow things like \"let the dough rise\" get parked on a side counter; when the timer dings, that ticket joins the queue. The chef is never two places at once, but tickets keep flowing.</p></div><h4>One tick, deconstructed</h4><p>Each loop iteration (a \"tick\") does three things in strict order:</p><ul><li>Run <strong>one macrotask</strong> to completion (e.g. one timer callback or click handler).</li><li>Then <strong>fully drain the microtask queue</strong> — every promise reaction, including ones scheduled by other microtasks.</li><li>Then <strong>render</strong> if the screen is due for a repaint.</li></ul><p>Because there is one stack and each job runs to completion, JS is <strong>run-to-completion</strong>: nothing ever preempts your code mid-function. That's a gift (no surprise interruptions, no data races on the main thread) and a curse (one long synchronous loop freezes everything).</p><h4>Why it matters</h4><p>The loop gives you <strong>concurrency, not parallelism</strong> — jobs are interleaved on one thread, not run side by side. Heavy CPU work belongs in a Web Worker; otherwise it monopolizes the chef and the UI locks up. Trace this: <code>console.log(1); setTimeout(()=>console.log(3)); Promise.resolve().then(()=>console.log(2));</code> prints <strong>1, 2, 3</strong> — synchronous first, then the microtask, then the timer macrotask on a later tick.</p><div class=\"key\">The event loop runs one macrotask, drains <em>all</em> microtasks, then maybe renders — and it gives you concurrency (interleaving), never true parallelism.</div>"},
  "call-stack": {"teach":"<p>At its core, the <strong>call stack</strong> is just a to-do list of the functions running <em>right now</em>, stacked so the most recently started one is always the one being worked on.</p><h4>Push and pop</h4><p>Every time you call a function, the engine adds a <strong>frame</strong> on top of the stack — that frame holds the function's local variables and where to return when it finishes. When the function returns, its frame is <strong>popped</strong> off, and control goes back to whatever called it. The function on top is always the one currently executing.</p><div class=\"analogy\"><p>Think of a stack of cafeteria trays. You always take from and add to the <em>top</em>. If tray A asks tray B for help, you set B on top of A; B can't finish until you lift it off, and only then does A continue. \"Last on, first off\" — the newest call is always the first to complete. This is exactly LIFO order.</p></div><h4>One stack, one realm</h4><p>JavaScript has <strong>exactly one</strong> call stack per realm. That single-ness is the whole reason two of your functions never run at literally the same instant on the main thread. It's also why the event loop will only place a queued callback onto the stack when the stack is <strong>empty</strong> — there's no slot to interrupt into.</p><p>Trace it: <code>function a(){b();} function b(){c();} a();</code>. Calling <code>a</code> pushes <code>a</code>; <code>a</code> calls <code>b</code> so <code>b</code> goes on top; <code>b</code> calls <code>c</code>, pushing <code>c</code>. The stack is now a→b→c. Then it unwinds: <code>c</code> returns and pops, then <code>b</code>, then <code>a</code> — c→b→a.</p><h4>When it bites</h4><ul><li><strong>Deep recursion</strong> keeps pushing frames without popping. Overflow the stack's size limit and you get <code>RangeError: Maximum call stack size exceeded</code>.</li><li>A <strong>long synchronous loop</strong> keeps one frame parked on the stack for ages. While it sits there, the stack is never empty, so <em>no</em> async callback can run and the page can't even repaint. The UI freezes.</li></ul><p>This is the crux of a common misconception: people imagine an incoming click or timer can \"interrupt\" running synchronous code. It cannot. The callback simply waits in its queue until your current call chain fully unwinds and the stack goes empty.</p><div class=\"key\">There is one call stack; async callbacks never interrupt running sync code — they wait until the stack is completely empty before the event loop places them on it.</div>"},
  "microtask": {"teach":"<p>At its core, a <strong>microtask</strong> is just a high-priority \"do this the instant the current code finishes, before anything else gets a turn\" job.</p><h4>Who creates them</h4><p>Microtasks are queued by:</p><ul><li>Promise reactions — <code>.then</code>, <code>.catch</code>, <code>.finally</code> callbacks;</li><li><code>await</code> continuations (the code after an <code>await</code>);</li><li><code>queueMicrotask()</code>;</li><li><code>MutationObserver</code> callbacks.</li></ul><h4>The draining rule, dissected</h4><p>Here's the sentence that trips people up: \"At each microtask checkpoint the loop <strong>drains the entire queue</strong> — including microtasks scheduled by other microtasks — before taking the next task or rendering.\" Let's take it apart:</p><ul><li><strong>Microtask checkpoint</strong> — the moment right after the call stack empties (after the current sync code or current macrotask finishes).</li><li><strong>Drains the entire queue</strong> — it doesn't run just the microtasks that were waiting when it started; it keeps going until the queue is truly empty.</li><li><strong>Including ones scheduled by other microtasks</strong> — if a microtask queues another microtask, that new one runs in this same drain, not the next tick.</li><li><strong>Before the next task or rendering</strong> — only once the queue is fully empty does the loop move on to a macrotask or paint.</li></ul><div class=\"analogy\"><p>Imagine you're leaving the office for the day (the macrotask) and someone hands you a sticky note: \"reply before you go.\" You handle it. But replying generates two more sticky notes, and those generate more. The rule is: you cannot walk out the door until the sticky-note pile is completely empty — even notes created <em>while</em> clearing the pile. Only then do you leave (paint / next task).</p></div><h4>Why it matters, and the trap</h4><p>This priority guarantees Promise chains settle promptly — your <code>.then</code> logic resolves before the browser bothers with the next timer or repaint. The downside: a microtask that re-queues itself forever will <strong>starve</strong> rendering and timers, hanging the page, because the loop never escapes the drain.</p><p>The classic gotcha: people assume a <code>setTimeout(fn, 0)</code> queued <em>before</em> a Promise runs first. It doesn't. The Promise is a microtask and always wins. Proof: <code>queueMicrotask(()=>console.log('mt')); console.log('sync');</code> prints <strong>'sync' then 'mt'</strong> — both before any <code>setTimeout</code> callback could fire.</p><div class=\"key\">Microtasks drain completely — including ones spawned mid-drain — before any macrotask or repaint, so promises always beat <code>setTimeout(0)</code>, but a self-scheduling microtask can freeze the page.</div>"},
  "macrotask": {"teach":"<p>At its core, a <strong>macrotask</strong> (just called a \"task\" in the spec) is just one regular queued job — a timer firing, a click arriving, some I/O finishing — and the event loop handles exactly one of them per lap around the loop.</p><h4>What counts as a macrotask</h4><ul><li><code>setTimeout</code> and <code>setInterval</code> callbacks;</li><li>I/O completions (a file read, a socket message);</li><li>UI events like clicks, keypresses, scroll;</li><li><code>MessageChannel</code> messages;</li><li>the initial evaluation of a <code>&lt;script&gt;</code>.</li></ul><h4>One per tick — and why that's a feature</h4><p>The event loop processes <strong>one macrotask per iteration</strong>, then fully drains the microtask queue, then possibly renders. The \"one per tick\" rhythm is the important part: because the loop returns to the top after each macrotask, it gets a chance to <strong>paint between them</strong>.</p><div class=\"analogy\"><p>Think of a single ferry that carries one car across the river per trip, and on the return leg always sweeps the deck (microtasks) and lets a sightseer photograph the view (render). If you load all your cargo as one giant car (a long task), nobody gets to sightsee until it's done. But if you split cargo across many small cars (chunked macrotasks), the view gets a photo between each — the screen stays smooth.</p></div><p>That's exactly why macrotasks are <strong>safer for chunking heavy work</strong> than recursively scheduled microtasks. Microtasks all drain in one checkpoint with no paint in between; macrotasks naturally yield to rendering. So to process a big list without freezing the UI, you do one batch, then schedule the next with <code>setTimeout(processNext, 0)</code> — the browser can paint each frame in between.</p><h4>The timing trap</h4><p>People think <code>setTimeout(fn, 0)</code> runs \"immediately.\" It doesn't, for two reasons:</p><ul><li>It always waits for the <strong>microtask queue to drain first</strong>, so any pending promise callbacks run before it.</li><li><strong>Nested timers are clamped</strong>: after about 5 levels of nesting, the browser enforces a minimum delay of roughly 4ms, regardless of the 0 you passed. Background tabs throttle timers even harder.</li></ul><div class=\"key\">One macrotask runs per loop tick, so macrotasks naturally yield to rendering between them — making <code>setTimeout(0)</code> the right tool for chunking heavy work, even though it never truly runs \"immediately.\"</div>"},
  "promise": {"teach":"<p>At its core, a <strong>Promise</strong> is just a placeholder you can hold <em>now</em> for a value that will arrive <em>later</em> — and onto which you can pin follow-up steps to run once it arrives.</p><h4>Three states, one transition</h4><p>A promise starts <em>pending</em>. Then it transitions <strong>once and permanently</strong> to one of two final states:</p><ul><li><strong>fulfilled</strong> — it succeeded and carries a value;</li><li><strong>rejected</strong> — it failed and carries a reason (usually an error).</li></ul><p>\"Once and permanently\" is load-bearing: a settled promise can never change its mind or re-fire. This is what makes promises safe to pass around — whoever holds it sees the same single outcome.</p><div class=\"analogy\"><p>Think of a restaurant pager — the buzzer they hand you when your order isn't ready. Right now it's silent (pending). Eventually it buzzes green: food's up (fulfilled, with your meal as the value). Or it buzzes red: the kitchen ran out (rejected, with a reason). It buzzes exactly once and stays in that state. You can tell a friend \"when this buzzes, grab the food\" — that's attaching a <code>.then</code> handler. And here's the subtle part: even if you attach that instruction <em>after</em> the buzzer already went off, your friend still acts on it — just on the next free moment, not instantly.</p></div><h4>Handlers always run as microtasks</h4><p>Callbacks attached via <code>.then</code>, <code>.catch</code>, <code>.finally</code> run as <strong>microtasks</strong> — even if the promise was already settled when you attached them. This is the #1 gotcha: people expect <code>.then</code> on an already-resolved promise to fire synchronously, but it's always deferred to the microtask checkpoint. This consistency means promise code never \"sometimes sync, sometimes async\" — it's reliably async.</p><h4>Chaining and flattening</h4><p>Each <code>.then</code> <strong>returns a new promise</strong>, so you can chain steps. If a handler returns a thenable (another promise), the chain <strong>flattens</strong> it — it adopts that inner promise's eventual value rather than nesting. You never end up with a <code>Promise&lt;Promise&gt;</code> to unwrap manually.</p><p>Trace it: <code>Promise.resolve(1).then(v=>v+1).then(console.log)</code> logs <strong>2</strong>, and crucially, <em>asynchronously</em> — after the current synchronous code finishes.</p><div class=\"key\">A promise settles exactly once to fulfilled or rejected; its <code>.then</code>/<code>.catch</code> handlers <em>always</em> run as deferred microtasks, never synchronously — even when the promise is already settled.</div>"},
  "async-await": {"teach":"<p>At its core, <code>async</code>/<code>await</code> is just a way to write promise-based code that <em>reads</em> like ordinary top-to-bottom code — no <code>.then</code> nesting, no callback pyramids.</p><h4>It's sugar over promises</h4><p>Nothing new happens under the hood — it's syntactic sugar:</p><ul><li>An <code>async</code> function <strong>always returns a promise</strong>. Whatever you <code>return</code> becomes the fulfillment value; whatever you <code>throw</code> becomes the rejection reason.</li><li><code>await p</code> <strong>pauses</strong> the async function, schedules the rest of it as a <strong>microtask</strong> to resume when <code>p</code> settles, and <strong>yields control to the caller</strong> in the meantime. It does <em>not</em> block the thread — other work keeps running.</li><li>When <code>p</code> fulfills, <code>await</code> hands you the value. When <code>p</code> rejects, <code>await</code> <strong>throws</strong> the reason, so you catch failures with an ordinary <code>try/catch</code>.</li></ul><div class=\"analogy\"><p>Imagine following a recipe where step 3 says \"await the bread rising.\" Instead of standing frozen at the counter staring at dough (blocking), you put the recipe down with a bookmark and go do other chores. A timer (the promise settling) calls you back to the exact bookmarked line to continue. The recipe reads as simple linear steps, but you were never actually idle-waiting — the kitchen kept moving.</p></div><h4>The \"after await isn't synchronous\" trap</h4><p>Even <code>await Promise.resolve()</code> — a promise that's already done — still defers everything after it to a microtask. The very first <code>await</code> always splits your function: code before it runs synchronously, code after it runs later. Forgetting this causes ordering bugs where you assume the post-<code>await</code> line ran immediately.</p><h4>Independent awaits run sequentially — fix it</h4><p>This is the performance pitfall. <code>await f(); await g();</code> runs <code>g</code> only <em>after</em> <code>f</code> finishes — serialized, even if they don't depend on each other. To overlap independent work, either start the promises before awaiting, or use a combinator:</p><p><code>const [a,b] = await Promise.all([f(), g()]);</code> kicks off <code>f</code> and <code>g</code> together and waits for both, so their latencies overlap instead of adding up.</p><div class=\"key\"><code>await</code> defers the rest of the function to a microtask and never blocks the thread — but two independent awaits run sequentially, so reach for <code>Promise.all</code> to overlap them.</div>"},
  "promise-combinators": {"teach":"<p>At its core, a <strong>promise combinator</strong> is just a helper that takes a bunch of promises and waits on all of them at once — differing only in <em>what counts as done</em> and <em>how it treats failures</em>.</p><h4>The four, dissected</h4><ul><li><strong><code>Promise.all</code></strong> — fulfills with an array of <em>all</em> values once every input fulfills, but <strong>rejects on the first rejection</strong>. Important: the other promises aren't cancelled — they keep running, you just stop listening. Use when you need <em>every</em> result and any failure should abort.</li><li><strong><code>Promise.allSettled</code></strong> — <strong>never rejects</strong>. It waits for all to finish and gives you one status object per input: <code>{status:'fulfilled', value}</code> or <code>{status:'rejected', reason}</code>. Use when you want every outcome regardless of failures.</li><li><strong><code>Promise.race</code></strong> — adopts the <strong>first settlement</strong>, whether that's a success or a failure. The earliest one to finish wins, period.</li><li><strong><code>Promise.any</code></strong> — resolves with the first <strong>fulfillment</strong>, ignoring rejections, and only rejects (with an <code>AggregateError</code> bundling all reasons) if <em>every</em> input rejects.</li></ul><div class=\"analogy\"><p>You send four runners to fetch water. <code>all</code> = the team succeeds only if all four return, but the instant one trips you declare failure (the rest keep running, you've just stopped caring). <code>allSettled</code> = you wait for all four and write down what happened to each. <code>race</code> = whoever crosses the line first decides the result, even if they crossed by collapsing (a failure). <code>any</code> = you want the first runner who <em>actually delivers water</em>; only if all four fail do you give up.</p></div><h4>Empty-array edge cases</h4><p>These surprise people, so memorize them: with an empty input array, <code>all</code> and <code>allSettled</code> <strong>fulfill immediately</strong> (with an empty array), <code>any</code> <strong>rejects</strong> (nothing can ever fulfill), and <code>race</code> <strong>never settles</strong> (no contestant to win).</p><h4>The classic trap</h4><p>Reaching for <code>Promise.all</code> when you actually need every result. One rejection throws away all the successful values too. If partial success is acceptable, use <code>allSettled</code> and filter.</p><p>Real uses: a timeout guard — <code>Promise.race([fetch(url), timeout(5000)])</code> — and a fastest-mirror fetch — <code>Promise.any(mirrors.map(fetch))</code>.</p><div class=\"key\"><code>all</code> = all-or-first-failure, <code>allSettled</code> = every outcome never rejects, <code>race</code> = first to settle, <code>any</code> = first to succeed — and on an empty array only <code>race</code> hangs forever.</div>"},
  "set-timeout-zero": {"teach":"<p>At its core, <code>setTimeout(fn, 0)</code> is just a way to say \"<em>run this a bit later</em> — after the current code and all promise jobs finish\" — emphatically <strong>not</strong> \"run this instantly.\"</p><h4>What the 0 really means</h4><p>The 0 is a <em>minimum</em> delay, not a guarantee of immediacy. <code>setTimeout(fn, 0)</code> schedules <code>fn</code> as a <strong>macrotask</strong>. Before it can run, two things must happen:</p><ul><li>the current synchronous script must finish running to completion;</li><li>the entire <strong>microtask queue must drain</strong> (all pending promise callbacks, awaits, <code>queueMicrotask</code> jobs).</li></ul><p>Only then, on a <em>later loop tick</em>, does <code>fn</code> get its turn.</p><div class=\"analogy\"><p>It's like dropping a letter in the outbox marked \"send ASAP.\" ASAP doesn't mean it teleports — it means it goes out with the <em>next</em> mail run, after the courier finishes the current route and clears all the priority express items (microtasks) first. You've guaranteed it leaves soon, not that it leaves this instant.</p></div><h4>Clamping and throttling</h4><p>Browsers don't always honor the 0:</p><ul><li><strong>Nested clamp</strong>: once timers are nested about 5 levels deep, the minimum is bumped to roughly <strong>4ms</strong>. So a chain of <code>setTimeout(0)</code> calls each spaced ~4ms apart, not 0.</li><li><strong>Background throttling</strong>: timers in hidden/background tabs are slowed dramatically to save power.</li></ul><h4>What it's good for — and the trap</h4><p>The right use is <strong>yielding to the browser</strong>: let it paint, or break a long task into chunks so the UI stays responsive (one batch, then <code>setTimeout(processNext, 0)</code>). It is the <em>wrong</em> tool for precise timing, and the wrong tool if you expect it to beat a promise.</p><p>That's the trap people hit. Trace: <code>setTimeout(()=>console.log('B')); Promise.resolve().then(()=>console.log('A'));</code> prints <strong>A then B</strong> — the microtask drains before the macrotask runs, every time.</p><div class=\"key\"><code>setTimeout(fn, 0)</code> queues a macrotask that runs only after sync code <em>and</em> the whole microtask queue finish — so it's for yielding to the browser, never for instant execution or beating a promise.</div>"},
  "request-animation-frame": {"teach":"<p>At its core, <code>requestAnimationFrame(cb)</code> is just a way to say \"run my visual update at the <em>perfect moment</em> — right before the screen's next repaint — so animation stays buttery and in sync with the display.\"</p><h4>Where it fits in the loop</h4><p>The event loop has a dedicated <strong>render step</strong>, and <code>rAF</code> callbacks run inside it, just before the browser paints the next frame. They're aligned to the display's refresh rate: about every <strong>16.7ms at 60Hz</strong>, or <strong>8.3ms at 120Hz</strong>. The callback even receives a high-resolution timestamp so you can compute motion based on real elapsed time.</p><p>The precise ordering within a tick is: <strong>task → microtasks → rAF callbacks → style/layout/paint</strong>. So your <code>rAF</code> code runs <em>after</em> microtasks but <em>just before</em> pixels hit the screen — the ideal place to set up what's about to be drawn.</p><div class=\"analogy\"><p>Think of a flip-book animator who only redraws a page <em>the instant before</em> the projector flashes it. There's no point drawing 1000 frames a second if the projector only flashes 60 — you'd waste effort and tear the image. <code>rAF</code> is the projector tapping your shoulder: \"I'm about to flash the next frame — draw now.\" You draw exactly one frame, perfectly timed, never ahead of or behind the display.</p></div><h4>Two killer features</h4><ul><li><strong>It fires once.</strong> Each <code>rAF</code> schedules a single callback. For continuous animation you re-schedule from inside the callback, creating a loop that's automatically paced to the refresh rate.</li><li><strong>It pauses in hidden/background tabs.</strong> When the user switches away, <code>rAF</code> stops calling you — saving CPU and battery — and resumes when the tab is visible again. It also <strong>batches</strong> visual updates so you avoid layout thrashing.</li></ul><h4>The trap</h4><p>Using <code>setInterval</code> for animation. A fixed interval <strong>drifts</strong> relative to the actual refresh rate and isn't paint-synced, so frames stutter or tear, and it keeps burning CPU even in a hidden tab. <code>rAF</code> solves all three.</p><p>The canonical loop: <code>function loop(t){ move(t); requestAnimationFrame(loop);} requestAnimationFrame(loop);</code> — smooth, refresh-synced, and self-pausing when hidden.</p><div class=\"key\"><code>requestAnimationFrame</code> runs your callback once, in the render step just before the next repaint, synced to the refresh rate and paused in hidden tabs — making it the correct tool for animation, not <code>setInterval</code>.</div>"},
  "throttle-debounce": {"teach":"<p>At its core, the difference is timing strategy: <strong>debounce waits for the user to stop</strong> before acting; <strong>throttle acts at a steady maximum rate</strong> while activity continues.</p><h4>Debounce, dissected</h4><p>Debounce delays the call until there have been <code>delay</code> milliseconds of <strong>inactivity</strong>. Every new event <strong>resets the timer</strong>. So if events keep arriving faster than <code>delay</code>, nothing fires — until the burst ends, and then only the <em>final</em> event triggers the action once.</p><div class=\"analogy\"><p>Debounce is an elevator door. Every time someone steps in, the \"close\" timer resets. The door only shuts once there's been a few seconds with <em>nobody</em> stepping in. It waits for the activity to <em>settle</em>, then acts once.</p></div><h4>Throttle, dissected</h4><p>Throttle guarantees <strong>at most one call per <code>limit</code> ms</strong> during continuous activity. Events keep streaming in, but the action fires on a fixed cadence — ignoring the extras in between.</p><div class=\"analogy\"><p>Throttle is a turnstile that lets one person through every 2 seconds no matter how big the crowd pushing behind it. The crowd (events) never stops, but the rate of passage is capped and steady.</p></div><h4>Which to use where</h4><ul><li><strong>Debounce</strong> when you only care about the <em>settled</em> final state: search-as-you-type (wait until they stop typing), resize-end recalculation, autosave after edits pause.</li><li><strong>Throttle</strong> when you want <em>steady, rate-limited</em> updates <em>during</em> activity: scroll position tracking, <code>mousemove</code>, pointer/drag tracking, where you need ongoing feedback but not on every single pixel.</li></ul><h4>The trap: swapping them</h4><p>This is the bug that bites. If you <strong>debounce a scroll handler</strong>, it waits for scrolling to <em>stop</em> — so while the user scrolls continuously, your handler may <strong>never fire at all</strong>, and your scroll-driven UI freezes mid-scroll. Scroll wants throttle (steady updates); search wants debounce (one update when typing settles).</p><p>Canonical debounce use: <code>input.addEventListener('input', debounce(query, 300))</code> — fires one search request 300ms after the user stops typing, collapsing a whole burst of keystrokes into a single call.</p><div class=\"key\">Debounce fires once after activity <em>stops</em> (great for search/autosave); throttle fires at a steady capped rate <em>during</em> activity (great for scroll/mousemove) — swapping them, like debouncing scroll, can mean the handler never fires.</div>"},
  "abort-controller": {"teach":"<p>At its core, an <strong><code>AbortController</code></strong> is just a remote control with a single \"cancel\" button that you wire into an in-flight async operation — most commonly a <code>fetch</code> — so you can stop it from the outside.</p><h4>The two pieces</h4><ul><li><strong><code>AbortController</code></strong> — the remote itself. It exposes <code>.abort(reason)</code> (the button) and a <code>.signal</code>.</li><li><strong><code>AbortSignal</code></strong> — the receiver you hand to the operation. Signal-aware APIs (like <code>fetch</code>, accepting <code>{signal}</code>) watch it. When you press abort, <code>signal.aborted</code> flips to <code>true</code>, the signal fires an <code>abort</code> event, and the operation rejects with an <strong><code>AbortError</code></strong> (a <code>DOMException</code>).</li></ul><p>One controller's signal can be wired into <strong>many</strong> operations at once — pressing abort cancels them all together.</p><div class=\"analogy\"><p>Think of a single master kill-switch wired to several machines on a factory line. Each machine (a fetch) is told \"watch this switch.\" Flip it once and every wired machine halts immediately and reports \"stopped by operator\" (AbortError) rather than \"broke down\" (a real error). The switch isn't the machine — it's the shared signal they all listen to.</p></div><h4>2026 ergonomics</h4><ul><li><strong><code>AbortSignal.timeout(ms)</code></strong> — a pre-built signal that auto-aborts after a deadline. No manual <code>setTimeout</code> plumbing for request timeouts.</li><li><strong><code>AbortSignal.any([...])</code></strong> — combine several signals into one that aborts when <em>any</em> of them does. Perfect for \"cancel on user click <em>or</em> on timeout.\"</li></ul><p>In React, you create a controller in an effect and call <code>.abort()</code> in the cleanup function, so when a component re-renders or unmounts, the stale request is dropped and you never set state from an outdated response.</p><h4>The trap</h4><p>Letting the resulting <code>AbortError</code> bubble up and get logged as a genuine failure. Aborting is <em>expected</em>, not an error in your logic. Detect it explicitly — <code>if (e.name === 'AbortError')</code> — and treat it as a normal cancellation, not a crash.</p><p>Minimal pattern: <code>const ac = new AbortController(); fetch(url, {signal: ac.signal}); ac.abort();</code> — the fetch promise rejects with an <code>AbortError</code>.</p><div class=\"key\">An <code>AbortController</code>'s <code>.signal</code> lets you cancel one or many signal-aware operations with <code>.abort()</code>, rejecting them with an <code>AbortError</code> you must detect (<code>e.name==='AbortError'</code>) and treat as expected, not a real failure.</div>"},
  "queue-microtask": {"teach":"<p>At its core, <code>queueMicrotask(fn)</code> is just a direct way to say \"run <code>fn</code> the moment the current code finishes — before any timer or repaint, but after right now.\"</p><h4>What it does</h4><p>It places <code>fn</code> onto the <strong>microtask queue</strong>. That means <code>fn</code> runs at the next microtask checkpoint — after the current synchronous code completes, and crucially <strong>before</strong> the next macrotask (like a <code>setTimeout</code> callback) and <strong>before</strong> the browser paints.</p><p>Timing-wise it's identical to <code>Promise.resolve().then(fn)</code>, but it's <strong>clearer</strong> about intent and doesn't allocate a throwaway promise just to schedule a callback.</p><div class=\"analogy\"><p>Imagine you're mid-sentence in a meeting (current sync code). <code>queueMicrotask</code> is jotting a note that says \"do this the second I stop talking, before we move to the next agenda item or take a coffee break.\" It's not an interruption — you finish your sentence — but it jumps ahead of everything that isn't already in progress. The coffee break (rendering) and the next agenda item (macrotask) both wait.</p></div><h4>Why you'd reach for it</h4><ul><li><strong>Batching</strong>: collect a bunch of synchronous mutations, then schedule one flush to process them all together once the current code settles — staying ahead of paint.</li><li><strong>Guaranteeing async</strong>: a function that's <em>sometimes</em> synchronous and <em>sometimes</em> asynchronous is a notorious bug source (callers can't reason about ordering). Wrapping its callback in <code>queueMicrotask</code> makes it <strong>always</strong> async, consistently — without the cost of waiting for a full timer tick.</li></ul><h4>The trap</h4><p>Recursively re-queuing microtasks. Because the loop <strong>drains the entire microtask queue</strong> before it renders or runs timers, a microtask that schedules another microtask forever never lets the loop escape — rendering and timers are <strong>starved</strong> and the page hangs. When you genuinely need to yield back to the browser (let it paint, let a timer fire), use <code>setTimeout</code> instead, which queues a macrotask and gives the render step a turn.</p><p>Canonical use: <code>queueMicrotask(() => flushBatchedUpdates());</code> after collecting synchronous mutations in one operation.</p><div class=\"key\"><code>queueMicrotask</code> schedules a callback to run right after current code but before any macrotask or paint — ideal for batching and forcing consistent async — but recursively re-queuing one starves rendering and freezes the page.</div>"},
  "unhandled-rejection": {"teach":"<p>At its core, an <strong>unhandled promise rejection</strong> is just a promise that <em>failed</em> while nobody was listening for the failure.</p><h4>How one happens</h4><p>A rejected promise gives the engine until the <strong>microtask checkpoint</strong> to find a rejection handler attached to it. If none exists by then, it's declared <em>unhandled</em>. Common causes:</p><ul><li>a forgotten <code>.catch</code> on a promise chain;</li><li>an <code>await</code> that isn't wrapped in <code>try/catch</code>;</li><li>a <strong>floated</strong> fire-and-forget promise whose result nobody ever inspects;</li><li>a <code>.finally</code> handler that itself throws or rejects.</li></ul><div class=\"analogy\"><p>Picture a smoke alarm wired to a monitoring service. If the kitchen catches fire (the rejection) and the alarm is connected (a <code>.catch</code>), help is dispatched — handled. But if you never wired it to anyone, the fire just spreads and the building's emergency override kicks in: in a browser, an alert is broadcast (<code>unhandledrejection</code> event); in Node, the whole building is evacuated — <strong>the process exits</strong>.</p></div><h4>Browser vs Node</h4><ul><li><strong>Browser</strong>: fires a global <code>unhandledrejection</code> event you can listen for. The page keeps running.</li><li><strong>Node (since v15)</strong>: by default it <strong>terminates the process</strong>. A single forgotten <code>.catch</code> can crash your whole server — so this is not a warning to ignore.</li></ul><h4>How to handle it properly</h4><p>Add a <em>global</em> handler for telemetry and as a safety net — <code>window.addEventListener('unhandledrejection', e => log(e.reason));</code> — and call <code>e.preventDefault()</code> if you want to suppress the default behavior. But the global handler is a backstop, not a strategy: always handle rejections <strong>at the source</strong> with a real <code>.catch</code> or <code>try/catch</code>. For deliberately fire-and-forget calls, mark the intent explicitly: <code>void p.catch(report)</code> — this documents that you meant to float it and still routes errors somewhere.</p><h4>The sneaky trap</h4><p>Assuming a <code>try/catch</code> around a loop will catch errors from an <strong>async <code>forEach</code></strong> callback. It won't — <code>forEach</code> doesn't await your async callback, so each one returns a floating promise. Their rejections escape your <code>try/catch</code> entirely and become unhandled rejections. Use a <code>for...of</code> loop with <code>await</code>, or <code>Promise.all(map(...))</code>, instead.</p><div class=\"key\">A rejected promise with no handler by the microtask checkpoint fires <code>unhandledrejection</code> in the browser but <em>crashes the process</em> in Node — so always <code>.catch</code> at the source (and beware async <code>forEach</code>, which escapes <code>try/catch</code>).</div>"},
  "generator-iterator": {"teach":"<p>At its core, a <strong>generator</strong> is just a function that can <em>pause itself</em> partway through, hand you a value, and later resume from exactly where it stopped.</p><h4>The two roles</h4><ul><li>An <strong>iterator</strong> is any object with a <code>.next()</code> method that returns <code>{value, done}</code> — <code>value</code> is the next item, <code>done</code> is <code>true</code> when there's nothing left.</li><li>A <strong>generator</strong> (written <code>function*</code>) is the easiest way to <em>produce</em> an iterator. Calling it doesn't run the body — it hands back an iterator. Each <code>.next()</code> runs the body until the next <code>yield</code>, which emits a value and <strong>freezes</strong> the function in place. The following <code>.next()</code> thaws it and continues.</li></ul><p>Generators are the <strong>only native way to suspend and resume a function</strong>. That's what makes <strong>lazy</strong> and even <strong>infinite</strong> sequences possible without ever allocating a giant array — you compute each value only when asked.</p><div class=\"analogy\"><p>Think of a Pez dispenser. The candies aren't all laid out on a table (an array); they sit stacked inside, and you get exactly one each time you click the head (<code>.next()</code>). The dispenser \"remembers\" its position between clicks. You could have an endless dispenser and never run out of memory, because it only ever produces the <em>next</em> candy on demand — never the whole supply at once.</p></div><h4>Iterables and <code>for...of</code></h4><p>An object becomes <strong>iterable</strong> — usable in <code>for...of</code> and spread <code>[...x]</code> — by implementing <code>[Symbol.iterator]</code>, which returns an iterator. Generators implement this automatically, which is why you can spread or loop over them directly.</p><h4>Async generators</h4><p>An <code>async function*</code> combined with <code>for await...of</code> drives <strong>async iterators</strong> — perfect for <em>streamed</em> or <em>paginated</em> data, where each chunk arrives over time (e.g. reading a network stream page by page, awaiting the next batch only when you're ready for it).</p><h4>The trap</h4><p>Thinking generators run <strong>eagerly</strong>. They don't. The instant you call a generator, <em>nothing in its body executes</em>. Code runs only when you pull a value via <code>.next()</code> (or iterate). They are <strong>pull-based and lazy</strong> — values are produced on demand, never ahead of time.</p><p>Tiny example: <code>function* range(n){ for(let i=0;i&lt;n;i++) yield i; } [...range(3)] // [0,1,2]</code> — the loop only advances as the spread pulls each value.</p><div class=\"key\">A <code>function*</code> generator returns a lazy, pull-based iterator that runs nothing until <code>.next()</code> is called and pauses at each <code>yield</code> — making infinite/streamed sequences possible; <code>async function*</code> + <code>for await...of</code> extends this to data arriving over time.</div>"},
  "single-threaded": {"teach":"<p>At its core, the <strong>single-threaded model</strong> means JavaScript runs your code on <em>one worker at a time</em> — and stays fast not by adding workers, but by <em>delegating the waiting</em> to the system around it.</p><h4>One stack, one heap</h4><p>Per realm, JS has a <strong>single main thread</strong> with one call stack (the to-do list of running functions) and one heap (where objects live). Because there's one stack, two of your functions can never literally execute at the same instant on that thread.</p><h4>How it stays responsive</h4><p>The trick is that slow operations — timers, network requests, disk I/O — aren't done <em>by</em> the JS thread. They're handed off to the <strong>host environment</strong> (the browser or Node), which has its own machinery (often multiple threads internally) to handle them. When one completes, the host queues a callback, and the event loop feeds it back to the single thread once the stack is clear.</p><p>This is the crucial distinction: it's <strong>concurrency, not parallelism</strong>. Many operations are <em>in progress</em> at once (concurrency), but your JS code only ever <em>runs</em> one piece at a time (no parallelism on the main thread).</p><div class=\"analogy\"><p>Think of a single chef who's also great at delegation. A customer orders a slow-braised dish — the chef doesn't stand and watch it braise. He puts it in the oven (hands it to the host), and while it cooks he takes new orders, plates salads, rings up the till. A timer dings; he pulls the dish out (the callback). One pair of hands, but the kitchen hums because the <em>waiting</em> was offloaded to the oven, not the chef.</p></div><h4>True parallelism, when you need it</h4><p>For genuinely simultaneous CPU work, you use <strong>Web Workers</strong> (browser) or worker threads (Node). These run on separate threads with <strong>isolated state</strong> — they don't share the main thread's variables — and communicate by <strong>message passing</strong> or via a <code>SharedArrayBuffer</code>. That's the only way to actually run two pieces of JS at the same instant.</p><h4>The trap</h4><p>Blaming async APIs when the UI freezes. The async layer almost never blocks — a frozen page is nearly always <strong>synchronous CPU work hogging the single thread</strong>. Proof: <code>while (Date.now() &lt; end) {}</code> blocks clicks, timers, <em>and</em> rendering until it finishes, because that tight loop never lets the stack empty. The fix is to move heavy computation off the main thread (a Worker) or chunk it across macrotasks — not to sprinkle more <code>async</code>.</p><div class=\"key\">JS runs on one main thread and delegates waiting to the host — giving concurrency, not parallelism — so a frozen UI is virtually always synchronous CPU work hogging the thread, fixed by Web Workers or chunking, never by more <code>async</code>.</div>"}
});
