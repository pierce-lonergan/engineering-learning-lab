/* Compute (EC2, Lambda, Containers) (CLOUD-COMPUTE) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"CLOUD-COMPUTE","front":"When to pick Lambda vs containers vs EC2 (the core decision)","back":"<ul><li><strong>Lambda</strong>: short, event-driven, spiky/bursty, glue work (S3 triggers, Kinesis/MSK consumers, API backends). No infra to manage; dies at 15 min.</li><li><strong>Containers (ECS/EKS on Fargate)</strong>: long-running services, steady traffic, custom runtimes, anything exceeding Lambda's limits.</li><li><strong>EC2</strong>: full OS control, special hardware (GPU, large memory, local NVMe), licensing, or sustained 24/7 load where reserved pricing wins.</li></ul>"},
  {"sec":"CLOUD-COMPUTE","front":"EC2 purchasing models: On-Demand vs Reserved/Savings Plans vs Spot","back":"<ul><li><strong>On-Demand</strong>: pay per second, no commitment, highest price. For unpredictable/short workloads.</li><li><strong>Reserved Instances / Savings Plans</strong>: 1- or 3-year commitment for committed-use discounts (up to ~72% for the least-flexible options); for steady baseline load.</li><li><strong>Spot</strong>: up to ~90% off spare capacity, but AWS can reclaim it with a 2-minute warning. For fault-tolerant, interruptible work.</li></ul>"},
  {"sec":"CLOUD-COMPUTE","front":"What makes a workload a good fit for Spot instances?","back":"Spot suits <strong>fault-tolerant, stateless, interruptible</strong> work that can checkpoint and resume: batch ETL, Spark/EMR executors, CI runners, rendering, ML training with checkpoints. Avoid Spot for stateful single-node services, databases, or anything that can't survive a <code>2-minute</code> reclaim notice. Best practice: diversify across instance types/AZs and mix with On-Demand for a stable core."},
  {"sec":"CLOUD-COMPUTE","front":"Spot interruption: what notice do you get and how do you handle it?","back":"AWS sends a <strong>2-minute interruption notice</strong> via instance metadata (and an EventBridge event) before reclaiming. Handlers should <strong>checkpoint state, drain connections, deregister from the load balancer, and exit gracefully</strong>. Spot also surfaces <em>rebalance recommendations</em> earlier (before the 2-min notice) so you can proactively replace the instance."},
  {"sec":"CLOUD-COMPUTE","front":"On-Demand vs Reserved vs Savings Plans: how do RIs and Savings Plans differ?","back":"Both give committed-use discounts. <strong>Reserved Instances</strong> commit to a specific instance family/region (Standard, up to ~72%) or allow family changes (Convertible, up to ~66%). <strong>Savings Plans</strong> commit to a <code>$/hour</code> spend level: <em>Compute Savings Plans</em> are flexible across instance family, region, OS, and even Fargate/Lambda but cap at ~66%; <em>EC2 Instance Savings Plans</em> reach ~72% but lock to a family in a region. Compute Savings Plans are usually preferred for flexibility, trading a few points of discount for it."},
  {"sec":"CLOUD-COMPUTE","front":"What is a Lambda cold start and when does it happen?","back":"A cold start is the extra latency when Lambda must <strong>provision a new execution environment</strong>: download code, start the runtime, and run init code before handling the request. It happens on the first invoke, after scale-up, and after idle environments are reclaimed. Magnitude depends on runtime (JVM/.NET worse than Python/Node), package size, and VPC/ENI attachment. Mitigate with <strong>provisioned concurrency</strong> or lighter runtimes."},
  {"sec":"CLOUD-COMPUTE","front":"Lambda hard limits a data engineer must design around","back":"<ul><li><strong>15-minute</strong> max execution timeout.</li><li><strong>10 GB</strong> max memory; CPU scales with memory (vCPU is proportional, not separately configurable).</li><li><strong>512 MB-10 GB</strong> <code>/tmp</code> ephemeral disk; <strong>6 MB</strong> sync request/response payload (async event payload was raised from 256 KB to 1 MB in late 2025).</li><li>Default <strong>1,000</strong> concurrent executions per account/region (soft, raisable).</li></ul>"},
  {"sec":"CLOUD-COMPUTE","front":"Reserved vs Provisioned concurrency in Lambda","back":"<strong>Reserved concurrency</strong> caps and guarantees a slice of the account concurrency pool for a function (a ceiling + a floor) — it does <em>not</em> remove cold starts, and setting it to <code>0</code> effectively disables the function. <strong>Provisioned concurrency</strong> pre-initializes a number of warm environments so invocations skip cold starts — it costs money even when idle and is what you use for latency-sensitive paths."},
  {"sec":"CLOUD-COMPUTE","front":"How is Lambda billed?","back":"Pay per <strong>invocation</strong> plus <strong>duration</strong> = (GB-of-memory x wall-clock ms), billed at 1 ms granularity. CPU is bundled with the memory setting, so raising memory speeds execution and can <em>lower</em> total cost for CPU-bound work. Provisioned concurrency adds a separate per-hour charge for kept-warm environments. There is no duration charge while idle (except provisioned concurrency, which bills continuously)."},
  {"sec":"CLOUD-COMPUTE","front":"ECS vs EKS: what's the difference and when to choose each?","back":"<strong>ECS</strong> is AWS's proprietary, simpler container orchestrator — minimal control plane, tight AWS integration, no control-plane fee. <strong>EKS</strong> is managed Kubernetes — portable, huge ecosystem (Helm, operators, CRDs), but more operational complexity and a ~<code>$0.10</code>/hr per-cluster control-plane fee. Choose ECS for AWS-native simplicity; choose EKS when you need Kubernetes portability, existing k8s tooling/skills, or multi-cloud."},
  {"sec":"CLOUD-COMPUTE","front":"What is Fargate and what does it replace?","back":"Fargate is the <strong>serverless compute engine for containers</strong>: you run ECS tasks or EKS pods without provisioning, patching, or scaling EC2 nodes — you specify vCPU/memory per task and pay for what the task uses. It removes node management but costs more per vCPU-hour than well-utilized self-managed EC2, can't use GPUs or DaemonSets, and has cold-start-like task-launch latency. Use it to avoid running a node fleet."},
  {"sec":"CLOUD-COMPUTE","front":"Fargate vs EC2 launch type for ECS/EKS: the tradeoff","back":"<strong>EC2 launch type</strong>: you manage the node fleet (capacity, AMIs, bin-packing) but get cheaper per-unit compute, GPU/special instances, and DaemonSets; good for steady, dense, cost-sensitive workloads. <strong>Fargate</strong>: zero node management and per-task isolation, but higher per-vCPU cost and feature limits; good for variable or low-ops workloads. Many teams mix: Fargate for spiky services, EC2 (often Spot) for batch."},
  {"sec":"CLOUD-COMPUTE","front":"Lambda vs Fargate for a data pipeline task — how to choose?","back":"Pick <strong>Lambda</strong> for short (<15 min), event-driven, highly bursty units (per-file S3 processing, stream record handlers) where instant scale-to-many and scale-to-zero matter. Pick <strong>Fargate</strong> for longer jobs, large container images, heavy memory/CPU, custom runtimes, or steady throughput where the per-request model gets expensive. The 15-minute wall is the most common forcing function to move off Lambda."},
  {"sec":"CLOUD-COMPUTE","front":"EC2 instance families: how are they named and grouped?","back":"The first letter signals the workload class: <strong>M</strong> = general/balanced, <strong>C</strong> = compute-optimized, <strong>R/X</strong> = memory-optimized, <strong>I/D</strong> = storage (local NVMe), <strong>P/G/Inf/Trn</strong> = GPU/accelerated, <strong>T</strong> = burstable. A name like <code>r6g.xlarge</code> reads as family <em>r</em>, generation <em>6</em>, <em>g</em> = Graviton (ARM), size <em>xlarge</em>. Data workloads typically want R/X for memory or I/D for local SSD shuffle."},
  {"sec":"CLOUD-COMPUTE","front":"What are T-family (burstable) instances and their cost trap?","back":"T-instances (<code>t3</code>, <code>t4g</code>) are cheap <strong>burstable</strong> CPUs that accrue <em>CPU credits</em> while idle and spend them to burst above a low baseline. The trap: under sustained load they exhaust credits and either throttle or, in <strong>unlimited mode</strong>, silently bill extra. Great for dev/spiky web, dangerous for steady CPU-bound data jobs — use M/C there instead."},
  {"sec":"CLOUD-COMPUTE","front":"Why does Graviton (ARM) matter for cost on EC2/Fargate/Lambda?","back":"Graviton is AWS's in-house <strong>ARM64</strong> processor, offering roughly <strong>20-40% better price/performance</strong> than equivalent x86 instances (typically ~10-20% cheaper plus higher throughput) and available across EC2 (the <code>g</code> suffix), Fargate, and Lambda. The catch: your code/containers/dependencies must support ARM64 (multi-arch images, recompiled native libs). For most JVM/Python/Go data workloads it's a near-free cost win once the build is multi-arch."}
);

Object.assign(GLOSSARY_DATA, {
  "ec2-purchasing-models": {"term":"EC2 Purchasing Models","group":"CLOUD-COMPUTE","aliases":["on-demand","reserved instances","savings plans","purchasing options","committed use discount"],"eli5":"Different ways to pay for the same VM: flexible-but-pricey, commit-and-save, or dirt-cheap-but-can-be-yanked.","memory":"On-Demand = taxi (pay anytime), Reserved/Savings = lease (commit, save), Spot = standby airline seat (cheap, can be bumped).","full":"<p>EC2 sells identical compute under different pricing contracts. <strong>On-Demand</strong> is pay-as-you-go per second, no commitment, the baseline price. <strong>Reserved Instances</strong> and <strong>Savings Plans</strong> trade a 1- or 3-year commitment for committed-use discounts on steady baseline load. <strong>Spot</strong> sells spare capacity at up to ~90% off but reclaims it on a 2-minute notice.</p><p>Savings Plans come in two flavors: <em>Compute</em> (flexible across family/region/OS and even covers Fargate + Lambda, but caps around ~66% off) and <em>EC2 Instance</em> (reaches ~72% but locked to a family/region). Standard RIs also reach ~72%, Convertible RIs ~66%. The mature pattern is a layered fleet: Reserved/Savings Plans for the always-on baseline, On-Demand for predictable spikes, Spot for interruptible burst.</p>","trap":"Interviewers ask 'how would you cut a 24/7 EMR/EC2 bill?' The strong answer is layered: Savings Plans for baseline + Spot for interruptible executors, NOT 'just buy Reserved.' Know the discount nuance: the flexible Compute Savings Plan caps ~66%, while the locked EC2 Instance Savings Plan / Standard RI reach ~72% — and only Compute Savings Plans discount Fargate and Lambda.","worked":"A steady 100-node service: cover ~70 nodes with a 3-yr Savings Plan, let ~30 float On-Demand for headroom. A nightly Spark batch: run executors on Spot (up to ~90% off) with checkpointing, keep the driver On-Demand.","related":["spot-instances","ec2-instance-families"]},
  "spot-instances": {"term":"Spot Instances","group":"CLOUD-COMPUTE","aliases":["spot","spot capacity","interruptible instances"],"eli5":"Rent AWS's leftover capacity for pennies, but they can take it back with two minutes' warning.","memory":"Spot = standby seat: up to 90% off, but get bumped on 2 minutes' notice — only fly Spot if you can re-route.","full":"<p>Spot Instances are spare EC2 capacity sold at up to ~90% below On-Demand. The price/availability fluctuates with supply, and AWS reclaims the instance when it needs the capacity back, giving a <strong>2-minute interruption notice</strong> (via instance metadata and EventBridge) plus earlier <em>rebalance recommendations</em>.</p><p>Spot only makes sense for <strong>fault-tolerant, stateless, checkpointable</strong> work: Spark/EMR executors, batch ETL, CI, ML training with checkpoints, stateless web behind a load balancer. Resilience comes from <em>diversification</em> — spread requests across many instance types and AZs so a single capacity shortfall doesn't kill the whole fleet — and from mixing a small On-Demand core with Spot burst (Spot Fleet / EC2 Auto Scaling mixed-instances policy).</p>","trap":"Common gotcha: 'is Spot cheaper than Reserved?' Per-hour yes, but Spot can be reclaimed, so it's NOT for stateful/always-up workloads. Also: the 2-min notice is for graceful drain/checkpoint, not negotiation — design idempotent, resumable tasks.","worked":"EMR with a managed scaling policy: core nodes (HDFS) On-Demand for durability, task nodes on a diversified Spot pool across 6 instance types. If Spot reclaims executors, the Spark job retries those tasks elsewhere and the run still completes far cheaper.","related":["ec2-purchasing-models","ec2-instance-families"]},
  "ec2-instance-families": {"term":"EC2 Instance Families","group":"CLOUD-COMPUTE","aliases":["instance families","instance types","graviton","burstable instances","t-instances"],"eli5":"Menu of VM shapes — balanced, CPU-heavy, RAM-heavy, GPU, or local-SSD — named by a letter + generation + size.","memory":"First letter = the bottleneck it relieves: C=Compute, R=RAM, I=I/O (storage), G=GPU, T=Tiny/burst. Trailing 'g' = Graviton.","full":"<p>Instance type names encode the shape: a leading letter for the class (<strong>M</strong> general, <strong>C</strong> compute, <strong>R/X</strong> memory, <strong>I/D</strong> local-NVMe storage, <strong>P/G/Inf/Trn</strong> accelerated, <strong>T</strong> burstable), a generation number, optional capability suffixes (<code>g</code> = Graviton/ARM, <code>n</code> = enhanced networking, <code>d</code> = local NVMe), then size (<code>large</code>, <code>xlarge</code>...). Example: <code>r6gd.2xlarge</code> = memory-optimized, gen 6, Graviton, with local SSD.</p><p>Two cost levers matter most for data work. <strong>Graviton</strong> (ARM64) gives ~20-40% better price/performance if your stack is multi-arch. <strong>T-burstable</strong> instances are cheap but run on a CPU-credit budget — fine for spiky/dev, a throttling or surprise-bill trap under sustained load (unlimited mode). Memory-heavy shuffles want R/X; local-SSD-heavy shuffles/temp want I/D.</p>","trap":"Gotcha: running a steady, CPU-bound job on a <code>t3</code> to save money — it burns CPU credits then throttles (or quietly bills extra in unlimited mode). Burstable is for spiky/idle, not sustained load.","worked":"Right-sizing a Spark cluster: memory-bound joins -> <code>r7g</code> (Graviton memory-optimized); shuffle-heavy with big spill -> <code>i4i</code>/<code>d3</code> for local NVMe; CPU-bound transforms -> <code>c7g</code>. Moving x86 -> Graviton on these typically cuts compute cost with a multi-arch image.","related":["ec2-purchasing-models","spot-instances"]},
  "lambda-cold-start": {"term":"Lambda Cold Start","group":"CLOUD-COMPUTE","aliases":["cold start","cold starts","init latency","warm start"],"eli5":"The first request to a fresh function pays a startup tax to boot the runtime before any of your code runs.","memory":"Cold start = cold engine on a winter morning: the first crank is slow, after that it idles warm until it sits long enough to cool off again.","full":"<p>A cold start is the latency to <strong>create a new execution environment</strong>: AWS downloads your code/layers, starts the language runtime, and runs your init code (imports, SDK clients, connection setup) before the handler runs. Subsequent invocations reuse that warm environment (a <em>warm start</em>) and skip the tax. Cold starts hit on first invoke, on scale-up under concurrency, and after idle environments are reclaimed.</p><p>Magnitude depends on runtime (JVM/.NET are heaviest; Python/Node/Go are lighter), deployment package size, and whether the function attaches to a VPC. Mitigations: keep init code minimal and lazy, shrink the package, use <strong>provisioned concurrency</strong> for latency-sensitive paths, or move to a faster runtime/ARM. Note init code billing differs from handler billing.</p>","trap":"Classic gotcha: 'reserved concurrency removes cold starts.' It does NOT — only <em>provisioned</em> concurrency pre-warms environments. Reserved concurrency just partitions the concurrency limit. Also, VPC-attached Lambdas historically added ENI cold-start latency (largely mitigated by AWS's Hyperplane ENI改进, but still test it).","worked":"A synchronous API on Lambda sees p99 spikes to ~800ms whenever traffic scales up (Java runtime). Fix: enable provisioned concurrency = expected concurrent requests, so those environments are pre-initialized and p99 drops to warm-path latency — at the cost of a per-hour charge for the kept-warm capacity.","related":["lambda-limits","lambda-concurrency","lambda-billing"]},
  "lambda-limits": {"term":"Lambda Execution Limits","group":"CLOUD-COMPUTE","aliases":["lambda timeout","15 minute limit","lambda memory limit","lambda payload limit"],"eli5":"Lambda is great for short bursts but has hard ceilings — most importantly it dies at 15 minutes.","memory":"The big three Lambda walls: 15 minutes, 10 GB RAM, 6 MB sync payload. Hit any -> reach for Fargate.","full":"<p>Lambda's constraints force architecture decisions. The headline is the <strong>15-minute</strong> max timeout — any unit of work that can exceed it must move to Fargate/Batch/Step Functions or be chunked. Memory is configurable up to <strong>10 GB</strong>, and vCPU scales <em>proportionally</em> with memory (you can't set CPU independently). Ephemeral <code>/tmp</code> is 512 MB up to 10 GB.</p><p>Payloads are capped: <strong>6 MB</strong> synchronous request/response; the asynchronous/event payload limit was raised from 256 KB to <strong>1 MB</strong> in late 2025 (and upstream sources like SNS/SQS/EventBridge still impose their own 256 KB message limits). Large data must go via S3 references, not inline. Default account concurrency is <strong>1,000</strong> simultaneous executions per region (a soft, raisable quota). Knowing these is the difference between 'use Lambda' and 'this needs a container.'</p>","trap":"Interview trap: proposing Lambda for a job that 'usually takes 8 minutes.' Variance pushes it past 15 min and it gets killed mid-run with no clean resume. Either chunk via Step Functions or use Fargate. Also: CPU isn't a separate knob — buy CPU by buying memory.","worked":"Per-file S3 -> transform -> S3: fine on Lambda when files are small. But a daily 50 GB compaction routinely runs ~25 min -> it WILL be killed at 15 min. Move it to a Fargate task (or EMR), keeping Lambda only as the lightweight S3-event trigger.","related":["lambda-cold-start","lambda-concurrency","fargate","lambda-vs-fargate"]},
  "lambda-concurrency": {"term":"Lambda Concurrency (Reserved vs Provisioned)","group":"CLOUD-COMPUTE","aliases":["reserved concurrency","provisioned concurrency","concurrency limit","throttling"],"eli5":"Two different knobs: one carves out (and caps) how many copies a function can run; the other pre-warms copies to kill cold starts.","memory":"Reserved = roped-off lane (yours, and you can't exceed it). Provisioned = engines kept idling so there's no cold crank.","full":"<p>Lambda scales by running many concurrent <em>execution environments</em>, bounded by the account's regional concurrency quota (default 1,000). <strong>Reserved concurrency</strong> dedicates a slice of that pool to one function: it guarantees that function can reach the reserved number <em>and</em> caps it there (protecting downstream systems and other functions). Setting reserved concurrency to <code>0</code> throttles the function to off. It does nothing for cold starts.</p><p><strong>Provisioned concurrency</strong> pre-initializes N environments so they're warm and ready, eliminating cold starts for the first N concurrent requests — billed per hour whether used or not, and typically managed with Application Auto Scaling to track demand. Use reserved to <em>shape/cap</em> concurrency, provisioned to <em>remove latency</em>.</p>","trap":"The #1 mixup: 'reserved concurrency reduces cold starts.' No — that's provisioned. Reserved only partitions/caps the limit (and can starve a function to 0). Also watch: reserving concurrency for one function shrinks the unreserved pool available to all others.","worked":"A Lambda fronting a fragile RDS instance: set <em>reserved</em> concurrency to 50 so a traffic spike can't open 1,000 DB connections. A latency-sensitive checkout API: set <em>provisioned</em> concurrency to 200 so peak requests skip cold starts. Different problems, different knobs.","related":["lambda-cold-start","lambda-limits","lambda-billing"]},
  "lambda-billing": {"term":"Lambda Billing","group":"CLOUD-COMPUTE","aliases":["lambda pricing","lambda cost","gb-second","pay per invocation"],"eli5":"You pay a tiny fee per request plus memory-time for how long it ran, rounded to the millisecond.","memory":"Lambda bill = requests + (memory x milliseconds). More memory = more $/ms but often fewer ms — sometimes a net win.","full":"<p>Lambda charges two things: a per-<strong>request</strong> fee (~$0.20 per million invocations) and a <strong>duration</strong> charge measured in GB-seconds = (memory allocated in GB) x (wall-clock execution time), billed at <strong>1 ms granularity</strong>. Because vCPU is bundled with the memory setting, raising memory both speeds execution and can <em>lower</em> total cost for CPU-bound work (the shorter runtime offsets the higher per-ms rate).</p><p><strong>Provisioned concurrency</strong> adds a separate charge: a per-hour fee for keeping environments warm (billed continuously while enabled, even idle) plus a lower duration rate when they execute. Note that Compute Savings Plans discount the Lambda <em>duration</em> charge but not the per-request charge. Otherwise there's no charge while a function sits idle.</p>","trap":"Gotcha: assuming the lowest memory is always cheapest. For CPU-bound work, bumping memory (which adds vCPU) can finish so much faster that total GB-seconds drop. Also: provisioned concurrency bills 24/7 whether or not it's invoked, and Savings Plans only cover duration, not request fees.","worked":"A CPU-bound transform at 512 MB runs 4 s; at 1,769 MB (1 full vCPU) it runs 1 s. GB-seconds: 0.5x4=2 vs 1.73x1=1.73 — the bigger setting is both faster and cheaper.","related":["lambda-cold-start","lambda-limits","lambda-concurrency"]},
  "ecs-vs-eks": {"term":"ECS vs EKS","group":"CLOUD-COMPUTE","aliases":["ecs","eks","kubernetes on aws","container orchestration"],"eli5":"Two AWS container orchestrators: ECS is the simple AWS-native one; EKS is managed Kubernetes for portability and the k8s ecosystem.","memory":"ECS = AWS's house orchestrator (simple, native, free control plane). EKS = Kubernetes you don't have to self-host (portable, powerful, pay-per-cluster).","full":"<p>Both schedule containers, but differ in philosophy. <strong>ECS (Elastic Container Service)</strong> is AWS's proprietary orchestrator: a simple task/service model, deep AWS integration (IAM, ALB, CloudWatch), no control-plane fee, and far less operational surface. <strong>EKS (Elastic Kubernetes Service)</strong> is managed upstream Kubernetes: portable across clouds/on-prem, the full ecosystem (Helm, operators, CRDs, KEDA, Argo), but more moving parts and a per-cluster hourly charge (~<code>$0.10</code>/hr in standard support) for the managed control plane.</p><p>Both can run on the <strong>EC2 launch type</strong> (you manage nodes) or <strong>Fargate</strong> (serverless, no nodes). Pick ECS for AWS-only simplicity and lowest ops; pick EKS when you need Kubernetes portability, existing k8s skills/tooling, advanced scheduling, or multi-cloud strategy. The cost of EKS is complexity; the cost of ECS is lock-in.</p>","trap":"Gotcha: choosing EKS 'because Kubernetes' when the team has no k8s expertise and runs only on AWS — you pay the ~$0.10/hr control-plane fee and the operational tax for portability you'll never use. ECS is often the right boring answer.","worked":"A data platform standardizing on Argo Workflows / Spark-on-k8s / KEDA-driven autoscaling -> EKS, because those tools are Kubernetes-native. A handful of internal microservices on AWS with a small team -> ECS on Fargate, minimal ops, no control-plane bill.","related":["fargate","lambda-vs-fargate"]},
  "fargate": {"term":"AWS Fargate","group":"CLOUD-COMPUTE","aliases":["fargate","serverless containers","fargate spot"],"eli5":"Run containers without owning any servers — you hand AWS a task definition with vCPU/memory and it runs the container for you.","memory":"Fargate = 'containers, hold the servers.' You bring the task definition; AWS brings (and hides) the box.","full":"<p>Fargate is the <strong>serverless compute engine</strong> for ECS and EKS: instead of provisioning, patching, scaling, and bin-packing an EC2 node fleet, you declare vCPU/memory per task or pod and AWS runs it on managed, per-task-isolated infrastructure. You pay for the vCPU and memory your tasks request, for the time they run — no idle node capacity.</p><p>The tradeoff: Fargate costs <strong>more per vCPU-hour</strong> than well-utilized self-managed EC2, can't use GPUs or DaemonSets, has feature/size ceilings, and incurs task-launch latency (a cold-start analog). It shines for variable, low-ops, or per-task-isolated workloads. <strong>Fargate Spot</strong> exists too (deep discount, interruptible — though not available for EKS pods). Compute Savings Plans can also discount Fargate.</p>","trap":"Gotcha: assuming Fargate is always cheaper because it's serverless. For dense, steady, high-utilization fleets, self-managed EC2 (especially Spot) is cheaper per unit — Fargate's premium buys away node ops. Also: no GPUs and no DaemonSets on Fargate.","worked":"Spiky internal API: Fargate scales tasks up/down with traffic, zero node management, no idle EC2 bill. Steady 24/7 GPU inference or a 200-node batch fleet at 90% utilization: self-managed EC2 (GPU instances / Spot) wins on cost, and GPUs aren't even available on Fargate.","related":["ecs-vs-eks","lambda-vs-fargate","ec2-purchasing-models"]},
  "lambda-vs-fargate": {"term":"Lambda vs Fargate (the choice)","group":"CLOUD-COMPUTE","aliases":["lambda or fargate","serverless compute choice","fargate vs lambda"],"eli5":"Both are 'no servers,' but Lambda is for short bursty events and Fargate is for longer-running containerized work.","memory":"Lambda = sprint (short, explosive, scale-to-zero). Fargate = marathon (long, steady, full container).","full":"<p>Both are serverless, but they sit at different points on the duration/granularity curve. <strong>Lambda</strong> excels at short (<15 min), event-driven, highly bursty units with instant fan-out and true scale-to-zero — S3-event processing, stream record handlers, webhooks, glue. <strong>Fargate</strong> runs full containers for longer, heavier, or steadier work — anything beyond Lambda's 15-min / 10 GB / 6 MB-sync-payload ceilings, custom runtimes, large images, or sustained throughput where per-invocation pricing gets expensive.</p><p>The decision usually hinges on three things: <em>duration</em> (does it risk exceeding 15 min?), <em>burstiness</em> (do you need scale-to-zero and instant fan-out, or steady throughput?), and <em>packaging</em> (lightweight function vs full container image). A common pattern uses Lambda as the trigger and Fargate/Batch for the heavy lifting.</p>","trap":"Gotcha: defaulting to Lambda for everything serverless. The 15-minute timeout, 6 MB sync payload, and 10 GB memory ceilings are the forcing functions — when a job brushes any of them, Fargate (or Batch/EMR) is the right move, not heroics chunking Lambda with Step Functions.","worked":"Ingest: an S3 PutObject fires a Lambda that validates and enqueues (sub-second, bursty) — perfect Lambda. The downstream 40-minute Parquet compaction the event represents runs as a Fargate task (or EMR job) — too long and too heavy for Lambda.","related":["lambda-limits","fargate","lambda-cold-start","ecs-vs-eks"]}
});
