/* Data Skew (SPARK-SKEW) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"SPARK-SKEW","front":"What is data skew in a Spark shuffle, mechanically?","back":"Skew is when the hash-partitioning of a shuffle key sends a disproportionate share of rows to a few partitions, so a handful of <strong>straggler</strong> tasks process far more data than the rest. Because a stage finishes only when its slowest task finishes, those stragglers dominate wall-clock while most executors sit idle. The job's parallelism is effectively capped at the size of the largest partition, not the total core count."},
  {"sec":"SPARK-SKEW","front":"How do you spot skew in the Spark UI?","back":"On the <strong>Stages</strong> tab, open the task <em>summary metrics</em> and compare the <code>Max</code> to the <code>75th percentile</code> (or median) for <em>Duration</em>, <em>Shuffle Read Size/Records</em>, and <em>Spill</em>. A skewed stage shows one or a few tasks with shuffle read and runtime 10x+ the median, often with heavy disk/memory spill, while the rest finished long ago. The event timeline shows a long tail of one bar extending past all others."},
  {"sec":"SPARK-SKEW","front":"Why does a single hot key cap parallelism no matter how many cores you add?","back":"All rows for a given key hash to the <em>same</em> reduce partition, and one partition is processed by exactly one task on one core. So every row of the hot key must flow through a single task; adding executors speeds up the other partitions but does nothing for the straggler. This is why throwing hardware at skew fails — you must change the <em>key distribution</em>, not the cluster size."},
  {"sec":"SPARK-SKEW","front":"What is key salting and how does it break up a hot join/group key?","back":"<strong>Salting</strong> appends a random suffix (e.g. <code>0..N-1</code>) to the key so one hot key becomes N synthetic keys that hash to N different partitions, spreading the load across N tasks. For <em>groupBy</em> you aggregate twice: once on the salted key (partial), then strip the salt and aggregate again to combine partials. For <em>joins</em> you salt the skewed side and replicate the small side across all N salt values so every salted row still finds its match."},
  {"sec":"SPARK-SKEW","front":"Salted group-by aggregation: why must you aggregate twice, and which aggregations is it safe for?","back":"First pass groups on <code>(key, salt)</code> producing N partials per real key; second pass strips the salt and re-aggregates the partials into the final value. It's exact only for <em>associative + commutative</em> reductions (<code>sum</code>, <code>count</code>, <code>min</code>, <code>max</code>, and <code>avg</code> if you carry sum+count separately). For non-decomposable aggregates like exact <code>median</code>/percentiles or exact <code>countDistinct</code>, two-stage salting doesn't trivially work — you need sketches (<code>approx_count_distinct</code>/HLL, <code>percentile_approx</code>) or a different approach."},
  {"sec":"SPARK-SKEW","front":"How does a broadcast (map-side) join eliminate shuffle skew entirely?","back":"If one side fits in memory, Spark broadcasts it to every executor and the large side is joined locally with no shuffle of the big table — so its key distribution is irrelevant and skew disappears. It's auto-triggered when the estimated build side is under <code>spark.sql.autoBroadcastJoinThreshold</code> (default 10MB), or forced with <code>broadcast(df)</code> / hint <code>/*+ BROADCAST */</code>. Caveat: the small side is collected to the driver, then shipped to every executor (TorrentBroadcast), so an over-large or mis-estimated broadcast OOMs the driver or executors."},
  {"sec":"SPARK-SKEW","front":"What does AQE skew-join handling do, and what triggers it?","back":"With <code>spark.sql.adaptive.enabled=true</code> and <code>spark.sql.adaptive.skewJoin.enabled=true</code>, AQE reads real shuffle map-output sizes at runtime and <em>splits</em> any partition deemed skewed into multiple sub-partitions, reading (replicating) the matching partition on the other side so each split joins correctly. A partition is skewed if its size exceeds both <code>skewedPartitionFactor</code> (default 5.0) x the <em>median</em> partition size AND <code>skewedPartitionThresholdInBytes</code> (default 256MB)."},
  {"sec":"SPARK-SKEW","front":"What are the exact AQE skew-join thresholds and their defaults?","back":"A shuffle partition is treated as skewed when its size is greater than <code>spark.sql.adaptive.skewJoin.skewedPartitionFactor</code> (default <strong>5.0</strong>) times the <em>median</em> partition size, AND greater than <code>spark.sql.adaptive.skewJoin.skewedPartitionThresholdInBytes</code> (default <strong>256MB</strong>). Both conditions must hold. AQE then splits the offending partition into chunks whose target size is <code>max(advisoryPartitionSizeInBytes, average non-skewed partition size)</code> — where <code>spark.sql.adaptive.advisoryPartitionSizeInBytes</code> defaults to 64MB."},
  {"sec":"SPARK-SKEW","front":"Why are NULL join keys a classic skew source, and how do you fix them?","back":"All <code>NULL</code> keys hash to one partition, yet in SQL semantics <code>NULL = NULL</code> never matches — so on an inner join they generate a giant straggler that produces <em>zero</em> output (pure waste). Fix by filtering nulls out before an inner join, or by mapping them to a random/non-matching sentinel (e.g. <code>coalesce(key, concat('__null_', rand()))</code>) so they scatter and drop. Skewed nulls are the cheapest skew to fix because on an inner join the rows can't contribute to the result anyway."},
  {"sec":"SPARK-SKEW","front":"Why is groupByKey discouraged versus reduceByKey/aggregateByKey for skew?","back":"<code>groupByKey</code> ships <em>every</em> value for a key across the shuffle with no map-side combine, so a hot key sends all its raw values to one reducer — maximal shuffle volume and spill. <code>reduceByKey</code>/<code>aggregateByKey</code> apply a map-side combiner first, so each mapper sends one partial per key, drastically cutting the bytes the hot partition must read. The hot key is still on one partition, but the volume (and spill) is far smaller, and in the DataFrame API the Catalyst hash aggregate already does partial aggregation for you."},
  {"sec":"SPARK-SKEW","front":"What is the 'isolate the hot keys' / two-path strategy for skewed joins?","back":"Identify the few skewed keys (e.g. via approx frequency or sampling), then split the workload: process the hot keys with a <em>broadcast</em> or <em>salted</em> join, and process the long tail of normal keys with an ordinary shuffle join, then <code>union</code> the results. This avoids salting the entire dataset (which inflates the small side N-fold) and confines the expensive treatment to the handful of keys that actually need it."},
  {"sec":"SPARK-SKEW","front":"How is salting a skewed join different from salting a group-by?","back":"In a join you can't just salt the big side — the matching rows on the small side must still find every salted variant. So you salt the skewed side with <code>0..N-1</code> and <em>explode/replicate</em> the small side across all N salt values (cross-join with the salt range), so a key's N salted rows each match a replicated small-side row. Group-by salting needs no replication because there's only one stream; it just re-aggregates to remove the salt."},
  {"sec":"SPARK-SKEW","front":"Will increasing spark.sql.shuffle.partitions fix skew?","back":"Generally no. Raising <code>spark.sql.shuffle.partitions</code> (default 200) helps when <em>many</em> moderately-large partitions exist, but a single hot key still hashes entirely to one partition regardless of the partition count — that straggler is unchanged. More partitions can even hurt via small-file/scheduling overhead. Use it for general balance, but treat true key skew with salting, broadcast, isolation, or AQE."},
  {"sec":"SPARK-SKEW","front":"Why can salt cardinality N be too high or too low, and how do you pick it?","back":"N too low under-splits the hot key (straggler persists); N too high multiplies the small-side replica / partial count, adds shuffle overhead, and creates tiny partitions for normal keys. Choose N roughly so the hottest key's volume divided by N approaches the median partition size — i.e. size the salt to the <em>skew ratio</em>, not a fixed constant. For mixed workloads, salt only the hot keys (variable salt) so cold keys keep N=1."},
  {"sec":"SPARK-SKEW","front":"What runtime symptom besides long task duration signals skew, and why?","back":"Heavy <strong>disk/memory spill</strong> on one or two tasks (visible as <em>Spill (Memory)</em>/<em>Spill (Disk)</em> in task metrics). The straggler's partition exceeds executor memory, so its sort/aggregate/join build spills to disk, adding I/O on top of the volume imbalance. You may also see one task with a far larger <em>Peak Execution Memory</em> and GC time, and in extreme cases an executor OOM/lost-task on that partition specifically."},
  {"sec":"SPARK-SKEW","front":"When does AQE skew-join NOT help, and what's the fallback?","back":"AQE skew splitting only applies to <em>sort-merge / shuffle-hash joins</em> on the shuffle stage — it does nothing for skew in a pure <code>groupBy</code>/aggregation, for <code>window</code> partitions, or in a non-join shuffle. It also can't split below one row, so a single mega-key still bottlenecks. Fall back to salting (for group-by/window), hot-key isolation, or broadcasting the small side."}
);

Object.assign(GLOSSARY_DATA, {
  "straggler-task": {"term":"Straggler Task","group":"SPARK-SKEW","aliases":["straggler","long-tail task","tail task"],"eli5":"The one task in a stage that takes vastly longer than its peers and holds up the whole stage.","memory":"A stage runs at the speed of its slowest task — the straggler sets the clock.","full":"<p>A straggler is the task whose runtime sits in the long tail of a stage's task-duration distribution. Because Spark stages are barrier-synchronized at shuffle boundaries — the next stage can't start until <em>all</em> tasks of the current stage finish — a single straggler stretches the stage's wall-clock to its own duration regardless of how fast the others were.</p><p>Skew is the dominant cause (an oversized partition), but stragglers can also come from a slow/failing node, GC pauses, or hot remote-read contention. Spark's <em>speculative execution</em> (<code>spark.speculation</code>) re-launches suspected stragglers on another node, which helps node-level slowness but NOT data skew — a re-run of the same giant partition is just as slow.</p>","trap":"Speculative execution is a tempting answer but it does NOT fix skew: re-running the same oversized partition elsewhere is equally slow. Speculation cures slow nodes, not big partitions.","related":["data-skew","hot-key"]},
  "key-salting": {"term":"Key Salting","group":"SPARK-SKEW","aliases":["salting","salt","salted key","salted join"],"eli5":"Add a random number to a hot key so it splits into many keys that spread across many partitions.","memory":"Salt the key, scatter the load: one hot key -> N cooler keys across N partitions.","full":"<p>Salting appends a random suffix <code>0..N-1</code> to a key so one hot value becomes N synthetic keys hashing to N partitions, parallelizing the straggler across N tasks. For <strong>group-by</strong> you aggregate in two stages: aggregate on <code>(key, salt)</code> for partials, strip the salt, then aggregate the partials — exact only for associative+commutative reductions (sum, count, min, max, avg-as-sum/count).</p><p>For <strong>joins</strong> you salt the skewed side and must <em>replicate</em> the small side across all N salt values (cross-join with the salt range) so every salted row still finds its match. Pick N by the skew ratio (hottest-key volume / median partition size); too small leaves a straggler, too large inflates shuffle and the small-side replica. Best practice: variable salt — N>1 only for hot keys, N=1 for cold ones.</p>","trap":"The classic miss: people salt only the big side of a join and forget to REPLICATE the small side across all salt values — then the salted rows match nothing and you silently lose join output.","worked":"Hot key 'X' with 100M rows, median partition 1M rows -> N≈100. Big side key becomes concat('X','_', rand_int(0,99)); small side row for 'X' is exploded into X_0..X_99.","related":["data-skew","hot-key","two-stage-aggregation","hot-key-isolation","broadcast-join"]},
  "two-stage-aggregation": {"term":"Two-Stage (Salted) Aggregation","group":"SPARK-SKEW","aliases":["two-phase aggregation","partial then final aggregation","aggregate twice"],"eli5":"Aggregate on the salted key first, then strip the salt and aggregate the partials to get the real answer.","memory":"Partial then final: salt-aggregate, de-salt, re-aggregate.","full":"<p>To de-skew a <code>groupBy</code> on a hot key, you split each real key into N salted keys, aggregate to get N partial results spread across N partitions (stage 1), then remove the salt and combine the N partials into the final value (stage 2). The hot work is now parallel across N tasks in stage 1; stage 2 only shuffles N small partials per key.</p><p>This is exact only when the aggregation is decomposable (associative + commutative): <code>sum</code>, <code>count</code>, <code>min</code>, <code>max</code>, and <code>avg</code> if you carry <code>sum</code> and <code>count</code> separately and divide at the end. Non-decomposable aggregates — exact <code>median</code>/percentiles, exact <code>countDistinct</code> — don't combine from partials, so use approximate sketches (<code>approx_count_distinct</code>/HLL, <code>percentile_approx</code>) or a different layout.</p>","trap":"Computing avg by averaging the partial averages is WRONG — averages aren't associative. Carry sum and count through both stages and divide only at the very end.","worked":"sum(amount) by merchant: stage1 = sum over (merchant, salt); stage2 = sum of those partial sums over merchant. Result is bit-for-bit identical to the unsalted sum.","related":["key-salting","hot-key","group-by-key-antipattern"]},
  "aqe-skew-join": {"term":"AQE Skew-Join Optimization","group":"SPARK-SKEW","aliases":["adaptive query execution skew","skewJoin","OptimizeSkewedJoin","adaptive skew handling"],"eli5":"At runtime Spark notices an oversized shuffle partition and splits it into pieces so several tasks share the load.","memory":"AQE = a runtime referee that chops the fat shuffle partition into slices, but only for joins.","full":"<p>Adaptive Query Execution (<code>spark.sql.adaptive.enabled=true</code>, on by default since Spark 3.2) reads actual shuffle map-output statistics after the map stage and, with <code>spark.sql.adaptive.skewJoin.enabled=true</code>, <em>splits</em> any skewed shuffle partition into multiple sub-partitions, reading the corresponding partition on the other join side once per split so each piece still produces correct results. A partition counts as skewed only if it exceeds BOTH <code>skewedPartitionFactor</code> (default 5.0) x the <em>median</em> AND <code>skewedPartitionThresholdInBytes</code> (default 256MB).</p><p>It's powerful because it needs no code change and uses real runtime sizes, but it's scoped to <strong>joins</strong> (sort-merge / shuffle-hash) — it won't de-skew a plain <code>groupBy</code>, a <code>window</code> partition, or a single un-splittable mega-key. For those, fall back to salting or hot-key isolation.</p>","trap":"Candidates assume AQE fixes ALL skew. It only splits JOIN shuffle partitions — group-by/window skew is untouched, and a single key that can't be split below itself still bottlenecks.","worked":"Median non-skewed partition is 200MB and one partition is 2GB: 2GB > 5x200MB and > 256MB, so it qualifies. The split target is max(advisory 64MB, ~200MB avg) = ~200MB, so AQE carves it into ~10 chunks, each joined against a re-read of the other side's matching partition.","related":["data-skew","broadcast-join","key-salting","null-key-skew"]},
  "null-key-skew": {"term":"Null-Key Skew","group":"SPARK-SKEW","aliases":["null key skew","null skew","skewed nulls"],"eli5":"All the NULL join keys pile into one partition, run forever, and produce zero matches.","memory":"Nulls all rush one door, then match nothing — filter or scatter them.","full":"<p>Every <code>NULL</code> (or sentinel like empty string / <code>0</code>) hashes to a single partition, creating a straggler — yet SQL's <code>NULL = NULL</code> is never true, so an inner join on nulls emits <em>no</em> output. You pay a massive straggler for rows that can't contribute, the most wasteful skew there is.</p><p>Fixes: filter nulls before an inner join (<code>where key is not null</code>), or for an outer join where you must keep them, map nulls to a random non-matching sentinel so they scatter and naturally drop, e.g. <code>coalesce(key, concat('__null_', cast(rand()*100 as int)))</code>. Because null rows never satisfy an equi-join, scattering them is always safe for inner joins.</p>","trap":"On an inner join you can drop null keys outright (they never match), so the 'fix' is often just a pre-filter — interviewers want you to recognize the work is pure waste, not to reach for salting.","worked":"orders JOIN customers ON customer_id where 8% of orders have NULL customer_id: those rows form one 8%-sized straggler producing zero joins; a WHERE customer_id IS NOT NULL before the join removes the straggler entirely.","related":["data-skew","hot-key","key-salting","aqe-skew-join"]},
  "group-by-key-antipattern": {"term":"groupByKey Anti-Pattern","group":"SPARK-SKEW","aliases":["groupByKey","groupByKey vs reduceByKey","avoid groupByKey"],"eli5":"groupByKey ships every raw value across the shuffle with no pre-combine, so a hot key floods one reducer.","memory":"groupByKey moves the data; reduceByKey moves the answer.","full":"<p>In the RDD API, <code>groupByKey</code> performs no map-side combine: every value for a key is shuffled to the reducer that owns that key. For a hot key this sends all raw values to one partition, maximizing shuffle bytes and spill. <code>reduceByKey</code>/<code>aggregateByKey</code> apply a map-side combiner first, so each mapper emits one partial per key — the hot partition still exists but reads far fewer bytes.</p><p>The DataFrame/Dataset API mostly removes this footgun: Catalyst's hash aggregate does partial aggregation automatically, so <code>df.groupBy().agg(sum())</code> behaves like <code>reduceByKey</code>. The anti-pattern persists in raw RDD code and in typed Dataset <code>groupByKey().mapGroups</code>/<code>flatMapGroups</code>, which materialize all values per group and can spill or OOM on a hot key.</p>","trap":"Saying 'always avoid groupByKey' is too glib for the DataFrame API — there Catalyst already does partial aggregation. The real hazard is RDD groupByKey and typed Dataset mapGroups/flatMapGroups, which buffer all values per key.","worked":"Counting events per user: rdd.groupByKey().mapValues(_.size) shuffles every event to one reducer for a hot user; rdd.map((_,1)).reduceByKey(_+_) combines on the map side so the hot reducer reads one partial per mapper.","related":["data-skew","hot-key","two-stage-aggregation"]},
  "hot-key-isolation": {"term":"Hot-Key Isolation (Two-Path Join)","group":"SPARK-SKEW","aliases":["hot key isolation","two-path join","skew split union","isolate hot keys"],"eli5":"Handle the few skewed keys with a special join and the rest normally, then union the two results.","memory":"Split, treat, union: VIP line for the hot keys, regular line for the rest.","full":"<p>Rather than applying expensive treatment to the whole dataset, you identify the handful of skewed keys (via sampling/approx frequency), split the input into a hot subset and a cold subset, join each with the appropriate strategy — broadcast or salt the hot keys, ordinary shuffle join the cold tail — and <code>union</code> the results. This confines salting's small-side replication or broadcast cost to only the keys that need it.</p><p>It's the manual counterpart to AQE skew splitting and is preferred when you know your skewed keys are stable (e.g. a known default id) or when the skew is in a group-by/window that AQE won't split. The cost is added pipeline complexity (two code paths, a union) and the need to keep the hot-key list current.</p>","trap":"If your hot-key list goes stale (a new value becomes hot), the cold path silently re-skews. Re-derive the hot keys from data rather than hard-coding them.","worked":"Known bot user_id 42 is 25% of rows: filter it out, broadcast-join just user 42's rows, shuffle-join the rest, then union — no salt explosion on the 75% of normal traffic.","related":["hot-key","key-salting","broadcast-join","aqe-skew-join"]}
});
