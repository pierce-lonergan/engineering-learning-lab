/* Threat Modeling & Secure Design (SEC-THREAT) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"SEC-THREAT","front":"What is STRIDE and what is it used for?","back":"STRIDE is a threat-classification taxonomy used to systematically enumerate threats against a design. It stands for <strong>Spoofing</strong>, <strong>Tampering</strong>, <strong>Repudiation</strong>, <strong>Information disclosure</strong>, <strong>Denial of service</strong>, and <strong>Elevation of privilege</strong>. You walk each element of a data-flow diagram (processes, data stores, flows, external entities) and ask which STRIDE categories apply, so coverage is structured rather than ad hoc."},
  {"sec":"SEC-THREAT","front":"Map each STRIDE category to the security property it violates.","back":"<ul><li><strong>Spoofing</strong> → Authentication</li><li><strong>Tampering</strong> → Integrity</li><li><strong>Repudiation</strong> → Non-repudiation</li><li><strong>Information disclosure</strong> → Confidentiality</li><li><strong>Denial of service</strong> → Availability</li><li><strong>Elevation of privilege</strong> → Authorization</li></ul>Each letter is the violation of one desirable property, which is why the mitigation for each is the corresponding control (e.g. spoofing → strong authn, tampering → signing/hashing)."},
  {"sec":"SEC-THREAT","front":"What is a trust boundary, and why does it matter in threat modeling?","back":"A trust boundary is any point in a system where data or control crosses between zones of differing privilege or trust (e.g. internet→service, service→database, user-space→kernel, tenant A→tenant B). It matters because threats concentrate at boundaries: every input crossing one must be treated as hostile and re-validated/re-authorized. On a data-flow diagram, drawing the boundaries first tells you exactly where to apply STRIDE."},
  {"sec":"SEC-THREAT","front":"What does \"treat every input crossing a boundary as hostile\" mean in practice?","back":"Validate, canonicalize, and authorize data at the point it enters your trust zone, regardless of where it claims to come from. Concretely: never trust client-supplied tenant IDs, roles, prices, or auth claims without server-side verification; use allow-lists over deny-lists; parameterize queries; and re-check authorization on the server even if the UI already hid the button. The attack surface is the sum of all such inputs."},
  {"sec":"SEC-THREAT","front":"What is the CIA triad?","back":"The three core security properties a system protects: <strong>Confidentiality</strong> (only authorized parties can read data), <strong>Integrity</strong> (data is not modified undetectably), and <strong>Availability</strong> (the system is usable when needed). Most controls map to one or more: encryption serves confidentiality, signing/hashing serves integrity, redundancy and rate-limiting serve availability."},
  {"sec":"SEC-THREAT","front":"How do confidentiality, integrity, and availability sometimes trade off against each other?","back":"They pull in different directions. Aggressive fail-secure behavior (deny on doubt) protects confidentiality/integrity but harms availability; caching and replication boost availability but widen the surface for stale or leaked data. A classic tension: fail-closed firewalls and circuit breakers protect integrity but can cause an outage. Senior design names which property dominates for a given system (e.g. a payments ledger prioritizes integrity, a CDN prioritizes availability)."},
  {"sec":"SEC-THREAT","front":"What is the principle of least privilege?","back":"Every user, service, process, and credential should hold only the minimum permissions needed for its task, for the minimum time. In practice: scoped API keys instead of god tokens, per-service IAM roles, read-only DB users for read paths, short-lived credentials, and just-in-time elevation. It shrinks blast radius: a compromised component can only do what it was narrowly authorized to do."},
  {"sec":"SEC-THREAT","front":"What is defense in depth, and why isn't a single strong control enough?","back":"Defense in depth layers independent controls so that the failure of any one does not breach the system: WAF + input validation + parameterized queries + least-privilege DB user + encryption at rest all defend the same asset. No single control is perfect (bugs, misconfig, novel attacks), so redundancy across layers means an attacker must defeat several independent mechanisms. It is the security analog of not having a single point of failure."},
  {"sec":"SEC-THREAT","front":"What does secure-by-default / fail-secure mean?","back":"<strong>Secure-by-default</strong>: the out-of-the-box configuration is the safe one (TLS on, auth required, ports closed, verbose errors off) so safety doesn't depend on operators remembering to harden. <strong>Fail-secure</strong> (fail-closed): when a component errors or is uncertain, it denies rather than allows — e.g. an authz check that throws should reject the request, not let it through. The opposite, fail-open, is how outages turn into breaches."},
  {"sec":"SEC-THREAT","front":"What is complete mediation?","back":"Every access to every protected resource must be checked against the authorization policy <em>every time</em> — no bypass paths, no relying on a cached \"already allowed\" decision, no trusting that the caller went through the front door. Practically this means authz is enforced at a chokepoint the request cannot skip (e.g. a gateway or middleware), not scattered and skippable per-handler, and that direct-object access (IDOR) is blocked because each fetch re-checks ownership."},
  {"sec":"SEC-THREAT","front":"What is zero trust and how does it differ from a perimeter model?","back":"Zero trust removes implicit trust based on network location: being \"inside the VPC/corp network\" grants nothing. Every request is authenticated and authorized on its own merits (verified identity, device posture, scoped token), and traffic is encrypted east-west, not just at the edge. The perimeter (castle-and-moat) model trusts anything past the firewall — fatal once one internal host is compromised, since the attacker moves laterally freely."},
  {"sec":"SEC-THREAT","front":"Why must secrets live in a vault rather than committed .env files?","back":"Committed secrets leak: they persist in git history forever (even after deletion), get copied to laptops and CI logs, and can't be rotated centrally. A secrets manager (HashiCorp Vault, AWS/GCP Secrets Manager) gives centralized storage, access control, audit logging, automatic rotation, and dynamic short-lived credentials. Apps fetch secrets at runtime via a workload identity, so nothing sensitive sits in the repo or the image."},
  {"sec":"SEC-THREAT","front":"What is \"shift-left\" in security, and why threat-model at design time?","back":"Shift-left means moving security activities earlier in the lifecycle — threat modeling at design review, SAST/dependency scanning in CI, secure defaults in scaffolding — rather than pen-testing after ship or fixing after a breach. Fixing a design flaw on a whiteboard costs minutes; fixing it after it's in production, has data dependencies, and caused an incident costs orders of magnitude more. Threat modeling is the cheapest control because it's just thinking before building."},
  {"sec":"SEC-THREAT","front":"What is the difference between the attack surface and the attack vector?","back":"The <strong>attack surface</strong> is the total set of points where an attacker could try to enter or extract data — every exposed endpoint, input field, port, dependency, and trust boundary crossing. An <strong>attack vector</strong> is a specific path an attacker actually uses (e.g. SQL injection on the login form). You reduce risk by shrinking the surface (close ports, remove unused features, fewer trust boundaries) and by hardening each remaining vector."},
  {"sec":"SEC-THREAT","front":"How does authentication differ from authorization, and which STRIDE threats target each?","back":"<strong>Authentication</strong> establishes <em>who</em> you are; <strong>authorization</strong> establishes <em>what</em> you may do. Spoofing attacks the authn layer (impersonating an identity); elevation of privilege attacks the authz layer (acting beyond your granted permissions, e.g. via a missing ownership check / IDOR). They are distinct controls — passing authn says nothing about being authorized for a specific resource, which is why complete mediation re-checks authz per access."},
  {"sec":"SEC-THREAT","front":"How does repudiation get mitigated, and why does it depend on observability?","back":"Repudiation is a user plausibly denying they performed an action. The mitigation is non-repudiation: tamper-evident, attributable audit logs (who, what, when, from where), ideally append-only or signed, plus synchronized clocks. This is where security meets observability — without trustworthy structured logs you can neither prove an action occurred nor reconstruct an incident timeline. Logs used for security must themselves be integrity-protected so an attacker can't erase their tracks."}
);

Object.assign(GLOSSARY_DATA, {
  "stride": {"term":"STRIDE","group":"SEC-THREAT","aliases":["stride","stride model","stride threat model"],"eli5":"A six-letter checklist that forces you to ask 'how could this be attacked?' in six specific ways for every part of your design.","memory":"Each letter maps to one violated property; the fix for each is the matching control (spoof→authn, tamper→signing, repudiate→audit log, info-disclosure→encryption, DoS→rate-limit, EoP→authz).","full":"<p>STRIDE is a threat taxonomy from Microsoft (Garg and Kohnfelder, ~1999) that classifies threats into six categories: <strong>S</strong>poofing, <strong>T</strong>ampering, <strong>R</strong>epudiation, <strong>I</strong>nformation disclosure, <strong>D</strong>enial of service, and <strong>E</strong>levation of privilege. Each category is the negation of a desirable property: authentication, integrity, non-repudiation, confidentiality, availability, and authorization respectively.</p><p>In practice you draw a data-flow diagram, mark trust boundaries, then walk each element (external entity, process, data store, data flow) and ask which STRIDE categories apply and what mitigation each demands. The value is <em>structure</em> and coverage — it converts 'think about security' into a finite, reviewable enumeration that a team can do at design time.</p>","trap":"Interviewers ask you to map STRIDE to mitigations, not just recite the acronym. If you can't say 'Tampering → integrity → HMAC/signing' you've only memorized the letters. Also: STRIDE enumerates threats, it does NOT rank them — you still need a risk model (likelihood × impact, e.g. a risk matrix, or the older DREAD which Microsoft itself deprecated for subjective scoring) to prioritize.","worked":"Upload endpoint behind an API gateway: Spoofing → forged JWT (mitigate: verify signature + audience); Tampering → modified file in transit (TLS + checksum); Information disclosure → another tenant reading the object (per-object ownership check); Elevation → path traversal writing outside the tenant prefix (canonicalize + allow-list).","related":["trust-boundary","cia-triad","least-privilege","complete-mediation"]},
  "trust-boundary": {"term":"Trust Boundary","group":"SEC-THREAT","aliases":["trust boundary","trust boundaries","attack surface","boundary"],"eli5":"A line on the diagram where data moves from somewhere you control to somewhere you don't — so anything crossing it could be a lie.","memory":"Boundaries are where you draw the dashed lines on a DFD — and dashed lines are where STRIDE bites. No boundary crossed, no new threat introduced.","full":"<p>A trust boundary is any junction where data or control passes between zones of different privilege or trustworthiness: internet→edge, service→service, app→database, one tenant→another, user-space→kernel. Threats cluster at these lines because crossing one is exactly where an attacker tries to inject, impersonate, or exfiltrate.</p><p>The operational rule is: <strong>every input crossing inward is hostile until validated and authorized</strong>. That means server-side validation (never trust client-supplied roles, prices, or tenant IDs), canonicalization before checks, parameterized queries, and re-authorization at the boundary. The <em>attack surface</em> is the union of everything crossing your boundaries; reducing the number and width of boundaries reduces risk.</p>","trap":"Candidates forget that internal/service-to-service hops are trust boundaries too. 'It's behind the firewall' is the perimeter fallacy — in a zero-trust world the service→service call is a boundary that must re-authenticate. The single most common real bug: trusting a client-supplied X-Tenant-Id header instead of deriving tenant from the verified token.","worked":"A reporting API derives tenant from a client header and runs SELECT ... WHERE tenant_id = :header. Any user sets the header to a victim's ID and reads their data — a cross-tenant info-disclosure. Fix: take tenant_id from the authenticated principal, never from request-controlled input, and scope queries by it server-side.","related":["stride","zero-trust","complete-mediation","least-privilege"]},
  "cia-triad": {"term":"CIA Triad","group":"SEC-THREAT","aliases":["cia triad","cia","confidentiality integrity availability","confidentiality","integrity","availability"],"eli5":"The three things security protects: keeping secrets secret, keeping data un-tampered, and keeping the system up.","memory":"C-I-A = read / write / reach. Confidentiality = can't read it, Integrity = can't change it, Availability = can reach it.","full":"<p>The CIA triad names the three properties security exists to preserve. <strong>Confidentiality</strong>: only authorized parties read the data (controls: encryption, access control, least privilege). <strong>Integrity</strong>: data isn't modified undetectably (controls: hashing, digital signatures/HMAC, write authorization, append-only logs). <strong>Availability</strong>: the system responds when needed (controls: redundancy, rate-limiting, DDoS protection, capacity).</p><p>It's a useful lens because every control and every threat maps onto one or more of the three, and because the three can conflict — fail-secure favors C and I at the cost of A; aggressive caching favors A but risks C and I. Senior design states which property dominates for the asset at hand.</p>","trap":"Don't conflate integrity with confidentiality. Encryption alone does NOT guarantee integrity — unauthenticated stream/CTR-style modes are malleable, which is why you use authenticated encryption (AES-GCM) or sign-then-encrypt. 'It's encrypted so it can't be tampered with' is wrong.","worked":"A signed-and-encrypted audit record: AES-GCM gives confidentiality (can't read) AND integrity (auth tag detects tampering); storing it append-only on replicated storage adds availability. Drop the GCM tag for plain CTR mode and you keep confidentiality but lose integrity — flipping a ciphertext bit flips the corresponding plaintext bit on decrypt, undetected.","related":["stride","defense-in-depth","secrets-management"]},
  "defense-in-depth": {"term":"Defense in Depth","group":"SEC-THREAT","aliases":["defense in depth","defence in depth","layered security","layered defense"],"eli5":"Stack several independent locks so that one failing doesn't open the door.","memory":"Swiss-cheese model: every layer has holes, but stack enough offset slices and no straight path goes through.","full":"<p>Defense in depth layers multiple independent controls protecting the same asset, so that the failure or bypass of any single layer does not result in a breach. Example layers for a database-backed API: network policy → WAF → authentication → input validation → parameterized queries → least-privilege DB user → encryption at rest → audit logging.</p><p>The key word is <strong>independent</strong>: layers must fail for different reasons. Two libraries with the same parsing bug aren't depth, they're one layer counted twice. It's the security analogue of avoiding a single point of failure, and it buys you margin against the inevitable bug, misconfiguration, or zero-day in any one mechanism.</p>","trap":"Defense in depth is not an excuse to weaken any individual layer ('the WAF will catch it' is how injection bugs ship). Each layer should be as strong as if it were the only one. And redundant-but-correlated controls give a false sense of depth — diversity of failure mode is what counts.","worked":"SQL injection attempt: WAF blocks common payloads (layer 1, bypassable), but even if it's bypassed the query is parameterized so the payload is inert (layer 2), and even on RCE the DB user is read-only and scoped to one schema (layer 3), and the access is logged for detection (layer 4).","related":["least-privilege","cia-triad","secure-by-default","zero-trust"]},
  "secure-by-default": {"term":"Secure-by-Default / Fail-Secure","group":"SEC-THREAT","aliases":["secure by default","secure-by-default","fail secure","fail-secure","fail closed","fail-closed","fail open"],"eli5":"Ship with the safe settings already on, and when something breaks, lock the door rather than leave it open.","memory":"Secure-by-default = the lazy path is the safe path. Fail-secure = when in doubt, deny.","full":"<p><strong>Secure-by-default</strong> means the shipped configuration is the safe one without operator intervention: authentication required, TLS enforced, unused ports closed, debug/verbose errors off, permissions default-deny. Safety shouldn't depend on someone remembering to harden.</p><p><strong>Fail-secure</strong> (fail-closed) governs error behavior: when a control errors, times out, or is uncertain, it should <em>deny</em>. An authorization check that throws an exception must reject the request; a token-validation service that's unreachable must not wave traffic through. The dangerous opposite, <strong>fail-open</strong>, is precisely how a transient dependency outage becomes a security bypass.</p>","trap":"Fail-secure and high availability are in tension — a fail-closed auth dependency becomes a single point of outage. Interviewers probe this: the answer is to make the authz path itself highly available (cache decisions with short TTL, local policy eval) so you can fail closed without failing the business. Note that 'secure' isn't always 'deny': life-safety systems deliberately fail toward access — electrified egress-door locks are wired fail-safe so they UNLOCK on power loss or fire alarm (free egress) — so context decides which direction is the safe one.","worked":"An authz middleware calls a policy service that times out. Fail-secure: return 503/deny. Fail-open bug: a try/except that on error sets allowed = true 'to avoid blocking users.' The latter means any policy-service blip silently disables authorization platform-wide.","related":["complete-mediation","defense-in-depth","zero-trust","cia-triad"]},
  "complete-mediation": {"term":"Complete Mediation","group":"SEC-THREAT","aliases":["complete mediation","mediation","every access checked"],"eli5":"Check permission on every single access, every time — no skipping because it was allowed a moment ago.","memory":"Authenticate once, authorize every time. The check belongs on the read path, not the route table.","full":"<p>Complete mediation (a Saltzer-and-Schroeder principle) requires that <em>every</em> access to <em>every</em> protected resource be authorized against current policy on <em>every</em> request — no unchecked paths, no trusting a stale 'already allowed' decision, no assuming the caller entered through the front door. Authorization is enforced at a chokepoint the request cannot bypass.</p><p>The most common real-world violation is the <strong>IDOR</strong> (insecure direct object reference): an endpoint authenticates the user but fetches an object by client-supplied ID without re-checking that the user owns it. The fix is to scope every data access by the authenticated principal (e.g. <code>findByIdAndTenantId</code>) so authorization is inseparable from retrieval.</p>","trap":"Authentication is NOT mediation — knowing who the caller is doesn't authorize a specific object. Also, hiding a button or filtering a list in the UI is not mediation; the server must re-check, because the attacker calls the API directly. Caching authz decisions trades mediation completeness for latency — bound the TTL and invalidate on permission change.","worked":"GET /api/invoices/{id} authenticates the JWT, then does SELECT * FROM invoices WHERE id = :id. User authenticated as tenant A requests an invoice belonging to tenant B and gets it. Complete mediation fix: WHERE id = :id AND tenant_id = :auth_tenant — and return 404 (not 403) so the response doesn't confirm the object exists to a probing attacker.","related":["trust-boundary","least-privilege","secure-by-default","zero-trust"]},
  "zero-trust": {"term":"Zero Trust","group":"SEC-THREAT","aliases":["zero trust","zero-trust","ztna","beyondcorp","perimeter model","castle and moat"],"eli5":"Never trust a request just because it came from inside the network — make every request prove who it is and what it's allowed to do.","memory":"'Never trust, always verify.' The network is hostile, including yours. Identity is the new perimeter.","full":"<p>Zero trust eliminates implicit trust based on network position. There is no 'inside' that confers privilege: a request from the same VPC is treated like one from the internet. Every request is authenticated (verified identity, often mTLS or short-lived workload tokens) and authorized (scoped policy, sometimes device posture), and east-west traffic is encrypted, not just north-south at the edge.</p><p>It replaces the <strong>perimeter / castle-and-moat</strong> model, whose fatal flaw is that a single compromised internal host can move laterally with full trust. Zero trust assumes breach: by authenticating every hop and applying least privilege per service, it contains lateral movement. In practice it's identity-centric segmentation (service identity + policy) rather than IP-based firewall rules.</p>","trap":"Zero trust is NOT 'put everything behind a VPN' — a VPN just relocates the perimeter and still trusts whoever's on the tunnel. It's also not a single product. The senior framing: authenticate and authorize per request using verified identity and least privilege, assume the network is already compromised.","worked":"Service A calls Service B. Perimeter model: B accepts any caller from the cluster's IP range. Zero trust: A presents a short-lived SPIFFE X.509-SVID (SPIFFE ID in the cert SAN) over mTLS, B authenticates it, checks a policy that A may call this specific RPC, and the call is encrypted — so a compromised Service C in the same cluster still can't call B.","related":["trust-boundary","least-privilege","complete-mediation","secure-by-default"]}
});
