/* Architecture & Execution Model (SPARK-ARCH) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"SPARK-ARCH","front":"The three runtime roles in a Spark application","back":"<ul><li><strong>Driver</strong>: runs <code>main()</code>, hosts the <code>SparkSession</code>/DAG scheduler, plans the job, and tracks executor state. One per application.</li><li><strong>Executors</strong>: JVM processes on worker nodes that run tasks and hold cached/shuffled data. Multiple per app, long-lived for the app's duration.</li><li><strong>Cluster manager</strong>: external resource broker (Standalone, YARN, Kubernetes, Mesos-legacy) that grants the driver executors/containers.</li></ul>The driver and executors are <em>Spark</em>; the cluster manager is pluggable infrastructure."},
  {"sec":"SPARK-ARCH","front":"What does the cluster manager actually do — and what does it NOT do?","back":"The cluster manager only <strong>allocates resources</strong>: it launches the executor processes/containers the driver requests and reports node health. It does <strong>not</strong> schedule tasks, build the DAG, or know about RDDs/partitions — all of that lives in the driver. Swapping YARN for Kubernetes changes nothing about the logical execution model, only how containers get provisioned."},
  {"sec":"SPARK-ARCH","front":"Spark on YARN: client mode vs cluster mode (where the driver runs)","back":"<ul><li><strong>cluster mode</strong>: the driver runs <em>inside</em> a YARN ApplicationMaster container on the cluster. Survives client disconnect; preferred for production/scheduled jobs.</li><li><strong>client mode</strong>: the driver runs in the <em>submitting</em> process (e.g. your shell/notebook). Needed for interactive REPLs; if the client dies, the app dies, and driver-cluster network latency matters.</li></ul>Spark-on-K8s has the same distinction; <code>spark-submit --deploy-mode</code> selects it."},
  {"sec":"SPARK-ARCH","front":"SparkSession vs SparkContext — what changed and why","back":"<code>SparkSession</code> (Spark 2.0+) is the single unified entry point wrapping <code>SparkContext</code>, <code>SQLContext</code>, and <code>HiveContext</code>. It owns the Catalyst optimizer, the catalog, and DataFrame/Dataset APIs. <code>SparkContext</code> still exists underneath (<code>spark.sparkContext</code>) as the connection to the cluster and the only way to create low-level RDDs. You get <strong>one</strong> active <code>SparkContext</code> per JVM; you can derive multiple <code>SparkSession</code>s from it via <code>newSession()</code> (e.g. for isolated catalogs/temp views)."},
  {"sec":"SPARK-ARCH","front":"Job → Stage → Task: how a Spark computation decomposes","back":"<ul><li>A <strong>job</strong> is triggered by one <em>action</em> (e.g. <code>count()</code>, <code>save()</code>).</li><li>A job splits into <strong>stages</strong> at <em>shuffle boundaries</em> — each stage is a chain of narrow (pipelineable) transformations.</li><li>A stage runs as a set of <strong>tasks</strong>, exactly <em>one task per partition</em> of the stage's input.</li></ul>So #tasks in a stage = #partitions, and #stages = (number of shuffles on the job's execution path) + 1."},
  {"sec":"SPARK-ARCH","front":"Why does a stage boundary occur exactly at a shuffle?","back":"Within a stage, all transformations are <strong>narrow</strong> — each output partition depends on a single input partition, so they pipeline in one task with no data movement. A <strong>wide</strong> dependency (e.g. <code>groupByKey</code>, <code>join</code>, <code>repartition</code>) requires data from many partitions to be redistributed across the network. That redistribution (the shuffle) must <em>complete and materialize</em> map-side output before the next stage's reduce tasks can read it, forcing a barrier — hence the boundary."},
  {"sec":"SPARK-ARCH","front":"Narrow vs wide dependencies — definition and examples","back":"<ul><li><strong>Narrow</strong>: each parent partition feeds at most one child partition. Examples: <code>map</code>, <code>filter</code>, <code>union</code>, <code>mapPartitions</code>. Pipelined within a stage, no shuffle, cheap recovery.</li><li><strong>Wide (shuffle)</strong>: a child partition depends on multiple parent partitions. Examples: <code>groupByKey</code>, <code>reduceByKey</code>, <code>join</code> (non-co-partitioned), <code>distinct</code>, <code>repartition</code>. Triggers a shuffle and a new stage.</li></ul>The narrow/wide distinction is what the DAG scheduler uses to cut stages."},
  {"sec":"SPARK-ARCH","front":"DAG scheduler vs task scheduler — division of labor","back":"<ul><li><strong>DAGScheduler</strong> (high-level, driver): builds the stage DAG from the RDD lineage, splits at shuffle boundaries, submits stages as <code>TaskSet</code>s in dependency order, and handles stage-level fault tolerance (resubmit on lost shuffle output).</li><li><strong>TaskScheduler</strong> (low-level, driver): takes a <code>TaskSet</code> and launches individual tasks on executors, applying <em>locality</em> preferences, handling retries of failed tasks, and speculative execution.</li></ul>DAGScheduler thinks in stages; TaskScheduler thinks in tasks and executor slots."},
  {"sec":"SPARK-ARCH","front":"Lazy evaluation: what's deferred and what forces execution","back":"<strong>Transformations</strong> (<code>map</code>, <code>filter</code>, <code>join</code>, <code>select</code>…) are lazy — they only extend the logical plan/lineage DAG and return a new RDD/DataFrame; nothing runs. An <strong>action</strong> (<code>count</code>, <code>collect</code>, <code>save</code>, <code>show</code>, <code>foreach</code>) materializes a result and submits a job. Laziness lets Catalyst/the DAG scheduler see the whole pipeline at once to fuse operations, push down filters/projections, and prune unread columns/partitions before any data moves."},
  {"sec":"SPARK-ARCH","front":"Why can lazy evaluation make errors surface 'late'?","back":"Because building a transformation chain runs no compute, a bug in a UDF or a bad path may not throw until an <em>action</em> triggers the job. Note the asymmetry: in <strong>classic</strong> Spark, DataFrame <strong>analysis</strong> errors (unknown column, type mismatch) are caught eagerly when the transformation is built (the analyzer runs per-DataFrame), while <strong>runtime</strong> errors (UDF exceptions, missing files) only appear at the action. (Spark Connect defers even analysis to the action.) RDDs defer essentially everything to the action."},
  {"sec":"SPARK-ARCH","front":"What runs on the driver vs on the executors?","back":"<ul><li><strong>Driver</strong>: plan construction, scheduling, <em>and</em> any plain Scala/Python code outside Spark operators — including the body that processes results from <code>collect()</code>/<code>take()</code>.</li><li><strong>Executors</strong>: the closures inside transformations/actions (<code>map</code>, <code>filter</code>, <code>foreachPartition</code>), which run in parallel across partitions.</li></ul>A closure that references a driver-side variable gets <em>serialized and shipped</em> to executors — non-serializable captures (e.g. a DB connection) cause <code>Task not serializable</code>."},
  {"sec":"SPARK-ARCH","front":"Why is collect() dangerous, and what to use instead","back":"<code>collect()</code> pulls <em>every</em> partition's data back to the driver's JVM heap, so a large result OOMs the single driver and serializes a distributed dataset through one process. Use <code>take(n)</code>/<code>show(n)</code> for inspection, <code>write</code> to a sink for full output, or aggregate first. If you truly need all rows locally, bound it with <code>limit</code> and size the driver heap (<code>spark.driver.memory</code>, plus <code>spark.driver.maxResultSize</code>, default <strong>1g</strong>, which caps total serialized results)."},
  {"sec":"SPARK-ARCH","front":"How does lineage give RDDs fault tolerance, and what's the cost?","back":"Each RDD records its <strong>lineage</strong> (the transformations that built it). If an executor dies and a partition is lost, Spark recomputes <em>only</em> that partition by replaying its narrow-dependency lineage — no replication needed. The catch: a <em>wide</em> dependency means recomputation may need many parent partitions, which is expensive. <code>checkpoint()</code> truncates lineage by writing to reliable storage; <code>cache()</code>/<code>persist()</code> avoids recompute but isn't fault-tolerant (lost cache blocks fall back to lineage)."},
  {"sec":"SPARK-ARCH","front":"Default partition count: where do partitions come from?","back":"Partition count is set at the source and at shuffles: file sources split by block size and <code>spark.sql.files.maxPartitionBytes</code> (default <strong>128MB</strong>); <code>parallelize</code> defaults to <code>spark.default.parallelism</code> (which itself defaults to local #cores, or total executor cores / 2 on a cluster). After a shuffle, output partitions default to <code>spark.sql.shuffle.partitions</code> (<strong>200</strong>). Since Spark 3.0, <strong>AQE</strong> (<code>spark.sql.adaptive.enabled</code>, on by default in 3.2+) coalesces these post-shuffle partitions at runtime, making the static 200 less critical."},
  {"sec":"SPARK-ARCH","front":"Why does one giant partition or a skewed key wreck a stage?","back":"A stage finishes only when its <em>slowest</em> task finishes, and there's one task per partition. If a shuffle sends a hot key's rows to a single partition (data skew), that one task does most of the work while others idle — wall-clock is dominated by the straggler, and that task can OOM. Mitigations: AQE skew join handling (<code>spark.sql.adaptive.skewJoin.enabled</code>, on by default but requires AQE enabled), salting the key, broadcast join for the small side, or repartitioning on a higher-cardinality key."},
  {"sec":"SPARK-ARCH","front":"Where is the parallelism cap actually set in a Spark app?","back":"Concurrent task slots = <code>(num executors) × (spark.executor.cores)</code>. Each task occupies one core slot, so a stage with more partitions than slots runs in waves. Right-sizing partitions to ~2-3× total cores keeps all slots busy without excessive scheduling overhead; far more partitions adds task-launch overhead, far fewer leaves cores idle."}
);

Object.assign(GLOSSARY_DATA, {
  "driver-executor-model": {"term":"Driver / Executor / Cluster-Manager Model","group":"SPARK-ARCH","aliases":["driver","executor","cluster manager","spark architecture","master-worker"],"eli5":"One brain (driver) plans the work, many workers (executors) do it in parallel, and a separate landlord (cluster manager) hands out the machines.","memory":"Driver = conductor, executors = orchestra, cluster manager = the venue's booking agent. The agent just rents you the hall; it never plays a note.","full":"<p>A Spark application is one <strong>driver</strong> process coordinating many <strong>executor</strong> processes. The driver runs your <code>main()</code>, holds the <code>SparkSession</code> and schedulers, builds the execution plan, and tracks which task ran where. Executors are long-lived JVMs (one per app, several per node) that execute tasks and cache/shuffle data in their own heaps. They communicate directly with the driver, not through the cluster manager.</p><p>The <strong>cluster manager</strong> (Standalone, YARN, Kubernetes) is pluggable and only brokers resources: the driver asks for N executors with M cores/G memory, the manager launches those containers. This separation is why the same application logic runs unchanged across YARN and K8s — only the provisioning layer differs.</p>","trap":"Candidates often say the cluster manager 'schedules tasks.' It does not — it only allocates executors. Task scheduling (which task on which executor, with locality) is entirely the driver's TaskScheduler.","worked":"On YARN cluster mode: <code>spark-submit</code> asks YARN's ResourceManager for an ApplicationMaster container; the driver runs there, requests executor containers, and from then on schedules tasks onto those executors directly — YARN is out of the loop until an executor dies.","related":["sparksession","dag-scheduler","job-stage-task","driver-side-execution"]},
  "sparksession": {"term":"SparkSession (vs SparkContext)","group":"SPARK-ARCH","aliases":["sparksession","sparkcontext","spark context","entry point","sqlcontext"],"eli5":"SparkSession is the one modern door into Spark; SparkContext is the older low-level connection it now wraps.","memory":"Session wraps Context: the Session is the lobby with all the amenities (SQL, catalog, config); the Context is the elevator shaft to the cluster.","full":"<p><code>SparkSession</code> (Spark 2.0+) unified the previously separate <code>SparkContext</code>, <code>SQLContext</code>, and <code>HiveContext</code> into a single entry point. It owns the DataFrame/Dataset APIs, the Catalyst optimizer, the catalog (databases/tables/temp views), and configuration. The underlying <code>SparkContext</code> — the actual connection to the cluster and the factory for low-level RDDs — is still reachable via <code>spark.sparkContext</code>.</p><p>Cardinality matters: there is exactly <strong>one</strong> active <code>SparkContext</code> per JVM (a second one throws <em>'Only one SparkContext may be running in this JVM'</em>), but you can spawn multiple <code>SparkSession</code>s from it via <code>newSession()</code> to get isolated catalogs/temp-view namespaces while sharing the same cluster connection, executors, and cached data.</p>","trap":"'Can you have two SparkContexts in one JVM?' — No (without unsupported hacks). But you CAN have multiple SparkSessions over one SparkContext via newSession(); that's the intended isolation mechanism.","worked":"<code>spark = SparkSession.builder.appName(\"x\").getOrCreate()</code> gives the session; <code>spark.sparkContext.parallelize([1,2,3])</code> drops to the RDD layer through the wrapped context.","related":["driver-executor-model","lazy-evaluation","partition"]},
  "job-stage-task": {"term":"Job → Stage → Task Decomposition","group":"SPARK-ARCH","aliases":["job","stage","task","taskset","stages","tasks"],"eli5":"Each action is a job, a job breaks into stages at every shuffle, and each stage runs one task per partition.","memory":"Count the shuffles, add one — that's your stage count. Count the partitions — that's your task count per stage.","full":"<p>A <strong>job</strong> is the unit triggered by a single action. The DAGScheduler splits the job's lineage into <strong>stages</strong> at shuffle boundaries: each stage is a maximal chain of narrow transformations that can be pipelined without moving data. Stages run in dependency order, with shuffle-map stages feeding result/reduce stages.</p><p>A stage executes as a <strong>TaskSet</strong> — one <strong>task</strong> per partition of that stage's data. The task is the atomic unit of execution sent to an executor; it runs the pipelined operators over its single partition. Therefore: #stages = #shuffles-on-the-path + 1, and #tasks in a stage = #partitions. Wall-clock per stage is gated by the slowest task.</p>","trap":"Multiple actions on the same DataFrame submit multiple JOBS and recompute shared upstream work unless you <code>cache()</code>. Interviewers probe whether you know one action = one job, and why caching prevents redundant jobs.","worked":"<code>df.filter(...).groupBy(\"k\").count().collect()</code>: one action → one job. The <code>groupBy</code> is a shuffle → 2 stages. If the input has 64 partitions, stage 1 has 64 tasks; stage 2 has <code>spark.sql.shuffle.partitions</code> (e.g. 200, or fewer with AQE) tasks.","related":["shuffle-boundary","narrow-wide-dependency","dag-scheduler","partition"]},
  "shuffle-boundary": {"term":"Shuffle Boundary","group":"SPARK-ARCH","aliases":["shuffle","shuffle boundary","stage boundary","exchange","shuffle write"],"eli5":"A shuffle is the network reshuffle of data by key, and it forces Spark to stop, write intermediate files, and start a new stage.","memory":"Shuffle = the cards getting re-dealt across the table. Everyone must finish dealing (map write) before anyone can pick up their new hand (reduce read).","full":"<p>A shuffle redistributes data across partitions so that all records sharing a key land together — required by wide operations (<code>groupByKey</code>, <code>reduceByKey</code>, <code>join</code>, <code>distinct</code>, <code>repartition</code>). Mechanically, <strong>map-side</strong> tasks partition their output by the target partitioner and write it to local disk (shuffle files); <strong>reduce-side</strong> tasks then fetch their slice over the network. Because reduce tasks can't start until the map side has materialized output, the shuffle is a hard <em>barrier</em> — this is precisely why a new stage begins.</p><p>Shuffles are the dominant cost in most Spark jobs: disk I/O for spill, network transfer, serialization, and sensitivity to skew. In Spark SQL plans a shuffle appears as an <code>Exchange</code> node. Minimizing/optimizing shuffles (broadcast joins, pre-partitioning, AQE) is the core of Spark tuning.</p>","trap":"'Does a shuffle write to disk?' Yes — even though Spark is 'in-memory,' shuffle map output is written to local disk by default; it's not a pure network/RAM operation. Many candidates wrongly assume shuffles stay in memory.","worked":"In <code>df.explain()</code>, an <code>Exchange hashpartitioning(k, 200)</code> node marks the shuffle; the operators below it are stage N, the operators above are stage N+1.","related":["narrow-wide-dependency","job-stage-task","partition"]},
  "narrow-wide-dependency": {"term":"Narrow vs Wide Dependencies","group":"SPARK-ARCH","aliases":["narrow dependency","wide dependency","narrow transformation","wide transformation","dependency"],"eli5":"Narrow means each output partition needs just one input partition (cheap, local); wide means it needs many (expensive, shuffle).","memory":"Narrow = one-to-one pipe (water flows straight through). Wide = many-to-one funnel that needs a shuffle to fill.","full":"<p>A <strong>narrow</strong> dependency: every parent partition contributes to at most one child partition (<code>map</code>, <code>filter</code>, <code>union</code>, <code>mapPartitions</code>). These pipeline within a single stage with no data movement, and partition loss is recovered by recomputing one lineage chain. A <strong>wide</strong> dependency: a child partition draws from multiple parents (<code>groupByKey</code>, <code>join</code>, <code>distinct</code>, <code>repartition</code>), requiring a shuffle and thus a stage boundary.</p><p>This distinction drives everything downstream: stage cutting (DAGScheduler splits on wide deps), performance (wide = network + disk), and fault-tolerance cost (recomputing a partition behind a wide dep may pull in many parents). <code>reduceByKey</code> beats <code>groupByKey</code> because it does map-side combine before the wide shuffle, shrinking the data moved.</p>","trap":"<code>join</code> is NOT always wide: if both sides are already co-partitioned on the join key (same partitioner), Spark can do a narrow/shuffle-free join. And <code>union</code> is narrow (a RangeDependency, no shuffle) — but it just concatenates partition lists, so the child has the SUM of both parents' partitions, which can leave you with many small partitions.","worked":"<code>rdd.map(f).filter(g)</code> = both narrow → one stage, pipelined. <code>rdd.reduceByKey(h)</code> = wide → map-side combine, then shuffle, then new stage.","related":["shuffle-boundary","job-stage-task","lineage","dag-scheduler"]},
  "dag-scheduler": {"term":"DAGScheduler vs TaskScheduler","group":"SPARK-ARCH","aliases":["dagscheduler","dag scheduler","taskscheduler","task scheduler","scheduling"],"eli5":"The DAGScheduler carves the job into stages; the TaskScheduler places individual tasks onto executor cores.","memory":"DAG scheduler = the architect (blueprint of stages). Task scheduler = the foreman (assigns each worker a brick, honoring locality).","full":"<p>Both live in the driver and form a two-level scheduler. The <strong>DAGScheduler</strong> is stage-oriented: it converts the RDD/plan lineage into a DAG of stages by cutting at shuffle boundaries, submits stages in topological order as <code>TaskSet</code>s, computes preferred locations, and recovers from lost shuffle output by resubmitting the missing map stage. The <strong>TaskScheduler</strong> (backed by a <code>SchedulerBackend</code>) is task-oriented: given a <code>TaskSet</code>, it launches tasks on available executor slots honoring <em>locality levels</em> (PROCESS_LOCAL → NODE_LOCAL → RACK_LOCAL → ANY), retries failed tasks, and runs speculative copies of stragglers.</p><p>The split mirrors the abstraction: DAGScheduler reasons about <em>what</em> to compute and in what order; TaskScheduler reasons about <em>where</em> to run each piece given cluster resources.</p>","trap":"Stage-level retries (lost shuffle files → resubmit map stage) are DAGScheduler's job; task-level retries (a single task failed → rerun it) are TaskScheduler's. Mixing these up is a common slip.","worked":"When an executor with shuffle output dies, the TaskScheduler reports lost tasks, but it's the DAGScheduler that recognizes the missing map outputs and resubmits the upstream shuffle-map stage to regenerate them.","related":["job-stage-task","shuffle-boundary","driver-executor-model","lineage"]},
  "lazy-evaluation": {"term":"Lazy Evaluation","group":"SPARK-ARCH","aliases":["lazy evaluation","laziness","transformation","action","lazy"],"eli5":"Spark records what you want but does nothing until you call an action — then it optimizes and runs the whole plan at once.","memory":"Spark is a procrastinator with a plan: it writes the whole to-do list (transformations) and only acts at the deadline (action) — by which point it has optimized the list.","full":"<p>Transformations are <strong>lazy</strong>: <code>map</code>, <code>filter</code>, <code>select</code>, <code>join</code> only append to a logical lineage/plan and return a new dataset; no computation occurs. An <strong>action</strong> (<code>count</code>, <code>collect</code>, <code>save</code>, <code>show</code>, <code>foreach</code>) forces materialization and submits a job. Deferring execution lets Spark see the entire pipeline before running, enabling Catalyst to push down predicates and projections, prune partitions/columns, fuse narrow operators into one stage (whole-stage codegen), and pick join strategies.</p><p>The cost is debuggability: a logical error in a transformation chain may not throw until the triggering action. In classic Spark, DataFrame <em>analysis</em> errors (bad column/type) are still caught eagerly at plan-build time; only <em>runtime</em> errors surface at the action. (Spark Connect defers analysis too.)</p>","trap":"'Why did my <code>filter</code> not fail on a bad UDF until later?' Because transformations don't execute — only the action runs them. And re-running two actions recomputes the chain twice unless you cache.","worked":"<code>df2 = df.filter(\"x > 0\").select(\"y\")</code> runs nothing. <code>df2.write.parquet(path)</code> is the action that triggers the job; Catalyst pushes the filter into the scan and reads only column <code>y</code>.","related":["job-stage-task","sparksession","driver-side-execution"]},
  "driver-side-execution": {"term":"Driver-Side Execution & collect() Hazards","group":"SPARK-ARCH","aliases":["collect","driver memory","pull to driver","driver-side","maxResultSize","task not serializable"],"eli5":"Code outside Spark operators and results you pull back all run on the single driver — easy to blow its memory.","memory":"The driver is a funnel with one bucket. Pour the whole lake (<code>collect</code>) through it and the bucket overflows.","full":"<p>Closures passed to transformations/actions run on <strong>executors</strong>, in parallel across partitions. Everything else — plan construction, the loop processing a <code>collect()</code> result, plain library calls — runs on the <strong>driver</strong>, a single JVM. Actions that return data to the driver (<code>collect</code>, <code>take</code>, <code>toPandas</code>) funnel a distributed dataset through that one heap; <code>collect()</code> on a large result OOMs the driver, and <code>spark.driver.maxResultSize</code> (default <strong>1g</strong>) exists to abort such jobs before they do.</p><p>The driver/executor boundary also explains <code>Task not serializable</code>: a closure capturing a driver-side object (e.g. a DB connection, a non-serializable client) is serialized and shipped to executors; if the capture can't serialize, the task fails. Fix by constructing the resource inside <code>mapPartitions</code>/<code>foreachPartition</code> on the executor, or broadcasting an immutable value.</p>","trap":"'How do I get all results to a local Python list?' If the answer is just <code>collect()</code>, push back: bound it, write to a sink, or aggregate first. And know that the closure-capture rule is what causes <code>Task not serializable</code>.","worked":"<code>conn = make_db(); df.foreach(lambda r: conn.write(r))</code> fails — <code>conn</code> ships to executors and isn't serializable. Use <code>df.foreachPartition(lambda part: ...)</code> creating the connection inside, per executor.","related":["driver-executor-model","lazy-evaluation","partition","job-stage-task"]}
});
