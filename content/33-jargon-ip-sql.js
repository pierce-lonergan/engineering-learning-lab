/* Frontend jargon & acronyms — clickable reference (IP-SQL, auto-assembled) */
Object.assign(GLOSSARY_DATA, {
  "cte": {"term":"CTE","group":"IP-SQL","aliases":["Common Table Expression","Common Table Expressions","WITH clause"],"eli5":"A CTE is a named, temporary result set defined with a WITH clause that you can reference in the main query to make complex SQL readable and reusable.","memory":"Common Table Expression — the WITH clause; name a subquery once, reference it like a temp table.","full":"<p><strong>What it is:</strong> A CTE (Common Table Expression) is a named temporary result set introduced by the <code>WITH</code> keyword that exists only for the duration of a single query. You define one or more named subqueries up front, then reference them by name in the main <code>SELECT</code> (or in later CTEs) as if they were tables. CTEs make multi-step SQL readable by giving each logical step a name, and a special <em>recursive</em> form lets a CTE reference itself to walk hierarchies and graphs.</p><p><strong>History / origin:</strong> CTEs were standardized in SQL:1999, which introduced both the <code>WITH</code> clause and recursive queries. Database vendors adopted them over the following years, and they are now supported across the major engines (PostgreSQL, SQL Server, Oracle, MySQL 8+, and cloud warehouses), making them a portable, standard feature rather than a vendor extension.</p><p><strong>Purpose &amp; why still used:</strong> CTEs solve two problems. First, <em>readability and structure</em>: they flatten deeply nested subqueries into a top-to-bottom sequence of named steps, and they sidestep the rule that you cannot reference a <code>SELECT</code> alias in the same query's <code>WHERE</code> — you compute a value in a CTE, then filter on it in the outer query. Second, <em>recursion</em>: the recursive CTE (an anchor query <code>UNION ALL</code> a query that references the CTE) is the standard way to traverse hierarchies like org charts, parent/child trade chains, or bill-of-materials. They remain heavily used because they are standard, expressive, and far clearer than the alternatives.</p>","worked":"<p>Compute a classification in a CTE, then filter on it in the outer query (you can't reference a SELECT alias in the same WHERE):</p><p><code>WITH matched AS (\n  SELECT i.venue_exec_id, i.qty,\n         CASE WHEN b.venue_exec_id IS NULL THEN 'unmatched' ELSE 'matched' END AS status\n  FROM internal i LEFT JOIN broker b USING (venue_exec_id)\n)\nSELECT * FROM matched WHERE status = 'unmatched';</code></p><p>Recursive form — walk a parent/child trade chain via anchor <code>UNION ALL</code> recursive member:</p><p><code>WITH RECURSIVE chain AS (\n  SELECT trade_id, parent_id FROM trades WHERE parent_id IS NULL   -- anchor\n  UNION ALL\n  SELECT t.trade_id, t.parent_id FROM trades t JOIN chain c ON t.parent_id = c.trade_id  -- recursive\n)\nSELECT * FROM chain;</code></p>","teach":"<p>At its core, a CTE is just a way to <em>name</em> a query result so you can use it like a table in the rest of your SQL. You write <code>WITH some_name AS ( ... )</code>, and from then on <code>some_name</code> behaves like a temporary table that exists only for this one statement.</p><div class=\"analogy\">Think of cooking from a recipe. Instead of one giant run-on instruction (\"chop the onions that you sautéed in the oil you heated in the pan you...\"), a good recipe defines steps: \"1. Make the sauce. 2. Cook the pasta. 3. Combine.\" Each named step builds on the last. A CTE is that named step — you prepare \"the matched trades,\" give it a name, and then the final query just says \"now filter the matched trades.\"</div><p>One very practical reason CTEs exist: SQL won't let you use a column alias you created in <code>SELECT</code> inside that same query's <code>WHERE</code> (the <code>WHERE</code> is evaluated before the <code>SELECT</code> aliases exist — \"alias scoping\"). So if you compute, say, a <code>status</code> column with a <code>CASE</code> expression, you can't immediately write <code>WHERE status = 'unmatched'</code>. The fix is to compute it in a CTE, then filter in an outer query that <em>can</em> see the name. Clean and readable.</p><p>The second superpower is the <strong>recursive CTE</strong>, which references itself. It has two parts joined by <code>UNION ALL</code>: an <em>anchor</em> query that produces the starting rows (e.g., top-level trades with no parent), and a <em>recursive</em> query that joins back to the CTE to find the next layer (each trade's children). The engine repeats the recursive part, feeding each round's output back in, until no new rows appear. That's how you walk an org chart, a comment thread, or a parent/child trade chain without a loop in application code.</p><div class=\"key\">CTE = a named, temporary result set via <code>WITH</code>. Use it to make multi-step SQL readable and to filter on computed values the <code>WHERE</code> can't see yet; the recursive form (anchor <code>UNION ALL</code> recursive member) traverses hierarchies.</div>"}
});
