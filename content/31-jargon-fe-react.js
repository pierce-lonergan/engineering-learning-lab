/* Frontend jargon & acronyms — clickable reference (FE-REACT, auto-assembled) */
Object.assign(GLOSSARY_DATA, {
  "jsx": {"term":"JSX","group":"FE-REACT","aliases":["JavaScript XML","JavaScript Syntax Extension"],"eli5":"An HTML-like syntax you write inside JavaScript that a compiler turns into plain function calls describing what UI to render.","memory":"JSX = JavaScript + XML-like tags — sugar that compiles to element-creating function calls.","full":"<p><strong>JSX</strong> is a syntax extension for JavaScript that lets you write HTML-like markup directly in your code. It is not understood by browsers natively; a compiler (<strong>Babel</strong>, <strong>SWC</strong>, <strong>esbuild</strong>, or the TypeScript compiler) <em>lowers</em> it into ordinary JavaScript function calls. A tag like <code>&lt;div className=\"x\"&gt;hi&lt;/div&gt;</code> compiles to a call (historically <code>React.createElement</code>, and under the modern automatic runtime <code>jsx(...)</code>) that returns an immutable plain object of the shape <code>{type, props, key}</code> — a lightweight <em>description</em> of UI, not actual DOM.</p><p>JSX was introduced by <strong>Facebook</strong> alongside <strong>React</strong> in 2013. While most associated with React, it is a general syntax that other libraries (Preact, Solid, and others) also consume via their own compile targets ('pragmas'). The 'new' automatic JSX transform, which imports the runtime for you so you no longer need <code>React</code> in scope, shipped around React 17 (2020).</p><p>Its purpose is ergonomics: expressing component trees in a familiar, declarative, markup-like form while keeping the full power of JavaScript (expressions in <code>{}</code>, mapping over arrays, conditionals). It remains the dominant authoring syntax for React in 2026 because it produces those immutable element objects the reconciler diffs each render. A subtlety worth knowing: passing children as <em>fresh JSX every render</em> creates new element objects each time, which can defeat referential-equality optimizations like <code>React.memo</code> if not handled carefully.</p>","worked":"<p>What you write (JSX):</p><ul><li><code>const el = &lt;h1 className=\"title\"&gt;Hello {name}&lt;/h1&gt;;</code></li></ul><p>What the compiler lowers it to (automatic runtime):</p><ul><li><code>const el = jsx('h1', { className: 'title', children: ['Hello ', name] });</code></li></ul><p>The result is an immutable description object, roughly:</p><p><code>{ type: 'h1', props: { className: 'title', children: [...] }, key: null }</code> — a plan for the UI, not a real DOM node yet.</p>","teach":"<p>At its core, JSX is just <em>syntactic sugar</em>: it looks like HTML, but a compiler rewrites it into plain JavaScript function calls before the browser ever sees it.</p><div class=\"analogy\">Think of JSX as a blueprint, not a building. When you write <code>&lt;Button /&gt;</code>, you're not constructing a button on screen — you're sketching a note that says 'a Button goes here, with these props.' React later reads your stack of blueprints, compares them to the last set, and only then sends construction orders to the real DOM.</div><p>Concretely, the build tool (Babel/SWC/esbuild) transforms each tag into a function call that returns an object like <code>{ type, props, key }</code>. That object is <strong>immutable</strong> and is just data — a description. It contains no DOM nodes. This is the key mental shift: <em>JSX produces descriptions, not elements on the page.</em></p><p>Because it's still JavaScript underneath, you get superpowers HTML never had: drop any expression in curly braces (<code>{user.name}</code>), map an array into a list (<code>items.map(i =&gt; &lt;li&gt;{i}&lt;/li&gt;)</code>), and branch with ternaries. The markup and the logic live together.</p><p>One gotcha falls out of this directly. Each render re-runs your function, so each render creates <em>brand-new</em> element objects — fresh JSX is never <code>===</code> to last render's JSX. That's usually fine, but it means optimizations based on referential equality (like <code>React.memo</code> comparing props) can be defeated if you pass freshly-created JSX as a child or prop every time. Knowing JSX is 'just objects from function calls' is what makes that behavior obvious instead of mysterious.</p><div class=\"key\">JSX is HTML-flavored syntax that compiles to function calls returning immutable <code>{type, props, key}</code> description objects. It describes UI; React turns those descriptions into real DOM. Fresh JSX each render = new objects, which matters for memoization.</div>","related":["dom","esm","ast"]},
  "cra": {"term":"CRA","group":"FE-REACT","aliases":["Create React App","create-react-app"],"eli5":"A now-deprecated official tool that scaffolded a ready-to-run React project with zero build configuration.","memory":"CRA = Create React App: one command and your React app exists, no webpack config needed.","full":"<p><strong>CRA</strong> (Create React App) is a command-line scaffolding tool that generates a complete, ready-to-build React single-page-app project with no build configuration to maintain. It bundles webpack, Babel, ESLint, and a dev server behind a single dependency (<code>react-scripts</code>), so beginners could run <code>npx create-react-app my-app</code> and immediately start coding.</p><p>CRA was created and maintained by the <strong>React team / Meta (Facebook)</strong>, introduced around 2016. For years it was the default recommended way to start a React project. Its configuration was intentionally hidden; you could \"eject\" to expose the underlying webpack config, but that was a one-way door. Over time it fell behind faster tooling — and in 2025 the React team formally <strong>deprecated CRA</strong>, steering newcomers toward frameworks like Next.js, Remix, or build tools like Vite.</p><p>Its purpose was to remove the painful, error-prone task of hand-configuring a build toolchain so people could learn React, not webpack. Today it is mostly seen in <em>mature, existing</em> codebases — the deck notes you might still pick Jest over Vitest precisely because you're \"on a mature CRA/Next/webpack stack with a huge existing Jest suite.\" New projects no longer use it.</p>","worked":"<p>The classic (now-legacy) scaffolding command:</p><pre><code># Historically, the one-liner that started every React tutorial:\nnpx create-react-app my-app\ncd my-app\nnpm start   # dev server with hot reload, zero config\n\n# CRA shipped Jest pre-wired, which is why old CRA codebases\n# typically have large existing Jest test suites:\nnpm test</code></pre><p>In 2026 a new project would instead use <code>npm create vite@latest</code> or a framework like Next.js.</p>","teach":"<p>At its core, CRA is just a <strong>project generator that hides the messy build setup</strong>. Modern React doesn't run in the browser as-is: JSX and modern JavaScript must be transformed (Babel), bundled (webpack), and served with hot-reload. Wiring all that by hand is tedious and easy to break. CRA pre-assembled it so you never had to look.</p><div class=\"analogy\">Think of a meal-kit delivery box. Instead of buying flour, eggs, pans, and figuring out the recipe yourself, you get a box with everything measured and a card that says \"cook for 20 minutes.\" CRA was that box for React apps: one command, and a working kitchen appeared. The catch — like a meal kit — is you can't easily customize the ingredients without breaking the convenience.</div><p>The \"zero config\" magic came from a single dependency, <code>react-scripts</code>, which owned the webpack/Babel/Jest setup behind the scenes. As long as you stayed inside CRA's assumptions, everything worked. If you needed something CRA didn't support, you had to <code>eject</code> — which dumped all the hidden config into your project permanently, and you owned it forever after.</p><p>So why is CRA fading? Newer tools like <strong>Vite</strong> start dev servers almost instantly using native ES modules and esbuild, while CRA's webpack-based startup grew sluggish on big apps. Frameworks like Next.js also bundle routing, server rendering, and data fetching that CRA never offered. The React team deprecated CRA in 2025, recommending frameworks or Vite instead.</p><div class=\"key\">The takeaway: <strong>CRA was the on-ramp that taught a generation React without forcing them to learn webpack</strong> — but it's now legacy. You'll meet it in existing codebases (often with large Jest suites), not in new ones.</div>","related":["swr","e2e"]}
});
