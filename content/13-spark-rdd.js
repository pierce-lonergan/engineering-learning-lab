/* RDDs & the Core Model (SPARK-RDD) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"SPARK-RDD","front":"What is an RDD, precisely?","back":"A <strong>Resilient Distributed Dataset</strong>: an immutable, lazily-evaluated, partitioned collection of records spread across the cluster, with enough <strong>lineage</strong> (the DAG of operations that built it) to recompute any lost partition. It is a typed, distributed handle — not the data itself — carrying five core properties: a list of partitions, a compute function per partition, dependencies on parent RDDs, optionally a partitioner, and optionally preferred locations."},
  {"sec":"SPARK-RDD","front":"What are the five defining properties of an RDD?","back":"<ul><li><code>getPartitions()</code> — the list of partitions (units of parallelism).</li><li><code>compute(partition, ctx)</code> — function to produce that partition's records as an iterator.</li><li><code>getDependencies()</code> — parent RDDs and dep type (narrow/wide).</li><li><code>partitioner</code> — optional (e.g. <code>HashPartitioner</code>) for key-value RDDs.</li><li><code>getPreferredLocations(partition)</code> — optional locality hints (e.g. HDFS block hosts).</li></ul>"},
  {"sec":"SPARK-RDD","front":"Transformations vs actions — what is the operational difference?","back":"<strong>Transformations</strong> (<code>map</code>, <code>filter</code>, <code>join</code>) are <em>lazy</em>: they only extend the lineage DAG and return a new RDD, doing no work. <strong>Actions</strong> (<code>collect</code>, <code>count</code>, <code>saveAsTextFile</code>, <code>reduce</code>) are <em>eager</em>: they trigger the scheduler to materialize the DAG into stages and tasks and actually compute. Nothing runs until an action is called."},
  {"sec":"SPARK-RDD","front":"Why is lazy evaluation more than just a deferral trick?","back":"Laziness lets Spark see the whole lineage before executing, enabling optimizations: <strong>pipelining</strong> narrow transformations into a single pass per partition (no intermediate materialization) and choosing where shuffle boundaries fall. A chain like <code>map().filter().map()</code> runs as one fused loop over each partition, not three separate passes. (Branch/column pruning is a Catalyst optimization on DataFrames; the raw RDD API does no such logical-plan rewriting.)"},
  {"sec":"SPARK-RDD","front":"Narrow vs wide dependency — define by partition fan-in.","back":"A <strong>narrow dependency</strong> means each parent partition is consumed by at most one child partition (<code>map</code>, <code>filter</code>, <code>union</code>, <code>coalesce</code> without shuffle, co-partitioned <code>join</code>) — computable locally, pipelineable. A <strong>wide dependency</strong> (a.k.a. shuffle dependency) means a child partition depends on <em>many</em> parent partitions (<code>groupByKey</code>, <code>reduceByKey</code>, <code>join</code> on differently-partitioned data, <code>repartition</code>, <code>distinct</code>) — requires data movement across the network."},
  {"sec":"SPARK-RDD","front":"Why do wide dependencies force a stage boundary?","back":"Spark splits the DAG into <strong>stages</strong> at every wide dependency. Narrow transformations pipeline within a stage; a shuffle cannot be pipelined because the child needs records from all parents, so the parent stage must fully complete and write its map output to local disk before the child (reduce) stage can fetch it. Thus <em>number of stages = number of shuffles + 1</em> along any path."},
  {"sec":"SPARK-RDD","front":"Mechanically, what happens during a shuffle?","back":"Map-side tasks partition each record by the target partitioner (e.g. <code>hash(key) % numPartitions</code>) and, under the default sort-based shuffle, write a single output file <em>ordered by partition id</em> (not by key, unless the op needs key order) plus an index, spilling to disk if the buffer overflows. Reduce-side tasks then <em>fetch</em> their assigned blocks from every map task's output over the network. It is the most expensive operation: disk I/O, serialization, and all-to-all network transfer."},
  {"sec":"SPARK-RDD","front":"How does an RDD recover a lost partition after an executor dies?","back":"It <strong>recomputes</strong> the lost partition from <strong>lineage</strong> — re-running the chain of transformations from the last available source (input data or a persisted/checkpointed ancestor) for <em>only that partition</em>. There is no data replication of RDD contents; the recipe (DAG) is the redundancy. Narrow lineage recovers cheaply; if the shuffle map outputs feeding a wide dependency are also lost, the parent stage may need re-execution."},
  {"sec":"SPARK-RDD","front":"Why recompute from lineage instead of replicating data like HDFS?","back":"Replicating large intermediate datasets across the network is expensive in bandwidth and memory. Lineage stores only the <em>transformations</em> (kilobytes of metadata), so the redundancy cost is near-zero, and recovery is parallel and partition-local. The tradeoff: recovery latency can be high for long or wide lineages — which is exactly what <code>checkpoint</code> exists to bound."},
  {"sec":"SPARK-RDD","front":"reduceByKey vs groupByKey — why prefer reduceByKey?","back":"<code>reduceByKey</code> performs a <strong>map-side combine</strong>: it pre-aggregates values per key <em>within each partition</em> before the shuffle, so far fewer bytes cross the network. <code>groupByKey</code> ships <em>every</em> value to the reducer with no pre-aggregation, risking huge network traffic and OOM when one key has many values. For associative+commutative reductions, <code>reduceByKey</code> (or <code>aggregateByKey</code>/<code>combineByKey</code>) is almost always correct. Both are still wide dependencies — the combine shrinks shuffle volume, it does not remove the shuffle."},
  {"sec":"SPARK-RDD","front":"persist/cache — what do they do and how do they differ?","back":"Both mark an RDD to be retained after its first computation so later actions reuse it instead of recomputing the lineage. For RDDs, <code>cache()</code> is shorthand for <code>persist(MEMORY_ONLY)</code>. <code>persist(level)</code> takes an explicit <code>StorageLevel</code> (<code>MEMORY_ONLY</code>, <code>MEMORY_AND_DISK</code>, <code>DISK_ONLY</code>, <code>*_SER</code>, replicated variants). Both are <em>lazy</em> — the RDD isn't actually stored until the next action runs. (Note: DataFrame <code>cache()</code> defaults to <code>MEMORY_AND_DISK</code>, not <code>MEMORY_ONLY</code>.)"},
  {"sec":"SPARK-RDD","front":"When does caching actually pay off (and when is it wasted)?","back":"Cache when an RDD is reused by <strong>multiple actions</strong> or inside an <strong>iterative algorithm</strong> (e.g. ML loops) — you pay compute once and reuse the materialized partitions. It is wasted (pure overhead) on a linear pipeline used by a single action, where nothing is reread. Under memory pressure, <code>MEMORY_ONLY</code> silently drops (LRU-evicts) partitions and recomputes them; <code>MEMORY_AND_DISK</code> spills instead, trading disk reads for recompute."},
  {"sec":"SPARK-RDD","front":"cache vs checkpoint — what's the difference and why does it matter for recovery?","back":"<code>cache</code>/<code>persist</code> stores partitions but <em>keeps the lineage</em>, so a lost cached partition is still recomputed from its DAG. <code>checkpoint</code> writes the RDD to reliable storage (HDFS/S3) and <strong>truncates the lineage</strong> — the RDD's new parent is the checkpoint file. Use checkpointing to cap recovery cost on very long or iterative lineages (e.g. GraphX/streaming) where recomputation would be catastrophic. Caveat: checkpointing recomputes the RDD, so <code>cache()</code> it first or it runs the lineage twice."},
  {"sec":"SPARK-RDD","front":"How does the DAGScheduler turn an action into execution?","back":"On an action, the <strong>DAGScheduler</strong> walks the RDD lineage backward, cuts it into stages at each shuffle (wide) dependency, and submits stages in dependency order. Each stage becomes a <strong>TaskSet</strong> — one task per output partition — handed to the <code>TaskScheduler</code> for placement (respecting preferred locations). A <code>ShuffleMapStage</code> produces shuffle output; the final <code>ResultStage</code> produces the action's result."},
  {"sec":"SPARK-RDD","front":"What is partition skew and why does it bite wide operations specifically?","back":"<strong>Skew</strong> is when one (or few) partitions hold disproportionately many records — typically a hot key after a hash shuffle. Because a stage finishes only when its <em>slowest</em> task does, one fat reducer becomes a straggler that stalls the whole stage and may OOM. It surfaces on wide ops (<code>groupByKey</code>, <code>join</code>); mitigations include key salting, <code>reduceByKey</code> map-side combine, raising partition count, or AQE skew-join handling in the DataFrame API."},
  {"sec":"SPARK-RDD","front":"Why are RDDs immutable, and what does that buy the execution model?","back":"Each transformation returns a <em>new</em> RDD rather than mutating in place. Immutability makes lineage a stable, deterministic recipe — any partition can be recomputed identically at any time, which is the foundation of fault tolerance. It also makes RDDs safe to share/cache across tasks and re-execute on speculation or failure without side effects."}
);

Object.assign(GLOSSARY_DATA, {
  "rdd": {"term":"Resilient Distributed Dataset (RDD)","group":"SPARK-RDD","aliases":["rdd","rdds","resilient distributed dataset"],"eli5":"A distributed, read-only collection plus the recipe for rebuilding any piece of it that gets lost.","memory":"R-D-D: Recompute-from-lineage, Distributed-by-partition, Deterministic-because-immutable.","full":"<p>An RDD is Spark's core abstraction: an immutable, partitioned collection of records distributed across executors, evaluated lazily. It is best understood as five things — a set of <code>partitions</code>, a <code>compute</code> function per partition, a list of parent <code>dependencies</code> (narrow or wide), an optional <code>partitioner</code>, and optional <code>preferredLocations</code> for data locality.</p><p>Crucially an RDD holds metadata (the lineage recipe), not necessarily materialized data — the data is produced on demand when an action fires and, unless persisted, discarded after. 'Resilient' comes from lineage-based recomputation, 'distributed' from partitioning, 'dataset' from the typed record collection.</p>","trap":"Saying an RDD 'stores' its data. By default it stores nothing — it stores how to compute partitions. Data only lives in memory/disk if you persist/cache it.","related":["lineage","narrow-vs-wide","transformation-vs-action","persist-cache"]},
  "lineage": {"term":"Lineage (RDD lineage / DAG)","group":"SPARK-RDD","aliases":["lineage","rdd lineage","lineage graph","dag"],"eli5":"The full recipe of transformations that built an RDD, used to rebuild lost pieces.","memory":"Lineage = the cooking recipe; you don't store leftovers (data), you store how to re-cook the dish.","full":"<p>Lineage is the directed acyclic graph of transformations connecting an RDD back to its source data. Because RDDs are immutable and transformations are deterministic, the graph is a reproducible recipe: if a partition is lost, Spark replays only the operations needed to regenerate <em>that</em> partition.</p><p>This is the mechanism behind fault tolerance without data replication — the redundancy is the (tiny) metadata of the DAG, not copies of the dataset. The cost is recovery latency: long or wide lineages are expensive to replay, which is why <code>checkpoint</code> exists to truncate lineage to reliable storage.</p>","trap":"Confusing lineage (logical recompute recipe) with caching (physical stored partitions). Caching does NOT truncate lineage; only checkpointing does — a cached-but-lost partition is still recomputed from its DAG.","worked":"<code>rdd.map(f).filter(g)</code> loses one partition. Spark re-reads just that source partition's block and re-applies <code>f</code> then <code>g</code> to it — no other partition and no full re-run.","related":["rdd","fault-tolerance-recompute","persist-cache","narrow-vs-wide"]},
  "transformation-vs-action": {"term":"Transformations vs Actions","group":"SPARK-RDD","aliases":["transformation","transformations","action","actions","lazy evaluation","lazy"],"eli5":"Transformations just write down the plan; actions press 'run'.","memory":"Transformations are IOUs; an action is the bank cashing them all at once.","full":"<p>Transformations (<code>map</code>, <code>filter</code>, <code>flatMap</code>, <code>join</code>, <code>reduceByKey</code>) are lazy — each returns a new RDD and only extends the lineage DAG, performing no computation. Actions (<code>collect</code>, <code>count</code>, <code>take</code>, <code>reduce</code>, <code>saveAsTextFile</code>) are eager — they force the DAGScheduler to materialize the lineage into stages/tasks and execute.</p><p>Laziness is what enables whole-DAG optimization: pipelining consecutive narrow transformations into one pass per partition, and deferring decisions about shuffle boundaries until the full plan is known. A useful tell: if a call returns an RDD it's a transformation; if it returns a value or writes output, it's an action.</p>","trap":"Assuming a transformation 'ran' because the line executed without error. It didn't — until an action fires, you've only built a plan. This is why an exception in <code>map</code>'s lambda surfaces at <code>collect()</code>, not at the <code>map()</code> line.","related":["rdd","lineage","dag-scheduler-stages","narrow-vs-wide"]},
  "narrow-vs-wide": {"term":"Narrow vs Wide Dependencies","group":"SPARK-RDD","aliases":["narrow dependency","narrow dependencies","wide dependency","wide dependencies","shuffle dependency"],"eli5":"Narrow = each parent partition feeds one child (stay local); wide = a child needs many parents (must shuffle).","memory":"Narrow = one-to-one lane (no merging); Wide = many lanes merge = traffic jam = shuffle.","full":"<p>A narrow dependency means each parent partition contributes to at most one child partition — <code>map</code>, <code>filter</code>, <code>union</code>, <code>coalesce</code> (no shuffle), and co-partitioned joins. These pipeline within a stage and recover cheaply. A wide (shuffle) dependency means a child partition draws from many parent partitions — <code>groupByKey</code>, <code>reduceByKey</code>, <code>join</code> across differently-partitioned RDDs, <code>repartition</code>, <code>distinct</code>.</p><p>The dependency type drives everything downstream: wide deps force a shuffle (disk + network), create a stage boundary, and make fault recovery costlier (lost shuffle output may need re-fetch or parent re-run). Designing a job is largely about minimizing and shrinking wide dependencies.</p>","trap":"Assuming <code>join</code> is always wide. If both RDDs share the same partitioner (co-partitioned), the join is narrow — no shuffle. Conversely, even <code>reduceByKey</code> is wide despite its map-side combine; combine reduces bytes, not the shuffle itself.","worked":"<code>a.join(b)</code> where both were <code>partitionBy(HashPartitioner(100))</code> on the join key: matching keys are already co-located, so it's a narrow dependency with no shuffle.","related":["shuffle","dag-scheduler-stages","reducebykey-vs-groupbykey","lineage"]},
  "shuffle": {"term":"Shuffle","group":"SPARK-RDD","aliases":["shuffle","shuffles","shuffling","shuffle files"],"eli5":"The all-to-all redistribution of data by key across the cluster — the slow, expensive part.","memory":"Shuffle = the airport hub: every passenger (record) must be re-sorted onto the right connecting flight (partition) — disk, network, and waiting.","full":"<p>A shuffle physically repartitions data so records with the same key land in the same downstream partition. Under the default sort-based shuffle, each map task writes a single output file to <strong>local disk</strong> ordered by target partition id (plus an index); reduce-side tasks fetch their blocks from every map task across the network. It is the dominant cost in most Spark jobs: serialization, disk I/O, and all-to-all transfer.</p><p>Every wide dependency triggers exactly one shuffle and one stage boundary. The shuffle write must fully complete before the read stage starts, which is why a slow/straggling map task or a skewed reducer dominates wall-clock time. Tuning levers: partition count (<code>spark.sql.shuffle.partitions</code>, default 200 in Spark SQL), map-side combine, and avoiding unnecessary repartitions.</p>","trap":"Thinking map output is sorted by KEY. Under sort-based shuffle it's ordered by target partition id, not by key (key ordering only happens for ops like sortByKey). Also: map output goes to local disk first (so it survives reducer retries); the fetch is what crosses the network. And the SQL default of 200 shuffle partitions ignores data size unless AQE coalesces them.","related":["narrow-vs-wide","dag-scheduler-stages","partition-skew","reducebykey-vs-groupbykey"]},
  "fault-tolerance-recompute": {"term":"Fault Tolerance by Recomputation","group":"SPARK-RDD","aliases":["fault tolerance","recomputation","recompute lost partition","lineage recovery"],"eli5":"When a partition is lost, Spark rebuilds just that piece from the lineage instead of keeping spare copies.","memory":"No spare tire (replication); instead, the blueprint to re-manufacture the exact tire that blew (lineage).","full":"<p>Spark achieves fault tolerance not by replicating RDD data but by replaying lineage. When an executor dies and takes partitions with it, the DAGScheduler recomputes only the missing partitions from the nearest available ancestor (source data, a persisted partition, or a checkpoint). Because RDDs are immutable and transformations deterministic, the recomputed partition is bit-identical.</p><p>For narrow lineage this is cheap and partition-local. For wide lineage it can be costly: if the lost partition depended on a shuffle whose map outputs are also gone, parent stages may need re-execution. This recovery-latency risk is the reason long iterative jobs checkpoint, and why shuffle map outputs are persisted to disk rather than held only in memory.</p>","trap":"Claiming recomputation is always cheap. A single lost partition behind a wide dependency can trigger re-fetching or re-running an entire parent stage — which is precisely the scenario checkpointing is designed to bound.","related":["lineage","rdd","persist-cache","narrow-vs-wide"]},
  "persist-cache": {"term":"persist / cache","group":"SPARK-RDD","aliases":["persist","cache","caching","storage level","storagelevel","checkpoint","checkpointing"],"eli5":"Tell Spark to keep a computed RDD around so reuse doesn't re-run the whole recipe.","memory":"Cache = sticky note kept on the fridge (fast, but tied to the recipe); checkpoint = photocopy filed in a vault (durable, recipe forgotten).","full":"<p>For RDDs, <code>cache()</code> = <code>persist(MEMORY_ONLY)</code>; <code>persist(level)</code> lets you choose a <code>StorageLevel</code> — <code>MEMORY_ONLY</code>, <code>MEMORY_AND_DISK</code>, <code>DISK_ONLY</code>, serialized (<code>*_SER</code>) variants, and replicated variants (<code>_2</code>). Both are lazy: partitions are stored only when the first action materializes them, and reused by subsequent actions. Pays off for multi-action reuse and iterative algorithms; pure overhead on single-pass linear pipelines.</p><p>Persisting keeps lineage intact (a dropped cached partition is recomputed from the DAG). <code>checkpoint()</code> is the heavier cousin: it writes the RDD to reliable storage and <em>truncates</em> lineage, capping recovery cost for very long or wide DAGs. Rule of thumb: cache for speed/reuse, checkpoint to bound fault-recovery cost.</p>","trap":"Two gotchas: (1) RDD <code>cache()</code> defaults to <code>MEMORY_ONLY</code>, but DataFrame/Dataset <code>cache()</code> defaults to <code>MEMORY_AND_DISK</code> — know which API you're in. (2) <code>cache()</code> is lazy (nothing stored until the next action), and under <code>MEMORY_ONLY</code> pressure partitions are silently LRU-evicted and recomputed (not spilled) — use <code>MEMORY_AND_DISK</code> if recompute is expensive. (3) <code>checkpoint()</code> recomputes the RDD unless you cache it first.","worked":"An ML loop reads the same training RDD 50 times. Without <code>cache()</code> the full source-read + transforms re-run 50x; with <code>MEMORY_AND_DISK</code> it computes once and every later iteration reads the stored partitions.","related":["lineage","fault-tolerance-recompute","rdd","transformation-vs-action"]},
  "reducebykey-vs-groupbykey": {"term":"reduceByKey vs groupByKey","group":"SPARK-RDD","aliases":["reducebykey","reduceByKey","groupbykey","groupByKey","map-side combine","combiner"],"eli5":"reduceByKey pre-adds within each partition before shipping; groupByKey ships everything raw.","memory":"reduceByKey = pack a suitcase before the flight (combine, then ship); groupByKey = ship every loose sock, sort at destination.","full":"<p>Both produce per-key results, but <code>reduceByKey</code> applies a <strong>map-side combine</strong>: it folds values for each key locally within a partition <em>before</em> the shuffle, so each map partition emits at most one partial result per distinct key it contains. <code>groupByKey</code> performs no pre-aggregation — every value is shuffled to the reducer, then grouped.</p><p>For associative and commutative reductions (sum, count, max), <code>reduceByKey</code> (or the more general <code>aggregateByKey</code>/<code>combineByKey</code>) drastically cuts shuffle bytes and avoids the OOM risk of a single key with millions of values materializing in one reducer's memory. Use <code>groupByKey</code> only when you genuinely need all raw values together and can't express the work as a combine.</p>","trap":"Believing the map-side combine eliminates the shuffle — it doesn't; both are wide dependencies that shuffle. The combine only shrinks the volume of data shuffled. Also, <code>reduceByKey</code> requires the result type to equal the value type; use <code>aggregateByKey</code>/<code>combineByKey</code> when they differ.","worked":"Counting 1B records over 10M distinct keys: <code>groupByKey</code> shuffles all 1B values; <code>reduceByKey(_+_)</code> shuffles at most one partial count per key per partition (so ≤ 10M × numPartitions, and typically far fewer) — orders of magnitude less network and no fat-key OOM.","related":["shuffle","narrow-vs-wide","partition-skew"]},
  "dag-scheduler-stages": {"term":"DAGScheduler & Stages","group":"SPARK-RDD","aliases":["dagscheduler","dag scheduler","stage","stages","stage boundary","taskset","shufflemapstage","resultstage"],"eli5":"The component that slices the lineage at shuffles into stages, then launches one task per partition.","memory":"Scissors cut the lineage at every shuffle; each cut-out piece is a stage, each partition a task.","full":"<p>When an action fires, the DAGScheduler walks the RDD lineage backward and cuts it into <strong>stages</strong> at each wide (shuffle) dependency. Narrow transformations are pipelined inside a stage; a shuffle ends a <code>ShuffleMapStage</code> (which writes shuffle output) and begins the next. The final stage is a <code>ResultStage</code> that yields the action's result. Each stage is submitted as a <code>TaskSet</code> with one task per output partition, placed by the TaskScheduler honoring preferred locations.</p><p>So along any path, <em>stages = shuffles + 1</em>, and tasks within a stage run fully in parallel (subject to slots). A stage completes only when its slowest task does, which is why skew and stragglers, not average task time, govern stage latency.</p>","trap":"Counting stages from the code's transformation count. Stages map to shuffle boundaries, not to operator count — ten chained narrow ops are one stage; a single <code>groupByKey</code> adds a boundary. Use the Spark UI DAG view to confirm.","worked":"<code>textFile.map().filter().reduceByKey().map().collect()</code> → Stage 0 (read+map+filter, ShuffleMapStage) | shuffle | Stage 1 (reduce+map, ResultStage) = 2 stages, 1 shuffle.","related":["narrow-vs-wide","shuffle","transformation-vs-action","partition-skew"]},
  "partition-skew": {"term":"Partition Skew","group":"SPARK-RDD","aliases":["skew","partition skew","data skew","straggler","hot key"],"eli5":"When one partition gets way more data than the rest, its single task stalls the whole stage.","memory":"One checkout lane with 500 people while nine lanes are empty — the store closes when that lane clears.","full":"<p>Skew occurs when records are unevenly distributed across partitions — usually because a hash shuffle concentrates a hot key (or null) into one reducer. Since a stage finishes only when its slowest task completes, a single oversized partition becomes a straggler that dominates wall-clock time and may OOM that executor while others sit idle.</p><p>It is intrinsically a wide-operation problem (<code>groupByKey</code>, <code>join</code>, aggregations). Mitigations: convert to map-side combine (<code>reduceByKey</code>), increase partition count, <strong>salt</strong> hot keys (append a random suffix to spread them, then re-aggregate), broadcast-join a small side to avoid the shuffle entirely, or in the DataFrame/SQL path enable AQE skew-join splitting.</p>","trap":"Fixing skew by only raising partition count. More partitions don't help if a single key still hashes to one partition — you must salt the key or broadcast-join. Watch the Spark UI task-duration distribution, not the average.","worked":"A <code>join</code> on <code>country</code> where 70% of rows are <code>US</code>: salt as <code>(US, rand(0..9))</code> on the big side, replicate the small side across the 10 salts, join, then strip the salt — spreading the hot key over 10 reducers.","related":["shuffle","narrow-vs-wide","reducebykey-vs-groupbykey","dag-scheduler-stages"]}
});
