/* Messaging (SQS, SNS, EventBridge) (CLOUD-MSG) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"CLOUD-MSG","front":"SQS Standard vs FIFO: the three-axis tradeoff","back":"<ul><li><strong>Standard</strong>: at-least-once delivery, <em>best-effort</em> ordering, nearly unlimited throughput.</li><li><strong>FIFO</strong>: exactly-once <em>processing</em>, strict in-order per <code>MessageGroupId</code>, but capped throughput (300 msg/s, or 3,000/s batched; high-throughput mode raises this substantially).</li></ul>Pick Standard unless duplicates or ordering would actually break correctness — FIFO costs throughput and adds group-key design overhead."},
  {"sec":"CLOUD-MSG","front":"What does SQS \"at-least-once\" delivery mean for your consumer?","back":"Standard queues can deliver the same message <strong>more than once</strong> (a copy may survive on a host that didn't get the delete). Your consumer logic must be <strong>idempotent</strong> — dedupe on a business key or use a conditional write. This is the single most common SQS correctness gotcha."},
  {"sec":"CLOUD-MSG","front":"How does the SQS visibility timeout work?","back":"When a consumer receives a message it becomes <strong>invisible</strong> to other consumers for the visibility-timeout window (default 30s, max 12h) instead of being deleted. The consumer must call <code>DeleteMessage</code> after processing; if it crashes or is slow, the timeout expires and the message <strong>reappears</strong> for redelivery. SQS never auto-deletes on receive."},
  {"sec":"CLOUD-MSG","front":"Why can a slow SQS consumer cause duplicate processing even on a single worker?","back":"If processing takes longer than the visibility timeout, the message becomes visible again and a second worker picks it up <em>while the first is still working</em>. Fix by sizing the timeout to worst-case processing time, or call <code>ChangeMessageVisibility</code> to extend (heartbeat) for long jobs. This is independent of Standard's at-least-once duplicates."},
  {"sec":"CLOUD-MSG","front":"When does a message move to an SQS dead-letter queue (DLQ)?","back":"A DLQ is a separate queue attached via a <strong>redrive policy</strong>. After a message has been received <code>maxReceiveCount</code> times without being deleted (i.e. repeatedly failed processing), SQS moves it to the DLQ. It isolates <em>poison-pill</em> messages so they stop blocking the main queue, and you alarm on DLQ depth."},
  {"sec":"CLOUD-MSG","front":"SQS short polling vs long polling — which and why?","back":"<strong>Short polling</strong> samples a subset of hosts and may return empty even when messages exist, wasting empty <code>ReceiveMessage</code> calls. <strong>Long polling</strong> (<code>WaitTimeSeconds</code> up to 20s) waits for a message before returning — fewer empty receives, lower request cost, lower latency. Enable long polling on virtually every queue; it's a pure win for cost and efficiency."},
  {"sec":"CLOUD-MSG","front":"What problem does the SNS → SQS fan-out pattern solve?","back":"One <code>Publish</code> to an SNS topic delivers a copy to <em>every</em> subscribed SQS queue, giving each downstream consumer its own <strong>independent, durable, replayable-until-consumed buffer</strong>. Decouples producer from N consumers, lets each consumer fail/retry/scale independently, and adding a new consumer = subscribe a new queue (no producer change)."},
  {"sec":"CLOUD-MSG","front":"Why subscribe SQS queues to SNS instead of subscribing Lambdas directly to SNS?","back":"Direct SNS→Lambda has no durable buffer: if Lambda is throttled or down past SNS's retry budget (and any on-failure target isn't configured), events can be <strong>lost</strong>. Inserting an SQS queue gives each consumer durability, backpressure absorption, DLQ support, and independent reprocessing. The queue is the shock absorber between a fan-out topic and bursty/fragile consumers."},
  {"sec":"CLOUD-MSG","front":"SNS standard vs FIFO topics — what's the delivery constraint?","back":"<strong>Standard SNS</strong> = high throughput, best-effort ordering, possible duplicates; fans out to SQS, Lambda, HTTP/S, email, SMS, mobile push. <strong>SNS FIFO</strong> = strict ordering + dedup, but delivers <em>only to SQS queues</em> (FIFO <strong>or</strong> Standard) — no direct Lambda/HTTP/email/SMS. For ordering that survives the fan-out, use SNS FIFO → SQS FIFO end-to-end (reach Lambda by triggering it from the queue)."},
  {"sec":"CLOUD-MSG","front":"EventBridge vs SNS — what does the event bus add over a topic?","back":"SNS fans out a <em>message</em> to subscribers with attribute- or payload-based filtering. EventBridge is an <strong>event bus</strong> with richer <strong>content-based routing rules</strong> (match on any JSON field, many rules per bus), a <strong>schema registry</strong>, archive/replay, input transformation, and native <strong>SaaS + AWS-service event</strong> sources. Choose EventBridge for many-source/many-rule routing and integration; SNS for cheap high-fan-out/high-throughput broadcast."},
  {"sec":"CLOUD-MSG","front":"What is EventBridge content-based routing?","back":"A <strong>rule</strong> contains an <strong>event pattern</strong> that matches against the event's JSON (e.g. <code>{\"detail\":{\"status\":[\"FAILED\"]}}</code>); matching events are routed to up to <strong>5 targets</strong> (Lambda, SQS, SNS, Step Functions, another bus, etc.). Routing is decided by event <em>content</em>, not by topic identity — so one bus can feed many consumers off different field values."},
  {"sec":"CLOUD-MSG","front":"Queue vs topic vs bus — the one-line decision","back":"<ul><li><strong>SQS (queue)</strong>: one producer → buffered work pulled by <em>one logical consumer group</em>; decouple + absorb load.</li><li><strong>SNS (topic)</strong>: one event → <em>many</em> pushed broadcasts; cheap high-throughput fan-out.</li><li><strong>EventBridge (bus)</strong>: many sources → <em>routed</em> by content to many targets; integration + filtering hub.</li></ul>"},
  {"sec":"CLOUD-MSG","front":"SQS message ordering: what does FIFO actually order?","back":"FIFO orders messages <strong>within a single <code>MessageGroupId</code></strong>, not across the whole queue. Different group IDs are processed in parallel and independently. Pick a group key that needs strict order (e.g. per-account-id); using one constant group key serializes the entire queue and destroys throughput."},
  {"sec":"CLOUD-MSG","front":"How does SQS FIFO achieve exactly-once and dedup?","back":"Within a <strong>5-minute deduplication window</strong>, SQS rejects messages with a duplicate <code>MessageDeduplicationId</code> (supplied explicitly, or content-hash if <code>ContentBasedDeduplication</code> is on). Combined with single-in-flight-per-group delivery, this gives exactly-once <em>processing</em>. The window is fixed at 5 minutes — re-sends after that will duplicate."},
  {"sec":"CLOUD-MSG","front":"EventBridge Pipes vs a Rule — when do you reach for Pipes?","back":"A <strong>Rule</strong> routes events that already arrive on a bus to up to 5 targets. <strong>EventBridge Pipes</strong> is point-to-point source→target plumbing (e.g. SQS/Kinesis/DynamoDB Streams → target) with optional filter and enrichment, replacing glue Lambdas. Use Pipes to connect a streaming/queue <em>source</em> to a single target; use Rules to fan out from the bus by content."},
  {"sec":"CLOUD-MSG","front":"What's the cost/latency trap of EventBridge vs SQS at high volume?","back":"EventBridge is priced per <strong>custom event published</strong> and adds rule-evaluation latency; it's not built for millions of high-rate messages cheaply. For a tight high-throughput producer→consumer pipe, <strong>SQS</strong> (or Kinesis/MSK for streaming) is cheaper and lower-latency. Reserve EventBridge for routing/integration breadth, not as a raw firehose."}
);

Object.assign(GLOSSARY_DATA, {
  "sqs-standard-vs-fifo": {"term":"SQS Standard vs FIFO","group":"CLOUD-MSG","aliases":["sqs standard","sqs fifo","fifo queue","standard queue"],"eli5":"Standard is a fast queue that may deliver a message twice and slightly out of order; FIFO is a strict, in-order, no-duplicates queue that runs slower.","memory":"Standard = Speedy but Sloppy; FIFO = First-In-First-Out, Fussy and Finite.","full":"<p><strong>Standard</strong> queues give nearly unlimited throughput, <em>at-least-once</em> delivery, and best-effort ordering — you may see duplicates and slight reordering. <strong>FIFO</strong> queues give strict ordering per <code>MessageGroupId</code> and exactly-once <em>processing</em> via a deduplication window, but throughput is capped (300 msg/s, 3,000/s with batching; high-throughput mode raises the ceiling well beyond that).</p><p>FIFO ordering and dedup are <em>per message group</em>, not queue-wide. The group key is the central design decision: it must be the unit that needs ordering (e.g. per customer), because all messages in a group are processed one in-flight at a time.</p>","trap":"Candidates say FIFO is 'exactly-once delivery' — it's exactly-once <em>processing</em> within a 5-minute dedup window; the same payload re-sent 6 minutes later WILL duplicate.","worked":"Per-account ledger events use <code>MessageGroupId=accountId</code>: events for account A stay ordered, account B runs in parallel — order where it matters, throughput where it doesn't.","related":["sqs","visibility-timeout","message-group-id"]},
  "sqs": {"term":"Amazon SQS","group":"CLOUD-MSG","aliases":["sqs","simple queue service","queue"],"eli5":"A fully managed durable queue that decouples a producer from a consumer so the consumer can fail or fall behind without losing work.","memory":"SQS = the shock absorber: it soaks up bursts so downstream doesn't blow up.","full":"<p>SQS is a managed pull-based queue: producers <code>SendMessage</code>, consumers <code>ReceiveMessage</code>, process, then <code>DeleteMessage</code>. It is the canonical <em>decoupling and load-absorption</em> primitive — it buffers bursts, lets consumers scale independently, and survives consumer outages (messages persist up to the retention period, default 4 days, max 14).</p><p>There is no consumer offset/replay like Kafka — once deleted a message is gone, and there's no fan-out from a single queue (each message goes to one consumer). For fan-out you put SNS or EventBridge in front. Visibility timeout, DLQs, and long polling are the three knobs you tune in every real deployment.</p>","trap":"SQS is NOT a log/stream — no replay, no multi-consumer fan-out per message. If the interviewer wants replay or multiple independent readers of the same data, that's Kinesis/MSK or SNS-fan-out, not a bare queue.","worked":"A spike-prone API writes jobs to SQS; a fleet of workers drains it at a steady rate, so a 10x traffic spike just grows queue depth instead of overwhelming the database.","related":["sqs-standard-vs-fifo","visibility-timeout","dlq","long-polling","sns-sqs-fanout"]},
  "visibility-timeout": {"term":"Visibility Timeout","group":"CLOUD-MSG","aliases":["visibility timeout","invisible","in-flight message"],"eli5":"When you receive a message it hides for a while instead of being deleted, so if you crash it comes back for someone else to retry.","memory":"Receive = 'borrow under a cloak' for N seconds; Delete = 'I'm done, drop it'. No delete before the cloak lifts → it reappears.","full":"<p>On <code>ReceiveMessage</code>, SQS makes the message <strong>invisible</strong> (in-flight) for the visibility timeout (default 30s, max 12h) rather than deleting it. The consumer must <code>DeleteMessage</code> after success; if it crashes or runs long, the timeout lapses and the message becomes visible again for redelivery. This is how SQS guarantees no-message-left-behind without trusting the consumer.</p><p>Sizing it is a balance: too short → a slow consumer's message reappears and gets double-processed; too long → a crashed consumer's message is stuck invisible for ages, hurting latency. For variable jobs, extend dynamically with <code>ChangeMessageVisibility</code> (heartbeating).</p>","trap":"Gotcha: processing time > visibility timeout silently causes duplicate processing even with ONE worker — the message reappears mid-job. Always size the timeout to worst-case latency or heartbeat it.","worked":"A 90s enrichment job on a 30s-timeout queue: at 30s the message reappears, a second worker starts it, and you double-write. Fix: set timeout to 120s or call ChangeMessageVisibility every 20s.","related":["sqs","dlq","sqs-standard-vs-fifo"]},
  "dlq": {"term":"Dead-Letter Queue (DLQ)","group":"CLOUD-MSG","aliases":["dlq","dead letter queue","redrive policy","maxreceivecount","poison pill"],"eli5":"A side queue that catches messages that keep failing, so one bad message can't block the whole queue forever.","memory":"maxReceiveCount strikes and you're OUT — sent to the DLQ penalty box.","full":"<p>A DLQ is an ordinary SQS queue designated in a source queue's <strong>redrive policy</strong>. When a message has been received <code>maxReceiveCount</code> times without a successful delete, SQS moves it to the DLQ. This quarantines <em>poison-pill</em> messages — malformed payloads or ones that trip a consumer bug — so they stop being redelivered and stalling throughput.</p><p>Operationally you alarm on DLQ <code>ApproximateNumberOfMessages</code> (any growth = something is broken), inspect the failures, fix the cause, then <strong>redrive</strong> them back to the source queue. A DLQ must match the source type (FIFO source → FIFO DLQ).</p>","trap":"DLQ depth > 0 is an alarm, not a feature you ignore. A silent, un-monitored DLQ means you're losing events and won't notice until retention (14 days max) deletes them.","worked":"A consumer NPEs on messages missing a field. With <code>maxReceiveCount=5</code>, each bad message retries 5 times then lands in the DLQ; good messages flow normally instead of being stuck behind the poison pill.","related":["sqs","visibility-timeout"]},
  "long-polling": {"term":"SQS Long Polling","group":"CLOUD-MSG","aliases":["long polling","short polling","waittimeseconds","receivemessagewaittimeseconds"],"eli5":"Instead of asking 'any messages?' over and over and usually getting 'no', you ask once and let SQS wait until a message shows up.","memory":"Short poll = repeatedly knocking and hearing silence; long poll = leave a note 'wake me when it's here'.","full":"<p><strong>Short polling</strong> (WaitTimeSeconds=0) samples only a subset of SQS's distributed hosts, so it can return empty even when the queue has messages, generating many billable empty <code>ReceiveMessage</code> calls. <strong>Long polling</strong> (<code>WaitTimeSeconds</code> 1–20) holds the request open until a message arrives or the timer fires, querying all hosts.</p><p>Long polling cuts request cost (fewer empty receives), reduces latency to first message, and is recommended for essentially all workloads. Set it per-call on <code>ReceiveMessage</code> or as the queue default via <code>ReceiveMessageWaitTimeSeconds</code>.</p>","trap":"Empty receives are BILLED as API requests — a tight short-poll loop quietly racks up request charges and can rate-limit you. Long polling is the default-on best practice, not an edge-case tuning.","related":["sqs"]},
  "sns": {"term":"Amazon SNS","group":"CLOUD-MSG","aliases":["sns","simple notification service","topic","pub/sub"],"eli5":"A pub/sub topic: publish one message and it's pushed to every subscriber at once.","memory":"SNS = a megaphone (push to many); SQS = a mailbox (pull one at a time).","full":"<p>SNS is a managed <strong>push-based pub/sub</strong> topic. A single <code>Publish</code> is delivered to all subscribers — SQS queues, Lambda, HTTP/S endpoints, email, SMS, mobile push. It's the cheap, high-throughput <em>broadcast/fan-out</em> primitive: producers don't know or care who's listening, and subscribers come and go independently.</p><p>SNS has <strong>message filtering</strong> — subscription filter policies on message <em>attributes</em> or, with <code>FilterPolicyScope=MessageBody</code>, on the JSON <em>payload</em> — so a subscriber receives only the subset it wants. It has no durable storage of its own: if a push target is unavailable beyond SNS's retry policy, delivery to that target fails. That's why durable consumers subscribe an SQS queue rather than taking the push directly.</p>","trap":"SNS does not buffer for you — a direct SNS→Lambda or SNS→HTTP subscriber that's down past the retry budget LOSES the message. Durability requires an SQS queue between topic and consumer.","worked":"An 'order-placed' topic fans out to billing, fulfillment, and analytics queues. Adding a fraud-check consumer = subscribe one more queue; the order service never changes.","related":["sns-sqs-fanout","eventbridge","queue-vs-topic-vs-bus"]},
  "sns-sqs-fanout": {"term":"SNS → SQS Fan-Out","group":"CLOUD-MSG","aliases":["fan-out","fanout","sns to sqs","fan out pattern"],"eli5":"Publish once to a topic; each consumer reads from its own private durable queue subscribed to that topic.","memory":"Topic = the splitter, queues = per-consumer buckets that catch their own copy.","full":"<p>The canonical AWS decoupling pattern: an SNS topic with multiple SQS queues subscribed. One <code>Publish</code> drops an independent copy into <em>every</em> queue, so each consumer gets a <strong>durable, replayable-until-consumed, independently-scalable</strong> buffer with its own DLQ and retry behavior. The producer is fully decoupled from the count and health of consumers.</p><p>It combines SNS's broadcast with SQS's durability and backpressure: a slow or down consumer just grows its own queue depth without affecting siblings or the producer. To preserve strict ordering across the fan-out, use SNS FIFO → SQS FIFO end-to-end.</p>","trap":"Set the SQS access policy to allow the SNS topic to send, or deliveries silently fail. And remember each queue gets a FULL copy — N queues = N× the storage/processing, not a shared single copy.","worked":"A 'user-signup' SNS topic fans out to email-welcome, CRM-sync, and data-lake-ingest SQS queues; the data-lake consumer can be down for an hour and simply backfills from its queue while the others run unaffected.","related":["sns","sqs","dlq","queue-vs-topic-vs-bus"]},
  "eventbridge": {"term":"Amazon EventBridge","group":"CLOUD-MSG","aliases":["eventbridge","event bus","cloudwatch events","eventbridge rule","event pattern"],"eli5":"A smart event bus that routes events to different targets based on what's inside the event, and natively understands AWS and SaaS events.","memory":"SNS routes by WHO subscribed; EventBridge routes by WHAT the event says.","full":"<p>EventBridge is a serverless <strong>event bus</strong> with <strong>content-based routing</strong>: a <em>rule</em> holds an <em>event pattern</em> that matches any field of the event JSON and forwards matches to up to <strong>5 targets</strong> (Lambda, SQS, SNS, Step Functions, another bus, etc.). It adds a <strong>schema registry</strong>, archive + <strong>replay</strong>, input transformation, and first-class integrations with AWS-service events and dozens of <strong>SaaS</strong> sources (partner event buses).</p><p>Choose EventBridge when you have many event sources and many consumers that each want different slices by content, or need SaaS/AWS event integration. It is richer but more expensive per event and higher-latency than SNS — it is not a raw high-throughput firehose. EventBridge <strong>Pipes</strong> complements it for point-to-point source→(filter/enrich)→target plumbing.</p>","trap":"EventBridge is billed per custom event and adds routing latency — don't use it as a cheap high-volume transport. For millions of msgs/s producer→consumer, that's SQS or Kinesis/MSK, not the bus.","worked":"One bus receives 'payment' events; a rule <code>{\"detail\":{\"status\":[\"FAILED\"]}}</code> routes only failures to a Step Functions retry workflow, while a separate rule archives all events to a data lake.","related":["sns","queue-vs-topic-vs-bus","sqs"]},
  "queue-vs-topic-vs-bus": {"term":"Queue vs Topic vs Bus (SQS/SNS/EventBridge)","group":"CLOUD-MSG","aliases":["queue vs topic","when to use sqs sns eventbridge","sqs vs sns vs eventbridge"],"eli5":"Queue = buffer work for one consumer group; Topic = broadcast the same message to many; Bus = route events to different targets by their content.","memory":"Buffer / Broadcast / Brains-based-routing → SQS / SNS / EventBridge.","full":"<p><strong>SQS (queue)</strong>: decouple a producer from <em>one logical consumer</em> and absorb load; pull-based, durable, no fan-out, no content routing. <strong>SNS (topic)</strong>: push one message to <em>many</em> subscribers cheaply at high throughput; attribute- or payload-based filtering but not rich rule routing. <strong>EventBridge (bus)</strong>: ingest many sources and <em>route by event content</em> to many targets, with schema registry, replay, and SaaS/AWS integrations.</p><p>They compose: SNS→SQS fan-out for durable broadcast; EventBridge→SQS so a routed event lands in a durable buffer. Pick by the question being asked — 'buffer it?' → SQS, 'tell everyone?' → SNS, 'route by what's inside, integrate sources?' → EventBridge.</p>","trap":"Don't default to EventBridge as the universal answer: for cheap high-throughput simple fan-out, SNS is the right (cheaper, faster) tool; for plain decoupling, SQS. EventBridge earns its cost only when you need content routing or SaaS/AWS integration.","worked":"Ingest pipeline: EventBridge routes S3 'object-created' events by prefix to the right processor; that processor's work queues on SQS; completion publishes to an SNS topic that fans out to notifier + audit queues.","related":["sqs","sns","eventbridge","sns-sqs-fanout"]},
  "message-group-id": {"term":"MessageGroupId (FIFO ordering scope)","group":"CLOUD-MSG","aliases":["messagegroupid","message group","dedup window","messagededuplicationid"],"eli5":"In a FIFO queue, ordering and one-at-a-time delivery apply within a group key you choose, not across the whole queue.","memory":"Group ID = the lane; messages stay in order within a lane, lanes run side by side.","full":"<p><code>MessageGroupId</code> is the FIFO partition key: SQS guarantees strict order and a single in-flight message <em>per group</em>, while different groups are processed in parallel. Choosing the group key is the core FIFO design task — it should be the smallest unit that genuinely needs ordering (e.g. per entity id) to maximize parallelism.</p><p>Dedup is governed by <code>MessageDeduplicationId</code> within a fixed <strong>5-minute window</strong> (or a content hash if <code>ContentBasedDeduplication</code> is enabled). Within that window a repeated id is dropped, delivering exactly-once processing; outside it, a re-send duplicates.</p>","trap":"A single constant MessageGroupId serializes the ENTIRE queue down to one-at-a-time — a classic FIFO throughput-killer. And dedup is only 5 minutes, fixed: it won't catch a retry an hour later.","worked":"Per-user activity stream uses <code>MessageGroupId=userId</code>: each user's events stay ordered, but 10,000 users process concurrently instead of one global line.","related":["sqs-standard-vs-fifo","sqs"]}
});
