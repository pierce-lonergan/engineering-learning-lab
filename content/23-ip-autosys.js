/* Automation System Design (IP-AUTOSYS) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"IP-AUTOSYS","front":"What is the canonical idempotency pattern (the \"Stripe pattern\"), and why does Trading Operations care?","back":"<strong>Idempotency</strong> = an operation can be retried safely and produce the same effect as a single execution. The pattern: the client generates a unique <code>idempotency_key</code>; the server stores a row keyed on <code>(caller, idempotency_key)</code> holding the <strong>request_hash</strong>, the saved <strong>response</strong>, and a <strong>status</strong>.<ul><li>Same key + <em>same</em> payload (request_hash matches) &rarr; return the <strong>original stored response</strong>, do NOT re-execute.</li><li>Same key + <em>different</em> payload (hash mismatch) &rarr; reject with <code>409 Conflict</code> (community/IETF convention; present as a pattern, not literal Stripe docs).</li><li>New key &rarr; execute once, persist the result atomically.</li></ul><em>Why ops cares:</em> networks and queues deliver <strong>at-least-once</strong>; without idempotency a retried order-entry or settlement instruction double-books. The same primitive that protects a payment charge protects an order or a settlement instruction &mdash; which is why payments/billing/ledger experience maps ~1:1 onto post-trade automation.</li></ul>"},
  {"sec":"IP-AUTOSYS","front":"At-least-once vs exactly-once vs effectively-once delivery &mdash; define each and say which one you actually build.","back":"<ul><li><strong>At-least-once</strong>: every message is delivered, but possibly <em>more than once</em> (retries, redeliveries). The realistic default for queues, webhooks, and exchange feeds.</li><li><strong>At-most-once</strong>: never duplicated, but may be <em>lost</em>. Usually unacceptable for trades.</li><li><strong>Exactly-once</strong> <em>delivery</em>: a theoretical ideal &mdash; practically impossible across an unreliable network with independent failures of producer, broker, and consumer.</li></ul><strong>What you actually build = effectively-once (exactly-once processing)</strong>: accept at-least-once delivery, then make the <em>consumer</em> idempotent via a dedup key (client order id / trade id / idempotency key). Store processed ids; on redelivery, recognize and skip (or return the prior result). <em>Don't claim exactly-once delivery in an interview &mdash; claim effectively-once via idempotency + dedup.</em>"},
  {"sec":"IP-AUTOSYS","front":"Sketch the architecture of a reconciliation system: internal record vs external truth.","back":"<strong>Internal vs external truth</strong> recon is a pipeline:<ul><li><strong>Ingest</strong> the external truth (clearing/custodian SOD file, exchange/processor settlement file, on-chain data) and the internal records.</li><li><strong>Normalize</strong> both sides to one schema (symbol, qty, price, amount, currency, trade id, settlement date).</li><li><strong>Match</strong> on <em>stable identifiers</em> (trade id, then amount + currency + symbol + settle date).</li><li><strong>Classify</strong>: matched within tolerance; <strong>break</strong> (unmatched / out-of-tolerance); expected-discrepancy (timing, partial fill, fee, rounding &rarr; tolerate).</li><li><strong>Exception queue</strong>: breaks routed to an ops queue for manual/bulk correction in tooling.</li><li><strong>Resolve + audit</strong>: every action immutably logged.</li><li><strong>Idempotent re-run</strong>: re-running the same input set reproduces the same matches/breaks &mdash; no double-counting.</li></ul>The external source is the <strong>truth</strong>; the internal book bends to it. The headline KPI is the <strong>STP rate</strong>."},
  {"sec":"IP-AUTOSYS","front":"Why is reconciliation matching not just <code>internal == external</code>? How do you match and tolerate?","back":"Exact equality fails because both sides legitimately differ. <strong>Match on stable identifiers</strong> with a fallback ladder, and <strong>tolerate expected discrepancies</strong>.<ul><li><strong>Primary key</strong>: shared <code>trade_id</code> when both systems carry it.</li><li><strong>Composite fallback</strong>: <code>symbol + qty + amount + currency + settlement_date</code> when ids differ across venues.</li><li><strong>Tolerances</strong>: timing (T vs T+1 booking lag), partial fills (sum of fills = parent qty), fees/commissions, FX/decimal rounding. Compare amounts with an epsilon, not <code>=</code>.</li></ul><em>Anything still unmatched or out-of-tolerance is a <strong>break</strong>.</em> Example SQL spine: <code>SELECT * FROM internal i FULL OUTER JOIN external e ON i.trade_id = e.trade_id WHERE i.trade_id IS NULL OR e.trade_id IS NULL OR ABS(i.amount - e.amount) > 0.01</code> surfaces breaks on both sides."},
  {"sec":"IP-AUTOSYS","front":"Write the dedup/idempotency check in Python+SQL for safely processing a possibly-redelivered settlement instruction.","back":"Store the key first, execute once, persist the result atomically:<pre><code>def process(caller, key, payload):\n    h = sha256(canonical_json(payload))\n    row = db.fetch(\n      \"SELECT request_hash, status, response FROM idempotency \"\n      \"WHERE caller=%s AND idempotency_key=%s FOR UPDATE\",\n      (caller, key))\n    if row:\n        if row.request_hash != h:\n            raise Conflict(409)        # same key, different payload\n        return row.response            # replay -&gt; original result\n    db.execute(\n      \"INSERT INTO idempotency(caller, idempotency_key, request_hash, status) \"\n      \"VALUES (%s,%s,%s,'in_progress')\", (caller, key, h))\n    resp = do_settlement(payload)       # the side effect, once\n    db.execute(\"UPDATE idempotency SET status='done', response=%s \"\n               \"WHERE caller=%s AND idempotency_key=%s\", (resp, caller, key))\n    return resp</code></pre><strong>FOR UPDATE</strong> serializes concurrent retries; the <strong>UNIQUE constraint</strong> on <code>(caller, idempotency_key)</code> is the real guard against a race that slips past the SELECT."},
  {"sec":"IP-AUTOSYS","front":"What is an immutable audit trail / event sourcing, and why is it non-negotiable in trade ops?","back":"<strong>Event sourcing</strong>: persist state as an <em>append-only sequence of immutable events</em> (TradeBooked, FillReceived, BreakRaised, BreakResolved) rather than overwriting a current-state row. Current state is a <strong>fold/replay</strong> of the events.<ul><li><strong>Immutable</strong>: events are never updated/deleted &mdash; corrections are new compensating events. You can answer \"what did we know, and when\" for any past instant.</li><li><strong>Auditability</strong>: regulators and clearing disputes require a provable, tamper-evident history of every position and correction.</li><li><strong>Replay</strong>: rebuild a projection (positions, a recon report) by re-folding events &mdash; pairs with <em>idempotent re-runnable</em> pipelines.</li></ul><em>Trap:</em> never <code>UPDATE</code>/<code>DELETE</code> a booked record in place &mdash; append a reversing/adjusting event so the trail stays intact."},
  {"sec":"IP-AUTOSYS","front":"What is a dead-letter queue (DLQ), and how does it fit a trade-ops pipeline?","back":"A <strong>dead-letter queue</strong> is where a message goes after it <em>repeatedly fails</em> processing (max retries exceeded) or fails validation, so it stops blocking the main flow.<ul><li>The consumer pulls a settlement/fill message; on a transient error it <strong>retries with backoff</strong>; after N attempts it is routed to the DLQ instead of looping forever (poison-message protection).</li><li>DLQ entries are <strong>alerted on</strong> and worked by ops &mdash; often the same place a recon <strong>break</strong> surfaces.</li><li>DLQ messages must carry enough context (original payload, error, attempt count) to <strong>replay idempotently</strong> after a fix.</li></ul><em>Contrast with a break:</em> a break = data that doesn't reconcile; a DLQ entry = a message that couldn't be processed. Both end in an exception queue for humans, and both must be re-runnable without double effect."},
  {"sec":"IP-AUTOSYS","front":"What is backpressure, and how do you handle exchange API rate limits and bursts?","back":"<strong>Backpressure</strong> = a downstream consumer signaling/forcing an upstream producer to slow down when it can't keep up, instead of overflowing memory or getting throttled/banned. Mechanisms:<ul><li><strong>Bounded queues</strong>: block or shed when full rather than buffering unboundedly.</li><li><strong>Rate limiting</strong>: token-bucket / leaky-bucket to stay under the venue's published limits.</li><li><strong>Retries with exponential backoff + jitter</strong> on <code>429</code>/<code>5xx</code>; honor <code>Retry-After</code>.</li><li><strong>Idempotency (client order ids)</strong> so a backed-off retry can't double-submit.</li></ul>Critical for crypto/prediction venues that are predominantly <strong>REST + WebSocket</strong> with hard rate limits; you pace request volume to the slowest dependency and let the queue absorb bursts."},
  {"sec":"IP-AUTOSYS","front":"What is STP and the STP rate, and why is it THE KPI for this role?","back":"<strong>STP = Straight-Through Processing</strong>: a trade flows from execution through capture, matching/affirmation, clearing and settlement <em>with no manual intervention</em>. The <strong>STP rate</strong> = fraction of trades that settle fully automatically (no human touch, no break).<ul><li>It is the headline KPI because <strong>manual touches don't scale</strong> and each one is latency + error risk.</li><li>Under <strong>T+1</strong> (US, since May 28, 2024) the intraday window to fix exceptions is only ~2-5 hrs before the <strong>9:00 PM ET DTCC affirmation cutoff</strong>; low STP &rarr; missed affirmation &rarr; <strong>settlement fails</strong>.</li><li>Every automation you build (recon, dedup, exception auto-resolution) is justified by its lift to STP and reduction in break count / mean-time-to-resolve.</li></ul><em>In an interview: frame your work as \"raise STP, shrink the exception queue.\"</em>"},
  {"sec":"IP-AUTOSYS","front":"How does T+1 change the engineering requirements for post-trade automation?","back":"<strong>T+1</strong> = settle one business day after trade date (US moved T+2 &rarr; T+1 on <strong>May 28, 2024</strong> for equities, corp/muni bonds, UITs, ADRs, ETFs; EU/UK align <strong>Oct 11, 2027</strong>).<ul><li><strong>Compressed window</strong>: affirmation must hit the <strong>DTCC 9:00 PM ET</strong> cutoff on trade date &mdash; only ~2-5 hrs to clear exceptions vs an overnight batch before.</li><li>Forces <strong>real-time / intraday</strong> recon and exception management, not end-of-day batch.</li><li>Raises the bar on <strong>STP</strong>: anything not auto-affirmed risks a <strong>settlement fail</strong>.</li><li><strong>STP + idempotent re-runnable</strong> pipelines let you re-process late/corrected files inside the window without double-booking.</li></ul>Engineering answer: move from batch to streaming/intraday, automate break resolution, and make every step replay-safe."},
  {"sec":"IP-AUTOSYS","front":"Walk the US equities settlement chain: ITP, NSCC, DTC &mdash; and where affirmation gates it.","back":"<strong>DTCC</strong> subsidiaries handle the post-trade chain:<ul><li><strong>ITP (Institutional Trade Processing)</strong>: pre-settlement <strong>matching/affirmation</strong> &mdash; \"match-to-instruct.\" The trade's economics are agreed and an affirmed instruction is produced.</li><li><strong>NSCC</strong>: the <strong>CCP</strong> &mdash; <strong>multilateral netting</strong>, <strong>novation</strong> (steps in as counterparty), and the <strong>guarantee</strong>, via <strong>Continuous Net Settlement (CNS)</strong>.</li><li><strong>DTC</strong>: the <strong>central securities depository</strong> &mdash; <strong>book-entry settlement & custody</strong> (delivery vs payment).</li></ul><strong>Affirmation gates NSCC/DTC settlement</strong>: a trade unaffirmed by the <strong>9:00 PM ET</strong> cutoff doesn't flow into guaranteed netting/settlement and risks a fail. Your automation's job is to drive the affirmation rate (a facet of STP)."},
  {"sec":"IP-AUTOSYS","front":"What is a FIX drop-copy feed and how do you use it for trade-capture and reconciliation?","back":"<strong>FIX</strong> = a tag=value protocol over stateful sessions with <strong>sequence numbers</strong> (<code>NewOrderSingle 35=D</code>, <code>ExecutionReport 35=8</code>). A <strong>drop-copy</strong> is a read-only copy of all <code>35=8</code> <strong>Execution Reports</strong> for trade capture/recon.<ul><li><code>150=F</code> Trade (fill), <code>150=G</code> Trade Correction, <code>150=H</code> Trade Cancel &mdash; your internal book must apply corrections/cancels, not just fills.</li><li><strong>Sequence numbers</strong> let you detect gaps; a <strong>Resend Request</strong> (the FIX session message <code>35=2</code>) replays missed messages &mdash; recon must be <em>idempotent</em> so replays don't double-book.</li><li>Used by venues such as <strong>Coinbase Derivatives</strong> and <strong>Zero Hash</strong> as the standard trade-capture feed.</li></ul><em>Contrast:</em> most crypto/prediction venues are <strong>REST + WebSocket</strong>, not FIX, so you normalize both into one internal trade model."},
  {"sec":"IP-AUTOSYS","front":"How do prediction-market mechanics change post-trade settlement and recon vs equities?","back":"<strong>Prediction markets</strong>: binary <em>event</em> contracts priced <strong>$0-$1</strong> (= implied probability), settling to <strong>$1 if the event occurs, else $0</strong>.<ul><li>Settlement is to <strong>EVENT RESOLUTION</strong>, not a clearinghouse delivery &mdash; introducing <strong>oracle/resolution risk</strong>, <strong>disputed outcomes</strong>, and <strong>multi-platform arbitrage</strong> (same event on Kalshi vs Polymarket).</li><li>No DTCC/NSCC/DTC chain: <strong>Kalshi</strong> = a CFTC-regulated DCM (designated contract market, since 2020); <strong>Polymarket</strong> = crypto-native (Polygon/USDC), a CFTC-regulated US exchange via the Amended Order of <strong>Nov 25, 2025</strong>.</li><li><strong>CFTC Prediction Markets Advisory (Feb 25, 2026)</strong>: flags insider trading, wash/pre-arranged trading, spoofing, and manipulation &mdash; surveillance/recon must catch these.</li></ul><em>Recon implication:</em> your \"external truth\" is the platform's <strong>resolution</strong> plus on-chain/settlement records; you reconcile position &rarr; resolved payout, and handle resolution disputes as a distinct break class."},
  {"sec":"IP-AUTOSYS","front":"What's different about reconciling crypto post-trade settlement?","back":"Crypto has <strong>no central depository</strong>, so the \"external truth\" is <strong>on-chain data</strong> plus venue/processor records.<ul><li><strong>Custody</strong>: hot (online, fast, riskier) vs cold (offline); recon must map internal balances to on-chain <strong>addresses</strong>.</li><li><strong>Finality is probabilistic</strong>: settlement can be reversed by <strong>reorgs</strong> until enough confirmations &mdash; recon must not treat an unconfirmed tx as final.</li><li><strong>24/7</strong> markets: no end-of-day batch boundary; recon is continuous.</li><li><strong>Many chains + gas fees + rounding</strong> = expected discrepancies to tolerate.</li><li><strong>Cash leg</strong>: in 2026 stablecoins/tokenized deposits settle on-chain; the federal <strong>GENIUS Act</strong> stablecoin framework (effective Mar 2, 2026) requires timely redemption <strong>no later than 2 business days</strong>.</li></ul>Match on tx hash / address / amount / asset / block, tolerate gas and confirmation timing, and re-run idempotently as confirmations accrue."},
  {"sec":"IP-AUTOSYS","front":"Why does a payments/billing/ledger background (e.g. a senior bank engineer) map ~1:1 onto trade-ops automation? Name the shared primitives.","back":"Post-trade automation and payments are the <em>same engineering shape</em>: a side effect that must happen <strong>effectively once</strong>, reconciled against an <strong>external truth</strong>, with <strong>exceptions</strong> worked by humans.<ul><li><strong>Idempotency keys</strong>: payments dedup charges; trade ops dedup order-entry/settlement instructions &mdash; the identical <code>(caller, key) -&gt; request_hash/response/status</code> store.</li><li><strong>Reconciliation against external truth</strong>: payments recon an internal ledger vs a processor settlement file; trade ops recon the book vs clearing/custodian SOD or on-chain data &mdash; the same ingest/normalize/match/classify pipeline.</li><li><strong>Exception handling</strong>: failed payments vs settlement <strong>breaks</strong> &mdash; the same exception-queue + audit pattern.</li><li>Plus shared infra: <strong>Python/Java, Kafka/Spark, Snowflake, AWS</strong>, immutable ledgers, DLQs.</li></ul><em>Positioning line:</em> \"the primitives are the same; I'm changing the domain, not the engineering.\""},
  {"sec":"IP-AUTOSYS","front":"Why validate data at boundaries, and what do you check on an inbound settlement/SOD file?","back":"<strong>Validate at the boundary</strong> (the ingest edge) so bad data never enters the pipeline and corrupts the book &mdash; fail fast, with a clear rejection, before normalization/matching.<ul><li><strong>Structural</strong>: schema, required fields, types, encoding, expected row/control-count.</li><li><strong>Referential</strong>: symbols/accounts/currencies exist; settlement dates are valid business days.</li><li><strong>Semantic/invariants</strong>: qty/price/amount signs, <code>price*qty ~= amount</code> within tolerance, no duplicate trade ids.</li><li><strong>Idempotency of ingest</strong>: re-ingesting the same file (by file hash / batch id) doesn't double-load.</li></ul>Rows that fail go to a <strong>quarantine/DLQ</strong> with the reason, alerted to ops &mdash; they don't silently become phantom positions. Tools: <code>pydantic</code>/<code>pandera</code> schemas, <code>CHECK</code> constraints in SQL. <em>The boundary is your last chance before garbage becomes a settled (wrong) trade.</em>"},
  {"sec":"IP-AUTOSYS","front":"How do orchestration/scheduling, monitoring, and alerting make an automation system operable?","back":"Automation isn't just the transform &mdash; it's the <strong>operability</strong> around it.<ul><li><strong>Orchestration/scheduling</strong> (e.g. Airflow-style DAGs or cron): model dependencies (SOD file arrives &rarr; ingest &rarr; recon &rarr; report), enforce SLAs (must finish before the <strong>9:00 PM ET</strong> cutoff), and make tasks <strong>idempotent + retryable</strong> so a re-run is safe.</li><li><strong>Monitoring</strong>: track <strong>STP rate</strong>, break count, queue depth/age, DLQ size, ingest latency, and file-arrival timeliness &mdash; the leading indicators of a missed settlement window.</li><li><strong>Alerting</strong>: page on <em>SLA risk</em> (file late, exceptions unresolved with N minutes to cutoff, DLQ growing), not just on crashes.</li></ul><em>Design rule:</em> every scheduled step is re-runnable without double effect (idempotency), every break/DLQ entry is observable, and alerts fire <em>before</em> the T+1 window closes &mdash; not after a fail."}
);

Object.assign(GLOSSARY_DATA, {
  "stp-rate": {"term":"Straight-Through Processing (STP) Rate","group":"IP-AUTOSYS","eli5":"The share of trades that settle automatically with zero human touches.","memory":"STP rate = the headline KPI; raise it, shrink the exception queue.","full":"<p><strong>Straight-Through Processing</strong> is a trade flowing from execution through capture, matching/affirmation, clearing and settlement with <em>no manual intervention</em>. The <strong>STP rate</strong> is the fraction of trades achieving this.</p><p>It is THE KPI for a trading-ops automation role because manual touches don't scale and each is latency + error risk. Under T+1's compressed window, low STP directly causes missed affirmations and settlement fails. Every automation (recon, dedup, auto-resolution) is justified by its lift to STP.</p>","trap":"Don't measure 'jobs ran' &mdash; measure trades settled with no human touch and no break; a green pipeline with a full exception queue is low STP.","worked":"Automating partial-fill aggregation drops daily breaks from 300 to 30, lifting STP from 94% to 99.4% and freeing ops to work only true exceptions before the 9:00 PM ET cutoff.","related":["break","t-plus-1","reconciliation"]},
  "t-plus-1": {"term":"T+1 Settlement","group":"IP-AUTOSYS","eli5":"Trades settle one business day after the trade date, so there's much less time to fix problems.","memory":"US T+2 -> T+1 on May 28, 2024; DTCC affirmation by 9:00 PM ET on trade date.","full":"<p><strong>T+1</strong> means settlement one business day after trade date. The US moved from T+2 to T+1 on <strong>May 28, 2024</strong> (equities, corp/muni bonds, UITs, ADRs, ETFs); the EU/UK align on <strong>Oct 11, 2027</strong>. The <strong>DTCC affirmation cutoff is 9:00 PM ET on trade date</strong>.</p><p>The compressed ~2-5 hr intraday exception window forces real-time recon and exception management over end-of-day batch, and raises the premium on STP and idempotent, replay-safe pipelines so corrected/late files can be reprocessed inside the window without double-booking.</p>","trap":"T+1 didn't just shorten the timeline &mdash; it killed the overnight batch model; unresolved exceptions now become fails the same day.","worked":"A custodian sends a corrected SOD file at 7 PM ET; the idempotent recon job re-runs, clears the remaining breaks, and affirmation lands before the 9:00 PM cutoff &mdash; no fail.","related":["dtcc-chain","stp-rate","break"]},
  "audit-trail": {"term":"Immutable Audit Trail / Event Sourcing","group":"IP-AUTOSYS","eli5":"Record every change as a permanent event you never erase, so you can replay history exactly.","memory":"Append-only events; corrections are new events; state = replay of events.","full":"<p><strong>Event sourcing</strong> persists state as an append-only sequence of immutable events (TradeBooked, FillReceived, BreakRaised, BreakResolved); current state is a fold/replay of those events. Nothing is updated or deleted in place &mdash; corrections are new compensating events.</p><p>This gives a tamper-evident, point-in-time-reconstructable history (what did we know, and when) required for regulators and clearing disputes, and it pairs with idempotent, re-runnable pipelines: you rebuild any projection (positions, a recon report) by re-folding events.</p>","trap":"Never UPDATE/DELETE a booked record to 'fix' it &mdash; append a reversing/adjusting event, or you destroy the audit trail.","worked":"A mis-booked quantity is corrected by appending a TradeAdjusted event; the original TradeBooked stays, and replaying both yields the right position while preserving history.","related":["reconciliation","idempotency-key","break"]}
});
