/* Teach me from the ground up: IP-SQL (auto-assembled) */
enh({
  "window-function": {"teach":"<p>At its core, a window function is just <strong>an aggregate that doesn't squash your rows together</strong>. A normal <code>SUM()</code> with <code>GROUP BY symbol</code> hands you back one row per symbol — your 10,000 individual fills vanish into a single total. A window function lets you compute that same total <em>while keeping all 10,000 rows</em>, stamping each one with a number computed from its neighbors.</p><h4>Dissecting the OVER clause</h4><p>The magic word is <code>OVER (...)</code>. Everything inside it describes the \"window\" of rows the function gets to look at for the current row:</p><ul><li><code>PARTITION BY symbol</code> — split the data into independent piles, one per symbol. AAPL's running total never sees TSLA's rows. Think of it like <code>GROUP BY</code>, except it groups <em>for the calculation only</em>, not for the output.</li><li><code>ORDER BY ts</code> — sort within each pile by time, so \"the rows before me\" has a meaning. Without an order, \"running\" is undefined.</li></ul><div class=\"analogy\"><p>Imagine a relay race photo-finish. <code>GROUP BY</code> reports only the final team time — you lose every runner. A window function instead writes on each runner's bib their <em>cumulative team time as they crossed</em>, so every runner still appears in the photo, each carrying a number computed from everyone ahead of them.</p></div><h4>The toolbox</h4><ul><li><code>SUM() OVER (... ORDER BY ts)</code> — running totals, e.g. your <strong>running position</strong> per symbol.</li><li><code>ROW_NUMBER()</code> — a strictly unique 1,2,3 per partition; perfect for \"keep the latest amended fill\".</li><li><code>LAG()</code>/<code>LEAD()</code> — peek at the previous/next row to compute deltas or spot a missing FIX sequence number.</li><li><code>RANK()</code>/<code>DENSE_RANK()</code> — top-N rankings.</li></ul><h4>The frame — and why it bites</h4><p>When you add <code>ORDER BY</code>, SQL silently picks a default <em>frame</em> of <code>RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW</code>. <code>RANGE</code> groups <strong>tied</strong> ordering keys together — so two fills with the identical <code>exec_ts</code> both get the full sum-through-both, not a stepwise total. That's nondeterministic and usually not what you want. Writing the frame explicitly as <code>ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW</code> forces a true row-by-row running total even across timestamp ties.</p><h4>The classic trap</h4><p><code>ROW_NUMBER()</code> is <em>always</em> unique (1,2,3,4). <code>RANK()</code> gives ties the same number and then skips (1,1,3). If you dedup amended fills with <code>RANK()=1</code> and two versions tie, <strong>both</strong> survive and your duplicate fill leaks back in, double-counting the position. For \"pick exactly one winner\", always reach for <code>ROW_NUMBER()</code> with a deterministic tiebreaker.</p><div class=\"key\">A window function keeps one output row per input row while computing across related rows — and the moment you add <code>ORDER BY</code>, spell out <code>ROWS BETWEEN ...</code> so ties don't quietly corrupt your running totals.</div>"},
  "running-position": {"teach":"<p>At its core, a running net position is just <strong>your buys minus your sells in a symbol, accumulated trade by trade</strong>. After every fill you ask \"how much do I now own?\" and the answer is the sum of everything that came before, with sells counting as negatives.</p><h4>Building it from fills</h4><p>Two pieces make it work. First, you have to <strong>sign the quantity</strong> by side — a buy adds, a sell subtracts:</p><ul><li><code>CASE WHEN side='BUY' THEN qty ELSE -qty END AS signed_qty</code></li></ul><p>Then you accumulate those signed quantities in time order, per symbol, with a window function:</p><ul><li><code>SUM(signed_qty) OVER (PARTITION BY symbol ORDER BY exec_ts)</code></li></ul><p>The very last row for each symbol holds the <strong>current position</strong> — the number you actually own right now. That single number drives position-keeping, exposure limits, margin, and unrealized P&L.</p><div class=\"analogy\"><p>It's a bank-account balance. Each fill is a transaction: deposits (buys) push the balance up, withdrawals (sells) pull it down. The \"running position\" is the balance line on your statement after each transaction — and the bottom line is what you have today.</p></div><h4>Why derive it instead of storing it</h4><p>It is tempting to keep a single <code>positions</code> row and just mutate it on every fill. But the disciplined design treats positions as a <strong>derived view</strong> of the immutable fills table, not an independent source of truth. The fills are the facts; the position is a <em>function</em> of those facts.</p><p>Why does that matter? Because you can always <strong>recompute</strong> from scratch and <strong>re-reconcile</strong> against the broker. If a late correction arrives, or a fill was double-booked, you don't have to surgically patch a mutable counter and pray — you replay the fills and the truth re-emerges. (This mirrors a hard-won lesson from real trading systems: the source of truth is the confirmed broker activity, and the session's own running tally is just a shadow that must be reconcilable.)</p><h4>The trap that silently doubles your size</h4><p>Forget the sign and you sum raw <code>qty</code> — now a 100-share buy and a 100-share sell <em>both add</em>, reporting a 200-share position when your true exposure is flat (zero). You'd think you're long, hedge against a phantom, and lose money on a position that never existed. Always sign before you sum.</p><div class=\"key\">Sign the quantity by side, then cumulatively <code>SUM</code> in time order per symbol — and keep positions as a recomputable view of fills, never as a mutable counter you can't reconcile.</div>"},
  "row-number-dedup": {"teach":"<p>At its core, this trick is just <strong>\"number the copies newest-first, then keep only number one.\"</strong> When the same logical event arrives several times — an original fill, then a correction, then maybe a cancel — you want exactly one survivor: the most recent, authoritative version. <code>ROW_NUMBER()</code> hands you that survivor cleanly.</p><h4>How the numbering works</h4><p>Look at the clause piece by piece:</p><ul><li><code>PARTITION BY venue_exec_id</code> — gather all the copies of one execution into a pile. Each distinct execution gets its own pile.</li><li><code>ORDER BY version DESC, received_ts DESC</code> — sort each pile so the <em>newest</em> version sits on top. The highest version wins; if versions tie, the latest receive time breaks it.</li><li><code>ROW_NUMBER()</code> stamps 1 on the top row, 2 on the next, and so on.</li></ul><p>Filter <code>WHERE rn = 1</code> and you've collapsed every pile down to its single latest state.</p><div class=\"analogy\"><p>Think of email threads. A correction or cancel isn't a brand-new conversation — it's a reply that supersedes the earlier message. Sorting each thread newest-first and reading only the top message gives you the current truth without re-reading the whole back-and-forth. <code>rn=1</code> is \"show me only the top of each thread.\"</p></div><h4>Why trading desks need exactly this</h4><p>A <strong>FIX drop-copy</strong> feed pushes <code>35=8</code> ExecutionReports, and those are <em>not</em> append-only. You'll see ExecType <strong>Trade Correction (<code>150=G</code>)</strong> revising a price or quantity, and <strong>Trade Cancel (<code>150=H</code>)</strong> busting a trade entirely. If you naively sum every report you'll book ghost fills. Collapsing to latest-version-per <code>venue_exec_id</code> first gives you the true final state to position from.</p><h4>The traps</h4><ul><li><strong><code>DISTINCT</code> can't choose.</strong> If two rows differ in any column, <code>DISTINCT</code> keeps both — it has no notion of \"which one wins.\" It deduplicates identical rows, not competing versions.</li><li><strong><code>RANK()</code> leaks.</strong> <code>RANK()</code> gives tied top rows the <em>same</em> rank, so <code>rank=1</code> can return two rows and your duplicate slips through. <code>ROW_NUMBER()</code> is always unique, so <code>rn=1</code> is exactly one row.</li><li><strong>Always add a deterministic tiebreaker</strong> (like <code>received_ts</code> or a monotonic id). Without it, ties resolve arbitrarily and your \"winner\" can flip between runs.</li></ul><div class=\"key\">For latest-wins dedup use <code>ROW_NUMBER()</code> (always unique) partitioned by the entity key, ordered newest-first <em>with a tiebreaker</em>, and keep <code>rn=1</code> — never <code>DISTINCT</code> (can't pick) or <code>RANK()</code> (ties leak duplicates).</div>"},
  "null-semantics": {"teach":"<p>At its core, <code>NULL</code> doesn't mean zero and it doesn't mean empty — it means <strong>\"unknown.\"</strong> And once you accept that, SQL's strange behavior around it stops being magic and becomes logic: <em>you can't compare against something you don't know.</em></p><h4>Three-valued logic</h4><p>Ordinary logic has two answers: TRUE and FALSE. SQL adds a third, <strong>UNKNOWN</strong>. Any comparison touching <code>NULL</code> produces UNKNOWN — not TRUE, not FALSE. So <code>qty = NULL</code> is UNKNOWN, and even <code>NULL = NULL</code> is UNKNOWN, because \"is one unknown thing equal to another unknown thing?\" is itself unknowable.</p><div class=\"analogy\"><p>Imagine two sealed boxes, each labeled only \"a number is inside.\" Ask \"do these contain the same number?\" The honest answer isn't yes or no — it's \"I can't tell.\" That's UNKNOWN. SQL refuses to guess.</p></div><h4>Why NULLs silently vanish</h4><p>Here's the part that bites: <code>WHERE</code> keeps a row only when its condition is <strong>TRUE</strong>. UNKNOWN is not TRUE, so rows that evaluate to UNKNOWN are <em>silently dropped</em>. Filter on a column that's sometimes NULL and those rows disappear with no error and no warning. The same is true for <code>JOIN</code> conditions — a NULL join key never matches anything, so unmatched records quietly fall out of an inner join.</p><h4>How aggregates treat NULL</h4><ul><li>Most aggregates (<code>SUM</code>, <code>AVG</code>) simply ignore NULLs.</li><li><code>COUNT(*)</code> counts <em>every</em> row, but <code>COUNT(col)</code> counts only rows where <code>col</code> is non-NULL — a subtle source of mismatched totals.</li></ul><h4>The two nastiest gotchas</h4><ul><li><strong><code>NOT IN</code> with a NULL in the list returns nothing.</strong> <code>x NOT IN (1, 2, NULL)</code> can never be TRUE — because \"x ≠ NULL\" is UNKNOWN — so the whole predicate collapses and your query returns zero rows. Use <code>NOT EXISTS</code> instead.</li><li><strong>Reconciliation breaks hide in NULLs.</strong> If a trade exists on one side but is missing (NULL) on the other, a naive <code>s.qty &lt;&gt; i.qty</code> evaluates to UNKNOWN and the row drops off your break report — masking exactly the discrepancy you were hunting.</li></ul><h4>The NULL-safe toolkit</h4><ul><li><code>IS NULL</code> / <code>IS NOT NULL</code> — the only correct way to test for NULL.</li><li><code>COALESCE(col, default)</code> — substitute a real value so comparisons behave.</li><li><code>IS NOT DISTINCT FROM</code> — NULL-safe equality where two NULLs <em>do</em> count as equal.</li></ul><p>That's why a break report writes <code>WHERE COALESCE(s.qty,0) &lt;&gt; COALESCE(i.qty,0)</code>: it turns a missing side into a concrete <code>0</code>, so a present-vs-missing pair becomes a genuine, visible break instead of vanishing.</p><div class=\"key\"><code>NULL</code> means \"unknown,\" so it compares as UNKNOWN and <code>WHERE</code>/<code>JOIN</code> silently drop those rows — guard every NULL-prone comparison with <code>COALESCE</code> or <code>IS [NOT] DISTINCT FROM</code>, especially in reconciliation where a dropped row is a hidden break.</div>"},
  "select-for-update": {"teach":"<p>At its core, <code>SELECT ... FOR UPDATE</code> is just <strong>putting a \"hands off, I'm working on this\" sign on a row</strong> while you read it, change it, and write it back — so another worker can't sneak in and overwrite your change.</p><h4>The bug it prevents: lost update</h4><p>Picture two fills for AAPL landing at the same instant. Both workers read <code>net_qty = 500</code>. Worker A adds 100 and writes 600. Worker B, who also read 500, adds 100 and writes 600. You applied <em>two</em> 100-share fills but the position only moved by 100. One update was silently lost. That's a real correctness bug — your books now disagree with the market.</p><div class=\"analogy\"><p>It's a shared whiteboard with the current count. Two people glance at \"500,\" each walk away, each come back and write their own \"+100\" result. Because they both copied the old number, the second person erases the first person's contribution. A lock is making the first person hold the marker until they're done, so the second person reads the <em>updated</em> 600 before adding.</p></div><h4>Isolation levels — the anomaly ladder</h4><p>Databases offer tunable <strong>isolation levels</strong>, each removing more concurrency anomalies (and costing more contention):</p><ul><li><strong>READ UNCOMMITTED</strong> — weakest; can see other transactions' uncommitted \"dirty\" data. (Postgres never actually does this — its floor is READ COMMITTED.)</li><li><strong>READ COMMITTED</strong> — only sees committed data, but the same query can return different results within one transaction.</li><li><strong>REPEATABLE READ</strong> — rows you've read won't change underneath you.</li><li><strong>SERIALIZABLE</strong> — strongest; transactions behave as if run one at a time.</li></ul><p>The anomalies they remove, in order, are <em>dirty reads</em>, then <em>non-repeatable reads</em>, then <em>phantoms</em>.</p><h4>The trap</h4><p>The common, costly mistake: assuming your default isolation protects you. Most engines default to <strong>READ COMMITTED</strong>, and that level does <em>not</em> stop lost updates — both transactions happily read the same committed 500. You need to either take an explicit row lock with <code>FOR UPDATE</code>, or run at <strong>SERIALIZABLE</strong> (which will abort one transaction so you retry). Hoping READ COMMITTED \"just works\" is how position ledgers drift.</p><h4>The pattern in practice</h4><p>Wrap the read-modify-write so the lock is held for the whole sequence:</p><ul><li><code>BEGIN;</code></li><li><code>SELECT net_qty FROM positions WHERE symbol='AAPL' FOR UPDATE;</code> — now the row is locked.</li><li><code>UPDATE positions SET net_qty = net_qty + 100 WHERE symbol='AAPL';</code></li><li><code>COMMIT;</code> — lock released, next worker proceeds on fresh data.</li></ul><p>This is essential in <strong>position-keeping ledgers</strong> and <strong>settlement-instruction state machines</strong>, where two concurrent updates to the same row must serialize or the result is simply wrong.</p><div class=\"key\">Default READ COMMITTED does <em>not</em> prevent lost updates — for any read-modify-write on shared state (positions, settlement state), take an explicit <code>SELECT ... FOR UPDATE</code> row lock inside a transaction, or run SERIALIZABLE and retry on conflict.</div>"},
  "prediction-market": {"teach":"<p>At its core, a prediction market contract is just <strong>a bet on whether something will happen, packaged as a tradable security</strong>. You buy a \"YES\" or \"NO\" share, and when the real-world event resolves, your share is worth either a full dollar or nothing.</p><h4>The price <em>is</em> a probability</h4><p>Each binary contract trades between <strong>$0.00 and $1.00</strong>. That price is the market's collective estimate of the probability the event happens. A contract at <code>$0.62</code> means the crowd thinks there's roughly a 62% chance. At resolution the math is brutally simple:</p><ul><li>Event <strong>occurred</strong> → contract settles to <strong>$1.00</strong>.</li><li>Event <strong>did not occur</strong> → contract settles to <strong>$0.00</strong>.</li></ul><div class=\"analogy\"><p>It's a raffle ticket whose price floats. If a ticket is currently selling for 62 cents, the market is saying \"this ticket has about a 62% shot at the $1 prize.\" When the draw happens, every winning ticket is redeemed for exactly $1 and every losing ticket becomes a worthless scrap. There's no in-between at the end — only $1 or $0.</p></div><h4>The two big venues (as of 2026)</h4><ul><li><strong>Kalshi</strong> — a CFTC-regulated Designated Contract Market (DCM), designated in 2020. Fully inside the US regulatory perimeter.</li><li><strong>Polymarket</strong> — crypto-native, settling in USDC on Polygon. It became a CFTC-regulated US exchange via an <em>Amended Order of Designation on Nov 25, 2025</em>, after acquiring QCX/QCEX. The CFTC then issued a <em>Prediction Markets Advisory on Feb 25, 2026</em>, asserting authority over trading abuses on these DCMs.</li></ul><h4>Why settlement is weird — and risky to automate</h4><p>Unlike a stock (priced continuously by supply/demand) or a crypto token, a prediction contract settles to an <strong>event resolution</strong>. Some authority or oracle has to declare \"yes, it happened.\" That introduces failure modes equities never have: <strong>oracle/resolution risk</strong>, <strong>disputed outcomes</strong>, and the chance the same event is priced differently on Kalshi vs Polymarket — a genuine <strong>cross-platform arbitrage</strong>. Each of these is a distinct reconciliation and settlement-automation headache.</p><h4>The trap that warps your P&L</h4><p>The seductive mistake is marking an open position at its <strong>last trade price</strong> as though that were money in the bank. It isn't. Until the event resolves, every cent of that gain is <strong>unrealized</strong> — the price can still swing, and the final payout is only ever $1 or $0. Your P&L doesn't <em>crystallize</em> until resolution. A YES bought at <code>$0.62</code> realizes <code>+$0.38</code> if the event occurs, or <code>-$0.62</code> if it doesn't — and not a moment before.</p><div class=\"key\">A prediction contract's price is an implied probability that settles to exactly $1 or $0 at <em>event resolution</em>, not at last trade — so mark open positions as unrealized, and respect the oracle, dispute, and cross-venue arbitrage risks that make these unlike equities or crypto.</div>"},
  "fix-dropcopy": {"teach":"<p>At its core, a FIX drop-copy feed is just <strong>a read-only firehose of every trade your firm does</strong>, pushed to you live so you can capture and reconcile them. You can't send orders down it — you only listen — and what you hear is the authoritative record of executions.</p><h4>What FIX actually is</h4><p><strong>FIX</strong> (Financial Information eXchange) is a messaging protocol built from simple <code>tag=value</code> pairs strung together, carried over a session that numbers every message with a <em>sequence number</em>. A couple of message types you'll meet constantly:</p><ul><li><code>35=D</code> — a <strong>NewOrderSingle</strong> (placing an order).</li><li><code>35=8</code> — an <strong>ExecutionReport</strong> (what happened to an order: filled, corrected, cancelled).</li></ul><div class=\"analogy\"><p>Think of a security camera feed of your own trading floor. You can't reach through the screen and move anyone (read-only), but you see everything that happens, time-stamped and in order. If a frame is missing, you can ask for that segment again — that's the FIX <code>ResendRequest (35=2)</code> recovering a sequence gap.</p></div><h4>The execution types that matter</h4><p>A drop-copy session streams <code>35=8</code> reports, and the <strong>ExecType</strong> tag tells you what each one is:</p><ul><li><strong>Trade (<code>150=F</code>)</strong> — a normal fill.</li><li><strong>Trade Correction (<code>150=G</code>)</strong> — amends a prior execution (e.g. fixes a price or quantity).</li><li><strong>Trade Cancel (<code>150=H</code>)</strong> — busts a prior execution entirely.</li></ul><h4>Why this is the standard recon source</h4><p>Drop-copy is the canonical trade-capture feed because it's independent of your order-entry path — a clean second observation to reconcile your own books against. But it carries a sharp edge.</p><h4>The trap: it's not append-only</h4><p>The seductive error is treating those <code>35=8</code> reports as an immutable log you can just sum up. They're <em>not</em>. A <code>150=G</code> correction or <code>150=H</code> cancel <strong>revises or removes</strong> an earlier execution. Naively adding every report double-counts corrected trades and keeps cancelled ones. You must first <strong>collapse to the latest version per execution</strong> — typically <code>ROW_NUMBER() OVER (PARTITION BY venue_exec_id ORDER BY version DESC)</code> keeping <code>rn=1</code> — <em>before</em> computing positions.</p><h4>Automation realities</h4><p>FIX drop-copy isn't only an equities thing — even crypto-derivatives venues expose it (Coinbase Derivatives, Zero Hash), while spot crypto and prediction venues lean on REST + WebSocket instead. Whichever transport you automate against, you need the same hygiene: <strong>idempotency</strong> via client order IDs (so a retry doesn't double-book), <strong>rate-limit handling</strong>, <strong>retries with backoff</strong>, and real <strong>error handling</strong>.</p><div class=\"key\">A drop-copy feed is a read-only stream of <code>35=8</code> ExecutionReports — but corrections (<code>150=G</code>) and cancels (<code>150=H</code>) amend prior trades, so collapse to latest-version-per-execution before you ever sum a position.</div>"}
});
