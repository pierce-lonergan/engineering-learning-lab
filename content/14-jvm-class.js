/* Class Loading & Linking (JVM-CLASS) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"JVM-CLASS","front":"The three phases of a class's lifecycle in the JVM","back":"<strong>Loading</strong> (find the bytecode and create the <code>Class</code> object) → <strong>Linking</strong> (verification, preparation, resolution) → <strong>Initialization</strong> (run <code>&lt;clinit&gt;</code> — static initializers and static field assignments). The JLS guarantees this order, but resolution within linking may be lazy, and initialization is deferred until first <em>active use</em>."},
  {"sec":"JVM-CLASS","front":"What happens during the LOADING phase specifically?","back":"The classloader obtains the binary representation (the <code>.class</code> bytes) for a fully-qualified name, parses it into the method area / metaspace, and creates the corresponding <code>java.lang.Class</code> instance. Loading does <em>not</em> run any code from the class. Array classes are special: they are synthesized directly by the JVM, not loaded from bytes by a loader."},
  {"sec":"JVM-CLASS","front":"What are the three sub-steps of LINKING, in order?","back":"<ul><li><strong>Verification</strong> — prove the bytecode is structurally well-formed and type-safe (no operand-stack over/underflow, valid branch targets, well-typed instructions).</li><li><strong>Preparation</strong> — allocate static fields and set them to <em>default</em> zero values (<code>0</code>, <code>0.0</code>, <code>false</code>, <code>null</code>); no user code runs.</li><li><strong>Resolution</strong> — replace symbolic references in the constant pool with direct references; may be lazy.</li></ul>"},
  {"sec":"JVM-CLASS","front":"Preparation vs initialization: what value does a static field hold after each?","back":"After <strong>preparation</strong> a static field holds its type's <em>default</em> value (<code>0</code>/<code>false</code>/<code>null</code>) — even <code>static int x = 5;</code> is <code>0</code> at this point. The assignment to <code>5</code> happens later in <strong>initialization</strong>, inside <code>&lt;clinit&gt;</code>. One exception: a <code>static final</code> compile-time constant (a primitive or <code>String</code> constant expression) is given its value during preparation, not via <code>&lt;clinit&gt;</code>."},
  {"sec":"JVM-CLASS","front":"What triggers INITIALIZATION (first \"active use\") of a class?","back":"Per JLS 12.4.1: instantiating it (<code>new</code>), invoking a declared static method, accessing or assigning a non-constant static field <em>declared in that class</em>, reflection (<code>Class.forName</code> with init), or initializing a subclass (forces the superclass first). <em>Not</em> triggers: reading a <code>static final</code> compile-time constant, accessing a superclass-declared static field <em>via</em> a subclass name, using a <code>.class</code> literal, or array allocation of the type."},
  {"sec":"JVM-CLASS","front":"What is the JVM classloader hierarchy (modern, Java 9+)?","back":"<strong>Bootstrap</strong> (native, loads core <code>java.base</code> modules; its parent is conceptually <code>null</code>) → <strong>Platform</strong> classloader (replaced the old \"extension\" loader; loads platform/JDK modules) → <strong>Application/System</strong> classloader (loads your classpath/module path). Each user loader also has a parent reference forming a delegation chain up to bootstrap."},
  {"sec":"JVM-CLASS","front":"Explain PARENT-DELEGATION and the security property it provides","back":"Before loading a class itself, a loader first asks its <strong>parent</strong> (recursively up to bootstrap) to load it; the loader only attempts its own search if every ancestor fails. This means core types like <code>java.lang.String</code> are always loaded by bootstrap, so a malicious classpath JAR cannot <em>spoof</em> or shadow JDK classes. It also avoids loading the same core class twice."},
  {"sec":"JVM-CLASS","front":"ClassNotFoundException vs NoClassDefFoundError — the precise distinction","back":"<code>ClassNotFoundException</code> (checked <code>Exception</code>) is thrown when you ask for a class <em>by name at runtime</em> — <code>Class.forName</code>, <code>loadClass</code>, <code>ClassLoader.findClass</code> — and the bytes aren't found. <code>NoClassDefFoundError</code> (a <code>LinkageError</code>, i.e. an <code>Error</code>) is thrown when the class was present at <em>compile</em> time and referenced via normal linkage (e.g. <code>new Foo()</code>), but is missing or failed to load/initialize at runtime."},
  {"sec":"JVM-CLASS","front":"A class's static initializer threw once; later code touches the class again. What happens?","back":"You get <code>NoClassDefFoundError</code>, not the original exception. If <code>&lt;clinit&gt;</code> throws, the JVM wraps it in <code>ExceptionInInitializerError</code> on the first attempt and marks the class <em>erroneous/failed</em>. Every subsequent attempt to use that class throws <code>NoClassDefFoundError: Could not initialize class X</code> — a classic source of confusing downstream errors that hides the real root cause (the first failure)."},
  {"sec":"JVM-CLASS","front":"Is class initialization thread-safe? What guarantees it?","back":"Yes. The JVM holds a per-class initialization lock so <code>&lt;clinit&gt;</code> runs <strong>exactly once</strong>; concurrent threads block until the initializing thread finishes. This is precisely why the <em>initialization-on-demand holder idiom</em> (a private static holder class) is a correct, lock-free lazy singleton — the classloader provides the synchronization for free."},
  {"sec":"JVM-CLASS","front":"Order of execution among static fields and static blocks in one class","back":"Static field initializers and <code>static { }</code> blocks run in <strong>textual (source) order</strong>, all merged into a single synthetic <code>&lt;clinit&gt;</code> method by the compiler. A static field referenced before its own initializer line runs will still read the default/zero value — a subtle forward-reference bug."},
  {"sec":"JVM-CLASS","front":"Static-init vs instance-init ordering across a superclass/subclass hierarchy","back":"On the first <code>new Sub()</code>: static init runs once — super <code>&lt;clinit&gt;</code> → sub <code>&lt;clinit&gt;</code>. Then per instance, each constructor first calls <code>super(...)</code>, and only after the super chain returns does that class run its own instance-field initializers + instance blocks (textual order) followed by the rest of its constructor body. Net effect: super init code completes before sub init code; static runs once ever, instance order repeats per object."},
  {"sec":"JVM-CLASS","front":"What is a CLASSLOADER LEAK and why does it cause metaspace growth?","back":"A class is uniquely identified by <code>(fully-qualified name, defining classloader)</code>, and a <code>Class</code> object strongly references its loader (and vice-versa). If <em>any</em> live object pins a classloader — a static field, a <code>ThreadLocal</code>, a registered JDBC driver or shutdown hook holding a reference — the whole loader and <em>all</em> its loaded classes can't be GC'd. On repeated redeploys this leaks metaspace until <code>OutOfMemoryError: Metaspace</code>."},
  {"sec":"JVM-CLASS","front":"Why is the classloader leak \"the classic app-server leak\"? Name common pins","back":"App servers (Tomcat, JBoss) give each web app its own classloader and discard it on redeploy. Common things that pin the old loader: a library's <code>static</code> cache keyed by app classes, an un-removed <code>ThreadLocal</code> on a pooled (container-owned) thread, a JDBC <code>Driver</code> still registered in <code>DriverManager</code>, custom log appenders, or a thread spawned by the app that never stops. Each redeploy leaks one full loader → metaspace OOM."},
  {"sec":"JVM-CLASS","front":"Can the same .class be loaded twice as two distinct types? When does that break?","back":"Yes — two different classloaders defining the same bytes produce two <em>distinct</em> runtime classes with separate identity. Casting an instance from loader A to the same-named type from loader B throws <code>ClassCastException</code> (\"X cannot be cast to X\"), and they have separate static state. This is intentional isolation (plugins, web apps) but a common gotcha when objects cross loader boundaries."},
  {"sec":"JVM-CLASS","front":"Does loading a class also initialize it? What does Class.forName control?","back":"No — loading/linking and initialization are separate. <code>Class.forName(\"Foo\")</code> (one-arg) <em>does</em> initialize <code>Foo</code>, but <code>Class.forName(\"Foo\", false, loader)</code> loads and links it while the <code>false</code> flag <em>skips</em> initialization — its static blocks have not run. A plain <code>Foo.class</code> literal neither initializes nor (necessarily) triggers any of <code>Foo</code>'s code."}
);

Object.assign(GLOSSARY_DATA, {
  "class-lifecycle": {"term":"Class Loading Lifecycle (Loading → Linking → Initialization)","group":"JVM-CLASS","aliases":["class lifecycle","loading linking initialization","jvm class lifecycle"],"eli5":"A class goes through find-it, wire-it-up, then run-its-setup-code — and the last step is delayed until you actually use the class.","memory":"\"Load it, Link it, Light it up\" — and the lights stay off until first active use.","full":"<p>The JVM brings a type to life in three JLS-mandated phases. <strong>Loading</strong> finds the bytecode and builds the <code>Class</code> object. <strong>Linking</strong> has three sub-steps — verification (type-safety proof), preparation (static fields get default zero values), and resolution (symbolic constant-pool refs become direct refs, often lazily). <strong>Initialization</strong> runs <code>&lt;clinit&gt;</code>: static initializers and static assignments, exactly once, thread-safely.</p><p>The key timing nuance: loading and linking can be eager or lazy depending on the JVM, but <em>initialization</em> is strictly deferred until the first <em>active use</em> of the class. So a class can be loaded and linked long before any of its code ever runs.</p>","trap":"Interviewers love asking what value <code>static int x = 5;</code> holds between preparation and initialization. Answer: <code>0</code>. Preparation only assigns defaults; the <code>5</code> is applied in <code>&lt;clinit&gt;</code> during initialization.","worked":"<code>Class.forName(\"Foo\", false, loader)</code> loads and links <code>Foo</code> but the <code>false</code> flag <em>skips</em> initialization — <code>Foo</code>'s static blocks have not run yet.","related":["linking-phase","initialization","active-use"]},
  "linking-phase": {"term":"Linking (Verification, Preparation, Resolution)","group":"JVM-CLASS","aliases":["verification","preparation","resolution","link phase"],"eli5":"Three steps that check the bytecode is safe, reserve space for static fields at zero, and turn name-references into real pointers.","memory":"V-P-R = \"Verify, Prep (zeros), Resolve (names→pointers).\"","full":"<p><strong>Verification</strong> proves the bytecode obeys the JVM's structural and type rules — no operand-stack over/underflow, valid branch targets, well-typed instructions, consistent stack maps. It is the JVM's first line of defense against malformed or hostile class files. <strong>Preparation</strong> allocates memory for static fields and sets each to its type default (<code>0</code>, <code>0.0</code>, <code>false</code>, <code>null</code>) — no initializer code runs (a <code>static final</code> constant-expression is the one case where the real value is set here). <strong>Resolution</strong> rewrites symbolic references in the constant pool (a method/field/class named by string) into direct references.</p><p>Resolution is permitted to be <em>lazy</em>: the JVM may resolve a constant-pool entry only when the instruction that uses it first executes, which spreads out (or avoids) the cost of resolving never-used references. A failed resolution surfaces as a <code>LinkageError</code> subtype, not a checked exception.</p>","trap":"Candidates conflate preparation with initialization. Preparation = defaults only, no user code; initialization = your declared values and static blocks. A failed resolution surfaces as a <code>LinkageError</code> subtype (e.g. <code>NoClassDefFoundError</code>), not a checked exception.","related":["class-lifecycle","initialization","cnfe-vs-ncdfe"]},
  "initialization": {"term":"Class Initialization (&lt;clinit&gt;)","group":"JVM-CLASS","aliases":["clinit","static initialization","class init"],"eli5":"The one-time run of all the static setup code, fired the first time you really touch the class.","memory":"<code>&lt;clinit&gt;</code> = \"class-init,\" the static-sibling of <code>&lt;init&gt;</code> (the constructor). Class runs once; init runs per object.","full":"<p>Initialization executes the synthetic <code>&lt;clinit&gt;</code> method the compiler builds by concatenating, in source order, every static field initializer and every <code>static { }</code> block. It runs <strong>exactly once</strong> per class per defining loader, guarded by a per-class initialization lock so it is inherently thread-safe. Initializing a class first initializes its superclass — and any superinterface that declares a <em>default</em> method (plain interfaces with only constants/abstract methods are not initialized by a subtype's init).</p><p>It fires only on <em>active use</em> — <code>new</code>, a static method call, a non-constant static field access on the declaring class, reflective init, or subclass init. If <code>&lt;clinit&gt;</code> throws, the class is marked erroneous and all later uses throw <code>NoClassDefFoundError</code>.</p>","trap":"\"Is the holder-idiom singleton thread-safe without locks?\" Yes — because the JVM serializes <code>&lt;clinit&gt;</code> with the initialization lock, lazy init of the holder class is correct and lock-free.","worked":"<code>static final int MAX = 100;</code> is a compile-time constant — reading <code>Foo.MAX</code> does <em>not</em> initialize <code>Foo</code> (the value is inlined at the call site). Change it to <code>static final int MAX = compute();</code> and now reading it <em>does</em> trigger init.","related":["class-lifecycle","active-use"]},
  "active-use": {"term":"Active Use (Initialization Trigger)","group":"JVM-CLASS","aliases":["first active use","initialization trigger","lazy initialization"],"eli5":"The specific list of things you can do to a class that finally make its static setup code run.","memory":"\"You must <em>actively touch</em> live code or live state.\" Constants and <code>.class</code> literals are inert.","full":"<p>JLS 12.4.1 enumerates the <em>active uses</em> that trigger initialization: creating an instance, invoking a declared static method, accessing or assigning a static field that is <em>not</em> a compile-time constant <em>and is declared in that class</em>, reflective initialization (<code>Class.forName</code> with the init flag), and initializing a subclass (which first initializes the superclass). Everything else is <em>passive</em> and does not trigger init.</p><p>Classic passive uses that fool people: reading a <code>static final</code> compile-time constant (inlined), referencing a static field through a subclass when the field is declared in the superclass (only the <em>declaring</em> class initializes), evaluating a <code>Foo.class</code> literal, and allocating an array of the type (<code>new Foo[10]</code> does not init <code>Foo</code>).</p>","trap":"<code>new Foo[10]</code> does NOT initialize <code>Foo</code> — array creation needs the element type loaded, not initialized. A frequent trick question.","related":["initialization","class-lifecycle"]},
  "parent-delegation": {"term":"Parent-Delegation Model","group":"JVM-CLASS","aliases":["delegation model","parent delegation","classloader delegation"],"eli5":"A classloader asks its parent to load a class first, only loading it itself if every ancestor gives up.","memory":"\"Ask your parents first.\" Override <code>findClass</code> to stay polite; override <code>loadClass</code> to rebel (parent-last).","full":"<p>When asked to load a class, a loader's <code>loadClass</code> first checks if it's already loaded, then <em>delegates upward</em> to its parent before trying <code>findClass</code> itself. The chain runs Application → Platform → Bootstrap, so core JDK types are always defined by the bootstrap loader regardless of who first requested them. This gives two properties: <strong>uniqueness</strong> (a core class is loaded once, by the highest loader that can) and <strong>safety</strong> (a classpath JAR can't substitute its own <code>java.lang.String</code> to spoof core behavior).</p><p>You break delegation by overriding <code>loadClass</code> instead of <code>findClass</code>. Some frameworks deliberately invert it — OSGi and web-app loaders search locally <em>first</em> for app/plugin classes — but they still delegate core <code>java.*</code> packages to the parent to preserve safety.</p>","trap":"\"How do you make a custom loader without breaking delegation?\" Override <code>findClass</code>, not <code>loadClass</code> — the default <code>loadClass</code> already implements the parent-first delegation algorithm and calls your <code>findClass</code> only as a last resort.","worked":"Tomcat's <code>WebappClassLoader</code> is intentionally parent-<em>last</em> for app classes (so each webapp sees its own library versions) but still parent-first for <code>java.*</code>/<code>javax.*</code> to prevent spoofing.","related":["classloader-hierarchy","classloader-identity","classloader-leak"]},
  "classloader-hierarchy": {"term":"Classloader Hierarchy (Bootstrap / Platform / Application)","group":"JVM-CLASS","aliases":["bootstrap classloader","platform classloader","application classloader","system classloader","extension classloader"],"eli5":"Three built-in loaders stacked from the native core loader up to the one that reads your classpath.","memory":"\"BPA from the bottom up\" — Bootstrap → Platform → Application, native to classpath.","full":"<p><strong>Bootstrap</strong> is native code (no Java <code>ClassLoader</code> object — it shows as <code>null</code> in <code>getClassLoader()</code>) and loads the core platform (<code>java.base</code> and friends). <strong>Platform</strong> (Java 9+, replacing the old <em>extension</em> loader and the <code>jre/lib/ext</code> mechanism) loads JDK platform modules. <strong>Application</strong>/system loader loads your application classpath/modules and is returned by <code>ClassLoader.getSystemClassLoader()</code>.</p><p>This is a <em>delegation</em> hierarchy by parent reference, not a Java inheritance hierarchy. Each loader holds a <code>parent</code> field; the chain terminates at bootstrap (<code>parent == null</code>). User-defined loaders normally sit below the application loader.</p>","trap":"Naming the obsolete \"extension classloader\" reveals a pre-Java-9 mental model. Since Java 9 it's the <strong>platform</strong> classloader, and <code>jre/lib/ext</code> is gone. Also: bootstrap returns <code>null</code>, not a loader object — checking for it requires a <code>null</code> guard.","related":["parent-delegation","classloader-identity","classloader-leak"]},
  "cnfe-vs-ncdfe": {"term":"ClassNotFoundException vs NoClassDefFoundError","group":"JVM-CLASS","aliases":["classnotfoundexception","noclassdeffounderror","exceptionininitializererror"],"eli5":"One is 'I asked for a class by name and it's missing'; the other is 'a class I already linked against vanished or failed to start at runtime.'","memory":"<code>ClassNotFoundException</code> = \"I went looking and didn't find it\" (dynamic, checked). <code>NoClassDefFoundError</code> = \"it was here at compile time and now it's broken\" (linkage, error).","full":"<p><code>ClassNotFoundException</code> is a checked <code>Exception</code> thrown by <em>explicit, by-name</em> loading — <code>Class.forName</code>, <code>ClassLoader.loadClass/findClass</code> — when the loader can't find the bytes. Your code chose to look the class up dynamically. <code>NoClassDefFoundError</code> is an unchecked <code>Error</code> (subtype of <code>LinkageError</code>) thrown when a class was present at <em>compile</em> time and referenced through normal linkage (<code>new</code>, a field of that type, an <code>extends</code>), but at runtime its definition is absent, on a different classpath, or <em>failed to initialize</em>.</p><p>The most deceptive form: if a class's <code>&lt;clinit&gt;</code> throws, the JVM wraps the cause in <code>ExceptionInInitializerError</code> on the first attempt and marks the class failed; every <em>subsequent</em> reference then throws <code>NoClassDefFoundError: Could not initialize class X</code> — far from the real cause.</p>","trap":"Seeing repeated <code>NoClassDefFoundError: Could not initialize class X</code> almost always means a static initializer failed <em>earlier</em>; the first occurrence (an <code>ExceptionInInitializerError</code>) holds the real root cause. Don't chase the <code>NoClassDefFoundError</code> — find the first failure.","worked":"A typo in a config read inside <code>static { CONFIG = load(); }</code> throws once as <code>ExceptionInInitializerError</code>; ten downstream call sites then all report <code>NoClassDefFoundError</code>, masking the single underlying bug.","related":["initialization","linking-phase"]},
  "classloader-leak": {"term":"Classloader Leak (Metaspace Growth)","group":"JVM-CLASS","aliases":["classloader leak","metaspace leak","perm gen leak","redeploy leak"],"eli5":"Something keeps a pointer to a discarded classloader, so it and all its classes can never be freed, and the metadata area keeps growing.","memory":"\"One stray pointer pins a whole universe of classes.\" The leak is a <em>loader</em> leak, measured in metaspace, not heap.","full":"<p>Classes live in metaspace (PermGen pre-Java-8) and are reclaimed only when their <em>entire defining classloader</em> becomes unreachable — because a class strongly references its loader and the loader references all its classes. App servers create a fresh loader per deployment and drop it on redeploy, expecting GC to reclaim the old classes. A <strong>leak</strong> occurs when any live root still pins the old loader: a third-party <code>static</code> cache holding an app object, a <code>ThreadLocal</code> left set on a container-owned (pooled) thread, a JDBC <code>Driver</code> still registered in <code>DriverManager</code>, a custom log appender, or an app-spawned thread that never stops.</p><p>Each redeploy then leaks one full loader's worth of classes, and metaspace climbs until <code>OutOfMemoryError: Metaspace</code>. Diagnosis: a heap dump showing multiple instances of the webapp classloader, or duplicate copies of the same class name; the fix is breaking the GC root (deregister drivers, clear <code>ThreadLocal</code>s, stop threads on undeploy).</p>","trap":"\"Why didn't capping <code>-Xmx</code> stop the redeploy OOM?\" Because class metadata lives in <strong>metaspace</strong>, sized by <code>-XX:MaxMetaspaceSize</code>, not the Java heap. The symptom is <code>OutOfMemoryError: Metaspace</code>, and heap tuning won't touch it.","worked":"A driver loaded by a webapp registers itself in the bootstrap-level <code>DriverManager</code>; that static registry outlives the webapp, pinning its loader. The fix is <code>DriverManager.deregisterDriver()</code> in a <code>ServletContextListener</code> on shutdown.","related":["classloader-identity","classloader-hierarchy","parent-delegation"]},
  "classloader-identity": {"term":"Classloader-Based Type Identity","group":"JVM-CLASS","aliases":["class identity","defining classloader","classcastexception same class","runtime class identity"],"eli5":"A class's true identity is its name PLUS the loader that defined it, so the same bytes loaded by two loaders are two different types.","memory":"\"Name + loader = identity.\" Same name, different loader = different class. Hence <code>Foo</code> ≠ <code>Foo</code>.","full":"<p>The JVM identifies a runtime class by the pair <code>(fully-qualified name, defining classloader)</code>, not by name alone. Two distinct loaders that each define the same <code>.class</code> bytes produce two <em>separate</em> runtime classes: separate <code>Class</code> objects, separate static state, and incompatible identity. Assigning or casting an instance of one to the same-named type from the other throws <code>ClassCastException</code> with the infamous \"<code>com.x.Foo cannot be cast to com.x.Foo</code>\" message.</p><p>This is the mechanism that gives plugin systems, app servers, and OSGi their isolation — each module's classes are namespaced by their loader. It also underlies why classloader leaks are per-loader and why crossing objects between loaders requires shared parent-loaded interfaces (so both sides agree on the type's identity).</p>","trap":"A <code>ClassCastException</code> where both types print the identical name is the tell-tale sign of two classloaders defining the same class — not a logic bug. The fix is to load the shared type from a <em>common parent</em> loader so both sides see one identity.","worked":"A plugin and the host both bundle <code>lib-v2.jar</code>; an object created in the plugin's loader is passed to host code typed against the host's copy → <code>ClassCastException: Widget cannot be cast to Widget</code>. Promoting <code>Widget</code> to a shared parent classpath resolves it.","related":["parent-delegation","classloader-hierarchy","classloader-leak"]}
});
