/* Catalyst Optimizer & Tungsten (SPARK-CATALYST) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"SPARK-CATALYST","front":"What are the four stages of the Catalyst query-compilation pipeline?","back":"<ul><li><strong>Unresolved logical plan</strong> — parsed tree of operators with column/table refs not yet bound.</li><li><strong>Analyzed logical plan</strong> — the analyzer resolves references against the catalog, assigns types, and validates.</li><li><strong>Optimized logical plan</strong> — rule-based rewrites (pushdown, constant folding, etc.); cost-based steps (e.g. join reordering) also run here when stats exist.</li><li><strong>Physical plan</strong> — one or more <code>SparkPlan</code> trees; the cheapest is selected, then refined by preparation rules (codegen collapse, exchange insertion).</li></ul>"},
  {"sec":"SPARK-CATALYST","front":"What does the Catalyst analyzer do that distinguishes an unresolved plan from an analyzed plan?","back":"The analyzer walks the unresolved tree and binds <code>UnresolvedAttribute</code>/<code>UnresolvedRelation</code> nodes against the session catalog: resolving column names to specific relations, inferring and coercing data types, expanding <code>*</code>, resolving function names, and validating that everything type-checks. Output is a fully typed, resolved <code>LogicalPlan</code>. Failures here surface as <code>AnalysisException</code> before any optimization or execution."},
  {"sec":"SPARK-CATALYST","front":"Predicate pushdown vs. projection pushdown in Catalyst — what does each save, and where does it run?","back":"<strong>Predicate pushdown</strong> moves filter conditions as close to the scan as possible, ideally into the data source (Parquet row-group/page skipping, JDBC <code>WHERE</code>), reducing rows read. <strong>Projection (column) pushdown</strong> prunes unneeded columns so only referenced fields are read — huge for columnar formats like Parquet/ORC. Both are rule-based logical rewrites; the actual skipping happens at the scan when a DataSource V2 source implements <code>SupportsPushDownFilters</code>/<code>SupportsPushDownRequiredColumns</code>."},
  {"sec":"SPARK-CATALYST","front":"What is constant folding, and name two related rule-based simplifications Catalyst applies.","back":"Constant folding pre-evaluates deterministic constant expressions at compile time, e.g. <code>2 + 3</code> → <code>5</code> or <code>WHERE 1=1</code> → drop. Related rules: <strong>boolean simplification</strong> (<code>x AND true</code> → <code>x</code>), <strong>null propagation</strong> (e.g. <code>x = NULL</code> folds to a null literal, which a filter treats as not-true under three-valued logic), and <strong>combine/collapse</strong> of adjacent filters and projects. These are deterministic-only — a non-deterministic expression like <code>rand()</code> is excluded from folding and from reordering below operators."},
  {"sec":"SPARK-CATALYST","front":"How does Catalyst choose among multiple physical plans, and which decision is cost-based?","back":"For an optimized logical operator Catalyst can emit several physical strategies (e.g. broadcast hash join vs. sort-merge join). A <strong>cost model</strong> driven by table/column statistics picks among them. The most consequential cost-based decision is <strong>join strategy selection</strong> — broadcasting the smaller side when its estimated size is under <code>spark.sql.autoBroadcastJoinThreshold</code> (default 10 MB). AQE can revise this at runtime using actual shuffle sizes."},
  {"sec":"SPARK-CATALYST","front":"What is whole-stage code generation (WSCG) and what problem does it solve?","back":"WSCG fuses a chain of codegen-capable physical operators within a stage into a <strong>single generated JVM function</strong> (a <code>Janino</code>-compiled class) rather than chaining iterator <code>next()</code> calls per operator. It eliminates virtual-call dispatch and intermediate row materialization between operators, keeps hot data in CPU registers, and lets the JIT inline tight loops — collapsing the classic Volcano iterator model into hand-written-quality code. It is on by default."},
  {"sec":"SPARK-CATALYST","front":"The Volcano iterator model vs. whole-stage codegen — what's the core performance difference?","back":"The classic <strong>Volcano (pull) model</strong> calls a virtual <code>next()</code> on each operator per row, materializing a row object at every boundary — millions of polymorphic calls and poor CPU-cache/register behavior. WSCG generates one straight-line function for the whole stage, so a row flows through filter/project/aggregate as local-variable updates with no per-operator virtual dispatch or intermediate object allocation. This is Tungsten's answer to being CPU/memory-bound rather than I/O-bound on modern hardware."},
  {"sec":"SPARK-CATALYST","front":"Why does Tungsten store data off-heap in a binary format instead of as JVM objects?","back":"JVM objects carry header overhead, padding, and pointer-chasing, and they pressure the garbage collector. Tungsten's <code>UnsafeRow</code> packs a row into a contiguous binary blob (fixed-length region + variable-length region) in off-heap or on-heap memory managed via <code>sun.misc.Unsafe</code>. This slashes memory footprint, makes serialization close to a memcpy, and removes most GC pressure from Spark's execution memory."},
  {"sec":"SPARK-CATALYST","front":"What makes Tungsten's computation 'cache-aware', and give one concrete technique.","back":"Tungsten lays out data to respect CPU cache lines and minimize random memory access. Concrete example: <strong>cache-aware sorting</strong> stores the sort key (a prefix) inline alongside a pointer in a contiguous array, so comparisons read keys sequentially without dereferencing scattered records for most comparisons — cutting L1/L2 cache misses versus comparing full deserialized objects. The binary <code>UnsafeRow</code> format and columnar in-memory caching reinforce the same locality goal."},
  {"sec":"SPARK-CATALYST","front":"In what sense is any UDF (Scala or Python) a 'black box' to Catalyst's logical optimizer?","back":"Catalyst optimizes by reasoning over expression trees, but a UDF wraps opaque user code: the optimizer cannot inspect its body, so it cannot determine which columns it reads or what predicates it implies. Consequently it <strong>cannot push filters through or below the UDF, cannot prune columns it might touch, and cannot constant-fold it</strong> — for both Scala and Python UDFs alike. Prefer built-in SQL expressions, which Catalyst understands and can reorder, push down, and optimize natively."},
  {"sec":"SPARK-CATALYST","front":"Does a Scala UDF break whole-stage codegen the way a Python UDF does?","back":"No. A <code>ScalaUDF</code> runs in the JVM and implements <code>doGenCode</code>, so it <strong>is fused into</strong> the whole-stage-generated function (the call is inlined) — it does not split the stage. It remains a logical-optimizer black box (no pushdown/pruning/folding), but it is not a WSCG barrier. A <strong>Python UDF</strong> is different: it executes in a separate Python worker (<code>BatchEvalPythonExec</code>/<code>ArrowEvalPythonExec</code>), which does break out of whole-stage codegen."},
  {"sec":"SPARK-CATALYST","front":"What extra penalty does a plain Python UDF incur beyond being a Catalyst black box?","back":"A plain (row-at-a-time) Python UDF runs in a separate Python worker process, so each row is <strong>serialized from the JVM, piped to Python, deserialized, executed, then serialized back</strong> — per-row cross-process overhead, and it breaks out of whole-stage codegen. <strong>Pandas/vectorized UDFs (Arrow-based)</strong> transfer column batches via Apache Arrow, amortizing serialization and enabling vectorized execution, but they remain Catalyst-opaque for pushdown/folding."},
  {"sec":"SPARK-CATALYST","front":"If you put a UDF in a WHERE clause, what happens to predicate pushdown?","back":"Catalyst can't push the UDF predicate into the data source — the scan reads more data than necessary and the UDF filter is applied later in the plan. A UDF predicate sitting above a join can also prevent that condition from being pushed below the join. Rewriting the condition with native expressions (or filtering on raw columns before invoking the UDF) restores pushdown."},
  {"sec":"SPARK-CATALYST","front":"Where does Adaptive Query Execution (AQE) sit relative to Catalyst's static optimization?","back":"Catalyst's rule- and cost-based optimization happens at <strong>compile time</strong> on estimated statistics. <strong>AQE</strong> (<code>spark.sql.adaptive.enabled</code>, default true since Spark 3.2) re-optimizes at <strong>runtime</strong> using actual shuffle-stage statistics: it coalesces shuffle partitions, can switch sort-merge joins to broadcast joins, and dynamically handles skew. It corrects exactly the bad estimates that static cost-based optimization is prone to. It only kicks in at shuffle/stage boundaries."},
  {"sec":"SPARK-CATALYST","front":"What does the InternalRow / UnsafeRow abstraction give Spark SQL over an RDD of objects?","back":"Spark SQL operators consume and produce <code>InternalRow</code>, whose Tungsten implementation is <code>UnsafeRow</code> — a binary record with O(1) field access by offset and no per-row deserialization. Versus an RDD of case-class objects, this means: no JVM object per row, generated code reads fields directly from the byte buffer, shuffles move bytes rather than re-serialized objects, and the optimizer/encoders know the schema. Encoders bridge typed Datasets to this binary format."},
  {"sec":"SPARK-CATALYST","front":"How can you inspect whether Catalyst optimized your query the way you expect?","back":"Call <code>df.explain(true)</code> (or <code>EXPLAIN FORMATTED</code> in SQL) to print the parsed, analyzed, optimized logical, and selected physical plans. In the physical plan, operators marked <code>*(n)</code> with a leading <code>*</code> are whole-stage-codegen'd (<code>n</code> is the codegen stage id); <code>PushedFilters</code> and a reduced <code>ReadSchema</code> on the scan confirm predicate and projection pushdown reached the source."},
  {"sec":"SPARK-CATALYST","front":"Why are non-deterministic expressions specially handled by Catalyst optimization rules?","back":"Rules like constant folding, predicate pushdown, and subexpression elimination assume an expression yields the same value for the same input. Functions such as <code>rand()</code>, <code>monotonically_increasing_id()</code>, and <code>current_timestamp()</code> are flagged <code>deterministic = false</code> (and non-foldable), so Catalyst will <strong>not</strong> fold them, reorder them across operators, or push them below a filter/join — preserving evaluation semantics and how many times they run."}
);

Object.assign(GLOSSARY_DATA, {
  "catalyst-pipeline": {"term":"Catalyst Optimizer Pipeline","group":"SPARK-CATALYST","aliases":["catalyst","catalyst optimizer","query plan pipeline"],"eli5":"Spark turns your query into a tree, fixes up the names, rewrites it to be cheaper, then picks the fastest physical plan to actually run.","memory":"Unresolved-Analyzed-Optimized-Physical → 'UAOP'.","full":"<p>Catalyst is Spark SQL's extensible, tree-transformation-based query optimizer. A DataFrame/SQL query becomes an <strong>unresolved logical plan</strong>, which the analyzer resolves against the catalog into an <strong>analyzed logical plan</strong>. The logical optimizer then applies <strong>rule-based</strong> rewrites (predicate/projection pushdown, constant folding, boolean simplification, join reordering) to produce the <strong>optimized logical plan</strong>. Finally, planning strategies emit one or more <strong>physical plans</strong>, a cost model picks one, and physical preparation rules insert exchanges and collapse whole-stage codegen.</p><p>The framework is built on immutable <code>TreeNode</code> trees and pattern-matching rules, which is why it's easy to extend. Logical = <em>what</em> to compute; physical = <em>how</em> to compute it.</p>","trap":"Interviewers probe the boundary: predicate/projection pushdown and constant folding are LOGICAL rule-based steps; choosing broadcast-vs-sort-merge join is the PHYSICAL cost-based step. Mixing these up is the classic miss.","worked":"<code>spark.table('t').filter('a>1').select('a','b').explain(true)</code> prints all four plan stages; the optimized logical plan shows the filter/projection pushed toward the scan.","related":["rule-based-optimization","cost-based-optimization","whole-stage-codegen","predicate-pushdown"]},
  "rule-based-optimization": {"term":"Rule-Based Logical Optimization","group":"SPARK-CATALYST","aliases":["rbo","logical rewrites","operator optimization"],"eli5":"A fixed library of always-true rewrites that make the plan cheaper without needing any data statistics.","memory":"'Push it down, prune it out, fold it up' — the three core rule-based moves.","full":"<p>The logical optimizer runs batches of deterministic rewrite rules to a fixed point. Key rules: <strong>predicate pushdown</strong> (move filters toward the scan), <strong>column pruning</strong> (drop unread columns), <strong>constant folding</strong> (pre-evaluate constants), <strong>boolean/null simplification</strong>, <strong>combine filters</strong>, <strong>collapse projects</strong>, and limited <strong>join reordering</strong>. These transformations preserve query semantics and improve almost any plan, so they need no statistics.</p><p>Each rule is a tree-to-tree function applied via pattern matching, idempotently, until the plan stops changing. (Join reordering and similar can additionally consult stats when CBO is enabled.)</p>","trap":"Rule-based optimization never looks at row counts. If asked 'how does Spark decide to push a filter?', the answer is 'a deterministic rule, no stats' — not the cost model.","worked":"<code>WHERE 1=1 AND price > 10</code> → constant folding drops <code>1=1</code>; pushdown moves <code>price>10</code> into the Parquet scan as a <code>PushedFilter</code>.","related":["catalyst-pipeline","predicate-pushdown","cost-based-optimization"]},
  "cost-based-optimization": {"term":"Cost-Based Optimization (CBO)","group":"SPARK-CATALYST","aliases":["cbo","cost model","join selection"],"eli5":"When several physical plans would work, Spark uses table statistics to estimate which is cheapest — most importantly which join to use.","memory":"CBO = the 'which physical plan' chooser; its headline call is broadcast-vs-sort-merge join.","full":"<p>After rule-based rewrites, planning strategies can produce multiple physical plans for the same logical operator. The cost model uses statistics — collected via <code>ANALYZE TABLE ... COMPUTE STATISTICS</code> (and column stats) — to estimate cardinalities and pick the cheapest. The highest-impact CBO decision is <strong>join strategy</strong>: broadcast hash join when the smaller side is estimated under <code>spark.sql.autoBroadcastJoinThreshold</code> (default 10 MB), otherwise sort-merge join; CBO also informs join reordering.</p><p>Static CBO is only as good as its estimates, which degrade with stale stats, skew, or complex filters. <strong>AQE</strong> exists precisely to re-decide at runtime from actual shuffle sizes.</p>","trap":"CBO needs stats present and fresh — without <code>ANALYZE TABLE</code>, Spark falls back to size-based heuristics. Saying 'Catalyst always knows cardinalities' is wrong; it estimates, often badly, which is why AQE was added.","worked":"A 5 MB dimension joined to a 1 TB fact: CBO estimates the dim under 10 MB and picks <code>BroadcastHashJoin</code>, shipping the small side to every executor instead of shuffling 1 TB.","related":["catalyst-pipeline","adaptive-query-execution","rule-based-optimization"]},
  "unsafe-row": {"term":"UnsafeRow / Binary Row Format","group":"SPARK-CATALYST","aliases":["unsaferow","internalrow","binary row","off-heap row"],"eli5":"A row stored as one compact stretch of bytes — a null bitset and fixed-size slots up front, variable-size data after — so any field is reachable by offset with no object unpacking.","memory":"Null bitset + fixed region = the index; variable region = the warehouse. Pointers in the index point into the warehouse.","full":"<p><code>UnsafeRow</code> is Tungsten's implementation of <code>InternalRow</code>: a single contiguous, 8-byte-aligned byte region with a <strong>null bit set</strong> (one bit per field), a <strong>fixed-length region</strong> (8 bytes per field holding either the value for fixed-width types or an offset+length pointer for variable-width types), and a <strong>variable-length region</strong> for strings/arrays/maps. Field access is O(1) pointer arithmetic; no per-row deserialization, no JVM object per field.</p><p>This makes shuffles move raw bytes (serialization is close to a memcpy), lets generated code read fields straight from the buffer, and removes most GC pressure. <strong>Encoders</strong> translate between typed JVM objects (for Datasets) and this binary form; built-in expressions operate directly on it.</p>","trap":"Calling any UDF requires real JVM/Python objects, so the engine must materialize the value out of <code>UnsafeRow</code> to invoke it and re-encode the result — a hidden cost on top of the optimizer opacity.","worked":"A <code>(int, string)</code> row: null bitset, then 8 bytes for the int, then 8 bytes holding (offset,length) of the string, then the UTF-8 bytes appended in the variable region — all in one buffer.","related":["tungsten","whole-stage-codegen","udf-black-box"]},
  "whole-stage-codegen": {"term":"Whole-Stage Code Generation","group":"SPARK-CATALYST","aliases":["wscg","codegen","whole stage codegen","janino"],"eli5":"Instead of each operator calling the next one row by row, Spark writes one custom Java function for the whole stage and compiles it on the fly.","memory":"Volcano calls next() a billion times; WSCG writes one big loop. Star <code>*</code> in explain = 'this part got codegen'd'.","full":"<p>WSCG fuses chainable, codegen-capable physical operators inside a stage (scan → filter → project → partial aggregate, etc.) into a single generated Java method, compiled at runtime with <code>Janino</code>. It replaces the <strong>Volcano pull model</strong>'s per-row virtual <code>next()</code> calls with straight-line code where a row moves through operators as local-variable updates — no intermediate row objects, no megamorphic dispatch, registers stay hot, and the JIT can inline.</p><p>Not everything fuses: operators that consume their whole input (sorts, shuffles/exchanges) or that fall back to interpreted eval (those marked <code>CodegenFallback</code>) split the stage. A <strong>Python</strong> UDF breaks WSCG because it evaluates in a separate process (<code>BatchEvalPythonExec</code>); a <strong>Scala</strong> UDF (<code>ScalaUDF</code>) does NOT break WSCG — it implements <code>doGenCode</code> and is inlined. In <code>explain</code>, fused operators share a <code>*(n)</code> marker.</p>","trap":"Don't say 'any UDF breaks whole-stage codegen.' A Scala/Java <code>ScalaUDF</code> is codegen'd inline; only Python UDFs (separate-process eval) split the stage. All UDFs still block logical pushdown/pruning regardless.","worked":"<code>scan → filter → project</code> shows as one <code>*(1)</code> WholeStageCodegen node; inserting a Python UDF splits it with an <code>BatchEvalPythonExec</code>/serialization boundary, whereas a Scala UDF stays inside the fused stage.","related":["tungsten","udf-black-box","catalyst-pipeline","unsafe-row"]},
  "udf-black-box": {"term":"UDFs as Optimizer Black Boxes","group":"SPARK-CATALYST","aliases":["udf","user defined function","python udf","scala udf","pandas udf"],"eli5":"A UDF is code Spark can't see inside, so it can't rewrite or push down around it — and Python UDFs also pay to ship every row to a Python process and break codegen.","memory":"Built-ins are glass; UDFs are concrete. Scala UDF = opaque but inlined; Python UDF = opaque AND ships rows to another process.","full":"<p>Catalyst optimizes by reasoning about expression trees; a UDF is an opaque function. The optimizer therefore <strong>cannot</strong> push filters through it, prune columns it might read, constant-fold it, or include it in subexpression elimination — this opacity applies to Scala AND Python UDFs alike. A UDF in a <code>WHERE</code> clause defeats predicate pushdown to the source.</p><p>They differ on execution, though: a <strong>Scala/Java UDF</strong> runs in the JVM and is fused into whole-stage codegen (<code>ScalaUDF.doGenCode</code>), so it is opaque-but-codegen'd. A <strong>plain Python UDF</strong> additionally serializes each row to a separate Python worker and back (<code>BatchEvalPythonExec</code>), breaking WSCG. <strong>Pandas/vectorized UDFs</strong> use Apache Arrow to transfer batches, amortizing that overhead, but remain optimizer-opaque. Rule: prefer built-in SQL functions; reach for UDFs only when no native expression exists.</p>","trap":"'Rewrite the Python UDF in Scala' removes the cross-process serialization and the WSCG break, but NOT the optimizer opacity — a Scala UDF still blocks pushdown, pruning, and folding. Only native expressions are truly optimizable.","worked":"<code>df.filter(my_udf(col('x')))</code> reads the whole table; refactoring to <code>df.filter(col('x') > 0)</code> pushes the filter into Parquet and skips row groups.","related":["whole-stage-codegen","predicate-pushdown","tungsten","unsafe-row"]},
  "adaptive-query-execution": {"term":"Adaptive Query Execution (AQE)","group":"SPARK-CATALYST","aliases":["aqe","adaptive execution","runtime reoptimization"],"eli5":"Spark re-plans the query mid-flight using the real sizes of shuffle data instead of trusting compile-time guesses.","memory":"AQE = Catalyst with a feedback loop: 'plan, run a stage, look at the real numbers, re-plan'.","full":"<p>AQE (<code>spark.sql.adaptive.enabled</code>, on by default since Spark 3.2) re-optimizes the physical plan at runtime after a shuffle/query stage completes, using <strong>actual</strong> partition statistics rather than Catalyst's compile-time estimates. Its three headline features: <strong>coalescing shuffle partitions</strong> (merging many tiny post-shuffle partitions), <strong>dynamically switching joins</strong> (e.g. sort-merge → broadcast when a side turns out small), and <strong>skew join handling</strong> (splitting oversized partitions).</p><p>AQE complements Catalyst: static rule- and cost-based optimization happen at compile time on estimates; AQE corrects them with observed reality. It's the standard answer to 'how does Spark cope with bad cardinality estimates and skew?'.</p>","trap":"AQE doesn't replace CBO or stats — it only kicks in at shuffle/stage boundaries. A query with no shuffle (e.g. a simple narrow scan) gets no AQE benefit.","worked":"A sort-merge join estimated at 50 MB per side actually produces a 6 MB build side after filtering; AQE rewrites it to a broadcast join at runtime, skipping a full shuffle.","related":["cost-based-optimization","catalyst-pipeline"]}
});
