/* Teach me from the ground up: FE-REACT (auto-assembled) */
enh({
  "reconciliation": {"teach":"<p>At its core, reconciliation is just React playing a game of \"spot the difference\" between two pictures of your UI and then changing only the bits that actually moved.</p><h4>The setup</h4><p>Every time your state changes, your components run and produce a brand-new tree of <strong>React elements</strong> (lightweight description objects). React already has the <em>previous</em> tree from the last render sitting in memory. Reconciliation is the step where it walks both trees side by side and asks, position by position, \"what's the smallest set of real-DOM edits that turns the old picture into the new one?\"</p><h4>Dissecting the heuristics</h4><p>A truly optimal tree-diff is expensive (think $O(n^3)$). React refuses to pay that, so it makes two pragmatic assumptions to get to roughly $O(n)$:</p><ul><li><strong>Different type = different thing.</strong> If an element at a given slot changes type (say <code>&lt;a&gt;</code> became <code>&lt;span&gt;</code>), React doesn't try to be clever. It tears down the old node and its <em>entire subtree</em> and builds the new one from scratch — losing all state inside.</li><li><strong>Same type, changed props = patch in place.</strong> It keeps the node and just updates the changed attributes/text.</li><li><strong>Keys match siblings.</strong> Among a list of siblings of the same type, <code>key</code> tells React \"this is the same logical item as before,\" so it can reorder and reuse rather than recreate.</li></ul><div class=\"analogy\">Imagine a theater stage between two scenes. The director (React) compares the new script to the old one. A chair that's just moved left? Slide it (patch). A chair that became a table? You can't morph a chair into a table — haul the chair off, bring a table on (unmount + mount). Numbered name-tags on the actors (keys) let the director shuffle the same actors into new positions instead of firing everyone and re-casting.</div><h4>Why it matters</h4><p>The output isn't applied piecemeal — React collects all the needed changes (\"effects\") and commits them <strong>atomically</strong> in the commit phase, so users never see a half-updated screen. Modern React runs this on the <strong>Fiber</strong> architecture, which makes the diffing work interruptible so urgent input can jump the queue.</p><p>The practical bite: reconciliation diffs React's own element/fiber trees — <em>not</em> the real DOM against itself. And swapping an element's type at a slot is surprisingly destructive: it nukes the whole subtree below it.</p><div class=\"key\">Reconciliation computes the <strong>minimal patch</strong>, not a full re-render. Same type → patch in place; different type → tear down and rebuild the subtree; keys let same-type siblings be reused across reorders.</div>"},
  "virtual-dom": {"teach":"<p>At its core, the virtual DOM is just a cheap sketch of your UI drawn on scratch paper, so React can figure out what changed before touching the slow, expensive real thing.</p><h4>What the \"sketch\" actually is</h4><p>When a component returns JSX, you don't get HTML — you get plain JavaScript objects shaped roughly like <code>{ type, props, key }</code>. A whole screen is a tree of these tiny <strong>React element</strong> objects. That in-memory tree is the virtual DOM. Creating and comparing these objects is fast because they're just data; nothing has been rendered to the browser yet.</p><h4>The two-step dance</h4><p>On each update React (1) builds a fresh element tree from your components, then (2) diffs it against the previous tree (reconciliation) and applies only the necessary mutations to the real DOM, <strong>batching</strong> the writes together so the browser doesn't re-layout repeatedly.</p><div class=\"analogy\">Think of editing a book. The real DOM is sending the whole manuscript to the printing press — slow and you don't want to redo it for every typo. The virtual DOM is your marked-up draft on paper: you scribble all your edits cheaply, then hand the press a single list of exact changes (\"page 12, line 4, change one word\") instead of reprinting the book.</div><h4>The myth to kill</h4><p>The common claim — \"the virtual DOM is always faster than direct DOM manipulation\" — is <strong>false</strong>. The diff itself costs CPU. A skilled engineer hand-writing imperative DOM updates can beat React. The real win isn't raw speed; it's the <strong>declarative model</strong>: you describe \"what the UI should look like for this state\" and React works out the messy imperative \"how\" — correctly, every time. That trade — a little diffing cost for enormous developer sanity and correctness — is the actual value.</p><h4>The modern footnote</h4><p>The React team now de-emphasizes the term \"virtual DOM.\" The structure React truly keeps around and diffs — the thing that persists across renders and holds your hook state — is the <strong>Fiber</strong> tree. The element objects are recreated every render; the fibers endure.</p><div class=\"key\">The virtual DOM buys you a declarative API and correct, batched updates — not magic speed. The diff has real cost; its value is letting you write UI as a function of state instead of imperative DOM surgery.</div>"},
  "jsx-runtime": {"teach":"<p>At its core, JSX is just a friendlier way to write function calls — the angle brackets are a disguise that a compiler strips off before your code ever runs.</p><h4>It's not HTML, it's sugar</h4><p>JSX looks like HTML living inside JavaScript, but the browser never sees it. A build tool (Babel, SWC, or esbuild) compiles every JSX tag into a function call that returns a React <strong>element object</strong> — an inert description of what to render, not a live DOM node.</p><h4>Classic vs. automatic runtime</h4><p>There are two compilation styles:</p><ul><li><strong>Classic runtime (legacy):</strong> tags became <code>React.createElement(...)</code>, which is why you had to <code>import React</code> in every JSX file even if you never referenced <code>React</code> directly.</li><li><strong>Automatic runtime (since React 17, the modern default):</strong> tags become <code>jsx</code>/<code>jsxs</code> calls that the compiler <em>auto-imports</em> from <code>react/jsx-runtime</code>. No more boilerplate <code>import React</code> just to use JSX.</li></ul><p>Concretely, <code>&lt;Btn type=\"x\"&gt;Go&lt;/Btn&gt;</code> compiles to <code>jsx(Btn, { type: 'x', children: 'Go' })</code>. Notice the children become a <code>children</code> prop, and any <code>key</code> is pulled out as a separate argument because React consumes it specially.</p><h4>The capitalization rule, explained</h4><p>The compiler decides what <code>type</code> to emit by the first letter. A <strong>capitalized</strong> name like <code>Btn</code> compiles to a <em>reference to your component</em> (a variable). A <strong>lowercase</strong> name like <code>div</code> compiles to the <em>string</em> <code>'div'</code>, which React maps to a real HTML tag. Forget to capitalize your component and React thinks you meant a literal unknown tag.</p><div class=\"analogy\">JSX is like a mail-merge template. You write something readable with placeholders; the compiler runs the merge and hands the post office (React) a stack of plain addressed envelopes (element objects). The pretty template never travels — only the envelopes do, and they just sit in the bag until React decides to deliver them.</div><h4>The gotchas</h4><p>Because it's JavaScript, not HTML, reserved words get renamed: <code>class</code> → <code>className</code>, <code>for</code> → <code>htmlFor</code>. And crucially, an element is <strong>immutable and inert</strong> — calling JSX doesn't render anything; it just manufactures a description that React may render later.</p><div class=\"key\">JSX is function-call sugar producing immutable element objects. Capitalized = your component reference, lowercase = a tag string, and the automatic runtime means you no longer import React just to use it.</div>"},
  "keys": {"teach":"<p>At its core, a key is just a name-tag you pin on each item in a list so React can tell which one is which after the list gets shuffled.</p><h4>Why React needs the tag</h4><p>During reconciliation, when React diffs a list of sibling elements of the same type, it faces a question: is the item now in slot 2 the <em>same</em> logical item that used to be in slot 2, or did everything reorder? Without help, React matches by position. With <code>key</code>, it matches by identity — so it can <strong>move and reuse</strong> the existing fiber (carrying its DOM and internal state) instead of destroying and rebuilding it.</p><h4>The two requirements</h4><ul><li><strong>Unique among siblings.</strong> Keys only need to be unique within one list, not globally.</li><li><strong>Stable across renders.</strong> The same logical item must keep the same key every render. A key generated fresh each render (like <code>Math.random()</code>) defeats the entire purpose.</li></ul><p>Also note: <code>key</code> is consumed by React itself — you <em>cannot</em> read it as a prop inside your component.</p><h4>The index trap</h4><p>The classic mistake is <code>key={index}</code>. An index describes <em>position</em>, not the <em>item</em>. So when you sort, insert, or delete, the item moves but its index-key stays glued to the slot.</p><div class=\"analogy\">Imagine numbered coat hooks on a wall (indexes 0,1,2) versus name-tags on the coats themselves (ids). If you rearrange the coats but key by hook number, React thinks \"hook 1 still has a coat, no change\" — and leaves hook 1's coat (and any state, like a half-typed note in its pocket) right where it was. Now the wrong coat is on the wrong hook. Name-tag the coats instead and React moves each coat — and its pocket contents — to follow the actual coat.</div><h4>Where it bites in practice</h4><p>The damage shows up with <strong>per-row state</strong>: a row with an <code>&lt;input&gt;</code>, a checkbox, or focus. <code>items.map(i =&gt; &lt;Row key={i.id} /&gt;)</code> survives a sort cleanly — typed text follows its row. With <code>key={index}</code>, after a sort the input text appears to \"jump\" to the wrong row, because React reused the DOM node sitting at that position.</p><div class=\"key\">Keys give list siblings a stable identity so React reuses fibers (and their state/DOM) across reorders. Use a real, stable id from your data — never the array index for lists that can reorder, insert, or hold per-row state.</div>"},
  "render-commit": {"teach":"<p>At its core, every React update happens in two acts: first React figures out <em>what</em> the screen should become (thinking, no side effects), then it actually changes the screen (acting).</p><h4>Act one: the render phase</h4><p>React calls your components to build the new element tree and diffs it against the old one. This phase must be <strong>pure</strong>: same inputs in, same JSX out, and no touching the outside world. Why so strict? Because under concurrent rendering this phase is <strong>interruptible and restartable</strong> — React may start rendering, pause to handle an urgent keystroke, throw the half-done work away, and start over. To surface accidental impurity, <strong>Strict Mode in development double-invokes</strong> your render function; if your second run differs, you have a bug.</p><h4>Act two: the commit phase</h4><p>Once React knows the minimal set of changes, the commit phase runs <strong>synchronously</strong> and is allowed to have side effects on the real world. In order, it: mutates the DOM, sets <code>ref</code>s, and runs <code>useLayoutEffect</code> callbacks. Then the browser <strong>paints</strong> the pixels. <em>After</em> paint, the asynchronous passive <code>useEffect</code>s fire.</p><div class=\"analogy\">Think of a film editor versus a film projector. The editor (render phase) cuts and re-cuts the reel in private, tossing takes, trying alternatives — nothing is shown to the audience yet, so it had better not, say, charge admission. The projector (commit phase) then plays the final cut to the room exactly once. You'd never trigger real-world consequences during editing, because that cut might get discarded.</div><h4>Why the split governs where code goes</h4><p>This is the deep reason side effects belong in <strong>effects or event handlers</strong>, never in the component body. If you subscribe to something, fire a network request, or mutate a ref during render, and React renders twice or discards that render, you get duplicate subscriptions, doubled requests, or <strong>tearing</strong> (inconsistent UI).</p><p>One sharp consequence: if you need to read DOM measurements like <code>node.offsetHeight</code> and reposition something <em>before</em> the user sees it, that work must run after commit but before paint — which is exactly <code>useLayoutEffect</code>, not the component body and not <code>useEffect</code>.</p><div class=\"key\">Render = pure, interruptible \"what changed\" computation that may run twice or be thrown away; commit = synchronous \"apply it\" with side effects. Keep side effects out of render — put them in effects or handlers.</div>"},
  "use-effect-vs-layout": {"teach":"<p>At its core, both of these run code <em>after</em> React updates the DOM — the only real difference is whether they run before or after the user's eyes see the new pixels.</p><h4>useEffect: after the paint</h4><p><code>useEffect</code> fires <strong>asynchronously, after the browser has painted</strong>. The screen updates first; your effect runs a moment later. Because it never blocks the visual update, it's the correct default for almost everything: data fetching, subscriptions, logging, syncing with external systems. The user isn't waiting on it.</p><h4>useLayoutEffect: after commit, before paint</h4><p><code>useLayoutEffect</code> fires <strong>synchronously after the DOM is committed but before the browser paints</strong>. React will not let the user see anything until your callback finishes. That's powerful for exactly one job: <strong>measure the freshly-rendered DOM and synchronously mutate it</strong> so the user never witnesses an in-between state.</p><div class=\"analogy\">Picture a stage with a curtain. The commit phase places all the props on the stage behind the closed curtain. <code>useLayoutEffect</code> is the stagehand who darts out <em>before</em> the curtain rises to nudge a chair into its exact spot — the audience only ever sees the corrected scene. <code>useEffect</code> is the stagehand who waits until the curtain is already up and the show is running, then quietly tidies backstage. If your fix has to happen before anyone looks, you need the first stagehand; otherwise the second is less disruptive.</div><h4>The shared mechanics</h4><p>Both take a <strong>dependency array</strong>, compared with <code>Object.is</code> to decide when to re-run, and both run their <strong>cleanup</strong> before the next run and on unmount. So their structure is identical; only the timing differs.</p><h4>Why default to useEffect</h4><p>Because <code>useLayoutEffect</code> <strong>blocks paint</strong>, heavy work inside it delays what the user sees and can hurt your <strong>INP</strong> (interaction responsiveness) score. It also <strong>doesn't run during server-side rendering</strong> and warns if you use it there, since there's no layout to measure on the server. So reach for it <em>only</em> when you genuinely must read layout and re-mutate synchronously to avoid a visible flash.</p><p>The canonical case: a tooltip that must read <code>getBoundingClientRect()</code> and set its <code>top</code>/<code>left</code>. Done in <code>useLayoutEffect</code>, the user never sees it flicker at <code>(0, 0)</code> before snapping into place; done in <code>useEffect</code>, they'd catch that flash.</p><div class=\"key\">Default to <code>useEffect</code> (async, after paint, never blocks). Use <code>useLayoutEffect</code> only to measure and synchronously fix the DOM before paint — it blocks the screen and skips SSR.</div>"},
  "deps-array": {"teach":"<p>At its core, a dependency array is just a short list you hand React that answers one question: \"which values, if they change, should make this effect or computation run again?\"</p><h4>How the comparison works</h4><p>On every render React compares your new array to the previous one, element by element, using <code>Object.is</code>. If any slot differs, it re-runs the effect (after running its cleanup) or recomputes the memo. Two special cases:</p><ul><li><code>[]</code> (empty) — \"nothing here is reactive,\" so run once on mount only.</li><li>No array at all — run on <em>every</em> render.</li></ul><h4>The exhaustive-deps rule, dissected</h4><p>The <strong>exhaustive-deps</strong> ESLint rule insists that <em>every reactive value</em> your callback reads — props, state, and values derived from them — appears in the array. \"Reactive\" just means \"could be different on a future render.\" The rule exists because of a sneaky failure mode called a <strong>stale closure</strong>.</p><div class=\"analogy\">A closure is like a photograph. When your effect callback is created during a render, it takes a snapshot of all the variables visible at that moment. If you lock the effect to <code>[]</code> but it reads <code>count</code>, the callback forever holds the photo of <code>count</code> from the very first render — even as the real <code>count</code> climbs. You're reading a years-old photo and wondering why the person looks young. The deps array is how you tell React \"throw out the old photo and take a fresh one when this value changes.\"</div><h4>The wrong fix vs. the right fix</h4><p>The tempting mistake is to silence the linter to \"run only on mount\" while still reading props/state inside — that doesn't make the values fresh, it just hides that you're reading stale ones. The honest fixes <em>preserve</em> correctness:</p><ul><li><strong>Functional updaters</strong> — <code>setN(n =&gt; n + 1)</code> reads the latest value from React, so you don't need <code>n</code> in deps.</li><li><strong>Stabilize functions</strong> with <code>useCallback</code> so they can safely be deps.</li><li><strong>Move logic inside</strong> the effect so fewer outside values are referenced.</li><li><strong>Refs</strong> when you only need the latest value to read, not to react to.</li></ul><p>Classic example: <code>useEffect(() =&gt; { const id = setInterval(() =&gt; setN(n =&gt; n + 1), 1000); return () =&gt; clearInterval(id); }, [])</code>. The empty deps are <em>honest</em> here precisely because the functional updater <code>n =&gt; n + 1</code> means <code>n</code> is never read from the closure.</p><div class=\"key\">The deps array is an <code>Object.is</code>-compared claim about what's reactive inside. Never lie to it to \"run once\" — fix staleness with functional updaters, stable callbacks, or refs instead.</div>"},
  "referential-stability": {"teach":"<p>At its core, referential stability means handing React the <em>same exact object or function</em> across renders, instead of a fresh copy that merely looks the same — because React checks sameness by identity, not by contents.</p><h4>Why identity, not value</h4><p>React decides whether props or dependencies \"changed\" using <code>Object.is</code>, which for objects, arrays, and functions compares <strong>reference identity</strong> — are these literally the same thing in memory? Two objects with identical fields are still <em>different</em> references. So an inline <code>{...}</code>, <code>[...]</code>, or arrow function created during render is a brand-new reference every time, even when its logical value is unchanged.</p><div class=\"analogy\">Imagine you photocopy a document and hand a colleague a fresh copy every morning. The words are identical, but it's physically a new sheet each day. A colleague who only checks \"is this the same physical paper I had yesterday?\" (that's <code>Object.is</code>) concludes \"new paper — better re-read the whole thing!\" To skip the re-read, you must hand them the <em>same</em> physical sheet. <code>useMemo</code> and <code>useCallback</code> are how you keep handing over the same sheet.</div><h4>What breaks without it</h4><p>Two things rely on stable identity:</p><ul><li><strong><code>React.memo</code> bailouts.</strong> A memoized child skips re-rendering only if its props are referentially equal. Pass a new inline function each render and the memo is defeated — the child re-renders anyway.</li><li><strong>Effect/memo deps.</strong> A fresh object in a dependency array re-runs the effect needlessly.</li></ul><p>The fixes: <code>useMemo</code> preserves a <em>value's</em> identity across renders (given stable deps); <code>useCallback</code> does the same for a <em>function</em>. So <code>const onClick = useCallback(() =&gt; act(id), [id])</code> keeps the same function reference until <code>id</code> changes, letting a <code>React.memo</code>'d button skip re-rendering.</p><h4>The two traps</h4><p>First, these are <strong>performance hints, not guarantees</strong> — React is allowed to evict the cache and recompute. So <em>never</em> rely on a memoized identity persisting for correctness; it's an optimization, not a contract. Second, memoizing <em>everything</em> \"to be safe\" is pure overhead when nothing downstream is memoized — you pay the comparison cost for no benefit.</p><p>The modern relief: the <strong>React Compiler</strong> (a build-time tool, v1.0 stable Oct 2025) auto-memoizes for you, erasing most manual <code>useMemo</code>/<code>useCallback</code>.</p><div class=\"key\">React compares by identity, so stable references let memoized children and effects skip needless work — but treat memoization as a hint that can be dropped, never a correctness guarantee, and don't over-apply it.</div>"},
  "context-rerender": {"teach":"<p>At its core, the context pitfall is this: when a context's value changes, <em>every single consumer re-renders</em> — even one that only cares about a tiny field it didn't touch.</p><h4>How context decides to update</h4><p>A context Provider holds a <code>value</code>. React compares the new <code>value</code> to the previous one with <code>Object.is</code>. If they differ, React re-renders <strong>all</strong> components that consume that context. There's no built-in way to say \"only re-render me if the <code>user.name</code> field changed\" — context has <strong>no selectors and no per-field bailout</strong>. It's all-or-nothing.</p><h4>The classic bug</h4><p>The most common trigger is an <strong>inline object literal</strong> as the value: <code>&lt;Ctx.Provider value={{ user, setUser }}&gt;</code>. That object is freshly created on every render of the provider, so its reference always differs — meaning <code>Object.is</code> always reports \"changed,\" so <em>all</em> consumers re-render every time the provider renders, even when <code>user</code> didn't actually change.</p><div class=\"analogy\">Imagine a building intercom with one button that buzzes <em>every</em> apartment at once. Someone wants to tell apartment 4 that a package arrived, but pressing the button wakes the whole building. Context is that intercom: there's no way to ring just one unit. Worse, the inline-object bug is like accidentally leaning on the button on every walk past — buzzing everyone constantly, for no reason.</div><h4>The fixes</h4><ul><li><strong>Memoize the value:</strong> <code>value={useMemo(() =&gt; ({ user, setUser }), [user])}</code>. Now the reference only changes when <code>user</code> truly changes, stopping the gratuitous re-renders.</li><li><strong>Split contexts by update cadence.</strong> Put rarely-changing things (like a stable <code>dispatch</code> function) in one context and volatile state in another, so a hot value doesn't drag stable consumers along.</li><li><strong>Use an external store</strong> read via <code>useSyncExternalStore</code> with selectors — the approach libraries like Zustand and Redux take. This gives you the per-field subscription context lacks.</li></ul><h4>Why it matters</h4><p>People reach for context as a \"global state store,\" then are shocked when one frequently-changing field re-renders an entire subtree of consumers and tanks performance. Context is a great <em>dependency injection</em> mechanism; it is <strong>not</strong> a performance-optimized state manager.</p><div class=\"key\">Context re-renders every consumer when its <code>value</code> reference changes and has no selectors. Memoize the value, split contexts by how often they change, or use an external store with selectors for hot state.</div>"},
  "rules-of-hooks": {"teach":"<p>At its core, the Rules of Hooks boil down to one habit: call your hooks in the same order, every render, right at the top — no <code>if</code>, no loop, no early return wrapping them.</p><h4>Why order is sacred</h4><p>Here's the secret that explains everything: React doesn't track your hooks by name. It tracks them by <strong>call order</strong>. The first <code>useState</code> you call gets slot 1 on the fiber, the second gets slot 2, and so on. On the next render React walks the same sequence and matches each call to its slot by position.</p><p>So if you hide a hook behind a condition, you shift the count. Skip the third hook on one render, and what was your fourth hook now lands in the third slot — reading the wrong state. The whole alignment shears off.</p><div class=\"analogy\">Picture a coat check where tickets are handed out strictly in order: first coat → ticket 1, second → ticket 2. The attendant remembers nothing about the coats; they trust that the order never changes. If one day you skip handing over your second coat, everyone behind you gets the wrong ticket number, and at pickup you walk out with a stranger's jacket. Hooks are coat-check tickets bound to position, not to you.</div><h4>The two rules, stated plainly</h4><ul><li><strong>Call hooks unconditionally, at the top level.</strong> Never inside <code>if</code>, loops, or after an early <code>return</code>.</li><li><strong>Call hooks only from React function components or other custom hooks.</strong> Not from plain functions, class methods, or event handlers.</li></ul><h4>How it's enforced and named</h4><p>The <code>use</code> prefix is the convention that lets tooling recognize hooks — both built-ins and your custom hooks. <code>eslint-plugin-react-hooks</code> enforces the ordering rule <em>and</em> exhaustive-deps, catching most violations before they ship.</p><h4>The deceptive part and one exception</h4><p>The danger is that a conditional hook often <em>seems</em> to work — until the branch flips. <code>if (open) useEffect(...)</code> runs fine while <code>open</code> stays constant, then desyncs the moment it toggles. The fix is to invert it: always call the hook, put the condition <em>inside</em>: <code>useEffect(() =&gt; { if (open) {...} }, [open])</code>.</p><p>One documented exception worth knowing: React 19's <code>use()</code> is technically an <strong>API, not a hook</strong>, and it <em>can</em> be called conditionally — the one sanctioned break from the unconditional rule.</p><div class=\"key\">React matches hooks to state by call order, not name — so call them unconditionally at the top level of components/custom hooks. Move conditions inside the hook, never wrap the hook in a condition.</div>"},
  "use-reducer-pattern": {"teach":"<p>At its core, <code>useReducer</code> is just moving all your \"when this happens, change state like that\" logic out of scattered <code>setState</code> calls and into one tidy, testable function.</p><h4>The shape</h4><p>You call <code>useReducer(reducer, initialArg, init?)</code> and get back <code>[state, dispatch]</code>. The <strong>reducer</strong> is a <strong>pure</strong> function <code>(state, action) =&gt; newState</code>: hand it the current state and an action describing what happened, and it returns the next state. To trigger a change you call <code>dispatch({ type: 'increment' })</code>, and React runs your reducer to compute the new state.</p><div class=\"analogy\">Think of a vending machine. <code>state</code> is its current contents and credit. An <code>action</code> is a button press (\"B4\", \"refund\"). The reducer is the machine's wiring: given the current state and which button was pressed, it deterministically decides the next state. It never reaches outside itself — it doesn't phone the bank or roll dice. Same state + same button always yields the same result. That predictability is the whole point.</div><h4>When to prefer it over useState</h4><p><code>useState</code> shines for simple, independent values. Reach for <code>useReducer</code> when your transitions are <strong>interdependent</strong> (changing one field forces changes in another), span <strong>multiple fields</strong>, or are complex enough that you'd like to <strong>test them in isolation</strong> — and a pure reducer is trivial to unit-test: feed it state and an action, assert the output.</p><h4>The stable-dispatch superpower</h4><p>Here's a property that unlocks a whole pattern: <code>dispatch</code> has a <strong>stable identity</strong> for the component's entire lifetime — it never changes reference. That means you can pass <code>dispatch</code> down through context <em>without</em> causing consumers to re-render from a changing reference. This is the backbone of the lightweight \"reducer + context\" store: volatile state in one context, the rock-stable <code>dispatch</code> in another.</p><h4>Lazy initialization</h4><p>The optional third argument lets you compute the initial state lazily: when <code>init</code> is provided, React sets the initial state to <code>init(initialArg)</code>, so any expensive setup runs once rather than on every render.</p><h4>The purity trap</h4><p>Reducers must be <strong>pure</strong> — no fetches, no <code>Math.random()</code>, no mutating the existing state object (return a new one). Side effects belong in effects or event handlers, not the reducer. To help you catch violations, Strict Mode <strong>double-invokes your reducer in development</strong>; if double-running changes the outcome, your reducer is impure.</p><div class=\"key\">Centralize interdependent state in a pure <code>(state, action) =&gt; newState</code> reducer. Keep it side-effect-free, and exploit <code>dispatch</code>'s stable identity to pass it through context without re-renders.</div>"},
  "custom-hook": {"teach":"<p>At its core, a custom hook is just a regular function whose name starts with <code>use</code> and which calls other hooks — a way to bottle up reusable stateful logic and pour it into many components.</p><h4>What it actually is</h4><p>There's no special API. If you write a function named <code>useSomething</code> and it calls built-in hooks (or other custom hooks), congratulations — it's a custom hook. Its job is to <strong>extract stateful logic</strong>: subscriptions, timers, data fetching, form handling, window-size tracking, anything that uses hooks. You pull that logic out of a component so multiple components can share the <em>logic</em>.</p><h4>The crucial subtlety: shared logic, not shared state</h4><p>This trips up nearly everyone. Two components using the same custom hook do <strong>not</strong> share state. Each call gets its own completely independent state. Hooks aren't singletons — calling a custom hook is like running its body fresh inside <em>your</em> component, on <em>your</em> fiber.</p><div class=\"analogy\">A custom hook is a recipe, not a shared pot of soup. Hand the same recipe to ten cooks and you get ten separate pots — same procedure, independent contents. If cook A salts their soup, cook B's pot is untouched. If you genuinely want everyone eating from <em>one</em> pot of soup, you need a shared source — context or an external store — not a recipe.</div><h4>What it returns and why the prefix matters</h4><p>A custom hook returns whatever shape is convenient: a single value, a tuple <code>[value, setter]</code>, an object of helpers, refs — your call. The <code>use</code> prefix isn't decorative: it's how <code>eslint-plugin-react-hooks</code> knows to apply the Rules of Hooks <em>inside</em> the function (checking call order and deps). Drop the prefix and the linter stops protecting you.</p><h4>Choosing the right tool</h4><p>A simple division of labor: use <strong>custom hooks for stateful logic</strong>, <strong>components for markup</strong>, and <strong>plain functions for stateless helpers</strong> (a date formatter doesn't need to be a hook).</p><p>A tiny example: <code>function useToggle(init = false) { const [on, set] = useState(init); return [on, () =&gt; set(o =&gt; !o)]; }</code>. Any component can grab <code>const [open, toggle] = useToggle()</code> and get its own private boolean — fully isolated from every other consumer.</p><div class=\"key\">A custom hook is a <code>use</code>-prefixed function that composes other hooks to reuse stateful <em>logic</em> — but each call site gets its own independent state. For genuinely shared state, reach for context or an external store.</div>"},
  "server-components": {"teach":"<p>At its core, a React Server Component is a component that runs <em>only on the server</em>, fetches its data directly, and ships <strong>zero JavaScript</strong> for itself to the browser.</p><h4>The default flips</h4><p>In the RSC model (React 19, used by frameworks like Next.js App Router), components are <strong>Server Components by default</strong>. Such a component executes on the server, can be declared <code>async</code> and <code>await</code> its data inline (hit the database, call an API), then renders to a serialized payload sent to the client. Because the component runs and stays on the server, <strong>none of its code lands in the client bundle</strong> — your data-fetching logic, heavy libraries, and secrets never reach the browser.</p><p>The trade-off: a Server Component has no interactivity. <strong>No <code>useState</code>, no <code>useEffect</code>, no event handlers, no browser APIs</strong> — there's no browser running it.</p><div class=\"analogy\">Think of a restaurant. Server Components are the kitchen: they have direct access to the pantry (database) and do the heavy cooking out of sight; the diner never sees the raw ingredients or the recipes. What arrives at the table is a finished plate (serialized HTML/payload). Client Components are the things on the table the diner actually interacts with — the salt shaker, the call button. You don't put the pantry on the table, and you don't expect the salt shaker to cook.</div><h4>Crossing the boundary</h4><p>The <code>'use client'</code> directive at the top of a file marks the entry into the <strong>interactive client bundle</strong> — inside it, hooks and event handlers are allowed again. A Server Component can render Client Components and pass them props, but with a hard constraint: those props must be <strong>serializable</strong> (data that can be sent over the wire). It can also pass <code>children</code> through.</p><h4>Server Actions</h4><p>For mutations, the <code>'use server'</code> directive exposes async server functions to the client. The client can call them like ordinary functions, but they execute on the server — a clean way to write forms and data changes without hand-rolling API endpoints.</p><h4>Where it bites</h4><p>The two classic errors: (1) using <code>useState</code>/<code>useEffect</code>/<code>onClick</code> in a Server Component — there's no interactivity there; (2) passing a <strong>non-serializable</strong> prop across the boundary, like a plain function (unless it's a Server Action) or a class instance. React can't serialize those, so it throws.</p><p>The payoff is real: <code>async function Page() { const data = await db.query(); return &lt;List rows={data} /&gt; }</code> fetches on the server and ships <em>no client JavaScript</em> for that fetch.</p><div class=\"key\">Server Components run only on the server, fetch data inline, and add no client JS — at the cost of no state/effects/handlers. Cross into interactivity with <code>'use client'</code>, and only pass serializable props (or Server Actions) over the boundary.</div>"},
  "suspense": {"teach":"<p>At its core, <code>&lt;Suspense&gt;</code> is just a sign that says \"hang on, loading…\" which React shows automatically while anything inside it is still fetching or downloading.</p><h4>The pending-state declarative</h4><p>You wrap part of your tree: <code>&lt;Suspense fallback={&lt;Spinner /&gt;}&gt;...&lt;/Suspense&gt;</code>. If any descendant <strong>suspends</strong> — meaning it hits something not-yet-ready — React shows the <code>fallback</code> instead of that subtree, then swaps in the real content once everything resolves. You declare the loading UI <em>once</em>, and React orchestrates when to show it. You never write <code>if (loading) return &lt;Spinner /&gt;</code> by hand.</p><h4>What can suspend</h4><p>Three main things trigger Suspense:</p><ul><li>A lazily-loaded code chunk via <code>React.lazy</code> that hasn't downloaded yet.</li><li>A streamed SSR segment still arriving from the server.</li><li>A data read via <code>use(promise)</code> or a Server Component awaiting data.</li></ul><div class=\"analogy\">Suspense is like a \"Now Loading\" placeholder in a video game. The level designer doesn't manually track every texture and model; they just draw a boundary that says \"until everything inside this region is loaded, show the loading screen.\" The engine fills in the rest. You mark the <em>region</em>, not the bookkeeping.</div><h4>It's about loading, not errors</h4><p>This is the key distinction. Suspense handles the <strong>pending</strong> state. Its partner, the <strong>error boundary</strong>, handles the <strong>failed</strong> state — it catches errors <em>thrown during render</em> and shows a fallback UI. You typically pair them: Suspense for \"still loading,\" error boundary for \"it broke.\"</p><h4>The two traps</h4><p>First, <strong>neither Suspense nor error boundaries catch errors in event handlers or async callbacks.</strong> If your <code>onClick</code> throws or your <code>fetch().then()</code> rejects outside render, React's boundaries don't see it — wrap those in plain <code>try/catch</code>. Suspense only reacts to render-phase suspension; error boundaries only to render-phase throws.</p><p>Second, Suspense shows its fallback for <em>any</em> suspending child in its subtree. Put one giant boundary around your whole page and a single slow component blanks the entire screen with a spinner. So <strong>place boundaries thoughtfully</strong> — granular boundaries let already-ready parts render while only the slow corner shows a fallback, avoiding jarring full-screen flashes.</p><p>Canonical use: <code>&lt;Suspense fallback={&lt;Spinner /&gt;}&gt;&lt;LazyChart /&gt;&lt;/Suspense&gt;</code> shows the spinner exactly until the code-split chunk finishes loading, then reveals the chart.</p><div class=\"key\">Suspense declaratively shows a fallback while descendants load; it does <em>not</em> handle errors (that's error boundaries) and ignores event-handler/async throws. Scope boundaries tightly so one slow piece doesn't flash a full-screen spinner.</div>"},
  "concurrent-transition": {"teach":"<p>At its core, concurrent rendering lets you tell React \"this update isn't urgent\" — so React can pause it mid-flight to keep typing, clicking, and scrolling buttery-smooth.</p><h4>The mental model: priorities</h4><p>On the Fiber scheduler, React can treat rendering work as <strong>interruptible and prioritized</strong>. Some updates are urgent (the character you just typed must appear instantly). Others are heavy but can wait (re-rendering a giant filtered list). Concurrent features let urgent work jump ahead and interrupt the heavy work, then resume it.</p><h4>useTransition: when you own the setter</h4><p><code>useTransition</code> returns <code>[isPending, startTransition]</code>. Wrap a state update in <code>startTransition</code> and you mark it <strong>non-urgent</strong>: <code>start(() =&gt; setQuery(input))</code>. React renders any urgent updates (the keystroke) first, then does the transition work, interruptibly. <code>isPending</code> is a boolean you can use to show a subtle \"updating…\" indicator while the low-priority render is in flight.</p><h4>useDeferredValue: when you only have the value</h4><p>Sometimes you don't control the setter — a value just arrives as a prop. <code>useDeferredValue</code> wraps <em>a value you receive</em> and returns a <strong>lagging copy</strong>. You feed the snappy current value to the input and the deferred (slightly stale) value to the expensive subtree, so the input stays responsive while the heavy part catches up a beat later.</p><p>Rule of thumb: <strong>own the setter → <code>useTransition</code></strong>; <strong>only have the value → <code>useDeferredValue</code></strong>.</p><div class=\"analogy\">Picture a restaurant kitchen during a rush. A customer asking \"can I get water?\" (urgent input) gets handled immediately, even if the chef is mid-way through an elaborate ten-course tasting menu (the heavy list render). The chef pauses the big job, pours the water, then returns to the menu. Concurrent rendering is that chef: it can set down low-priority work to serve quick requests, then pick it back up — instead of making the thirsty customer wait for ten courses.</div><h4>The myth and the trap</h4><p>The big misconception: <strong>transitions don't make rendering faster.</strong> The same total work happens. What changes is the <em>order and interruptibility</em> — React reprioritizes so the app <em>feels</em> responsive even though the heavy render still takes just as long.</p><p>The trap: never wrap a controlled input's own <code>value</code> update in a transition. If you defer the very keystroke that updates the textbox, the box itself lags behind the user's typing — the opposite of what you want. Defer the <em>expensive downstream</em> work, keep the input's own update urgent.</p><div class=\"key\">Concurrent features reprioritize and interrupt — they don't speed up work. Use <code>useTransition</code> when you own the setter, <code>useDeferredValue</code> when you only have the value, and never defer a controlled input's own value update.</div>"},
  "key-as-reset": {"teach":"<p>At its core, \"key as reset\" is a trick: change a component's <code>key</code> and React will throw away the old instance entirely and build a brand-new one — a clean slate, no leftover state.</p><h4>Why a new key resets everything</h4><p>Recall how reconciliation uses keys: a key is an identity tag. When React diffs and sees the <em>same</em> position but a <em>different</em> key, it concludes \"this is a different thing now.\" So it <strong>unmounts the old component</strong> — discarding all its internal state, refs, and effects — and <strong>mounts a fresh one</strong>, re-running its mount effects from scratch. You're not refreshing the component; you're replacing it with a new copy.</p><div class=\"analogy\">Think of a hotel room between guests. When a new guest checks in (new key/identity), housekeeping doesn't leave the previous guest's half-eaten room service and rumpled sheets — they completely reset the room: fresh linens, emptied minibar, cleared safe. Keeping the same guest (same key) means their belongings stay put. Changing the key is checking in a new guest, which forces a full turnover.</div><h4>The problem it elegantly solves</h4><p>Suppose you have an edit form, and the user switches which record they're editing. You want the form's draft state wiped so it doesn't carry over stale edits. The naive approach is to watch the incoming prop in a <code>useEffect</code> and copy it into state to \"sync\" — an error-prone anti-pattern the React docs actively discourage.</p><p>The clean, declarative alternative: <code>&lt;EditForm key={selectedId} record={record} /&gt;</code>. When <code>selectedId</code> changes, the key changes, React remounts <code>EditForm</code>, and all its draft state vanishes automatically. You expressed \"this is a different editing session\" directly, and React handled the reset. This is React's <em>recommended</em> way to reset state on identity change.</p><h4>When it bites</h4><p>The cost is real: it's a <strong>full remount</strong>, not a cheap re-render. That means you <strong>lose focus</strong>, <strong>interrupt in-flight animations</strong>, drop scroll position inside the component, and pay the full mount cost (re-running effects, rebuilding DOM). So don't reach for it as a generic \"force a refresh\" lever. Use it deliberately, only when the component's <em>logical identity</em> genuinely changed and a clean reset is what you actually want.</p><div class=\"key\">Changing a component's <code>key</code> remounts it — discarding all state/refs and re-running mount effects. It's the idiomatic way to reset state on identity change, but it's a full remount (lost focus/animations, mount cost), so use it intentionally.</div>"},
  "fiber": {"teach":"<p>At its core, a Fiber is React's internal unit of work — a small bookkeeping object for each piece of your UI that lets React pause its work partway, set it aside, and come back later.</p><h4>What a fiber holds</h4><p>Since React 16, every element in your tree gets a corresponding <strong>fiber node</strong>. Each fiber stores that element's <code>type</code>, its current state (including your hook state), the effects it needs to run, and — crucially — links to its <strong>parent, child, and sibling</strong> fibers. So the fiber tree is structured as a linked list you can walk in any direction, one node at a time.</p><h4>Why the linked-list structure matters</h4><p>Representing rendering work as a chain of discrete fibers means React can do the work <strong>incrementally</strong>. Instead of one giant uninterruptible recursive call that must finish before the browser regains control, React processes <em>one fiber at a time</em> and can <strong>pause, abort, resume, and prioritize</strong> between units. After each small unit it can ask, \"did something more urgent come in?\"</p><div class=\"analogy\">Picture reading a long novel with a bookmark versus being trapped in a single unbreakable sentence. The old recursive reconciler was the unbreakable sentence — once started, you couldn't stop until the end, even if the phone rang. Fibers add bookmarks between paragraphs: React reads a paragraph (processes a fiber), and if the doorbell rings (an urgent keystroke), it drops the bookmark, answers the door, then resumes exactly where it left off. The bookmark is what makes interruption possible.</div><h4>The foundation of concurrency</h4><p>This is precisely what powers <strong>concurrent rendering</strong>. <code>useTransition</code> and <code>useDeferredValue</code> can mark work low-priority, Suspense can stream content, and renders can be interrupted — <em>all</em> because the fiber scheduler can keep urgent input ahead of low-priority updates, pausing and resuming fiber-by-fiber. A long list render started inside <code>startTransition</code> can be halted mid-fiber-tree so a keystroke renders first, then resumed — impossible without this granular work unit.</p><h4>The distinction to keep straight</h4><p>Don't conflate the fiber with the element tree. <strong>Elements are immutable descriptions, recreated from scratch on every render.</strong> <strong>Fibers persist across renders</strong> — they're the durable internal nodes that carry your hook state (matched by call order) and drive the scheduler. Elements are the disposable blueprint; fibers are the standing structure React actually keeps and schedules against.</p><div class=\"key\">Fiber is React's persistent, linked-list unit of work that makes rendering interruptible, resumable, and prioritizable — the foundation under all concurrent features. It endures across renders and holds hook state, unlike the disposable element tree.</div>"}
});
