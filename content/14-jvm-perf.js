/* Performance & Profiling (JVM-PERF) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"JVM-PERF","front":"Why does async-profiler avoid the safepoint bias that plagues most Java sampling profilers?","back":"JVMTI-based profilers (e.g. classic <code>jstack</code>-sampling tools) can only capture a thread's stack when it reaches a <strong>safepoint</strong>, so samples cluster at safepoint-pollable bytecodes and misattribute hot code. async-profiler uses the kernel <code>perf_events</code> / <code>SIGPROF</code> signal plus the JVM's <code>AsyncGetCallTrace</code> API to walk the stack at the <em>actual</em> interrupted instruction, regardless of safepoint location, giving an unbiased CPU profile."},
  {"sec":"JVM-PERF","front":"JFR vs async-profiler — when do you reach for each in production?","back":"<ul><li><strong>JFR</strong>: built-in, always-on, ~1% overhead at default settings, structured event stream (allocation, GC, locks, I/O, exceptions). Best for continuous flight recording and correlating events across subsystems.</li><li><strong>async-profiler</strong>: external agent, low-overhead and accurate CPU/alloc <em>flame graphs</em>, no safepoint bias, can merge native + JIT frames in one graph. Best for deep CPU/allocation hotspot drilldowns.</li></ul>They are complementary; async-profiler can even emit its output in JFR format and its bundled <code>jfrconv</code> can flame-graph a <code>.jfr</code> file."},
  {"sec":"JVM-PERF","front":"What does async-profiler's <code>alloc</code> mode sample, and why is it nearly free?","back":"It samples <strong>TLAB</strong> allocation events: when a thread fills its thread-local allocation buffer and gets a new one (or allocates on the slow path outside a TLAB), that event is recorded. Because the common fast-path bump-pointer allocation isn't touched, you only sample roughly every N kB (N ≈ TLAB size), so overhead stays low while still statistically attributing allocation pressure to call sites. You see <em>which code allocates the bytes that drive GC</em>, not just heap snapshots."},
  {"sec":"JVM-PERF","front":"Allocation profiling mantra: \"the cheapest GC is the object you never allocate\" — what is the actual production cost of allocation?","back":"Allocation itself is a cheap bump-pointer op, but every allocated object eventually feeds <strong>GC</strong>: more allocation = higher minor-GC frequency, more survivor copying, more chance of promotion to old gen, and more CPU/pause overhead. On a hot data-plane path, cutting allocation rate reduces GC frequency and tail latency far more than tuning GC flags. Profile allocation by call site, not just heap occupancy."},
  {"sec":"JVM-PERF","front":"What is escape analysis, and what optimizations does it unlock?","back":"Escape analysis is a JIT (C2) analysis that proves an object does not <em>escape</em> the method/thread that created it. When it doesn't escape it enables: <strong>scalar replacement</strong> (the object is decomposed into individual fields kept in registers — no heap allocation at all) and <strong>lock elision</strong> (synchronizing on a non-escaping object becomes a no-op). HotSpot has no general on-stack object allocation — the realized win is allocation <em>elimination</em> via scalar replacement, not just faster allocation."},
  {"sec":"JVM-PERF","front":"Why might escape analysis silently fail to eliminate an allocation you expected it to?","back":"EA only fires when the method is <strong>JIT-compiled</strong> (not in the interpreter) and the object provably doesn't escape. It is defeated by: storing the object in a field/static/array, passing it to a method that can't be inlined, returning it, or insufficient inlining depth (the allocating call chain must be inlined enough to prove non-escape). Large or complex objects may also exceed scalar-replacement limits. Verify with <code>-XX:+PrintEliminateAllocations</code> (needs <code>-XX:+UnlockDiagnosticVMOptions</code>, typically on a debug build), never assume."},
  {"sec":"JVM-PERF","front":"What is false sharing and how do you detect and fix it?","back":"<strong>False sharing</strong>: two independent fields written by different threads land on the same 64-byte <strong>cache line</strong>, so each write invalidates the other core's copy via the cache-coherence protocol, ping-ponging the line despite no logical contention. Detect via <code>perf c2c</code> or high HITM cache events. Fix by padding fields onto separate cache lines, or annotate hot fields with <code>@jdk.internal.vm.annotation.Contended</code> (user code requires <code>-XX:-RestrictContended</code>)."},
  {"sec":"JVM-PERF","front":"How does <code>@Contended</code> work and what is its cost?","back":"<code>@Contended</code> tells the JVM to pad a field (or named group) so it sits alone, away from other fields, preventing false sharing. The JVM inserts padding (<code>-XX:ContendedPaddingWidth</code>, default 128 bytes — double a 64-byte line to also defeat adjacent-line prefetch). Cost is memory bloat per instance, so use it only on genuinely hot, concurrently-written fields (e.g. <code>LongAdder</code> cells, ring-buffer sequence counters)."},
  {"sec":"JVM-PERF","front":"Why is reflection slower than a direct call, and what mitigates it?","back":"Reflective <code>Method.invoke</code> pays for access checks, argument boxing into an <code>Object[]</code>, and an indirection the JIT often can't inline or devirtualize. The JIT can optimize a reflective call well only if the <code>Method</code> object is effectively constant (cached in a <code>static final</code>). Mitigations: cache <code>Method</code>/<code>Field</code> handles, call <code>setAccessible(true)</code> once, or prefer <code>MethodHandle</code> / <code>LambdaMetafactory</code>-generated functional interfaces, which the JIT can treat as constants and inline."},
  {"sec":"JVM-PERF","front":"Why is throwing exceptions on a hot path expensive, and how do you remove the cost?","back":"The dominant cost is <strong>stack-trace fill</strong>: <code>Throwable</code>'s constructor calls <code>fillInStackTrace()</code>, which walks the entire call stack — O(depth) and uncached. For control-flow-style exceptions, override <code>fillInStackTrace()</code> to a no-op or use the protected <code>Throwable(msg, cause, enableSuppression, writableStackTrace=false)</code> constructor. The JVM also has <code>-XX:+OmitStackTraceInFastThrow</code> (on by default), which elides traces for repeated implicit exceptions like NPE/AIOOBE after C2 swaps in pre-allocated stackless instances — a debugging footgun."},
  {"sec":"JVM-PERF","front":"Monomorphic vs bimorphic vs megamorphic call sites — what changes for the JIT?","back":"<ul><li><strong>Monomorphic</strong> (1 receiver type seen): JIT inlines the target directly behind a cheap type guard — fastest.</li><li><strong>Bimorphic</strong> (2 types): inline cache holds both, still inlinable.</li><li><strong>Megamorphic</strong> (3+ types): the inline cache gives up; the call becomes a real virtual dispatch through the vtable (class) or itable (interface), blocking inlining and downstream optimization.</li></ul>Interface (itable) dispatch is the costliest. Keeping hot call sites mono/bimorphic is a key inlining lever."},
  {"sec":"JVM-PERF","front":"In JMH, what is a Blackhole and what failure does it prevent?","back":"A <strong>Blackhole</strong> consumes a computed value so the JIT cannot prove the result is unused and apply <strong>dead-code elimination</strong> (DCE), which would optimize your benchmark body to nothing and report absurd throughput. You either <code>return</code> the result (JMH sinks it) or call <code>bh.consume(x)</code>. Blackholes also defuse constant folding by treating inputs as opaque. Forgetting them is the #1 way to measure the empty loop."},
  {"sec":"JVM-PERF","front":"Why does a JMH benchmark need warmup iterations, and what are you waiting for?","back":"The JVM starts in the <strong>interpreter</strong>, then C1, then C2 under tiered compilation only after methods cross invocation/back-edge thresholds; profiles, inlining decisions, and branch predictions also need to stabilize. Measuring before steady state captures interpreter + compilation noise, not peak throughput. Warmup runs the code until the JIT has settled. Conversely, to study cold start you must deliberately constrain compilation (e.g. <code>-XX:TieredStopAtLevel=1</code> or <code>-Xint</code>) and measure that regime on purpose."},
  {"sec":"JVM-PERF","front":"Name three JMH-specific traps beyond missing blackholes.","back":"<ul><li><strong>Constant folding</strong>: literal/<code>final</code> inputs get folded — feed inputs via <code>@State</code> fields the JIT can't treat as compile-time constants.</li><li><strong>Loop optimization</strong>: hand-written loops inside <code>@Benchmark</code> get unrolled/vectorized unpredictably — let JMH drive iteration.</li><li><strong>Not forking</strong>: JMH forks each benchmark into a fresh JVM by default (<code>@Fork(5)</code>) so profile pollution (e.g. a call site made megamorphic by a prior benchmark) and residual static state don't bias results.</li></ul>"},
  {"sec":"JVM-PERF","front":"What does \"mechanical sympathy\" mean for a Java data-platform engineer in concrete terms?","back":"Writing code that works <em>with</em> the hardware: keep hot data in contiguous, cache-friendly layouts (arrays of primitives over arrays of objects to avoid pointer chasing), respect the 64-byte cache line (avoid false sharing), favor predictable branches and sequential access for the prefetcher, and minimize allocation to reduce GC and improve locality. The LMAX Disruptor (single-writer, padded sequence, ring buffer) is the canonical example."},
  {"sec":"JVM-PERF","front":"Why can a heap dump mislead you about allocation pressure, and what shows the truth?","back":"A heap dump / occupancy snapshot shows <em>live, surviving</em> objects at one instant — it hides short-lived garbage that dies before any GC, which is exactly what drives <strong>allocation rate</strong> and minor-GC frequency. Use an <em>allocation profiler</em> (JFR <code>jdk.ObjectAllocationSample</code> events or async-profiler <code>alloc</code> mode) to attribute allocated bytes to call sites over time. Occupancy answers \"what leaks\"; allocation profiling answers \"what churns.\""}
);

Object.assign(GLOSSARY_DATA, {
  "jfr": {"term":"Java Flight Recorder (JFR)","group":"JVM-PERF","aliases":["jfr","flight recorder","java flight recorder","jdk flight recorder"],"eli5":"A black-box flight recorder built into the JVM that continuously logs structured performance events at about 1% overhead.","memory":"JFR = the plane's flight recorder: always running, survives the crash, tells you what every subsystem was doing.","full":"<p>JFR is a low-overhead, always-on event-recording framework built into the HotSpot JVM (open-sourced in JDK 11; previously a commercial Oracle feature). It emits typed events — allocation samples, GC pauses, lock contention, I/O, exceptions, thread states, JIT compilation — into a ring buffer that can be dumped to a <code>.jfr</code> file and analyzed in JDK Mission Control or programmatically.</p><p>Its design goal is production safety: instrumentation is baked into the runtime and sampled, so the default profile targets roughly 1% overhead — cheap enough to leave on permanently. Start it with <code>-XX:StartFlightRecording</code> or attach via <code>jcmd &lt;pid&gt; JFR.start</code>. Because events carry timestamps and thread context, JFR's strength is <em>correlation</em> across subsystems, not just CPU sampling.</p>","trap":"Interviewers ask its overhead and want a number with a caveat: ~1% for the default profile, but custom high-frequency events or fine-grained allocation sampling can push it higher. \"Free\" is wrong.","worked":"<code>jcmd 12345 JFR.start name=prof settings=profile duration=60s filename=rec.jfr</code> captures a 60s profile including <code>jdk.ObjectAllocationSample</code> events you can flame-graph.","related":["async-profiler","allocation-profiling","safepoint-bias"]},
  "async-profiler": {"term":"async-profiler","group":"JVM-PERF","aliases":["async-profiler","async profiler","asyncgetcalltrace"],"eli5":"A low-overhead sampling profiler that grabs the real stack at the interrupted instruction, so flame graphs aren't lying to you.","memory":"\"async\" = it samples asynchronously via a signal, not at safepoints — that's the whole point and the source of its accuracy.","full":"<p>async-profiler is an external sampling profiler for the JVM that avoids safepoint bias by sampling on a signal (<code>SIGPROF</code> from a timer, or hardware <code>perf_events</code>) and walking the stack via HotSpot's <code>AsyncGetCallTrace</code>. Because it interrupts at the actual program counter rather than waiting for a safepoint, hot frames are attributed correctly. It can profile CPU, allocations (<code>alloc</code> via TLAB events), locks, and hardware counters, merging native and JIT-compiled frames into a single flame graph.</p><p>It is the go-to for drilling into a specific CPU or allocation hotspot. It needs no application code changes — attach as an agent or via <code>asprof</code> — and can emit collapsed-stack, flame-graph, or <strong>JFR</strong> output; its bundled <code>jfrconv</code> converter can also turn any <code>.jfr</code> file into a flame graph. The trade-off vs JFR is that it's an external profiling tool, not a broad correlated event stream.</p>","trap":"If asked \"why not just use jstack sampling?\" the answer is safepoint bias: jstack/JVMTI sampling can only see safepoints and systematically misattributes hot loops. async-profiler's AsyncGetCallTrace does not.","worked":"<code>asprof -d 30 -e alloc -f flame.html 12345</code> produces a 30s allocation flame graph showing which call sites allocate the most bytes.","related":["jfr","safepoint-bias","allocation-profiling"]},
  "safepoint-bias": {"term":"Safepoint Bias","group":"JVM-PERF","aliases":["safepoint bias","safepoint","safepoint-biased profiling"],"eli5":"Old Java profilers can only photograph a thread when it pauses at a safepoint, so the photos cluster in the wrong places.","memory":"Safepoint bias = you can only photograph the runner when he stops at a checkpoint, so you conclude he spends all his time at checkpoints.","full":"<p>A <strong>safepoint</strong> is a point in execution where a thread's state is fully known to the JVM (consistent stack maps), allowing stop-the-world operations like GC, deoptimization, and JVMTI stack capture. The JIT inserts safepoint polls at method entries/exits and loop back-edges, but not on every instruction — and it can elide them inside tight counted loops.</p><p><strong>Safepoint bias</strong> is the measurement error in profilers (anything built on <code>GetStackTrace</code>/JVMTI, including many older sampling tools) that can only sample at safepoints: the captured stacks cluster at safepoint-pollable locations rather than where the CPU actually spends time, misattributing or hiding hot code. Signal-based profilers using <code>AsyncGetCallTrace</code> (async-profiler) or hardware <code>perf</code> avoid it by sampling at the true interrupted instruction.</p>","trap":"A subtle senior gotcha: long time-to-safepoint (TTSP) is itself a perf/latency problem — a thread in a counted loop with elided polls can delay a GC because everyone waits for it to reach a safepoint. Bias and TTSP are two faces of the same mechanism.","related":["async-profiler","jfr"]},
  "allocation-profiling": {"term":"Allocation Profiling","group":"JVM-PERF","aliases":["allocation profiling","alloc profiling","tlab","allocation rate"],"eli5":"Measuring which code creates the most garbage, because allocation rate — not heap size — is what wakes up the garbage collector.","memory":"Allocation rate is the GC's heartbeat: more churn = faster heartbeat = more pauses. Profile the churn, not the snapshot.","full":"<p>Allocation profiling attributes <em>allocated bytes</em> to the call sites that produced them, over time. It is distinct from heap occupancy (a live-object snapshot): most allocation is short-lived garbage that never appears in a heap dump but still drives minor-GC frequency, survivor copying, and promotion. The cheapest GC is the object you never allocate, so reducing allocation rate on hot paths is often the highest-leverage GC tuning available.</p><p>Mechanically, allocation is sampled at <strong>TLAB</strong> (Thread-Local Allocation Buffer) boundaries: each thread bump-allocates within its private TLAB, and the JVM records a sampled event when the buffer is refilled (or on the slow path outside a TLAB). JFR exposes <code>jdk.ObjectAllocationSample</code>; async-profiler exposes <code>alloc</code> mode. Both keep overhead low by sampling refills rather than every allocation.</p>","trap":"\"Allocation is cheap so it doesn't matter\" is the trap. The allocation is cheap; the downstream GC it causes is not. And escape analysis may or may not eliminate it — never assume it does on the hot path.","worked":"A logging line building a <code>String</code> per request via concatenation can dominate an allocation flame graph even though zero of those strings survive to a heap dump.","related":["jfr","async-profiler","escape-analysis"]},
  "false-sharing": {"term":"False Sharing & @Contended","group":"JVM-PERF","aliases":["false sharing","@contended","contended","cache line","cache-line padding"],"eli5":"Two threads writing two unrelated variables that happen to sit on the same 64-byte cache line keep stealing the line from each other.","memory":"False sharing = two people editing different paragraphs but the editor only checks out the whole page at once, so they keep grabbing it back from each other.","full":"<p>CPUs move memory in <strong>cache lines</strong> (64 bytes on mainstream x86/ARM). The cache-coherence protocol (MESI-family) tracks ownership per line, not per variable. <strong>False sharing</strong> occurs when two threads write two logically independent fields that reside on the same line: each write forces the other core's copy into Invalid, causing the line to ping-pong between cores' caches (HITM events) and serializing what should be parallel work — pure mechanical-sympathy waste.</p><p>The fix is to ensure hot, independently-written fields occupy distinct cache lines. Manual padding works but is brittle (the JIT can reorder/elide fields); the supported tool is <code>@jdk.internal.vm.annotation.Contended</code>, which instructs the JVM to pad the field (or named group) so it sits alone (<code>-XX:ContendedPaddingWidth</code> default 128 bytes, two lines to also defeat adjacent-line prefetch). User-code use requires <code>-XX:-RestrictContended</code>. Detect false sharing with <code>perf c2c</code>.</p>","trap":"The word \"false\": there is zero logical contention — the variables are unrelated. The contention is an artifact of cache-line granularity. If two threads share the <em>same</em> variable, that's true sharing, a different problem.","worked":"<code>LongAdder</code> beats a single <code>AtomicLong</code> under contention by spreading counts across <code>@Contended</code>-padded cells on separate cache lines, so threads update different cells instead of fighting over one hot counter's cache line.","related":["mechanical-sympathy"]},
  "megamorphic-call-site": {"term":"Megamorphic Call Sites","group":"JVM-PERF","aliases":["megamorphic","monomorphic","bimorphic","inline cache","polymorphic call site","devirtualization"],"eli5":"A virtual call the JIT can inline when it sees one or two receiver types, but gives up on once it sees many.","memory":"Mono = 1, Bi = 2, Mega = many. The inline cache holds up to two; the third type breaks the camel's back and dispatch goes virtual.","full":"<p>At a virtual/interface call site, the JIT maintains a type profile and an <strong>inline cache</strong>. With one observed receiver type the site is <strong>monomorphic</strong>: the JIT speculatively inlines the single target behind a cheap class-check guard. With two types it is <strong>bimorphic</strong> (still inlinable). At three-plus types the site becomes <strong>megamorphic</strong>: the inline cache gives up and the call falls back to a real dynamic dispatch through the vtable (class) or itable (interface), which cannot be inlined.</p><p>This matters because inlining is the gateway optimization — it enables escape analysis, constant propagation, and loop optimizations across the call boundary. A megamorphic hot call site blocks all of that and adds dispatch overhead; interface (itable) dispatch is the worst — its stub does a linear walk and lacks a fixed vtable slot, with poor branch prediction. Keeping hot call sites mono/bimorphic — by reducing the number of concrete types reaching them — is a real, often-overlooked perf lever.</p>","trap":"The benchmarking gotcha: a microbenchmark in one JVM that only ever sees one implementation reports monomorphic speed; production with many implementations is megamorphic and far slower. JMH forking + a realistic type mix is needed to catch this (profile pollution).","worked":"A generic <code>Comparator</code> interface call hit by 5 lambda implementations in a hot sort is megamorphic; specializing the call site to one comparator type lets C2 inline and vectorize it.","related":["escape-analysis","jmh","mechanical-sympathy"]},
  "jmh": {"term":"JMH (Java Microbenchmark Harness)","group":"JVM-PERF","aliases":["jmh","java microbenchmark harness","blackhole","dead-code elimination","dce"],"eli5":"The only sane way to microbenchmark Java, because it stops the JIT from optimizing your benchmark into nothing.","memory":"JMH's three guardians: Warmup (reach JIT steady state), Blackhole (defeat DCE), Fork (defeat profile pollution). Skip any one and the number is fiction.","full":"<p>JMH is the OpenJDK harness for microbenchmarks. It exists because naive timing loops in Java are almost always wrong: the JIT performs <strong>dead-code elimination</strong> (deletes results you never use), constant folding (precomputes results from literal/final inputs), and loop unrolling/vectorization that distort hand-rolled loops. JMH manages <strong>warmup</strong> (running until tiered compilation reaches steady state), <strong>forking</strong> (each benchmark in a fresh JVM, default <code>@Fork(5)</code>, to avoid profile pollution between benchmarks), and measurement iterations with proper statistics.</p><p>Its core defenses are <strong>Blackholes</strong> (consume results so DCE can't fire — return the value or call <code>bh.consume()</code>) and <code>@State</code>-provided inputs (so the JIT can't treat them as compile-time constants). Annotations like <code>@BenchmarkMode</code>, <code>@Warmup</code>, <code>@Measurement</code>, and <code>@Fork</code> control the protocol.</p>","trap":"\"I called the method 1e9 times in a for-loop and divided\" is the classic failed benchmark — DCE and constant folding make it measure an empty loop. Also: not forking lets a prior benchmark make a call site megamorphic, biasing the next one.","worked":"Without a Blackhole, a <code>@Benchmark</code> that computes <code>Math.sqrt(2)</code> and drops the result is deleted entirely by DCE; returning it (or <code>bh.consume</code>) forces real work to be measured.","related":["megamorphic-call-site","mechanical-sympathy","safepoint-bias"]},
  "mechanical-sympathy": {"term":"Mechanical Sympathy","group":"JVM-PERF","aliases":["mechanical sympathy","cache-friendly","data locality","lmax disruptor"],"eli5":"Writing code that cooperates with how the CPU and memory actually work, instead of fighting them.","memory":"Drive the hardware like a sympathetic race driver: smooth, sequential, no cache-line ping-pong, no surprise allocations.","full":"<p>Mechanical sympathy (term popularized by Martin Thompson / LMAX) is the discipline of designing software with awareness of the underlying hardware: the memory hierarchy (L1/L2/L3/RAM latency cliffs), 64-byte cache lines, hardware prefetchers that reward sequential access, branch predictors that reward predictable branches, and the cost of pointer chasing through scattered heap objects.</p><p>For a data-platform engineer this translates to concrete choices: prefer arrays of primitives (or columnar/struct-of-arrays layouts) over arrays of object references to keep data contiguous and avoid cache misses; minimize allocation to reduce GC and improve locality; avoid false sharing on hot concurrent fields; and keep hot call sites monomorphic so the JIT can inline. The LMAX Disruptor — a single-writer ring buffer with cache-line-padded sequence counters and no locks — is the canonical embodiment.</p>","trap":"It is not premature micro-optimization — interviewers want you to apply it only to the measured hot path. The sequence is always: profile first, then apply mechanical sympathy where the data says it pays off.","worked":"Iterating a <code>long[]</code> of 10M values is far faster than a <code>List&lt;Long&gt;</code> of the same size: the array is contiguous and prefetch-friendly, while the boxed list is a scatter of heap objects causing a cache miss per element (pointer chasing).","related":["false-sharing","megamorphic-call-site","allocation-profiling"]}
});
