/* Data Engineering for Trade Ops (IP-DATAENG) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"IP-DATAENG","front":"Walk me through the end-to-end data flow for trade capture, from exchange to reporting.","back":"<strong>Exchange &rarr; Capture &rarr; Ledger/Position store &rarr; Reconciliation &rarr; Reporting.</strong><ul><li><strong>Ingest:</strong> consume fills from the venue &mdash; <code>FIX</code> drop-copy (<code>35=8</code> ExecutionReports) for equities/derivs, REST+WebSocket for crypto/prediction venues.</li><li><strong>Capture (OLTP):</strong> write each fill to the trade-capture DB on a stable key (<code>provider_trade_id</code>); the system of record for what we think happened.</li><li><strong>Ledger/position store:</strong> apply fills to running positions and cash; an <strong>event-driven</strong> Kafka consumer fans fills into the ledger <em>idempotently</em>.</li><li><strong>Reconciliation:</strong> match internal records vs an <strong>external truth</strong> (clearing/custodian SOD files, exchange/processor settlement files, on-chain data); unmatched = <strong>breaks</strong>.</li><li><strong>Reporting (OLAP):</strong> ETL/ELT into a warehouse (Snowflake) for P&amp;L, recon dashboards, regulatory reporting.</li></ul>Under <strong>T+1</strong> (US, since May 28 2024) this whole chain must clear in a compressed intraday post-trade window (roughly 2&ndash;5 hrs), so each hop is automated and monitored."},
  {"sec":"IP-DATAENG","front":"ETL vs ELT &mdash; which would you choose for trade/position data and why?","back":"<strong>ETL</strong> = Extract, Transform, then Load (transform on the way in, on a separate engine like Spark). <strong>ELT</strong> = Extract, Load raw, then Transform inside the warehouse (e.g. <code>dbt</code> on Snowflake).<ul><li><strong>ELT</strong> for the analytics/recon warehouse: land raw fills/SOD files immediately (cheap, full audit trail), transform with SQL where compute scales. Lets you <em>replay/re-derive</em> when reconciliation logic changes.</li><li><strong>ETL</strong> when you must validate/cleanse/PII-scrub or conform schema <em>before</em> it touches the store, or for legacy targets without compute.</li></ul>For trade ops I lean <strong>ELT into Snowflake</strong> &mdash; landing raw lets us re-run a recon for a prior day without re-pulling the source, which matters when a break is found late. Keep the raw layer immutable; build conformed/marts on top."},
  {"sec":"IP-DATAENG","front":"OLTP vs OLAP &mdash; how do they map onto a trade-ops stack?","back":"<ul><li><strong>OLTP</strong> (Online Transaction Processing) = the <strong>trade-capture DB</strong>. Row-oriented, normalized, low-latency single-row writes/reads, ACID. Handles high-frequency fill inserts and position updates. Postgres/MySQL.</li><li><strong>OLAP</strong> (Online Analytical Processing) = the <strong>warehouse</strong> (Snowflake/Redshift/BigQuery). Columnar, denormalized, scans huge ranges for aggregation &mdash; P&amp;L, recon reports, break analytics.</li></ul><strong>Why separate:</strong> running heavy analytical scans on the OLTP box would contend with the latency-critical capture path. You move data OLTP&rarr;OLAP via <strong>CDC</strong> or batch ETL. Rule of thumb: <em>OLTP answers \"what is this one trade right now\"; OLAP answers \"what happened across all trades.\"</em>"},
  {"sec":"IP-DATAENG","front":"Design an event-driven trade-capture pipeline with Kafka. What are the key components?","back":"<ul><li><strong>Producer:</strong> the gateway consuming venue fills (FIX drop-copy / WS) publishes one event per fill to a Kafka topic <code>fills</code>, keyed by instrument or account so a symbol's events stay ordered within a partition.</li><li><strong>Topic/partitions:</strong> partitioning gives parallelism + per-key ordering. Retention lets you <strong>replay</strong>.</li><li><strong>Consumers:</strong> a ledger consumer applies fills to positions; a recon consumer lands them for matching; a reporting sink writes to the warehouse.</li><li><strong>Delivery:</strong> Kafka is <strong>at-least-once</strong> by default &mdash; consumers <em>must be idempotent</em> (dedupe on <code>provider_trade_id</code>) so a redelivered fill doesn't double-book a position.</li></ul>Benefits: decoupling (add a consumer without touching capture), buffering bursts, and replayability for recovery."},
  {"sec":"IP-DATAENG","front":"At-least-once vs exactly-once vs at-most-once delivery &mdash; which do you target for fills-to-ledger?","back":"<ul><li><strong>At-most-once:</strong> deliver, maybe lose. Unacceptable &mdash; a dropped fill means a wrong position.</li><li><strong>At-least-once:</strong> never lose, may duplicate. The pragmatic default.</li><li><strong>Exactly-once:</strong> each event applied once. Kafka offers EOS (transactions + idempotent producer) <em>within Kafka</em>, but a ledger write hits an external DB, so true end-to-end exactly-once is hard.</li></ul><strong>The real-world answer:</strong> use <strong>at-least-once delivery + idempotent consumers</strong> &rarr; <em>effectively-once</em>. Dedupe on <code>provider_trade_id</code> with an upsert or a processed-events table. Booking a position is not naturally idempotent (<code>pos += qty</code> on replay double-counts), so the dedupe key is what makes it safe."},
  {"sec":"IP-DATAENG","front":"How do you make a Kafka consumer idempotent so a redelivered fill doesn't double-book?","back":"Persist a <strong>dedupe key</strong> and check-then-apply transactionally. The fill's <code>provider_trade_id</code> is the natural key.<br><code>INSERT INTO processed_fills(trade_id) VALUES (%s)<br>ON CONFLICT (trade_id) DO NOTHING;<br>-- if 0 rows inserted, we've seen it: skip</code><br>Then apply the effect in the same transaction:<br><code>INSERT INTO positions(acct,sym,qty) VALUES (...)<br>ON CONFLICT (acct,sym) DO UPDATE SET qty = positions.qty + EXCLUDED.qty</code> &mdash; safe only because you <em>first</em> gated on the trade_id.<ul><li>Do the dedupe-insert and the ledger mutation in <strong>one DB transaction</strong>; commit the Kafka offset only after.</li><li>Same idea as the <strong>idempotency-key</strong> pattern on the write path &mdash; store the key, skip/return on replay.</li></ul>"},
  {"sec":"IP-DATAENG","front":"Batch vs streaming for trade-ops data &mdash; when each?","back":"<ul><li><strong>Streaming</strong> (Kafka, Flink, Spark Structured Streaming): real-time position keeping, intraday risk, live break alerting. Essential for <strong>24/7 crypto</strong> markets and when T+1 leaves no slack to wait for a nightly job.</li><li><strong>Batch</strong> (scheduled Spark/SQL jobs): end-of-day reconciliation against the <strong>SOD file</strong> the custodian/clearer drops, regulatory reports, backfills. Naturally aligned to file-based truth sources that arrive on a cadence.</li></ul>Typical hybrid: <em>stream</em> fills into the ledger all day for live positions, then run a <em>batch</em> recon against the external settlement/SOD file. Grounding example: Devexperts syncs platform records vs Apex clearing SOD files &mdash; that's a batch recon."},
  {"sec":"IP-DATAENG","front":"What is Change Data Capture (CDC) and where would you use it in trade ops?","back":"<strong>CDC</strong> streams row-level changes (inserts/updates/deletes) out of a source DB <em>as events</em>, typically by reading the transaction log (e.g. Debezium reading the Postgres WAL), rather than polling.<ul><li><strong>Use:</strong> propagate the OLTP trade-capture DB into Kafka &rarr; warehouse without heavy batch ETL or load on the source; keeps the OLAP recon store near-real-time.</li><li><strong>Log-based</strong> CDC captures every change in order, including deletes, and won't miss fast intermediate states a polling query would skip.</li></ul><strong>Why it matters here:</strong> it's how you get OLTP&rarr;OLAP continuously, enabling streaming recon instead of waiting for nightly extracts &mdash; valuable under T+1's compressed window. Watch for schema-evolution handling on the CDC stream."},
  {"sec":"IP-DATAENG","front":"How would you reconcile internal fills against an exchange/clearing settlement file?","back":"The recon primitive: <strong>compare internal vs external truth, match on stable identifiers, tolerate expected discrepancies, surface the rest as breaks.</strong><ul><li><strong>Match key:</strong> join on (provider/trade id, symbol, amount, currency, settlement date).</li><li><strong>Tolerate:</strong> timing gaps, partial fills, fees, rounding, chargebacks &mdash; within tolerance, not a break.</li><li><strong>Breaks:</strong> internal-only (we booked, they didn't), external-only (they have it, we don't), or value mismatch.</li></ul><code>m = internal.merge(external, on='trade_id', how='outer', indicator=True)<br>internal_only = m[m._merge=='left_only']<br>external_only = m[m._merge=='right_only']<br>mismatch = m[(m._merge=='both') &amp; (m.amt_x.sub(m.amt_y).abs() &gt; TOL)]</code><br>Breaks route to tooling for manual/bulk correction. Under T+1, unresolved breaks become settlement <strong>fails</strong>."},
  {"sec":"IP-DATAENG","front":"Show idiomatic pandas to find reconciliation breaks between two DataFrames.","back":"<strong>Outer merge with an indicator, then classify.</strong><br><code>m = internal.merge(<br>&nbsp;&nbsp;external, on=['trade_id'], how='outer',<br>&nbsp;&nbsp;suffixes=('_int','_ext'), indicator=True)<br><br># missing on each side<br>int_only = m[m['_merge']=='left_only']<br>ext_only = m[m['_merge']=='right_only']<br><br># value breaks within matched, beyond tolerance<br>both = m[m['_merge']=='both']<br>amt_break = both[(both.amt_int - both.amt_ext).abs() &gt; 0.01]</code><ul><li>Normalize first: cast dtypes, round amounts, <code>.str.strip()</code> ids, align timezones &mdash; most \"breaks\" are formatting.</li><li>For 1-to-many fills, <code>groupby('order_id').agg({'qty':'sum'})</code> before matching.</li><li>Use a <strong>tolerance</strong> on floats; never <code>==</code> on money.</li></ul>"},
  {"sec":"IP-DATAENG","front":"Data quality and validation in a trade pipeline &mdash; what do you check and where?","back":"Validate <strong>at ingest</strong> (fail fast, quarantine) and <strong>at recon</strong> (cross-source).<ul><li><strong>Schema/type:</strong> required fields present, correct types, enum membership (side &isin; {buy,sell}).</li><li><strong>Referential:</strong> symbol exists, account known, venue recognized.</li><li><strong>Business rules:</strong> price &gt; 0; for <strong>prediction markets</strong> price &isin; <code>[0.00, 1.00]</code> (= implied probability, settles $1/$0); qty within limits; settlement date valid.</li><li><strong>Completeness/freshness:</strong> expected fill count vs FIX session sequence numbers; no gaps; file arrived on time.</li><li><strong>Uniqueness:</strong> no duplicate <code>trade_id</code>.</li></ul>Bad records go to a <strong>dead-letter / quarantine</strong> table with the reason, never silently dropped &mdash; a dropped fill is a phantom position. Tools: Great Expectations, dbt tests, or explicit checks. <em>Validation failures are themselves a break class.</em>"},
  {"sec":"IP-DATAENG","front":"What is schema evolution and how do you handle it on a Kafka/warehouse pipeline?","back":"<strong>Schema evolution</strong> = the message/table shape changes over time (new venue adds a field, a type widens) without breaking existing producers/consumers.<ul><li><strong>Schema registry</strong> (Avro/Protobuf) enforces <strong>compatibility</strong>: <em>backward</em> (new consumer reads old data), <em>forward</em> (old consumer reads new data), <em>full</em> (both).</li><li><strong>Safe changes:</strong> add optional fields with defaults; never rename or repurpose a field; never drop a field consumers rely on.</li><li><strong>Warehouse:</strong> land raw/JSON variant + use schema-on-read, or use additive DDL so old queries still run.</li></ul><strong>Why it bites trade ops:</strong> you integrate many heterogeneous venues (equities FIX, crypto REST, prediction-market APIs) each evolving independently &mdash; a breaking change must not halt fill capture. Default to additive, backward-compatible changes."},
  {"sec":"IP-DATAENG","front":"Explain idempotency keys and how they apply to sending order/settlement instructions.","back":"Pattern (the community/IETF <strong>idempotency-key</strong> convention, popularized by Stripe): store <code>(caller, idempotency_key) &rarr; request_hash, response, status</code>.<ul><li><strong>Same key + same payload</strong> &rarr; return the stored original response (including the original error). The retry is a no-op.</li><li><strong>Same key + different payload</strong> &rarr; <code>409 Conflict</code> &mdash; you're reusing a key for a different request, a bug.</li></ul><strong>Why it matters:</strong> exchange APIs and settlement instructions need safe <em>retries</em> under network failure. A retried order must not place two orders. Use a <strong>client order id</strong> as the key; the venue dedupes. Same primitive as Kafka consumer dedupe &mdash; <em>store the key, replay returns the original outcome</em>. This is exactly the payments&rarr;trade-ops mapping: idempotent writes are the same in billing and order entry."},
  {"sec":"IP-DATAENG","front":"How does FIX drop-copy fit the data-engineering picture for trade capture?","back":"<strong>FIX drop-copy</strong> is a read-only session that pushes <code>35=8</code> ExecutionReports for your flow &mdash; the standard <strong>trade-capture/recon feed</strong>, exposed even by crypto-derivs venues (Coinbase Derivatives, Zero Hash).<ul><li>Distinguish exec types via <code>ExecType (150)</code>: Trade <code>150=F</code>, Trade Correction <code>150=G</code>, Trade Cancel <code>150=H</code>.</li><li>FIX is <strong>session-based with sequence numbers</strong> &mdash; a gap triggers a <code>ResendRequest (35=2)</code>; the pipeline must detect missing seq nums (a completeness check) and replay.</li><li>Corrections/cancels mean your ledger must be <strong>append-and-supersede</strong>, not overwrite &mdash; book the correction as a new event keyed to the original.</li></ul>Engineering-wise: a FIX engine producing into Kafka, parsing <code>tag=value</code>, with idempotent downstream consumers. Crypto/prediction venues are mostly REST+WebSocket instead, so the pipeline normalizes both into one internal fill schema."},
  {"sec":"IP-DATAENG","front":"What's different about reconciliation for crypto vs prediction markets vs equities?","back":"<ul><li><strong>Equities:</strong> external truth = <strong>DTCC</strong> &mdash; ITP affirmation (\"match-to-instruct\") gates NSCC (the CCP: multilateral netting, novation, guarantee via CNS) and DTC (the central securities depository: book-entry settlement &amp; custody). SOD files from clearer/custodian. T+1 since May 28 2024.</li><li><strong>Crypto:</strong> truth = <strong>on-chain data</strong>; no central depository. Recon must handle multiple chains, gas fees, address/wallet management, <strong>reorgs</strong> (finality is probabilistic), hot vs cold custody, 24/7 markets. 2026: stablecoins/tokenized deposits as the on-chain <em>cash leg</em>.</li><li><strong>Prediction markets:</strong> binary $0&ndash;$1 event contracts settle to <strong>event resolution</strong> ($1 if it occurs, else $0) &mdash; truth is an oracle/resolution, creating disputed-outcome and oracle risk plus Kalshi-vs-Polymarket multi-platform arb. Kalshi = CFTC-regulated DCM (designated 2020); Polymarket became a CFTC-regulated US exchange via Amended Order of Designation Nov 25 2025.</li></ul>The recon <em>primitive</em> is identical; the <strong>truth source and tolerance rules</strong> differ per asset."},
  {"sec":"IP-DATAENG","front":"How does T+1 settlement change the engineering requirements for these systems?","back":"<strong>T+1</strong> (US equities, corporate &amp; municipal bonds, UITs, ADRs, ETFs &mdash; effective <strong>May 28, 2024</strong>; EU &amp; UK align <strong>Oct 11, 2027</strong>) compresses the post-trade window to roughly <strong>2&ndash;5 hours</strong> on trade day.<ul><li><strong>Data management becomes the #1 challenge</strong> &mdash; no time for manual fixes; <strong>STP</strong> (straight-through processing) and automation are mandatory.</li><li>Affirmation/matching must happen <em>same day</em>; DTCC ITP \"match-to-instruct\" feeds NSCC clearing and DTC settlement.</li><li>Pushes recon from <strong>batch toward streaming</strong> &mdash; you can't wait for a nightly job.</li><li><strong>Unresolved exceptions become settlement fails</strong> (cost, penalties, risk), so break detection must be fast and break management near-real-time.</li></ul>Engineering takeaway: low-latency capture, idempotent retries, automated exception routing, and continuous (CDC/stream) recon instead of EOD-only."},
  {"sec":"IP-DATAENG","front":"Why does a payments/ledger/billing background map onto post-trade trade-ops automation?","back":"The primitives are the <strong>same system</strong> wearing different domain clothes.<ul><li><strong>Idempotency keys</strong> &mdash; safe retry of a charge == safe retry of an order/settlement instruction (<code>(caller, key) &rarr; response/status</code>, 409 on payload mismatch).</li><li><strong>Reconciliation against an external truth</strong> &mdash; ledger vs processor settlement file == internal fills vs clearing SOD / on-chain data; match on stable ids, tolerate fees/timing, surface breaks.</li><li><strong>Exception handling</strong> &mdash; payment disputes/chargebacks == trade breaks/fails: detect, route, resolve fast.</li><li>Same infra: <strong>Kafka</strong> (event-driven capture), <strong>Spark/Snowflake</strong> (recon analytics), <strong>AWS</strong>, at-least-once + idempotent consumers.</li></ul>So a Python/Java engineer who built a billing/ledger system has already built post-trade automation &mdash; the <em>vocabulary</em> changes (fills, breaks, T+1), not the engineering."}
);

Object.assign(GLOSSARY_DATA, {
  "kafka-trade-capture": {"term":"Event-driven trade capture (Kafka)","group":"IP-DATAENG","eli5":"Each fill becomes a message on a queue that any number of systems can consume independently.","memory":"One fill in, many consumers out &mdash; publish once, react everywhere.","full":"<p>A producer publishes one event per fill to a Kafka topic, keyed (e.g. by symbol/account) so a key's events stay ordered within a partition. Independent consumers apply fills to the ledger, land them for recon, and sink to the warehouse &mdash; decoupled, buffered against bursts, and <strong>replayable</strong> from retained offsets.</p><p>Kafka delivers <strong>at-least-once</strong> by default, so every consumer must be idempotent to avoid double-booking a position on redelivery.</p>","trap":"Assuming Kafka gives exactly-once for free &mdash; end-to-end exactly-once against an external ledger DB requires idempotent consumers, not just Kafka EOS.","worked":"FIX drop-copy <code>35=8</code> reports are parsed and published to topic <code>fills</code>; a ledger consumer upserts positions keyed on <code>provider_trade_id</code>.","related":["delivery-semantics","idempotent-consumer","oltp-vs-olap"]},
  "delivery-semantics": {"term":"At-least-once / exactly-once / at-most-once","group":"IP-DATAENG","eli5":"Guarantees about whether a message can be lost or duplicated when delivered.","memory":"At-least-once = never lose, maybe dupe; exactly-once = the hard ideal; at-most-once = maybe lose.","full":"<p><strong>At-most-once</strong> may drop messages; <strong>at-least-once</strong> never drops but may duplicate; <strong>exactly-once</strong> applies each event precisely once. Streaming systems default to at-least-once because true end-to-end exactly-once across an external sink is expensive/hard.</p><p>For fills-to-ledger, the practical target is <strong>at-least-once delivery + idempotent consumers = effectively-once</strong>: duplicates are harmless because the consumer dedupes on a stable key.</p>","trap":"Choosing at-most-once for performance &mdash; a dropped fill is a phantom/incorrect position, which is unacceptable.","worked":"A consumer re-reads a fill after a crash before its offset committed; the dedupe table already has the <code>trade_id</code>, so the second apply is skipped.","related":["kafka-trade-capture","idempotent-consumer","idempotency-key"]},
  "cdc": {"term":"Change Data Capture (CDC)","group":"IP-DATAENG","eli5":"Streaming every row change out of a database as events by reading its transaction log.","memory":"Tail the log, not poll the table.","full":"<p><strong>CDC</strong> emits inserts/updates/deletes from a source DB as a change stream, usually log-based (e.g. Debezium reading the Postgres WAL) rather than polling. It propagates the OLTP trade-capture DB into Kafka and onward to the warehouse with low source load and full ordering, including deletes.</p><p>It's how you get continuous OLTP&rarr;OLAP movement, enabling near-real-time recon instead of nightly extracts &mdash; valuable under T+1's compressed window.</p>","trap":"Equating CDC with periodic polling &mdash; polling misses fast intermediate states and deletes; log-based CDC captures every change in order.","worked":"Debezium publishes each new fill row to Kafka the instant it commits in Postgres, keeping the Snowflake recon model seconds-fresh.","related":["oltp-vs-olap","schema-evolution","kafka-trade-capture"]},
  "dtcc-itp-nscc-dtc": {"term":"DTCC: ITP / NSCC / DTC","group":"IP-DATAENG","eli5":"The three DTCC pieces that match, clear, and settle US equity trades after execution.","memory":"ITP matches, NSCC nets/guarantees (CCP), DTC settles/holds (depository).","full":"<p>DTCC runs the US post-trade rails through three roles. <strong>ITP</strong> (Institutional Trade Processing) handles pre-settlement matching/affirmation &mdash; \"match-to-instruct\"; affirmation gates downstream settlement. <strong>NSCC</strong> (National Securities Clearing Corp) is the <em>CCP</em>: multilateral netting, novation, and a settlement guarantee via Continuous Net Settlement (CNS). <strong>DTC</strong> (Depository Trust Company) is the central securities depository: book-entry settlement and custody.</p><p>Under T+1, same-day affirmation through ITP is what lets NSCC/DTC settle the next morning.</p>","trap":"Conflating the three &mdash; NSCC is the clearer/CCP (netting, guarantee), DTC is the depository (custody/settlement); they are distinct legal entities.","worked":"An affirmed institutional trade flows ITP &rarr; NSCC (netted into a single CNS obligation) &rarr; DTC (book-entry delivery vs payment).","related":["t-plus-one","reconciliation","settlement-fail"]}
});
