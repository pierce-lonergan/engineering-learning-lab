/* Teach me from the ground up: FE-HTML (auto-assembled) */
enh({
  "accessibility-tree": {"teach":"<p>At its core, the accessibility tree is just a <strong>second copy of your page written for software that can't see it</strong> — a screen reader, a voice-control tool, a braille display. The browser builds it automatically, and it describes every meaningful thing on the page in three words: <em>what is this, what is it called, and what state is it in?</em></p><h4>Where it comes from</h4><p>The browser starts with the DOM (your HTML structure) plus the CSS, and from those it <strong>derives</strong> a parallel tree. Each node it keeps gets four things:</p><ul><li><strong>Role</strong> — the kind of thing it is: button, link, heading, checkbox.</li><li><strong>Accessible name</strong> — what it's called out loud (\"Submit\"). This is computed by the <em>accname algorithm</em>, which gathers text from labels, inner text, and ARIA attributes in a defined priority order.</li><li><strong>State</strong> — checked, expanded, disabled, selected.</li><li><strong>Properties</strong> — extra facts like \"required\" or \"has a popup\".</li></ul><p>The browser then hands this tree to the operating system's accessibility API — <code>UIA</code> on Windows, <code>AX</code> on macOS, <code>AT-SPI</code> on Linux — and assistive technologies read from there. Your page never talks to the screen reader directly; it talks through this tree.</p><div class=\"analogy\"><p>Think of a museum. The paintings are your visual page. The accessibility tree is the <strong>audio-guide script</strong> the curator writes: \"Exhibit 4: a button labelled 'Buy ticket', currently enabled.\" A blind visitor never sees the painting — they experience the script. If the script is wrong, the visitor is misled even though the painting is gorgeous.</p></div><h4>Why native elements win</h4><p>When you use a real <code>&lt;button&gt;</code>, the browser fills in the tree <em>for free</em>: role button, focusable, Enter/Space activate it, disabled state announced. Choose the right native element and the audio-guide script writes itself, correctly.</p><h4>The ARIA trap</h4><p>ARIA attributes only <strong>edit the tree</strong> — they patch the role, name, or state. They add <em>zero</em> behavior: no keyboard handling, no focus, no styling. So a <code>&lt;div role=\"button\"&gt;</code> announces \"button\" but does nothing when you press Enter unless you wire it up by hand: <code>tabindex=0</code>, a <code>keydown</code> listener for Enter <em>and</em> Space, and <code>aria-disabled</code> for the disabled state. Worse, a wrong role <em>silently overrides</em> correct native semantics — slap <code>role=\"heading\"</code> on a real button and you've broken it for AT while it still looks fine on screen.</p><div class=\"key\"><p>The accessibility tree is what assistive tech actually consumes. Native elements populate it correctly and automatically; ARIA only <em>edits</em> it and never adds behavior — so the right HTML element beats a div-plus-ARIA reconstruction every time.</p></div>"},
  "semantic-elements": {"teach":"<p>At its core, a semantic element is just an HTML tag whose <strong>name tells you what the content means</strong>, not how it looks. A <code>&lt;nav&gt;</code> says \"this is navigation.\" A <code>&lt;div&gt;</code> says nothing — it's a blank box.</p><h4>The meaning-bearing tags</h4><p>HTML gives you a vocabulary for structure: <code>&lt;article&gt;</code> (a self-contained piece, like a blog post), <code>&lt;section&gt;</code> (a thematic grouping), <code>&lt;nav&gt;</code> (navigation links), <code>&lt;main&gt;</code> (the page's unique content), <code>&lt;aside&gt;</code> (tangential content), <code>&lt;header&gt;</code>/<code>&lt;footer&gt;</code>, <code>&lt;figure&gt;</code>, and <code>&lt;time&gt;</code>. Several map to <strong>landmark roles</strong> — <code>nav</code>→navigation, <code>main</code>→main, <code>aside</code>→complementary — and screen-reader users navigate by jumping between landmarks the way you'd skim a page by its headings. They also help search engines understand the page and make your markup self-documenting for the next developer.</p><div class=\"analogy\"><p>Imagine moving into a house where every room is an identical unlabelled cube — that's a page built entirely of <code>&lt;div&gt;</code>s. Now imagine a house where rooms are labelled \"kitchen,\" \"bedroom,\" \"entrance hall.\" A guest (or a screen-reader user) can find what they need instantly. Semantic tags are those labels: they let someone navigate by purpose, not by guesswork.</p></div><h4>div and span: the honest blanks</h4><p><code>&lt;div&gt;</code> and <code>&lt;span&gt;</code> are <strong>semantically neutral</strong> — and that's a feature, not a failure. When you genuinely just need to group two columns for layout, <code>&lt;div class=\"flex-row\"&gt;</code> is exactly right. There's no semantic element for \"a box I'm flexing,\" so reaching for a meaningful tag there would be lying.</p><h4>The senior judgment</h4><p>The skill isn't \"always use semantic tags\" — it's knowing when a div is genuinely correct versus a missed opportunity. And there's a sharp trap: <strong>reaching for <code>&lt;section&gt;</code> or <code>&lt;article&gt;</code> as generic containers</strong>. A <code>&lt;section&gt;</code> with no accessible name is no more useful to assistive tech than a plain div — it just adds a region the user has to wade through. So if you use <code>&lt;section&gt;</code>, give it a name (usually via a heading and <code>aria-labelledby</code>, or <code>aria-label</code>). Misused semantics don't just waste markup; they actively mislead screen-reader users into thinking structure exists where it doesn't.</p><p>Practical examples: <code>&lt;nav aria-label=\"Primary\"&gt;</code> for the main menu (the label distinguishes it from other navs), <code>&lt;main&gt;</code> for the one unique content region per page, and <code>&lt;div class=\"flex-row\"&gt;</code> purely to lay out columns.</p><div class=\"key\"><p>Semantic elements communicate <em>meaning</em>, powering landmarks, SEO, and readable code — but they earn their keep only when used honestly. A nameless <code>&lt;section&gt;</code> is just a div in disguise, and a div is the right answer for pure layout.</p></div>"},
  "document-outline": {"teach":"<p>At its core, the document outline is just the <strong>table of contents a screen reader builds automatically from your headings</strong> — and blind users lean on it constantly to jump around a page instead of reading top to bottom.</p><h4>How users actually move</h4><p>A sighted reader skims by glancing at big bold text. A screen-reader user can't glance, so they press a key to hop heading-to-heading. That list of headings <em>is</em> their map of the page. If your headings are a coherent nested structure, they navigate effortlessly; if they're a mess, the map lies.</p><h4>The rules of a good outline</h4><ul><li>Exactly one <code>&lt;h1&gt;</code> — the page title.</li><li>Then <code>&lt;h2&gt;</code> through <code>&lt;h6&gt;</code> in sequence, <strong>never skipping a level</strong>. h2 → h3 → h2 is fine; h2 → h4 is broken.</li><li><strong>Level is structure; size is CSS.</strong> Never pick <code>&lt;h4&gt;</code> because you wanted small text — pick the level that's structurally correct, then style it with CSS.</li></ul><div class=\"analogy\"><p>Think of a book's chapter numbering. \"Chapter 3 → Section 3.1 → Section 3.1.1\" tells you exactly where you are and how deep. Now imagine the book jumped from \"Chapter 3\" straight to \"Section 3.1.1\" with no 3.1 — you'd flip back, confused: did I miss a section? That gap is exactly what a skipped heading level does to a screen-reader user. The font size is just the typesetting; the <em>number</em> is what tells them the depth.</p></div><h4>The myth that bites people</h4><p>There was once an HTML5 <strong>outline algorithm</strong> meant to auto-scope headings: drop an <code>&lt;h1&gt;</code> inside each <code>&lt;section&gt;</code> or <code>&lt;article&gt;</code> and the browser would supposedly figure out the real level from nesting. It sounded elegant — so people believe sectioning elements \"reset\" heading levels and sprinkle multiple <code>&lt;h1&gt;</code>s freely. <strong>No browser ever implemented this, and it was removed from the spec.</strong> That's precisely why one-h1-per-page is today's best practice. Multiple h1s are still <em>valid</em> HTML, but nothing auto-levels them — to assistive tech they're all just \"level 1,\" flattening your structure.</p><p>Accessibility auditors flag skipped levels under <strong>WCAG 1.3.1 (Info and Relationships)</strong>, so this isn't cosmetic — it's a compliance issue.</p><p>A clean outline reads: <code>h1</code> page title → <code>h2</code> section → <code>h3</code> subsection → <code>h2</code> next section. Jump <code>h2</code>→<code>h4</code> and the user is left wondering what level-3 content they somehow missed.</p><div class=\"key\"><p>One <code>&lt;h1&gt;</code>, then sequential headings with no skipped levels — chosen for structure, styled with CSS. The auto-leveling outline algorithm was never real, so your heading hierarchy is the page map, and you must keep it honest.</p></div>"},
  "constraint-validation-api": {"teach":"<p>At its core, the Constraint Validation API is just the browser's <strong>built-in form-checker, with a JavaScript steering wheel and CSS paint job</strong>. You declare rules in HTML, and the browser both enforces them and lets you read or restyle the result.</p><h4>Step one: declare the rules</h4><p>You attach constraints right on the input: <code>required</code>, <code>type=\"email\"</code>, <code>pattern=\"...\"</code>, <code>min</code>/<code>max</code>, <code>minlength</code>/<code>maxlength</code>. That's the whole rulebook — no JS needed for the browser to start enforcing it.</p><h4>Step two: read the verdict from JS</h4><p>Every control exposes an API:</p><ul><li><code>validity</code> — a <code>ValidityState</code> object of boolean flags (<code>valueMissing</code>, <code>typeMismatch</code>, <code>patternMismatch</code>, <code>tooShort</code>…). It tells you <em>why</em> something failed.</li><li><code>validationMessage</code> — the human-readable error string.</li><li><code>checkValidity()</code> — returns true/false and fires an <code>invalid</code> event, but shows no UI.</li><li><code>reportValidity()</code> — same check, <em>plus</em> it pops the browser's native error bubble.</li><li><code>setCustomValidity(msg)</code> — your own app-specific error (e.g. \"passwords must match\").</li></ul><h4>Step three: style by state with CSS</h4><p>The pseudo-classes <code>:valid</code> and <code>:invalid</code> match live as the user types. <code>:user-invalid</code> is the polite cousin — it only matches <em>after</em> the user has interacted, so you don't scream \"INVALID\" at an empty field they haven't touched yet.</p><div class=\"analogy\"><p>Think of an airport security checkpoint. The <strong>rules</strong> (no liquids over 100ml) are the HTML constraints. The <strong>scanner</strong> that flags a bag is <code>checkValidity()</code>. The <strong>red light and announcement</strong> is <code>reportValidity()</code>'s native bubble. And <code>setCustomValidity()</code> is the officer writing a custom note — \"this specific bag needs a hand search\" — that the scanner then treats as a failure too.</p></div><h4>The novalidate trap</h4><p>Add <code>novalidate</code> to the <code>&lt;form&gt;</code> and people assume validation is <em>off</em>. It isn't. <code>novalidate</code> only suppresses the <strong>native error bubbles</strong> and the <strong>submit gate</strong> (the form will submit even with invalid fields). But the entire JS API stays live — <code>validity</code> flags still flip, <code>:invalid</code> still matches, <code>checkValidity()</code> still works. That's <em>exactly why</em> teams use it: they want to keep all the validation logic but render their own accessible error messages instead of the browser's inconsistent, unstylable bubbles.</p><h4>Worked example</h4><p><code>input.setCustomValidity('Passwords must match')</code> marks the field invalid and sets its <code>validationMessage</code>. Crucially, you must call <code>input.setCustomValidity('')</code> (empty string) to clear it — otherwise the field stays \"invalid\" forever, even after the user fixes it.</p><div class=\"key\"><p>HTML constraints feed a live API you read in JS and style in CSS. <code>novalidate</code> doesn't disable validation — it only hides native bubbles and the submit gate, which is precisely what lets you build custom, accessible error UIs on top of it.</p></div>"},
  "label-association": {"teach":"<p>At its core, label association is just <strong>tying a field's visible text to the field itself</strong>, so a screen reader can announce what the field is for and a click on the text jumps focus into it.</p><h4>Why a field needs a name</h4><p>An empty text box means nothing to a blind user — \"edit text\" tells them to type <em>what</em>? The label supplies the field's <strong>accessible name</strong> (the thing AT reads aloud) and, as a bonus, extends the clickable area so tapping the word \"Email\" focuses the input. That bigger hit target matters enormously on touch screens.</p><h4>Two ways to connect them</h4><ul><li><strong>Explicit</strong>: <code>&lt;label for=\"email\"&gt;Email&lt;/label&gt;</code> paired with <code>&lt;input id=\"email\"&gt;</code>. The <code>for</code> attribute points at the input's <code>id</code>. This is the most robust form because the two can sit anywhere in the DOM — across columns, wrapped in other markup — and still stay linked.</li><li><strong>Implicit</strong>: wrap the control inside the label — <code>&lt;label&gt;Email &lt;input&gt;&lt;/label&gt;</code>. No <code>id</code> needed, but the two must be physically nested.</li></ul><div class=\"analogy\"><p>Think of a coat-check ticket. The label is the number on your stub; the <code>for</code>/<code>id</code> pair is the matching number on the hook. Explicit association is handing over numbered tickets — the coat and stub can be on opposite ends of the room and still pair up perfectly. Implicit association is pinning the stub directly to the coat: convenient, but they have to physically travel together.</p></div><h4>The placeholder trap</h4><p>The single most common audit failure here is treating <code>placeholder</code> text as the label. A placeholder is <strong>not a label</strong>, and here's why it fails:</p><ul><li>It <strong>vanishes the moment the user types</strong> — so anyone who pauses mid-form has lost the field's name entirely.</li><li>It frequently <strong>fails color-contrast</strong> requirements (placeholders are typically light gray).</li><li>It's an <strong>unreliable accessible name</strong> — support varies and it was never meant to label.</li></ul><p>So it fails WCAG, and it's a classic ding on accessibility audits.</p><h4>The rule</h4><p>Every interactive form control needs a programmatic name via one of: a <code>&lt;label&gt;</code>, an <code>aria-label</code> (an invisible string name), or <code>aria-labelledby</code> (point at another element's id for the name). A visible label is almost always the best choice because it helps <em>everyone</em>, sighted users included.</p><p>Done right — <code>&lt;label for=\"email\"&gt;Email&lt;/label&gt;&lt;input id=\"email\" type=\"email\" required&gt;</code> — clicking the word \"Email\" focuses the field, and AT announces \"Email, edit text, required.\" That last word comes from the <code>required</code> attribute, surfaced through the same accessibility plumbing.</p><div class=\"key\"><p>Every form control needs a programmatic accessible name, ideally from a real <code>&lt;label&gt;</code> (explicit <code>for</code>/<code>id</code> is the most robust). A placeholder is never a label — it disappears on typing and fails WCAG.</p></div>"},
  "responsive-images": {"teach":"<p>At its core, responsive images are just <strong>markup that hands the browser several versions of a picture and lets it download the one that fits the screen</strong> — saving bytes on phones and serving crisp detail on retina laptops.</p><h4>Two jobs, two tools</h4><p>There are really two distinct problems, and HTML solves them separately.</p><h4>Problem 1: same image, right number of pixels (resolution switching)</h4><p>This is the <code>&lt;img&gt;</code> + <code>srcset</code> job. You offer the same picture at multiple resolutions:</p><ul><li><strong>Density descriptors</strong> (<code>x</code>): for <em>fixed-size</em> images. <code>srcset=\"a.jpg 1x, a@2x.jpg 2x\"</code> swaps by device pixel ratio — a 2x retina screen grabs the denser file.</li><li><strong>Width descriptors</strong> (<code>w</code>) plus a <code>sizes</code> attribute: for <em>fluid</em> images that flex with layout. You tell the browser each file's intrinsic pixel width (<code>400w</code>) and, via <code>sizes</code>, how wide the image will <em>render</em> at each breakpoint. The browser does the math and picks.</li></ul><p>Either way it's the <strong>same image, different pixel counts</strong>, and crucially the <em>user agent makes the final choice</em> — it may even factor in network conditions.</p><div class=\"analogy\"><p><code>sizes</code> is like telling a print shop \"this photo will be displayed as a 4-inch wide print\" — <em>not</em> \"the file is 1200 pixels.\" The shop then picks a negative with enough detail for a 4-inch print. The trap is confusing the <strong>display size</strong> with the <strong>file size</strong>: <code>sizes</code> is always the on-screen <em>layout</em> width.</p></div><h4>Problem 2: different image entirely (picture)</h4><p><code>&lt;picture&gt;</code> wraps several <code>&lt;source&gt;</code>s and handles cases where you need a <em>different</em> image, not just more pixels:</p><ul><li><strong>Art direction</strong>: a wide hero crop on desktop, a tight square crop on mobile, chosen by each source's <code>media</code> query.</li><li><strong>Format negotiation</strong>: offer AVIF, then WebP, then JPEG via each source's <code>type</code>. The browser takes the <em>first matching source it understands</em>.</li></ul><p>The nested <code>&lt;img&gt;</code> inside <code>&lt;picture&gt;</code> is <strong>mandatory</strong>: it's the fallback for old browsers, it's the element that actually renders, and it carries the <code>alt</code> text. Forget it and nothing displays.</p><h4>The sizes trap, sharpened</h4><p><code>sizes</code> is the image's <strong>layout width at each breakpoint, evaluated before layout happens</strong> — not the file's pixel width. Get it wrong and the browser systematically over-fetches (wasting bandwidth) or under-fetches (blurry images). Example: <code>&lt;img srcset=\"s.jpg 400w, l.jpg 800w\" sizes=\"(max-width:600px) 100vw, 50vw\" src=\"l.jpg\" alt=\"…\"&gt;</code> — on a 375px phone, <code>100vw</code> means the image renders ~375px wide, so the browser fetches the <code>400w</code> file, not the wasteful 800w one.</p><div class=\"key\"><p>Use <code>srcset</code>/<code>sizes</code> on <code>&lt;img&gt;</code> for resolution switching (same image, the UA picks by pixels) and <code>&lt;picture&gt;</code> for art direction and format negotiation (different images, first match wins). <code>sizes</code> is the rendered <em>layout</em> width — never the file's pixel width.</p></div>"},
  "native-dialog": {"teach":"<p>At its core, the native <code>&lt;dialog&gt;</code> element is just a <strong>modal box the browser knows how to run for you</strong> — it handles the dark backdrop, traps your keyboard focus inside, and closes on Esc, all the fiddly things people used to hand-build (badly) with divs.</p><h4>What showModal() actually does</h4><p>Calling <code>dialog.showModal()</code> triggers a remarkable amount of behavior:</p><ul><li><strong>Top layer</strong>: the dialog is promoted above <em>every</em> stacking context — no <code>z-index</code> war, and it's never clipped by an ancestor's <code>overflow:hidden</code> or <code>transform</code>. This single feature solves a decade of modal-rendering pain.</li><li><strong>::backdrop</strong>: a styleable pseudo-element paints the dimmed area behind it.</li><li><strong>Focus trap</strong>: focus moves inside and <em>stays</em> inside — Tab cycles within the dialog, never escaping to the page behind.</li><li><strong>Inert background</strong>: the rest of the page becomes <code>inert</code>, so clicks and screen-reader navigation can't reach it.</li><li><strong>Esc to close</strong>: pressing Escape fires a <code>cancel</code> event, then a <code>close</code> event.</li></ul><div class=\"analogy\"><p>Think of <code>showModal()</code> as a theater usher who, when the play starts, dims the house lights (<code>::backdrop</code>), locks the auditorium doors so you can't wander into the lobby (focus trap + <code>inert</code>), and stands ready to open the exit the instant you say \"leave\" (Esc). <code>show()</code>, by contrast, is just propping a poster on an easel in a fully-lit, open room — visible, but nothing is contained.</p></div><h4>Forms that close dialogs</h4><p>A <code>&lt;form method=\"dialog\"&gt;</code> is special: submitting it <strong>closes the dialog</strong> and sets <code>dialog.returnValue</code> to the submitting button's <code>value</code>. So <code>&lt;button value=\"ok\"&gt;OK&lt;/button&gt;</code> closes the dialog and records \"ok\" — no JavaScript click handler required.</p><h4>The two traps</h4><p>First, <code>dialog.show()</code> (no \"Modal\") opens the dialog <strong>non-modally</strong> and gives you <em>none</em> of the magic — no focus trap, no inerting, no backdrop. It is <strong>not</strong> a substitute for a real modal; using it and expecting modal behavior is a classic mistake.</p><p>Second, don't rely on default focus placement. When a dialog opens, focus has to land <em>somewhere</em>, and if your first focusable control is a destructive \"Delete\" button, an Enter-press could fire it instantly. Set <code>autofocus</code> deliberately on a safe element.</p><h4>Worked example</h4><p><code>&lt;dialog id=\"d\"&gt;&lt;form method=\"dialog\"&gt;&lt;button value=\"ok\"&gt;OK&lt;/button&gt;&lt;/form&gt;&lt;/dialog&gt;</code>, then <code>d.showModal()</code>, and listen with <code>d.addEventListener('close', () =&gt; console.log(d.returnValue))</code> to read back which button closed it.</p><div class=\"key\"><p><code>showModal()</code> gives you a real modal — top layer, backdrop, focus trap, inert page, Esc-to-close — for free; <code>show()</code> gives you none of that. Always place <code>autofocus</code> deliberately so focus never lands on a destructive action.</p></div>"},
  "shadow-dom": {"teach":"<p>At its core, Shadow DOM is just a <strong>private little DOM hidden inside a component</strong>, with a wall around it so its styles don't leak out onto your page and your page's styles don't leak in and wreck it.</p><h4>The encapsulation problem it solves</h4><p>Normal CSS is global — one stray <code>button { color: red }</code> can repaint every button in an app, including ones inside a reusable widget you didn't write. Shadow DOM builds a boundary so a component's internals are sealed off and self-contained.</p><h4>Open vs closed</h4><p>You create it with <code>el.attachShadow({mode: 'open'})</code>. With <code>'open'</code>, outside code can reach the inside via <code>el.shadowRoot</code>. With <code>'closed'</code>, <code>el.shadowRoot</code> returns <code>null</code> to the outside — though the component itself still holds its own private reference internally.</p><h4>What the wall blocks — and what crosses it</h4><p>The wall is selective, not total:</p><ul><li>Styles defined inside <strong>don't leak out</strong>, and most outside styles <strong>don't leak in</strong>.</li><li>But <strong>inherited properties</strong> like <code>color</code> and <code>font-family</code> still cross — that's how inheritance works in CSS, and it's deliberate so components match surrounding text.</li><li><strong>CSS custom properties</strong> (variables) also pierce the boundary, which is the intended way to let a page theme a component.</li></ul><div class=\"analogy\"><p>Think of Shadow DOM as a <strong>hotel room</strong>. The walls block sound and mess from spilling between rooms (style encapsulation). But the building's <em>central heating</em> still flows in (inherited properties like <code>color</code>), and there's a <em>thermostat dial</em> on the wall the guest can turn (CSS custom properties) to adjust their room within limits. The room is private — but not a sealed vault.</p></div><h4>The flattened tree and slots</h4><p>What you actually <em>see</em> is the <strong>flattened tree</strong>: the component's internal shadow markup combined with your own \"light DOM\" content projected in through <code>&lt;slot&gt;</code> placeholders. The browser composes these two sources into the final rendered output.</p><h4>Controlled style hooks</h4><p>Since outside CSS can't freely reach in, components expose deliberate hooks: <code>::part()</code> styles internals the author explicitly marked with a <code>part</code> attribute; CSS custom properties theme it; <code>:host</code> / <code>:host()</code> style the component element itself from inside; and <code>::slotted()</code> styles the top-level nodes you projected in.</p><h4>The security trap</h4><p>Do <strong>not</strong> treat <code>closed</code> shadow DOM as a security boundary. It's a <em>soft</em> encapsulation signal — a \"please don't poke at my internals\" hint — and it's bypassable by determined code. Never use it to hide secrets, tokens, or anything that matters for security.</p><div class=\"key\"><p>Shadow DOM scopes a component's markup and CSS, but the wall is selective — inherited properties and custom properties cross it on purpose. And <code>closed</code> mode is encapsulation, not security; never rely on it to hide anything sensitive.</p></div>"},
  "custom-elements": {"teach":"<p>At its core, custom elements let you <strong>invent your own HTML tags</strong> — like <code>&lt;user-card&gt;</code> — that the browser treats as first-class elements, complete with hooks that fire when they're added, removed, or changed.</p><h4>Defining one</h4><p>You write a class that extends <code>HTMLElement</code> and register it: <code>customElements.define('x-y', cls)</code>. The name <strong>must contain a hyphen</strong> — that's the rule that guarantees your tag never collides with a future standard HTML element (the spec promises built-ins will never have hyphens).</p><h4>The lifecycle hooks</h4><p>The browser calls specific methods at specific moments — these are the <strong>lifecycle reactions</strong>:</p><ul><li><code>constructor</code> — runs when the element is created. Set up internal state and attach the shadow root here, but <strong>touch nothing external</strong>: no attributes, no children, no parent. They may not exist yet.</li><li><code>connectedCallback</code> — fires when the element is <em>inserted into the DOM</em>. This is where you do real work: render, fetch, add listeners. It can fire <strong>multiple times</strong> if the element is moved or re-inserted, so make it idempotent.</li><li><code>disconnectedCallback</code> — fires on removal. Clean up: remove listeners, cancel timers, abort fetches.</li><li><code>attributeChangedCallback(name, old, new)</code> — fires when a watched attribute changes, but <strong>only</strong> for attributes listed in <code>static get observedAttributes()</code>.</li><li><code>adoptedCallback</code> — fires when the element is moved to a different document.</li></ul><div class=\"analogy\"><p>Think of the lifecycle like a new employee. The <strong>constructor</strong> is HR creating their record before they've even arrived — you can't ask them about their desk yet, it doesn't exist. <strong>connectedCallback</strong> is their first day actually <em>in the office</em>: now they can plug in and start working (and if they transfer departments and come back, it's another \"first day\"). <strong>disconnectedCallback</strong> is the exit interview — return your badge and laptop. And <strong>attributeChangedCallback</strong> only pings you for the specific HR forms you signed up to be notified about.</p></div><h4>The two classic traps</h4><p>First, <strong>reading attributes or children in the constructor</strong>. At construction time the element may have no attributes and no children yet — your code reads <code>null</code> and breaks. Do that work in <code>connectedCallback</code>.</p><p>Second, <strong>forgetting <code>observedAttributes</code></strong> and then wondering why <code>attributeChangedCallback</code> never fires. The browser only watches the attributes you explicitly opt into. <code>static get observedAttributes() { return ['open']; }</code> tells it to watch <code>open</code>; now <code>attributeChangedCallback(n, o, v) { if (n === 'open') this.render(); }</code> runs every time you toggle <code>[open]</code>.</p><h4>The bigger picture</h4><p>Custom elements + Shadow DOM + <code>&lt;template&gt;</code> together form <strong>Web Components</strong> — reusable, framework-agnostic UI primitives that work in any page, with React or Vue or nothing at all.</p><div class=\"key\"><p>Custom elements give you real HTML tags with lifecycle hooks — do setup in the constructor, real work in <code>connectedCallback</code>, and remember <code>attributeChangedCallback</code> only fires for attributes named in <code>observedAttributes</code>.</p></div>"},
  "template-slot": {"teach":"<p>At its core, these are two complementary tools: <code>&lt;template&gt;</code> is <strong>a stencil of HTML the browser parses but holds inert until you stamp it out</strong>, and <code>&lt;slot&gt;</code> is <strong>a window inside a component where the content you pass in shows up</strong>.</p><h4>template: the inert stencil</h4><p>Markup inside <code>&lt;template&gt;</code> is parsed into real DOM but kept <strong>dormant</strong>: scripts don't run, images don't load, nothing renders. It just sits there as a blueprint. When you want a copy, you clone <code>tmpl.content</code> — a <code>DocumentFragment</code> — and insert it. This is the efficient way to instantiate repeated markup (a table row, a card) because you author it once and stamp out many cheap copies.</p><div class=\"analogy\"><p><code>&lt;template&gt;</code> is a <strong>cookie cutter</strong> sitting in a drawer. The cutter itself isn't a cookie — it doesn't get eaten (scripts don't run, images don't load). But whenever you press it into dough (<code>clone tmpl.content</code>), out comes a real, edible cookie (live DOM). One cutter, unlimited cookies.</p></div><h4>slot: the projection window</h4><p>Inside a component's shadow tree you place a <code>&lt;slot&gt;</code>. It projects the element's <strong>light-DOM children</strong> — the content the page author wrote between your component's tags — into the shadow tree at the slot's position.</p><p>Here's the subtle, crucial part: <strong>slotted nodes are not moved</strong>. They physically remain in the light DOM where the author wrote them; they merely <em>render</em> at the slot's location. The browser composes this combined view into the <strong>flattened tree</strong>.</p><ul><li><strong>Named slots</strong> (<code>&lt;slot name=\"title\"&gt;</code>) match children carrying <code>slot=\"title\"</code>.</li><li>The <strong>default</strong> (unnamed) slot catches everything else.</li><li><code>slotchange</code> fires whenever the set of assigned nodes changes.</li></ul><h4>The trap: projection isn't relocation</h4><p>People assume <code>&lt;slot&gt;</code> <em>moves</em> nodes into the shadow root. It doesn't — it <strong>projects them in place</strong>. This has a real consequence for styling: because the nodes stay in the light DOM, you can't reach them with ordinary shadow-tree selectors. You style them with <code>::slotted()</code> — and even then, only the <strong>top-level</strong> projected nodes, not their descendants.</p><h4>Worked example</h4><p>Shadow tree contains <code>&lt;slot name=\"title\"&gt;&lt;/slot&gt;</code>. The page writes <code>&lt;my-card&gt;&lt;h2 slot=\"title\"&gt;Hi&lt;/h2&gt;&lt;/my-card&gt;</code>. Result: the <code>&lt;h2&gt;</code> stays in the light DOM but renders exactly where the named slot sits inside the component. The author controls the content; the component controls the placement.</p><div class=\"key\"><p><code>&lt;template&gt;</code> is inert, clonable markup you stamp out on demand; <code>&lt;slot&gt;</code> <em>projects</em> light-DOM children in place (it never moves them), which is why you must reach them with <code>::slotted()</code> and only at the top level.</p></div>"},
  "iframe-sandbox": {"teach":"<p>At its core, the <code>sandbox</code> attribute is just a <strong>lockdown switch for an embedded page</strong>: turn it on and the iframe loses almost all its powers, then you hand back only the specific capabilities you actually trust.</p><h4>Deny-by-default, allow-by-token</h4><p>This is the key mental model. <code>&lt;iframe sandbox&gt;</code> with <em>no value</em> imposes maximum restriction:</p><ul><li>Scripts <strong>off</strong>.</li><li>Form submission <strong>off</strong>.</li><li>Popups <strong>off</strong>.</li><li>A unique <strong>opaque origin</strong> — the frame can't read same-origin cookies, <code>localStorage</code>, or anything tied to your real origin.</li><li>No top-level navigation (it can't hijack your tab and redirect the whole page).</li><li>No plugins.</li></ul><p>Then you re-grant capabilities one at a time with space-separated tokens: <code>allow-scripts</code>, <code>allow-forms</code>, <code>allow-popups</code>, <code>allow-same-origin</code>, <code>allow-top-navigation</code>, and more. You add back only what the embedded content genuinely needs.</p><div class=\"analogy\"><p>Think of a high-security visitor badge. By default it opens <em>no</em> doors — the visitor can stand in the lobby and that's it. Security then activates specific doors on the badge: \"OK, they can enter the lab (<code>allow-scripts</code>) and the print room (<code>allow-forms</code>),\" but nothing else. You never start by trusting them with the master key; you start from zero and grant deliberately.</p></div><h4>The dangerous combination</h4><p>Here's the trap that bites people: setting <strong>both <code>allow-scripts</code> and <code>allow-same-origin</code></strong> on content from <em>your own origin</em>. Why is that dangerous? With scripts enabled <em>and</em> same-origin access, the framed page's JavaScript can reach up, <strong>delete its own <code>sandbox</code> attribute, and reload</strong> — escaping the sandbox entirely. The two tokens together hand the prisoner the keys to their own cell. If you must allow scripts on untrusted content, keep it cross-origin (or at least an opaque origin) so it can't do this.</p><h4>Companion attributes</h4><p><code>sandbox</code> handles capabilities, but pair it with siblings for full isolation:</p><ul><li><code>allow=\"...\"</code> — a Permissions Policy controlling powerful features like camera and geolocation (a separate axis from sandbox).</li><li><code>referrerpolicy</code> — limits what referrer info leaks to the embedded page.</li><li><code>credentialless</code> — isolates a third-party frame's storage and cookies.</li><li><code>loading=\"lazy\"</code> — defers the frame's load until it's near the viewport.</li></ul><h4>Worked example</h4><p><code>&lt;iframe sandbox=\"allow-scripts\" src=\"https://untrusted.example/widget\"&gt;</code> runs the widget's JavaScript but keeps it on an opaque origin with no storage, no cookies, and no ability to navigate your top window — exactly the right posture for embedding untrusted third-party code.</p><div class=\"key\"><p><code>sandbox</code> starts from total restriction and you grant capabilities back by token. Never combine <code>allow-scripts</code> with <code>allow-same-origin</code> on same-origin content — the framed script can remove its own sandbox and escape.</p></div>"},
  "head-metadata": {"teach":"<p>At its core, <code>&lt;head&gt;</code> metadata is just <strong>a set of invisible instructions at the top of your page</strong> — they render nothing themselves, but they tell the browser how to decode text, how to scale on phones, and how the link should look when someone shares it.</p><h4>charset: decode the bytes correctly</h4><p><code>&lt;meta charset=\"utf-8\"&gt;</code> tells the browser which character encoding to read your file in. The critical rule: it <strong>must appear within the first 1024 bytes</strong> of the document. The browser reads the start of the file to guess the encoding; find the declaration late and it may have already begun parsing with the wrong encoding, forcing a costly re-parse (and mojibake like \"caf&eacute;\" turning into garbage). So <code>charset</code> goes first, right after <code>&lt;head&gt;</code>.</p><h4>viewport: behave on mobile</h4><p><code>&lt;meta name=\"viewport\" content=\"width=device-width, initial-scale=1\"&gt;</code> is <strong>required</strong> for responsive mobile rendering. Without it, phones pretend to be ~980px wide and shrink your whole layout to fit, making text microscopic. This line says \"use the device's real width, at 100% zoom.\"</p><div class=\"analogy\"><p>The viewport tag is like telling a tailor the customer's <em>actual</em> measurements instead of defaulting to a generic mannequin. <code>width=device-width</code> says \"measure this specific person\"; without it, the browser dresses everyone in the same oversized suit and then shrinks the photo so it appears to fit.</p></div><p>But there's a trap: <strong>do not add <code>user-scalable=no</code> or <code>maximum-scale=1</code></strong> for a \"cleaner\" feel. These block pinch-to-zoom, which low-vision users depend on, failing <strong>WCAG 1.4.4 (Resize Text)</strong> and <strong>1.4.10 (Reflow)</strong>. Modern browsers increasingly ignore these anyway — so you gain nothing and fail an audit.</p><h4>Open Graph: control the link preview</h4><p>When someone pastes your URL into Slack, iMessage, or a social feed, <strong>Open Graph</strong> tags drive the preview card: <code>og:title</code>, <code>og:description</code>, and <code>og:image</code>. A sharp detail: these use the <code>property</code> attribute, <strong>not <code>name</code></strong> — <code>&lt;meta property=\"og:image\" content=\"...\"&gt;</code>. Mixing that up is why previews silently fail to render.</p><p>And don't forget <code>&lt;title&gt;</code> itself: it remains the <strong>strongest on-page SEO signal</strong> and the page name that assistive tech announces, so it's doing double duty.</p><h4>Worked example</h4><p><code>&lt;meta property=\"og:image\" content=\"https://site/card.png\"&gt;</code> is what makes a rich image appear in a Slack or iMessage link unfurl instead of a bare URL.</p><div class=\"key\"><p>Put <code>charset</code> in the first 1024 bytes, always include a responsive <code>viewport</code> but never disable zoom (WCAG), and remember Open Graph tags use <code>property</code>, not <code>name</code> — small details that decide whether your page decodes, scales, and shares correctly.</p></div>"},
  "lazy-loading": {"teach":"<p>At its core, <code>loading=\"lazy\"</code> is just <strong>a polite instruction to the browser: \"don't download this image or iframe until the user is about to scroll to it.\"</strong> Why fetch the footer gallery while someone's still reading the headline?</p><h4>How it works</h4><p>Add <code>loading=\"lazy\"</code> to an <code>&lt;img&gt;</code> or <code>&lt;iframe&gt;</code> and the browser <strong>defers the fetch</strong> until the element nears the viewport, using its own built-in distance heuristic. The win is concrete: a long, image-heavy page no longer downloads dozens of off-screen images up front, so the initial payload shrinks and the first load is faster. And it's native — no <code>IntersectionObserver</code>, no scroll listeners, no JavaScript at all.</p><div class=\"analogy\"><p>Think of a restaurant kitchen. Eager loading is cooking <em>every</em> dish on the menu the moment you sit down — wasteful, slow, and most plates go cold untouched. Lazy loading is cooking each course only as you're ready to order it. The food you actually want arrives faster because the kitchen isn't drowning in dishes nobody asked for yet.</p></div><h4>The one image you must NOT lazy-load</h4><p>Here's the trap that quietly hurts performance: <strong>never lazy-load your above-the-fold LCP image</strong>. The LCP (Largest Contentful Paint) image is usually the big hero at the top — and it's a Core Web Vital that Google measures.</p><p>Why does lazy-loading it backfire? Lazy-loading delays the fetch until <em>after layout has run</em> (the browser must lay out the page to know the element is near the viewport). But your hero is already on screen — you wanted it fetched <em>immediately</em>. By deferring it, you push back the moment it paints, directly regressing LCP. So slapping <code>loading=\"lazy\"</code> on <em>every</em> image, hero included, makes your most important metric worse.</p><h4>What to do instead for the hero</h4><ul><li>Leave it <strong>eager</strong> (eager is the default — you don't add the attribute).</li><li>Add <code>fetchpriority=\"high\"</code> so the browser fetches it before lower-priority resources.</li><li>Optionally <code>&lt;link rel=\"preload\" as=\"image\"&gt;</code> to start the download even earlier.</li><li>Always set <code>width</code>/<code>height</code> (or <code>aspect-ratio</code>) so the layout doesn't jump when it loads — that jump is <strong>CLS</strong> (Cumulative Layout Shift), another Core Web Vital.</li></ul><h4>Worked example</h4><p>Hero: <code>&lt;img src=\"hero.jpg\" fetchpriority=\"high\" width=\"1200\" height=\"600\"&gt;</code> — eager and prioritized. Footer thumbnail: <code>&lt;img src=\"thumb.jpg\" loading=\"lazy\" width=\"…\" height=\"…\"&gt;</code> — deferred until scrolled into view.</p><div class=\"key\"><p>Lazy-load <em>below</em>-the-fold images and iframes to cut initial payload — but keep the above-the-fold LCP hero eager (with <code>fetchpriority=\"high\"</code> and explicit dimensions), because lazy-loading it regresses a Core Web Vital.</p></div>"}
});
