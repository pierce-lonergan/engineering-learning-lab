/* DOM, Events & Web APIs (FE-DOM) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"FE-DOM","front":"Node vs Element: what's the difference and why does it matter when traversing the DOM?","back":"<ul><li>A <strong>Node</strong> is the base type for everything in the tree: elements, <strong>text nodes</strong>, comments, the document itself. <code>nodeType</code> 1=element, 3=text, 8=comment, 9=document.</li><li>An <strong>Element</strong> is a Node subtype representing a tag (<code>&lt;div&gt;</code>).</li><li><em>Trap:</em> <code>childNodes</code> is a <code>NodeList</code> that <strong>includes whitespace text nodes</strong>; <code>children</code> is an <code>HTMLCollection</code> of elements only. Likewise <code>firstChild</code> (any node) vs <code>firstElementChild</code> (element only).</li><li>Use the <code>*Element*</code> variants when you mean tags; use node APIs when you genuinely need text/comments.</li></ul>"},
  {"sec":"FE-DOM","front":"querySelectorAll vs getElementsBy*: static vs live collections","back":"<ul><li><code>querySelectorAll()</code> returns a <strong>static</strong> <code>NodeList</code> — a snapshot; later DOM mutations are <em>not</em> reflected.</li><li><code>getElementsByTagName/ClassName</code> and <code>.children</code> return a <strong>live</strong> <code>HTMLCollection</code> that auto-updates.</li><li><em>Classic bug:</em> looping a live collection by index while removing nodes skips elements because the collection shrinks under you. Snapshot first: <code>[...el.children]</code>.</li><li><code>querySelector</code> takes <strong>CSS selectors</strong> (flexible); <code>getElementById</code> is a hashed lookup and typically fastest for a single id.</li><li>Both <code>NodeList</code> and (since DOM4) <code>HTMLCollection</code> are iterable, but only <code>NodeList</code> has <code>forEach</code>; spread either to use <code>map</code>/<code>filter</code>.</li></ul>"},
  {"sec":"FE-DOM","front":"textContent vs innerText vs innerHTML — pick correctly under perf + security pressure","back":"<ul><li><code>textContent</code>: <strong>all</strong> text incl. hidden/&lt;script&gt; nodes, ignores CSS, <em>no reflow</em>, fastest, no HTML parsing — <strong>safe</strong> for untrusted strings (sets text, never markup).</li><li><code>innerText</code>: <strong>rendered</strong> text only — respects <code>display:none</code> and <code>text-transform</code>; <em>reading</em> it <strong>can trigger reflow/layout</strong> (slow in loops).</li><li><code>innerHTML</code>: parses a string as HTML — powerful but <strong>injects markup</strong>; never assign untrusted input (<strong>XSS</strong>). Note injected inline <code>&lt;script&gt;</code> won't execute, but <code>onerror</code>/<code>onload</code> attributes will. Sanitize (e.g. <code>setHTML()</code>/DOMPurify) or use <code>textContent</code>.</li><li>Rule of thumb: plain text → <code>textContent</code>; need visible/rendered text → <code>innerText</code>; building trusted markup → <code>innerHTML</code>.</li></ul>"},
  {"sec":"FE-DOM","front":"append/prepend vs insertAdjacentHTML: how do you insert nodes and where?","back":"<ul><li><code>append()</code>/<code>prepend()</code> insert nodes imperatively; they accept <strong>multiple nodes and strings</strong> and return <code>undefined</code> (vs legacy <code>appendChild</code> = one Node, returns it; strings aren't allowed).</li><li><code>insertAdjacentHTML(position, html)</code> parses a string at <code>'beforebegin'</code>, <code>'afterbegin'</code>, <code>'beforeend'</code>, <code>'afterend'</code> — <em>without</em> reparsing existing children (unlike <code>innerHTML +=</code>, which destroys and rebuilds the subtree, dropping listeners and resetting form state).</li><li><code>insertAdjacentElement</code>/<code>insertAdjacentText</code> are the node/text siblings.</li><li><em>Security:</em> <code>insertAdjacentHTML</code> shares <code>innerHTML</code>'s XSS surface — don't feed it untrusted data.</li></ul>"},
  {"sec":"FE-DOM","front":"Walk through the three phases of DOM event flow.","back":"<ul><li><strong>1. Capture</strong> (trickle down): from <code>window</code> → <code>document</code> → ancestors toward the target. Listeners fire here only if registered with <code>{capture:true}</code>.</li><li><strong>2. Target</strong>: the event reaches <code>event.target</code>; on the target itself, capture and bubble listeners both fire <em>in registration order</em>.</li><li><strong>3. Bubble</strong> (rise up): back from target → ancestors → <code>window</code>. Default (bubble-phase) listeners fire here.</li><li>Inspect with <code>event.eventPhase</code> (1=capture, 2=target, 3=bubble). Some events (<code>focus</code>, <code>blur</code>, most <code>scroll</code>) <strong>don't bubble</strong> — use capture or <code>focusin</code>/<code>focusout</code> for delegation.</li></ul>"},
  {"sec":"FE-DOM","front":"addEventListener options: capture, once, passive, signal","back":"<ul><li><code>capture</code>: fire during the capture phase instead of bubble.</li><li><code>once</code>: auto-remove after the first invocation — no manual cleanup.</li><li><code>passive: true</code>: promises you <em>won't</em> call <code>preventDefault()</code>, so the browser can scroll without waiting on your handler — <strong>important for <code>touchstart</code>/<code>touchmove</code>/<code>wheel</code></strong>. Browsers already default these to passive on <code>window</code>/<code>document</code>/<code>body</code>.</li><li><code>signal</code>: pass an <code>AbortController</code>'s <code>signal</code>; <code>controller.abort()</code> removes the listener — <strong>cleanest teardown</strong>, drops many listeners at once.</li><li><em>Trap:</em> the third arg can be a boolean (=capture) <em>or</em> an options object; <code>removeEventListener</code> must match the <code>capture</code> flag <em>and</em> the same function reference (<code>once</code>/<code>passive</code> aren't part of the match).</li></ul>"},
  {"sec":"FE-DOM","front":"What is event delegation and why does it scale?","back":"<p>Attach <strong>one listener to a common ancestor</strong> and use <strong>bubbling</strong> to handle events from many descendants, branching on <code>event.target.closest(selector)</code>.</p><ul><li><strong>Wins:</strong> one listener instead of O(n); <strong>works for dynamically added children</strong> with no re-binding; lower memory.</li><li><code>e.target.closest('.btn')</code> matches even when the click lands on a nested child inside the button; guard with <code>if (match &amp;&amp; container.contains(match))</code>.</li><li><em>Caveats:</em> non-bubbling events (<code>focus</code>, <code>blur</code>) need capture or <code>focusin</code>/<code>focusout</code>; a downstream <code>stopPropagation()</code> kills delegation; very deep trees add per-hop dispatch cost.</li></ul>"},
  {"sec":"FE-DOM","front":"event.target vs event.currentTarget — when do they differ?","back":"<ul><li><code>event.target</code>: the <strong>deepest element</strong> that originated the event (where you clicked). Constant throughout propagation.</li><li><code>event.currentTarget</code>: the element whose listener is <strong>currently running</strong> (the one you bound). Changes as the event propagates; it equals <code>this</code> in a non-arrow handler.</li><li>In <strong>delegation</strong> they differ: <code>currentTarget</code> is the container, <code>target</code> is the clicked descendant.</li><li><em>Trap:</em> <code>currentTarget</code> is <code>null</code> if you read it <strong>after the handler returns</strong> (e.g. inside a later <code>setTimeout</code>) — the browser resets it post-dispatch, so capture it into a variable first.</li></ul>"},
  {"sec":"FE-DOM","front":"preventDefault vs stopPropagation vs stopImmediatePropagation","back":"<ul><li><code>preventDefault()</code>: cancels the browser's <strong>default action</strong> (link navigation, form submit, checkbox toggle). Does <em>not</em> stop propagation. Check <code>event.defaultPrevented</code>.</li><li><code>stopPropagation()</code>: stops the event from traveling to <strong>further elements</strong> (no more capture/bubble), but <em>other listeners on the same element still fire</em>.</li><li><code>stopImmediatePropagation()</code>: stops propagation <strong>and</strong> prevents <em>remaining listeners on the same element</em> from running.</li><li><em>Trap:</em> on a <code>passive</code> listener, <code>preventDefault()</code> is ignored (console warning). Returning <code>false</code> only cancels the default in inline/jQuery handlers, never in <code>addEventListener</code>.</li></ul>"},
  {"sec":"FE-DOM","front":"CustomEvent & dispatchEvent: how do you build a decoupled component event?","back":"<p>Create with <code>new CustomEvent('cart:add', { detail: {...}, bubbles: true, composed: true })</code> and fire via <code>el.dispatchEvent(evt)</code>. Listen with <code>addEventListener('cart:add', e =&gt; e.detail)</code>.</p><ul><li><code>detail</code> carries arbitrary payload data.</li><li><code>bubbles: true</code> lets ancestors/delegation catch it (defaults to <strong>false</strong>).</li><li><code>composed: true</code> lets it cross <strong>shadow DOM</strong> boundaries (otherwise it stops at the shadow root; <code>event.target</code> is retargeted to the host outside).</li><li><code>dispatchEvent</code> is <strong>synchronous</strong> — handlers run before the call returns; it returns <code>false</code> if the event is <code>cancelable</code> and a handler called <code>preventDefault()</code>, else <code>true</code>.</li></ul>"},
  {"sec":"FE-DOM","front":"IntersectionObserver: what problem does it solve and what are its key options?","back":"<p>Asynchronously reports when a target enters/leaves a <strong>root</strong> (viewport by default) — for <strong>lazy-loading, infinite scroll, impression/ad tracking, scroll-spy</strong> — without scroll-handler layout thrashing.</p><ul><li><code>new IntersectionObserver(cb, { root, rootMargin, threshold })</code>.</li><li><code>threshold</code>: 0 → fires on first pixel, 1 → fully visible, or an array for multiple stops.</li><li><code>rootMargin</code>: grow/shrink the root box (e.g. <code>'200px'</code> to preload before visible).</li><li>Callback receives <code>entries</code> with <code>isIntersecting</code> and <code>intersectionRatio</code>; the first callback fires <strong>asynchronously soon after <code>observe()</code></strong>. Call <code>observer.unobserve(el)</code> after a one-shot load. <em>Note:</em> reports overlap, not pixel-level paint visibility (CSS <code>visibility:hidden</code> still counts as intersecting).</li></ul>"},
  {"sec":"FE-DOM","front":"MutationObserver vs ResizeObserver vs IntersectionObserver — when to reach for each?","back":"<ul><li><strong>MutationObserver</strong>: watches DOM <em>structure</em> — child add/remove (<code>childList</code>), attribute changes (<code>attributes</code>), text (<code>characterData</code>). Configure via <code>observe(node, options)</code> + <code>subtree:true</code>; callback gets <code>MutationRecord[]</code>, delivered as a <strong>microtask</strong>. Replaced the deprecated Mutation Events.</li><li><strong>ResizeObserver</strong>: fires when an <em>element's box size</em> changes (not just the window) — ideal for responsive canvases / container-query-style logic. Read <code>entry.contentRect</code> / <code>contentBoxSize</code> / <code>borderBoxSize</code>.</li><li><strong>IntersectionObserver</strong>: visibility/overlap with a root.</li><li>All are <strong>async + batched</strong>; <em>trap:</em> writing layout-affecting styles in a <code>ResizeObserver</code> callback can throw the benign <code>ResizeObserver loop completed with undelivered notifications</code> warning — defer via <code>requestAnimationFrame</code>.</li></ul>"},
  {"sec":"FE-DOM","front":"requestIdleCallback: what is it for and what's the gotcha?","back":"<ul><li><code>requestIdleCallback(cb, { timeout })</code> schedules <strong>low-priority work</strong> during the browser's idle periods (after paint) so you don't block frames — analytics flush, prefetch, non-urgent DOM work.</li><li>Callback gets an <code>IdleDeadline</code>: call <code>deadline.timeRemaining()</code> (capped ≈50ms) and yield before it hits 0; re-schedule another <code>requestIdleCallback</code> for leftover work.</li><li><code>timeout</code> forces it to run even if no idle time appears (prevents indefinite starvation; <code>deadline.didTimeout</code> is then true).</li><li><em>Gotchas:</em> <strong>don't do heavy layout-affecting DOM</strong> here (no reflow budget); not for animation (use <code>requestAnimationFrame</code>); <strong>Safari still ships it behind a feature flag in 2026</strong> — feature-detect and fall back to <code>setTimeout</code>.</li></ul>"},
  {"sec":"FE-DOM","front":"History API: pushState, replaceState, and the popstate event","back":"<ul><li><code>history.pushState(state, '', '/path')</code> adds a history entry and changes the URL <strong>without a reload</strong> — the backbone of client-side routing (SPAs). The 2nd arg (title) is ignored by browsers.</li><li><code>replaceState</code> swaps the current entry (no new back-button step).</li><li><code>popstate</code> fires when the user navigates back/forward — read <code>event.state</code> to restore view. <em>Trap:</em> <code>popstate</code> does <strong>not</strong> fire on your own <code>pushState</code>/<code>replaceState</code> calls.</li><li>The <code>state</code> object is <strong>structured-cloned</strong> and size-limited (browser-specific — Firefox caps it around 16 MiB); keep it small and store real data elsewhere. <em>Modern alternative:</em> the <strong>Navigation API</strong> (<code>navigation.navigate</code>, <code>navigate</code> event, <code>currentEntry</code>) — <strong>Baseline as of Jan 2026</strong>.</li></ul>"},
  {"sec":"FE-DOM","front":"Web Storage: localStorage vs sessionStorage limits and pitfalls","back":"<ul><li>Both are <strong>synchronous, string-only</strong>, per-<strong>origin</strong> key/value stores capped at ~<strong>5MB</strong> (varies by browser).</li><li><code>localStorage</code> <strong>persists</strong> until cleared; <code>sessionStorage</code> is scoped to the <strong>tab</strong> and cleared on close (a duplicated tab inherits a copy, but tabs don't share it live).</li><li><em>Traps:</em> values are coerced to strings — <code>JSON.parse/stringify</code> yourself; writes are <strong>blocking on the main thread</strong> (don't store large blobs — use <strong>IndexedDB</strong>); throws <code>QuotaExceededError</code> when full; access can <strong>throw</strong> in some private modes / sandboxed iframes / third-party contexts — wrap in try/catch.</li><li>The <code>storage</code> event fires in <em>other</em> same-origin tabs (not the writer) — useful for cross-tab sync.</li></ul>"},
  {"sec":"FE-DOM","front":"DocumentFragment: why insert via a fragment instead of the live DOM?","back":"<p>A <code>DocumentFragment</code> is a <strong>lightweight, off-document container</strong>. Build many nodes inside it, then append the fragment once — its <em>children</em> move into the DOM and the fragment empties.</p><ul><li><strong>One layout pass</strong> instead of N — avoids inserting into the live tree inside a loop.</li><li>Created via <code>document.createDocumentFragment()</code> or <code>&lt;template&gt;.content</code> (an inert fragment whose scripts/images don't run/load until cloned into a live document).</li><li><em>Note:</em> a single batched <code>append(...nodes)</code> already coalesces into one reflow, so a fragment's win is largest when you must touch the live tree repeatedly or clone a <code>&lt;template&gt;</code> via <code>cloneNode(true)</code>.</li></ul>"},
  {"sec":"FE-DOM","front":"Layout thrashing: what is it and how do you batch DOM reads/writes?","back":"<p><strong>Layout thrashing (forced synchronous reflow)</strong> happens when you interleave DOM <em>writes</em> and geometry <em>reads</em> in a loop: each read of a <strong>layout property</strong> (<code>offsetWidth</code>, <code>getBoundingClientRect()</code>, <code>scrollTop</code>, <code>getComputedStyle()</code>) forces the browser to flush pending style/layout <strong>synchronously</strong> so the value is accurate — O(n) reflows.</p><ul><li><strong>Fix:</strong> <em>batch all reads first, then all writes</em> (read/write separation).</li><li>Schedule visual writes in <code>requestAnimationFrame</code>; libraries like <strong>fastdom</strong> formalize this.</li><li><code>content-visibility</code> / <code>contain</code> can scope/skip layout work.</li><li><em>Tell-tale:</em> a loop reading <code>el.offsetHeight</code> then setting <code>el.style.height</code> — classic thrash.</li></ul>"}
);

Object.assign(GLOSSARY_DATA, {
  "node-vs-element": {"term":"Node vs Element","group":"FE-DOM","eli5":"Every piece of the page is a node; only the tag pieces are elements.","memory":"All Elements are Nodes; not all Nodes are Elements.","full":"<p>The DOM is a tree of <strong>Node</strong> objects. <code>Node</code> is the abstract base — its subtypes include <code>Element</code> (tags), <code>Text</code>, <code>Comment</code>, and <code>Document</code>. <code>nodeType</code> distinguishes them (1=element, 3=text, 8=comment, 9=document).</p><p>This is why <code>childNodes</code>/<code>firstChild</code> (node-level, include whitespace text and comments) differ from <code>children</code>/<code>firstElementChild</code> (element-level). Reaching for the element-level APIs avoids surprise whitespace text nodes from your indentation.</p>","trap":"Assuming <code>childNodes</code> only contains elements — it includes whitespace <strong>text nodes</strong> from source formatting.","worked":"<code>&lt;ul&gt; &lt;li&gt;a&lt;/li&gt; &lt;/ul&gt;</code> → <code>ul.childNodes.length</code> is 3 (text, li, text) but <code>ul.children.length</code> is 1.","related":["live-vs-static","document-fragment"]},
  "live-vs-static": {"term":"Live vs Static Collections","group":"FE-DOM","eli5":"Some lists update themselves when the page changes; some are frozen snapshots.","memory":"querySelectorAll = Snapshot (Static); getElementsBy = Live.","full":"<p><code>getElementsByTagName/ClassName</code> and <code>.children</code> return <strong>live</strong> <code>HTMLCollection</code>s that mirror the DOM in real time. <code>querySelectorAll</code> returns a <strong>static</strong> <code>NodeList</code> snapshot taken at call time. (<code>NodeList</code> can be either — <code>childNodes</code> is live, but <code>querySelectorAll</code>'s is static.)</p><p>Live collections can cause skipped elements or infinite loops when you add/remove nodes while iterating by index, because the length shifts mid-loop. Snapshot with spread (<code>[...coll]</code>) before mutating.</p>","trap":"Iterating a live <code>HTMLCollection</code> by index while removing items — every removal shifts indices and you skip elements.","worked":"<code>const items=document.getElementsByClassName('x'); while(items.length) items[0].remove();</code> works <em>because</em> it's live; an index <code>for</code> loop would skip half.","related":["node-vs-element","query-selector"]},
  "query-selector": {"term":"querySelector / querySelectorAll","group":"FE-DOM","eli5":"Find elements using the same selectors you write in CSS.","memory":"querySelector = first match; querySelectorAll = all matches, frozen.","full":"<p><code>querySelector(sel)</code> returns the first element matching a <strong>CSS selector</strong> (or <code>null</code>); <code>querySelectorAll(sel)</code> returns a static <code>NodeList</code> of all matches. Both exist on <code>document</code> and any element, scoping to that subtree.</p><p>Flexible but generally slower than <code>getElementById</code> for hot single-id lookups. Only the returned <code>NodeList</code> has a native <code>forEach</code>; spread it for <code>map</code>/<code>filter</code>.</p>","trap":"Believing the result is live — it's a snapshot, so nodes added later won't appear until you re-query.","worked":"<code>document.querySelectorAll('nav a[href^=\"/\"]')</code> selects internal nav links; <code>[...nodeList].map(a=&gt;a.href)</code>.","related":["live-vs-static","event-delegation"]},
  "event-flow": {"term":"Event Flow (Capture → Target → Bubble)","group":"FE-DOM","eli5":"An event dives down to the clicked thing, then ripples back up.","memory":"Trickle Down (capture), hit Target, Bubble Up.","full":"<p>A dispatched event travels in three phases: <strong>capture</strong> (window→target), <strong>target</strong>, then <strong>bubble</strong> (target→window). Listeners default to the bubble phase; pass <code>{capture:true}</code> for the capture phase.</p><p>Understanding flow is the foundation of delegation, and explains why a parent's listener can intercept or block a child's event before or after the target acts.</p>","trap":"Thinking all events bubble — <code>focus</code>, <code>blur</code>, and most <code>scroll</code>/<code>load</code> don't; use capture or <code>focusin</code>/<code>focusout</code>.","worked":"<code>document.addEventListener('click', fn, true)</code> fires <em>before</em> any bubbling button handler — useful for global capture-phase guards.","related":["event-delegation","stop-propagation","target-vs-currenttarget"]},
  "event-delegation": {"term":"Event Delegation","group":"FE-DOM","eli5":"Put one listener on a parent and let child events bubble up to it.","memory":"One listener to rule them all (the children).","full":"<p>Delegation attaches a single listener to a stable ancestor and inspects <code>event.target.closest(sel)</code> to decide what was hit, leveraging <strong>bubbling</strong>. It scales to many/dynamic children with one listener and no re-binding when content changes.</p><p>It's the standard pattern for lists, tables, and any server-rendered or virtualized content.</p>","trap":"Trying to delegate non-bubbling events (<code>focus</code>/<code>blur</code>) on the bubble phase — they never reach the ancestor; use <code>focusin</code>/<code>focusout</code> or capture.","worked":"<code>ul.addEventListener('click', e =&gt; { const li = e.target.closest('li'); if (li &amp;&amp; ul.contains(li)) select(li); });</code>","related":["event-flow","target-vs-currenttarget","query-selector"]},
  "target-vs-currenttarget": {"term":"target vs currentTarget","group":"FE-DOM","eli5":"target is what you clicked; currentTarget is whose handler is running now.","memory":"tarGET = where it Got clicked; CURRENTtarget = where the listener CURRENTly lives.","full":"<p><code>event.target</code> is the element that originated the event and stays constant. <code>event.currentTarget</code> is the element whose listener is executing — it changes as the event propagates and equals <code>this</code> in a normal-function handler.</p><p>In delegation they diverge: <code>currentTarget</code> is the container you bound, <code>target</code> is the clicked descendant.</p>","trap":"Reading <code>currentTarget</code> after the handler returns (e.g. in a later <code>setTimeout</code>) — the browser resets it to <code>null</code> post-dispatch; save it to a local variable synchronously.","worked":"Delegated handler: <code>currentTarget</code> = <code>&lt;ul&gt;</code>, <code>target</code> = the <code>&lt;span&gt;</code> inside the clicked <code>&lt;li&gt;</code>.","related":["event-delegation","event-flow"]},
  "stop-propagation": {"term":"preventDefault vs stopPropagation vs stopImmediatePropagation","group":"FE-DOM","eli5":"One cancels the browser's reaction; the others stop the event from spreading.","memory":"preventDefault = cancel the ACTION; stopPropagation = cancel the TRAVEL; stopImmediate = travel + sibling listeners.","full":"<p><code>preventDefault()</code> cancels the default browser action without affecting propagation. <code>stopPropagation()</code> halts further capture/bubble travel but lets other listeners on the same node run. <code>stopImmediatePropagation()</code> additionally blocks remaining same-node listeners.</p><p>They are orthogonal: you often call <code>preventDefault()</code> and let the event keep bubbling, or stop propagation while allowing the default action.</p>","trap":"Calling <code>preventDefault()</code> inside a <code>passive:true</code> listener — it's ignored with a console warning.","worked":"Custom dropdown: <code>e.preventDefault()</code> stops the link navigating, while the event still bubbles to a delegation root that closes other menus.","related":["event-flow","addeventlistener-options","event-delegation"]},
  "addeventlistener-options": {"term":"addEventListener options (capture/once/passive/signal)","group":"FE-DOM","eli5":"Extra settings that change when, how often, and how a listener runs and is removed.","memory":"COPS: Capture, Once, Passive, Signal.","full":"<p>The options object tunes a listener: <code>capture</code> (phase), <code>once</code> (auto-remove after one fire), <code>passive</code> (promise not to <code>preventDefault</code>, so scroll isn't blocked), and <code>signal</code> (remove via an <code>AbortController</code>).</p><p><code>passive</code> is critical for scroll/touch performance (and is the browser default for <code>wheel</code>/<code>touchstart</code> on window/document); <code>signal</code> is the cleanest teardown, letting one <code>abort()</code> drop many listeners — ideal on component unmount.</p>","trap":"Passing a boolean where an options object is expected (or vice versa) — the legacy 3rd arg is the <code>capture</code> boolean; <code>removeEventListener</code> must match capture and the same function reference.","worked":"<code>const ac=new AbortController(); el.addEventListener('scroll',fn,{passive:true,signal:ac.signal}); /* later */ ac.abort();</code>","related":["event-flow","stop-propagation"]},
  "observers": {"term":"Intersection / Mutation / Resize Observers","group":"FE-DOM","eli5":"Browser watchers that tell you when things become visible, change in the DOM, or change size — efficiently.","memory":"Intersection=VISIBILITY, Mutation=STRUCTURE, Resize=SIZE.","full":"<p>These async, batched observer APIs replace expensive scroll/poll loops. <code>IntersectionObserver</code> reports overlap with a root (lazy-load, infinite scroll). <code>MutationObserver</code> reports DOM tree/attribute/text changes (delivered as a microtask). <code>ResizeObserver</code> reports element box-size changes (responsive components).</p><p>All deliver records to a callback off the synchronous event path, reducing jank versus hand-written scroll/resize handlers.</p>","trap":"Writing layout-affecting styles inside a <code>ResizeObserver</code> callback and triggering the <code>ResizeObserver loop completed with undelivered notifications</code> warning — defer changes with <code>requestAnimationFrame</code>.","worked":"<code>const obs=new IntersectionObserver((es)=&gt;es.forEach(e=&gt;{if(e.isIntersecting){load(e.target);obs.unobserve(e.target);}}),{rootMargin:'200px'})</code>","related":["layout-thrashing","request-idle-callback"]},
  "layout-thrashing": {"term":"Layout Thrashing","group":"FE-DOM","eli5":"Mixing 'measure' and 'change' in a loop forces the browser to redo layout over and over.","memory":"Read all, THEN write all — never ping-pong.","full":"<p>Reading a geometry property (<code>offsetWidth</code>, <code>getBoundingClientRect()</code>, <code>scrollTop</code>) after a write forces a <strong>synchronous reflow</strong> so the returned value is accurate. In a loop this is O(n) reflows = <strong>layout thrashing</strong>.</p><p>The fix is <strong>read/write batching</strong>: gather all measurements first, then apply all mutations (ideally in <code>requestAnimationFrame</code>). Tools like fastdom enforce the pattern.</p>","trap":"Assuming the browser coalesces your reads — any forced geometry read flushes the pending style/layout queue immediately, defeating batching.","worked":"Bad: <code>for(el of els){ el.style.width = el.offsetWidth+10+'px'; }</code> → instead measure all widths into an array first, then assign in a second loop.","related":["observers","document-fragment","request-idle-callback"]},
  "document-fragment": {"term":"DocumentFragment","group":"FE-DOM","eli5":"An invisible bag to assemble nodes, then drop them all in at once.","memory":"Build off-screen, insert once.","full":"<p>A <code>DocumentFragment</code> is a parentless, off-document node container. Appending nodes to it costs nothing layout-wise; appending the fragment to the live DOM moves its children in and leaves the fragment empty.</p><p><code>&lt;template&gt;.content</code> is an inert fragment whose scripts/images don't execute or load until cloned into the document — ideal for repeatable markup via <code>cloneNode(true)</code>.</p>","trap":"Expecting the fragment node itself to end up in the DOM — only its <em>children</em> are inserted; the fragment is left empty.","worked":"<code>const f=document.createDocumentFragment(); for(const t of items){const li=document.createElement('li');li.textContent=t;f.append(li);} list.append(f);</code>","related":["layout-thrashing","node-vs-element"]},
  "request-idle-callback": {"term":"requestIdleCallback","group":"FE-DOM","eli5":"Run non-urgent work only when the browser has spare time after painting.","memory":"Idle = leftovers; rAF = every frame.","full":"<p><code>requestIdleCallback(cb,{timeout})</code> defers low-priority work to idle periods, passing an <code>IdleDeadline</code> with <code>timeRemaining()</code> (capped ~50ms). Yield before it reaches 0 and re-schedule remaining work to keep frames smooth.</p><p>Use for analytics, prefetch, and background processing — never animation (use <code>requestAnimationFrame</code>) or heavy layout-affecting DOM (no reflow budget). <strong>Safari still ships it behind a feature flag in 2026</strong>, so feature-detect and fall back to <code>setTimeout</code>.</p>","trap":"Relying on it firing promptly — without a <code>timeout</code> it can be starved indefinitely on a busy main thread (and it's absent in default Safari).","worked":"<code>function flush(d){ while(d.timeRemaining()&gt;0 &amp;&amp; queue.length) process(queue.shift()); if(queue.length) requestIdleCallback(flush,{timeout:2000}); } requestIdleCallback(flush,{timeout:2000});</code>","related":["layout-thrashing","observers"]}
});
