/* Pandas for Trade Data (IP-PANDAS) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"IP-PANDAS","front":"You have internal fills and an exchange drop-copy file. How do you reconcile them in pandas to produce a break report?","back":"<strong>Outer merge with an indicator</strong> is the core reconciliation primitive. Match on <strong>stable identifiers</strong>, not on incidental fields.<ul><li><code>brk = internal.merge(exch, on=['exec_id','symbol'], how='outer', suffixes=('_int','_exch'), indicator=True)</code></li><li><code>_merge=='left_only'</code> &rarr; in internal but NOT on exchange (we think we traded, venue disagrees).</li><li><code>_merge=='right_only'</code> &rarr; on exchange but NOT internal (<strong>missing capture</strong> — often the scariest break).</li><li><code>_merge=='both'</code> &rarr; matched key; now check <strong>economics</strong>: <code>qty</code>, <code>price</code>, <code>side</code>, fees within tolerance.</li></ul>Then <em>tolerate expected discrepancies</em> (timing, partial fills, fees, rounding) and emit unmatched rows + economic mismatches as the <strong>break report</strong> for the ops tool. The exchange drop-copy here is the FIX <code>35=8</code> Execution Report feed (the standard trade-capture source). This is literally compare-internal-vs-external-truth, match on stable IDs, flag the rest."},
  {"sec":"IP-PANDAS","front":"Explain merge how= (inner/left/right/outer) and when each is right for trade ops.","back":"<ul><li><code>how='inner'</code> &mdash; only keys in BOTH. Use when you only care about matched trades; <strong>hides breaks</strong>, so wrong for first-pass recon.</li><li><code>how='left'</code> &mdash; keep all internal, attach exchange where found; <code>NaN</code> on the right = <strong>internal trade with no exchange match</strong>.</li><li><code>how='right'</code> &mdash; mirror; surfaces <strong>exchange fills we never captured</strong>.</li><li><code>how='outer'</code> &mdash; <strong>everything from both sides</strong>; the only one that catches breaks in <em>both</em> directions at once. Pair with <code>indicator=True</code>.</li></ul>Default <code>how='inner'</code> is the trap: it silently drops the exact rows recon exists to find."},
  {"sec":"IP-PANDAS","front":"What does merge(indicator=True) give you and why is it the reconciliation workhorse?","back":"<code>indicator=True</code> adds a categorical column <code>_merge</code> with three values: <code>left_only</code>, <code>right_only</code>, <code>both</code> (you can rename via <code>indicator='source'</code>).<ul><li><code>left_only</code> &rarr; internal-only break</li><li><code>right_only</code> &rarr; exchange-only break (missing trade capture)</li><li><code>both</code> &rarr; matched; proceed to economic diffs</li></ul>One <code>value_counts()</code> on <code>_merge</code> is an instant recon dashboard: <code>brk['_merge'].value_counts()</code>. It turns set membership (which side has the key) into a column you can filter, count, and route to exception queues."},
  {"sec":"IP-PANDAS","front":"How do you do an anti-join in pandas (rows in A not in B)?","back":"pandas has no <code>anti</code> keyword, so use the indicator pattern:<ul><li><code>merged = a.merge(b, on='key', how='left', indicator=True)</code></li><li><code>anti = merged[merged['_merge']=='left_only'].drop(columns='_merge')</code></li></ul>Alternatives: <code>a[~a['key'].isin(b['key'])]</code> (clean for a single key, but loses multi-column keys and is awkward with NaNs). The anti-join <strong>is</strong> the break list — every settlement break starts as 'this row exists on one side only'. <em>Watch NaN keys</em>: NaN never equals NaN, so rows with null match keys land in the anti-join spuriously; drop or coalesce keys first."},
  {"sec":"IP-PANDAS","front":"How do you compute net positions by symbol from a fills file?","back":"Sign the quantity by side, then <strong>groupby + agg</strong>:<ul><li><code>f['signed'] = f['qty'] * f['side'].map({'BUY':1,'SELL':-1})</code></li><li><code>pos = f.groupby('symbol').agg(net_qty=('signed','sum'), gross=('qty','sum'), n_fills=('exec_id','size'))</code></li><li>Notional is cleaner as its own column first: <code>f['notional'] = f['signed'] * f['price']</code>, then <code>...notional=('notional','sum')</code> in the agg.</li></ul>For VWAP per symbol: <code>(f.qty*f.price).groupby(f.symbol).sum() / f.groupby('symbol').qty.sum()</code>. Named-agg (<code>net_qty=('signed','sum')</code>) is the idiomatic, readable form. This collapses a raw fill blotter into a <strong>position-keeping</strong> snapshot you reconcile against the clearer/custodian Start-of-Day (SOD) file."},
  {"sec":"IP-PANDAS","front":"asof merge — what is it and why does trade ops need it?","back":"<code>pd.merge_asof</code> joins on <strong>nearest key</strong> (typically time) rather than exact equality — a <em>nearest-time</em> join. Both sides must be <strong>sorted</strong> on the key.<ul><li><code>pd.merge_asof(fills, quotes, on='ts', by='symbol', direction='backward', tolerance=pd.Timedelta('1s'))</code></li><li><code>direction='backward'</code> = last quote at-or-before the fill (the prevailing market) — the usual choice for stamping a fill with its reference price.</li><li><code>by='symbol'</code> matches within instrument; <code>tolerance</code> caps how stale a match may be.</li></ul>Use it to attach prevailing NBBO/mark to each fill for TCA, slippage, or to line up two feeds whose timestamps never match exactly. Exact <code>merge</code> would drop everything."},
  {"sec":"IP-PANDAS","front":"Why never use iterrows() for trade files — and what do you do instead?","back":"<code>iterrows()</code> yields a new Series per row (boxing each value, often up-casting dtypes) and runs in Python, not C — it's <strong>orders of magnitude slower</strong>, and you should never mutate the frame while iterating it. On a multi-million-row fills file it's a non-starter.<ul><li><strong>Vectorize</strong>: <code>f['notional'] = f.qty * f.price</code> operates on whole arrays in C.</li><li>Conditionals: <code>np.where(cond, a, b)</code> or <code>np.select</code>.</li><li>Mapping: <code>.map()</code> / <code>.merge()</code> for lookups instead of per-row dict gets.</li><li>Group logic: <code>groupby().transform()</code>, not a manual loop.</li></ul><em>Rule of thumb:</em> if you wrote a Python <code>for</code> over rows, there is almost always a vectorized form. Reserve loops for genuinely sequential state machines."},
  {"sec":"IP-PANDAS","front":"How do you represent money in pandas — float vs Decimal — and what breaks?","back":"<strong>Binary float64 cannot represent most decimals exactly</strong>: <code>0.1 + 0.2 == 0.30000000000000004</code>. Summing millions of fees/notionals accumulates error and produces <strong>phantom recon breaks</strong> of fractions of a cent.<ul><li>Store prices/amounts as <strong>integer minor units</strong> (cents, or 1e-8 'sats' for crypto) — exact and fast.</li><li>Or <code>Decimal</code> via <code>dtype=object</code> (exact, but slow and not vectorized).</li><li>If you must use float, <strong>never <code>==</code></strong> — compare with a tolerance: <code>np.isclose(a, b, atol=0.005)</code>.</li></ul>Tolerances are also correct ops policy: timing, rounding, and fee discrepancies are <em>expected</em>, so recon should match within tolerance, not bit-exact."},
  {"sec":"IP-PANDAS","front":"You loaded a fills CSV and amounts behave oddly. How do dtypes cause this?","back":"<code>read_csv</code> <strong>infers dtypes</strong>, and inference bites:<ul><li>A symbol/ID column with leading zeros or mixed content becomes <code>object</code>; an all-numeric ID may become <code>int64</code> and <strong>drop leading zeros</strong> — pass <code>dtype={'account':'string','cusip':'string'}</code>.</li><li>One bad cell turns a whole price column to <code>object</code>, so arithmetic silently does string ops or errors.</li><li>Missing ints force a column to <code>float</code> (NaN is a float) — use nullable <code>Int64</code>.</li><li>Timestamps stay strings unless you <code>parse_dates=</code> / <code>pd.to_datetime</code>.</li></ul>Always inspect <code>df.dtypes</code> first and set dtypes explicitly on ingest. Use <code>pd.to_numeric(col, errors='coerce')</code> to surface unparseable values as NaN you can quarantine."},
  {"sec":"IP-PANDAS","front":"How do you handle tz-aware timestamps across equities, 24/7 crypto, and prediction markets?","back":"Store and compute in <strong>UTC</strong>; convert to a market tz only for display/session logic.<ul><li><code>ts = pd.to_datetime(df.ts, utc=True)</code> &rarr; tz-aware UTC.</li><li><code>ts.dt.tz_convert('America/New_York')</code> for US equity sessions / DST-correct boundaries.</li><li><strong>Never compare tz-aware to tz-naive</strong> — pandas raises a <code>TypeError</code>.</li><li><code>tz_localize</code> assigns a zone to naive times; <code>tz_convert</code> shifts an already-aware time.</li></ul>Crypto is 24/7 (no session reset); equities have ET sessions and a T+1 clock; prediction markets settle on <strong>event resolution</strong>, not a clock — keep an explicit <code>settle_ts</code> separate from <code>exec_ts</code>. A classic bug: a stored 'ET' column that is actually UTC mislabeled — always re-derive from the raw exchange timestamp."},
  {"sec":"IP-PANDAS","front":"resample() vs groupby() for time series — when and how?","back":"<code>resample</code> is groupby for a <strong>DatetimeIndex</strong> on fixed calendar/clock frequencies; it fills the time grid even where there are no trades.<ul><li>1-min OHLCV bars: <code>f.set_index('ts').resample('1min').agg(o=('price','first'), h=('price','max'), l=('price','min'), c=('price','last'), v=('qty','sum'))</code></li><li>Closed/label side matters: <code>resample('1min', closed='left', label='left')</code>.</li><li><code>asfreq()</code> just reindexes to the grid; <code>resample()</code> aggregates.</li></ul>Use plain <code>groupby</code> when buckets aren't time (symbol, trader, venue). For 24/7 crypto resample runs continuously; for equities filter to RTH first or you'll create empty overnight bars."},
  {"sec":"IP-PANDAS","front":"pivot_table for trade ops — give a real use.","back":"<code>pivot_table</code> reshapes long fills into a matrix and aggregates in one call:<ul><li>Net qty by symbol &times; venue: <code>pd.pivot_table(f, index='symbol', columns='venue', values='signed_qty', aggfunc='sum', fill_value=0, margins=True)</code></li><li><code>margins=True</code> adds row/column totals (the <code>All</code> line) — instant firm-wide net per symbol.</li><li><code>aggfunc</code> can be a list/dict: <code>aggfunc={'signed_qty':'sum','exec_id':'count'}</code>.</li></ul>vs <code>pivot</code> (no aggregation, errors on duplicate index/column pairs) — for trade data with many fills per (symbol,venue) you almost always want <code>pivot_table</code>. <code>fill_value=0</code> keeps absent cells as flat positions, not NaN."},
  {"sec":"IP-PANDAS","front":"How do you handle NaN correctly in a reconciliation pipeline?","back":"NaN means <em>missing/unmatched</em>, and its semantics trip people up:<ul><li><strong>NaN != NaN</strong> — never appears in a join match and breaks equality filters; use <code>.isna()</code>, not <code>== np.nan</code>.</li><li>After an outer merge, NaN on one side IS the break signal — don't blindly <code>fillna</code> it away.</li><li><code>fillna(0)</code> on quantities can fabricate flat positions; on prices it fabricates zero-cost — fill only where 0 is the true default.</li><li><code>.sum()</code> skips NaN by default (<code>skipna=True</code>), so a missing leg silently undercounts — decide if that's intended.</li><li>Nullable <code>Int64</code>/<code>Float64</code>/<code>boolean</code> and <code>pd.NA</code> propagate missingness cleanly through integer columns.</li></ul>Quarantine rows with NaN in <strong>match keys</strong> before joining — they pollute anti-joins."},
  {"sec":"IP-PANDAS","front":"A daily fills file is too big / slow in pandas. How do you make it performant?","back":"<ul><li><strong>Read less</strong>: <code>usecols=</code>, set <code>dtype=</code> on load, parse only needed dates. Downcast IDs to <code>category</code>.</li><li><strong>Better format</strong>: <code>Parquet</code> (columnar, typed, compressed) over CSV — faster IO, preserves dtypes, supports column/row-group pushdown.</li><li><strong>category dtype</strong> for symbol/venue/side: large memory savings + faster groupby/merge.</li><li><strong>Chunk</strong>: <code>pd.read_csv(..., chunksize=)</code> and aggregate incrementally; or move to <code>Polars</code>/<code>DuckDB</code>/Dask when it exceeds RAM.</li><li><strong>Pre-sort + merge_asof</strong> instead of nested lookups; avoid repeated <code>.apply</code>/<code>iterrows</code>.</li><li>Check <code>df.memory_usage(deep=True)</code> to find the offenders.</li></ul>For very large recon, push the join into <strong>SQL/DuckDB</strong> and only pull breaks into pandas."},
  {"sec":"IP-PANDAS","front":"merge_asof gotcha: what must be true and what silently goes wrong?","back":"<ul><li><strong>Both frames MUST be sorted</strong> on the <code>on=</code> key — unsorted input raises a <code>ValueError</code> ('left keys must be sorted'). Sort first.</li><li><code>by=</code> groups (e.g. per <code>symbol</code>) — forgetting it matches across instruments, stamping an AAPL fill with an MSFT quote.</li><li><code>direction</code> defaults to <code>'backward'</code>; <code>'forward'</code>/<code>'nearest'</code> change which side you grab — backward = last-known state, the usual recon/TCA choice.</li><li>Without <code>tolerance</code>, a fill can match a quote hours stale (e.g. across an outage) — always cap it.</li><li>Key dtypes must match (both tz-aware UTC datetimes, or both int).</li></ul>It's the right tool for joining feeds whose clocks never align exactly, but only if sorted, grouped, and tolerance-bounded."},
  {"sec":"IP-PANDAS","front":"Walk through a full pandas recon: internal vs exchange fills to a break report.","back":"<ol><li><strong>Ingest typed</strong>: <code>read_csv(..., dtype={'exec_id':'string','symbol':'category'}, parse_dates=['ts'])</code>; UTC timestamps; money as int minor units.</li><li><strong>Normalize</strong>: unify symbol/side enums, currency, and the match-key set across sources.</li><li><strong>Outer merge</strong> on stable IDs: <code>m = internal.merge(exch, on=['exec_id','symbol'], how='outer', indicator=True, suffixes=('_int','_exch'))</code>.</li><li><strong>Membership breaks</strong>: <code>left_only</code> (internal-only), <code>right_only</code> (uncaptured exchange fill).</li><li><strong>Economic breaks</strong> on <code>both</code>: <code>m.query(\"_merge=='both'\")</code> then flag where <code>qty_int!=qty_exch</code>, <code>side</code> differs, or <code>abs(px_int-px_exch) &gt; tol</code>.</li><li><strong>Tolerate</strong> expected fee/timing/rounding diffs.</li><li><strong>Emit</strong> one tidy break frame (id, type, internal vs exchange values, magnitude) to the exception tool / SQL queue.</li></ol>Under <strong>T+1</strong> (US, since May 28 2024) the intraday post-trade window is only a few hours — this must run fast, because unresolved breaks become settlement <strong>fails</strong>."},
  {"sec":"IP-PANDAS","front":"What does the post-trade lifecycle look like, and where does your pandas/SQL tooling plug in?","back":"<strong>Post-trade</strong> = everything after execution, before full settlement: trade capture &rarr; allocation &rarr; confirmation/affirmation &rarr; matching/clearing &rarr; settlement. For US equities the rails are <strong>DTCC</strong>: <strong>ITP</strong> (Institutional Trade Processing — pre-settlement matching/affirmation, 'match-to-instruct'), <strong>NSCC</strong> (the CCP — multilateral netting, novation, guarantee; CNS), <strong>DTC</strong> (the central securities depository — book-entry settlement & custody). Affirmation gates NSCC/DTC settlement.<ul><li>Your automation lives at <strong>capture and recon</strong>: ingest fills (FIX <code>35=8</code> drop-copy), reconcile internal vs an external truth (clearer/custodian SOD, exchange settlement files, on-chain data), and surface <strong>breaks</strong> fast.</li><li>Crypto adds custody (hot/cold wallets) and on-chain finality, 24/7, no central depository. Prediction markets settle <strong>$1 if the event resolves yes, else $0</strong>, with oracle/resolution risk.</li></ul>Under T+1, exceptions left unresolved = settlement fails, so STP/automation is the whole point of the role."}
);

Object.assign(GLOSSARY_DATA, {
  "outer-merge-indicator": {"term":"merge(how='outer', indicator=True)","group":"IP-PANDAS","eli5":"A join that keeps every row from both files and labels which file each row came from, so you can see what didn't match.","memory":"Outer = both sides survive; indicator = name tag (left_only / right_only / both).","full":"<p>The core pandas reconciliation primitive. <code>how='outer'</code> retains all keys from both frames; <code>indicator=True</code> adds a <code>_merge</code> categorical with values <code>left_only</code>, <code>right_only</code>, <code>both</code>.</p><p>In trade ops, <code>left_only</code> = internal record with no exchange match, <code>right_only</code> = exchange fill never captured internally, <code>both</code> = matched key (then check economics within tolerance). One <code>value_counts()</code> on <code>_merge</code> is a live break dashboard.</p>","trap":"Leaving <code>how</code> at its default <code>'inner'</code>, which silently drops the unmatched rows — i.e. the exact breaks reconciliation exists to surface.","worked":"<code>brk = internal.merge(exch, on=['exec_id','symbol'], how='outer', indicator=True); brk['_merge'].value_counts()</code>","related":["anti-join","reconciliation-primitive","match-key"]},
  "anti-join": {"term":"Anti-join","group":"IP-PANDAS","eli5":"Find the rows that exist on one side but have no partner on the other — the unmatched ones.","memory":"left_only = the leftovers; ~isin = 'not in the other set'.","full":"<p>pandas has no <code>anti</code> keyword. Idiom: left-merge with <code>indicator=True</code>, then keep <code>_merge=='left_only'</code>. For a single key, <code>a[~a['key'].isin(b['key'])]</code> works but loses multi-column keys and mishandles NaNs.</p><p>The anti-join <em>is</em> the break list: every settlement break begins as 'present on one side only'.</p>","trap":"NaN keys: NaN never equals NaN, so rows with null match keys fall into the anti-join even when a real match was intended. Quarantine/coalesce null keys before joining.","worked":"<code>m=a.merge(b,on='key',how='left',indicator=True); anti=m[m._merge=='left_only'].drop(columns='_merge')</code>","related":["outer-merge-indicator","match-key"]},
  "match-key": {"term":"Stable match identifier","group":"IP-PANDAS","eli5":"The columns you join on to decide two records are the same trade — pick ones that don't change between systems.","memory":"Match on the IDs both sides agree on; tolerate the rest.","full":"<p>Reconciliation compares internal vs an external truth source and matches on <strong>stable identifiers</strong> — provider/exchange trade id (<code>exec_id</code>), symbol, amount, currency, side, settlement date — while <em>tolerating</em> expected discrepancies (timing, partial fills, fees, rounding).</p><p>Unmatched rows become <strong>breaks</strong> resolved in an exception tool. Good keys are immutable across systems; joining on volatile fields (a local row number, a float price) manufactures false breaks.</p>","trap":"Joining on a non-deterministic or float field (e.g. raw price) so genuine matches fail; or assuming one key is unique when partials reuse it — use a composite key.","worked":"<code>internal.merge(exch, on=['exec_id','symbol'], how='outer', indicator=True)</code>","related":["outer-merge-indicator","reconciliation-primitive","money-precision"]},
  "dtcc-roles": {"term":"DTCC: ITP / NSCC / DTC","group":"IP-PANDAS","eli5":"Three DTCC pieces: one matches trades before settlement, one nets and guarantees them, one actually moves the shares.","memory":"ITP matches, NSCC nets & guarantees (CCP), DTC settles & holds (CSD).","full":"<p>DTCC runs the US post-trade rails through three roles: <strong>ITP</strong> (Institutional Trade Processing) handles pre-settlement matching/affirmation — 'match-to-instruct'; <strong>NSCC</strong> (National Securities Clearing Corporation) is the central counterparty (CCP): multilateral netting, novation, and a settlement guarantee, via Continuous Net Settlement (CNS); <strong>DTC</strong> (Depository Trust Company) is the central securities depository (CSD): book-entry settlement and custody.</p><p>Affirmation through ITP gates NSCC/DTC settlement — under T+1 this must happen same-day.</p>","trap":"Conflating the three: NSCC is the CCP/clearer (nets & guarantees), DTC is the depository (holds & settles), ITP is the pre-settlement matching layer. Don't call DTC the clearer or NSCC the depository.","worked":"Affirm via ITP → NSCC nets your day's buys/sells per symbol and novates → DTC moves the net shares/cash by book entry.","related":["post-trade-lifecycle","t-plus-one","reconciliation-primitive"]},
  "groupby-agg": {"term":"groupby / agg","group":"IP-PANDAS","eli5":"Split rows into buckets (like by symbol), then compute a number per bucket (like net position).","memory":"Split-apply-combine; named-agg for readable columns.","full":"<p><code>groupby</code> partitions rows by key(s); <code>agg</code> reduces each group. Sign quantity by side first, then <code>groupby('symbol').agg(net_qty=('signed','sum'), ...)</code> to turn a fill blotter into a position snapshot.</p><p><code>transform</code> returns group results aligned back to original rows (for ratios/normalization); <code>filter</code> keeps/drops whole groups. Named aggregation (<code>col=('src','func')</code>) is the idiomatic modern form.</p>","trap":"Forgetting to sign quantity by side before summing, so buys and sells inflate each other instead of netting; also <code>sum</code> skips NaN, silently undercounting.","worked":"<code>f['signed']=f.qty*f.side.map({'BUY':1,'SELL':-1}); pos=f.groupby('symbol').agg(net=('signed','sum'))</code>","related":["vectorization"]},
  "merge-asof": {"term":"merge_asof (nearest-time join)","group":"IP-PANDAS","eli5":"Join each row to the closest earlier row in another table by time, instead of needing an exact timestamp match.","memory":"asof = 'as of that moment' → last known value; sort first, group by symbol, cap with tolerance.","full":"<p><code>pd.merge_asof</code> joins on the <strong>nearest</strong> key (usually time), not equality. <code>direction='backward'</code> (default) grabs the last row at-or-before each target — e.g. the prevailing quote/mark for a fill. <code>by='symbol'</code> matches within instrument; <code>tolerance</code> bounds staleness.</p><p>Essential for joining feeds whose clocks never align exactly (fills vs quotes, two venues) for TCA, slippage, and time-aligned recon.</p>","trap":"Both inputs MUST be sorted on the key (unsorted raises ValueError), and you should set <code>by=</code> and <code>tolerance</code> — missing <code>by</code> stamps a fill with another symbol's quote, no tolerance matches stale data across outages.","worked":"<code>pd.merge_asof(fills, quotes, on='ts', by='symbol', direction='backward', tolerance=pd.Timedelta('1s'))</code>","related":["tz-aware","match-key"]},
  "money-precision": {"term":"Money precision (float vs Decimal)","group":"IP-PANDAS","eli5":"Binary floats can't store decimals like 0.10 exactly, so summing money drifts and creates fake mismatches.","memory":"Store cents as ints, or Decimal; never == on floats — use a tolerance.","full":"<p>float64 is binary: <code>0.1+0.2 = 0.30000000000000004</code>. Summing many fees/notionals accumulates error and produces phantom sub-cent recon breaks. Store amounts as <strong>integer minor units</strong> (cents; 1e-8 / sats for crypto) for exactness and speed, or <code>Decimal</code> via <code>dtype=object</code> (exact but unvectorized/slow).</p><p>If you must use float, compare with a tolerance (<code>np.isclose</code>), which doubles as correct ops policy since fee/rounding diffs are expected.</p>","trap":"Using <code>==</code> on float prices/amounts in a recon — it flags rows that differ only by floating-point dust as breaks.","worked":"<code>np.isclose(px_int, px_exch, atol=0.005)</code> instead of <code>px_int==px_exch</code>; or store <code>price_cents:int</code>.","related":["dtypes-ingest","reconciliation-primitive"]},
  "tz-aware": {"term":"tz-aware timestamps","group":"IP-PANDAS","eli5":"Timestamps that carry their time zone, so you don't accidentally compare a New York time to a UTC time.","memory":"Compute in UTC, display in market tz; localize (assign) vs convert (shift).","full":"<p>Store/compute in <strong>UTC</strong> (<code>pd.to_datetime(s, utc=True)</code>); convert to a market zone only for session logic/display (<code>dt.tz_convert('America/New_York')</code>). <code>tz_localize</code> attaches a zone to naive times; <code>tz_convert</code> shifts an aware time.</p><p>Equities run ET sessions on a T+1 clock; crypto is 24/7 with no reset; prediction markets settle on <strong>event resolution</strong>, not a clock — keep <code>settle_ts</code> distinct from <code>exec_ts</code>.</p>","trap":"Comparing tz-aware to tz-naive raises a TypeError; and trusting an 'ET' column that is actually UTC mislabeled — re-derive ET from the raw exchange timestamp.","worked":"<code>df.ts = pd.to_datetime(df.ts, utc=True); df.ts.dt.tz_convert('America/New_York')</code>","related":["merge-asof","reconciliation-primitive"]},
  "performance-large-files": {"term":"Large fill-file performance","group":"IP-PANDAS","eli5":"Make big trade files fast by loading less, using compact types, and columnar formats.","memory":"Read less + typed + Parquet + category; chunk or push to DuckDB when it won't fit.","full":"<p>Speed/memory levers for daily fills: <code>usecols=</code> and explicit <code>dtype=</code> on load; <code>category</code> dtype for symbol/venue/side; <strong>Parquet</strong> over CSV (columnar, typed, compressed, pushdown); <code>chunksize=</code> streaming; and offloading the join to <strong>DuckDB/Polars/Dask</strong> when data exceeds RAM.</p><p>Inspect <code>df.memory_usage(deep=True)</code> to find offenders; for very large recon, do the outer join in SQL and pull only breaks into pandas.</p>","trap":"Defaulting to CSV + object dtypes + <code>apply</code>/<code>iterrows</code> on millions of rows, then blaming pandas; the fix is typed columnar IO and vectorized/SQL joins.","worked":"<code>pd.read_parquet('fills.parquet', columns=['exec_id','symbol','qty','price'])</code> with <code>symbol</code> as <code>category</code>.","related":["vectorization","dtypes-ingest","outer-merge-indicator"]},
  "dtypes-ingest": {"term":"dtypes on ingest","group":"IP-PANDAS","eli5":"Tell pandas the real type of each column when loading, so IDs keep leading zeros and money stays numeric.","memory":"Inspect df.dtypes first; set dtype= on read; coerce with errors='coerce'.","full":"<p><code>read_csv</code> infers types and gets them wrong: numeric-looking IDs lose leading zeros as <code>int64</code>; one bad cell turns a price column to <code>object</code>; missing ints force <code>float</code>; timestamps stay strings unless parsed.</p><p>Set <code>dtype={'cusip':'string','symbol':'category'}</code>, use nullable <code>Int64</code> for ints-with-missing, <code>parse_dates=</code>, and <code>pd.to_numeric(col, errors='coerce')</code> to quarantine unparseable values as NaN.</p>","trap":"Trusting inferred dtypes — silent string arithmetic, dropped leading zeros on account/CUSIP, and int columns demoted to float by a single NaN.","worked":"<code>pd.read_csv('fills.csv', dtype={'account':'string','symbol':'category'}, parse_dates=['ts'])</code>","related":["money-precision","performance-large-files"]}
});
