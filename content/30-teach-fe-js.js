/* Teach me from the ground up: FE-JS (auto-assembled) */
enh({
  "this-binding": {"teach":"<p>At its core, <code>this</code> is just a hidden extra argument that JavaScript hands to your function — and the value it hands over depends on <em>how you call the function</em>, not where you typed it.</p><p>That last part is the whole drama. Most variables you write mean the same thing no matter how the code runs. <code>this</code> is different: it is <strong>late-bound</strong>, decided fresh at the moment of every call.</p><div class=\"analogy\"><p>Think of <code>this</code> like the word \"here\" in a phone call. The sentence \"It's raining here\" is written down once, but what \"here\" refers to depends entirely on <em>who is speaking and from where</em>. Move the speaker, and \"here\" changes — even though the sentence is identical.</p></div><h4>The four rules, in priority order</h4><p>When a function runs, JavaScript asks these questions top to bottom and stops at the first match:</p><ul><li><strong>new</strong> — called with <code>new Foo()</code>? <code>this</code> is the brand-new object being constructed.</li><li><strong>explicit</strong> — called via <code>fn.call(obj)</code>, <code>fn.apply(obj)</code>, or a function pre-bound with <code>fn.bind(obj)</code>? <code>this</code> is that <code>obj</code>.</li><li><strong>implicit</strong> — called as <code>obj.method()</code>? <code>this</code> is whatever sits <em>before the dot</em> — <code>obj</code>.</li><li><strong>default</strong> — a plain <code>fn()</code> with nothing in front? <code>this</code> is <code>undefined</code> in strict mode, otherwise the global object (<code>globalThis</code>).</li></ul><h4>Why detaching breaks it</h4><p>Look at the worked example: <code>const f = o.get; f();</code> returns <code>undefined</code>. You copied the <em>function</em> out of the object, leaving the dot behind. At call time there's no object before a dot, so the <strong>default</strong> rule kicks in. The function never \"remembered\" <code>o</code> — there was nothing to remember, because binding only happens at the call. That's exactly the trap: assuming <code>this</code> is captured lexically like a closure variable. For ordinary functions it is dynamic.</p><h4>Arrow functions: the deliberate exception</h4><p>Arrow functions have <strong>no <code>this</code> of their own</strong>. They reach outward to the surrounding lexical scope and use whatever <code>this</code> was there — permanently. None of the four rules apply; <code>call</code>/<code>apply</code> can't change it. This makes arrows perfect for callbacks (<code>setTimeout(() =&gt; this.tick())</code> keeps your object's <code>this</code>), but wrong as object methods that need a dynamic receiver — an arrow method would point at the enclosing scope, not the object.</p><div class=\"key\"><code>this</code> is resolved at <strong>call time</strong> by how the function is invoked — <code>new</code> &gt; explicit (<code>call</code>/<code>apply</code>/<code>bind</code>) &gt; implicit (object before the dot) &gt; default — except arrow functions, which freeze <code>this</code> lexically and ignore all four rules.</div>"},
  "hoisting-tdz": {"teach":"<p>At its core, hoisting is just JavaScript reading the whole scope <em>first</em> to learn what names exist, before running any line. The variables don't physically move — the engine simply notices the declarations early.</p><div class=\"analogy\"><p>Imagine a teacher taking attendance before class starts. She reads every name on the roster up front, so she knows who <em>should</em> be present. But knowing a name exists isn't the same as that student being ready to answer — some arrive late. Hoisting is the attendance read; <code>let</code>/<code>const</code> are the students who exist on the roster but can't be called on until they actually walk in.</p></div><h4>Two kinds of \"hoisted\"</h4><p>Here's the subtlety that trips people up: <em>all</em> declarations are hoisted, but they're not all <em>initialized</em> the same way.</p><ul><li><code>var</code> is hoisted <strong>and</strong> immediately set to <code>undefined</code>. So reading it before its line gives you <code>undefined</code> — no error, just a silent placeholder.</li><li><code>let</code>, <code>const</code>, and <code>class</code> are hoisted but left <strong>uninitialized</strong>. From the moment the scope opens until the line that declares them runs, they sit in the <strong>Temporal Dead Zone</strong>.</li><li>Function <em>declarations</em> are hoisted fully — name <em>and</em> body — so you can call them above where they're written.</li></ul><h4>What the TDZ actually does</h4><p>\"Temporal\" means \"relating to time\" — it's a <em>time window</em>, not a place. During that window, touching the variable throws a <code>ReferenceError</code> instead of returning <code>undefined</code>. In the worked example, <code>console.log(a)</code> prints <code>undefined</code> (var), but <code>console.log(b)</code> throws because <code>b</code> is still in its dead zone.</p><p>This is a <strong>feature, not a bug</strong>. The old <code>var</code> behavior let typos and use-before-define mistakes slide by as <code>undefined</code>, producing weird downstream failures far from the real cause. The TDZ converts that into a loud, immediate, point-at-the-line error.</p><h4>The trap worth internalizing</h4><p>People say \"<code>let</code> and <code>const</code> aren't hoisted.\" That's <em>wrong</em>, and the distinction matters in interviews. They <strong>are</strong> hoisted — the engine knows about them from the top of the block, which is why a <code>let</code> can shadow an outer variable even before its declaration line. What they lack is early <em>initialization</em>. The TDZ is precisely the mechanism that enforces \"hoisted but not yet usable.\"</p><div class=\"key\"><code>let</code>/<code>const</code>/<code>class</code> <strong>are</strong> hoisted — they're just uninitialized until their declaration runs, and the TDZ enforces that gap by throwing <code>ReferenceError</code> on early access, turning silent <code>undefined</code> bugs into loud ones.</div>"},
  "var-let-const": {"teach":"<p>At its core, <code>var</code>, <code>let</code>, and <code>const</code> are three ways to say \"give this name a value\" — they differ in <strong>where the name is visible</strong> and <strong>whether you can point it somewhere new later</strong>.</p><h4>The two axes that separate them</h4><p><strong>Scope</strong> — how far the variable's visibility reaches:</p><ul><li><code>var</code> is <strong>function-scoped</strong>: it leaks out of <code>if</code>/<code>for</code> blocks and is visible everywhere in the enclosing function. At the top level of a script it even attaches to <code>globalThis</code>.</li><li><code>let</code> and <code>const</code> are <strong>block-scoped</strong>: they live and die inside the nearest <code>{ }</code>.</li></ul><p><strong>Reassignment</strong> — whether you can rebind the name:</p><ul><li><code>var</code> and <code>let</code> can be reassigned. <code>var</code> can even be <em>redeclared</em> in the same scope; <code>let</code>/<code>const</code> cannot.</li><li><code>const</code> forbids reassignment entirely.</li></ul><p>All three are hoisted, but <code>var</code> initializes to <code>undefined</code> immediately while <code>let</code>/<code>const</code> sit in the Temporal Dead Zone until their line runs.</p><h4>The big misconception: const ≠ immutable</h4><p>This is the one that bites everyone. <code>const</code> locks the <strong>binding</strong> — the arrow from the name to the value — not the value itself.</p><div class=\"analogy\"><p>Think of a <code>const</code> variable like a name tag glued onto a specific house. You can't peel the tag off and stick it on a different house (no rebinding). But nothing stops you from repainting the walls, moving furniture, or knocking out a room inside <em>that</em> house. The address is fixed; the contents are not.</p></div><p>The worked example shows it exactly: <code>u.n = 2</code> succeeds because you're editing the object's contents, but <code>u = {}</code> throws <code>TypeError</code> because you're trying to re-aim the name at a brand-new object. If you genuinely need the <em>value</em> frozen, reach for <code>Object.freeze(u)</code> — but note that's <strong>shallow</strong> (nested objects stay mutable).</p><h4>What to actually use</h4><p>Modern style: reach for <code>const</code> by default. It documents \"this name never gets re-pointed,\" which makes code easier to reason about and catches accidental reassignment. Use <code>let</code> only when you truly need to reassign (loop counters, accumulators, values built across branches). Avoid <code>var</code> in new code — its function-scoping and global leakage are exactly the footguns <code>let</code>/<code>const</code> were created to remove.</p><div class=\"key\"><code>const</code> only freezes the <strong>binding</strong>, not the data behind it — a <code>const</code> object's properties stay mutable. Default to <code>const</code>, use <code>let</code> when you must reassign, and reach for <code>Object.freeze</code> (shallow) when you need real value immutability.</div>"},
  "prototype-chain": {"teach":"<p>At its core, the prototype chain is just JavaScript's answer to \"I asked an object for a property it doesn't have — where else should I look?\" The answer: ask its prototype, then <em>that</em> object's prototype, and so on up a chain until found or you hit <code>null</code>.</p><div class=\"analogy\"><p>Picture asking a question at a help desk. The clerk doesn't know, so they ask their supervisor; the supervisor doesn't know, so they ask the regional manager; eventually someone answers — or you reach the top of the org chart (<code>null</code>) and the answer is \"nobody knows\" (<code>undefined</code>). Crucially, the clerk didn't <em>memorize</em> the answer — they <strong>delegated</strong> the question upward.</p></div><h4>Delegation, not copying</h4><p>That word matters. Many languages do inheritance by <em>copying</em> methods into each instance. JavaScript instead keeps a live link — the internal <code>[[Prototype]]</code> — and <em>delegates</em> lookups along it. When you read <code>b.greet</code> in the worked example, <code>b</code> has no own <code>greet</code>, so the engine follows <code>b</code>'s prototype to <code>a</code>, finds it there, and runs it. That's why <code>Object.hasOwn(b, 'greet')</code> is <code>false</code> — <code>b</code> never owned the method; it borrowed it through the chain.</p><p>This is also memory-efficient: a thousand instances can share one method object on their common prototype instead of each carrying a copy.</p><h4>The confusing pair: .prototype vs [[Prototype]]</h4><p>Here's the trap that snags nearly everyone. <strong>Functions</strong> have a property literally named <code>.prototype</code>. That property is <em>not</em> the function's own prototype link — it's the object that will become the <code>[[Prototype]]</code> of any instance you create with <code>new</code>. Meanwhile every object (including the function) has its <em>actual</em> hidden link, the <code>[[Prototype]]</code>, exposed historically as <code>__proto__</code>.</p><ul><li><code>Foo.prototype</code> → the blueprint object handed to instances of <code>Foo</code>.</li><li><code>obj.__proto__</code> / <code>Object.getPrototypeOf(obj)</code> → the object <code>obj</code> actually delegates to.</li></ul><p>So <code>(new Foo()).__proto__ === Foo.prototype</code>. One is a <em>setup</em> property on a constructor; the other is the <em>live link</em> on a created object.</p><h4>How to set the link</h4><p>You wire prototypes deliberately with <code>Object.create(proto)</code> (make an object linked to <code>proto</code>), class <code>extends</code> (sets up the chain for you), or — sparingly, because it's a performance deoptimization — <code>Object.setPrototypeOf</code>.</p><div class=\"key\">JavaScript inherits by <strong>delegation up a live chain of <code>[[Prototype]]</code> links</strong>, not by copying — and a constructor's <code>.prototype</code> property (the blueprint for its instances) is a different thing from any object's actual <code>[[Prototype]]</code> link.</div>"},
  "class-fields": {"teach":"<p>At its core, a JavaScript <code>class</code> is just a cleaner way to set up the prototype chain you'd otherwise build by hand — but modern classes added genuinely new powers you couldn't easily fake before, the biggest being <strong>true privacy</strong>.</p><h4>The pieces of a modern class body</h4><ul><li><strong>Public fields</strong> — <code>x = 0</code> written directly in the class body. Each instance gets its own copy, initialized when the object is built. (Methods, by contrast, live once on the prototype and are shared.)</li><li><strong>Private members</strong> — names starting with <code>#</code>, like <code>#bal</code>. These are <em>enforced by the engine</em>: code outside the class body literally cannot read or even reference them. Try <code>new Acct().#bal</code> from outside and you get a <strong>SyntaxError</strong> — the parser rejects it before the program even runs.</li><li><strong>Static members and blocks</strong> — attached to the class itself, not instances. Useful for factory methods, constants, or one-time setup logic in a <code>static { ... }</code> block.</li></ul><h4>Why # is real and _ is theater</h4><div class=\"analogy\"><p>A leading-underscore <code>_password</code> is like a sign on an unlocked door that says \"Staff Only.\" Polite people stay out, but anyone can walk in. A <code>#password</code> is a door with an actual lock that only opens from inside the room — the building's architecture (the JS engine) physically prevents outsiders from entering. The convention <em>asks</em>; the syntax <em>enforces</em>.</p></div><p>This is the trap to nail in an interview: <code>_x</code> is just a naming convention and is fully accessible from anywhere. Only <code>#</code> fields give you real, engine-level encapsulation. Reach for <code>#</code> when you need to guarantee outside code can't poke at internal state.</p><h4>Quiet but important details</h4><ul><li><strong>Strict mode always.</strong> Class bodies run in strict mode automatically — no opting out.</li><li><strong>Methods are non-enumerable.</strong> They won't show up in a <code>for...in</code> loop, unlike properties you'd hand-roll onto a prototype.</li><li><strong>TDZ-bound.</strong> Like <code>let</code>/<code>const</code>, a class declaration sits in the Temporal Dead Zone — you can't use it before its line.</li><li><strong>Brand checks.</strong> Writing <code>#field in obj</code> safely tests whether <code>obj</code> is an instance carrying that private field, returning a <code>boolean</code> instead of throwing. It's the clean way to ask \"are you really one of mine?\"</li></ul><div class=\"key\">Only <code>#</code> fields are <strong>truly private — enforced by the engine, a SyntaxError to touch from outside</strong>. A leading underscore is just a convention with zero protection. Classes also bring per-instance public fields, static members, automatic strict mode, and the <code>#x in obj</code> brand check.</div>"},
  "es-modules": {"teach":"<p>At its core, both module systems answer the same question — \"how does one file share code with another?\" — but they answer it with opposite philosophies. <strong>ES Modules (ESM)</strong> are the modern, standardized, statically-analyzable system. <strong>CommonJS (CJS)</strong> is Node's older, dynamic, runtime system.</p><h4>The defining contrast: static vs dynamic</h4><p>ESM uses <code>import</code>/<code>export</code>, which must appear at the top level and are figured out <em>before</em> the code runs. Because the engine can see the entire import graph up front, it enables <strong>tree-shaking</strong> — bundlers prune exports nobody imports. CommonJS uses <code>require()</code>, an ordinary function call you can put anywhere, even conditionally. That flexibility costs static analysis: the tools can't be sure what you'll actually load.</p><p>ESM is also <strong>asynchronous</strong> (it supports top-level <code>await</code>), always runs in <strong>strict mode</strong>, and resolves imports through a load-then-link-then-evaluate process. CommonJS is <strong>synchronous</strong> — <code>require</code> blocks until the file is loaded and run.</p><h4>The subtle one: live bindings vs snapshots</h4><p>This is the deep idea and the classic gotcha.</p><div class=\"analogy\"><p>A CommonJS export is like <strong>photocopying</strong> a page and mailing it: the recipient holds a frozen snapshot. If the original author later edits their page, your copy is unchanged. An ESM export is like sharing a <strong>live Google Doc link</strong>: you're looking at a read-only window onto the <em>original</em>, so when the author updates it, what you see updates too.</p></div><p>The worked example shows the live-binding magic: <code>import { n, inc }</code>, then calling <code>inc()</code> bumps <code>n</code> inside the source module — and your imported <code>n</code> reads <code>1</code>, because it's a live, read-only view onto the source's binding, not a copy. Try this in CommonJS and you'd see the stale value, because <code>module.exports</code> handed you a <em>snapshot</em> at require time. (Read-only, though: importers can't reassign <code>n</code> themselves.)</p><p>That's exactly the trap — expecting a CJS <code>require</code> to reflect later changes to the exporting module's variables. It won't; only ESM bindings stay live.</p><h4>Practical guidance</h4><p>Prefer <strong>named exports</strong>: they're explicit, refactor-safe (renames are catchable), and tree-shake cleanly. A <code>default</code> export is nothing magical — it's just an export stored under the key literally named <code>default</code>.</p><div class=\"key\">ESM is static, async, strict, and exposes <strong>live read-only bindings</strong> (importers see later mutations of the source); CommonJS is dynamic, synchronous, and hands back a <strong>copied snapshot</strong> frozen at require time.</div>"},
  "shallow-vs-deep-clone": {"teach":"<p>At its core, the difference is one question: when you copy an object, do you also copy the things <em>inside</em> it, or just point at the same inner things? A <strong>shallow clone</strong> copies the top layer and shares everything nested. A <strong>deep clone</strong> duplicates the whole structure, top to bottom.</p><div class=\"analogy\"><p>Imagine photocopying a folder. A <strong>shallow</strong> copy duplicates the folder's cover and its <em>list</em> of contents — but the list still references the same physical documents in the same drawer. Scribble on a document via either folder and both \"copies\" see the edit. A <strong>deep</strong> copy walks into the drawer and photocopies every document too, so the new folder is genuinely independent.</p></div><h4>Why shallow copies surprise people</h4><p>Spread (<code>{...o}</code>), <code>Object.assign</code>, and <code>arr.slice()</code> all copy <em>by value</em> at the top level — but a nested object's value <em>is a reference</em>. So you copy the reference, not the object it points to. The worked example is the canonical trap: <code>const s = { ...a }; s.n.x = 9;</code> changes <code>a.n.x</code> too, because <code>s.n</code> and <code>a.n</code> are the <em>same</em> object. You thought you had an independent copy; you only protected the outermost layer.</p><h4>The modern deep-clone tool: structuredClone</h4><p>The built-in <code>structuredClone()</code> does a real deep copy and is impressively capable:</p><ul><li>Recurses through arbitrary nesting.</li><li>Correctly clones <code>Date</code>, <code>Map</code>, <code>Set</code>, <code>RegExp</code>, and typed arrays.</li><li>Handles <strong>circular references</strong> (object pointing back at itself) without infinite-looping.</li></ul><p>But it has firm limits. It <strong>cannot</strong> clone functions, DOM nodes, or symbols — those throw. And it <strong>drops the prototype</strong>: a class instance comes out as a plain object with the same data but none of its methods or class identity. Know these before reaching for it.</p><h4>Why not the old JSON trick?</h4><p>The classic <code>JSON.parse(JSON.stringify(obj))</code> is a tempting one-liner but quietly broken for real data: it <strong>silently drops</strong> <code>undefined</code> values and functions, <strong>mangles</strong> <code>Date</code> objects into strings, loses <code>Map</code>/<code>Set</code>, and <strong>throws on circular references</strong>. \"Silently\" is the dangerous word — you get a copy that <em>looks</em> fine until a missing field bites you in production. Prefer <code>structuredClone</code> for plain data; use a library or custom logic when you need to preserve functions or class identity.</p><div class=\"key\">A spread/<code>Object.assign</code> copy is <strong>shallow</strong> — nested objects are shared, so mutating them leaks into the original. For true independence use <code>structuredClone()</code>, remembering it can't clone functions/DOM/symbols and discards the prototype.</div>"},
  "primitive-vs-reference": {"teach":"<p>At its core, JavaScript stores two kinds of things differently: simple values it copies <em>directly</em>, and complex values it copies <em>by handing you a pointer</em> to one shared thing. That single distinction explains a huge amount of \"why did my variable change?\" confusion.</p><h4>The two camps</h4><p>The <strong>7 primitives</strong> — <code>string</code>, <code>number</code>, <code>boolean</code>, <code>null</code>, <code>undefined</code>, <code>symbol</code>, <code>bigint</code> — are <strong>immutable</strong> and copied <strong>by value</strong>. When you assign one to a new variable, the actual value is duplicated; the two are now totally independent.</p><p>Everything else — objects, arrays, functions — is a <strong>reference type</strong>. The variable doesn't hold the object; it holds a <em>pointer</em> to where the object lives. Copy the variable and you copy the pointer, so both names point at the <em>same</em> object.</p><div class=\"analogy\"><p>A primitive is like writing a phone number on a sticky note and handing someone a fresh sticky with the same digits — they can scribble on theirs without touching yours. A reference is like giving someone the <em>address</em> of your house. You both have the same address written down, but there's only one house — and if they go repaint the kitchen, you'll see it when you get home.</p></div><h4>What this explains</h4><ul><li><strong>Equality:</strong> <code>{} === {}</code> is <code>false</code> because each <code>{}</code> is a different house at a different address. Primitives compare by value (<code>1 === 1</code> is <code>true</code>); references compare by identity (same address or not).</li><li><strong>Mutation visibility:</strong> in the worked example, <code>const y = x; y.v = 2;</code> changes <code>x.v</code> too — same object, two name tags.</li><li><strong>Why cloning matters:</strong> if you want an independent object you must explicitly copy it; assignment alone never does.</li></ul><h4>The function-argument trap</h4><p>People think \"passing an object to a function copies it.\" Not quite. The <em>reference</em> (the address) is copied, so the function and caller share the same object. If the function does <code>obj.x = 5</code>, the caller sees it — same house, repainted. But if the function does <code>obj = {}</code>, it only re-points <em>its own local parameter</em> at a new object; the caller's variable still points at the original. So <strong>mutating properties leaks out; reassigning the parameter does not</strong>. JavaScript is always pass-by-value — it's just that the \"value\" for objects is a reference.</p><div class=\"key\">Primitives copy <strong>by value</strong> (independent); objects copy <strong>by reference</strong> (shared address). This is why <code>{} === {}</code> is false, why mutating a passed object is visible to the caller, and why you must explicitly clone to get independence.</div>"},
  "type-coercion": {"teach":"<p>At its core, coercion is JavaScript quietly converting a value from one type to another so an operation can proceed — and the strict equals <code>===</code> is your way of telling it \"don't you dare convert anything.\"</p><h4>Implicit vs explicit</h4><p><strong>Explicit</strong> coercion is when you ask for it: <code>Number('42')</code>, <code>String(x)</code>, <code>Boolean(v)</code>. Clear and intentional. <strong>Implicit</strong> coercion happens behind your back — when you use loose <code>==</code>, add a string with <code>+</code> (<code>1 + '2'</code> becomes <code>'12'</code>), or put a value in a boolean spot like <code>if (x)</code>. The engine silently inserts a conversion.</p><div class=\"analogy\"><p>Implicit coercion is like a hyper-helpful translator who, the moment two people speak different languages, instantly translates without asking — usually helpful, occasionally producing \"<code>0 == ''</code> is true\" nonsense nobody intended. Using <code>===</code> is like telling the translator to stay silent: if the two sides aren't already speaking the same language, the answer is simply \"no, they don't match.\"</p></div><h4>Why prefer ===</h4><p>Loose <code>==</code> coerces both sides before comparing, producing genuinely surprising results: <code>0 == ''</code> is <code>true</code>, but <code>0 === ''</code> is <code>false</code>. Strict <code>===</code> checks type <em>and</em> value with no conversion, so it's predictable. The one blessed use of <code>==</code> is the idiom <code>x == null</code>, which neatly matches <strong>both</strong> <code>null</code> and <code>undefined</code> in one check — handy and intentional.</p><h4>The falsy set — memorize it exactly</h4><p>In a boolean context, exactly these values are <strong>falsy</strong>:</p><ul><li><code>false</code></li><li><code>0</code>, <code>-0</code>, and <code>0n</code> (the BigInt zero)</li><li><code>''</code> (empty string)</li><li><code>null</code></li><li><code>undefined</code></li><li><code>NaN</code></li><li>(plus the legacy oddity <code>document.all</code>)</li></ul><p><strong>Everything else is truthy.</strong> That includes the great surprises: <code>[]</code> (empty array), <code>{}</code> (empty object), and <code>'0'</code> (a string containing zero) are all <strong>truthy</strong>. The worked example nails it: <code>Boolean([])</code> is <code>true</code>.</p><h4>Where it bites</h4><p>The trap is assuming \"empty\" means falsy. Writing <code>if (arr)</code> to check for an empty array is wrong — an empty array is truthy, so you'd check <code>arr.length</code> instead. And <code>if (str === '0')</code> versus <code>if (str)</code> behave very differently because <code>'0'</code> is truthy. These mistakes pass naive guard checks and slip into production.</p><div class=\"key\">Use <code>===</code> to avoid surprise conversions (reserve <code>==</code> only for the <code>x == null</code> idiom), and memorize that <strong>only</strong> <code>false</code>/<code>0</code>/<code>-0</code>/<code>0n</code>/<code>''</code>/<code>null</code>/<code>undefined</code>/<code>NaN</code> are falsy — <code>[]</code>, <code>{}</code>, and <code>'0'</code> are all truthy.</div>"},
  "nullish-optional": {"teach":"<p>At its core, these are two small operators that handle the same everyday headache — \"this thing might be missing\" — without the verbose <code>if</code>-checks that used to clutter your code. <code>?.</code> safely <em>reads</em> through possibly-missing values; <code>??</code> supplies a <em>default</em> only when something is actually absent.</p><h4>Optional chaining: ?.</h4><p>When you write <code>user?.address?.city</code>, each <code>?.</code> asks: \"is the thing on my left <code>null</code> or <code>undefined</code>?\" If yes, the whole expression <strong>short-circuits</strong> to <code>undefined</code> right there — no further access, no crash. If no, it proceeds normally. This prevents the infamous <code>Cannot read properties of undefined</code> <code>TypeError</code> when drilling into deep, possibly-absent structures. It works on property access (<code>a?.b</code>), indexing (<code>a?.[i]</code>), and calls (<code>fn?.()</code>).</p><div class=\"analogy\"><p>Optional chaining is like a cautious courier delivering a package up a chain of offices: \"Is reception there? Yes — is the manager's office there? No — okay, I'll stop and report 'undelivered' rather than barge into a wall.\" Without it, the courier slams into the first missing door and the whole delivery crashes.</p></div><h4>Nullish coalescing: ??</h4><p><code>a ?? b</code> returns <code>b</code> <strong>only</strong> when <code>a</code> is <em>nullish</em> — that is, <code>null</code> or <code>undefined</code>. For literally any other value (including <code>0</code>, <code>''</code>, <code>false</code>, <code>NaN</code>), it keeps <code>a</code>. This is the crucial difference from the old <code>||</code> trick.</p><h4>The trap: || destroys valid falsy values</h4><p>People long wrote <code>const port = cfg.port || 8080</code> to mean \"use 8080 if no port given.\" But <code>||</code> fires on <em>any</em> falsy value — so if someone configures <code>cfg.port = 0</code>, <code>||</code> wrongly throws it away and uses 8080. Same disaster with <code>''</code> as a deliberate empty string or <code>false</code> as a real setting. The worked example fixes it: <code>cfg.port ?? 8080</code> keeps <code>0</code> because <code>0</code> isn't nullish — it's a real, intended value. Use <code>??</code> whenever <code>0</code>, <code>''</code>, or <code>false</code> are legitimate inputs.</p><h4>One syntax rule</h4><p>You can't mix <code>??</code> directly with <code>||</code> or <code>&amp;&amp;</code> in the same expression without parentheses — <code>a || b ?? c</code> is a <code>SyntaxError</code>. Wrap to clarify intent: <code>(a || b) ?? c</code>. This is deliberate, forcing you to spell out precedence rather than guess.</p><div class=\"key\"><code>?.</code> stops a deep access from throwing when something is <code>null</code>/<code>undefined</code>; <code>??</code> defaults <strong>only</strong> on nullish values, so unlike <code>||</code> it preserves valid <code>0</code>, <code>''</code>, and <code>false</code> — always reach for <code>??</code> over <code>||</code> when those are legitimate values.</div>"},
  "garbage-collection": {"teach":"<p>At its core, garbage collection is JavaScript automatically cleaning up memory for things your program can no longer reach — and a \"leak\" is simply your code accidentally keeping a grip on stuff it no longer needs.</p><h4>Reachability is the whole idea</h4><p>The engine doesn't ask \"is this object still useful?\" — it can't read your mind. It asks a mechanical question: \"<strong>Starting from the roots, can I still get to this object by following references?</strong>\" The roots are things always considered alive: global variables and whatever's currently on the call stack. The collector <strong>marks</strong> everything reachable from the roots, then <strong>sweeps</strong> away the rest. That's the classic <strong>mark-and-sweep</strong> algorithm.</p><div class=\"analogy\"><p>Imagine a building where the cleaning crew removes any room you can no longer walk to from the lobby. As long as there's <em>some</em> hallway connecting the lobby to a room, the crew leaves it alone — even if you've completely forgotten that room exists and never visit it. A leak is exactly that: a forgotten room that still has a hallway leading to it, so it's never cleaned, and the building slowly fills up.</p></div><p>Key consequence: <strong>you can't force collection</strong>. There's no \"free this now\" button. The only thing you control is <em>reachability</em> — to let something be collected, you must make it unreachable by dropping every reference to it.</p><h4>The trap: GC doesn't make leaks impossible</h4><p>People assume \"automatic GC means I can't leak.\" Wrong. If something stays <em>reachable but unneeded</em>, the collector dutifully keeps it forever. Common front-end leaks:</p><ul><li><strong>Un-removed event listeners and <code>setInterval</code>s</strong> — the timer/listener holds a reference to its callback (and everything the callback closes over) until you explicitly cancel it.</li><li><strong>Detached DOM nodes</strong> — you removed a node from the page, but a JS variable still points at it, so it (and its subtree) can't be freed.</li><li><strong>Closures retaining large data</strong> — a long-lived function quietly keeping a big array alive in its scope.</li><li><strong>Growing module-level caches</strong> — a <code>Map</code> you keep adding to and never trim.</li></ul><h4>How to prevent leaks</h4><p>Always pair setup with <strong>teardown</strong>: <code>clearInterval</code> the timer, <code>removeEventListener</code> the handler, abort in-flight work with <code>AbortController</code>. In frameworks, return a cleanup function from your effect (the worked example: <code>useEffect(() =&gt; () =&gt; clearInterval(id), [])</code>). For caches and lookups where you don't want to <em>own</em> the keys, use <code>WeakMap</code> or <code>WeakRef</code> — they hold references that don't prevent collection, so entries vanish automatically once nothing else needs them.</p><div class=\"key\">GC frees only what's <strong>unreachable from the roots</strong> — you can't force it, you can only drop references. Automatic collection does <em>not</em> prevent leaks: timers, listeners, detached DOM, and ever-growing caches stay reachable and leak until you explicitly tear them down.</div>"}
});
