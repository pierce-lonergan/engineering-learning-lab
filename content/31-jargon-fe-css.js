/* Frontend jargon & acronyms — clickable reference (FE-CSS, auto-assembled) */
Object.assign(GLOSSARY_DATA, {
  "dpr": {"term":"DPR","group":"FE-CSS","aliases":["Device Pixel Ratio","devicePixelRatio"],"eli5":"The ratio of a screen's physical pixels to CSS pixels — e.g. 2 means each CSS pixel is rendered with a 2×2 block of physical pixels (a \"Retina\" display).","memory":"DPR = Device Pixel Ratio — physical pixels ÷ CSS pixels. Retina ≈ 2.","full":"<p><strong>DPR (Device Pixel Ratio)</strong> is the ratio between a device's physical (hardware) pixels and its logical CSS pixels (also called device-independent pixels). A DPR of <code>1</code> means one CSS pixel maps to one physical pixel; a DPR of <code>2</code> (common on \"Retina\"/HiDPI displays) means each CSS pixel is drawn using a $2 \\times 2 = 4$ block of physical pixels, so content stays the same physical size but appears far sharper.</p><p>The concept became prominent around 2010 when high-density mobile and laptop displays (Apple branded theirs \"Retina\") shipped widely. The CSS pixel was deliberately decoupled from the hardware pixel so that existing layouts wouldn't shrink to unreadable sizes on dense screens. Browsers expose the value via <code>window.devicePixelRatio</code>, and CSS exposes it through the <code>resolution</code> media feature (e.g. <code>min-resolution: 2dppx</code>) and responsive-image tooling.</p><p>Its purpose is to let developers serve appropriately sized raster assets and draw crisp graphics on any display density. DPR remains essential today: you multiply a canvas's backing-store dimensions by DPR for sharp drawing, use <code>srcset</code> with <code>x</code> descriptors to serve 2x/3x images only to dense screens (saving bandwidth on 1x screens), and account for fractional DPRs (1.5, 1.75, 2.625) introduced by OS display scaling on Windows/Android, which can cause sub-pixel rounding artifacts if ignored.</p>","worked":"<p>Serve density-appropriate images with <code>srcset</code> so a DPR-1 phone doesn't download a 2x asset:</p><ul><li><code>&lt;img src=\"logo.png\" srcset=\"logo.png 1x, logo@2x.png 2x, logo@3x.png 3x\" alt=\"Logo\"&gt;</code></li></ul><p>Draw a crisp <code>&lt;canvas&gt;</code> by scaling the backing store by DPR:</p><ul><li><code>const dpr = window.devicePixelRatio || 1;</code></li><li><code>canvas.width = cssWidth * dpr; canvas.height = cssHeight * dpr;</code></li><li><code>canvas.style.width = cssWidth + \"px\"; ctx.scale(dpr, dpr);</code></li></ul>","teach":"<p>At its core, DPR is just a conversion factor between two different kinds of \"pixel.\" There's the <strong>physical pixel</strong> — an actual tiny light on the screen — and the <strong>CSS pixel</strong> — the abstract unit you write in your stylesheets (<code>width: 100px</code>). On old screens these were the same thing. On modern dense screens they're not, and DPR is the number that connects them: physical pixels per CSS pixel.</p><div class=\"analogy\">Think of printing a photo. \"Inches\" is a fixed real-world size (like the CSS pixel — it should look the same physical size everywhere). \"Dots per inch\" is how many ink dots you pack into each inch (like physical pixels). A 4×6 photo at 300 DPI and the same photo at 150 DPI are the <em>same physical size</em>, but the 300 DPI one is crisper because it crams more dots in. DPR is your screen's \"dots per CSS-inch\" multiplier.</div><p>Why was this invented? When phones started shipping super-dense screens around 2010, the makers had a problem: if a CSS pixel still equaled a physical pixel, a button styled at <code>44px</code> would shrink to a tiny, untappable speck because the physical pixels were so small. So they kept the CSS pixel at a comfortable, roughly consistent physical size and let one CSS pixel cover a 2×2 (or 3×3) grid of physical pixels. Layouts stayed usable; everything just got sharper.</p><p>For you as a developer, DPR shows up in two big places. First, <strong>images</strong>: a photo that fills a 200-CSS-px box looks blurry on a DPR-2 screen unless you supply a 400-physical-px source. That's what <code>srcset</code> with <code>2x</code>/<code>3x</code> descriptors is for — and it lets you <em>avoid</em> shipping heavy images to DPR-1 screens that can't use them. Second, <strong>canvas/WebGL</strong>: you must size the drawing buffer by <code>window.devicePixelRatio</code>, or your graphics render blurry. Watch out for fractional DPRs (1.5, 1.75) from OS scaling, which cause rounding quirks.</p><div class=\"key\">DPR = physical pixels per CSS pixel. It decouples how big things look from how sharp they are. Use it to serve right-sized images (<code>srcset</code>) and to scale canvas backing stores so graphics stay crisp on HiDPI displays.</div>"},
  "hsl": {"term":"HSL","group":"FE-CSS","aliases":["Hue Saturation Lightness","Hue, Saturation, Lightness","hsl()"],"eli5":"A color format that describes a color by its hue (which color), saturation (how vivid), and lightness (how light) — intuitive, but NOT perceptually uniform.","memory":"HSL = Hue, Saturation, Lightness — three intuitive dials, but 'lightness' doesn't match real perceived brightness.","full":"<p><strong>HSL (Hue, Saturation, Lightness)</strong> is a cylindrical color model that re-parameterizes the RGB color space into three more human-friendly axes: <em>hue</em> (an angle 0–360° around the color wheel), <em>saturation</em> (0–100%, gray to vivid), and <em>lightness</em> (0–100%, black through the pure color to white). It's a sibling of HSV/HSB.</p><p>HSL and HSV emerged from computer-graphics research in the 1970s (notably Alvy Ray Smith's work) as easier ways for people to pick and adjust colors than raw RGB triplets. CSS added the <code>hsl()</code> (and <code>hsla()</code>) functions in CSS Color Module Level 3, and they've enjoyed universal browser support for well over a decade, making HSL a long-standing staple for theming.</p><p>HSL is still widely used because it's intuitive and easy to manipulate: nudge the hue to shift color, drop saturation to mute, raise lightness to tint. <strong>However</strong>, its critical flaw is that it is <em>not perceptually uniform</em> — its \"lightness\" is a mathematical midpoint of RGB, not perceived brightness. Yellow at 50% L looks dramatically brighter than blue at 50% L. This makes HSL-generated palettes visually uneven, which is precisely why perceptually-uniform models like <strong>OKLCH</strong> are increasingly preferred for serious color systems.</p>","worked":"<p>HSL is intuitive but its lightness lies — equal L does NOT mean equal perceived brightness:</p><p><code>--yellow: hsl(60 100% 50%);  /* reads BLAZING bright */</code><br><code>--blue:   hsl(240 100% 50%);  /* reads much darker — same 50% L! */</code></p><p>Easy manipulation is HSL's selling point — mute a color by lowering saturation:</p><p><code>--brand:       hsl(210 80% 50%);</code><br><code>--brand-muted: hsl(210 30% 50%);  /* same hue & lightness, less vivid */</code></p>","teach":"<p>At its core, HSL is just <em>a friendlier set of knobs on top of RGB</em>: instead of \"how much red, green, blue,\" you say \"which color, how vivid, how light.\"</p><ul><li><strong>Hue (H):</strong> an angle on the color wheel — 0° red, 120° green, 240° blue, back to 360°.</li><li><strong>Saturation (S):</strong> 0% is gray, 100% is fully vivid.</li><li><strong>Lightness (L):</strong> 0% black, 50% the \"pure\" color, 100% white.</li></ul><p>That intuitiveness is why people reach for it: shifting a theme's hue, muting by lowering saturation, or building light/dark variants by nudging lightness all feel natural — much nicer than juggling hex digits.</p><div class=\"analogy\">But here's the trap. HSL is like a thermostat whose \"50\" means a different actual temperature in every room of the house. Set every room to 50 and some rooms freeze while others bake. HSL's lightness works the same way: set yellow, blue, and red all to 50% L and they are <em>wildly</em> different perceived brightnesses — the yellow scorches your eyes, the blue looks nearly navy. The number says \"same,\" your eyes say \"nothing alike.\"</div><p>This breaks the most common reason people use HSL: generating palettes. If you pick a row of swatches at equal lightness expecting an even ramp, you get a lumpy, inconsistent set. You also can't trust L for contrast/accessibility decisions, because L isn't perceived brightness.</p><p>The fix the industry moved toward is <strong>OKLCH</strong>, a perceptually-uniform model where equal lightness really does look equally bright. Use HSL when quick, intuitive tweaks are fine; reach for OKLCH when even palettes and reliable contrast matter.</p><div class=\"key\">HSL = Hue, Saturation, Lightness — intuitive and universally supported, but NOT perceptually uniform. Equal L ≠ equal perceived brightness (yellow ≫ blue at 50%). Great for casual tweaks; OKLCH is the better tool for systematic, accessible color.</div>","related":["oklch"]},
  "css": {"term":"CSS","group":"FE-CSS","aliases":["Cascading Style Sheets"],"eli5":"The language that controls how HTML looks — colors, layout, spacing, fonts, animation — separately from the content itself.","memory":"CSS = Cascading Style Sheets: styles 'cascade' down through rules of varying specificity.","full":"<p><strong>CSS (Cascading Style Sheets)</strong> is the stylesheet language that describes the presentation of HTML (and other markup) — layout, color, typography, spacing, responsiveness, and animation. The 'cascade' refers to the algorithm that resolves conflicting rules using origin, specificity, and source order, so multiple stylesheets can layer predictably. CSS keeps presentation separate from structure, letting the same HTML be restyled without touching its content.</p><p>CSS was proposed by Håkon Wium Lie in 1994 and developed at the W3C, with CSS Level 1 published in 1996 and CSS 2 in 1998. Rather than a monolithic 'CSS3,' the language has since evolved as independently versioned <em>modules</em> — Flexbox, Grid, Custom Properties (variables), Transforms, Animations, Container Queries, and more — each progressing on its own timeline. This modular approach lets the platform add capabilities continuously.</p><p>Its purpose is to control rendering efficiently and maintainably across screen sizes and devices. CSS is irreplaceable and ever-growing — it is how every styled page looks the way it does. It also has accessibility consequences: visual order created with <code>flex</code>/<code>grid</code> <code>order</code> or <code>flex-direction: row-reverse</code> can diverge from DOM order, breaking keyboard tab sequence; the fix is to correct DOM order rather than reach for positive <code>tabindex</code>. The browser even folds CSS into building the accessibility tree, since properties like <code>display:none</code> or <code>visibility:hidden</code> remove content from that tree entirely.</p>","worked":"<p>A CSS reorder that creates an accessibility trap — the visual order no longer matches tab order:</p><ul><li><code>.row { display: flex; flex-direction: row-reverse; }</code></li></ul><p>With that rule, a 'Back' button written first in the DOM appears visually on the right, yet keyboard focus still reaches it first — confusing for keyboard and screen-reader users. The correct fix is to reorder the elements in the HTML so DOM order matches visual order, <em>not</em> to patch it with <code>tabindex=\"1\"</code>.</p>","teach":"<p>At its core, CSS is just <strong>a list of rules that say 'find these elements, and make them look like this.'</strong></p><p>Each rule has a selector (which elements) and declarations (what to do). The browser gathers every rule that could apply to an element from every stylesheet, then runs the <em>cascade</em> to decide who wins when rules conflict — more specific selectors and later rules generally beat weaker, earlier ones. That's why it's 'Cascading': styles flow down and combine rather than just overwriting blindly.</p><div class=\"analogy\">Think of CSS like a dress code with a hierarchy of authority. The building has a general rule ('business casual'), your department adds a stricter one ('jackets on client days'), and your manager's direct instruction overrides both. When two rules disagree, you follow whoever has more authority — that's specificity and the cascade. The content (you, the person) doesn't change; only how you're presented does.</div><p>A subtle and important point: CSS controls <em>visual</em> order, but it does <strong>not</strong> reorder the underlying DOM. Properties like <code>order</code> in Flexbox/Grid or <code>flex-direction: row-reverse</code> can move boxes on screen while the document's real sequence — the one keyboard focus and screen readers follow — stays put. When those two disagree, users tabbing through the page jump around in a way that doesn't match what they see. The right fix is always to make the DOM order match the intended order, never to band-aid it with positive <code>tabindex</code> values (which create their own focus chaos).</p><p>CSS also feeds the accessibility tree the browser builds from DOM + CSS: hiding something with <code>display:none</code> doesn't just visually remove it, it removes it from what assistive tech can perceive. So CSS choices are accessibility choices, not just cosmetic ones.</p><div class=\"key\">CSS styles content via cascading rules without changing the DOM. Keep visual order and DOM order aligned, and remember that some CSS (like <code>display:none</code>) also changes what's exposed to assistive technology.</div>","related":["html","api"]}
});
