/* Teach me from the ground up: IP-APIS (auto-assembled) */
enh({
  "fix-protocol": {"teach":"<p>At its core, <strong>FIX is just a shared language that trading firms and exchanges agree to speak so a computer at a hedge fund can tell a computer at an exchange \"buy 100 shares of Apple\" and get back a precise, machine-readable answer.</strong> Before FIX, every venue had its own bespoke wire format; FIX standardized the vocabulary so one engine can talk to dozens of venues.</p><h4>The shape of a message</h4><p>A FIX message is a flat list of <code>tag=value</code> pairs glued together by a single invisible byte called <code>SOH</code> (Start of Heading, byte <code>0x01</code> — shown as <code>|</code> in docs). Each <em>tag</em> is just a number that means a field. So <code>35=D</code> reads as \"tag 35, the message type, equals D, which means NewOrderSingle.\" <code>55=AAPL</code> is \"symbol = AAPL.\" That is the whole idea — no nested objects, no XML, just numbered fields for speed and unambiguity.</p><div class=\"analogy\"><p>Think of a FIX message like a paper form where every blank has a printed number. Box 35 is \"what kind of form is this,\" box 55 is \"which stock,\" box 38 is \"how many.\" Both sides own an identical legend, so nobody has to guess what box 44 means — it is always Price.</p></div><h4>Two layers stacked together</h4><p>The crucial mental model is that FIX has <strong>two layers in one connection</strong>. The <em>session layer</em> is the plumbing: it keeps the link alive and lossless using per-message sequence numbers (<code>34</code>), heartbeats (<code>35=0</code>), and resend requests (<code>35=2</code>) when a gap appears. The <em>application layer</em> is the business content: NewOrderSingle (<code>35=D</code>) going in, and ExecutionReports (<code>35=8</code>) coming back for every state change of that order.</p><p><strong>This split is exactly the gotcha.</strong> Gap recovery, logon, and heartbeats are session-layer concerns. Whether your order filled or got rejected is application-layer. Engineers who blur the two end up trying to \"resend a fill\" at the wrong layer. If you miss a message, the session layer replays it by sequence number; you never re-ask the application layer to recreate it.</p><h4>Why it matters in practice</h4><p>FIX dominates equities and derivatives because order flow cannot tolerate silent loss — a dropped fill means your books disagree with reality and you may be unknowingly long or short. The in-protocol reliability (sequence numbers + resend) is what earns that trust, and it is also why drop-copy feeds (a read-only mirror of your fills) ride on FIX. Crypto and prediction-market venues, being newer, mostly skip FIX and use REST plus WebSocket instead.</p><div class=\"key\"><p>FIX is a <code>tag=value</code> language with two layers in one pipe: a session layer that guarantees ordered, gap-free delivery (seqnums, heartbeats, resend), and an application layer that carries the order lifecycle (<code>35=D</code> in, <code>35=8</code> back). Keep the two straight and the rest follows.</p></div>"},
  "fix-session": {"teach":"<p>At its core, <strong>a FIX session is just a promise between two parties that no message will ever silently vanish, and messages will arrive in the exact order they were sent.</strong> It is the difference between shouting across a noisy room (maybe heard, maybe not) and a numbered conversation where both sides can prove nothing was missed.</p><h4>The sequence number is the whole trick</h4><p>Every message carries <code>34=MsgSeqNum</code>, a counter that only ever goes up: 215, 216, 217... Both sides independently track \"the next number I expect to receive.\" If you expect <code>34=216</code> but the next message you get says <code>34=219</code>, you know with certainty that 216, 217, and 218 were lost. You don't guess — the gap is arithmetic. You then fire a <strong>ResendRequest (<code>35=2</code>)</strong> saying \"replay 216 through 218,\" and the sender resends them, marking each with <code>43=PossDupFlag=Y</code> so you know they're replays, not new events.</p><div class=\"analogy\"><p>Imagine two people reading a book aloud to each other, each sentence numbered. If you hear sentence 219 right after 215, you say \"wait, read me 216 to 218 again.\" Because every sentence is numbered, a missing one is impossible to overlook — that is the entire reliability guarantee, just counting.</p></div><h4>Keeping a quiet link honest</h4><p>Markets go quiet between trades, so how do you tell \"nothing is happening\" from \"the connection died\"? <strong>Heartbeats (<code>35=0</code>)</strong>. At a negotiated interval called <code>HeartBtInt</code>, each side sends an empty heartbeat — a pulse proving it's alive. If a side goes suspiciously silent, the other sends a <strong>TestRequest (<code>35=1</code>)</strong>: \"are you there? echo this back.\" No echo within the interval means the link is dead and gets torn down. Sessions begin with a <strong>Logon (<code>35=A</code>)</strong> that negotiates these parameters and end cleanly with a <strong>Logout (<code>35=5</code>)</strong>.</p><h4>The trap that desyncs everyone</h4><p>Because both sides track sequence numbers independently, they must stay perfectly aligned. If you reset your sequence numbers mid-session without sending a proper <strong>SequenceReset (<code>35=4</code>)</strong>, the other side still expects the old high number, sees your low number, and concludes either \"this is an ancient duplicate\" or \"massive gap.\" The link stalls. The discipline: never touch sequence numbers by hand — let the protocol's reset machinery do it so both counters move together.</p><h4>Why this matters</h4><p>This is precisely why FIX is trusted for order flow and drop-copy. A missed ExecutionReport isn't lost forever — you recover it by sequence number. You can always reconcile to the truth rather than discovering days later that a fill never landed in your books.</p><div class=\"key\"><p>A FIX session turns an unreliable socket into a lossless, ordered pipe using nothing but a counter (<code>34</code>), keep-alives (<code>35=0</code>/<code>35=1</code>), and replay (<code>35=2</code>). Treat the sequence numbers as sacred — desync them and the whole link stalls.</p></div>"},
  "execution-report": {"teach":"<p>At its core, <strong>an ExecutionReport is the exchange's one universal \"here's what just happened to your order\" message.</strong> Acknowledged? Filled a bit? Filled the rest? Rejected? Cancelled? Every single one of those events arrives as the same message type — <code>35=8</code>. You learn the truth about your order only by reading these.</p><h4>Reading the report</h4><p>An ExecutionReport packs two kinds of information: <em>what just happened</em> and <em>where things stand now</em>. Let us dissect the key tags:</p><ul><li><code>150</code> <strong>ExecType</strong> — the event that triggered <em>this</em> report (e.g. <code>F</code> = a trade/fill just occurred, <code>0</code> = new/acked, <code>8</code> = rejected).</li><li><code>39</code> <strong>OrdStatus</strong> — the order's <em>current</em> overall state (e.g. <code>1</code> = partially filled, <code>2</code> = fully filled).</li><li><code>32</code> <strong>LastQty</strong> / <code>31</code> <strong>LastPx</strong> — the size and price of <em>just this</em> fill.</li><li><code>14</code> <strong>CumQty</strong> / <code>151</code> <strong>LeavesQty</strong> — the running totals: how much is done so far, and how much is still working.</li><li><code>11</code> ClOrdID / <code>41</code> OrigClOrdID — your own ids, so you can match the report back to the order you sent.</li><li><code>17</code> <strong>ExecID</strong> — a unique id for this specific execution event.</li></ul><div class=\"analogy\"><p>Think of ordering 100 items from a warehouse that ships in batches. Each shipping notice tells you what's in <em>this box</em> (LastQty/LastPx), and the running tally on the slip (CumQty done, LeavesQty still to come). ExecType is \"a box just shipped\"; OrdStatus is \"40 of 100 delivered, partially complete.\" You don't trust your memory of what you ordered — you trust the shipping notices.</p></div><h4>The single most important discipline</h4><p>Here is the trap that burns trading systems: when you fire an order, your own code optimistically thinks \"I sent a buy for 100, so I'm long 100.\" <strong>That optimistic state is not reality.</strong> Maybe it filled 40. Maybe it was rejected. Maybe it filled at a different price. Only the venue's <code>35=8</code> is authoritative. Book your positions and P&L from the ExecutionReport, never from your own order-entry intent. Systems that book on intent end up with phantom positions that don't reconcile against the broker.</p><h4>Why it's the unit of truth</h4><p>Because <em>all</em> order state flows through one message type, the stream of <code>35=8</code> reports is the natural backbone of a trade-capture or drop-copy feed, and the exact record you reconcile against at end of day. One message shape, every event, fully sequenced — that's why it's the system of record.</p><div class=\"key\"><p>The ExecutionReport (<code>35=8</code>) is the only authoritative word on your order. Read <code>150</code>/<code>39</code> for what happened and the running state, <code>32</code>/<code>31</code> for this fill, <code>14</code>/<code>151</code> for the totals — and book from it, never from your own optimistic order-entry state.</p></div>"},
  "new-order-single": {"teach":"<p>At its core, <strong>NewOrderSingle is the FIX message that says \"please trade this for me\" — one order, submitted to the venue.</strong> It is the request side of trading; the ExecutionReport (<code>35=8</code>) is the answer. Identified by <code>35=D</code>, it's the most-sent application message in most trading systems.</p><h4>Deconstructing the order</h4><p>An order is really just a handful of decisions, and each is a tag. Let us translate them into plain English:</p><ul><li><code>11</code> <strong>ClOrdID</strong> — a unique id <em>you</em> invent for this order. This is your idempotency key and your join key; treat it as the order's name.</li><li><code>55</code> <strong>Symbol</strong> — what to trade (e.g. <code>AAPL</code>).</li><li><code>54</code> <strong>Side</strong> — <code>1</code> = buy, <code>2</code> = sell.</li><li><code>38</code> <strong>OrderQty</strong> — how many.</li><li><code>40</code> <strong>OrdType</strong> — <code>1</code> = market (take any price now), <code>2</code> = limit (only at my price or better).</li><li><code>44</code> <strong>Price</strong> — the limit price (only meaningful for limit orders).</li><li><code>59</code> <strong>TimeInForce</strong> — how long it lives: <code>0</code> = Day, <code>1</code> = GTC (good-till-cancelled), <code>3</code> = IOC (fill what you can instantly, cancel the rest), <code>4</code> = FOK (fill all instantly or nothing).</li></ul><div class=\"analogy\"><p>It's like a delivery slip you hand a courier: what item (Symbol), buy or return (Side), how many (Qty), pay-any-price or only-up-to-$X (OrdType/Price), and \"deliver today only\" vs \"keep trying until I cancel\" (TimeInForce). Fill the slip, hand it over, wait for the receipt (the ExecutionReport).</p></div><h4>The lifecycle around it</h4><p>You send <code>35=D</code>; the venue replies with an ExecutionReport acking, rejecting, or filling. To cancel later, you send an <strong>OrderCancelRequest (<code>35=F</code>)</strong> that points back via <code>41</code> OrigClOrdID — \"cancel the order I previously named ORD123.\" That back-reference is why your ClOrdID has to be a reliable handle.</p><h4>The trap: reusing a ClOrdID</h4><p>Your ClOrdID must be unique per order. If you reuse <code>ORD123</code> for a second, different order, idempotency collapses: the venue may dedupe it against the old order, or your reconciliation can't tell which fill belongs to which intent. Now you can't answer \"did my <em>new</em> order go through?\" — and in trading, ambiguity about live exposure is dangerous. Generate a fresh unique id for every distinct order, always.</p><div class=\"key\"><p>NewOrderSingle (<code>35=D</code>) submits one order as a few decisive tags — id, symbol, side, qty, type, price, time-in-force — and the venue answers with an ExecutionReport. The non-negotiable rule: a globally unique <code>11=ClOrdID</code> per order, because it's both your dedup key and your reconciliation handle.</p></div>"},
  "rest-vs-websocket": {"teach":"<p>At its core, <strong>REST is like sending a letter and waiting for a reply, while WebSocket is like leaving the phone line open so news reaches you the instant it happens.</strong> Both connect you to an exchange, but they solve opposite problems, and robust trading systems use them <em>together</em>.</p><h4>REST: ask, then get an answer</h4><p>REST is synchronous request/response. You make one call — \"place this order,\" \"cancel order X,\" \"what's my balance?\", \"give me the settlement file\" — and you get exactly one definitive answer back. Its great virtues are <strong>determinism and easy retries</strong>: if a call times out, you can safely ask again (with an idempotency key). Its weakness is that to learn about <em>changes</em>, you must keep asking — <em>polling</em> — which is slow and wasteful for fast-moving data like fills.</p><h4>WebSocket: a live push stream</h4><p>WebSocket holds a single long-lived connection open, and the venue <em>pushes</em> events to you the moment they occur: order-book updates, public trades, and — crucially — your own order and fill updates with very low latency. <strong>That private fill stream is the crypto/REST-world analogue of a FIX drop-copy</strong> — it's how you hear about executions in real time without polling.</p><div class=\"analogy\"><p>REST is checking a package's tracking page by refreshing it yourself. WebSocket is signing up for live SMS alerts that ping you the instant the truck moves. You'd never refresh a page 50 times a second to catch a fill — but a push notification gets there in milliseconds.</p></div><h4>The reconnect trap (this is the one that bites)</h4><p>WebSocket feels like a reliable firehose, but it is <strong>lossy across disconnects</strong>. If your connection drops for even a second, events that occurred during the gap are simply gone — the stream does not replay them the way a FIX session does. So the dangerous mistake is trusting your in-memory state after a reconnect. You think you have all your open orders; in reality three filled while you were disconnected, and your view is now a lie.</p><h4>The robust pattern</h4><p>Combine their strengths. <strong>Submit via REST</strong> (you get a definitive ack and the venue's order id). <strong>Watch fills via WS</strong> (low latency). And <strong>always resync via a REST snapshot after any WS reconnect</strong>:</p><ul><li>Reconnect the WS and resubscribe to your channels.</li><li>Pull a REST snapshot: <code>GET /open_orders</code>.</li><li>Discard buffered WS events older than the snapshot.</li><li>Apply newer WS events on top of the snapshot.</li></ul><p>This gives you the snapshot's authoritative truth plus the stream's freshness, with no gap of lost events.</p><div class=\"key\"><p>Use REST for definitive actions and authoritative snapshots, WebSocket for low-latency push — and never trust WS in-memory state after a reconnect: pull a REST snapshot, then layer newer WS events on top. Push gives you speed; the snapshot gives you truth.</p></div>"},
  "client-order-id": {"teach":"<p>At its core, <strong>a client order id is a unique name you stamp on an order before you send it, so that if you have to retry, the venue recognizes \"oh, this is the same order I already have\" instead of creating a second one.</strong> It's a tiny string with an outsized job: it makes retrying safe.</p><h4>The problem it solves</h4><p>Networks fail in the worst possible way: <em>ambiguously</em>. You send \"buy 100 AAPL,\" and... silence. Did the order reach the venue and the <em>reply</em> got lost? Or did the order never arrive at all? If you naively retry, you risk buying 200. If you don't retry, you might have bought 0. Neither guess is safe.</p><p>The <strong>client order id</strong> dissolves this. You generate a unique id (<code>11=ClOrdID</code> in FIX; a <code>client_order_id</code> field or <code>Idempotency-Key</code> header in REST) and attach it to the order. Now the operation is <strong>idempotent</strong>: send it once or send it five times, the venue keys on that id, executes the order exactly once, and every retry returns the <em>same</em> original order. You can retry freely.</p><div class=\"analogy\"><p>It's like writing a confirmation number on a restaurant reservation. If your call drops and you phone back, you don't say \"book me a table\" again (now you have two tables) — you say \"I'm calling about reservation #A7F3.\" The host finds the existing one. Same intent, one reservation, no duplicate.</p></div><h4>It's also your join key</h4><p>Beyond safe retries, the ClOrdID is the thread that stitches your whole world together. The same id appears in your internal order record, in every ExecutionReport (<code>35=8</code>), in WebSocket fill events, in the drop-copy feed, and in end-of-day settlement files. When you reconcile, you join on the ClOrdID to prove \"this fill belongs to that intent.\" Without it, matching fills to orders becomes guesswork.</p><h4>The trap: generate it too late</h4><p>The single most important rule is <strong>timing</strong>: the id must exist <em>before</em> the network call and be persisted first. If you let the server assign the id, or generate it after sending, then a timeout leaves you with no handle — you can't query \"did order #A7F3 land?\" because you never had #A7F3. The correct flow is: generate the id, persist it locally, <em>then</em> make the call. Now any timeout is recoverable: query by id, and retry only if it isn't there.</p><h4>The deeper symmetry</h4><p>This is the <em>identical</em> primitive to a payments idempotency key. An order and a settlement instruction (a wire) are the same shape of problem — both must survive a retry without duplicating. The payments→trade-ops mapping is 1:1, which is why the same discipline protects both.</p><div class=\"key\"><p>A client order id is a caller-generated unique key that makes order submission idempotent and serves as the join key across fills, drop-copy, and settlement. Generate and persist it <em>before</em> the network call — that's what turns an ambiguous timeout into a safe query-then-retry.</p></div>"},
  "idempotency-key-store": {"teach":"<p>At its core, <strong>an idempotency key store is just a little memory table that says \"I've seen this exact request before — here's the answer I gave last time\" so the work happens once even if the request arrives five times.</strong> The client provides the key; the store remembers the outcome.</p><h4>Why a key alone isn't enough</h4><p>It's tempting to think \"I have a unique key, the venue dedupes, done.\" But the server side needs real machinery to honor that promise across retries, crashes, and concurrent duplicates. That machinery is the store.</p><h4>What the table holds</h4><p>You store one row keyed on <strong>(caller, idempotency_key)</strong> — scoping by caller so two different clients can't collide on the same key. Each row carries:</p><ul><li><code>request_hash</code> — a fingerprint of the request body, to detect \"same key, different payload.\"</li><li><code>response</code> — the saved result of the first execution.</li><li><code>status</code> — <code>in_progress</code> or <code>completed</code>, so concurrent retries don't both execute.</li></ul><h4>The three cases</h4><p>The logic is a small decision tree:</p><ul><li><strong>New key</strong> → mark <code>in_progress</code>, execute the work, save the response, mark <code>completed</code>.</li><li><strong>Same key + same payload</strong> → don't re-run; return the stored original response. This is the whole point: one execution, identical answer.</li><li><strong>Same key + <em>different</em> payload</strong> → return <strong><code>409 Conflict</code></strong>. The client is misusing the key, reusing it for a genuinely different request, and you must refuse rather than silently corrupt.</li></ul><div class=\"analogy\"><p>Picture a deli counter where you take a numbered ticket. If you walk back up holding ticket #42 asking about \"my pastrami on rye,\" they hand you the <em>same</em> sandwich you already ordered — they don't make a second one. But if you wave #42 and now ask for \"a turkey club,\" they raise an eyebrow: ticket #42 was for pastrami. That mismatch is the <code>409</code> — same ticket, different order, refused.</p></div><h4>The trap: comparing only the key</h4><p>The subtle bug is checking the key but skipping the <code>request_hash</code> comparison. If a buggy or malicious client reuses a key with new data, and you only look at the key, you'll either return a stale answer for a request you never ran, or worse, treat it as duplicate and skip a legitimate new action. Always hash the payload and compare; a key-with-changed-body must get a <code>409</code>, never a stale or duplicated execution.</p><p>(Note: this 409-on-mismatch behavior is the widely-adopted community/IETF idempotency pattern — the general convention, not a verbatim quote from any one provider's docs.)</p><h4>Why it matters</h4><p>This same store makes <em>both</em> order entry and settlement instructions safe to retry. A retried request can never create a second order or fire a duplicate wire — the store collapses every retry to a single execution. In money systems, that guarantee is the difference between \"safe to retry on timeout\" and \"terrified to ever resend.\"</p><div class=\"key\"><p>An idempotency key store remembers (caller, key) → (request_hash, saved response, status). Same key + same payload replays the stored answer; same key + different payload returns <code>409</code>. Hashing the payload — not just matching the key — is what keeps a single execution honest and lets you retry orders and wires fearlessly.</p></div>"}
});
