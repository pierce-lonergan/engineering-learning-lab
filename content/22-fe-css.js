/* CSS Layout & the Box Model (FE-CSS) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"FE-CSS","front":"What does <code>box-sizing: border-box</code> change, and why is it the standard reset?","back":"It changes which parts of the box <code>width</code>/<code>height</code> measure.<ul><li><strong>content-box</strong> (default): <code>width</code> sets only the <em>content</em> area; padding and border are <em>added</em> on top. A <code>width:200px; padding:20px; border:5px</code> box renders <strong>250px</strong> wide.</li><li><strong>border-box</strong>: <code>width</code> includes content + padding + border. The same box renders exactly <strong>200px</strong>; content shrinks to absorb padding/border.</li></ul>Margin is <em>never</em> included either way. The canonical reset is <code>*, *::before, *::after { box-sizing: border-box }</code> so widths are predictable and padding doesn't blow out layouts."},
  {"sec":"FE-CSS","front":"Explain the four boxes of the CSS box model and which one paints the <code>background</code>.","back":"From inside out: <strong>content</strong> → <strong>padding</strong> → <strong>border</strong> → <strong>margin</strong>.<ul><li><strong>Content box</strong>: text/children; sized by <code>width</code>/<code>height</code>.</li><li><strong>Padding</strong>: transparent space inside the border; <em>covered by the element's <code>background</code></em>.</li><li><strong>Border</strong>: drawn between padding and margin; <code>background</code> extends to its <em>outer</em> edge by default (<code>background-clip: border-box</code>).</li><li><strong>Margin</strong>: <em>always transparent</em>, never shows background, and <em>collapses</em> vertically between block-level boxes.</li></ul>"},
  {"sec":"FE-CSS","front":"When does margin collapsing happen, and name three ways to prevent it.","back":"<strong>Adjacent vertical (block-axis) margins</strong> of <em>block-level</em> boxes in normal flow combine into one margin. It occurs in three cases: <strong>adjacent siblings</strong>, <strong>parent and first/last child</strong> (the child's margin pokes through), and an <strong>empty block</strong>'s own top/bottom touching.<br>The collapsed value: if both positive → the <em>larger</em>; if mixed signs → the largest positive <em>plus</em> the most negative (sum of max and min); if both negative → the more negative.<br>Prevent it by separating the margins:<ul><li>Insert <code>padding</code> or a <code>border</code> between them.</li><li>Establish a <strong>new BFC</strong> on the parent (<code>display: flow-root</code>, or <code>overflow: hidden/auto</code>).</li><li>Use <strong>flex/grid</strong> — items in flex/grid containers <em>never</em> margin-collapse.</li></ul><em>Only block-axis margins collapse; inline (horizontal) margins never do.</em>"},
  {"sec":"FE-CSS","front":"Contrast <code>display</code>: <code>block</code> vs <code>inline</code> vs <code>inline-block</code>.","back":"<ul><li><strong>block</strong>: starts on a new line, fills the available inline width, honors <code>width</code>/<code>height</code> and <em>all</em> margins/padding (vertical included in flow).</li><li><strong>inline</strong>: flows within a line, <em>ignores</em> <code>width</code>/<code>height</code>; vertical padding/border render but <em>don't push surrounding lines</em>; vertical margins have no effect; only horizontal margins/padding affect layout.</li><li><strong>inline-block</strong>: flows inline like text (sits on the baseline, respects <code>text-align</code>/<code>white-space</code>) but is a <em>block internally</em> — honors <code>width</code>/<code>height</code> and the full box. Gotcha: whitespace in the source between inline-block elements renders as a visible gap.</li></ul>"},
  {"sec":"FE-CSS","front":"What does <code>display: contents</code> do, and what's its accessibility caveat?","back":"<code>display: contents</code> makes an element generate <strong>no box of its own</strong> — its children are hoisted as if they were direct children of the grandparent. Useful to let a wrapper <code>&lt;div&gt;</code> disappear so its children participate directly in a parent <strong>flex/grid</strong> layout.<br><em>Caveat:</em> historically it removed the element from the <strong>accessibility tree</strong>, stripping the semantics of elements like <code>&lt;ul&gt;</code>/<code>&lt;button&gt;</code>. Current browsers (Chromium, Firefox, WebKit) preserve semantics for most elements, but verify interactive/semantic containers with a screen reader — replaced elements and some edge cases still misbehave."},
  {"sec":"FE-CSS","front":"Compare the five <code>position</code> values and what each is positioned relative to.","back":"<ul><li><strong>static</strong>: default; in normal flow; <code>top/left/z-index</code> ignored.</li><li><strong>relative</strong>: offset from its <em>own normal-flow position</em>; original space is preserved; becomes a <strong>containing block</strong> for absolute descendants.</li><li><strong>absolute</strong>: removed from flow; positioned relative to the padding box of the nearest <em>positioned ancestor</em> (or the initial containing block).</li><li><strong>fixed</strong>: removed from flow; relative to the <strong>viewport</strong> — unless an ancestor establishes a containing block via <code>transform</code>/<code>filter</code>/<code>perspective</code>/<code>contain</code>, which traps it.</li><li><strong>sticky</strong>: hybrid — in flow until a scroll threshold (e.g. <code>top: 0</code>) is crossed, then pins within its <em>nearest scrolling ancestor</em>, never leaving its containing block.</li></ul>"},
  {"sec":"FE-CSS","front":"What forms the <em>containing block</em> for a <code>position: absolute</code> element?","back":"The nearest <strong>ancestor whose <code>position</code> is not <code>static</code></strong> (relative/absolute/fixed/sticky); its <strong>padding box</strong> is the reference rectangle. If no such ancestor exists, the <strong>initial containing block</strong> (≈ viewport) is used.<br>Crucially, certain properties <em>also</em> create a containing block for absolutely- and fixed-positioned descendants even without a <code>position</code> value: any non-<code>none</code> <code>transform</code>, <code>filter</code>, <code>perspective</code>, <code>contain: layout/paint/strict/content</code>, or <code>will-change</code> naming one of those. This is why a stray <code>transform</code> can silently re-anchor a <code>position: fixed</code> child to the transformed ancestor instead of the viewport."},
  {"sec":"FE-CSS","front":"What creates a new <strong>stacking context</strong>? Why does <code>z-index</code> sometimes 'not work'?","back":"A stacking context is a self-contained painting layer; <code>z-index</code> only competes <em>among elements within the same context</em>. Triggers include:<ul><li>Root <code>&lt;html&gt;</code>.</li><li><code>position: relative/absolute</code> with a <code>z-index</code> other than <code>auto</code>.</li><li><code>position: fixed</code> or <code>sticky</code> (always, even with <code>z-index: auto</code>).</li><li><code>opacity &lt; 1</code>, any <code>transform</code>/<code>filter</code>/<code>perspective</code>/<code>clip-path</code>/<code>mask</code>, <code>mix-blend-mode</code> ≠ normal, <code>will-change</code> on a context-forming property, <code>isolation: isolate</code>, <code>contain: layout/paint/strict/content</code>.</li><li>A flex/grid <em>child</em> with <code>z-index</code> ≠ auto.</li></ul><em>The trap:</em> a high <code>z-index</code> inside a low-z-index parent context can <strong>never</strong> rise above a sibling of that parent — the parent's context caps it. Fix by flattening the ancestor's context or restructuring, not by escalating <code>z-index</code>."},
  {"sec":"FE-CSS","front":"In Flexbox, define main axis vs cross axis and which property controls each alignment.","back":"The <strong>main axis</strong> runs along <code>flex-direction</code> (<code>row</code> → inline/horizontal, <code>column</code> → block/vertical); the <strong>cross axis</strong> is perpendicular.<ul><li><code>justify-content</code> → distributes items along the <strong>main</strong> axis (<code>space-between</code>, <code>center</code>, …).</li><li><code>align-items</code> → aligns items on the <strong>cross</strong> axis within each line (<code>stretch</code> default, <code>center</code>, <code>baseline</code>).</li><li><code>align-content</code> → distributes <em>multiple lines</em> on the cross axis (only matters when <code>flex-wrap: wrap</code> produces ≥2 lines).</li><li><code>align-self</code> → overrides <code>align-items</code> for a single item.</li></ul><em>Switching <code>flex-direction</code> swaps which physical direction each property controls.</em>"},
  {"sec":"FE-CSS","front":"Decode the <code>flex</code> shorthand: what do grow, shrink, and basis mean? What is <code>flex: 1</code>?","back":"<code>flex: &lt;grow&gt; &lt;shrink&gt; &lt;basis&gt;</code> controls how an item resolves along the main axis.<ul><li><strong>flex-grow</strong>: share of <em>positive free space</em> to absorb (proportional to the value). <code>0</code> = don't grow.</li><li><strong>flex-shrink</strong>: share of <em>overflow</em> to give up (weighted by basis × shrink factor). <code>0</code> = never shrink below basis.</li><li><strong>flex-basis</strong>: the item's hypothetical main size <em>before</em> grow/shrink (<code>auto</code> resolves to <code>width</code>/<code>height</code> or content).</li></ul><code>flex: 1</code> expands to <code>1 1 0%</code> → grow, shrink, <strong>basis 0</strong>, so items split space <em>equally regardless of content</em>. Contrast <code>flex: auto</code> = <code>1 1 auto</code> (content-weighted) and <code>flex: none</code> = <code>0 0 auto</code> (rigid)."},
  {"sec":"FE-CSS","front":"In CSS Grid, explain <code>fr</code>, <code>repeat()</code>, and <code>minmax()</code>.","back":"<ul><li><strong><code>fr</code></strong>: a fraction of the <em>free space</em> left after fixed/auto tracks and gaps are subtracted. <code>1fr 2fr</code> → 1:2 split of the leftover. Note <code>minmax(0, 1fr)</code> is often needed to let an <code>fr</code> track shrink past its content's min-content size.</li><li><strong><code>repeat(n, ...)</code></strong>: shorthand for repeated track lists, e.g. <code>repeat(3, 1fr)</code> = <code>1fr 1fr 1fr</code>. Accepts <code>auto-fit</code>/<code>auto-fill</code> for a dynamic count.</li><li><strong><code>minmax(min, max)</code></strong>: a track never smaller than <code>min</code> nor larger than <code>max</code>. The classic responsive pattern: <code>grid-template-columns: repeat(auto-fit, minmax(200px, 1fr))</code> — as many ≥200px columns as fit, each stretching to share the row.</li></ul>"},
  {"sec":"FE-CSS","front":"<code>auto-fit</code> vs <code>auto-fill</code> in <code>repeat()</code> — what's the visible difference?","back":"Both let Grid compute the column count automatically; they differ only when there are <em>fewer items than tracks that fit</em>:<ul><li><strong><code>auto-fill</code></strong>: creates as many tracks as fit, leaving <em>empty phantom tracks</em>. With <code>minmax(200px, 1fr)</code>, filled items stay near their min size and the row shows trailing gaps.</li><li><strong><code>auto-fit</code></strong>: creates the tracks, then <em>collapses the empty ones to 0</em> and lets existing items <strong>stretch</strong> to fill the row.</li></ul>So with few items, <code>auto-fit</code> → fewer, wider columns; <code>auto-fill</code> → narrower columns with blank space. When there are enough items to fill every track, the two are identical."},
  {"sec":"FE-CSS","front":"What is a <strong>Block Formatting Context (BFC)</strong> and what three problems does it solve?","back":"A BFC is an isolated <em>flow region</em> whose block-level contents lay out independently of the outside. Triggers: <code>display: flow-root</code> (the clean modern way), <code>overflow</code> other than <code>visible</code>/<code>clip</code>, floats, <code>position: absolute/fixed</code>, flex/grid items, <code>display: inline-block</code>, table cells.<br>It solves:<ul><li><strong>Float containment</strong>: a BFC parent <em>grows to wrap floated children</em> (replaces the old clearfix hack).</li><li><strong>Stops margin collapsing</strong> across its boundary.</li><li><strong>Prevents content wrapping around an external float</strong> — a BFC sibling sits beside the float instead of flowing under it, enabling the media-object layout.</li></ul>Prefer <code>display: flow-root</code>: no side effects, vs <code>overflow:hidden</code> which clips overflow/shadows."},
  {"sec":"FE-CSS","front":"How does <code>overflow</code> create a scroll container, and what's the modern fix for scrollbar layout shift?","back":"Setting <code>overflow: auto</code> or <code>scroll</code> (on either axis) makes the element a <strong>scroll container</strong>: overflowing content becomes scrollable and the element also forms a <strong>BFC</strong>. <code>overflow: hidden</code> clips without visible scrollbars (still programmatically scrollable); <code>clip</code> is a hard, non-scrollable clip that does <em>not</em> create a scroll container.<ul><li>The <strong>scroll ancestor</strong> a <code>sticky</code> element pins to is the nearest overflow scroll container — sticky silently fails if an ancestor has <code>overflow: hidden/auto/scroll/clip</code> but isn't the intended scroller.</li><li>A scrollbar appearing/disappearing reflows content; <strong><code>scrollbar-gutter: stable</code></strong> reserves the gutter space up front so layout doesn't jump.</li></ul>"},
  {"sec":"FE-CSS","front":"Compare the CSS length units: <code>px</code>, <code>em</code>, <code>rem</code>, <code>%</code>, <code>vh</code>/<code>vw</code>, <code>ch</code>.","back":"<ul><li><strong>px</strong>: absolute CSS pixel (device-independent, not necessarily one physical pixel); ignores user font scaling.</li><li><strong>em</strong>: relative to the <em>element's own <code>font-size</code></em> (for the <code>font-size</code> property itself, relative to the parent) — <em>compounds</em> when nested.</li><li><strong>rem</strong>: relative to the <em>root</em> (<code>&lt;html&gt;</code>) font-size — no compounding; the go-to for scalable typography/spacing that honors user zoom.</li><li><strong>%</strong>: relative to the <em>containing block</em>; which dimension depends on the property (e.g. <code>padding</code>/<code>margin</code> percentages always resolve against the container's <em>inline size</em>, even vertically).</li><li><strong>vh/vw</strong>: 1% of viewport height/width; <code>dvh/svh/lvh</code> handle mobile dynamic toolbars (dynamic/small/large viewport).</li><li><strong>ch</strong>: advance width of the <code>0</code> glyph — handy for capping line length (<code>max-width: 65ch</code>).</li></ul>"},
  {"sec":"FE-CSS","front":"Walk through the main ways to center a box both horizontally and vertically.","back":"<ul><li><strong>Flexbox (most common):</strong> <code>display:flex; justify-content:center; align-items:center</code> on the parent — centers any single child.</li><li><strong>Grid (terse):</strong> <code>display:grid; place-items:center</code>.</li><li><strong>Absolute + transform:</strong> <code>position:absolute; top:50%; left:50%; transform:translate(-50%,-50%)</code> — works without knowing the box size, escapes flow.</li><li><strong>Margin auto (horizontal block only):</strong> <code>width:X; margin-inline:auto</code> — no vertical centering.</li><li><strong>Inline content:</strong> <code>text-align:center</code> + matching <code>line-height</code> for single-line vertical centering.</li></ul>Modern default is flex/grid; <code>place-items:center</code> is the shortest robust answer."},
  {"sec":"FE-CSS","front":"What is the <em>implicit grid</em>, and how do <code>grid-auto-rows</code>/<code>grid-auto-flow</code> govern it?","back":"The <strong>explicit grid</strong> is what you declare with <code>grid-template-rows/columns</code>. When items overflow that definition (more children than cells, or an item placed beyond the declared lines), Grid generates <strong>implicit tracks</strong> to hold them.<ul><li><code>grid-auto-rows</code> / <code>grid-auto-columns</code> size those generated tracks (e.g. <code>grid-auto-rows: minmax(100px, auto)</code>).</li><li><code>grid-auto-flow</code> controls the auto-placement direction (<code>row</code> default, <code>column</code>) and whether <code>dense</code> backfills earlier gaps left by larger items (at the cost of visual reordering).</li></ul>Forgetting <code>grid-auto-rows</code> is why extra rows unexpectedly collapse to their content height."},
  {"sec":"FE-CSS","front":"Explain <code>grid-template-areas</code> and one key constraint.","back":"<code>grid-template-areas</code> names regions with an ASCII map; items attach via <code>grid-area: header</code>:<br><code>grid-template-areas: \"head head\" \"nav main\" \"foot foot\";</code><ul><li>Each string is a <strong>row</strong>; a repeated name <em>spans multiple cells</em>.</li><li><code>.</code> (one or more) denotes an <strong>empty cell</strong>.</li><li><em>Constraint:</em> every named area must form a <strong>solid rectangle</strong> — L-shapes or disconnected repeats of a name make the whole <code>grid-template-areas</code> declaration invalid (ignored).</li></ul>It's the most readable way to express responsive layouts: redefine the areas inside a media query to reflow without touching markup or item order."}
);

Object.assign(GLOSSARY_DATA, {
  "box-sizing": {"term":"box-sizing","group":"FE-CSS","eli5":"Decides whether the width you set includes the padding and border or not.","memory":"\"border-box = what you set is what you get\" — the number is the final outer size.","full":"<p><code>box-sizing</code> controls how <code>width</code>/<code>height</code> map onto the box model. The default <strong>content-box</strong> sizes only the content area, so padding and border are added <em>outside</em> the declared width. <strong>border-box</strong> makes the declared width the sum of content + padding + border, shrinking content to fit.</p><p>Nearly every codebase applies <code>*, *::before, *::after { box-sizing: border-box }</code> as a reset so layout math is intuitive and padding can't overflow fixed-width containers. Margin is excluded from both models.</p>","trap":"Thinking margin is part of border-box. It is not — only content, padding, and border. Total layout footprint still adds margin on top.","worked":"<code>.box{ box-sizing:border-box; width:200px; padding:20px; border:5px solid }</code> renders exactly 200px wide; content area is 150px. Under content-box it'd be 250px.","related":["box-model","margin-collapsing"]},
  "box-model": {"term":"box model","group":"FE-CSS","eli5":"Every element is a set of nested rectangles: content, padding, border, margin.","memory":"\"Content, Padding, Border, Margin\" inside-out → CPBM.","full":"<p>The CSS box model wraps every element in four concentric areas: <strong>content</strong> (text/children), <strong>padding</strong> (inside the border, covered by the background), <strong>border</strong>, and <strong>margin</strong> (always transparent spacing outside). It's the foundation of all sizing and spacing.</p><p>By default <code>background</code> paints out to the border's outer edge (<code>background-clip: border-box</code>), and only margins on the block axis can collapse.</p>","trap":"Assuming margin shows the background or contributes to a click/hit target — margin is transparent and outside the element's painted and event box.","worked":"For <code>width:100; padding:10; border:2; margin:8</code> (content-box), painted box = 124px wide, total horizontal footprint = 140px.","related":["box-sizing","margin-collapsing","bfc"]},
  "margin-collapsing": {"term":"margin collapsing","group":"FE-CSS","eli5":"Stacked vertical margins merge into one margin instead of adding up.","memory":"\"Vertical margins shake hands — the bigger one usually wins.\"","full":"<p>Adjacent <strong>vertical (block-axis)</strong> margins of block boxes in normal flow collapse into a single margin. Two positives collapse to the larger; mixed signs sum the largest positive with the most negative; two negatives collapse to the more negative. It happens between siblings, between a parent and its first/last child, and within empty blocks.</p><p>Establishing a BFC, inserting padding/border between the margins, or switching to flex/grid all prevent it. Inline (horizontal) margins never collapse.</p>","trap":"Expecting two 20px margins to produce 40px of gap — they collapse to 20px. Also forgetting a child's top margin can 'escape' through a parent that isn't a BFC.","worked":"A <code>&lt;p&gt;</code> with <code>margin:20px</code> above another with <code>margin:30px</code> yields a 30px gap, not 50px. Add <code>display:flow-root</code> to the parent to contain a child's escaping margin.","related":["box-model","bfc","position"]},
  "position": {"term":"position","group":"FE-CSS","eli5":"Sets whether and how an element is offset from its normal spot or a parent/viewport.","memory":"\"Relative stays & anchors; Absolute escapes; Fixed glues to viewport; Sticky is relative-then-pinned.\"","full":"<p><code>position</code> takes <strong>static</strong> (default, in flow), <strong>relative</strong> (offset from itself, space kept, becomes a containing block), <strong>absolute</strong> (out of flow, anchored to the padding box of the nearest positioned ancestor), <strong>fixed</strong> (anchored to the viewport), and <strong>sticky</strong> (in flow until a scroll threshold, then pinned within its scroll container).</p><p>Non-static positioning plus a numeric <code>z-index</code> also creates a stacking context, coupling layout and paint order.</p>","trap":"Believing <code>fixed</code> is always relative to the viewport — any ancestor with <code>transform</code>/<code>filter</code>/<code>perspective</code>/<code>contain</code> becomes its containing block and traps it.","worked":"To anchor a badge to a card: the card gets <code>position:relative</code>, the badge gets <code>position:absolute; top:-8px; right:-8px</code>.","related":["containing-block","stacking-context","margin-collapsing"]},
  "containing-block": {"term":"containing block","group":"FE-CSS","eli5":"The rectangle an element's percentages and offsets are measured against.","memory":"\"Absolute hunts upward for the nearest non-static ancestor's padding box.\"","full":"<p>The containing block is the reference rectangle for percentage sizes and positioned offsets. For static/relative elements it's the content box of the nearest block-container ancestor; for <strong>absolute</strong> elements it's the padding box of the nearest <em>positioned</em> ancestor; for <strong>fixed</strong> it's the viewport (initial containing block).</p><p>Critically, <code>transform</code>, <code>filter</code>, <code>perspective</code>, <code>contain</code>, and <code>will-change</code> can <em>also</em> establish a containing block, re-anchoring absolute/fixed descendants unexpectedly.</p>","trap":"A stray <code>transform: translateZ(0)</code> (GPU/compositing hack) on an ancestor silently re-anchors a <code>position:fixed</code> modal so it no longer tracks the viewport.","worked":"<code>.parent{position:relative}</code> makes <code>.child{position:absolute; inset:0}</code> fill exactly the parent rather than the page.","related":["position","stacking-context","bfc"]},
  "stacking-context": {"term":"stacking context","group":"FE-CSS","eli5":"A self-contained layer that decides paint order; z-index only competes inside one.","memory":"\"z-index is a fight you can only win against siblings in the same room.\"","full":"<p>A stacking context is an atomic paint layer. The root element, positioned elements with a numeric <code>z-index</code>, fixed/sticky elements, and properties like <code>opacity&lt;1</code>, <code>transform</code>/<code>filter</code>/<code>perspective</code>, <code>mix-blend-mode</code>, <code>isolation:isolate</code>, <code>contain</code>, and <code>will-change</code> each create one.</p><p>Within a context, descendants paint by z-index, but a descendant can never escape its parent's context. <code>isolation: isolate</code> deliberately creates a context with no other visual side effects.</p>","trap":"Cranking <code>z-index: 9999</code> to fix overlap. If the element's ancestor sits in a lower-z context, no value can lift it above that ancestor's siblings.","worked":"A dropdown hidden behind a sibling card: the card had <code>opacity:.99</code> (a new context). Removing it, or adding <code>isolation:isolate</code> to a shared parent, restores correct ordering.","related":["position","containing-block","bfc"]},
  "flex-shorthand": {"term":"flex (shorthand)","group":"FE-CSS","eli5":"One property that says how much an item grows, shrinks, and its starting size.","memory":"\"Grow, Shrink, Basis — GSB. flex:1 = 1 1 0 = equal slices.\"","full":"<p><code>flex: &lt;grow&gt; &lt;shrink&gt; &lt;basis&gt;</code> resolves an item's main-axis size. <strong>grow</strong> distributes positive free space, <strong>shrink</strong> distributes overflow (weighted by basis × factor), and <strong>basis</strong> is the pre-distribution size.</p><p>Key keywords: <code>flex:1</code> = <code>1 1 0%</code> (equal tracks regardless of content), <code>flex:auto</code> = <code>1 1 auto</code> (content-weighted), <code>flex:none</code> = <code>0 0 auto</code> (rigid).</p>","trap":"Expecting <code>flex:1</code> items with different content to come out unequal — basis 0 ignores content, so they're equal. Use <code>flex:auto</code> if you want content to influence width.","worked":"Sidebar + fluid main: <code>.sidebar{flex:0 0 240px} .main{flex:1}</code> — sidebar fixed at 240px, main fills the rest.","related":["flex-axes","grid-fr"]},
  "flex-axes": {"term":"flex main/cross axis","group":"FE-CSS","eli5":"Flex items line up along one axis and align across the other.","memory":"\"justify = along the flow (main); align = across it (cross).\"","full":"<p>The <strong>main axis</strong> follows <code>flex-direction</code>; the <strong>cross axis</strong> is perpendicular. <code>justify-content</code> distributes along main, <code>align-items</code>/<code>align-self</code> align along cross, and <code>align-content</code> distributes wrapped lines along cross.</p><p>Switching <code>flex-direction</code> to <code>column</code> swaps which physical direction each property affects — a frequent source of confusion.</p>","trap":"Trying to vertically center a row's items with <code>justify-content:center</code>. In a row, vertical is the cross axis — use <code>align-items:center</code>.","worked":"Centered toolbar: <code>display:flex; justify-content:space-between; align-items:center</code> spreads items horizontally and centers them vertically.","related":["flex-shorthand","grid-fr"]},
  "grid-fr": {"term":"fr unit & minmax","group":"FE-CSS","eli5":"fr splits leftover space; minmax bounds a track between a floor and ceiling.","memory":"\"fr = free-space fraction; minmax(floor, ceiling).\"","full":"<p>The <code>fr</code> unit represents a share of the space remaining after fixed/auto tracks and gaps. <code>minmax(min,max)</code> bounds a track, enabling responsive columns. Combined with <code>repeat(auto-fit, minmax(200px,1fr))</code> you get a fluid, wrap-aware responsive grid.</p><p><code>auto-fit</code> collapses empty tracks so items stretch; <code>auto-fill</code> keeps empty tracks, leaving gaps. Use <code>minmax(0,1fr)</code> when an <code>fr</code> track must shrink below its content's min size.</p>","trap":"Using <code>repeat(auto-fit, 200px)</code> and wondering why columns don't stretch — without <code>minmax(...,1fr)</code> there is no growth term.","worked":"<code>grid-template-columns: repeat(auto-fit, minmax(220px, 1fr))</code> yields as many ≥220px columns as fit, each sharing the row equally.","related":["flex-shorthand","bfc"]},
  "bfc": {"term":"block formatting context","group":"FE-CSS","eli5":"An isolated layout bubble that contains floats and blocks margins from leaking.","memory":"\"flow-root = a clean bubble that wraps floats and walls off margins.\"","full":"<p>A Block Formatting Context is a region whose block-level layout is independent of the outside. It contains floated children (the parent grows to wrap them), prevents margin collapse across its edge, and stops content from wrapping around an external float.</p><p>Triggered by <code>display:flow-root</code> (side-effect-free), non-<code>visible</code> <code>overflow</code>, floats, absolute/fixed positioning, flex/grid items, and inline-blocks.</p>","trap":"Reaching for <code>overflow:hidden</code> to contain floats — it works but clips overflowing content (shadows, tooltips). Prefer <code>display:flow-root</code>.","worked":"Media object: <code>.media{display:flow-root}</code> with a floated avatar keeps the text body as a clean column beside the float instead of wrapping under it.","related":["margin-collapsing","box-model","stacking-context"]}
});
