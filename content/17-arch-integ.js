/* Service Communication & Integration (ARCH-INTEG) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"ARCH-INTEG","front":"Synchronous vs asynchronous service communication: the core tradeoff","back":"<strong>Synchronous</strong> (REST, gRPC) is request-blocking: the caller waits for a reply, giving simple control flow and immediate consistency but introducing <em>temporal coupling</em> (both services must be up <em>now</em>) and risk of cascading failure. <strong>Asynchronous</strong> (events/messaging) decouples in time: the producer fires and forgets, gaining resilience and independent scaling at the cost of eventual consistency and harder end-to-end debugging."},
  {"sec":"ARCH-INTEG","front":"What is temporal coupling, and why is it the hidden cost of synchronous calls?","back":"Temporal coupling means caller and callee must both be available <em>at the same instant</em> for the interaction to succeed. A synchronous REST/gRPC chain inherits the availability of every hop multiplicatively — five 99.9% services in series yield ~99.5% — and a slow downstream ties up the caller's threads. Async messaging breaks this: the broker buffers the message so the consumer can be down and catch up later."},
  {"sec":"ARCH-INTEG","front":"How does a synchronous call chain produce cascading failure, and what patterns stop it?","back":"A slow or dead downstream causes callers to block on threads/connections; the pool exhausts, so the caller itself starts failing requests it could otherwise serve, propagating <em>upward</em>. Defenses: <code>timeout</code>s on every hop, <strong>circuit breakers</strong> (fail fast when error rate spikes), <strong>bulkheads</strong> (isolate thread pools per dependency), and load shedding. These are reliability patterns, not architecture changes — async would sidestep the chain entirely."},
  {"sec":"ARCH-INTEG","front":"REST vs gRPC: when does a staff engineer reach for each?","back":"<ul><li><strong>REST/JSON over HTTP</strong> (typically HTTP/1.1, but not bound to it — REST runs over HTTP/2 too): human-readable, HTTP-cache-friendly via ETag/Cache-Control, ubiquitous tooling — best for public/partner APIs and browser clients.</li><li><strong>gRPC</strong> (HTTP/2 + Protobuf): binary, schema-first, multiplexed streams, lower latency and payload size, native bidirectional streaming — best for internal service-to-service east-west traffic.</li></ul>gRPC's friction: browsers can't speak native gRPC (need a grpc-web proxy, and even then client/bidi streaming from the browser isn't possible), plus weaker human debuggability and no out-of-the-box HTTP caching."},
  {"sec":"ARCH-INTEG","front":"Choreography vs orchestration: who drives the workflow?","back":"<strong>Choreography</strong>: no central brain — each service reacts to events and emits its own, so the workflow is emergent. Maximally decoupled but the end-to-end flow exists nowhere explicitly, making it hard to trace or reason about. <strong>Orchestration</strong>: a coordinator (orchestrator/saga executor) explicitly invokes each step in order. The flow is visible and easy to change in one place, but the coordinator is a central dependency and potential bottleneck/coupling point."},
  {"sec":"ARCH-INTEG","front":"You're designing a payment-checkout saga across 5 services. Choreography or orchestration?","back":"Lean <strong>orchestration</strong>. Multi-step business transactions with compensations (refund, release inventory, void auth) need an explicit owner of the happy path AND the rollback logic — burying that across event handlers (choreography) makes failure recovery and auditing nightmarish. Choreography shines for fan-out notifications and loosely-related reactions where no one needs the global picture."},
  {"sec":"ARCH-INTEG","front":"What concrete problems make choreography 'hard to trace'?","back":"Because the workflow lives only in the chain of who-listens-to-what, you can't read it from a single file. Diagnosing a stuck order means correlating events across services (you need <strong>distributed tracing</strong> + correlation IDs as table stakes). Adding a step risks an unexpected listener firing; removing one can silently break a downstream nobody documented. The coupling moves from code to the event schema."},
  {"sec":"ARCH-INTEG","front":"What is an API gateway, and which cross-cutting concerns belong in it?","back":"A single front-door entry point between external clients and internal services. It centralizes <em>cross-cutting concerns</em>: TLS termination, authentication/authZ, rate limiting/throttling, request routing, response aggregation, caching, and API-key/quota enforcement. The win is one place for edge policy; the risk is it becoming a bloated chokepoint, a single point of failure (mitigate with HA replicas), or a deployment bottleneck if business logic leaks into it."},
  {"sec":"ARCH-INTEG","front":"Backend-for-Frontend (BFF): what problem does it solve that a single gateway doesn't?","back":"A single general-purpose gateway forces a lowest-common-denominator API across very different clients. A <strong>BFF</strong> is a dedicated backend per client experience (iOS, web, partner) — typically owned by the team that owns that frontend — that aggregates and reshapes downstream calls to exactly that client's needs: trimming payloads, batching round-trips, client-specific auth. It moves client-coupling out of shared services. Cost: more services to own, and logic duplication risk across BFFs."},
  {"sec":"ARCH-INTEG","front":"API gateway vs service mesh: how do they differ?","back":"<ul><li><strong>API gateway</strong>: <em>north-south</em> traffic (external clients → cluster edge), centralized, handles edge concerns like auth and rate limiting.</li><li><strong>Service mesh</strong>: <em>east-west</em> traffic (service ↔ service inside the cluster), decentralized data plane (per-pod sidecars, or a sidecarless/ambient model), handles mTLS, retries, traffic shifting, and observability.</li></ul>They're complementary, not competing — many systems run both."},
  {"sec":"ARCH-INTEG","front":"What does a service mesh sidecar actually do, and what's the catch?","back":"A proxy (e.g. Envoy) deployed alongside each service instance intercepts all its traffic, providing <strong>mTLS</strong> between services, retries/timeouts/circuit-breaking, canary/traffic-splitting, and uniform telemetry — all <em>without changing app code</em>. Catches: per-hop latency and CPU/memory overhead from the extra proxies, plus operational complexity (control plane, cert rotation). The overhead drove the newer <em>sidecarless/ambient</em> mesh model (e.g. Istio ambient: a per-node L4 proxy + optional L7 waypoints). Meshes are often overkill below a few dozen services."},
  {"sec":"ARCH-INTEG","front":"Request-reply vs publish-subscribe: what's the semantic difference?","back":"<strong>Request-reply</strong> is 1-to-1 and the sender expects a correlated response (even over a message queue, via a reply-to queue + correlation ID). <strong>Publish-subscribe</strong> is 1-to-many: the publisher emits to a topic with no knowledge of who (if anyone) consumes it, and gets no reply. Pub-sub is the foundation of event-driven decoupling; request-reply preserves a conversational, often synchronous-feeling contract."},
  {"sec":"ARCH-INTEG","front":"Can you do asynchronous request-reply? When would you?","back":"Yes — the requester sends a message with a <code>reply-to</code> address and <code>correlationId</code>, then either blocks on a temporary reply queue or registers a callback/future and continues. Used when you want a definite answer but also want broker-level buffering, retries, and load-leveling (e.g. a long-running compute job). It trades the simplicity of synchronous RPC for resilience while keeping a logical request/response contract."},
  {"sec":"ARCH-INTEG","front":"Why is 'just call the service synchronously' often the wrong default for inter-service writes?","back":"Synchronous cross-service writes create distributed transactions with no clean 2PC option, couple availability, and bury retry/idempotency concerns in every caller. The architectural default for resilient writes is asynchronous, idempotent, event-carried state transfer or a saga: each service owns its data and publishes facts. Reserve synchronous calls for reads needing fresh data and for genuinely interactive, low-fan-out paths."},
  {"sec":"ARCH-INTEG","front":"Where should an API gateway stop, and the service mesh start, in a payment platform?","back":"Gateway owns the <em>edge contract</em>: client auth (OAuth/API keys), per-tenant rate limits, request validation, and routing into the cluster. Once inside, the <strong>service mesh</strong> owns service-to-service trust (mTLS), retries, and traffic policy. Keep <em>business logic</em> (idempotency keys, tenant ownership checks, payment routing) in the services themselves — neither gateway nor mesh should encode domain rules."}
);

Object.assign(GLOSSARY_DATA, {
  "sync-vs-async-comm": {"term":"Synchronous vs Asynchronous Communication","group":"ARCH-INTEG","aliases":["synchronous communication","asynchronous communication","sync vs async","blocking vs non-blocking calls"],"eli5":"Sync is a phone call where you wait on the line for an answer; async is a text message you fire off and get on with your day.","memory":"Sync = Synchronized swimmers (everyone moves together, now); Async = Answering machine (leave it, processed later).","full":"<p><strong>Synchronous</strong> communication (REST, gRPC) blocks the caller until a reply arrives. It gives simple, linear control flow and immediate read-your-writes consistency, but introduces <em>temporal coupling</em> (both ends must be live simultaneously) and makes a chain only as available as the product of its hops' availabilities.</p><p><strong>Asynchronous</strong> communication routes through a broker/queue: the producer emits a message and returns immediately; the consumer processes whenever ready. This decouples services in time and load, enabling independent scaling and resilience to downstream outages — at the price of eventual consistency, out-of-order/duplicate handling, and harder end-to-end debugging because there's no single call stack to follow.</p>","trap":"Interviewers probe whether you reflexively say 'async is better.' It isn't universally — async buys resilience but costs you immediate consistency, idempotency handling, and traceability. The senior answer names the specific coupling (temporal) sync introduces and the specific tax (eventual consistency + debugging) async charges.","worked":"A checkout calling inventory + payment + shipping synchronously fails entirely if shipping is down. Emitting an OrderPlaced event lets shipping consume it minutes later when it recovers — the order still completes.","related":["temporal-coupling","cascading-failure","request-reply-vs-pubsub","choreography-vs-orchestration"]},
  "temporal-coupling": {"term":"Temporal Coupling","group":"ARCH-INTEG","aliases":["temporal coupling","time coupling","availability coupling"],"eli5":"Two services are temporally coupled when both have to be awake at the exact same moment for the call to work.","memory":"Temporal = Time. Coupled in time = 'you must both show up to the meeting at once.'","full":"<p>Temporal coupling is the requirement that interacting services be simultaneously available for an interaction to succeed — the defining property of synchronous communication. Its insidious effect is on availability math: a request traversing N synchronous hops has an availability equal to the <em>product</em> of each hop's, so reliability degrades as chains lengthen, and a single slow dependency can stall the whole path by holding caller threads.</p><p>Asynchronous messaging removes temporal coupling by inserting a durable broker that buffers messages: the consumer can be entirely down when the producer sends, and catch up on recovery. This is why event-driven architectures are reached for when independent availability and load-leveling matter. Note temporal coupling is distinct from — and orthogonal to — schema/contract coupling, which async does <em>not</em> remove.</p>","trap":"Candidates conflate decoupling-in-time with decoupling-in-contract. Async breaks temporal coupling but the event schema is still a shared contract — change it carelessly and you break consumers. Decoupling is multi-dimensional; name which dimension you mean.","worked":"Five services at 99.9% each, called synchronously in series: 0.999^5 ≈ 99.5% effective availability — roughly 5x the downtime of a single service, purely from temporal coupling.","related":["sync-vs-async-comm","cascading-failure","request-reply-vs-pubsub"]},
  "cascading-failure": {"term":"Cascading Failure","group":"ARCH-INTEG","aliases":["cascading failure","failure cascade","retry storm","thundering herd"],"eli5":"One slow service backs up everyone calling it, until the whole chain falls over like dominoes.","memory":"Dominoes: pull threads at the bottom, the whole stack topples up. Circuit breaker = the gap that stops the next domino.","full":"<p>In a synchronous topology, a slow or failing downstream causes upstream callers to block on connections and threads. Once a caller's pool exhausts, it can no longer serve <em>any</em> request — so the failure propagates upward and outward, often amplified by naive retries (a retry storm) that multiply load on the already-struggling dependency. The blast radius is the whole synchronous subgraph.</p><p>Mitigations are reliability patterns applied per dependency: aggressive <strong>timeouts</strong> (never wait unbounded), <strong>circuit breakers</strong> that trip open to fail fast and shed load, <strong>bulkheads</strong> that isolate thread/connection pools so one sick dependency can't starve others, retry budgets with exponential backoff + jitter, and load shedding. Asynchronous designs avoid the cascade structurally because the broker absorbs the backpressure instead of the caller's threads.</p>","trap":"The classic gotcha: adding retries to fix flakiness <em>worsens</em> a cascade by multiplying load on a dying service. Senior answer pairs retries with a circuit breaker, retry budget, and jitter — and notes that the breaker protects the <em>caller</em>, not the callee.","worked":"Service A holds a 200-thread pool; downstream B's latency jumps from 20ms to 5s. A's threads pin waiting on B, the pool drains, and A starts 503-ing requests that never even touch B — the failure has cascaded.","related":["temporal-coupling","sync-vs-async-comm","service-mesh"]},
  "choreography-vs-orchestration": {"term":"Choreography vs Orchestration","group":"ARCH-INTEG","aliases":["choreography","orchestration","saga orchestration","event choreography"],"eli5":"Orchestration has a conductor telling each player when to come in; choreography is dancers each reacting to the music with no one in charge.","memory":"ORCHestra has a conductor (central). CHOREOgraphy is dancers reacting (no conductor). Same root, opposite control.","full":"<p><strong>Orchestration</strong> uses a central coordinator (an orchestrator or saga executor) that explicitly invokes each step of a workflow and owns the sequencing, error handling, and compensations. The end-to-end process is visible in one place and easy to change or audit, but the coordinator is a central dependency, a potential bottleneck, and can accrete logic that belongs in the services (the 'God orchestrator' smell).</p><p><strong>Choreography</strong> has no central brain: each service subscribes to events and reacts by emitting its own, so the workflow is <em>emergent</em> from the wiring of publishers and subscribers. It maximizes decoupling and autonomy but the global flow exists nowhere explicitly — making it hard to trace, hard to onboard onto, and easy to break by adding/removing a listener. Real systems often mix both: choreograph between bounded contexts, orchestrate complex multi-step transactions within one.</p>","trap":"Don't claim choreography is 'more loosely coupled, therefore better.' For multi-step transactions with compensations, choreography scatters the rollback logic and the happy path across handlers — orchestration's explicit, centralized flow is usually the maintainable choice. Match the pattern to whether anyone needs the global picture.","worked":"Order saga: orchestration = an OrderSaga service calls Payment, then Inventory, then Shipping, issuing compensations on failure. Choreography = OrderPlaced → Payment listens & emits PaymentTaken → Inventory listens & emits Reserved → Shipping listens. No single owner of the flow.","related":["sync-vs-async-comm","request-reply-vs-pubsub","api-gateway"]},
  "api-gateway": {"term":"API Gateway","group":"ARCH-INTEG","aliases":["api gateway","gateway","edge gateway","north-south traffic"],"eli5":"One guarded front door for all outside traffic, where you do the security and routing once instead of in every service.","memory":"Gateway = the building's front desk: checks your badge (auth), limits visitors (rate limit), directs you (routing) — but doesn't do your actual job.","full":"<p>An API gateway is a single entry point sitting between external clients and internal services, centralizing <em>cross-cutting edge concerns</em>: TLS termination, authentication and authorization, rate limiting and quota enforcement, request routing, response aggregation/composition, caching, and protocol translation. It handles <strong>north-south</strong> traffic (in/out of the cluster) and lets individual services stay focused on domain logic rather than re-implementing edge policy.</p><p>The risks are architectural: the gateway can become a single point of failure (mitigated by HA replicas), a deployment bottleneck if every team's routing change funnels through it, and a dumping ground for business logic it shouldn't own. The discipline is to keep it to <em>generic</em> edge policy; client-specific shaping belongs in a Backend-for-Frontend, and domain rules belong in the services.</p>","trap":"Interviewers test the boundary: an API gateway does generic edge concerns; it is NOT where you put per-client payload shaping (that's a BFF) or business rules (that's the service). Leaking domain logic into the gateway recreates the monolith at the edge.","worked":"In NexusPay, the gateway enforces per-tenant rate limits and validates the OAuth token, then routes to the payment service — but tenant-ownership checks and idempotency keys live in the service, not the gateway.","related":["backend-for-frontend","service-mesh","sync-vs-async-comm"]},
  "backend-for-frontend": {"term":"Backend-for-Frontend (BFF)","group":"ARCH-INTEG","aliases":["backend for frontend","bff","backends for frontends"],"eli5":"A custom backend per app type (web, iOS, partner) that hands each one exactly the data it needs, no more, no less.","memory":"BFF = 'Best Friend Forever' for ONE client — it knows exactly what that client likes.","full":"<p>A Backend-for-Frontend is a dedicated server-side layer per client experience — one for web, one for mobile, one for a partner API — that aggregates, reshapes, and trims downstream service calls to fit that specific client. It exists because a single general-purpose API forces a lowest-common-denominator contract: mobile wants tiny payloads and one round-trip, web wants richer data, partners want a stable versioned shape. The BFF absorbs that client-specific coupling so shared downstream services stay client-agnostic.</p><p>Coined by Sam Newman (~2015, from SoundCloud's architecture), a BFF is typically owned by the frontend team that consumes it, which keeps client churn out of core services. The cost is proliferation (more deployables) and the risk of duplicated logic across BFFs — mitigated by pushing genuinely shared concerns down into the services or a shared library, not copy-pasting across BFFs.</p>","trap":"A BFF is per-client-experience, not per-microservice. The anti-pattern is one BFF per downstream service (that's just a gateway with extra steps) or stuffing business logic into the BFF — it should aggregate and reshape, not own domain rules.","worked":"The iOS app needs {name, balance, last3txns} in one call; the web dashboard needs full transaction history + charts. Two BFFs each call the same account + ledger services and compose client-tailored responses, so neither service knows about iOS vs web.","related":["api-gateway","service-mesh"]},
  "service-mesh": {"term":"Service Mesh","group":"ARCH-INTEG","aliases":["service mesh","sidecar proxy","istio","linkerd","envoy","east-west traffic","mtls","ambient mesh"],"eli5":"An infrastructure layer that handles secure traffic, retries, and metrics between services so the app code doesn't have to.","memory":"Mesh = a 'sidecar' on every service's motorcycle — it carries the security, retries, and telemetry so the driver (app) just drives. (Modern ambient meshes move that load off each bike onto a shared truck per node.)","full":"<p>A service mesh manages <strong>east-west</strong> (service-to-service) traffic via a data plane of proxies plus a control plane that configures them. The classic model injects a <em>sidecar proxy</em> (commonly Envoy) next to every service instance; the newer <em>sidecarless/ambient</em> model (e.g. Istio ambient mode, GA since 2024) uses a per-node L4 proxy plus optional L7 'waypoint' proxies to cut overhead. Either way the mesh transparently provides <strong>mTLS</strong>, retries/timeouts/circuit-breaking, traffic shifting (canary, weighted routing), and uniform observability — <em>without modifying application code</em>.</p><p>Versus an API gateway, the mesh is decentralized (internal, not a single edge), handles service-to-service trust rather than external clients, and complements rather than replaces the gateway. Its costs are real: added per-hop latency and resource overhead (the main driver of the sidecarless shift), plus operational complexity (control plane, cert rotation, an extra network layer to debug). It's usually unjustified below a few dozen services, where libraries or a gateway suffice.</p>","trap":"Don't conflate it with an API gateway. Gateway = north-south, centralized, external clients; mesh = east-west, decentralized, internal mTLS/traffic. Also don't assume 'service mesh = sidecar' — ambient/sidecarless meshes are now mainstream. And a mesh is operational plumbing, not an excuse to skip app-level idempotency or domain auth.","worked":"Istio injects an Envoy sidecar (or, in ambient mode, routes via ztunnel) so all service-to-service calls are mutually authenticated and encrypted under STRICT mTLS, and a VirtualService shifts 5% of traffic to v2 — none of which touches application code.","related":["api-gateway","cascading-failure","backend-for-frontend"]},
  "request-reply-vs-pubsub": {"term":"Request-Reply vs Publish-Subscribe","group":"ARCH-INTEG","aliases":["request-reply","request reply","publish-subscribe","pub-sub","pubsub","messaging patterns"],"eli5":"Request-reply is asking one specific person a question and waiting for their answer; pub-sub is shouting an announcement to a room and not caring who acts on it.","memory":"Request-reply = a sealed letter to one address with a return address. Pub-sub = a radio broadcast — anyone tuned in hears it, no reply expected.","full":"<p><strong>Request-reply</strong> is a 1-to-1 conversational pattern where the sender expects a correlated response. Over messaging it's implemented with a <code>reply-to</code> destination and a <code>correlationId</code> so the requester can match the answer to its question; it can be synchronous (block on the reply) or asynchronous (callback/future while the broker buffers). It preserves a tight contract and a definite answer.</p><p><strong>Publish-subscribe</strong> is 1-to-many: a publisher emits to a topic with zero knowledge of subscribers and receives no reply. Adding a consumer requires no change to the publisher, which is the engine of event-driven decoupling — but it sacrifices the ability to know if anyone acted and any natural backpressure/response path. Choosing between them is choosing whether the sender needs an answer (request-reply) or is broadcasting a fact (pub-sub).</p>","trap":"Pub-sub does not inherently mean async, and request-reply does not inherently mean sync — they're orthogonal axes (interaction shape vs blocking behavior). Async request-reply (reply-to + correlationId) is common and worth naming; that distinction separates senior from junior answers.","worked":"Async request-reply: a billing service drops a 'compute-invoice' message with replyTo=temp-queue-abc and correlationId=xyz, registers a future, and continues; the worker computes and posts the result to temp-queue-abc with correlationId=xyz, resolving the future.","related":["sync-vs-async-comm","choreography-vs-orchestration","temporal-coupling"]}
});
