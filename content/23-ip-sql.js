/* SQL for Trade & Financial Data (IP-SQL) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"IP-SQL","front":"Sketch a minimal schema for trades / fills / positions in a multi-asset trade-ops system. What are the load-bearing columns?","back":"<p>Three tables, all keyed on <strong>stable business identifiers</strong> so reconciliation can match rows:</p><ul><li><code>orders</code>: <code>order_id PK</code>, <code>client_order_id</code> (your idempotency key), <code>symbol</code>, <code>side</code>, <code>qty</code>, <code>venue</code>, <code>asset_class</code> (equity/crypto/prediction), <code>created_ts</code>.</li><li><code>fills</code> (executions): <code>fill_id PK</code>, <code>order_id FK</code>, <code>venue_exec_id</code> (the exchange's id, e.g. FIX <code>ExecID(17)</code>), <code>symbol</code>, <code>side</code>, <code>qty</code>, <code>price</code>, <code>fee</code>, <code>currency</code>, <code>exec_ts</code>, <code>settle_date</code>.</li><li><code>positions</code>: <code>symbol</code>, <code>account</code>, <code>net_qty</code>, <code>avg_px</code>, <code>as_of_ts</code> — usually a <em>derived</em> view/materialization of fills, not source-of-truth.</li></ul><p>Key choices: <strong>store fee/currency/settle_date</strong> (recon tolerances live here), keep a <code>venue_exec_id</code> for matching against the external truth source, and treat <code>positions</code> as recomputable from <code>fills</code> via <code>SUM() OVER</code>. For prediction markets the &quot;symbol&quot; is an event/outcome contract that resolves to $1 or $0.</p>"},
  {"sec":"IP-SQL","front":"How would you reconcile internal fills against an exchange's external settlement file? Write the core query.","back":"<p>Recon = compare <strong>internal records vs an external truth source</strong>, match on stable identifiers, tolerate expected discrepancies, surface the rest as <strong>breaks</strong>. The classic anti-join finds rows present internally but missing externally:</p><pre><code>SELECT i.venue_exec_id, i.symbol, i.qty, i.price\nFROM internal_fills i\nLEFT JOIN exchange_fills x\n  ON i.venue_exec_id = x.venue_exec_id\nWHERE x.venue_exec_id IS NULL;</code></pre><ul><li>Run it <strong>both directions</strong> (internal-not-in-exchange AND exchange-not-in-internal) to catch missing data on either side.</li><li>For rows that DO match, compare <code>qty</code>/<code>price</code>/<code>fee</code> within a <strong>tolerance</strong> (rounding, timing, fees) before flagging.</li><li>Under <strong>T+1</strong> (US equities since May 28, 2024) the intraday window is ~2-5 hrs, so this must run fast and unresolved breaks become settlement <strong>fails</strong>.</li></ul>"},
  {"sec":"IP-SQL","front":"Anti-join four ways: LEFT JOIN ... IS NULL vs NOT IN vs NOT EXISTS vs EXCEPT — which for recon, and the gotcha?","back":"<ul><li><code>LEFT JOIN b ON ... WHERE b.id IS NULL</code> — workhorse; returns the unmatched <em>left</em> rows with their columns. Best for breaks reports.</li><li><code>NOT EXISTS (SELECT 1 FROM b WHERE b.id = a.id)</code> — semantically cleanest; <strong>NULL-safe</strong> and usually optimizes identically.</li><li><code>NOT IN (SELECT id FROM b)</code> — <strong>DANGER</strong>: if the subquery returns even one <code>NULL</code>, the predicate evaluates to <code>UNKNOWN</code> for every row and you get <em>zero rows</em>. Avoid on nullable columns.</li><li><code>SELECT ... EXCEPT SELECT ...</code> — set difference across <em>all</em> selected columns, de-dups; great for whole-row diffs but you lose which side / extra columns unless you structure it.</li></ul><p><strong>Trap:</strong> <code>NOT IN</code> + a NULL in the external feed silently hides every break — the worst possible recon failure. Default to <code>NOT EXISTS</code> or <code>LEFT JOIN ... IS NULL</code>.</p>"},
  {"sec":"IP-SQL","front":"Compute a running net position per symbol with a window function. Show the SQL.","back":"<p>Running position = cumulative signed quantity ordered by time, partitioned per symbol:</p><pre><code>SELECT\n  symbol,\n  exec_ts,\n  signed_qty,\n  SUM(signed_qty) OVER (\n    PARTITION BY symbol\n    ORDER BY exec_ts\n    ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n  ) AS running_position\nFROM (\n  SELECT symbol, exec_ts,\n         CASE WHEN side='BUY' THEN qty ELSE -qty END AS signed_qty\n  FROM fills\n) f;</code></pre><ul><li><code>PARTITION BY symbol</code> restarts the running sum per instrument; <code>ORDER BY exec_ts</code> defines accumulation order.</li><li>The explicit <strong>frame</strong> (<code>ROWS UNBOUNDED PRECEDING</code>) makes the running total deterministic even with ties (the default <code>RANGE</code> frame would lump tied timestamps together).</li><li>The final row per symbol is the current net position — the basis for position-keeping and P&L.</li></ul>"},
  {"sec":"IP-SQL","front":"You receive duplicate / amended fills on a drop-copy feed. How do you keep only the latest version per execution in SQL?","back":"<p><code>ROW_NUMBER()</code> over a partition, keep rank 1:</p><pre><code>WITH ranked AS (\n  SELECT *,\n    ROW_NUMBER() OVER (\n      PARTITION BY venue_exec_id\n      ORDER BY version DESC, received_ts DESC\n    ) AS rn\n  FROM raw_fills\n)\nSELECT * FROM ranked WHERE rn = 1;</code></pre><ul><li>Partition by the <strong>natural key</strong> (<code>venue_exec_id</code>); order so the winning row sorts first.</li><li>Real-world driver: a <strong>FIX drop-copy</strong> feed pushes <code>35=8</code> ExecutionReports including <strong>Trade Correction</strong> (<code>150=G</code>) and <strong>Trade Cancel</strong> (<code>150=H</code>) — you must collapse to the final state.</li><li><code>DISTINCT</code> won't help: amended rows differ in price/qty, so you need <em>latest-wins</em>, not de-dup-identical.</li></ul>"},
  {"sec":"IP-SQL","front":"When do you reach for LAG()/LEAD() in trade data? Give a concrete example.","back":"<p><code>LAG</code>/<code>LEAD</code> access the previous/next row in an ordered partition — ideal for <strong>deltas, gaps, and sequencing checks</strong>:</p><pre><code>SELECT session, seq_num,\n  seq_num - LAG(seq_num) OVER (\n    PARTITION BY session ORDER BY seq_num) AS gap\nFROM fix_messages;</code></pre><ul><li>Analytically detect a <strong>FIX sequence-number gap</strong> in captured messages — any <code>gap &gt; 1</code> means a hole. (Live recovery is the FIX engine's job: a gap triggers a <code>ResendRequest(35=2)</code>; this query audits/monitors after the fact.)</li><li>Position deltas: <code>net_qty - LAG(net_qty)</code> to see how a position moved between snapshots.</li><li>Price moves / time-between-trades for surveillance.</li></ul><p><strong>Trap:</strong> <code>LAG</code> on the first row returns <code>NULL</code> — supply a default (<code>LAG(x, 1, 0)</code>) or your arithmetic propagates NULL.</p>"},
  {"sec":"IP-SQL","front":"Net position and realized cash P&L by symbol with GROUP BY — and the NULL/fee subtleties.","back":"<pre><code>SELECT\n  symbol,\n  SUM(CASE WHEN side='BUY' THEN qty ELSE -qty END) AS net_qty,\n  SUM(CASE WHEN side='SELL' THEN qty*price ELSE 0 END)\n    - SUM(CASE WHEN side='BUY'  THEN qty*price ELSE 0 END)\n    - SUM(COALESCE(fee,0)) AS net_cash_flow\nFROM fills\nGROUP BY symbol;</code></pre><ul><li>This is cash flow / net-of-fees, not mark-to-market realized P&L on closed lots (that needs cost-basis matching, e.g. FIFO) — be precise in the interview.</li><li><code>SUM</code> and other aggregates <strong>ignore NULLs</strong>, so a NULL <code>fee</code> just drops; wrap in <code>COALESCE(fee,0)</code> so missing fees don't silently overstate the result.</li><li><code>COUNT(*)</code> counts rows incl. NULLs; <code>COUNT(col)</code> skips NULLs — a common gotcha.</li><li><strong>Prediction markets:</strong> a settled contract resolves to <code>$1</code> (event occurred) or <code>$0</code> — model the resolution price explicitly rather than using last trade price.</li></ul>"},
  {"sec":"IP-SQL","front":"Walk me through a CTE-based break report that classifies each break (missing / qty mismatch / price mismatch).","back":"<p>Compute the classification in a CTE, then filter in the outer query — you <strong>cannot reference a SELECT alias in the same WHERE</strong> (alias scoping):</p><pre><code>WITH matched AS (\n  SELECT i.venue_exec_id,\n         i.qty AS i_qty, x.qty AS x_qty,\n         i.price AS i_px, x.price AS x_px\n  FROM internal_fills i\n  FULL OUTER JOIN exchange_fills x\n    ON i.venue_exec_id = x.venue_exec_id\n),\nclassified AS (\n  SELECT venue_exec_id,\n    CASE\n      WHEN i_qty IS NULL THEN 'MISSING_INTERNAL'\n      WHEN x_qty IS NULL THEN 'MISSING_EXTERNAL'\n      WHEN i_qty <> x_qty THEN 'QTY_BREAK'\n      WHEN ABS(i_px - x_px) > 0.005 THEN 'PRICE_BREAK'\n      ELSE 'OK'\n    END AS break_type\n  FROM matched\n)\nSELECT * FROM classified WHERE break_type <> 'OK';</code></pre><ul><li><strong>CTEs</strong> name each step (readability + reuse) and let you filter on the derived <code>break_type</code>.</li><li><code>FULL OUTER JOIN</code> catches breaks on <em>both</em> sides in one pass.</li><li>The price tolerance (<code>0.005</code>) encodes the <strong>tolerate-expected-discrepancy</strong> rule (rounding).</li></ul>"},
  {"sec":"IP-SQL","front":"What's a recursive CTE and where would it show up in trade ops?","back":"<p>A <strong>recursive CTE</strong> has an <em>anchor</em> query UNION ALL a <em>recursive</em> query that references the CTE, iterating until no new rows are produced:</p><pre><code>WITH RECURSIVE chain AS (\n  SELECT trade_id, parent_id, 1 AS lvl\n  FROM amendments WHERE parent_id IS NULL\n  UNION ALL\n  SELECT a.trade_id, a.parent_id, c.lvl+1\n  FROM amendments a JOIN chain c ON a.parent_id = c.trade_id\n)\nSELECT * FROM chain;</code></pre><ul><li>Trace an <strong>amendment/correction chain</strong> (original -> correction -> cancel) back to root.</li><li>Walk allocation hierarchies, or build a <strong>date spine</strong> (generate business days for a settlement calendar) to find missing-day gaps.</li><li>Unwind dependency graphs in clearing/netting.</li></ul><p><strong>Trap:</strong> a non-converging join -> runaway recursion; most engines cap it (Postgres needs no hint but <code>UNION ALL</code> won't dedup, while SQL Server defaults to <code>MAXRECURSION 100</code>). Design the join to terminate.</p>"},
  {"sec":"IP-SQL","front":"Explain transaction isolation levels and why SELECT FOR UPDATE matters in a position-keeping ledger.","back":"<p>Isolation governs what concurrent transactions can see. SQL-standard levels, weakest->strongest: <strong>READ UNCOMMITTED -> READ COMMITTED -> REPEATABLE READ -> SERIALIZABLE</strong>, each removing an anomaly:</p><ul><li><em>Dirty read</em> (uncommitted data), <em>non-repeatable read</em> (same row changes), <em>phantom</em> (new rows match a re-run predicate).</li><li><code>SELECT ... FOR UPDATE</code> takes a <strong>row lock</strong> so a concurrent worker can't read-modify-write the same position and lose an update.</li></ul><pre><code>BEGIN;\nSELECT net_qty FROM positions\n  WHERE symbol='AAPL' FOR UPDATE;   -- lock the row\nUPDATE positions SET net_qty = net_qty + 100\n  WHERE symbol='AAPL';\nCOMMIT;</code></pre><p>Without the lock, two fills applied concurrently can produce a <strong>lost update</strong> — a real correctness bug in position/ledger automation. The same primitive guards settlement-instruction state machines. (Note: Postgres READ COMMITTED never shows dirty reads — its weakest real level — so the anomaly table is the standard, not every engine's actual behavior.)</p>"},
  {"sec":"IP-SQL","front":"How does an idempotency key work, and why is it the same primitive in payments and order entry?","back":"<p>To make a retried request safe, store and key on a tuple. The established community/IETF pattern (popularized via Stripe):</p><ul><li>Key: <code>(caller, idempotency_key)</code> -> store <code>request_hash</code> + <code>response</code> + <code>status</code>.</li><li><strong>Same key + same payload</strong> -> return the <em>original</em> stored response (even if it was an error). No double-submit.</li><li><strong>Same key + different payload</strong> -> <code>409 Conflict</code> (the key was reused for a different request).</li></ul><pre><code>INSERT INTO idempotency (caller, idem_key, request_hash, status)\nVALUES (:caller, :key, :hash, 'IN_PROGRESS')\nON CONFLICT (caller, idem_key) DO NOTHING;</code></pre><p>Directly reusable for safe retry of <strong>exchange order-entry</strong> (FIX/REST <em>client order IDs</em>, e.g. FIX <code>ClOrdID(11)</code>) and <strong>settlement instructions</strong> — the same idempotency/recon/exception primitives a billing/payments/ledger engineer already owns.</p>"},
  {"sec":"IP-SQL","front":"Your recon anti-join is slow over 50M fills. How do you diagnose and fix it with indexing?","back":"<ul><li>Run <code>EXPLAIN</code> / <code>EXPLAIN ANALYZE</code> — look for a <strong>Seq Scan</strong> on the big table or a hash join spilling to disk.</li><li>Index the <strong>join key</strong> on both sides: <code>CREATE INDEX ON exchange_fills (venue_exec_id);</code> turns the anti-join into an index lookup.</li><li>Use a <strong>covering index</strong> so the query is served from the index alone (no heap fetch): <code>CREATE INDEX ix ON fills (symbol, exec_ts) INCLUDE (qty, price);</code> (<code>INCLUDE</code> is Postgres 11+/SQL Server; MySQL covers via the composite key itself).</li><li>Match <strong>column order</strong> to query predicates (leftmost-prefix rule); a composite <code>(account, settle_date)</code> index serves filters on <code>account</code> or <code>account+settle_date</code>, not <code>settle_date</code> alone.</li></ul><p><strong>Trap:</strong> wrapping the indexed column in a function (<code>DATE(exec_ts)='...'</code>) defeats the index — filter on a <em>range</em> or add an expression index.</p>"},
  {"sec":"IP-SQL","front":"NULL semantics in SQL — the three-valued-logic traps that bite reconciliation queries.","back":"<ul><li>NULL means <em>unknown</em>, not zero/empty. <code>NULL = NULL</code> is <strong>UNKNOWN</strong>, not true — use <code>IS NULL</code> / <code>IS NOT DISTINCT FROM</code>.</li><li>Any comparison with NULL -> <strong>UNKNOWN</strong>, and <code>WHERE</code> keeps only <strong>TRUE</strong> rows, so NULLs silently vanish from filters.</li><li><code>NOT IN (subquery-with-NULL)</code> -> zero rows (see anti-join card). <code>aggregate(col)</code> ignores NULLs; <code>COUNT(*)</code> doesn't.</li><li>Joining on a column that's NULL on both sides will <strong>not</strong> match — so a missing <code>venue_exec_id</code> never reconciles; treat it as a break, not a match.</li></ul><p>Defensive habit: <code>COALESCE</code> tolerances (<code>COALESCE(fee,0)</code>), explicit <code>IS NULL</code> branches in break classification, and <code>IS NOT DISTINCT FROM</code> for NULL-safe equality.</p>"},
  {"sec":"IP-SQL","front":"ROW_NUMBER vs RANK vs DENSE_RANK — when does the difference change your answer?","back":"<ul><li><code>ROW_NUMBER()</code> — always unique 1,2,3,4 even on ties; use for <strong>dedup / latest-wins</strong> (pick <code>rn=1</code>).</li><li><code>RANK()</code> — ties share a rank, then <strong>skips</strong> (1,1,3); use for <em>top-N with ties</em> leaderboard semantics.</li><li><code>DENSE_RANK()</code> — ties share, <strong>no gaps</strong> (1,1,2).</li></ul><p>Matters when ordering isn't unique: if two amended fills share <code>received_ts</code>, <code>ROW_NUMBER</code> picks exactly one (deterministic only if you add a tiebreaker like <code>version DESC</code>), whereas <code>RANK</code> returns both as rank 1 and your <code>=1</code> filter keeps duplicates.</p><p><strong>Trap:</strong> using <code>RANK()=1</code> for dedup — ties leak through and you re-introduce duplicate fills into the position calc.</p>"},
  {"sec":"IP-SQL","front":"Compute net position deltas between two daily position snapshots (SOD vs internal). What SQL pattern?","back":"<p>Full-outer-join the two snapshots on symbol/account and difference the quantities — this is the <strong>Start-of-Day (SOD) clearing/custodian file vs internal position</strong> recon (e.g. platform positions vs an Apex-style clearing SOD file):</p><pre><code>SELECT\n  COALESCE(s.symbol, i.symbol) AS symbol,\n  COALESCE(s.qty,0) AS sod_qty,\n  COALESCE(i.qty,0) AS internal_qty,\n  COALESCE(i.qty,0) - COALESCE(s.qty,0) AS delta\nFROM sod_positions s\nFULL OUTER JOIN internal_positions i\n  ON s.symbol = i.symbol AND s.account = i.account\nWHERE COALESCE(s.qty,0) <> COALESCE(i.qty,0);</code></pre><ul><li><code>FULL OUTER JOIN</code> + <code>COALESCE</code> so a symbol present on only one side still shows (delta = the whole position).</li><li>Any non-zero <code>delta</code> is a <strong>position break</strong> to investigate before settlement.</li><li>The <code>COALESCE(...,0)</code> in the <code>WHERE</code> is essential — a raw <code>s.qty &lt;&gt; i.qty</code> drops rows where one side is NULL (three-valued logic).</li></ul>"},
  {"sec":"IP-SQL","front":"How would you express 'orders with no fills' and 'fills with no parent order' — and what does each mean operationally?","back":"<pre><code>-- Orders never executed (working / cancelled / lost)\nSELECT o.order_id\nFROM orders o\nLEFT JOIN fills f ON f.order_id = o.order_id\nWHERE f.order_id IS NULL;\n\n-- Orphan fills: execution with no matching internal order\nSELECT f.fill_id, f.venue_exec_id\nFROM fills f\nWHERE NOT EXISTS (\n  SELECT 1 FROM orders o WHERE o.order_id = f.order_id);</code></pre><ul><li><strong>Orders with no fills</strong> -> unexecuted/working orders, or a missing fill feed — benign or a data gap.</li><li><strong>Orphan fills</strong> -> a serious break: you were filled on an execution your system has no order for (dropped order, mis-mapped <code>client_order_id</code>, or a feed for the wrong account). High-priority exception.</li></ul><p>These two <strong>directional anti-joins</strong> are the heart of post-trade exception management under T+1 time pressure.</p>"}
);

Object.assign(GLOSSARY_DATA, {
  "window-function": {"term":"Window function","group":"IP-SQL","eli5":"An aggregate that runs across a sliding 'window' of related rows without collapsing them into one.","memory":"OVER (PARTITION BY ... ORDER BY ...) = aggregate that keeps every row.","full":"<p>A <strong>window function</strong> computes a value over a set of rows (<code>OVER (PARTITION BY ... ORDER BY ...)</code>) while still returning one output row per input row. Core trade-ops uses: <code>SUM() OVER (PARTITION BY symbol ORDER BY ts)</code> for a <strong>running position</strong>; <code>ROW_NUMBER()</code> for dedup/latest-wins on amended fills; <code>LAG/LEAD</code> for deltas and FIX sequence-gap detection; <code>RANK/DENSE_RANK</code> for top-N.</p><p>An explicit frame (<code>ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW</code>) makes running totals deterministic across ties; the default <code>RANGE</code> frame would aggregate all tied-key rows together.</p>","trap":"Confusing ROW_NUMBER (always unique) with RANK (ties share + skip) — using RANK()=1 for dedup leaks duplicate fills back in.","worked":"<code>SUM(signed_qty) OVER (PARTITION BY symbol ORDER BY exec_ts ROWS UNBOUNDED PRECEDING) AS running_position</code>","related":["running-position","row-number-dedup"]},
  "running-position": {"term":"Running net position","group":"IP-SQL","eli5":"The cumulative buy-minus-sell quantity for a symbol as of each trade.","memory":"Sign the qty (buy +, sell -), then cumulative SUM OVER partition by symbol.","full":"<p>A <strong>running net position</strong> is the cumulative signed quantity for an instrument, computed with <code>SUM(signed_qty) OVER (PARTITION BY symbol ORDER BY exec_ts)</code>. The last row per symbol is the current position — the basis for position-keeping, exposure, and P&L. Positions are usually a <em>derived</em> view of fills, not an independent source of truth, so they can always be recomputed and re-reconciled.</p>","trap":"Forgetting to sign the quantity by side — summing raw qty makes buys and sells both add, inflating the position.","worked":"<code>CASE WHEN side='BUY' THEN qty ELSE -qty END</code> feeding the cumulative SUM OVER.","related":["window-function","post-trade"]},
  "row-number-dedup": {"term":"ROW_NUMBER() dedup / latest-wins","group":"IP-SQL","eli5":"Number rows within each group by recency and keep only number 1 to get the newest version.","memory":"PARTITION BY natural key, ORDER BY version DESC, keep rn=1.","full":"<p><code>ROW_NUMBER() OVER (PARTITION BY key ORDER BY version DESC, received_ts DESC)</code> assigns 1,2,3 within each group; filtering <code>rn=1</code> keeps the <strong>latest version</strong>. Essential when a <strong>FIX drop-copy</strong> feed pushes amended executions — <code>35=8</code> ExecutionReports including Trade Correction (<code>150=G</code>) and Trade Cancel (<code>150=H</code>) — and you must collapse to final state per <code>venue_exec_id</code>.</p>","trap":"Using DISTINCT or RANK instead — DISTINCT can't pick a winner among differing rows, and RANK ties leak duplicates through rn=1. Always add a deterministic tiebreaker.","worked":"<code>WITH r AS (SELECT *, ROW_NUMBER() OVER (PARTITION BY venue_exec_id ORDER BY version DESC) rn FROM raw_fills) SELECT * FROM r WHERE rn=1;</code>","related":["window-function","fix-dropcopy","null-semantics"]},
  "null-semantics": {"term":"NULL three-valued logic","group":"IP-SQL","eli5":"NULL means 'unknown', so comparisons with it are neither true nor false — and WHERE quietly drops them.","memory":"NULL = NULL is UNKNOWN; WHERE keeps only TRUE.","full":"<p>SQL uses <strong>three-valued logic</strong>: TRUE, FALSE, UNKNOWN. Any comparison with <code>NULL</code> yields UNKNOWN, and <code>WHERE</code> keeps only TRUE rows, so NULLs silently disappear from filters and joins. Aggregates ignore NULLs; <code>COUNT(*)</code> counts all rows but <code>COUNT(col)</code> skips NULLs. Use <code>IS NULL</code>, <code>COALESCE</code>, and <code>IS NOT DISTINCT FROM</code> for NULL-safe equality.</p>","trap":"NOT IN (subquery containing NULL) returns zero rows; and joining on a NULL key never matches, so missing identifiers must be treated as breaks.","worked":"<code>WHERE COALESCE(s.qty,0) <> COALESCE(i.qty,0)</code> — without COALESCE, rows where one side is NULL are dropped from the break report.","related":["anti-join","reconciliation","break"]},
  "select-for-update": {"term":"SELECT FOR UPDATE / isolation","group":"IP-SQL","eli5":"Lock a row while you read-modify-write it so a concurrent worker can't clobber your update.","memory":"FOR UPDATE = row lock = no lost updates.","full":"<p>Transaction <strong>isolation</strong> levels (READ UNCOMMITTED -> READ COMMITTED -> REPEATABLE READ -> SERIALIZABLE) progressively remove anomalies: dirty reads, non-repeatable reads, phantoms. <code>SELECT ... FOR UPDATE</code> takes an explicit <strong>row lock</strong> so two transactions can't read-modify-write the same row and produce a <strong>lost update</strong>.</p><p>Critical in <strong>position-keeping ledgers</strong> and settlement-instruction state machines: applying two fills concurrently to the same position must serialize, or the net quantity is wrong — a real correctness bug, not a theoretical one.</p>","trap":"Relying on the default isolation (often READ COMMITTED) to prevent lost updates — it doesn't; you need FOR UPDATE or SERIALIZABLE. (Engines vary: Postgres' weakest level is READ COMMITTED, never dirty reads.)","worked":"<code>BEGIN; SELECT net_qty FROM positions WHERE symbol='AAPL' FOR UPDATE; UPDATE positions SET net_qty=net_qty+100 WHERE symbol='AAPL'; COMMIT;</code>","related":["idempotency-key","running-position"]},
  "prediction-market": {"term":"Prediction market contract","group":"IP-SQL","eli5":"A bet on whether an event happens, priced 0 to 1 dollar, that pays $1 if it does and $0 if it doesn't.","memory":"Price = implied probability; settles $1 (yes) or $0 (no) at event resolution.","full":"<p>A <strong>prediction market</strong> trades binary EVENT contracts priced $0.00-$1.00 (the price is the market-implied probability). At resolution each contract settles to <strong>$1 if the event occurred, else $0</strong>. <strong>Kalshi</strong> is a CFTC-regulated Designated Contract Market (DCM, designated 2020). <strong>Polymarket</strong> (crypto-native, Polygon/USDC) became a CFTC-regulated US exchange via an Amended Order of Designation on Nov 25, 2025 after acquiring QCX/QCEX; the CFTC issued a Prediction Markets Advisory on Feb 25, 2026 asserting authority over DCM trading abuses.</p><p>Operationally, settlement is to <strong>event resolution</strong>, which creates oracle/resolution risk, disputed outcomes, and multi-platform arbitrage (Kalshi vs Polymarket) — distinct recon and settlement-automation challenges versus equities or crypto.</p>","trap":"Marking an open prediction position at last trade price as if it were realized — until resolution it's unrealized; P&L crystallizes only when the contract resolves to $1 or $0.","worked":"A 'YES' contract bought at $0.62 settles to $1.00 if the event occurs (+$0.38) or $0.00 if not (-$0.62).","related":["post-trade","reconciliation","running-position"]},
  "fix-dropcopy": {"term":"FIX drop-copy feed","group":"IP-SQL","eli5":"A read-only copy of every trade your firm does, pushed over the FIX protocol for capture and recon.","memory":"Drop-copy pushes 35=8 ExecutionReports; 150=F fill, 150=G correction, 150=H cancel.","full":"<p><strong>FIX</strong> (Financial Information eXchange) is a tag=value session protocol with sequence numbers; <code>NewOrderSingle</code> is <code>35=D</code> and <code>ExecutionReport</code> is <code>35=8</code>. A <strong>drop-copy</strong> session is a read-only feed that pushes <code>35=8</code> Execution Reports — the standard trade-capture/recon source — including ExecType <strong>Trade (<code>150=F</code>)</strong>, <strong>Trade Correction (<code>150=G</code>)</strong>, and <strong>Trade Cancel (<code>150=H</code>)</strong>. A sequence gap is recovered with a <code>ResendRequest (35=2)</code>.</p><p>Even crypto-derivatives venues expose FIX drop-copy (e.g. Coinbase Derivatives, Zero Hash), while spot crypto and prediction venues are predominantly REST + WebSocket. Automation against any of them needs idempotency (client order IDs), rate-limit handling, retries with backoff, and error handling.</p>","trap":"Treating drop-copy ExecutionReports as append-only — corrections (150=G) and cancels (150=H) amend prior executions, so you must collapse to latest-version-per-exec before computing positions.","worked":"<code>ROW_NUMBER() OVER (PARTITION BY venue_exec_id ORDER BY version DESC)</code> to keep the final state of each 35=8 report.","related":["row-number-dedup","post-trade","idempotency-key"]}
});
