/* Teach me from the ground up: FE-CSS (auto-assembled) */
enh({
  "box-sizing": {"teach":"<p>At its core, <code>box-sizing</code> is just a setting that answers one question: <strong>when you say \"make this 200px wide,\" do you mean 200px for the text area alone, or 200px for the whole visible box?</strong></p><h4>The two answers</h4><p>Every element has a content area, then padding around it, then a border around that. The <code>width</code> you set has to land <em>somewhere</em> on that stack. <code>box-sizing</code> picks where.</p><ul><li><strong><code>content-box</code></strong> (the default): your <code>width</code> measures <em>only</em> the content. Padding and border are then piled on <em>outside</em> that number. So <code>width:200px; padding:20px; border:5px</code> actually paints $200 + 20 + 20 + 5 + 5 = 250\\text{px}$ wide. The number you typed and the box you see disagree.</li><li><strong><code>border-box</code></strong>: your <code>width</code> measures content + padding + border together. The browser keeps the outer edge at 200px and <em>shrinks the content</em> to make room. With the same values, the box is exactly 200px wide and the content area quietly becomes $200 - 20 - 20 - 5 - 5 = 150\\text{px}$.</li></ul><div class=\"analogy\"><p>Think of a framed photo you want to hang in a 200px-wide slot. <code>content-box</code> is ordering a 200px <em>photo</em> and being surprised when the mat and frame push the whole thing to 250px and it won't fit. <code>border-box</code> is ordering a 200px <em>framed piece</em> — the frame shop trims the photo so the finished product is exactly 200px and slides right into the slot.</p></div><h4>Why everyone resets to border-box</h4><p>With <code>content-box</code>, every time you add padding you have to mentally subtract it back out of your width or your layout overflows. That math compounds across nested boxes and breaks fixed-width containers. So nearly every codebase opens with:</p><p><code>*, *::before, *::after { box-sizing: border-box }</code></p><p>Now \"200px wide\" means the box is 200px, full stop — padding lives <em>inside</em> the budget and can never blow out the container. Layout math becomes intuitive.</p><h4>The one thing it does NOT include</h4><p>Here is the trap: <strong>margin is excluded from both models.</strong> <code>border-box</code> bundles content + padding + border — that's it. Margin is spacing that sits <em>outside</em> the box entirely, so a <code>border-box</code> element with <code>width:200px; margin:10px</code> still occupies $200 + 10 + 10 = 220\\text{px}$ of horizontal room in the layout. The element <em>renders</em> at 200px, but its total footprint is larger.</p><div class=\"key\"><code>border-box</code> makes <code>width</code> mean \"the visible box including padding and border\" so padding never overflows — but margin always sits outside and adds to the layout footprint regardless.</div>"},
  "box-model": {"teach":"<p>At its core, the box model is just this idea: <strong>CSS draws every single element as a set of four nested rectangles.</strong> Text, buttons, divs, images — all of them are boxes inside boxes, and once you can see those four layers, all of CSS spacing stops being mysterious.</p><h4>The four layers, from inside out</h4><ul><li><strong>Content</strong> — the innermost rectangle, holding the actual text, image, or child elements. Its size is what <code>width</code>/<code>height</code> describe (under the default sizing).</li><li><strong>Padding</strong> — breathing room <em>inside</em> the border. Crucially, the element's <code>background</code> paints across the content <em>and</em> the padding, so padding looks like part of the element.</li><li><strong>Border</strong> — the drawn edge wrapping content + padding.</li><li><strong>Margin</strong> — transparent space <em>outside</em> the border that pushes neighbors away. It never shows the background and is never painted.</li></ul><div class=\"analogy\"><p>Picture a framed painting on a wall. The <strong>content</strong> is the canvas. The <strong>padding</strong> is the mat board between canvas and frame — same color treatment, clearly part of the artwork. The <strong>border</strong> is the wooden frame. The <strong>margin</strong> is the empty wall space you leave around it so the next painting doesn't touch it. You can paint the mat, but the empty wall is just air.</p></div><h4>Where the background stops</h4><p>By default <code>background</code> extends out to the <em>outer edge of the border</em> (<code>background-clip: border-box</code>). That's why padding always looks filled-in: it's covered by the background. Margin, by contrast, is pure transparent air — the page behind it shows through.</p><h4>Why this matters in practice</h4><p>The trap is treating margin like part of the element. <strong>Margin is invisible and outside the element's painted box and its event box.</strong> A click or hover on the margin region does <em>not</em> hit the element. If you give a button a big margin hoping to enlarge its tap target, you've failed — you only pushed neighbors away. To grow the clickable area, use <code>padding</code> instead.</p><h4>Doing the arithmetic</h4><p>Take <code>width:100; padding:10; border:2; margin:8</code> under the default content-box. The <em>painted</em> box (what you see and can click) is $100 + 10 + 10 + 2 + 2 = 124\\text{px}$ wide. The <em>total horizontal footprint</em> in the layout — what spaces neighbors — adds the margins: $124 + 8 + 8 = 140\\text{px}$.</p><div class=\"key\">Every element is content → padding → border → margin. Background fills out to the border edge; margin is transparent space outside the element that affects layout but is not painted and not clickable.</div>"},
  "margin-collapsing": {"teach":"<p>At its core, margin collapsing is just CSS saying: <strong>\"two stacked vertical gaps shouldn't add up — let's keep the bigger one and throw away the smaller.\"</strong> It surprises everyone the first time because $20 + 30$ comes out to 30, not 50.</p><h4>What exactly collapses</h4><p>Only <strong>vertical (block-axis) margins</strong> of block-level boxes in normal flow collapse. Horizontal (inline) margins <em>never</em> collapse — they always add. So this is purely a top-and-bottom phenomenon.</p><p>It happens in three places:</p><ul><li>Between <strong>adjacent siblings</strong> (one block's bottom margin meets the next block's top margin).</li><li>Between a <strong>parent and its first or last child</strong> (a child's top margin can \"escape\" up through the parent).</li><li>Within an <strong>empty block</strong> whose own top and bottom margins meet.</li></ul><h4>The rules for combining</h4><ul><li>Two <strong>positives</strong> → the larger wins ($20$ and $30$ → $30$).</li><li><strong>Mixed signs</strong> → add the largest positive to the most negative ($30$ and $-10$ → $20$).</li><li>Two <strong>negatives</strong> → the more negative wins ($-10$ and $-20$ → $-20$).</li></ul><div class=\"analogy\"><p>Think of two people each reserving personal space in a queue. One insists on 20cm of room, the other on 30cm. They don't end up 50cm apart — they just both respect the <em>larger</em> request and stand 30cm apart. The smaller demand is already satisfied by the bigger one, so it adds nothing.</p></div><h4>Why it exists (and why it bites)</h4><p>It was designed so typographic flow looks right: a paragraph with <code>margin:1em</code> top and bottom shouldn't double the gap between paragraphs to <code>2em</code>. Collapsing keeps consistent rhythm. But it bites when a child's top margin <em>escapes</em> its parent — you set <code>margin-top</code> on a child and the whole <em>parent</em> mysteriously shifts down, with no gap appearing inside.</p><h4>How to stop it</h4><p>Put <em>something solid</em> between the margins: any padding or border on the parent breaks the contact. Or establish a <strong>BFC</strong> — the cleanest trigger is <code>display: flow-root</code> on the parent, which contains the child's margin without side effects. Switching the container to <strong>flex or grid</strong> also disables collapsing entirely (flex/grid items don't collapse).</p><div class=\"key\">Stacked vertical block margins collapse to the larger single margin (never sum) between siblings and through parents. To contain an escaping child margin, give the parent <code>display: flow-root</code>.</div>"},
  "position": {"teach":"<p>At its core, <code>position</code> just answers: <strong>\"where does this element sit, and what is it allowed to be offset from?\"</strong> By default everything flows top-to-bottom in document order; <code>position</code> lets you peel an element out of that flow or nudge it.</p><h4>The five values</h4><ul><li><strong><code>static</code></strong> — the default. The element sits in normal flow and ignores <code>top/right/bottom/left</code> entirely.</li><li><strong><code>relative</code></strong> — stays in flow (its original space is preserved), but you can nudge it from <em>its own</em> position with offsets. It also quietly becomes a <em>containing block</em> for absolute children — this is its most common real use.</li><li><strong><code>absolute</code></strong> — ripped <em>out of flow</em> (siblings close the gap as if it vanished) and anchored to the padding box of the nearest <em>positioned</em> ancestor.</li><li><strong><code>fixed</code></strong> — out of flow and anchored to the <em>viewport</em>, so it stays put while the page scrolls (think sticky headers, chat bubbles).</li><li><strong><code>sticky</code></strong> — a hybrid: it scrolls along in normal flow until it hits a threshold you set (e.g. <code>top:0</code>), then \"sticks\" pinned within its scroll container.</li></ul><div class=\"analogy\"><p>Imagine arranging guests in a theater. <code>static</code> guests take their assigned seats in row order. A <code>relative</code> guest leans 8cm left but their seat stays empty-reserved behind them. An <code>absolute</code> guest leaves the seating chart and stands at a spot measured from the nearest doorway you marked. A <code>fixed</code> guest stands glued to a spot on the <em>screen</em> no matter how the theater scrolls. A <code>sticky</code> guest walks with the crowd until they reach the aisle, then plants themselves there.</p></div><h4>It also affects paint order</h4><p>Non-static positioning combined with a numeric <code>z-index</code> creates a <strong>stacking context</strong>, so <code>position</code> couples <em>layout</em> with <em>paint order</em> — that's why positioned things often appear on top.</p><h4>The trap with fixed</h4><p>People assume <code>fixed</code> is <em>always</em> relative to the viewport. It usually is — until an ancestor has <code>transform</code>, <code>filter</code>, <code>perspective</code>, or <code>contain</code>. Any of those turns that ancestor into the fixed element's containing block, so your \"viewport-fixed\" modal suddenly scrolls away with its parent. A single <code>transform</code> on a wrapper can silently break a fixed overlay.</p><h4>Worked pattern</h4><p>To pin a notification badge to a card corner: give the card <code>position:relative</code> (making it the anchor) and the badge <code>position:absolute; top:-8px; right:-8px</code>. The badge now sits relative to the card, not the page.</p><div class=\"key\">Use <code>relative</code> to make an element an anchor, <code>absolute</code> to position a child against that anchor — but remember a <code>transform</code>/<code>filter</code> on any ancestor can hijack what <code>fixed</code> (and <code>absolute</code>) measure from.</div>"},
  "containing-block": {"teach":"<p>At its core, the containing block is just <strong>the rectangle that percentages and offsets are measured against.</strong> When you write <code>width:50%</code> or <code>top:0</code>, the browser has to ask \"50% of what? zero from where?\" — the containing block is that \"what\" and \"where.\"</p><h4>Which rectangle it is depends on position</h4><ul><li>For <strong>static / relative</strong> elements, the containing block is the <em>content box</em> of the nearest block-container ancestor. So a percentage width is a fraction of the parent's content area.</li><li>For <strong>absolute</strong> elements, it's the <em>padding box</em> of the nearest <em>positioned</em> ancestor (one with <code>position</code> set to anything but static).</li><li>For <strong>fixed</strong> elements, it's the <em>viewport</em> (the initial containing block) — normally.</li></ul><div class=\"analogy\"><p>Think of giving directions \"go 3 blocks north.\" That instruction is meaningless until you fix a starting corner. The containing block is the starting corner. Say \"50% of the way across\" and you also need to know which room you're measuring — a 50% that means half a closet is very different from half a ballroom. The containing block tells you which room.</p></div><h4>The sneaky part: layout properties can create one</h4><p>Here is what trips up senior engineers. It's not only <code>position</code> that establishes a containing block. The properties <code>transform</code>, <code>filter</code>, <code>perspective</code>, <code>contain</code>, and <code>will-change</code> <em>also</em> establish a containing block <strong>for absolutely and fixed-positioned descendants.</strong></p><p>So a perfectly innocent <code>transform: translateZ(0)</code> — a common GPU/compositing trick people sprinkle on to \"force hardware acceleration\" — silently re-anchors a <code>position:fixed</code> modal inside that element. Your modal stops tracking the viewport and starts scrolling with the page instead. The fix is invisible to the eye and maddening to debug, because nothing about <code>transform</code> obviously screams \"I changed your modal's anchor.\"</p><h4>Why this design exists</h4><p>It's a consequence of how compositing works: once an element is transformed or filtered, the browser must render its descendants relative to <em>it</em> to keep paint correct. \"Fixed-to-viewport\" can't be honored across a transform boundary, so the spec re-anchors instead.</p><h4>Worked example</h4><p>Give <code>.parent{position:relative}</code> and then <code>.child{position:absolute; inset:0}</code>. Because the parent is now positioned, it becomes the child's containing block, and <code>inset:0</code> (all four offsets at zero) makes the child fill the parent exactly — not the page.</p><div class=\"key\">The containing block is the reference rectangle for % sizes and offsets. Beyond <code>position</code>, the properties <code>transform/filter/perspective/contain/will-change</code> also create one — which is how a stray <code>transform</code> can hijack a <code>fixed</code> element.</div>"},
  "stacking-context": {"teach":"<p>At its core, a stacking context is just <strong>a self-contained layer of elements that paint together as one unit</strong> — and <code>z-index</code> only ranks elements <em>inside the same</em> context, never across different ones.</p><h4>Why \"context\" matters more than the number</h4><p>Beginners think <code>z-index</code> is a global priority number: bigger = always on top. It isn't. Elements are first grouped into stacking contexts, and a context paints as an <em>atomic blob</em> relative to its siblings. A child can shuffle freely <em>within</em> its parent's context, but it can <strong>never escape</strong> to sit above something outside that context — no matter how huge its <code>z-index</code>.</p><div class=\"analogy\"><p>Think of stacked transparent folders on a desk. Inside one folder you can reorder the pages however you like — page 9999 on top of page 1. But if Folder A sits <em>under</em> Folder B on the desk, no page inside Folder A can rise above Folder B's pages. The page number only competes against other pages in the same folder. <code>z-index</code> is the page number; the folder is the stacking context.</p></div><h4>What creates a new context</h4><p>The root <code>&lt;html&gt;</code> element always is one. Beyond that, a new context is created by:</p><ul><li>positioned elements (<code>relative/absolute/sticky/fixed</code>) with a <em>numeric</em> <code>z-index</code>;</li><li>fixed and sticky elements (even without z-index);</li><li><code>opacity</code> less than 1;</li><li><code>transform</code>, <code>filter</code>, <code>perspective</code>, <code>mix-blend-mode</code>;</li><li><code>isolation: isolate</code>, <code>contain</code>, and <code>will-change</code>.</li></ul><p>Notice many of these are \"harmless-looking\" visual properties. <strong><code>opacity: 0.99</code> alone creates a context</strong> — that's the classic gotcha.</p><h4>The trap: cranking z-index to 9999</h4><p>The instinct when something is hidden behind a sibling is to slam <code>z-index: 9999</code> on it. But if the element lives in an <em>ancestor</em> that itself sits in a lower-z context, no value on earth lifts it past that ancestor's siblings. You're raising a page within a folder that's already buried under another folder.</p><h4>Worked example</h4><p>A dropdown hides behind a sibling card. The cause: the card had <code>opacity:.99</code>, which spawned a fresh stacking context that paints atop the dropdown's. Two fixes: remove the stray <code>opacity</code>, or deliberately add <code>isolation:isolate</code> to a shared parent so both elements share one context and z-index ordering becomes predictable. <code>isolation:isolate</code> is special: it creates a context with <em>no</em> other visual side effects — your scalpel for exactly this problem.</p><div class=\"key\"><code>z-index</code> only competes within a single stacking context. When raising z-index doesn't help, look for an ancestor with <code>opacity/transform/filter/z-index</code> that trapped your element — and reach for <code>isolation:isolate</code> as a side-effect-free fix.</div>"},
  "flex-shorthand": {"teach":"<p>At its core, <code>flex</code> is just <strong>one line that tells a flex item three things: how eagerly it grows, how willingly it shrinks, and what size it starts at.</strong> Those three knobs together decide its final width (or height, in a column).</p><h4>The three parts of <code>flex: grow shrink basis</code></h4><ul><li><strong>grow</strong> — when there's <em>leftover</em> space on the main axis, how big a share does this item grab? Higher number = bigger slice of the surplus.</li><li><strong>shrink</strong> — when items <em>overflow</em> the container, how much does this item give back? Higher = shrinks more. (Shrinking is weighted by basis × factor, so bigger items naturally cede more.)</li><li><strong>basis</strong> — the item's size <em>before</em> any growing or shrinking is applied. The starting point.</li></ul><div class=\"analogy\"><p>Picture splitting a restaurant bill among friends. <strong>basis</strong> is what each person already pledged. <strong>grow</strong> is how the <em>refund</em> (leftover budget) gets divided — eager friends with grow:2 take twice the refund of grow:1 friends. <strong>shrink</strong> is how an <em>overage</em> gets covered — friends with higher shrink chip in more to cover the shortfall. Same three questions every shared-cost situation asks.</p></div><h4>The keywords you'll actually type</h4><ul><li><code>flex: 1</code> expands to <code>1 1 0%</code>. Because basis is <strong>0</strong>, content is ignored entirely — items become <em>equal tracks</em> regardless of how much text each holds.</li><li><code>flex: auto</code> expands to <code>1 1 auto</code>. Basis is the item's natural content size, so items grow from a <em>content-weighted</em> start — a longer item ends up wider.</li><li><code>flex: none</code> expands to <code>0 0 auto</code>. No grow, no shrink — a <strong>rigid</strong> item that holds its content size no matter what.</li></ul><h4>The classic confusion</h4><p>People put <code>flex:1</code> on items with very different content and expect the wordier one to be wider. It won't be — <code>flex:1</code> has <code>basis:0%</code>, so content is invisible to the sizing math and all items come out <em>equal</em>. If you <em>want</em> content to influence the result, reach for <code>flex:auto</code> instead. This single distinction (basis 0 vs basis auto) is the heart of most flex layout surprises.</p><h4>Worked layout</h4><p>A fixed sidebar beside a fluid main area: <code>.sidebar{flex:0 0 240px}</code> (no grow, no shrink, locked at 240px) and <code>.main{flex:1}</code> (grabs all remaining space). The sidebar stays exactly 240px; main fluidly fills whatever is left.</p><div class=\"key\"><code>flex:1</code> = <code>1 1 0%</code> makes equal tracks that ignore content; <code>flex:auto</code> = <code>1 1 auto</code> lets content influence width; <code>flex:none</code> = <code>0 0 auto</code> stays rigid. The <em>basis</em> is what separates them.</div>"},
  "flex-axes": {"teach":"<p>At its core, flexbox works on <strong>two perpendicular directions: a main axis where items line up, and a cross axis where they align across.</strong> Almost every flexbox bug comes from confusing which is which after you change direction.</p><h4>Main vs cross</h4><ul><li>The <strong>main axis</strong> is whichever way <code>flex-direction</code> points. With the default <code>row</code>, the main axis runs <em>horizontally</em> (left→right); items line up side by side along it.</li><li>The <strong>cross axis</strong> is always perpendicular. In a <code>row</code>, the cross axis runs <em>vertically</em>.</li></ul><h4>Which property controls which axis</h4><ul><li><strong><code>justify-content</code></strong> distributes items along the <em>main</em> axis (spacing them out, centering, pushing to ends).</li><li><strong><code>align-items</code></strong> / <strong><code>align-self</code></strong> align items along the <em>cross</em> axis (per-line and per-item, respectively).</li><li><strong><code>align-content</code></strong> distributes <em>wrapped lines</em> as a group along the cross axis (only matters when items wrap onto multiple lines).</li></ul><div class=\"analogy\"><p>Think of books on a shelf. The shelf board is the <strong>main axis</strong> — <code>justify-content</code> decides whether the books bunch left, spread evenly, or center along the board. The <strong>cross axis</strong> is up-and-down within the shelf's height — <code>align-items</code> decides whether short books sit on the shelf floor, float to the top, or center vertically. Now <em>turn the shelf 90°</em> (switch to <code>column</code>): the board is suddenly vertical, and every \"which way?\" answer flips. The properties didn't change meaning — the physical direction they point did.</p></div><h4>Why the flip bites</h4><p>When you set <code>flex-direction: column</code>, the main axis becomes <em>vertical</em> and the cross axis becomes <em>horizontal</em>. So <code>justify-content:center</code> now centers things <em>vertically</em>, and <code>align-items:center</code> now centers them <em>horizontally</em> — the exact opposite of the row case. Muscle memory built on rows quietly breaks.</p><h4>The trap</h4><p>The most common mistake: you have a <code>row</code> of items and want them centered <em>vertically</em>, so you reach for <code>justify-content:center</code>. But in a row, vertical is the <em>cross</em> axis — <code>justify-content</code> only touches the main (horizontal) axis. The correct tool is <code>align-items:center</code>.</p><h4>Worked example</h4><p>A centered toolbar: <code>display:flex; justify-content:space-between; align-items:center</code>. <code>justify-content:space-between</code> pushes items to the two ends along the main (horizontal) axis with even gaps; <code>align-items:center</code> vertically centers them on the cross axis. Logo left, nav right, everything vertically aligned.</p><div class=\"key\">Main axis follows <code>flex-direction</code>; <code>justify-content</code> works the main axis, <code>align-items</code> works the cross. Switching to <code>column</code> swaps which physical direction each one controls.</div>"},
  "grid-fr": {"teach":"<p>At its core, the <code>fr</code> unit is just <strong>\"give me a share of whatever space is left over,\"</strong> and <code>minmax()</code> is just <strong>\"keep this track between a floor and a ceiling.\"</strong> Together they're how CSS Grid does fluid, responsive columns without media queries.</p><h4>The fr unit</h4><p><code>fr</code> means <em>fraction of the free space</em> — the space remaining <em>after</em> fixed-size tracks, content-sized tracks, and gaps are subtracted. So <code>1fr 2fr</code> splits the leftover into thirds: one share and two shares. It's a proportional divider, not an absolute size.</p><h4>minmax(min, max)</h4><p><code>minmax(200px, 1fr)</code> says: this track is <em>never smaller than 200px</em> and <em>never wants to be bigger than its fair fr share</em>. The floor guarantees readability; the ceiling lets it stretch to fill.</p><h4>The responsive workhorse</h4><p>Combine them inside <code>repeat()</code>:</p><p><code>grid-template-columns: repeat(auto-fit, minmax(200px, 1fr))</code></p><p>This means: pack in as many columns as fit, each at least 200px, and let them share the row equally. Resize the window and columns reflow automatically — no breakpoints needed.</p><h4>auto-fit vs auto-fill — the subtle twin</h4><ul><li><strong><code>auto-fit</code></strong> <em>collapses</em> empty tracks to zero, so the existing items <em>stretch</em> to fill the whole row.</li><li><strong><code>auto-fill</code></strong> <em>keeps</em> empty phantom tracks reserved, leaving gaps where items would go if you had more.</li></ul><div class=\"analogy\"><p>Imagine seating people at a long table that fits up to 6 chairs. <code>auto-fill</code> keeps all 6 chairs out even if only 3 people show up — there are empty chairs and gaps. <code>auto-fit</code> removes the empty chairs and lets your 3 guests spread out comfortably across the whole table. Same table, different handling of \"what if it's not full.\"</p></div><h4>The trap that catches everyone</h4><p>People write <code>repeat(auto-fit, 200px)</code> and wonder why columns won't stretch to fill the row. There's <em>no growth term</em> — every column is locked at exactly 200px, so leftover space just sits empty on the right. You need the <code>1fr</code> ceiling: <code>minmax(200px, 1fr)</code> provides the \"grow to fill\" behavior.</p><p>A related gotcha: an <code>fr</code> track has an implicit minimum of <em>its content's size</em>, so a long word or image can blow it past its share. When you need a track to shrink <em>below</em> its content (e.g. to allow text-overflow ellipsis), use <code>minmax(0, 1fr)</code> to override that floor to zero.</p><h4>Worked example</h4><p><code>grid-template-columns: repeat(auto-fit, minmax(220px, 1fr))</code> produces as many ≥220px columns as fit the container, each one sharing the row width equally — a card grid that responds to any screen size on its own.</p><div class=\"key\"><code>fr</code> shares leftover space; <code>minmax(min,1fr)</code> gives a track both a floor and a stretch-to-fill ceiling. Forget the <code>1fr</code> and columns won't grow; use <code>minmax(0,1fr)</code> to let a track shrink below its content.</div>"},
  "bfc": {"teach":"<p>At its core, a Block Formatting Context (BFC) is just <strong>an isolated layout bubble</strong> — a region that lays out its block-level children independently of the world outside it. Inside the bubble, floats are contained and margins can't leak across the edge.</p><h4>What the bubble actually does</h4><p>A BFC gives you three powers:</p><ul><li><strong>It contains floated children.</strong> Normally a parent with only floated kids collapses to zero height (floats are out of flow). A BFC parent <em>grows to wrap</em> its floats — the classic \"clearfix\" problem, solved.</li><li><strong>It blocks margin collapse across its edge.</strong> A child's vertical margin can't escape through a BFC boundary, so the parent stops mysteriously shifting.</li><li><strong>It stops content from wrapping around an external float.</strong> Text inside a BFC stays in a clean rectangle beside a sibling float instead of flowing underneath it.</li></ul><div class=\"analogy\"><p>Think of a BFC as a fish tank on a counter. Whatever you pour in — water, fish, decorations (your floats and margins) — stays inside the glass. It can't spill onto the counter, and water from <em>another</em> tank on the same counter can't seep in. Without the tank, liquids (margins) merge and things (floats) overflow onto the surface. The tank is a self-contained layout world.</p></div><h4>What triggers a BFC</h4><ul><li><code>display: flow-root</code> — the <strong>purpose-built, side-effect-free</strong> trigger. Use this.</li><li><code>overflow</code> set to anything other than <code>visible</code> (e.g. <code>hidden</code>, <code>auto</code>).</li><li>floats themselves, and absolute/fixed positioned elements.</li><li>flex items, grid items, and inline-blocks (these are BFCs automatically).</li></ul><h4>The trap</h4><p>For years the go-to trick to contain floats was <code>overflow:hidden</code>. It works — but it has a nasty side effect: it <em>clips anything that overflows the box</em>. Drop shadows, tooltips, focus rings, and absolutely-positioned popovers that extend past the edge all get chopped off. That's a debugging nightmare because the BFC and the clipping are the same property.</p><p>The modern fix is <code>display: flow-root</code>: it establishes a BFC with <em>zero</em> visual side effects — no clipping, no scrollbars, nothing. It exists for exactly this job.</p><h4>Worked example</h4><p>The \"media object\" pattern — an avatar floated left next to a block of text. Give the wrapper <code>.media{display:flow-root}</code>. Now the floated avatar is contained, and the text body forms a clean column <em>beside</em> the float rather than wrapping awkwardly underneath it once it runs longer than the avatar is tall.</p><div class=\"key\">A BFC is an isolated layout region that contains floats and stops margins from collapsing across its edge. Trigger it with <code>display: flow-root</code> — the side-effect-free choice — rather than the clipping-prone <code>overflow:hidden</code>.</div>"}
});
