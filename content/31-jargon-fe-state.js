/* Frontend jargon & acronyms — clickable reference (FE-STATE, auto-assembled) */
Object.assign(GLOSSARY_DATA, {
  "fsm": {"term":"FSM","group":"FE-STATE","aliases":["Finite State Machine","Finite-State Machine","state machine"],"eli5":"A model where a system can be in exactly one of a fixed set of named states at a time, and only well-defined transitions move it between them.","memory":"Finite States + Machine — a fixed menu of modes, with arrows showing the only legal moves between them.","full":"<p><strong>What it is.</strong> A <strong>Finite State Machine (FSM)</strong> is a computational model consisting of a <em>finite</em> set of named states, a designated start state, and a set of transitions that move the machine from one state to another in response to events or inputs. At any instant the machine occupies exactly one state, which is the whole point: instead of tracking many loosely-related boolean flags (<code>isLoading</code>, <code>isError</code>, <code>isSuccess</code>) that can drift into impossible combinations, an FSM enumerates the legal states and the only transitions allowed between them.</p><p><strong>History / origin.</strong> The finite automaton is a foundational idea in theoretical computer science, formalized in the mid-20th century in the work on automata theory (associated with researchers such as McCulloch and Pitts on neural models in the 1940s, and later Moore and Mealy, whose names label the two classic FSM output styles). It long predates the web. In frontend development the pattern was popularized for UI by libraries like <strong>XState</strong> (created by David Khourshid, first released in the late 2010s), which brought statecharts — Harel's hierarchical extension of FSMs from the 1980s — to JavaScript.</p><p><strong>Purpose &amp; why still used.</strong> FSMs make UI logic explicit and verifiable: impossible states become unrepresentable, transitions are auditable, and the diagram of states <em>is</em> the spec. They remain widely used for complex flows — multi-step forms, media players, authentication, drag-and-drop — because they tame the combinatorial explosion of ad-hoc flags. Even teams that don't adopt a full library often think in FSM terms, and frameworks increasingly bake state-machine semantics into routing and form primitives.</p>","worked":"<p>A fetch flow as an FSM instead of scattered booleans:</p><ul><li><strong>idle</strong> → on <code>FETCH</code> → <strong>loading</strong></li><li><strong>loading</strong> → on <code>SUCCESS</code> → <strong>success</strong>; on <code>ERROR</code> → <strong>failure</strong></li><li><strong>failure</strong> → on <code>RETRY</code> → <strong>loading</strong></li></ul><p>In XState-style config:</p><p><code>const machine = { initial: 'idle', states: { idle: { on: { FETCH: 'loading' } }, loading: { on: { SUCCESS: 'success', ERROR: 'failure' } }, success: {}, failure: { on: { RETRY: 'loading' } } } }</code></p><p>Now <code>loading</code> and <code>success</code> can never both be true — the structure forbids it.</p>","teach":"<p>At its core, an FSM is just a list of the modes your thing can be in, plus the rules for hopping between them. Nothing more exotic than that.</p><p>Think about how UI bugs usually happen. You add <code>isLoading</code>, then <code>isError</code>, then <code>isSuccess</code>, each a separate boolean. With three booleans there are eight combinations — but most are nonsense. <em>Loading AND error AND success</em> all true at once should be impossible, yet nothing in your code prevents it. So eventually a stray <code>setState</code> produces a spinner on top of an error message, and you spend an afternoon hunting it.</p><div class=\"analogy\">A vending machine is the perfect mental model. It is <em>waiting for money</em>, or <em>has partial payment</em>, or <em>dispensing</em>, or <em>making change</em> — exactly one at a time. Inserting a coin while it's dispensing does nothing, because that transition simply isn't wired. You never see a vending machine that is simultaneously empty and full. The physical machine <em>is</em> a finite state machine.</div><p>An FSM gives your UI that same discipline. You declare the states by name (<code>idle</code>, <code>loading</code>, <code>success</code>, <code>failure</code>), pick a start state, and draw arrows: from <code>loading</code> a <code>SUCCESS</code> event goes to <code>success</code>, an <code>ERROR</code> event goes to <code>failure</code>. Crucially, you only define the transitions you <em>want</em>. An event that has no arrow from the current state is ignored — so a late, duplicate response can't yank you out of <code>success</code> back into <code>loading</code>.</p><p>Two payoffs follow. First, impossible states become unrepresentable: there's one <code>state</code> variable holding one value, not N independent flags. Second, the machine is self-documenting — the set of states and arrows is literally a diagram of how the feature behaves, which designers and testers can read.</p><div class=\"key\">An FSM replaces a tangle of booleans with one variable holding one named state and an explicit set of legal transitions. Impossible combinations vanish by construction, and the state diagram doubles as the spec.</div>","related":["hydration"]},
  "crud": {"term":"CRUD","group":"FE-STATE","aliases":["Create, Read, Update, Delete","Create Read Update Delete"],"eli5":"The four basic operations — Create, Read, Update, Delete — that cover everything you typically do to stored data.","memory":"Create, Read, Update, Delete — the four verbs that make up almost every data operation.","full":"<p><strong>What it is.</strong> <strong>CRUD</strong> is an acronym for the four fundamental operations on persistent data: <strong>C</strong>reate, <strong>R</strong>ead, <strong>U</strong>pdate, and <strong>D</strong>elete. It's a vocabulary for describing data manipulation at any layer — a database, a REST API, or a client-side state store. Most application features ultimately decompose into some combination of these four verbs.</p><p><strong>History / origin.</strong> The term comes from the database world and was popularized in the early 1980s (commonly attributed to James Martin's writing on database-driven systems). It long predates the web but mapped cleanly onto it: REST APIs typically express CRUD via HTTP methods (POST = create, GET = read, PUT/PATCH = update, DELETE = delete).</p><p><strong>Purpose &amp; why still used.</strong> CRUD gives developers a shared shorthand for the shape of data operations, and it shows up directly in frontend tooling. In Redux Toolkit, for example, <code>createEntityAdapter</code> generates ready-made <strong>CRUD reducers</strong> (addOne, updateOne, removeOne, etc.) plus a normalized <code>{ ids, byId }</code> shape and memoized selectors — so you don't hand-write the four operations for every entity type. CRUD is timeless and still the default mental model for data-backed apps.</p>","worked":"<p>CRUD mapped to a REST API for a <code>/todos</code> resource:</p><ul><li><strong>Create</strong> → <code>POST /todos</code></li><li><strong>Read</strong> → <code>GET /todos</code> / <code>GET /todos/42</code></li><li><strong>Update</strong> → <code>PUT</code> or <code>PATCH /todos/42</code></li><li><strong>Delete</strong> → <code>DELETE /todos/42</code></li></ul><p>CRUD in Redux Toolkit state:</p><p><code>const adapter = createEntityAdapter();</code><br><code>// auto-generates CRUD reducers: adapter.addOne, adapter.updateOne, adapter.removeOne, adapter.setAll</code><br><code>// plus normalized state: { ids: [...], entities: { [id]: {...} } }</code></p>","teach":"<p>At its core, CRUD is just the observation that almost everything you do to stored data is one of four things: make new data, look at existing data, change it, or get rid of it. Create, Read, Update, Delete. That's the whole acronym.</p><p>Its power is as shared vocabulary. When someone says \"this screen is basically CRUD,\" every developer instantly pictures a list view (Read), an Add button (Create), edit forms (Update), and a trash icon (Delete). It compresses a lot of design into one word.</p><div class=\"analogy\">Think of a filing cabinet. You can put a new folder in (Create), pull a folder out to read it (Read), open a folder and change its papers (Update), or shred a folder (Delete). There simply aren't other fundamental things you do to a filing cabinet — every clerical task is a sequence of those four. CRUD says your data layer is the same kind of cabinet.</div><p>The reason it sticks around is that it maps cleanly onto the layers frontend developers touch. On the network, REST APIs line CRUD up with HTTP methods: POST creates, GET reads, PUT/PATCH updates, DELETE deletes. That's not a coincidence — REST was designed around manipulating resources, which is exactly CRUD.</p><p>It also shows up inside your app's state. Tools like Redux Toolkit's <code>createEntityAdapter</code> recognize that you keep writing the same four operations for every kind of entity — adding a user, updating a user, removing a user — so they generate those <strong>CRUD reducers</strong> for you, along with a normalized lookup table (<code>ids</code> + <code>entities</code>) and fast selectors. You declare the entity once and get Create/Read/Update/Delete plumbing for free.</p><p>So while CRUD sounds like a dry database term, in practice it's the backbone abstraction connecting your UI buttons, your API calls, and your state store — all describing the same four verbs at different layers.</p><div class=\"key\">CRUD = Create, Read, Update, Delete: the four operations nearly all data work reduces to. It maps onto REST HTTP methods and onto state-store helpers like Redux Toolkit's <code>createEntityAdapter</code>, which auto-generates exactly these reducers.</div>","related":["url"]},
  "rtk": {"term":"RTK","group":"FE-STATE","aliases":["Redux Toolkit","@reduxjs/toolkit"],"eli5":"The official, batteries-included way to write Redux today — it removes the hand-written boilerplate that old Redux required.","memory":"Redux ToolKit: Redux + a toolkit of helpers (configureStore, createSlice) so you stop writing boilerplate.","full":"<p><strong>RTK (Redux Toolkit)</strong> is the official, recommended toolset for writing Redux logic. It is not a separate state library — it <em>is</em> Redux, packaged with opinionated helpers that eliminate the verbose, error-prone boilerplate (hand-rolled action-type constants, action creators, and giant <code>switch</code> reducers) that characterized \"classic\" Redux. That old pattern is now considered legacy boilerplate, not best practice.</p><p>RTK was developed by the <strong>Redux maintainers</strong> (notably Mark Erikson) and released around 2019 to address the community's biggest complaint: Redux required too much ceremony. Its headline APIs include <code>createSlice</code> (generates action creators and a reducer together, letting you write \"mutating\" logic safely via the Immer library) and <code>configureStore</code>, which sets up the store with sensible defaults — <strong>Redux DevTools</strong> integration, the <code>redux-thunk</code> middleware for async logic, and development-time checks for accidental state mutation and non-serializable values, all enabled by default. RTK also ships <strong>RTK Query</strong> for data fetching/caching.</p><p>RTK is the default way to use Redux in 2026. The official Redux docs steer everyone to it, and writing raw Redux by hand is actively discouraged. While lighter alternatives (Zustand, Jotai) and server-state libraries (TanStack Query) have taken some use cases, RTK remains the standard for substantial client-side global state where Redux's structure, DevTools time-travel, and middleware ecosystem are valued.</p>","worked":"<p><code>createSlice</code> generates action creators + reducer; you write Immer-powered \"mutating\" logic safely:</p><p><code>const counter = createSlice({</code><br><code>&nbsp;&nbsp;name: 'counter',</code><br><code>&nbsp;&nbsp;initialState: { value: 0 },</code><br><code>&nbsp;&nbsp;reducers: {</code><br><code>&nbsp;&nbsp;&nbsp;&nbsp;increment: (state) =&gt; { state.value += 1; },  // safe via Immer</code><br><code>&nbsp;&nbsp;},</code><br><code>});</code></p><p><code>configureStore</code> wires DevTools + thunk + dev checks automatically:</p><p><code>const store = configureStore({ reducer: { counter: counter.reducer } });</code></p>","teach":"<p>At its core, RTK is just <em>Redux with the tedious parts done for you</em>. Same store, same actions, same reducers, same one-way data flow — but you stop writing the mountain of boilerplate that made people hate classic Redux.</p><p>To appreciate it, recall what \"classic\" Redux demanded for even a trivial feature:</p><ul><li>A file of string action-type constants (<code>const INCREMENT = 'counter/increment'</code>).</li><li>Hand-written action creator functions returning <code>{ type, payload }</code>.</li><li>A reducer with a giant <code>switch</code> statement, where every case had to return a <em>brand-new</em> immutable copy of state by hand (<code>{ ...state, value: state.value + 1 }</code>).</li><li>Manual store setup wiring in DevTools and async middleware.</li></ul><div class=\"analogy\">Classic Redux is like assembling flat-pack furniture where the box gives you raw planks, a bag of screws, and tells you to cut the dowels yourself. RTK is the same furniture, but the panels come pre-drilled, the screws are pre-sorted, and the instructions are three steps. You build the exact same cabinet — far faster, with far fewer ways to mess up.</div><p>The two helpers that do the heavy lifting:</p><ul><li><strong><code>createSlice</code></strong> — you declare a name, initial state, and reducer functions. It <em>generates</em> the action types and action creators for you, and thanks to the bundled <strong>Immer</strong> library you can write what looks like mutating code (<code>state.value += 1</code>) while Immer produces the correct immutable update behind the scenes.</li><li><strong><code>configureStore</code></strong> — sets up the store with good defaults baked in: Redux DevTools (time-travel debugging), <code>redux-thunk</code> for async, and dev-only guards that warn you if you accidentally mutate state outside Immer or stash non-serializable values.</li></ul><p>On top of that, <strong>RTK Query</strong> handles server data fetching and caching, overlapping with tools like TanStack Query.</p><div class=\"key\">RTK = Redux Toolkit, the official default for Redux. <code>createSlice</code> kills action-constant/switch-reducer boilerplate (Immer makes \"mutations\" safe); <code>configureStore</code> bundles DevTools, thunk, and mutation/serializability dev checks on by default. Writing raw Redux by hand is now legacy — reach for RTK.</div>"},
  "swr": {"term":"SWR","group":"FE-STATE","aliases":["stale-while-revalidate","SWR (React Hooks for Data Fetching)"],"eli5":"A React data-fetching library that instantly shows cached (possibly stale) data while refetching fresh data in the background.","memory":"SWR = Stale-While-Revalidate: show the old, fetch the new.","full":"<p><strong>SWR</strong> is a React data-fetching library built around the HTTP caching strategy <em>stale-while-revalidate</em>. The pattern itself comes from the <code>Cache-Control</code> directive defined in RFC 5861 (an extension to HTTP caching): serve the cached (stale) response immediately, then revalidate it in the background so the next read is fresh. The SWR library applies that idea to client-side React: a component calls <code>useSWR(key, fetcher)</code> and gets back cached data instantly, while SWR refetches to keep it current.</p><p>SWR was created by <strong>Vercel</strong> (the company behind Next.js) and released around 2019, with a major <code>2.0</code> release later adding mutation, optimistic updates, and infinite-loading helpers. It sits in the same category as TanStack <em>React Query</em> and Redux Toolkit's <em>RTK Query</em> — all of which treat the server as the source of truth and your local store as a synchronized cache rather than hand-mutated state.</p><p>Its purpose is to eliminate the boilerplate of hand-rolled loading/error/cache/refetch logic: deduplication of requests, focus/reconnect revalidation, interval polling, and cache invalidation are all built in. It remains widely used in 2026 because it is tiny, hook-first, and pairs naturally with Next.js; React Query is the heavier-featured alternative, but SWR is still the go-to for lightweight server-cache needs.</p>","worked":"<p>A component that fetches a user profile and revalidates automatically:</p><pre><code>import useSWR from 'swr'\n\nconst fetcher = (url) =&gt; fetch(url).then(r =&gt; r.json())\n\nfunction Profile({ id }) {\n  const { data, error, isLoading } = useSWR(`/api/user/${id}`, fetcher)\n\n  if (error) return &lt;p&gt;Failed to load&lt;/p&gt;\n  if (isLoading) return &lt;p&gt;Loading…&lt;/p&gt;\n  return &lt;h1&gt;Hello {data.name}&lt;/h1&gt;\n}</code></pre><p>On revisit, <code>data</code> is returned instantly from cache (stale) while SWR refetches (revalidate) in the background.</p>","teach":"<p>At its core, SWR is just a <strong>smart cache that lives between your React component and your server</strong>. Instead of every component fetching data fresh and juggling loading flags, SWR keeps one shared cache keyed by a string (usually the URL) and hands every component the same cached value — refetching quietly when it suspects the data has gone stale.</p><div class=\"analogy\">Think of a barista who keeps a fresh pot of coffee ready. When you order, you don't wait for a brand-new pot — you get a cup from the pot that's already there (instant, maybe slightly old). Meanwhile, the barista starts brewing a fresh pot in the background, so your <em>next</em> cup is hotter. That is stale-while-revalidate: serve the stale cup now, brew the fresh one for later.</div><p>The mechanics: you call <code>useSWR(key, fetcher)</code>. The <code>key</code> identifies the data (e.g. <code>/api/user/3</code>); the <code>fetcher</code> is any function that returns a promise. SWR checks its cache for that key. If something's there, it returns it <em>immediately</em>. Then it runs the fetcher to get fresh data, updates the cache, and re-renders the component with the new value. Because the cache is keyed and shared, ten components asking for the same key trigger <em>one</em> network request — that's request deduplication.</p><p>SWR also revalidates on triggers that signal your data might be old: when the browser tab regains focus, when the network reconnects, or on a polling interval you set. You can also call <code>mutate()</code> to optimistically update the cache before the server confirms — making the UI feel instant.</p><div class=\"key\">The big mental shift: <strong>server data is not \"state\" you own and mutate</strong> — it's a remote source of truth you're caching locally. SWR (and React Query / RTK Query) manage that cache for you, so you stop writing reducers for things like \"is it loading\" and \"did it error\" and let the library synchronize.</div>","related":["e2e","cra"]}
});
