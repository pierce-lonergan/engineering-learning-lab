/* Frontend jargon & acronyms — clickable reference (FE-CSSARCH, auto-assembled) */
Object.assign(GLOSSARY_DATA, {
  "bem": {"term":"BEM","group":"FE-CSSARCH","aliases":["Block Element Modifier","Block, Element, Modifier","block__element--modifier"],"eli5":"A naming convention for CSS classes that flatly encodes a component (block), its parts (elements), and its variants (modifiers) into the class name itself.","memory":"BEM = Block __Element --Modifier: the double-underscore and double-dash literally spell the structure.","full":"<p><strong>BEM (Block, Element, Modifier)</strong> is a CSS class-naming methodology that structures every class name to convey its role: a <em>block</em> is a standalone component (<code>.card</code>), an <em>element</em> is a part that only makes sense inside that block (<code>.card__title</code>), and a <em>modifier</em> is a variant flag (<code>.card--featured</code>). The signature syntax — <code>block__element--modifier</code> — uses double underscores before elements and double hyphens before modifiers.</p><p>BEM originated at the Russian search company <strong>Yandex</strong> around 2009–2010 as part of a larger frontend component methodology, and the naming convention became its most widely adopted piece. It spread across the industry during the 2010s as a pragmatic answer to CSS's lack of native scoping.</p><p>Its purpose is to keep stylesheets <em>flat and predictable</em>: classes are nearly all single-class selectors, so <strong>specificity</strong> stays low and uniform (no nested-selector arms races, no <code>!important</code> wars), and names are human-readable and self-documenting. BEM still has real value today — especially in plain CSS, design systems, and utility-light codebases — but it has trade-offs: names get verbose, and it offers <em>no build-time scoping guarantee</em>, so collisions are prevented only by team discipline. Tools like CSS Modules, scoped styles, and CSS-in-JS later provided automatic scoping that BEM achieves by convention alone.</p>","worked":"<p>A card component in BEM — block, two elements, one modifier:</p><p><code>&lt;article class=\"card card--featured\"&gt;</code><br><code>&nbsp;&nbsp;&lt;h2 class=\"card__title\"&gt;Title&lt;/h2&gt;</code><br><code>&nbsp;&nbsp;&lt;p class=\"card__body\"&gt;…&lt;/p&gt;</code><br><code>&lt;/article&gt;</code></p><p><code>.card { }            /* block */</code><br><code>.card__title { }     /* element — part of card */</code><br><code>.card--featured { }  /* modifier — variant of card */</code></p><p>Every selector is a single class, so specificity stays flat at 0-1-0.</p>","teach":"<p>At its core, BEM is just <em>a disciplined way to name CSS classes so the name tells you exactly what the thing is and where it belongs.</em></p><p>The problem it solves: plain CSS has no scoping. Write <code>.title { color: red }</code> and it leaks onto every <code>.title</code> on the page. Developers historically fought this with deeply nested selectors (<code>.card .header .title</code>), which spikes specificity and starts wars you \"win\" with <code>!important</code>. BEM sidesteps all of that.</p><ul><li><strong>Block</strong> — a reusable, standalone component: <code>.menu</code>, <code>.card</code>, <code>.search-form</code>.</li><li><strong>Element</strong> — a piece that has no meaning outside its block, joined with <code>__</code>: <code>.menu__item</code>, <code>.card__title</code>.</li><li><strong>Modifier</strong> — a flag for a variant or state, joined with <code>--</code>: <code>.menu__item--active</code>, <code>.card--featured</code>.</li></ul><div class=\"analogy\">Think of BEM like the labeling system in a hardware store. The <em>block</em> is the aisle (\"Plumbing\"), the <em>element</em> is the shelf within it (\"Plumbing → Valves\"), and the <em>modifier</em> is the variant tag (\"Valves → ½-inch, brass\"). Anyone reading the label knows instantly what it is and where it lives — and no two unrelated parts accidentally share a bin, because the full path is baked into the name.</div><p>The payoff is <strong>flat specificity</strong>: because almost everything is a single class, every rule has the same low weight, so override order is just source order — no specificity arithmetic. The cost is verbosity (<code>card__media-gallery__thumbnail--selected</code> is a mouthful) and the fact that uniqueness is enforced by <em>convention</em>, not by the build — two developers can still collide if they pick the same block name.</p><div class=\"key\">BEM = Block (component) + Element (part, <code>__</code>) + Modifier (variant, <code>--</code>). It buys flat, low, predictable specificity and self-documenting markup through naming discipline alone — no tooling required, but also no automatic scoping. Modern alternatives (CSS Modules, scoped styles) give that scoping for free; BEM remains a solid choice for plain-CSS and design-system work.</div>","related":["specificity"]}
});
