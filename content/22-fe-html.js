/* HTML & Semantics (FE-HTML) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"FE-HTML","front":"What actually determines an element's role in the accessibility tree, and why does the right tag matter more than ARIA?","back":"<p>The browser builds the <strong>accessibility tree</strong> from the DOM + CSS, mapping each exposed node to a <strong>role</strong>, <strong>name</strong>, <strong>state</strong>, and <strong>properties</strong>, then surfaces it to assistive tech via platform APIs (UIA on Windows, AX on macOS, AT-SPI on Linux).</p><ul><li><strong>Native semantics win</strong>: a <code>&lt;button&gt;</code> ships role=button, focusability, Enter/Space activation, and a disabled state for <em>free</em>.</li><li>A <code>&lt;div role=\"button\"&gt;</code> gets the role <em>label</em> but none of the behavior — you must add <code>tabindex=\"0\"</code>, keydown handlers for Enter and Space, and <code>aria-disabled</code> yourself.</li></ul><p><strong>First Rule of ARIA</strong>: don't use ARIA if a native element with the needed semantics exists. ARIA changes only the a11y tree — never behavior, focus, or styling — and a wrong/over-specified role silently overrides the correct native one.</p>"},
  {"sec":"FE-HTML","front":"<section> vs <div>: when does <section> earn its place, and what's the gotcha?","back":"<p>Use <code>&lt;section&gt;</code> for a <strong>thematic grouping of content</strong> that belongs in the document outline — typically one that has (or could have) a heading. Use <code>&lt;div&gt;</code> for pure styling/layout grouping with no semantic meaning.</p><ul><li><strong>Gotcha</strong>: <code>&lt;section&gt;</code> only exposes the <code>region</code> landmark role <em>when it has an accessible name</em> (via <code>aria-labelledby</code> or <code>aria-label</code>). A nameless <code>&lt;section&gt;</code> has a generic role and is <em>not</em> a landmark — so it's no better than a div for AT navigation.</li><li>The old HTML5 <em>outline algorithm</em> (sectioning elements auto-scoping headings) was <strong>never implemented</strong> by any browser and was removed from the spec.</li></ul><p><em>Rule of thumb</em>: if you wouldn't give it a heading, it's probably a <code>&lt;div&gt;</code>.</p>"},
  {"sec":"FE-HTML","front":"<article> vs <section> — what's the distinguishing test?","back":"<p>An <code>&lt;article&gt;</code> is a <strong>self-contained, independently distributable</strong> unit — it would still make sense syndicated to a feed or shown out of context (a blog post, a product card, a comment, a forum post).</p><ul><li><strong>Test</strong>: <em>Could this stand alone?</em> Yes → <code>&lt;article&gt;</code>. Is it a thematic chapter of a larger whole? → <code>&lt;section&gt;</code>.</li><li>They nest both ways: a comment thread can be a <code>&lt;section&gt;</code> of comment <code>&lt;article&gt;</code>s inside the post <code>&lt;article&gt;</code>.</li></ul><p><code>&lt;article&gt;</code> maps to the <strong>article</strong> role and is a sectioning element with its own header/footer scope, but it is <em>not</em> a landmark — it won't appear in the AT landmark rotor.</p>"},
  {"sec":"FE-HTML","front":"Why does heading hierarchy (h1–h6) matter, and is multiple <h1> okay in 2026?","back":"<p>Screen-reader users navigate primarily by <strong>headings</strong> (the rotor / heading list). Headings should form a logical, <em>sequential</em> outline — don't skip levels (h2 → h4), because the gap reads as missing structure.</p><ul><li><strong>Multiple <code>&lt;h1&gt;</code></strong>: the outline algorithm that was supposed to scope them per-section was never implemented, so multiple h1s are <em>not</em> auto-leveled. Current guidance and most audits favor <strong>one <code>&lt;h1&gt;</code> per page</strong> as the document title, then sequential h2–h6. (Multiple h1s are valid HTML and not a hard WCAG failure, but they flatten the perceived structure.)</li><li>Visual size is CSS — <em>never</em> pick a heading level for its font size. Use the correct level and style it.</li></ul><p>Broken heading order is a common and damaging finding under WCAG 1.3.1 (Info and Relationships).</p>"},
  {"sec":"FE-HTML","front":"How does a native <label> associate with its control, and which approach is more robust?","back":"<p>Two ways to bind a <code>&lt;label&gt;</code> to a form control:</p><ul><li><strong>Explicit</strong>: <code>&lt;label for=\"email\"&gt;</code> matched to <code>&lt;input id=\"email\"&gt;</code>. The <code>for</code> value must equal the control's <code>id</code> exactly; this is the <em>most robust</em> and works even when DOM nesting isn't possible.</li><li><strong>Implicit</strong>: wrap the control — <code>&lt;label&gt;Email &lt;input&gt;&lt;/label&gt;</code>. No id needed, but some legacy AT historically mis-handled it, and it can't span DOM distance.</li></ul><p>A correct label gives the input its <strong>accessible name</strong> <em>and</em> expands the click/tap target (clicking the label focuses or toggles the control). <code>placeholder</code> is <em>not</em> a label — it vanishes on input and often fails contrast. Prefer explicit <code>for</code> in component libraries.</p>"},
  {"sec":"FE-HTML","front":"What does the Constraint Validation API give you, and how do you take over validation UI?","back":"<p>Built-in form validation exposes a JS API on every submittable control:</p><ul><li><code>el.validity</code> — a <code>ValidityState</code> object with flags like <code>valueMissing</code>, <code>typeMismatch</code>, <code>patternMismatch</code>, <code>tooLong</code>, <code>tooShort</code>, <code>rangeOverflow</code>, <code>customError</code>.</li><li><code>el.checkValidity()</code> returns a boolean and fires an <code>invalid</code> event on each invalid control; <code>el.reportValidity()</code> does the same <em>and</em> shows the native bubble + focuses the first invalid control.</li><li><code>el.setCustomValidity(msg)</code> sets a custom error (non-empty string ⇒ invalid, sets the <code>customError</code> flag); clear it with <code>setCustomValidity('')</code>.</li></ul><p>Add <code>novalidate</code> on the <code>&lt;form&gt;</code> to <strong>suppress native bubbles and block validation-gated submission</strong> while keeping the <code>validity</code> API and <code>:invalid</code> live — the standard pattern for custom error rendering. Constraints (<code>required</code>, <code>pattern</code>, <code>min</code>/<code>max</code>, <code>type=email</code>) still drive both.</p>"},
  {"sec":"FE-HTML","front":"srcset + sizes: what's the difference between width descriptors (w) and density descriptors (x)?","back":"<p>Both drive <strong>resolution switching</strong> on <code>&lt;img&gt;</code> — same image, different pixel dimensions; the browser picks one source.</p><ul><li><strong>x descriptors</strong> (<code>img@2x.jpg 2x</code>): for <em>fixed-size</em> images. The browser chooses by device pixel ratio. No <code>sizes</code> needed. Omitting a descriptor implies <code>1x</code>.</li><li><strong>w descriptors</strong> (<code>img-800.jpg 800w</code>): for <em>fluid</em> images, where <code>w</code> states each source's intrinsic pixel width. You MUST pair with <code>sizes</code>, which tells the browser the image's <em>rendered layout width</em> at each media condition (e.g. <code>sizes=\"(max-width:600px) 100vw, 50vw\"</code>) so it can compute the needed pixels and choose a file.</li></ul><p><strong>Gotcha</strong>: <code>sizes</code> describes <em>layout</em> width, not a file's pixel width — getting it wrong makes the browser over- or under-fetch. The browser may also pick a larger source than strictly needed (e.g. if it's already cached) — selection is a UA hint, not a guarantee.</p>"},
  {"sec":"FE-HTML","front":"When do you reach for <picture> instead of srcset?","back":"<p><code>srcset</code> on a plain <code>&lt;img&gt;</code> handles <strong>resolution switching</strong> (same image, scaled), with the browser making the final choice. <code>&lt;picture&gt;</code> handles <strong>art direction</strong> and <strong>format negotiation</strong> — <em>different</em> images or formats, with deterministic selection.</p><ul><li><strong>Art direction</strong>: crop/compose differently per viewport via <code>&lt;source media=\"(max-width:600px)\"&gt;</code> (e.g. tight portrait on mobile, wide landscape on desktop).</li><li><strong>Format fallback</strong>: <code>&lt;source type=\"image/avif\"&gt;</code> then <code>&lt;source type=\"image/webp\"&gt;</code>, with a JPEG/PNG <code>&lt;img&gt;</code> fallback. The browser picks the <em>first <code>&lt;source&gt;</code> whose <code>type</code>/<code>media</code> it matches</em> — order matters.</li></ul><p>The <code>&lt;img&gt;</code> inside <code>&lt;picture&gt;</code> is <strong>required</strong> — it's the ultimate fallback and the element that actually renders, carries <code>alt</code>, and receives sizing/styling. <code>srcset</code>/<code>sizes</code> can also live on the inner <code>&lt;img&gt;</code> and each <code>&lt;source&gt;</code>.</p>"},
  {"sec":"FE-HTML","front":"loading=\"lazy\" — how does it work and what's the one critical place NOT to use it?","back":"<p><code>loading=\"lazy\"</code> on <code>&lt;img&gt;</code> and <code>&lt;iframe&gt;</code> defers fetching until the element is near the viewport (browser-managed distance-from-viewport heuristic) — no IntersectionObserver needed.</p><ul><li><strong>Never lazy-load above-the-fold / LCP images.</strong> Lazy-loading the hero delays the <strong>LCP</strong> (Largest Contentful Paint) Core Web Vital, because the fetch can't start until layout determines the image is near the viewport. Instead use <code>fetchpriority=\"high\"</code> (and/or <code>&lt;link rel=\"preload\" as=\"image\"&gt;</code>) on the LCP image.</li><li>Always set <code>width</code>/<code>height</code> (or <code>aspect-ratio</code>) on lazy images to reserve space and avoid <strong>CLS</strong> (Cumulative Layout Shift).</li></ul><p><code>loading=\"eager\"</code> is the default. Note lazy loading is a hint — print and some preload paths fetch eagerly anyway.</p>"},
  {"sec":"FE-HTML","front":"Native <dialog>: what does showModal() give you that a div-modal must reimplement?","back":"<p>Open a modal with <code>dialog.showModal()</code> (vs <code>dialog.show()</code> for non-modal). The platform then provides, <em>for free</em>:</p><ul><li>The <strong>top layer</strong> — renders above all stacking contexts, immune to ancestor <code>z-index</code>/<code>overflow</code>/<code>transform</code> clipping.</li><li>A <strong>::backdrop</strong> pseudo-element to style the scrim.</li><li><strong>Focus moved into the dialog</strong>, focus <strong>trapped</strong> inside it, and <code>Esc</code> to dismiss (fires a <code>cancel</code> event, then <code>close</code>).</li><li>The rest of the page made <strong>inert</strong> — background is unfocusable and hidden from AT.</li></ul><p><strong>Gotchas</strong>: <code>dialog.show()</code> does <em>not</em> trap focus, add a backdrop, or inert the page. Return data via <code>&lt;form method=\"dialog\"&gt;</code> → <code>dialog.returnValue</code>. Initial focus goes to the first <code>autofocus</code>/focusable element; set <code>autofocus</code> deliberately to avoid focusing a destructive button.</p>"},
  {"sec":"FE-HTML","front":"<details>/<summary> and the 2026 exclusive-accordion + find-in-page behavior","back":"<p><code>&lt;details&gt;</code> is a native disclosure widget: <code>&lt;summary&gt;</code> is the always-visible toggle (exposed as a button, keyboard-operable), and the rest is hidden until the <code>open</code> attribute is present. Toggling fires a <code>toggle</code> event.</p><ul><li><strong>Exclusive accordion</strong>: give multiple <code>&lt;details&gt;</code> the same <code>name</code> attribute and only one stays open at a time — native, no JS (shipped across browsers in 2024).</li><li><strong>Find-in-page</strong>: modern browsers automatically search and auto-expand collapsed <code>&lt;details&gt;</code> content on a Ctrl+F match. The related <code>hidden=\"until-found\"</code> attribute brings that same auto-reveal to <em>arbitrary</em> hidden content, not just details.</li></ul><p><strong>Gotcha</strong>: <code>&lt;summary&gt;</code> should be the <em>first child</em>; without one the browser renders a default 'Details' label. Animating open/close needs CSS work (e.g. <code>::details-content</code> + <code>interpolate-size: allow-keywords</code> to animate to <code>auto</code> height).</p>"},
  {"sec":"FE-HTML","front":"iframe sandbox: what does the bare sandbox attribute do, and what's the security trap when re-granting?","back":"<p><code>&lt;iframe sandbox&gt;</code> with <em>no value</em> applies the maximum restrictions: no scripts, no form submission, no popups, a unique <strong>opaque origin</strong> (so no same-origin storage/cookies/access to its real origin), no top-level navigation, no plugins, no auto-features.</p><p>You re-grant capabilities token by token: <code>sandbox=\"allow-scripts allow-forms\"</code>.</p><ul><li><strong>The trap</strong>: granting <code>allow-scripts</code> <em>and</em> <code>allow-same-origin</code> to a frame whose content is served from the <em>embedder's own origin</em> lets the frame's script reach the parent DOM and <strong>remove its own sandbox attribute</strong>, then reload — defeating the sandbox. The browser console even warns about this combo. Reserve that pairing for genuinely cross-origin content.</li></ul><p>Pair with <code>allow=\"…\"</code> (Permissions Policy for camera/geolocation/etc.), <code>referrerpolicy</code>, and <code>loading=\"lazy\"</code>. <code>credentialless</code> further isolates third-party frame storage/cookies.</p>"},
  {"sec":"FE-HTML","front":"Custom element lifecycle: name the four callbacks and the attributeChangedCallback gotcha.","back":"<p>An autonomous custom element extends <code>HTMLElement</code> and is registered via <code>customElements.define('my-el', MyEl)</code> (the name must contain a hyphen). Lifecycle reactions:</p><ul><li><code>constructor()</code> — set up state/shadow root; <strong>don't</strong> inspect attributes or children or touch the DOM (the element may still be upgrading).</li><li><code>connectedCallback()</code> — inserted into a document; do setup, add listeners, read attributes. Can fire multiple times (move/re-insertion).</li><li><code>disconnectedCallback()</code> — removed; tear down listeners/observers.</li><li><code>attributeChangedCallback(name, oldVal, newVal)</code> — fires <em>only</em> for attributes listed in the static <code>observedAttributes</code> getter.</li></ul><p><strong>Gotcha</strong>: omit <code>static get observedAttributes()</code> and <code>attributeChangedCallback</code> <em>never runs</em>. Attributes already present when the element upgrades <em>do</em> trigger it once (old value <code>null</code>). There's also <code>adoptedCallback</code> for cross-document adoption.</p>"},
  {"sec":"FE-HTML","front":"Shadow DOM: open vs closed, and what does mode actually buy you for encapsulation?","back":"<p><code>this.attachShadow({ mode })</code> creates a <strong>shadow root</strong> that scopes DOM and CSS — styles defined inside don't leak out, most outside styles don't leak in, and page <code>querySelector</code> can't traverse into it.</p><ul><li><strong>open</strong>: <code>el.shadowRoot</code> returns the root — page JS can reach inside. The common choice.</li><li><strong>closed</strong>: <code>el.shadowRoot</code> is <code>null</code>; only the code that called <code>attachShadow</code> holds the reference. <em>Not</em> a security boundary — the component still captured the root, and it's bypassable, so it mostly signals 'hands off'.</li></ul><p><strong>Style hooks</strong>: expose <code>::part()</code> for targeted external styling, and CSS custom properties (which <em>inherit through</em> shadow boundaries). <code>:host</code> / <code>:host()</code> style the element itself; <code>::slotted()</code> styles top-level slotted light-DOM nodes; inherited properties (color, font) still cross the boundary.</p>"},
  {"sec":"FE-HTML","front":"<template> and <slot>: how does slotting differ from moving DOM, and what is the flattened tree?","back":"<p><code>&lt;template&gt;</code> holds <strong>inert, un-rendered</strong> DOM — parsed but not in the rendered tree; you clone <code>tmpl.content</code> (a DocumentFragment) to instantiate. Scripts inside don't run and images don't load until cloned and inserted.</p><p><code>&lt;slot&gt;</code> is a placeholder in the <em>shadow</em> tree where <strong>light-DOM children</strong> (the element's real content) are projected.</p><ul><li>Slotted nodes are <em>not moved</em> — they remain in the light DOM (and in document order) and are <strong>rendered in place</strong> of the slot. The composited result the user sees is the <strong>flattened tree</strong> (shadow tree with light-DOM projected into slots).</li><li>Named slots: <code>&lt;slot name=\"title\"&gt;</code> ↔ child with <code>slot=\"title\"</code>. An unnamed default slot catches the rest; <code>slotchange</code> fires when assigned nodes change.</li></ul><p>Style projected content with <code>::slotted(selector)</code> — it matches only <em>top-level</em> assigned nodes, not their descendants.</p>"},
  {"sec":"FE-HTML","front":"data-* attributes: how do you read them, and what are they NOT for?","back":"<p>Any <code>data-*</code> attribute is valid, custom HTML and surfaces in JS via the <strong>DOMStringMap</strong> at <code>el.dataset</code> with camelCased keys: <code>data-user-id=\"7\"</code> → <code>el.dataset.userId === \"7\"</code> (always a <em>string</em>). They're also targetable in CSS via attribute selectors (<code>[data-state=\"open\"]</code>) and readable via the CSS <code>attr()</code> function.</p><ul><li><strong>For</strong>: passing non-sensitive app data from server render to JS, CSS state hooks, and test selectors (<code>data-testid</code>).</li><li><strong>NOT for</strong>: data that has a semantic home — use <code>&lt;time datetime&gt;</code>, an input's <code>value</code>, <code>aria-*</code>, etc. instead. Don't encode accessibility info in <code>data-*</code>; AT ignores it.</li></ul><p>Writing <code>el.dataset.foo</code> reflects to the live <code>data-foo</code> attribute and vice-versa. Hyphenated keys map to camelCase (<code>data-foo-bar</code> ↔ <code>dataset.fooBar</code>).</p>"},
  {"sec":"FE-HTML","front":"<head> metadata that breaks mobile/SEO if missing: charset, viewport, and Open Graph.","back":"<ul><li><code>&lt;meta charset=\"utf-8\"&gt;</code> — must appear within the <strong>first 1024 bytes</strong> of the document, before any non-ASCII content, or the browser may re-parse with the wrong encoding (mojibake).</li><li><code>&lt;meta name=\"viewport\" content=\"width=device-width, initial-scale=1\"&gt;</code> — without it mobile browsers assume a ~980px layout viewport and shrink-to-fit, breaking responsive layouts. <em>Don't</em> add <code>user-scalable=no</code> / <code>maximum-scale=1</code> — it blocks pinch-zoom (WCAG 1.4.4 / 1.4.10 concern; modern browsers may ignore it anyway).</li><li><strong>Open Graph</strong> (<code>og:title</code>, <code>og:description</code>, <code>og:image</code>) drives link-preview unfurls on social/chat. OG tags use the <code>property</code> attribute (<code>&lt;meta property=\"og:image\"&gt;</code>), distinct from <code>&lt;meta name&gt;</code> SEO tags like <code>description</code>.</li></ul><p><code>&lt;title&gt;</code> remains the single strongest on-page SEO and a11y signal — it's the tab label and the AT page name.</p>"}
);

Object.assign(GLOSSARY_DATA, {
  "accessibility-tree": {"term":"Accessibility Tree","group":"FE-HTML","eli5":"A parallel version of the page that the browser hands to screen readers, describing what each thing is and does.","memory":"Role + Name + State = how a screen reader 'sees' it.","full":"<p>The <strong>accessibility tree</strong> is a representation the browser derives from the DOM and CSS, exposing each relevant node's <strong>role</strong> (button, link, heading…), <strong>accessible name</strong> (computed from associated labels/text/aria via the accname algorithm), <strong>state</strong> (checked, expanded, disabled), and <strong>properties</strong>. The browser feeds this to the OS accessibility APIs (UIA on Windows, AX on macOS, AT-SPI on Linux), which assistive technologies consume.</p><p>Choosing the right <em>native</em> element populates this tree correctly and for free. ARIA attributes <em>patch</em> the tree but change no behavior, so a wrong role produces a control that announces incorrectly or doesn't work with the keyboard.</p>","trap":"Thinking ARIA 'adds accessibility'. ARIA only edits the a11y tree; it never adds keyboard behavior, focus, or styling — and a bad role silently overrides correct native semantics.","worked":"<button> → role=button, focusable, Enter/Space activate, disabled state — all automatic. <div role=\"button\"> → you must add tabindex=0, keydown for Enter+Space, and aria-disabled by hand.","related":["semantic-elements","document-outline","label-association"]},
  "semantic-elements": {"term":"Semantic Elements","group":"FE-HTML","eli5":"HTML tags whose name tells you (and the browser) what the content means, not just how it looks.","memory":"Meaning over markup: pick the tag for what it IS.","full":"<p><strong>Semantic elements</strong> (<code>&lt;article&gt;</code>, <code>&lt;section&gt;</code>, <code>&lt;nav&gt;</code>, <code>&lt;main&gt;</code>, <code>&lt;aside&gt;</code>, <code>&lt;header&gt;</code>, <code>&lt;footer&gt;</code>, <code>&lt;figure&gt;</code>, <code>&lt;time&gt;</code>) convey the role and structure of content. Several map to <strong>landmark roles</strong> (<code>nav</code>→navigation, <code>main</code>→main, <code>aside</code>→complementary) that power AT landmark navigation; they also improve SEO and make markup self-documenting.</p><p><code>&lt;div&gt;</code> and <code>&lt;span&gt;</code> are the <em>semantically neutral</em> fallbacks — correct for pure styling/layout grouping where no other element fits. The senior judgment is knowing when a div is genuinely right versus a missed semantic opportunity.</p>","trap":"Reaching for <section>/<article> as generic boxes. A <section> without an accessible name is no more useful to AT than a div, and misused semantics mislead screen-reader users.","worked":"<nav aria-label=\"Primary\"> for the main menu; <main> for the unique page content; <div class=\"flex-row\"> purely to lay out two columns.","related":["accessibility-tree","document-outline"]},
  "document-outline": {"term":"Document Outline & Heading Hierarchy","group":"FE-HTML","eli5":"The nested table-of-contents a screen reader builds from your h1–h6, used to jump around the page.","memory":"Headings are signposts — number them in order, never skip.","full":"<p>Screen-reader users navigate by <strong>headings</strong>. A good outline uses a single <code>&lt;h1&gt;</code> as the page title, then sequential <code>&lt;h2&gt;</code>–<code>&lt;h6&gt;</code> with no skipped levels. Heading <em>level</em> is structure; visual size is CSS — never choose a level for its font size.</p><p>The HTML5 <em>outline algorithm</em> that was meant to auto-scope multiple <code>&lt;h1&gt;</code>s per sectioning element was never implemented by any browser and was removed from the spec, which is why one-h1-per-page is the current best practice. (Multiple h1s are still valid HTML — just not auto-leveled.)</p>","trap":"Believing sectioning elements 'reset' heading levels so you can sprinkle multiple h1s freely. No browser does this; auditors flag skipped levels under WCAG 1.3.1.","worked":"h1 Page title → h2 Section → h3 Subsection → h2 Next section. Jumping h2→h4 leaves a screen-reader user wondering what level-3 content they missed.","related":["semantic-elements","accessibility-tree"]},
  "constraint-validation-api": {"term":"Constraint Validation API","group":"FE-HTML","eli5":"The browser's built-in form-checking that you can read from JavaScript and style with CSS.","memory":"validity flags + reportValidity() + setCustomValidity() = own the errors.","full":"<p>HTML form constraints (<code>required</code>, <code>type=email</code>, <code>pattern</code>, <code>min</code>/<code>max</code>, <code>minlength</code>/<code>maxlength</code>) drive the <strong>Constraint Validation API</strong>. Each control exposes <code>validity</code> (a <code>ValidityState</code> of boolean flags), <code>validationMessage</code>, <code>checkValidity()</code> (returns bool + fires <code>invalid</code>), <code>reportValidity()</code> (also shows native UI), and <code>setCustomValidity(msg)</code> for app-specific errors. The CSS pseudo-classes <code>:valid</code>/<code>:invalid</code>/<code>:user-invalid</code> reflect state (<code>:user-invalid</code> only after interaction).</p><p>Adding <code>novalidate</code> to the <code>&lt;form&gt;</code> suppresses native error bubbles and the validation gate on submit, while keeping the JS API live — the standard way to render your own accessible error messages.</p>","trap":"Assuming novalidate disables validation entirely. It only hides native bubbles and the submit gate; validity flags and :invalid still work — that's exactly why it's used for custom UIs.","worked":"input.setCustomValidity('Passwords must match'); // marks invalid + sets validationMessage. Call input.setCustomValidity('') to clear and re-validate.","related":["label-association"]},
  "label-association": {"term":"Label Association","group":"FE-HTML","eli5":"Connecting a form field's visible text to the field so screen readers announce it and clicks focus it.","memory":"for matches id — or wrap the input.","full":"<p>A <code>&lt;label&gt;</code> gives a control its <strong>accessible name</strong> and extends the clickable hit area. <strong>Explicit</strong> association uses <code>&lt;label for=\"x\"&gt;</code> + <code>&lt;input id=\"x\"&gt;</code> (most robust, works across DOM distance). <strong>Implicit</strong> association wraps the control inside the label.</p><p>A <code>placeholder</code> is <em>not</em> a label: it disappears once the user types, frequently fails color contrast, and is an unreliable accessible name. Every interactive form control needs a programmatic name via a label, <code>aria-label</code>, or <code>aria-labelledby</code>.</p>","trap":"Using placeholder text as the only label. It's not a label, vanishes on input, and fails WCAG — a common audit ding.","worked":"<label for=\"email\">Email</label><input id=\"email\" type=\"email\" required> — clicking the word 'Email' focuses the field and AT announces 'Email, edit text, required'.","related":["constraint-validation-api","accessibility-tree"]},
  "responsive-images": {"term":"Responsive Images (srcset/sizes/picture)","group":"FE-HTML","eli5":"Markup that lets the browser download the right-sized or right-format image for each screen.","memory":"x = fixed size by DPR; w = fluid, needs sizes; picture = different image/format.","full":"<p>On a plain <code>&lt;img&gt;</code>, <code>srcset</code> with <strong>density (x)</strong> descriptors switches by device pixel ratio for fixed-size images, while <strong>width (w)</strong> descriptors plus a <code>sizes</code> attribute let the browser pick by rendered CSS layout width for fluid images — this is <em>resolution switching</em> (same image, different pixels), and the UA makes the final choice.</p><p><code>&lt;picture&gt;</code> with multiple <code>&lt;source&gt;</code>s handles <strong>art direction</strong> (different crops per <code>media</code>) and <strong>format negotiation</strong> (AVIF→WebP→JPEG via <code>type</code>), selecting the first matching source. The nested <code>&lt;img&gt;</code> is mandatory: it's the fallback, the rendered element, and carries <code>alt</code>.</p>","trap":"Thinking sizes is the file's pixel width. sizes is the image's LAYOUT width at each breakpoint, evaluated before layout — get it wrong and the browser over/under-fetches.","worked":"<img srcset=\"s.jpg 400w, l.jpg 800w\" sizes=\"(max-width:600px) 100vw, 50vw\" src=\"l.jpg\" alt=\"…\"> — on a 375px phone it fetches the 400w file.","related":["lazy-loading","head-metadata"]},
  "native-dialog": {"term":"Native <dialog> / showModal()","group":"FE-HTML","eli5":"A built-in modal box that handles focus, the backdrop, and Esc for you.","memory":"showModal() = top layer + focus trap + ::backdrop + inert page.","full":"<p><code>dialog.showModal()</code> promotes the dialog to the browser's <strong>top layer</strong> (above every stacking context, unclipped by ancestor overflow/transform), renders a styleable <code>::backdrop</code>, moves focus inside and <strong>traps</strong> it, makes the rest of the page <code>inert</code>, and closes on <code>Esc</code> (firing <code>cancel</code> then <code>close</code>). <code>&lt;form method=\"dialog\"&gt;</code> closes it and sets <code>returnValue</code>.</p><p><code>dialog.show()</code> opens it non-modally and provides <em>none</em> of the focus-trapping, inerting, or backdrop, so it's not a substitute for a true modal.</p>","trap":"Using dialog.show() expecting modal behavior, or relying on default focus — set autofocus deliberately so focus doesn't land on a destructive button.","worked":"<dialog id=\"d\"><form method=\"dialog\"><button value=\"ok\">OK</button></form></dialog>; d.showModal(); d.addEventListener('close',()=>console.log(d.returnValue)).","related":["semantic-elements"]},
  "shadow-dom": {"term":"Shadow DOM","group":"FE-HTML","eli5":"A private mini-DOM inside a custom element whose styles and markup don't leak in or out.","memory":"Encapsulation, not security — open lets you in, closed just hides the door.","full":"<p><strong>Shadow DOM</strong> scopes a subtree's markup and CSS to a component. <code>attachShadow({mode:'open'})</code> exposes <code>el.shadowRoot</code>; <code>'closed'</code> returns <code>null</code> to outside code (the component still holds its own reference). Styles inside don't leak out and most outside styles don't leak in, though <em>inherited</em> properties (color, font) and CSS custom properties cross the boundary. The visible <strong>flattened tree</strong> combines the shadow tree with light-DOM projected through <code>&lt;slot&gt;</code>.</p><p>Controlled style hooks: <code>::part()</code> for externally styling exposed internals, CSS custom properties, <code>:host</code>/<code>:host()</code> for the element itself, and <code>::slotted()</code> for top-level projected nodes.</p>","trap":"Treating closed shadow DOM as a security boundary. It's a soft encapsulation signal, bypassable; never rely on it to hide secrets.","worked":"this.attachShadow({mode:'open'}).innerHTML = `<style>:host{display:block}</style><slot></slot>`; outside CSS can only reach in via ::part, custom properties, or inheritance.","related":["custom-elements","template-slot"]},
  "custom-elements": {"term":"Custom Elements & Lifecycle","group":"FE-HTML","eli5":"Your own HTML tags, with hooks that run when they're added, removed, or change attributes.","memory":"constructor → connected → attributeChanged → disconnected (and observedAttributes gates the third).","full":"<p>A custom element extends <code>HTMLElement</code> and is registered with <code>customElements.define('x-y', cls)</code> (name must contain a hyphen). Lifecycle reactions: <code>constructor</code> (set up state/shadow, touch nothing external), <code>connectedCallback</code> (DOM-inserted — do work, may fire repeatedly), <code>disconnectedCallback</code> (clean up), <code>attributeChangedCallback</code> (fires only for attributes named in <code>static get observedAttributes()</code>), and <code>adoptedCallback</code> (moved between documents).</p><p>Together with Shadow DOM and <code>&lt;template&gt;</code>, custom elements make up <strong>Web Components</strong> — framework-agnostic, reusable UI primitives.</p>","trap":"Reading attributes/children in the constructor, or omitting observedAttributes and wondering why attributeChangedCallback never fires.","worked":"static get observedAttributes(){return['open']} ; attributeChangedCallback(n,o,v){ if(n==='open') this.render(); } — toggling [open] now re-renders.","related":["shadow-dom","template-slot"]},
  "template-slot": {"term":"<template> & <slot>","group":"FE-HTML","eli5":"<template> is inert HTML you stamp out later; <slot> is where a component shows the content you put inside it.","memory":"template = stamp; slot = projection hole.","full":"<p><code>&lt;template&gt;</code> holds parsed-but-inert DOM — scripts don't run, images don't load — until you clone <code>tmpl.content</code> (a DocumentFragment). It's the efficient way to instantiate repeated markup.</p><p><code>&lt;slot&gt;</code> projects an element's <strong>light-DOM children</strong> into its shadow tree. Slotted nodes are <em>not moved</em>; they stay in the light DOM and render at the slot's position, forming the flattened tree. Named slots match <code>slot=\"name\"</code> children; the default (unnamed) slot catches the rest; <code>slotchange</code> fires when assigned nodes change.</p>","trap":"Thinking <slot> moves DOM into the shadow root. It projects in place — the nodes remain light DOM, which is why ::slotted() (not normal shadow selectors) styles them, and only at the top level.","worked":"Shadow: <slot name=\"title\"></slot>. Light: <my-card><h2 slot=\"title\">Hi</h2></my-card> → the h2 renders where the named slot sits.","related":["shadow-dom","custom-elements"]},
  "iframe-sandbox": {"term":"iframe sandbox","group":"FE-HTML","eli5":"An attribute that locks down an embedded page, then lets you re-grant only the capabilities you trust.","memory":"Bare sandbox = deny all; add allow-* tokens back carefully.","full":"<p><code>&lt;iframe sandbox&gt;</code> with no value imposes maximum restrictions: scripts off, form submission off, popups off, a unique <strong>opaque origin</strong> (no same-origin storage/cookies), no top-level navigation, no plugins. You re-enable capabilities with space-separated tokens (<code>allow-scripts</code>, <code>allow-forms</code>, <code>allow-popups</code>, <code>allow-same-origin</code>, <code>allow-top-navigation</code>, …).</p><p>Pair it with <code>allow=\"…\"</code> (Permissions Policy for camera/geolocation/etc.), <code>referrerpolicy</code>, <code>credentialless</code> for storage/cookie isolation of third-party frames, and <code>loading=\"lazy\"</code>.</p>","trap":"Setting both allow-scripts and allow-same-origin on same-origin framed content — the framed script can then delete its own sandbox attribute and reload to escape entirely.","worked":"<iframe sandbox=\"allow-scripts\" src=\"https://untrusted.example/widget\"> runs JS but is an opaque origin with no storage/cookies and can't navigate the top window.","related":["lazy-loading","head-metadata"]},
  "head-metadata": {"term":"<head> Metadata (charset/viewport/OG)","group":"FE-HTML","eli5":"Invisible tags in <head> that set encoding, mobile scaling, and how links preview when shared.","memory":"charset first 1KB, viewport for mobile, og:* for share cards.","full":"<p>Key <code>&lt;head&gt;</code> tags: <code>&lt;meta charset=\"utf-8\"&gt;</code> must appear within the first 1024 bytes or the browser may re-parse with the wrong encoding. <code>&lt;meta name=\"viewport\" content=\"width=device-width, initial-scale=1\"&gt;</code> is required for responsive mobile rendering — but avoid <code>user-scalable=no</code>/<code>maximum-scale=1</code>, which blocks pinch-zoom (WCAG 1.4.4/1.4.10; modern browsers may ignore it).</p><p><strong>Open Graph</strong> tags (<code>&lt;meta property=\"og:title|og:description|og:image\"&gt;</code>) drive social/chat link previews; note they use <code>property</code>, not <code>name</code>. <code>&lt;title&gt;</code> remains the strongest on-page SEO and AT page-name signal.</p>","trap":"Adding user-scalable=no for a 'cleaner' mobile feel — it disables zoom and fails accessibility audits. Also, og:* uses property, not name.","worked":"<meta property=\"og:image\" content=\"https://site/card.png\"> renders the image in a Slack/iMessage/social link unfurl.","related":["responsive-images","semantic-elements"]},
  "lazy-loading": {"term":"loading=\"lazy\"","group":"FE-HTML","eli5":"Tells the browser to wait until an image or iframe is near the screen before downloading it.","memory":"Lazy below the fold; eager + fetchpriority for the hero.","full":"<p><code>loading=\"lazy\"</code> on <code>&lt;img&gt;</code>/<code>&lt;iframe&gt;</code> defers the fetch until the element approaches the viewport, using the browser's own distance heuristic — no IntersectionObserver required. It cuts initial payload and speeds first load for long, image-heavy pages.</p><p>The critical exception: <em>never</em> lazy-load the above-the-fold <strong>LCP</strong> image, since the deferral starts the fetch only after layout and hurts Largest Contentful Paint. Use <code>fetchpriority=\"high\"</code> and/or <code>&lt;link rel=\"preload\" as=\"image\"&gt;</code> for that one, and always set dimensions/aspect-ratio to avoid CLS.</p>","trap":"Slapping loading=lazy on every image including the hero — it regresses LCP, a Core Web Vital. Lazy is for below-the-fold only; eager is the default.","worked":"<img src=\"hero.jpg\" fetchpriority=\"high\" width=\"1200\" height=\"600\"> for the LCP; <img src=\"thumb.jpg\" loading=\"lazy\" width=\"…\" height=\"…\"> for the footer gallery.","related":["responsive-images","head-metadata"]}
});
