/* OLAP & Dimensional Modeling (LAKE-OLAP) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"LAKE-OLAP","front":"OLTP vs OLAP: the core physical and workload split","back":"<strong>OLTP</strong> = many small, latency-sensitive read/write transactions touching few rows; <strong>row-oriented</strong> storage, heavily <strong>normalized</strong>, tuned for point lookups and single-row mutations. <strong>OLAP</strong> = few large queries scanning millions of rows to aggregate; <strong>column-oriented</strong> storage, <strong>denormalized</strong>, tuned for full-column scans, vectorized execution, and compression. The split is about access pattern, not just SQL dialect."},
  {"sec":"LAKE-OLAP","front":"Why columnar storage wins for OLAP (3 concrete reasons)","back":"<ul><li><strong>I/O pruning:</strong> a scan reads only the columns referenced, not whole rows.</li><li><strong>Compression:</strong> same-type values cluster, so run-length/dictionary/delta encoding gets far higher ratios than row layout.</li><li><strong>Vectorized/SIMD execution:</strong> operators process column batches (typically ~1K-64K values per call), and SIMD registers crunch 8-32 of those values per instruction.</li></ul>This is why Parquet and Iceberg-on-Parquet are columnar; Snowflake micro-partitions are <em>hybrid-columnar</em> (PAX) but get the same scan/compression wins."},
  {"sec":"LAKE-OLAP","front":"Fact table: definition and what belongs in one","back":"A <strong>fact table</strong> records business <em>events/measurements</em> at a defined <strong>grain</strong>. It holds <strong>foreign keys</strong> to dimensions plus numeric <strong>measures</strong> (e.g. <code>quantity</code>, <code>amount</code>). It is long and narrow, grows fastest, and contains almost no descriptive text — context lives in dimensions. Keep it skinny: degenerate dimensions (like an order number) stay inline, but descriptions do not."},
  {"sec":"LAKE-OLAP","front":"What is the \"grain\" of a fact table and why declare it first?","back":"The <strong>grain</strong> is the precise meaning of <em>one row</em> (e.g. \"one row per line item per order\"). In Kimball's four-step design (pick process → declare grain → pick dimensions → pick facts), grain is declared <em>before</em> dimensions or measures: it determines which dimensions can attach and prevents mixing granularities. Mixed grain causes silent double-counting when you <code>SUM</code> measures across rows that mean different things."},
  {"sec":"LAKE-OLAP","front":"Additive vs semi-additive vs non-additive measures","back":"<ul><li><strong>Additive:</strong> safely summed across <em>all</em> dimensions (e.g. revenue, units).</li><li><strong>Semi-additive:</strong> summable across some dimensions but not time — e.g. account <code>balance</code> or <code>inventory_level</code>; you average or take a snapshot over time, not sum.</li><li><strong>Non-additive:</strong> can't be summed at all (ratios, percentages, distinct counts); store the raw numerator/denominator and compute the ratio at query time.</li></ul>"},
  {"sec":"LAKE-OLAP","front":"Fact table types: transaction vs periodic snapshot vs accumulating snapshot","back":"<ul><li><strong>Transaction fact:</strong> one row per event at the moment it happens (most common).</li><li><strong>Periodic snapshot:</strong> one row per entity per regular interval (daily balance, end-of-day inventory) — typically holds semi-additive measures.</li><li><strong>Accumulating snapshot:</strong> one row per process instance whose date columns and measures are <em>updated in place</em> as it moves through milestones (e.g. an order lifecycle).</li></ul>"},
  {"sec":"LAKE-OLAP","front":"Dimension table: definition and shape","back":"A <strong>dimension table</strong> holds the descriptive context you filter, group, and label by — wide and short, with many text/categorical columns (e.g. <code>customer_name</code>, <code>region</code>, <code>product_category</code>). It has a <strong>surrogate key</strong> as PK that facts reference. Dimensions answer the who/what/where/when/how; facts answer how-much/how-many."},
  {"sec":"LAKE-OLAP","front":"Star schema vs snowflake schema","back":"<strong>Star:</strong> one central fact joined directly to <em>denormalized</em> dimensions (one hop each) — fewer joins, simpler SQL, the analytics default. <strong>Snowflake:</strong> dimensions are <em>normalized</em> into sub-tables (e.g. product to brand to category), saving some storage at the cost of more joins. On modern columnar warehouses storage is cheap and joins are the cost, so star is usually preferred — snowflake is justified mainly for very large or highly hierarchical dimensions."},
  {"sec":"LAKE-OLAP","front":"Surrogate keys: why dimensions use them instead of natural keys","back":"A <strong>surrogate key</strong> is a meaningless integer/sequence PK independent of the source system's <strong>natural (business) key</strong>. It is required to support <strong>SCD Type 2</strong> (multiple versioned rows for the same business entity each need a distinct PK), it insulates the warehouse from source-key changes/reuse, and integer joins are cheaper than wide composite natural keys. The natural key is kept as a regular attribute for lineage."},
  {"sec":"LAKE-OLAP","front":"SCD Type 1: overwrite","back":"<strong>Type 1</strong> updates the dimension attribute <em>in place</em>, destroying history — the row always reflects the current value. Use it when prior values have no analytical meaning (correcting a typo, an attribute nobody reports on over time). <em>Trap:</em> overwriting silently rewrites the past, so every historical fact now rolls up under the new value as if it were always true."},
  {"sec":"LAKE-OLAP","front":"SCD Type 2: versioned rows with effective-date ranges","back":"<strong>Type 2</strong> preserves full history by inserting a <em>new row</em> per change, each with its own surrogate key plus <code>effective_from</code>/<code>effective_to</code> (or validity range) and usually an <code>is_current</code> flag. Facts join to the version that was active at the event time, giving correct <em>as-of</em> reporting. This is the default for attributes whose change you must analyze (e.g. a customer moving regions)."},
  {"sec":"LAKE-OLAP","front":"SCD Type 3: prior-value column","back":"<strong>Type 3</strong> adds a column to hold a <em>previous</em> value (e.g. <code>current_region</code> + <code>previous_region</code> + <code>changed_date</code>), keeping the row count stable. It captures only a <em>fixed number</em> of prior states (usually one), so it suits known, one-time realignments (a sales-territory reorg) where you want to report old and new side-by-side — not unbounded history."},
  {"sec":"LAKE-OLAP","front":"Conformed dimensions and the bus matrix","back":"A <strong>conformed dimension</strong> is a single dimension (e.g. <code>date</code>, <code>customer</code>, <code>product</code>) shared with identical keys and meaning across <em>multiple</em> fact tables/data marts, so metrics from different processes can be filtered and joined consistently (drill-across). Kimball's <strong>bus matrix</strong> (business processes as rows, conformed dimensions as columns) is the planning artifact. Without conformance, \"customer\" means different things in two marts and cross-process analysis is unreliable."},
  {"sec":"LAKE-OLAP","front":"The semantic / metrics layer","back":"A <strong>semantic (metrics) layer</strong> centrally defines metrics, dimensions, joins, and grain once (in tools like the dbt Semantic Layer/MetricFlow, Cube, or LookML) so every BI tool, notebook, and ad-hoc query computes \"revenue\" or \"active users\" identically. It decouples business definitions from physical tables, enforces consistent aggregation/grain, and kills the \"every dashboard reports a different number\" problem. It is logic and governance that compiles to SQL, not a storage tier."},
  {"sec":"LAKE-OLAP","front":"Why warehouses keep a dedicated date/calendar dimension","back":"A <strong>date dimension</strong> is a pre-built table with one row per day carrying rich attributes (<code>fiscal_quarter</code>, <code>day_of_week</code>, <code>is_holiday</code>, <code>week_start</code>). It lets analysts filter/group on business-calendar concepts without per-query date math, is the most universally <em>conformed</em> dimension, and avoids brittle <code>DATEPART</code> logic scattered across reports. Generate it once with a fixed integer key like <code>20260621</code>."}
);

Object.assign(GLOSSARY_DATA, {
  "oltp-vs-olap": {"term":"OLTP vs OLAP","group":"LAKE-OLAP","aliases":["oltp","olap","transactional vs analytical","row vs column"],"eli5":"OLTP is the cash register doing many tiny writes; OLAP is the analyst scanning millions of those receipts to find trends.","memory":"Row = transaction (one whole record at a time); Column = analysis (one whole field across all records).","full":"<p><strong>OLTP</strong> (online transaction processing) optimizes for high-concurrency, low-latency single-row reads/writes: row-oriented storage, normalized schemas, point-lookup indexes. <strong>OLAP</strong> (online analytical processing) optimizes for large scans and aggregations: column-oriented storage, denormalized star schemas, compression, and vectorized execution.</p><p>The decisive difference is the <em>access pattern</em>. Columnar wins for OLAP because a query reads only referenced columns, like-typed values compress hard, and SIMD processes columnar batches efficiently. This is why modern analytical formats (Parquet, Iceberg-on-Parquet) are columnar and Snowflake micro-partitions are hybrid-columnar, while operational stores stay row-oriented.</p>","trap":"Don't say \"OLAP = read-only\" or \"OLAP = a different SQL dialect.\" Both run SQL; the real distinction is physical layout and workload shape (point access vs wide scan/aggregate).","worked":"\"Insert one order, then read it back by id\" = OLTP. \"SUM(revenue) by region by month over 3 years\" = OLAP — touches 2 columns across billions of rows, so columnar pruning + compression makes it tractable.","related":["fact-table","star-schema","dimension-table"]},
  "fact-table": {"term":"Fact Table","group":"LAKE-OLAP","aliases":["fact","fact tables","measures table"],"eli5":"The long skinny table of things that happened, holding the numbers you add up plus pointers to the descriptive tables.","memory":"FACTs are FACTual events: foreign-keys + measures, nothing chatty.","full":"<p>A <strong>fact table</strong> stores business events/measurements at a declared <strong>grain</strong> (the meaning of one row). It contains foreign keys to dimensions and numeric <strong>measures</strong>, and almost no descriptive text. It is the largest, fastest-growing table in the model and is kept narrow.</p><p>Measures classify as <em>additive</em> (sum across all dimensions), <em>semi-additive</em> (sum across all but time, e.g. balances), or <em>non-additive</em> (ratios — store numerator/denominator instead). Common flavors: transaction (one row per event), periodic snapshot (one row per interval), and accumulating snapshot (one row per process, updated in place).</p>","trap":"Mixing grains in one fact table. Once two rows mean different things, any SUM double-counts silently. Declare grain first, then dimensions, then measures — in that order.","worked":"A sales fact at grain \"one row per order line\": columns <code>date_key</code>, <code>customer_key</code>, <code>product_key</code>, <code>order_number</code> (degenerate dim), <code>quantity</code>, <code>extended_amount</code>. To analyze whole orders you aggregate up; you never mix order-level and line-level rows.","related":["dimension-table","grain","star-schema","oltp-vs-olap"]},
  "grain": {"term":"Grain (Fact Granularity)","group":"LAKE-OLAP","aliases":["grain","granularity","fact grain"],"eli5":"A one-sentence statement of exactly what a single row in the fact table represents.","memory":"Grain is the period at the end of \"one row = ____.\" Write that sentence first.","full":"<p><strong>Grain</strong> is the precise definition of what one fact row means, e.g. \"one row per product per store per day.\" In Kimball's four-step design (select business process, declare grain, identify dimensions, identify facts) it is declared <em>before</em> dimensions or measures, because grain dictates which dimensions can legally attach and at what level measures are additive.</p><p>Finer grain = more rows but maximum analytical flexibility (you can always roll up, never drill down past stored grain). Coarser grain pre-aggregates and loses detail. The cardinal sin is letting two different meanings coexist in one table.</p>","trap":"Interviewers probe: \"What's the grain?\" If you can't state it in one sentence, your model is undefined. Also: you can roll UP from a fine grain but never recover detail you didn't store.","worked":"Retail POS grain: \"one row per scanned line item per transaction.\" That fixes the date, store, product, and cashier dimensions and makes <code>quantity</code>/<code>amount</code> fully additive.","related":["fact-table","dimension-table"]},
  "dimension-table": {"term":"Dimension Table","group":"LAKE-OLAP","aliases":["dimension","dimensions","dim table"],"eli5":"The wide descriptive lookup tables (customer, product, date) you filter and group by.","memory":"Dimensions DESCRIBE; facts MEASURE. Dims are wide-and-short, facts are narrow-and-tall.","full":"<p>A <strong>dimension table</strong> supplies the descriptive context — the who/what/where/when — that you slice, filter, and label facts by. It is wide (many text/categorical columns) and relatively short, with a <strong>surrogate key</strong> PK referenced by fact foreign keys.</p><p>Dimensions are typically denormalized (star schema) so a single join yields all attributes. Their attributes evolve over time, which is what <strong>Slowly Changing Dimension</strong> strategies manage. The most universal one is the <em>date dimension</em>, conformed across nearly every fact.</p>","trap":"Putting descriptive text on the fact table (bloats the biggest table and fragments the description). Push descriptions into dimensions; keep only keys, measures, and degenerate dimensions on the fact.","worked":"<code>dim_customer</code>: <code>customer_key</code> (surrogate PK), <code>customer_id</code> (natural key), <code>name</code>, <code>segment</code>, <code>region</code>, <code>effective_from</code>, <code>effective_to</code>, <code>is_current</code> — the date columns make it SCD Type 2.","related":["fact-table","surrogate-key","scd-type-2","conformed-dimension","star-schema"]},
  "star-schema": {"term":"Star Schema vs Snowflake","group":"LAKE-OLAP","aliases":["star schema","snowflake schema","star vs snowflake"],"eli5":"Star = one fact surrounded by flat dimension tables (one join each); snowflake = those dimensions split into normalized sub-tables.","memory":"STAR = flat points radiating from the fact (one hop); snowFLAKE = branchy crystals (multi-hop).","full":"<p>In a <strong>star schema</strong> a central fact joins directly to <em>denormalized</em> dimensions — each dimension is one hop away, minimizing joins and simplifying SQL. In a <strong>snowflake schema</strong> dimensions are normalized into chains of sub-tables (product to brand to category), trading more joins for less redundancy.</p><p>On modern columnar warehouses, dimension storage is trivially cheap while joins cost CPU and complexity, so the <strong>star is the default</strong>. Snowflaking is justified only for very large, rapidly changing, or highly hierarchical dimensions where the normalization genuinely pays off.</p>","trap":"Defending snowflake \"to save space\" in a columnar warehouse — space isn't the bottleneck, join count is. Lead with star unless you can name a concrete reason to normalize.","worked":"Star: <code>fact_sales JOIN dim_product</code> directly. Snowflake: <code>fact_sales JOIN dim_product JOIN dim_brand JOIN dim_category</code> — three joins to label one row by category.","related":["fact-table","dimension-table","conformed-dimension"]},
  "scd-type-2": {"term":"SCD Type 2 (Versioning)","group":"LAKE-OLAP","aliases":["scd type 2","type 2","slowly changing dimension type 2","scd2"],"eli5":"When a dimension attribute changes, you add a new dated row instead of overwriting, so history is preserved and facts join to the value that was true at the time.","memory":"Type 1 = overwrite (1 truth), Type 2 = two-or-more rows over time, Type 3 = one extra column for the prior value.","full":"<p><strong>SCD Type 2</strong> preserves full history by inserting a new dimension row on each change, each with its own surrogate key, a validity range (<code>effective_from</code>/<code>effective_to</code>), and typically an <code>is_current</code> flag. Facts reference the surrogate that was active at the event timestamp, enabling correct <em>as-of</em> reporting.</p><p>Contrast with <strong>Type 1</strong> (overwrite, no history) and <strong>Type 3</strong> (one prior-value column, limited history). Type 2 is why surrogate keys are mandatory: the same business entity now has multiple PKs. The cost is a growing dimension and the discipline of joining facts on the as-of-active version, not just <code>is_current</code>.</p>","trap":"Joining facts to <code>WHERE is_current = true</code> for historical analysis — that restates the past under today's attributes. Join on the surrogate active at the fact's event time (the whole point of Type 2).","worked":"Customer moves from East to West region: old row gets <code>effective_to = 2026-03-01</code>, a new row with a fresh surrogate key gets <code>effective_from = 2026-03-01</code>, <code>is_current = true</code>. February orders still roll up under East.","related":["surrogate-key","dimension-table","scd-type-1-3","conformed-dimension"]},
  "scd-type-1-3": {"term":"SCD Type 1 & Type 3","group":"LAKE-OLAP","aliases":["scd type 1","scd type 3","type 1","type 3","overwrite dimension"],"eli5":"Type 1 just overwrites the old value (no history); Type 3 keeps a single 'previous value' column alongside the current one.","memory":"Type 1 forgets, Type 3 remembers exactly one thing, Type 2 remembers everything.","full":"<p><strong>SCD Type 1</strong> updates the attribute in place, destroying prior values — appropriate for corrections or attributes nobody analyzes over time. Its danger is that every past fact silently rolls up under the new value as though it were always so.</p><p><strong>SCD Type 3</strong> adds a column for a bounded number of prior states (usually one): <code>current_x</code> + <code>previous_x</code> + <code>changed_date</code>. Row count stays stable, and you can report old vs new side-by-side. It fits known one-time realignments (a territory reorg), not unbounded change history — for that you need Type 2.</p>","trap":"Reaching for Type 3 to \"save history.\" It captures only N prior values (typically one). If you need full change history, it must be Type 2 — Type 3 is for a single planned realignment.","worked":"Type 1: fix a misspelled <code>product_name</code> in place. Type 3: sales reorg adds <code>previous_territory</code> next to <code>current_territory</code> so reports can compare pre/post-reorg attribution for one transition.","related":["scd-type-2","dimension-table","surrogate-key"]},
  "conformed-dimension": {"term":"Conformed Dimension","group":"LAKE-OLAP","aliases":["conformed dimension","conformed dimensions","bus matrix"],"eli5":"One shared, identical dimension (like date or customer) reused across many fact tables so different processes can be compared on the same terms.","memory":"Conformed = one source of truth for 'who/what/when', shared on the bus that all facts ride.","full":"<p>A <strong>conformed dimension</strong> has identical keys, attributes, and meaning everywhere it is used, letting metrics from separate fact tables / data marts be filtered, grouped, and joined consistently. It is the backbone of Kimball's integrated warehouse and is planned via the <strong>bus matrix</strong> (business processes x conformed dimensions).</p><p>Conformance enables <em>drill-across</em>: query two facts at a common dimension grain and align the results. Without it, \"customer\" or \"product\" diverges between marts and cross-process analysis (e.g. comparing sales and support by customer) becomes unreliable or impossible.</p>","trap":"Claiming dimensions are conformed when only the <em>name</em> matches. True conformance requires the same keys and semantics — a <code>customer</code> dim with different keys per mart is not conformed and will break drill-across.","worked":"A single <code>dim_date</code> and <code>dim_customer</code> shared by <code>fact_sales</code> and <code>fact_support_tickets</code> lets you compare revenue and ticket volume by the same customer and calendar — that alignment is drill-across over conformed dimensions.","related":["dimension-table","star-schema","semantic-layer","scd-type-2"]},
  "semantic-layer": {"term":"Semantic / Metrics Layer","group":"LAKE-OLAP","aliases":["semantic layer","metrics layer","headless bi","metric store"],"eli5":"A central place to define what each metric means once, so every dashboard and query computes 'revenue' the same way.","memory":"Define the metric ONCE, consume it EVERYWHERE — the layer is logic, not storage.","full":"<p>The <strong>semantic (metrics) layer</strong> defines metrics, dimensions, joins, and aggregation grain a single time, then serves consistent results to every BI tool, notebook, and API. Implementations include the dbt Semantic Layer (powered by MetricFlow), Cube, and LookML. It is sometimes called \"headless BI\" because the definitions are decoupled from any one visualization tool.</p><p>It solves the \"every dashboard shows a different number\" problem by centralizing business logic, enforcing correct grain/aggregation (including semi-additive handling), and providing governed, reusable definitions. It is a logical/governance tier, not a storage engine — it compiles metric requests into SQL against your warehouse or lakehouse.</p>","trap":"Calling it a caching/storage layer or a BI tool. It's a definition+compilation layer; it generates SQL against the warehouse and governs meaning, it does not store the data (though some, like Cube, add a caching tier on top).","worked":"Define <code>weekly_active_users</code> (with its grain and dedup rule) once in the semantic layer; Tableau, a Python notebook, and an embedded app all request that metric and get byte-identical numbers.","related":["conformed-dimension","fact-table","dimension-table"]}
});
