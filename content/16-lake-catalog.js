/* Catalogs & Metadata Governance (LAKE-CATALOG) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"LAKE-CATALOG","front":"What is a table catalog in a lakehouse, and why is it the \"source of truth\"?","back":"The catalog is the service that maps a logical table identifier (e.g., <code>db.schema.table</code>) to the physical location of its current metadata, and tracks which tables/namespaces exist. For formats like Iceberg, the catalog holds the single authoritative pointer to the <strong>current metadata file</strong>; engines resolve a name through the catalog before touching object storage. Without it, there is no agreed answer to \"which version of this table is live right now.\""},
  {"sec":"LAKE-CATALOG","front":"In Apache Iceberg, what exactly does the catalog store versus what lives in object storage?","back":"The catalog stores only the <strong>namespace list and a pointer to each table's current metadata file</strong> (the <code>metadata-location</code>). Everything else — the metadata JSON (schema, partition spec, snapshots), manifest lists, manifest files, and data files — lives in object storage. The catalog is deliberately tiny: it's just the swappable pointer plus namespace bookkeeping."},
  {"sec":"LAKE-CATALOG","front":"How does Iceberg achieve an atomic commit, and what catalog primitive does it depend on?","back":"A writer creates a brand-new metadata file reflecting the new snapshot, then asks the catalog to atomically swap the table's pointer from the old metadata location to the new one — a <strong>compare-and-swap</strong> (CAS): \"replace pointer X with Y only if it still equals X.\" If two writers race, only one CAS succeeds; the loser re-reads and retries (optimistic concurrency). Atomicity of the whole commit reduces to the atomicity of that single pointer swap."},
  {"sec":"LAKE-CATALOG","front":"Why can't a file-system-only catalog give Iceberg safe concurrent commits everywhere, while Hive Metastore can?","back":"Safe commits need an atomic compare-and-swap on the table pointer. Hive Metastore provides this via a transactional RDBMS row update, so it works. A pure <strong>HadoopCatalog</strong> (pointer = a file in object storage) relies on atomic rename, which S3 has never provided, so it was unsafe for concurrent writers and is now discouraged for production. This is why production deployments use a real catalog (REST, Glue, JDBC, Nessie). Note: S3 added conditional writes (<code>If-None-Match</code>) in late 2024, which newer object-store catalogs can use for a CAS-like guarantee — but plain HadoopCatalog predates and doesn't rely on it."},
  {"sec":"LAKE-CATALOG","front":"What problem does the Iceberg REST Catalog specification solve?","back":"It defines a single open <strong>HTTP/OpenAPI contract</strong> for catalog operations (list/create namespaces, load table, commit). Instead of every engine bundling a different client (Hive, Glue, JDBC, DynamoDB), an engine speaks one REST protocol and any compliant server can back it. This decouples the engine from the catalog implementation and lets the server own commit logic, credential vending, and access control server-side."},
  {"sec":"LAKE-CATALOG","front":"Name several production implementations of the Iceberg REST Catalog API and what they have in common.","back":"Examples: <strong>Apache Polaris</strong> (Snowflake's donation, incubating), <strong>Unity Catalog OSS</strong> (Databricks), Lakekeeper, Project Nessie, Apache Gravitino, and AWS Glue's Iceberg REST endpoint. They all expose the same REST spec, so Spark, Trino, Flink, DuckDB, and Snowflake can read/write the <strong>same single copy</strong> of an Iceberg table through whichever one you run."},
  {"sec":"LAKE-CATALOG","front":"What is Apache Polaris and where did it come from?","back":"Polaris is an open-source, vendor-neutral Iceberg <strong>REST catalog</strong> originally built by Snowflake and donated to the Apache Software Foundation in 2024 (currently <strong>Apache Polaris Incubating</strong>). It backs Snowflake Open Catalog (the managed version) and can be self-hosted. It adds catalog-level RBAC and credential vending on top of the Iceberg REST spec, and is a flagship of the \"any engine reads the same Iceberg tables\" movement."},
  {"sec":"LAKE-CATALOG","front":"What is Databricks Unity Catalog, and how does it interoperate with the open ecosystem?","back":"Unity Catalog is Databricks' governance catalog spanning tables, files, ML models, and functions, with a three-level <code>catalog.schema.object</code> namespace plus lineage and fine-grained access control. It was open-sourced in 2024 (Apache 2.0, hosted under LF AI &amp; Data) and exposes both the <strong>Iceberg REST Catalog API</strong> and an HMS-compatible interface, so external Iceberg/Hive clients can read UC-governed tables — often paired with Delta UniForm so Delta tables are readable by Iceberg clients."},
  {"sec":"LAKE-CATALOG","front":"How does Snowflake catalog its tables, and how does that differ for native vs. Iceberg tables?","back":"Snowflake's cloud-services metadata layer (a <strong>FoundationDB-backed</strong> key-value store) catalogs <strong>native</strong> tables and their micro-partition metadata — fully proprietary. For <strong>Iceberg</strong> tables, Snowflake can either be the catalog (Snowflake-managed Iceberg) or defer to an external Iceberg REST catalog (e.g., Open Catalog/Polaris/Glue), in which case the open metadata files in your storage are the source of truth and other engines can share them."},
  {"sec":"LAKE-CATALOG","front":"What is AWS Glue Data Catalog and what role does it play for Iceberg/Athena?","back":"Glue Data Catalog is AWS's managed, HMS-compatible metastore: it stores databases, tables, schemas, and partitions, and is the default catalog for Athena, EMR, and Redshift Spectrum. It can act as an <strong>Iceberg catalog</strong> (storing the metadata pointer and providing the atomic commit) and now also exposes an <strong>Iceberg REST endpoint</strong>, letting non-AWS engines (PyIceberg, OSS Spark) reach Glue-managed tables through the open spec."},
  {"sec":"LAKE-CATALOG","front":"Beyond table discovery, what four governance capabilities do modern catalogs add?","back":"<ul><li><strong>Access control</strong> — RBAC/ABAC, row/column masking enforced at the catalog boundary.</li><li><strong>Lineage</strong> — table- and column-level provenance across jobs.</li><li><strong>Tags / classification</strong> — PII, sensitivity, business glossary metadata.</li><li><strong>Audit + discovery</strong> — who-touched-what plus searchable data discovery.</li></ul>The catalog becomes the policy and metadata control plane, not just a name lookup."},
  {"sec":"LAKE-CATALOG","front":"Why does \"engine-neutral open catalog\" matter, and what does it eliminate?","back":"If Spark, Trino, Flink, and Snowflake all reach an Iceberg table through one open REST catalog, they share <strong>one physical copy</strong> of the data — no per-engine ingestion, no copy drift, one set of governance/permissions. It eliminates the historical pattern of duplicating each table into every engine's proprietary store, and prevents catalog-level vendor lock-in (the catalog, not just the file format, becomes the lock-in risk)."},
  {"sec":"LAKE-CATALOG","front":"What is a schema registry and why is it the \"catalog\" for streaming sources?","back":"A schema registry (e.g., Confluent Schema Registry, AWS Glue Schema Registry) stores versioned <strong>Avro/Protobuf/JSON schemas</strong> for Kafka topics and assigns each a schema ID embedded in the message. It enforces <strong>compatibility rules</strong> (backward/forward/full) so producers can't ship a breaking change that poisons consumers. It's the source of truth for \"what shape is this stream,\" the streaming analog of a table catalog's schema role."},
  {"sec":"LAKE-CATALOG","front":"What does \"compatibility mode\" mean in a schema registry, and what's the practical difference between BACKWARD and FORWARD?","back":"It's the rule the registry checks before accepting a new schema version. <strong>BACKWARD</strong> (Confluent's default): a consumer on the new schema can read data written with the old one — so you upgrade <em>consumers first</em> (you may add optional fields / delete fields). <strong>FORWARD</strong>: a consumer on the old schema can read data written with the new one — so you upgrade <em>producers first</em>. <strong>FULL</strong> requires both. The mode dictates your safe deploy order."},
  {"sec":"LAKE-CATALOG","front":"Why is credential vending a key feature of REST catalogs like Polaris/Unity Catalog?","back":"Because the catalog server owns access policy, it can issue <strong>short-lived, scoped storage credentials</strong> (e.g., temporary S3 tokens) at table-load time instead of clients holding broad standing cloud keys. This pushes authorization to the catalog boundary, enables per-table/per-namespace permissions for engines that don't natively understand cloud IAM, and is how one open catalog enforces consistent access across Spark/Trino/etc."}
);

Object.assign(GLOSSARY_DATA, {
  "data-catalog": {"term":"Data Catalog (Table Catalog)","group":"LAKE-CATALOG","aliases":["catalog","metastore","table catalog"],"eli5":"A phone book that turns a table's name into the address of its current metadata, and lists which tables exist.","memory":"Catalog = the swappable pointer + the namespace index. Tiny on purpose.","full":"<p>A catalog is the service that resolves a logical table identifier to its physical metadata and tracks the set of namespaces and tables. In a lakehouse it is the <strong>source of truth</strong> for existence and currency: it answers \"does this table exist?\" and \"which metadata version is live right now?\" For Iceberg it stores essentially one thing per table — a pointer to the current metadata file — plus namespace bookkeeping.</p><p>Catalog implementations range from legacy Hive Metastore and AWS Glue to the modern open REST catalogs (Polaris, Unity Catalog, Nessie, Lakekeeper). Modern catalogs also become the control plane for access control, lineage, and tags, not merely a name-to-location lookup.</p>","trap":"Candidates conflate the catalog with the table format. Iceberg/Delta define the file/metadata layout; the catalog only holds the pointer and does the atomic swap. You can change catalogs without rewriting data, and change formats without changing catalogs.","related":["iceberg-rest-catalog","atomic-commit-cas","hive-metastore","glue-data-catalog"]},
  "iceberg-rest-catalog": {"term":"Iceberg REST Catalog","group":"LAKE-CATALOG","aliases":["REST catalog","Iceberg REST spec","IRC"],"eli5":"One standard HTTP API for catalogs, so any engine can talk to any catalog without a special client.","memory":"REST = the USB-C port for catalogs: one plug, every engine, every server.","full":"<p>The Iceberg REST Catalog is an open OpenAPI/HTTP specification for catalog operations: listing and creating namespaces, loading a table, and committing changes. Instead of each engine bundling a catalog-specific client (Hive, JDBC, Glue, DynamoDB), engines speak one protocol and any compliant server backs it. Crucially the server can own commit logic, credential vending, and access control, so the client stays thin.</p><p>Implementations include Apache Polaris, Databricks Unity Catalog (OSS), Lakekeeper, Project Nessie, Apache Gravitino, and AWS Glue's Iceberg REST endpoint. This spec is the technical foundation of the \"engine-neutral catalog\" movement — Spark, Trino, Flink, DuckDB, and Snowflake all reach the same table copy through it.</p>","trap":"Don't confuse the REST <em>spec</em> with any one product. Polaris and Unity Catalog are <em>implementations</em>; the spec is the contract. \"Open\" means no proprietary extensions or capability gates that re-introduce lock-in.","related":["data-catalog","apache-polaris","unity-catalog","open-engine-neutral-catalog","atomic-commit-cas"]},
  "hive-metastore": {"term":"Hive Metastore (HMS)","group":"LAKE-CATALOG","aliases":["HMS","metastore","Hive Metastore Service"],"eli5":"The original data-lake catalog: an RDBMS-backed service that remembers your tables, schemas, and partitions.","memory":"HMS = the granddad catalog. Still the lingua franca (everyone speaks its Thrift dialect), but it chokes on partitions.","full":"<p>Hive Metastore is the legacy catalog standard from the Hadoop era: a Thrift service backed by a relational database (MySQL/Postgres) storing databases, tables, columns, SerDe info, and — notoriously — one row per partition. Its RDBMS transaction gives Iceberg the atomic pointer update it needs, so HMS works as an Iceberg catalog.</p><p>Its weaknesses drove the move to newer catalogs: partition explosion (millions of rows), a heavyweight always-on service, limited access control, and no native multi-engine governance. The <strong>HMS Thrift interface</strong> nonetheless became a de-facto compatibility standard — Glue and even Unity Catalog expose HMS-compatible endpoints so legacy clients keep working.</p>","trap":"\"Hive Metastore is dead\" is too glib. Its <em>interface</em> persists as a compatibility layer in Glue/Unity Catalog; what's obsolete is HMS as the <em>primary</em> catalog for modern table formats at scale.","related":["data-catalog","glue-data-catalog","atomic-commit-cas","unity-catalog"]},
  "glue-data-catalog": {"term":"AWS Glue Data Catalog","group":"LAKE-CATALOG","aliases":["Glue catalog","Glue Data Catalog"],"eli5":"AWS's managed, serverless Hive-compatible metastore that Athena, EMR, and Redshift Spectrum all share.","memory":"Glue = managed HMS-on-AWS; Athena's address book.","full":"<p>Glue Data Catalog is AWS's fully managed metastore: it stores databases, tables, schemas, and partitions and is HMS-API-compatible, making it the default catalog for Athena, EMR, Redshift Spectrum, and Glue ETL. It integrates with Lake Formation for fine-grained access control and can act as an <strong>Iceberg catalog</strong>, holding the metadata pointer and providing the atomic commit.</p><p>To join the open ecosystem, Glue now exposes an <strong>Iceberg REST endpoint</strong> (supporting Iceberg spec v1/v2, defaulting to v2), so engines outside AWS can reach Glue-managed Iceberg tables through the standard spec rather than an AWS-specific client.</p>","trap":"Two distinct things are both called \"Glue\": the Data Catalog (metastore) and Glue ETL (Spark jobs). In a catalog question they mean the Data Catalog. Also note Glue can be both an HMS-style catalog AND an Iceberg REST catalog.","related":["hive-metastore","data-catalog","iceberg-rest-catalog"]},
  "apache-polaris": {"term":"Apache Polaris","group":"LAKE-CATALOG","aliases":["Polaris","Snowflake Open Catalog","Polaris Catalog"],"eli5":"An open-source Iceberg REST catalog from Snowflake that lets any engine share one copy of your tables.","memory":"Polaris = the open Iceberg catalog Snowflake gave away. North star for vendor-neutral.","full":"<p>Apache Polaris is a vendor-neutral, open-source implementation of the Iceberg REST Catalog, built by Snowflake and donated to the Apache Software Foundation in 2024 (currently incubating, with a 1.0-incubating release out). It backs <strong>Snowflake Open Catalog</strong> (the managed offering) and can be self-hosted via containers. On top of the REST spec it adds catalog/principal RBAC and <strong>credential vending</strong> (short-lived scoped storage tokens).</p><p>Its pitch is interoperability without lock-in: Spark, Trino, Flink, Doris, Dremio, and Snowflake all read/write the same Iceberg tables through Polaris, with no proprietary protocol extensions.</p>","trap":"Polaris being open-source does not make Snowflake's whole platform open — native Snowflake tables still use proprietary FoundationDB-backed metadata. Polaris only governs the Iceberg surface.","related":["iceberg-rest-catalog","open-engine-neutral-catalog","credential-vending","unity-catalog"]},
  "unity-catalog": {"term":"Databricks Unity Catalog","group":"LAKE-CATALOG","aliases":["Unity Catalog","UC","Unity Catalog OSS"],"eli5":"Databricks' governance catalog for tables, files, models, and functions, now open-sourced and Iceberg-REST-speaking.","memory":"Unity = one catalog for data AND AI (models, functions too), with an Iceberg REST door bolted on.","full":"<p>Unity Catalog is Databricks' unified governance layer with a three-level <code>catalog.schema.object</code> namespace, spanning tables, volumes (files), ML models, and functions, plus built-in lineage and fine-grained access control. Databricks open-sourced it in 2024 under Apache 2.0 (hosted by LF AI &amp; Data).</p><p>For interoperability it implements the <strong>Iceberg REST Catalog API</strong> and an HMS-compatible interface, so external Iceberg/Hive clients can read UC-governed tables — frequently combined with <strong>Delta UniForm</strong>, which writes Iceberg metadata alongside Delta so Iceberg engines see Delta tables as Iceberg.</p>","trap":"Unity Catalog governs more than tables (models, files, functions) — broader than a pure table catalog. And UC \"open\" via Iceberg REST is read-focused for external engines; full read/write parity depends on managed-vs-foreign table type and version.","related":["iceberg-rest-catalog","apache-polaris","open-engine-neutral-catalog","data-catalog"]},
  "open-engine-neutral-catalog": {"term":"Engine-Neutral (Open) Catalog","group":"LAKE-CATALOG","aliases":["open catalog","engine-neutral catalog","shared catalog"],"eli5":"One catalog all engines share, so there's a single copy of each table instead of one copy per engine.","memory":"Open format stops file lock-in; open CATALOG stops the next lock-in. One table, many engines.","full":"<p>An engine-neutral catalog is a catalog (typically Iceberg-REST-based) that any compute engine can read and write through the same open protocol, so Spark, Trino, Flink, Snowflake, and DuckDB operate on a <strong>single physical copy</strong> of each table with one set of permissions and lineage. It removes the legacy pattern of ingesting every table separately into each engine's proprietary store, eliminating copy drift and per-engine governance.</p><p>The strategic point: the table format being open (Iceberg/Parquet) is necessary but not sufficient — if the <em>catalog</em> is proprietary, you're still locked in. Open catalogs move the lock-in boundary off the metadata control plane.</p>","trap":"Senior interview trap: \"we use Iceberg, so we're not locked in.\" Wrong — if your only catalog is a closed vendor service that other engines can't commit through, the catalog is the lock-in. Open file format + closed catalog ≠ open lakehouse.","related":["iceberg-rest-catalog","apache-polaris","unity-catalog","data-catalog"]},
  "credential-vending": {"term":"Credential Vending","group":"LAKE-CATALOG","aliases":["credential vending","vended credentials","scoped storage tokens"],"eli5":"The catalog hands out short-lived, table-scoped storage keys at load time, so clients never hold broad standing cloud keys.","memory":"The catalog is the bouncer: it vends a short-lived wristband per table, not the master key.","full":"<p>Credential vending is a REST-catalog feature where the catalog server, which already owns the access policy, issues <strong>short-lived, scoped storage credentials</strong> (e.g., temporary S3/GCS tokens) when an engine loads a table. The client receives just enough access to read/write that table's files for a short window, rather than carrying broad long-lived cloud IAM keys.</p><p>This pushes authorization to the catalog boundary and lets one open catalog enforce consistent per-table/per-namespace access across engines (Spark, Trino, Flink) that don't natively understand the underlying cloud IAM. It is core to Polaris and Unity Catalog's governance story.</p>","trap":"Vended credentials scope and time-bound access but don't replace storage-side encryption or network controls — and if an engine bypasses the catalog and reads the bucket directly with its own keys, vending is moot. The model only holds when all access flows through the catalog.","related":["iceberg-rest-catalog","apache-polaris","unity-catalog","data-catalog"]}
});
