/* JavaScript Core (FE-JS) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"FE-JS","front":"What are the 7 primitive types, and how does copying a primitive differ from copying an object?","back":"<p>The <strong>7 primitives</strong>: <code>string</code>, <code>number</code>, <code>boolean</code>, <code>null</code>, <code>undefined</code>, <code>symbol</code>, <code>bigint</code>. Everything else (objects, arrays, functions) is a <strong>reference type</strong>.</p><ul><li><strong>Primitives copy by value</strong>: <code>let b = a</code> duplicates the value; mutating <code>b</code> never touches <code>a</code>. Primitives are also <em>immutable</em>.</li><li><strong>Objects copy by reference</strong>: the variable holds a pointer. <code>let y = x</code> makes both point at the <em>same</em> object — <code>y.foo = 1</code> is visible through <code>x</code>.</li><li>Equality follows suit: <code>1 === 1</code> is <code>true</code>, but <code>{} === {}</code> is <code>false</code> (distinct identities).</li><li>Args pass the same way — JS is always <em>pass-by-value</em>, but for objects the value <em>is</em> the reference: you can mutate a passed object's properties, but reassigning the parameter doesn't affect the caller.</li></ul>"},
  {"sec":"FE-JS","front":"Explain == vs === and the rules of truthy/falsy values.","back":"<p><code>===</code> is <strong>strict equality</strong>: no coercion; types must match. <code>==</code> is <strong>loose equality</strong>: it coerces operands via the Abstract Equality algorithm. <em>Default to <code>===</code>.</em></p><ul><li>The one defensible <code>==</code> use: <code>x == null</code> matches <strong>both <code>null</code> and <code>undefined</code></strong> and nothing else.</li><li>Gotchas: <code>0 == ''</code> → <code>true</code>, <code>0 == '0'</code> → <code>true</code>, <code>'' == '0'</code> → <code>false</code>, <code>null == undefined</code> → <code>true</code>, <code>NaN == NaN</code> → <code>false</code>, <code>[] == ![]</code> → <code>true</code>.</li><li><strong>Falsy values</strong>: <code>false</code>, <code>0</code>, <code>-0</code>, <code>0n</code>, <code>''</code>, <code>null</code>, <code>undefined</code>, <code>NaN</code> — plus the legacy oddity <code>document.all</code>. <em>Everything else is truthy</em> — including <code>'0'</code>, <code>'false'</code>, <code>[]</code>, and <code>{}</code>.</li></ul>"},
  {"sec":"FE-JS","front":"What is hoisting, and how does the Temporal Dead Zone (TDZ) affect let/const vs var?","back":"<p><strong>Hoisting</strong>: declarations are registered at the top of their scope during the creation phase. <em>What differs is initialization.</em></p><ul><li><code>var</code> is hoisted and initialized to <code>undefined</code> — reading it before the line gives <code>undefined</code>, not an error.</li><li><code>let</code>/<code>const</code> are hoisted but <strong>not initialized</strong>. From scope entry until the declaration line they sit in the <strong>TDZ</strong>; touching them throws <code>ReferenceError</code>.</li><li><strong>Function declarations</strong> are fully hoisted (callable above their definition). <strong>Function expressions / arrows</strong> follow their variable's rules.</li><li><code>class</code> declarations are hoisted but TDZ-bound like <code>let</code>.</li></ul><p><em>TDZ exists to make use-before-declaration a loud error instead of a silent <code>undefined</code>.</em></p>"},
  {"sec":"FE-JS","front":"Compare var, let, and const on scope, redeclaration, and reassignment.","back":"<ul><li><strong>Scope</strong>: <code>var</code> is <em>function-scoped</em> (leaks out of blocks); <code>let</code>/<code>const</code> are <em>block-scoped</em> (<code>{}</code>, loops, <code>if</code>).</li><li><strong>Redeclaration</strong>: <code>var</code> can be redeclared in the same scope; <code>let</code>/<code>const</code> throw <code>SyntaxError</code> (a parse-time error, so the whole script/module fails to run).</li><li><strong>Reassignment</strong>: <code>var</code>/<code>let</code> can be reassigned; <code>const</code> cannot rebind — but <code>const obj</code> can still have its properties mutated (<em>const freezes the binding, not the value</em>).</li><li>Top-level <code>var</code>/function declarations create a property on <code>globalThis</code> in scripts; <code>let</code>/<code>const</code> never do. (In ES <em>modules</em>, top-level <code>var</code> also doesn't, since module scope ≠ global.)</li></ul><p><em>Modern default: <code>const</code> everywhere, <code>let</code> when you must reassign, <code>var</code> essentially never.</em></p>"},
  {"sec":"FE-JS","front":"What is a closure? Explain the classic for-loop var bug and its fixes.","back":"<p>A <strong>closure</strong> is a function bundled with a reference to its surrounding lexical scope — it keeps those variables alive after the outer function returns. Closures power private state, currying, memoization, and event handlers.</p><p><strong>The classic bug:</strong></p><pre><code>for (var i = 0; i &lt; 3; i++) setTimeout(() =&gt; console.log(i));\n// logs 3, 3, 3</code></pre><p><code>var</code> is one shared, function-scoped binding; all callbacks close over the <em>same</em> <code>i</code>, read after the loop ends.</p><ul><li><strong>Fix 1</strong>: use <code>let i</code> — the spec creates a <em>fresh binding per iteration</em> (and copies the value forward) → <code>0, 1, 2</code>.</li><li><strong>Fix 2</strong>: IIFE capturing <code>i</code> as an argument per iteration.</li><li><strong>Fix 3</strong>: pass <code>i</code> as a later <code>setTimeout</code> argument: <code>setTimeout(console.log, 0, i)</code>.</li></ul>"},
  {"sec":"FE-JS","front":"Walk through the rules of `this` binding (and where arrows fit).","back":"<p><code>this</code> is determined at <em>call time</em> by how the function is invoked (precedence high→low):</p><ul><li><strong>new binding</strong>: <code>new Fn()</code> → <code>this</code> is the freshly created object.</li><li><strong>Explicit binding</strong>: <code>fn.call(obj)</code>/<code>fn.apply(obj, args)</code>/<code>fn.bind(obj)</code> → <code>this</code> = <code>obj</code> (<code>bind</code> returns a permanently bound function).</li><li><strong>Implicit binding</strong>: <code>obj.fn()</code> → <code>this</code> = <code>obj</code>. <em>Lost on detachment</em> (<code>const f = obj.fn; f()</code>).</li><li><strong>Default binding</strong>: bare <code>fn()</code> → <code>undefined</code> in strict mode (modules/classes are strict), else <code>globalThis</code>.</li></ul><p><strong>Arrow functions have no own <code>this</code></strong> — they capture it <em>lexically</em> from the enclosing scope and ignore <code>call</code>/<code>apply</code>/<code>bind</code>; they also can't be used with <code>new</code>. Great for callbacks; wrong for object/prototype methods that need their own <code>this</code>.</p>"},
  {"sec":"FE-JS","front":"How does the prototype chain work, and how is prototypal inheritance set up?","back":"<p>Every object has an internal <code>[[Prototype]]</code> link (read via <code>Object.getPrototypeOf</code>; the <code>__proto__</code> accessor is a legacy alias). A property lookup walks this <strong>prototype chain</strong> until found or it hits <code>null</code>.</p><ul><li>A function's <code>.prototype</code> object becomes the <code>[[Prototype]]</code> of instances made with <code>new</code>; methods live there so instances <em>share</em> one copy.</li><li>Create links with <code>Object.create(proto)</code> or class <code>extends</code>. <em>Avoid mutating an existing object's prototype</em> (<code>Object.setPrototypeOf</code>/<code>__proto__</code> setter) — it deoptimizes hot objects; set the prototype at creation instead.</li><li><code>Object.hasOwn(obj, k)</code> (or <code>obj.hasOwnProperty(k)</code>) distinguishes own vs inherited; <code>in</code> checks the whole chain.</li></ul><p><em>JS inheritance is delegation: not copied, but looked up live along the chain.</em></p>"},
  {"sec":"FE-JS","front":"What do class fields, #private members, and static members add over function constructors?","back":"<p><code>class</code> is largely syntactic sugar over prototypes, but adds real capabilities:</p><ul><li><strong>Public class fields</strong> (<code>count = 0</code>) initialize per-instance; arrow-field methods auto-bind <code>this</code> (but live on the instance, not the prototype).</li><li><strong>Private members</strong> (<code>#balance</code>, <code>#method()</code>, accessors) are <em>truly private</em> — enforced by the engine, not convention. Accessing <code>#x</code> off a wrong-shaped object throws; <code>#x in obj</code> is a brand check that returns a boolean instead.</li><li><strong>Static</strong> members (<code>static create()</code>, <code>static #cache</code>, <code>static { }</code> init blocks) live on the class itself, not instances.</li><li>Class bodies are always <em>strict mode</em>; methods are <em>non-enumerable</em>; the declaration is <em>TDZ-bound</em> (not usable before its line). <code>extends</code> + <code>super</code> wire the chain; the constructor must call <code>super()</code> before using <code>this</code>.</li></ul>"},
  {"sec":"FE-JS","front":"ES Modules vs CommonJS — name the substantive differences a senior should know.","back":"<ul><li><strong>Loading</strong>: ESM is <em>static</em> — <code>import</code>/<code>export</code> are resolved before execution, enabling tree-shaking and cyclic resolution. CJS <code>require()</code> is <em>dynamic</em>, runs at call time.</li><li><strong>Bindings</strong>: ESM imports are <em>live read-only bindings</em> — importers see later mutations of an exported <code>let</code>. CJS gives a <em>copied snapshot</em> of <code>module.exports</code> at require time.</li><li><strong>Timing</strong>: ESM evaluation is <em>asynchronous</em> and supports top-level <code>await</code>; CJS is synchronous.</li><li><strong>Mode/this</strong>: ESM is always strict; top-level <code>this</code> is <code>undefined</code> (CJS: <code>module.exports</code>). No <code>__dirname</code>/<code>require</code> in ESM (use <code>import.meta.url</code>, <code>import.meta.dirname</code>, or <code>import.meta.resolve</code>).</li><li>Prefer <strong>named exports</strong> for tree-shaking and refactor-safety; a default export is just a named export keyed <code>default</code>.</li></ul>"},
  {"sec":"FE-JS","front":"What makes a function pure, and why does immutability matter in UI code?","back":"<p>A <strong>pure function</strong>: (1) same inputs → same output, and (2) no <em>side effects</em> (no mutation of args/external state, no I/O, no <code>Date.now</code>/<code>Math.random</code>). Pure functions are testable, memoizable, and referentially transparent.</p><p><strong>Immutability</strong> = never mutate existing data; produce new values instead (<code>[...arr, x]</code>, <code>{...obj, k}</code>, <code>map</code>/<code>filter</code> over <code>push</code>/<code>splice</code>).</p><ul><li><em>Why UI cares</em>: frameworks like React detect change by <strong>reference identity</strong> (<code>prevProps !== nextProps</code>, <code>Object.is</code> in hook deps). Mutating in place keeps the same reference → <strong>missed re-renders / stale memoization</strong>.</li><li><code>Object.freeze</code> enforces <em>shallow</em> immutability at runtime (silent in sloppy mode, throws in strict). Newer arrays also offer non-mutating twins: <code>toSorted</code>, <code>toReversed</code>, <code>toSpliced</code>, <code>with</code>.</li></ul>"},
  {"sec":"FE-JS","front":"Explain spread/rest and destructuring, including defaults and the array swap.","back":"<p><strong>Same <code>...</code> token, opposite roles by position:</strong></p><ul><li><strong>Spread</strong> <em>expands</em>: <code>[...a, ...b]</code>, <code>{...o1, ...o2}</code> (later keys win), <code>fn(...args)</code>. Produces a <em>shallow</em> copy.</li><li><strong>Rest</strong> <em>collects</em>: <code>function f(...args)</code>, <code>const [head, ...tail] = arr</code>, <code>const {a, ...others} = obj</code>. Must be last.</li></ul><p><strong>Destructuring</strong> binds by position (arrays, via iterator) or key (objects), with <em>defaults</em> (apply only on <code>undefined</code>, not <code>null</code>), renaming (<code>{a: x}</code>), nesting, and one-line swap <code>[a, b] = [b, a]</code>.</p><pre><code>const { user: { name = 'Anon' } = {} } = props;</code></pre><p><em>The <code>= {}</code> default guards against a missing <code>user</code> so destructuring its <code>name</code> doesn't throw.</em></p>"},
  {"sec":"FE-JS","front":"How do optional chaining (?.) and nullish coalescing (??) work, and what's the trap?","back":"<ul><li><code>a?.b</code> / <code>a?.[i]</code> / <code>a?.()</code>: if the left side is <code>null</code> or <code>undefined</code>, the whole expression <em>short-circuits</em> to <code>undefined</code> instead of throwing. It checks <em>nullish only</em>, not falsy.</li><li><code>a ?? b</code>: returns <code>b</code> <em>only</em> when <code>a</code> is <code>null</code>/<code>undefined</code> — unlike <code>||</code>, which also falls through on <code>0</code>, <code>''</code>, <code>false</code>, <code>NaN</code>.</li></ul><p><strong>Trap</strong>: <code>count ?? 10</code> preserves <code>0</code> (correct), whereas <code>count || 10</code> wrongly replaces a valid <code>0</code>. <em>Use <code>??</code> for defaults that should respect falsy-but-valid inputs.</em></p><p><strong>Syntax rule</strong>: you cannot mix <code>??</code> with <code>||</code>/<code>&amp;&amp;</code> at the same level without parentheses — it's a <code>SyntaxError</code>. Short-circuiting also <em>stops the chain</em>: <code>a?.b.c</code> returns <code>undefined</code> when <code>a</code> is nullish without touching <code>.c</code>.</p>"},
  {"sec":"FE-JS","front":"Shallow vs deep clone: what does spread copy, and what does structuredClone solve?","back":"<p><code>{...obj}</code>, <code>Object.assign({}, obj)</code>, and <code>arr.slice()</code> are <strong>shallow</strong>: top-level primitives are copied, but <em>nested objects remain shared references</em> — mutating <code>copy.nested.x</code> also changes the original.</p><ul><li><strong>Deep clone</strong>: <code>structuredClone(obj)</code> is the built-in. It handles nested structures, <code>Date</code>, <code>Map</code>, <code>Set</code>, <code>ArrayBuffer</code>, typed arrays, <code>RegExp</code>, and <em>circular references</em>.</li><li>It <em>cannot</em> clone <strong>functions</strong>, <strong>DOM nodes</strong>, or symbols (throws <code>DataCloneError</code>), and it <em>does not preserve the prototype</em> — a class instance clones to a plain object, losing its methods.</li><li>The old hack <code>JSON.parse(JSON.stringify(x))</code> loses <code>undefined</code>, functions, symbols, turns <code>Date</code> into a string, drops <code>Map</code>/<code>Set</code>, and throws on cycles — <em>avoid it now that <code>structuredClone</code> exists</em>.</li></ul>"},
  {"sec":"FE-JS","front":"How does JavaScript garbage collection work, and what are the most common leaks in a UI app?","back":"<p>JS uses <strong>automatic GC</strong> via <em>reachability</em> (mark-and-sweep): an object is collected when it's unreachable from any GC root (globals, the active call stack, etc.). <em>You can't force GC; you only make things unreachable.</em></p><p><strong>Common leaks in front-end apps:</strong></p><ul><li><strong>Forgotten listeners/intervals</strong>: <code>addEventListener</code> / <code>setInterval</code> not torn down on unmount keep their closures (and referenced DOM) alive.</li><li><strong>Detached DOM</strong>: removing a node from the tree but still holding a JS reference to it.</li><li><strong>Closures</strong> capturing large objects that outlive their need; growing module-level caches/arrays.</li><li><strong>Accidental globals</strong> or stale <code>window</code>/singleton references.</li></ul><p>Mitigate with cleanup (React effect teardown, <code>AbortController</code>) and <strong><code>WeakMap</code>/<code>WeakSet</code>/<code>WeakRef</code></strong> for non-owning references that don't pin their targets.</p>"},
  {"sec":"FE-JS","front":"Why does `const f = obj.method; f()` lose `this`, and how do you fix it?","back":"<p><code>this</code> binds at the <em>call site</em>, not where the function was defined. <code>obj.method()</code> uses <strong>implicit binding</strong> (<code>this = obj</code>), but assigning to <code>f</code> strips the receiver — calling <code>f()</code> is now a <strong>default binding</strong> (<code>this</code> = <code>undefined</code> in strict mode), so <code>this.x</code> throws.</p><p><strong>Fixes:</strong></p><ul><li><code>const f = obj.method.bind(obj)</code> — permanent binding.</li><li>Define the method as an <em>arrow class field</em>: <code>method = () =&gt; this.x</code> (captures <code>this</code> lexically at construction).</li><li>Call through the object: <code>obj.method()</code>, or wrap: <code>() =&gt; obj.method()</code>.</li></ul><p><em>This is the canonical React/event-handler binding gotcha (arrow fields and <code>useCallback</code> sidestep it).</em></p>"},
  {"sec":"FE-JS","front":"What is NaN, and what are the correct ways to detect it and check numeric integrity?","back":"<p><code>NaN</code> (\"Not-a-Number\") is a <strong>number-typed</strong> value produced by invalid math (<code>0/0</code>, <code>parseInt('x')</code>, <code>Math.sqrt(-1)</code>, <code>Number('foo')</code>). Its defining quirk: <strong><code>NaN !== NaN</code></strong> — it's the only value not equal to itself.</p><ul><li><strong>Detect</strong>: <code>Number.isNaN(x)</code> (strict, no coercion) — <em>not</em> the legacy global <code>isNaN()</code>, which coerces first so <code>isNaN('foo')</code> → <code>true</code>.</li><li>Trick: <code>x !== x</code> is <code>true</code> iff <code>x</code> is <code>NaN</code>.</li><li><code>Number.isFinite(x)</code> rejects <code>NaN</code> and <code>±Infinity</code>; <code>Number.isInteger(x)</code> checks integral.</li><li><code>Object.is(NaN, NaN)</code> is <code>true</code> (and distinguishes <code>+0</code> from <code>-0</code>), unlike <code>===</code>. Arrays sidestep the quirk: <code>[NaN].includes(NaN)</code> is <code>true</code> while <code>[NaN].indexOf(NaN)</code> is <code>-1</code>.</li></ul>"},
  {"sec":"FE-JS","front":"Primitive wrapper objects and autoboxing — what's the senior-level subtlety?","back":"<p>Primitives have no methods, yet <code>'hi'.toUpperCase()</code> works via <strong>autoboxing</strong>: the engine momentarily wraps the primitive in a temporary <code>String</code>/<code>Number</code>/<code>Boolean</code> object, calls the method, then discards the wrapper.</p><ul><li>Because the wrapper is <em>thrown away</em>, you can't store state on a primitive: <code>'x'.foo = 1; 'x'.foo</code> → <code>undefined</code> (a silent no-op in strict-less code; assignment throws in strict mode).</li><li><strong>Never use <code>new String()</code>/<code>new Number()</code>/<code>new Boolean()</code></strong>: they create <em>objects</em>, so <code>typeof</code> is <code>'object'</code> and <code>new Boolean(false)</code> is <em>truthy</em> — a classic bug.</li><li><code>null</code> and <code>undefined</code> have no wrapper → method access throws <code>TypeError</code> (hence <code>?.</code>).</li></ul><p><em>Call <code>String(x)</code>/<code>Number(x)</code>/<code>Boolean(x)</code> as plain functions for conversion; never with <code>new</code>.</em></p>"}
);

Object.assign(GLOSSARY_DATA, {
  "this-binding": {"term":"this Binding","group":"FE-JS","eli5":"`this` is decided by how you call a function, not where you wrote it.","memory":"New > Explicit > Implicit > Default; arrows steal from their parent.","full":"<p><code>this</code> is bound at call time by four rules in precedence order: <strong>new</strong> (the new instance), <strong>explicit</strong> (<code>call</code>/<code>apply</code>/<code>bind</code>), <strong>implicit</strong> (the object before the dot), and <strong>default</strong> (<code>undefined</code> in strict mode, else <code>globalThis</code>).</p><p>Arrow functions are the exception: they have no own <code>this</code> and inherit it lexically, which is why they're ideal for callbacks but wrong for object methods needing dynamic <code>this</code>.</p>","trap":"Assuming `this` is lexical like a normal variable. For non-arrow functions it's dynamic — detaching a method (<code>const f = obj.m</code>) loses the binding.","worked":"<pre><code>const o = { x: 1, get() { return this.x; } };\nconst f = o.get; f();      // undefined (default)\no.get.call({ x: 9 });      // 9 (explicit)</code></pre>","related":["closure","prototype-chain","class-fields"]},
  "hoisting-tdz": {"term":"Hoisting & TDZ","group":"FE-JS","eli5":"Declarations are noticed early, but let/const aren't usable until their line runs.","memory":"var = hoisted-and-undefined; let/const = hoisted-but-deadly (TDZ).","full":"<p><strong>Hoisting</strong> registers declarations at the top of their scope. <code>var</code> is initialized to <code>undefined</code> immediately; <code>let</code>/<code>const</code>/<code>class</code> are hoisted but uninitialized, sitting in the <strong>Temporal Dead Zone</strong> from scope entry to declaration.</p><p>Accessing a binding inside its TDZ throws <code>ReferenceError</code> — a deliberate safety feature converting silent <code>undefined</code> bugs into loud errors. Function declarations are fully hoisted and callable before their definition.</p>","trap":"Believing let/const are \"not hoisted.\" They are hoisted — they just aren't initialized, which is what the TDZ enforces.","worked":"<pre><code>console.log(a); // undefined\nvar a = 1;\nconsole.log(b); // ReferenceError (TDZ)\nlet b = 2;</code></pre>","related":["var-let-const","closure"]},
  "var-let-const": {"term":"var / let / const","group":"FE-JS","eli5":"Three ways to declare variables with different scope and reassignment rules.","memory":"var leaks (function), let/const block; const locks the binding, not the value.","full":"<p><code>var</code> is function-scoped, hoisted to <code>undefined</code>, redeclarable, and (in scripts) attaches to <code>globalThis</code> at top level. <code>let</code> and <code>const</code> are block-scoped, TDZ-bound, and non-redeclarable in the same scope.</p><p><code>const</code> forbids <em>rebinding</em> the variable but does not freeze the value — a <code>const</code> object's properties are still mutable. Modern style: <code>const</code> by default, <code>let</code> when reassignment is needed.</p>","trap":"Thinking `const` makes data immutable. It only locks the binding; use <code>Object.freeze</code> (shallow) for value immutability.","worked":"<pre><code>const u = { n: 1 };\nu.n = 2;        // OK — mutating value\nu = {};         // TypeError — rebinding const</code></pre>","related":["hoisting-tdz","immutability"]},
  "prototype-chain": {"term":"Prototype Chain","group":"FE-JS","eli5":"If an object lacks a property, JS looks it up on its prototype, then that one's prototype, and so on.","memory":"Lookup walks the chain until found or null.","full":"<p>Each object has an internal <code>[[Prototype]]</code> link. Property reads that miss on the object delegate up the <strong>prototype chain</strong> until found or it reaches <code>null</code>. This is how JS does inheritance — by <em>delegation</em>, not copying.</p><p>A constructor's <code>.prototype</code> object becomes instances' prototype, letting them share methods. Set links with <code>Object.create</code>, class <code>extends</code>, or (sparingly) <code>Object.setPrototypeOf</code>.</p>","trap":"Confusing a function's <code>.prototype</code> property (used when called with <code>new</code>) with an object's actual <code>[[Prototype]]</code> link (its <code>__proto__</code>).","worked":"<pre><code>const a = { greet() { return 'hi'; } };\nconst b = Object.create(a);\nb.greet();              // 'hi' (inherited)\nObject.hasOwn(b, 'greet'); // false</code></pre>","related":["class-fields","this-binding"]},
  "class-fields": {"term":"Class Fields & #private","group":"FE-JS","eli5":"Modern class syntax with per-instance fields, truly private members, and class-level statics.","memory":"# means hard-private, enforced by the engine, not a naming convention.","full":"<p>Class syntax sugars prototypes but adds real features: <strong>public fields</strong> (per-instance init), <strong>private members</strong> (<code>#x</code>, engine-enforced and inaccessible outside the class body), and <strong>static</strong> members/blocks on the class itself.</p><p>Class bodies run in strict mode, methods are non-enumerable, and the declaration is TDZ-bound. <code>#field in obj</code> performs a brand check returning a boolean instead of throwing.</p>","trap":"Treating leading-underscore `_private` as real privacy. Only <code>#</code> fields are enforced; <code>_x</code> is just a convention and fully accessible.","worked":"<pre><code>class Acct {\n  #bal = 0;\n  deposit(n) { this.#bal += n; return this.#bal; }\n}\nnew Acct().#bal; // SyntaxError — truly private</code></pre>","related":["prototype-chain","this-binding"]},
  "es-modules": {"term":"ES Modules vs CommonJS","group":"FE-JS","eli5":"ESM is the static, async standard module system; CommonJS is Node's older synchronous require().","memory":"ESM = live bindings + static (tree-shakable); CJS = snapshot copy + dynamic.","full":"<p><strong>ES Modules</strong> use <code>import</code>/<code>export</code>, are statically analyzable (enabling tree-shaking), asynchronous with top-level <code>await</code>, always strict, and expose <em>live read-only bindings</em>. <strong>CommonJS</strong> uses <code>require</code>/<code>module.exports</code>, is dynamic and synchronous, and exports a <em>copied snapshot</em>.</p><p>Prefer named exports for refactor-safety and tree-shaking; a default export is just the <code>default</code> key.</p>","trap":"Expecting a CommonJS `require` to reflect later changes to the exporting module's variables — it captured a snapshot. Only ESM bindings stay live.","worked":"<pre><code>// counter.mjs\nexport let n = 0;\nexport const inc = () =&gt; n++;\n// main.mjs\nimport { n, inc } from './counter.mjs';\ninc(); console.log(n); // 1 — live binding</code></pre>","related":["immutability","pure-function"]},
  "shallow-vs-deep-clone": {"term":"Shallow vs Deep Clone","group":"FE-JS","eli5":"Shallow copies share nested objects; deep copies duplicate everything.","memory":"Spread = top layer only; structuredClone = whole tree.","full":"<p><strong>Shallow clones</strong> (<code>{...o}</code>, <code>Object.assign</code>, <code>slice</code>) copy top-level values but share nested references. <strong>Deep clones</strong> duplicate the entire structure. The built-in <code>structuredClone()</code> handles nesting, <code>Date</code>/<code>Map</code>/<code>Set</code>/<code>RegExp</code>/typed arrays, and circular refs.</p><p><code>structuredClone</code> cannot clone functions, DOM nodes, or symbols, and it drops the prototype (class instances become plain objects). The old <code>JSON.parse(JSON.stringify())</code> trick silently drops <code>undefined</code>/functions, mangles <code>Date</code>, and fails on cycles.</p>","trap":"Assuming a spread copy is independent. Mutating a nested object in the copy still affects the original because the reference was shared.","worked":"<pre><code>const a = { n: { x: 1 } };\nconst s = { ...a }; s.n.x = 9; // a.n.x is now 9!\nconst d = structuredClone(a);  // fully independent</code></pre>","related":["immutability","primitive-vs-reference"]},
  "primitive-vs-reference": {"term":"Primitive vs Reference Types","group":"FE-JS","eli5":"Primitives copy their value; objects copy a pointer to the same thing.","memory":"7 primitives copy by value; everything else copies the address.","full":"<p>The <strong>7 primitives</strong> (<code>string</code>, <code>number</code>, <code>boolean</code>, <code>null</code>, <code>undefined</code>, <code>symbol</code>, <code>bigint</code>) are immutable and copied <em>by value</em>. Objects/arrays/functions are <strong>reference types</strong> — variables hold a pointer, so assignment and argument passing share the same underlying object.</p><p>This drives equality (<code>{} === {}</code> is <code>false</code>), mutation visibility, and why cloning matters.</p>","trap":"Thinking passing an object to a function copies it. The reference is copied, so mutating its properties is visible to the caller (though reassigning the parameter is not).","worked":"<pre><code>let a = 1, b = a; b = 2; // a stays 1\nconst x = {v:1}, y = x; y.v = 2; // x.v is 2</code></pre>","related":["shallow-vs-deep-clone","type-coercion"]},
  "type-coercion": {"term":"Type Coercion & Equality","group":"FE-JS","eli5":"JS sometimes auto-converts types when comparing or combining values; === skips that.","memory":"=== checks type+value; == coerces; ~8 falsy values, all else truthy.","full":"<p><strong>Coercion</strong> converts a value to another type, implicitly (<code>==</code>, <code>+</code> with a string, <code>if (x)</code>) or explicitly (<code>Number(x)</code>). Use <code>===</code> to avoid surprises; reserve <code>==</code> for the <code>x == null</code> idiom.</p><p>The <strong>falsy values</strong> are <code>false</code>, <code>0</code>, <code>-0</code>, <code>0n</code>, <code>''</code>, <code>null</code>, <code>undefined</code>, <code>NaN</code> (plus the legacy <code>document.all</code>); everything else — including <code>[]</code>, <code>{}</code>, and <code>'0'</code> — is truthy.</p>","trap":"Assuming `[]` or `'0'` is falsy. Both are truthy; only the listed values are falsy, which breaks many naive guards.","worked":"<pre><code>0 == ''        // true (coerced)\n0 === ''       // false\nBoolean([])    // true (truthy!)</code></pre>","related":["primitive-vs-reference","nullish-optional"]},
  "nullish-optional": {"term":"Optional Chaining & Nullish Coalescing","group":"FE-JS","eli5":"?. safely reads possibly-missing properties; ?? gives a default only for null/undefined.","memory":"?. = don't-throw on nullish; ?? = default only on nullish (keeps 0 and '').","full":"<p><code>?.</code> short-circuits to <code>undefined</code> when the left operand is <code>null</code>/<code>undefined</code>, preventing <code>TypeError</code> on deep property access, indexing, or calls. <code>??</code> returns its right operand only when the left is nullish, unlike <code>||</code> which also triggers on <code>0</code>, <code>''</code>, <code>false</code>, <code>NaN</code>.</p><p>Together they replace verbose guard chains while preserving valid falsy values. Note <code>??</code> can't sit beside <code>||</code>/<code>&amp;&amp;</code> without parentheses.</p>","trap":"Using `|| default` for numeric or string defaults — it wrongly overrides valid <code>0</code> or <code>''</code>. Use <code>??</code> instead.","worked":"<pre><code>const port = cfg.port ?? 8080;  // keeps 0 if set\nconst city = user?.address?.city; // no throw if missing</code></pre>","related":["type-coercion","primitive-vs-reference"]},
  "garbage-collection": {"term":"Garbage Collection & Leaks","group":"FE-JS","eli5":"JS frees memory for things nothing can reach anymore; leaks happen when you keep references by accident.","memory":"Unreachable = collected; listeners/timers/detached DOM = sneaky roots.","full":"<p>JS uses automatic <strong>mark-and-sweep</strong> GC based on <em>reachability</em> from roots (globals, the call stack). You can't force collection — you make objects unreachable.</p><p>Common front-end leaks: un-removed event listeners and <code>setInterval</code>s, detached DOM nodes still referenced in JS, closures retaining large data, and growing module-level caches. Use cleanup (effect teardown, <code>AbortController</code>) and <code>WeakMap</code>/<code>WeakRef</code> for non-owning references.</p>","trap":"Believing GC means leaks are impossible. Reachable-but-unneeded references (timers, listeners, caches) leak indefinitely.","worked":"<pre><code>const id = setInterval(tick, 1000);\n// leak unless: clearInterval(id) on teardown\nuseEffect(() =&gt; () =&gt; clearInterval(id), []);</code></pre>","related":["closure","immutability"]}
});
