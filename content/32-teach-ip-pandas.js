/* Teach me from the ground up: IP-PANDAS (auto-assembled) */
enh({
  "outer-merge-indicator": {"teach":"<p>At its core, <code>merge(how='outer', indicator=True)</code> is just a way to staple two lists together so that <strong>nobody gets left behind</strong> — and then tag every row with a little sticker saying which list it came from.</p><h4>What a merge even is</h4><p>A merge (a join) lines up two tables by some shared column — say a trade id. For each id, pandas tries to find a partner row in the other table and glues them side by side. The interesting question is: what do you do with rows that have <em>no</em> partner?</p><h4>Dissecting the two arguments</h4><ul><li><code>how='outer'</code> — <strong>keep everyone</strong>. Every key from the left table AND every key from the right table survives, even the lonely ones with no match. (The default, <code>'inner'</code>, keeps only keys present in <em>both</em>.)</li><li><code>indicator=True</code> — add a new column called <code>_merge</code>, a categorical that labels each row <code>left_only</code>, <code>right_only</code>, or <code>both</code>.</li></ul><div class=\"analogy\">Picture a school dance with two buses arriving: your firm's internal records and the exchange's fills. An <strong>outer</strong> join puts every single kid on the dance floor, matched or not. The <code>_merge</code> sticker on each kid's shirt reads \"came on the internal bus only,\" \"exchange bus only,\" or \"both buses brought a copy.\" The kids wearing only-one-bus stickers are exactly the ones you need to investigate.</div><h4>Why this is the heart of reconciliation</h4><p>In trade operations you compare your internal book against an external truth source (the exchange). The three labels map perfectly onto the three things that can go wrong or right:</p><ul><li><code>left_only</code> = you recorded a trade the exchange has no record of.</li><li><code>right_only</code> = the exchange filled something you never captured internally.</li><li><code>both</code> = matched key — now you go one level deeper and check the economics (price, quantity, fees) are within tolerance.</li></ul><p>The beautiful part: one line, <code>brk['_merge'].value_counts()</code>, becomes a live break dashboard. A glance tells you how many trades are unmatched on each side today.</p><h4>Why it matters in practice</h4><p>Reconciliation exists <em>specifically</em> to surface the unmatched rows. If you quietly throw them away, you've defeated the entire purpose.</p><div class=\"key\">Outer + indicator is the reconciliation primitive: it keeps every row from both sides and labels its origin, so a single <code>value_counts()</code> on <code>_merge</code> shows you exactly what didn't match. Inner join would silently hide those breaks.</div>"},
  "anti-join": {"teach":"<p>At its core, an anti-join is just answering one question: <strong>\"Which of my rows have no friend on the other side?\"</strong> You keep only the lonely rows — the ones with no match.</p><h4>The thing pandas doesn't give you</h4><p>SQL-flavored databases sometimes have an explicit anti-join, but pandas has no <code>how='anti'</code> keyword. So you build it from parts you already have. The standard idiom is a two-step move:</p><ul><li>Do a <strong>left merge</strong> with <code>indicator=True</code> — keep all left rows, and tag each with <code>_merge</code>.</li><li>Filter to <code>_merge == 'left_only'</code> — those are the left rows that found no partner. Drop the helper column and you're done.</li></ul><p>So <code>m = a.merge(b, on='key', how='left', indicator=True)</code> then <code>anti = m[m._merge=='left_only'].drop(columns='_merge')</code>.</p><h4>The tempting shortcut, and why it's fragile</h4><p>You might reach for <code>a[~a['key'].isin(b['key'])]</code> — \"keep rows whose key is NOT in b.\" It works for a <em>single</em> column key, but it can't express a multi-column key (you can't <code>isin</code> on a pair of columns cleanly), and it mishandles nulls.</p><div class=\"analogy\">Think of a coat check. The anti-join is the pile of coat tickets at the end of the night with <strong>no coat hanging on the matching hook</strong> — and the pile of coats with <strong>no ticket</strong>. Each orphan is a problem someone has to chase down. In trade ops, every settlement break begins life exactly here: a record present on one side only.</div><h4>The NaN trap — read this twice</h4><p>In pandas (and SQL), <strong>NaN never equals NaN</strong>. So if a row has a null match key, it can never match anything — and it falls straight into your anti-join even when a real match was intended. Your break list balloons with rows that aren't truly breaks, they just had a missing id.</p><p>The fix: before joining, quarantine or coalesce rows with null keys. Either route them to a separate \"missing-key\" exception queue, or fill the key from a backup field. Never let silent nulls inflate your break count.</p><h4>Why it matters</h4><p>The anti-join <em>is</em> the break list. Operations teams live and die by it: every unmatched fill, every unreconciled settlement, starts as \"present on one side only.\"</p><div class=\"key\">Anti-join = left-merge with <code>indicator=True</code> then keep <code>left_only</code>. Guard against NaN keys first, because NaN ≠ NaN sneaks genuine rows into your break list.</div>"},
  "match-key": {"teach":"<p>At its core, a match key is just the answer to one question: <strong>\"Which columns do I look at to decide that these two records describe the same trade?\"</strong> Pick the columns that stay identical no matter which system you read them from.</p><h4>Why the choice is everything</h4><p>Reconciliation compares your internal record against an external truth source and pairs them up. The pairing is only as good as the key you join on. A <strong>stable</strong> key is one that means the same thing in both systems and doesn't drift: the exchange/provider trade id (<code>exec_id</code>), the symbol, amount, currency, side, settlement date.</p><div class=\"analogy\">Matching trades is like reuniting lost luggage with travelers. You match on the <strong>booking reference and passport number</strong> — things that are stamped identically in both the airline's system and the traveler's ticket. You do <em>not</em> match on \"the third bag from the left on carousel 4,\" because that's a position that changes the moment anyone moves. A volatile key reunites the wrong people.</div><h4>What makes a good key — and what manufactures false breaks</h4><ul><li><strong>Immutable across systems.</strong> <code>exec_id</code> is assigned once and travels with the trade. A local row number or a database auto-increment id is meaningless to the exchange — joining on it guarantees mismatch.</li><li><strong>Not a float.</strong> Joining on raw price is a classic disaster: <code>100.10</code> internally vs <code>100.1000000001</code> from the feed will never be \"equal,\" so genuine matches fail. Floats are for comparing <em>within</em> tolerance, never for joining.</li><li><strong>Unique enough — use a composite.</strong> One id may not be unique if partial fills reuse it. Combine fields, e.g. <code>on=['exec_id','symbol']</code>, so each real trade maps to exactly one partner.</li></ul><h4>Tolerate the expected, flag the rest</h4><p>Crucially, matching is not about everything being identical. You match on stable identity, then <em>tolerate</em> the discrepancies you expect: timing differences, partial fills, fees, rounding. Only rows that fail to match at all become <strong>breaks</strong>, routed to an exception tool for a human to resolve.</p><h4>Why it matters in practice</h4><p>Choose a bad key and you drown ops in false breaks — real matches that failed because you joined on something volatile. Every false break costs investigation time and erodes trust in the recon itself.</p><div class=\"key\">Join on immutable, deterministic identifiers (composite if needed), never on float or row-local fields. The key decides identity; tolerances handle the expected wiggle.</div>"},
  "dtcc-roles": {"teach":"<p>At its core, DTCC is the plumbing that takes a US stock trade from \"we agreed on a price\" to \"the shares and cash actually changed hands.\" It does this through <strong>three distinct jobs</strong>, and the whole point is to not confuse them.</p><h4>The three roles, in plain terms</h4><ul><li><strong>ITP — Institutional Trade Processing.</strong> The pre-settlement matching layer. After a trade, the buyer's and seller's sides confirm they agree on the details — \"match-to-instruct.\" This step is called <em>affirmation</em>. Think of it as both parties signing off before money moves.</li><li><strong>NSCC — National Securities Clearing Corporation.</strong> The <em>clearer</em>, acting as a <strong>central counterparty (CCP)</strong>. It steps into the middle of every trade (<em>novation</em> — it becomes buyer to every seller and seller to every buyer), nets down all your day's buys and sells per symbol (<em>multilateral netting</em>, via Continuous Net Settlement / CNS), and <strong>guarantees</strong> settlement so you're not exposed if your counterparty defaults.</li><li><strong>DTC — Depository Trust Company.</strong> The <em>depository</em>, a <strong>central securities depository (CSD)</strong>. It actually holds the shares in custody and moves them by <em>book entry</em> (changing electronic ledger entries rather than shipping paper certificates).</li></ul><div class=\"analogy\">Picture an escrow-backed marketplace. <strong>ITP</strong> is the step where buyer and seller both initial the contract to confirm the order details match. <strong>NSCC</strong> is the escrow agent who stands between everyone, tallies up your net obligation for the day (\"you owe 100 shares net, not 4,000 gross\"), and promises the deal will close even if the other party vanishes. <strong>DTC</strong> is the vault that holds all the certificates and simply re-tags which are yours — no physical movement, just an entry change.</div><h4>The flow, end to end</h4><p>Affirm via <strong>ITP</strong> → <strong>NSCC</strong> nets your day's buys and sells per symbol and novates → <strong>DTC</strong> moves the net shares and cash by book entry. Affirmation through ITP <em>gates</em> NSCC/DTC settlement: if you don't affirm in time, settlement stalls.</p><h4>Why T+1 makes this urgent</h4><p>US equities settle on a <strong>T+1</strong> clock (one business day after trade). That compresses the timeline — affirmation through ITP now has to happen <em>same-day</em>, so any pre-settlement break must be caught and fixed fast.</p><h4>The interview trap</h4><p>Don't blur the roles. NSCC is the <strong>clearer/CCP</strong> (it nets and guarantees); DTC is the <strong>depository</strong> (it holds and settles); ITP is the <strong>pre-settlement matching</strong> layer. Calling DTC the clearer or NSCC the depository is the classic giveaway.</p><div class=\"key\">Three jobs, three names: ITP matches before settlement, NSCC nets-and-guarantees as the CCP, DTC holds-and-settles as the CSD. Affirmation gates settlement, and T+1 makes it same-day.</div>"},
  "groupby-agg": {"teach":"<p>At its core, <code>groupby</code> + <code>agg</code> is just \"sort your rows into buckets, then compute one number per bucket.\" Sort the fills by symbol, then add up the quantity in each pile — and you've turned a list of trades into a position snapshot.</p><h4>The split–apply–combine idea</h4><p>Every groupby follows three beats:</p><ul><li><strong>Split:</strong> <code>groupby('symbol')</code> partitions your rows into one group per symbol.</li><li><strong>Apply:</strong> <code>agg</code> reduces each group down to a summary number (a sum, mean, count).</li><li><strong>Combine:</strong> pandas stitches the per-group results back into one tidy table, one row per symbol.</li></ul><p>The modern, readable form is <strong>named aggregation</strong>: <code>agg(net_qty=('signed','sum'))</code> reads as \"make a column <code>net_qty</code> by summing the <code>signed</code> column within each group.\"</p><div class=\"analogy\">Imagine dumping a day's trade tickets onto a table and sorting them into labeled trays, one tray per symbol. <code>agg</code> is you walking down the row of trays adding up each one and writing the total on a sticky note. The wall of sticky notes is your position snapshot.</div><h4>The sign trap — the one that bites</h4><p>Here's the mistake everyone makes once. If you just <code>sum</code> the raw quantity, a buy of 100 and a sell of 100 add up to <strong>200</strong>, not the <strong>0</strong> net position you actually hold. Quantity has no direction until you give it one. So you sign it <em>first</em>:</p><p><code>f['signed'] = f.qty * f.side.map({'BUY':1,'SELL':-1})</code></p><p>then <code>f.groupby('symbol').agg(net=('signed','sum'))</code>. Now buys and sells correctly cancel.</p><p>A second, quieter trap: <code>sum</code> silently skips <code>NaN</code>. If some quantities are missing, your net is undercounted and nothing warns you. Validate for nulls before you trust the total.</p><h4>Three cousins worth knowing</h4><ul><li><code>agg</code> — collapses each group to one row (position totals).</li><li><code>transform</code> — returns a result <strong>aligned back to the original rows</strong>, same length as the input. Perfect for ratios and normalization (\"this fill as a fraction of the symbol's daily volume\").</li><li><code>filter</code> — keeps or drops <strong>whole groups</strong> based on a condition (\"only symbols with more than 10 fills\").</li></ul><div class=\"key\">groupby/agg is split–apply–combine: bucket by key, reduce per bucket. For positions, sign quantity by side <em>before</em> summing, and remember <code>sum</code> skips NaN.</div>"},
  "merge-asof": {"teach":"<p>At its core, <code>merge_asof</code> is a join that says \"don't make me match the timestamp <em>exactly</em> — just give me the closest one that came <strong>just before</strong>.\" It's the tool for gluing together two feeds whose clocks never line up to the microsecond.</p><h4>Why ordinary merge fails here</h4><p>A normal merge needs keys to be <em>equal</em>. But a fill stamped <code>10:00:00.123456</code> will almost never find a quote stamped at that exact instant — quotes tick on their own schedule. An equality join would match almost nothing. <code>merge_asof</code> joins on the <strong>nearest</strong> key instead.</p><h4>Dissecting the call</h4><ul><li><code>on='ts'</code> — the key to match on, usually time.</li><li><code>direction='backward'</code> (the default) — for each fill, grab the <strong>last row at-or-before</strong> it. That's exactly \"what was the prevailing quote/mark at the moment I traded?\"</li><li><code>by='symbol'</code> — match <em>within</em> instrument, so an AAPL fill only sees AAPL quotes.</li><li><code>tolerance=pd.Timedelta('1s')</code> — refuse matches older than this, so you don't staple a fill to a quote from before an outage.</li></ul><div class=\"analogy\">Imagine timestamping a photo (your fill) and wanting to know the temperature at that moment. The weather station logs every few minutes, never at your exact click. <code>merge_asof</code> with <code>direction='backward'</code> grabs the <strong>most recent reading before the photo</strong>. <code>by='symbol'</code> is \"use the thermometer in <em>this</em> city, not another.\" <code>tolerance</code> is \"if the last reading is over an hour old, the station was down — give me nothing rather than a stale lie.\"</div><h4>The two hard requirements</h4><ul><li><strong>Both inputs must be sorted on the key.</strong> Unsorted input raises a <code>ValueError</code>. This isn't optional — the algorithm walks both tables in time order.</li><li><strong>Set <code>by=</code> and <code>tolerance=</code> deliberately.</strong> Forget <code>by</code> and you'll stamp a fill with <em>another symbol's</em> quote. Forget <code>tolerance</code> and a stale quote from across an outage gets matched as if it were fresh.</li></ul><h4>Where you actually use it</h4><p>This is the backbone of <strong>transaction cost analysis (TCA)</strong> and slippage: align each fill with the prevailing market price to measure how far you traded from fair value. Also for time-aligned reconciliation across two venues whose clocks differ.</p><div class=\"key\"><code>merge_asof</code> joins on nearest-earlier time, not equality. Sort both sides, set <code>by</code> to scope per-instrument, and set <code>tolerance</code> to reject stale matches — that's what makes fill-vs-quote alignment correct.</div>"},
  "money-precision": {"teach":"<p>At its core, this is one uncomfortable fact: <strong>computers can't store most decimal money amounts exactly.</strong> A binary float can nail $0.5 but not $0.10 — and that tiny error, summed over thousands of trades, fabricates reconciliation breaks that don't really exist.</p><h4>The classic demonstration</h4><p>Type <code>0.1 + 0.2</code> in Python and you get <code>0.30000000000000004</code>. That's not a bug — <code>float64</code> is base-2, and <code>0.1</code> has no exact binary representation, the same way <code>1/3</code> has no exact decimal one. Every operation carries a little dust.</p><div class=\"analogy\">Imagine a ruler marked only in halves, quarters, eighths — powers of two. Ask it to measure exactly one-tenth of a meter and it can't; it gives you the nearest tick and a sliver of error. Now measure ten thousand tenths and add them: the slivers pile into a visible gap. A penny here, a penny there, and your book disagrees with the exchange's by an amount that is <em>pure arithmetic noise</em>.</div><h4>The two correct fixes</h4><ul><li><strong>Integer minor units (best).</strong> Store money as whole numbers of the smallest unit: cents for dollars, <code>1e-8</code> (sats) for crypto. <code>$10.05</code> becomes <code>1005</code>. Integers are exact and fast, and they vectorize beautifully in pandas.</li><li><strong><code>Decimal</code> objects.</strong> Python's <code>Decimal</code> stores base-10 exactly, so <code>0.1 + 0.2</code> is truly <code>0.3</code>. The cost: it lives in a column of <code>dtype=object</code>, so it's <strong>unvectorized and slow</strong> — fine for a final ledger total, painful for millions of rows.</li></ul><h4>If you're stuck with float</h4><p>Don't compare with <code>==</code>. Use a tolerance: <code>np.isclose(px_int, px_exch, atol=0.005)</code> means \"equal to within half a cent.\" This does double duty — in recon you <em>want</em> a tolerance anyway, because fee rounding and sub-cent differences are expected and shouldn't be flagged as breaks.</p><h4>The trap to never fall into</h4><p>Using <code>==</code> on float prices or amounts in a reconciliation. It will flag rows that differ only by floating-point dust as genuine breaks, burying your real problems under noise and eroding trust in the whole process.</p><div class=\"key\">Floats can't hold decimals like 0.10 exactly, so summed money drifts. Store amounts as integer minor units (or <code>Decimal</code> for exactness), and never use <code>==</code> on float money — compare with a tolerance.</div>"},
  "tz-aware": {"teach":"<p>At its core, a tz-aware timestamp is a time that <strong>carries its own zip code</strong>. It doesn't just say \"9:30\" — it says \"9:30 in New York\" — so you can never accidentally compare a New York open to a London close as if they were the same instant.</p><h4>The naive vs aware distinction</h4><p>A <em>naive</em> timestamp is just \"2026-06-28 09:30\" with no zone attached — you can't tell what real instant it points to. An <em>aware</em> timestamp pins it to an actual moment on the global clock. Two pandas operations bridge the gap, and confusing them is a common slip:</p><ul><li><code>tz_localize('America/New_York')</code> — <strong>attaches</strong> a zone to a naive time. \"This 9:30 was always New York time.\" The wall-clock number doesn't change; you're declaring what it meant.</li><li><code>tz_convert('America/New_York')</code> — <strong>shifts</strong> an already-aware time into a different zone. The instant stays the same; the displayed number changes.</li></ul><div class=\"analogy\">Naive timestamps are photos with no location data — \"taken at 3pm,\" but 3pm <em>where</em>? <code>tz_localize</code> is you writing the city on the back of the photo. <code>tz_convert</code> is asking \"what time was it in Tokyo when this was taken?\" — same moment, different clock on the wall.</div><h4>The discipline: store UTC, convert at the edges</h4><p>The rule professionals follow: <strong>store and compute in UTC</strong> (<code>pd.to_datetime(s, utc=True)</code>), and only convert to a market zone (<code>dt.tz_convert('America/New_York')</code>) for session logic or display. UTC has no daylight-saving jumps, so arithmetic and sorting are clean.</p><h4>Why markets make this sharp</h4><ul><li><strong>Equities</strong> run Eastern-Time sessions on a T+1 clock — session boundaries matter.</li><li><strong>Crypto</strong> is 24/7 with no daily reset — there is no \"close.\"</li><li><strong>Prediction markets</strong> settle on <strong>event resolution</strong>, not a clock — so keep <code>settle_ts</code> distinct from <code>exec_ts</code>; they answer different questions.</li></ul><h4>Two traps</h4><p>First, comparing a tz-aware timestamp to a tz-naive one raises a <code>TypeError</code> — pandas refuses to guess. Second, and nastier: trusting an \"ET\" column that is actually <strong>UTC mislabeled as ET</strong>. The numbers look plausible but every session-relative feature is silently wrong. The cure is to re-derive ET from the raw exchange timestamp rather than trusting a pre-labeled column.</p><div class=\"key\">Keep timestamps tz-aware, store/compute in UTC, convert to market zones only at the edges. <code>tz_localize</code> attaches a zone; <code>tz_convert</code> shifts one — and never trust an \"ET\" column you didn't derive yourself.</div>"},
  "performance-large-files": {"teach":"<p>At its core, making big trade files fast comes down to three habits: <strong>load less, use compact types, and read columnar formats.</strong> Most \"pandas is slow\" complaints are really \"I asked pandas to do the slow thing.\"</p><h4>Where the time and memory go</h4><p>A day of fills can be millions of rows. By default pandas loads <em>every</em> column as a roomy type — strings as Python objects, ids as 64-bit ints — and CSV forces it to re-parse text on every read. The levers that actually move the needle:</p><ul><li><strong><code>usecols=</code></strong> — only read the columns you need. The cheapest data is the data you never load.</li><li><strong>Explicit <code>dtype=</code></strong> — skip type inference and pin compact types up front.</li><li><strong><code>category</code> dtype</strong> — for low-cardinality repeated strings like symbol, venue, side. Pandas stores each distinct value once and uses small integer codes, slashing memory and speeding group-bys.</li><li><strong>Parquet over CSV</strong> — columnar, typed, compressed, and supports <em>predicate/column pushdown</em> (read only the columns and even row-groups you ask for). No re-parsing text.</li><li><strong><code>chunksize=</code></strong> — stream the file in pieces instead of loading it whole.</li><li><strong>Offload the join</strong> — when data exceeds RAM, push the heavy work to <strong>DuckDB, Polars, or Dask</strong>, which are built for out-of-core and parallel joins.</li></ul><div class=\"analogy\">CSV is a printed phone book: to find every plumber you re-read every page top to bottom. Parquet is a spreadsheet where each column is its own indexed sheet — you flip straight to the \"profession\" column, grab only plumbers, and never touch the other columns. <code>category</code> is the phone book printing \"New York\" once and writing a tiny code everywhere else instead of spelling it out a million times.</div><h4>Diagnose before you optimize</h4><p>Run <code>df.memory_usage(deep=True)</code> to see which columns are the hogs — usually object/string columns that should be <code>category</code>. For very large reconciliations, do the outer join in <strong>SQL/DuckDB</strong> and pull only the <em>breaks</em> into pandas; you rarely need the matched millions in memory.</p><h4>The anti-pattern that earns the blame</h4><p>Reaching for CSV + object dtypes + <code>apply</code> or <code>iterrows</code> over millions of rows, then concluding pandas can't scale. <code>iterrows</code> is a Python loop in disguise — orders of magnitude slower than vectorized operations or a SQL join.</p><div class=\"key\">Load less (<code>usecols</code>), type smart (<code>dtype</code>, <code>category</code>), read Parquet not CSV, and offload huge joins to DuckDB/Polars — pulling only breaks into pandas. The slowness is almost always the approach, not the library.</div>"},
  "dtypes-ingest": {"teach":"<p>At its core, setting dtypes on ingest is just <strong>telling pandas the truth about each column before it guesses wrong.</strong> Left to itself, <code>read_csv</code> sniffs the data and frequently mis-types it — and those mistakes corrupt your data silently.</p><h4>The four ways inference betrays you</h4><ul><li><strong>Leading zeros vanish.</strong> A CUSIP or account number like <code>007</code> looks numeric, so pandas makes it <code>int64</code> — and <code>007</code> becomes <code>7</code>. The identifier is now wrong and will never match the source system.</li><li><strong>One bad cell poisons the column.</strong> A price column with a single stray <code>\"N/A\"</code> can't be all-numeric, so pandas demotes the <em>whole</em> column to <code>object</code> (strings). Now <code>price * qty</code> does <strong>string arithmetic</strong> or errors.</li><li><strong>Missing ints become floats.</strong> Classic <code>int64</code> can't hold <code>NaN</code>, so any integer column with one missing value silently becomes <code>float64</code> — <code>100</code> displays as <code>100.0</code>, and ids gain decimal points.</li><li><strong>Timestamps stay strings.</strong> Unless you parse them, your time column is text and won't sort or subtract correctly.</li></ul><div class=\"analogy\">Imagine a new clerk filing your paperwork and <em>guessing</em> the category of each form by glancing at it. \"This one's all digits — must be a quantity\" — and they shred the leading zeros off your account numbers. Setting <code>dtype=</code> is handing the clerk a labeled filing chart up front: \"account numbers are <strong>text</strong>, keep every digit; prices are <strong>numbers</strong>; this column is a <strong>date</strong>.\" No guessing, no silent damage.</div><h4>The toolkit</h4><ul><li><code>dtype={'cusip':'string','symbol':'category'}</code> — pin identifiers as text (zeros preserved) and low-cardinality fields as category.</li><li><strong>Nullable <code>Int64</code></strong> (capital I) — an integer type that <em>can</em> hold missing values, so ints-with-gaps stay ints instead of becoming floats.</li><li><code>parse_dates=['ts']</code> — turn time columns into real datetimes at load.</li><li><code>pd.to_numeric(col, errors='coerce')</code> — convert what you can and turn the unparseable junk into <code>NaN</code> (quarantining bad cells instead of poisoning the column to object).</li></ul><h4>Why it matters</h4><p>Every one of these failures is <strong>silent</strong> — no error, just quietly wrong data that surfaces later as unmatched trades, broken joins, or a recon full of false breaks traced back to a dropped leading zero.</p><div class=\"key\">Never trust inferred dtypes for financial data. Declare ID columns as <code>string</code>, use nullable <code>Int64</code> for ints-with-missing, parse dates explicitly, and <code>to_numeric(errors='coerce')</code> to quarantine bad cells — because every inference mistake fails silently.</div>"}
});
