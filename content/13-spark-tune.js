/* Tuning & Pitfalls (SPARK-TUNE) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"SPARK-TUNE","front":"How do you right-size a Spark executor's cores and memory?","back":"Aim for <strong>4-5 cores per executor</strong> (<code>spark.executor.cores</code>) — beyond ~5, HDFS/object-store throughput degrades from concurrent-client I/O contention, and one fat executor wastes memory on a single GC heap. Size <code>spark.executor.memory</code> so each concurrent task gets a reasonable slice (cores share the heap), and remember the container also needs <strong><code>spark.executor.memoryOverhead</code></strong> (default ~10% of executor memory, min 384MB) for off-heap, shuffle/network buffers, and Python/Arrow. Several medium executors usually beat a few giant ones."},
  {"sec":"SPARK-TUNE","front":"Why is <code>spark.sql.shuffle.partitions = 200</code> often the wrong default?","back":"200 is a <em>fixed</em> default for post-shuffle (exchange) partitions regardless of data size. On a <strong>small dataset</strong> it over-partitions into tiny tasks (scheduling overhead, small files); on a <strong>large dataset</strong> it under-partitions, causing spill and amplifying skew. Tune it to data volume (target ~100-200MB per partition) or — in Spark 3.x — rely on <strong>AQE</strong> (<code>spark.sql.adaptive.enabled</code>, on by default in 3.2+) to coalesce shuffle partitions automatically at runtime."},
  {"sec":"SPARK-TUNE","front":"What is the small-files problem and how do you fix it on write?","back":"Thousands of tiny output files (often one per task partition) overload the metastore/driver with file listing and produce inefficient, high-overhead reads. Fix on write by reducing output partitions: <code>repartition(n)</code> (full shuffle, even sizes) or <code>coalesce(n)</code> (no shuffle, may be uneven) before <code>write</code>, or run a periodic <strong>compaction</strong> job. Delta/Iceberg offer <code>OPTIMIZE</code>/compaction and bin-packing. Target ~128MB-1GB files."},
  {"sec":"SPARK-TUNE","front":"<code>repartition()</code> vs <code>coalesce()</code> — when does each apply?","back":"<code>repartition(n)</code> does a <strong>full shuffle</strong> to produce <em>n</em> evenly-sized partitions and can increase or decrease the count (and redistribute by key). <code>coalesce(n)</code> only <strong>decreases</strong> the count and avoids a shuffle by merging existing partitions in place — cheaper, but can yield uneven partitions and reduce upstream parallelism (it collapses the preceding stage onto fewer tasks). Use <code>coalesce</code> to cut small files cheaply; use <code>repartition</code> when you need balance or a key-based redistribution."},
  {"sec":"SPARK-TUNE","front":"Why prefer Parquet over CSV/JSON, and what two pushdowns does it enable?","back":"Parquet is <strong>columnar + compressed</strong> with per-column row-group statistics (min/max) and metadata. It enables <strong>projection pushdown</strong> (read only the columns the query needs) and <strong>predicate pushdown</strong> with partition pruning and row-group pruning (skip row groups whose min/max can't match a filter). Row-oriented CSV/JSON must read and parse every byte of every row, defeating both."},
  {"sec":"SPARK-TUNE","front":"Why can a Python UDF wreck Spark performance, and what are the alternatives?","back":"A scalar Python UDF is a <strong>Catalyst black box</strong>: the optimizer can't push predicates/projections through it, and each row is <strong>serialized to a Python worker and back</strong>, breaking whole-stage codegen. Prefer <strong>built-in SQL functions</strong> (<code>pyspark.sql.functions</code>) which stay in the JVM and codegen. If you must run Python, use <strong>Pandas/vectorized UDFs</strong> (Arrow-based, batch-at-a-time) to amortize serialization — but they're still opaque to Catalyst, so a built-in wins when one exists."},
  {"sec":"SPARK-TUNE","front":"Why is <code>collect()</code> on a large DataFrame dangerous?","back":"<code>collect()</code> pulls <strong>every row to the driver JVM heap</strong>, so a multi-GB result OOMs the driver — and the driver dying takes the whole app down. It also funnels all data through one node, destroying parallelism. Use <code>show()</code>/<code>take(n)</code>/<code>limit(n)</code> for inspection, <code>write</code> to storage for results, and aggregate on the cluster first. <code>toPandas()</code> has the same risk. <code>spark.driver.maxResultSize</code> is a guardrail (aborts oversized results), not a fix."},
  {"sec":"SPARK-TUNE","front":"When does caching help, and when does it hurt?","back":"Cache only a DataFrame that is <strong>reused multiple times</strong> (e.g., iterative ML, or a base feeding several branches); for a single linear pass, caching just wastes memory and adds eviction churn. <code>DataFrame.cache()</code> = <code>persist(MEMORY_AND_DISK)</code> (note: <em>RDD</em> <code>cache()</code> defaults to <code>MEMORY_ONLY</code>). Caching is <strong>lazy</strong> — materialized on the first action. Over-caching evicts useful blocks and triggers recompute; <code>unpersist()</code> when done."},
  {"sec":"SPARK-TUNE","front":"What is a broadcast (map-side) join and when is it the right choice?","back":"When one side is <strong>small enough to fit in memory</strong>, Spark collects it to the driver, ships a copy to every executor, and joins map-side — <strong>avoiding the shuffle</strong> of the large side entirely. The optimizer/AQE auto-broadcasts when an estimated side is under <code>spark.sql.autoBroadcastJoinThreshold</code> (default 10MB); force it with <code>broadcast(df)</code>. Pitfall: broadcasting something too big OOMs the driver (it collects the build side first) and every executor."},
  {"sec":"SPARK-TUNE","front":"How does data skew manifest in Spark, and how do you mitigate it?","back":"A few keys hold most of the rows, so after a shuffle a handful of <strong>straggler tasks</strong> run far longer than the rest while the stage waits (long task-time tail in the Spark UI; heavy spill on those tasks). Mitigations: <strong>salting</strong> the hot key (split into N sub-keys, then re-aggregate), <strong>AQE skew-join handling</strong> (<code>spark.sql.adaptive.skewJoin.enabled</code>, default true, splits oversized partitions), broadcasting the small side to dodge the shuffle, or isolating hot keys for separate handling."},
  {"sec":"SPARK-TUNE","front":"In the Spark UI, what signals tell you a stage is shuffle-bound or spilling?","back":"Check the stage's <strong>Shuffle Read/Write</strong> bytes (large = expensive exchange) and the <strong>Spill (Memory)/Spill (Disk)</strong> task metrics — non-zero spill means tasks exhausted execution memory and wrote to disk (slow). The <strong>task-duration distribution</strong> (max vs median in the summary metrics) exposes stragglers/skew. The <strong>SQL tab's plan</strong> shows Exchange nodes and whether a join was broadcast or sort-merge."},
  {"sec":"SPARK-TUNE","front":"Why are wide transformations the main cost in a Spark job?","back":"Wide transformations (<code>groupByKey</code>, <code>join</code>, <code>distinct</code>, <code>repartition</code>) require a <strong>shuffle</strong>: data is repartitioned across the network by key, written to local disk as shuffle files, and fetched back — incurring serialization, disk I/O, and network cost, and forming a <strong>stage boundary</strong>. Narrow transformations (<code>map</code>, <code>filter</code>, <code>select</code>) pipeline within a partition with no exchange. Minimize and right-size shuffles first."},
  {"sec":"SPARK-TUNE","front":"<code>reduceByKey</code>/<code>groupBy</code>+agg vs <code>groupByKey</code> — why the difference?","back":"<code>reduceByKey</code> (RDD) and DataFrame <code>groupBy().agg()</code> do a <strong>map-side combine</strong> (partial aggregation before the shuffle), so far less data crosses the network. <code>groupByKey</code> (RDD) shuffles <strong>every raw value</strong> for a key with no pre-aggregation — it can OOM on a hot key and moves vastly more data. Prefer the aggregating forms; for SQL/DataFrame aggregations Catalyst applies partial aggregation automatically."},
  {"sec":"SPARK-TUNE","front":"What does AQE (Adaptive Query Execution) actually change at runtime?","back":"AQE (Spark 3.x, <code>spark.sql.adaptive.enabled</code>, on by default since 3.2) re-optimizes using <strong>actual shuffle statistics</strong>: it <strong>coalesces</strong> too-many small shuffle partitions, <strong>converts</strong> a sort-merge join to a broadcast join when a side turns out small, and <strong>splits skewed partitions</strong>. It reduces the need to hand-tune <code>spark.sql.shuffle.partitions</code>, but only re-plans at shuffle/exchange boundaries — it can't fix a bad scan or push through a UDF."},
  {"sec":"SPARK-TUNE","front":"What's the partition-count rule of thumb for parallelism and partition size?","back":"Target enough partitions to keep all cores busy — a common heuristic is <strong>2-4x the total executor cores</strong> so tasks pipeline and stragglers don't idle the cluster — while keeping each partition around <strong>100-200MB</strong>. Too few underuses the cluster and risks spill/OOM; too many adds scheduling overhead and produces small files. Check <code>df.rdd.getNumPartitions()</code> and the task count in the UI."}
);

Object.assign(GLOSSARY_DATA, {
  "partitioning": {"term":"Partitioning & Partition Sizing","group":"SPARK-TUNE","aliases":["partition","partitions","spark.sql.shuffle.partitions","repartition","coalesce","number of partitions"],"eli5":"Partitions are the chunks Spark processes in parallel — one task per partition — so their count and size set your parallelism and memory pressure.","memory":"Goldilocks: not too few (idle cores + OOM), not too many (small files + overhead), ~128MB just right.","full":"<p>A partition is the unit of parallelism: one task runs per partition per stage. Too <strong>few</strong> partitions underuse cores and force each task to hold too much data (spill/OOM); too <strong>many</strong> add scheduling overhead and emit small files. Target ~<strong>100-200MB per partition</strong> and enough partitions to cover <strong>2-4x total cores</strong>.</p><p>Input partitions come from file/block sizes (<code>spark.sql.files.maxPartitionBytes</code>, default 128MB); post-shuffle partitions come from <code>spark.sql.shuffle.partitions</code> (default 200). Reshape with <code>repartition(n)</code> (full shuffle, even) or <code>coalesce(n)</code> (no shuffle, decrease-only, possibly uneven). AQE can coalesce post-shuffle partitions automatically.</p>","trap":"\"<code>repartition</code> and <code>coalesce</code> are interchangeable.\" No: <code>coalesce</code> only shrinks and skips the shuffle, so it can collapse upstream parallelism and leave skewed partitions; <code>repartition</code> always shuffles to rebalance.","worked":"Reading a 100GB table at 128MB splits ≈ 800 input partitions/tasks. After a <code>groupBy</code> you drop to the 200 shuffle default — now ~500MB each, likely spilling — so bump <code>spark.sql.shuffle.partitions</code> or rely on AQE coalescing.","related":["shuffle","aqe","skew"]},
  "skew": {"term":"Data Skew","group":"SPARK-TUNE","aliases":["skew","data skew","stragglers","straggler task","hot key","hot partition"],"eli5":"A few keys hold most of the data, so a few tasks run forever while the rest of the cluster sits idle.","memory":"One whale in the partition pool drags the whole stage down.","full":"<p>Skew is an uneven key distribution: after a shuffle, one or a few partitions get most of the rows, so their tasks become <strong>stragglers</strong> — the stage can't finish until they do, and they spill heavily while everything else is done. In the Spark UI it shows as a huge gap between the max and median task duration/shuffle-read for a stage.</p><p>Mitigations: <strong>salting</strong> (append a random bucket to the hot key, aggregate/join in two passes), <strong>AQE skew-join handling</strong> (<code>spark.sql.adaptive.skewJoin.enabled</code>, default true, splits oversized partitions into sub-tasks), <strong>broadcasting</strong> the small side to dodge the shuffle, or separating the hot keys and handling them with a dedicated plan.</p>","trap":"\"More partitions / more executors fixes skew.\" It doesn't — all rows for a hot key still hash to one partition. You must redistribute the key itself (salting) or split the partition (AQE), not just add parallelism.","worked":"A <code>groupBy(\"country\").sum()</code> where 60% of rows are <code>US</code>: the US reducer runs 20x longer. Salt by grouping on <code>country, rand(0,9)</code> for a partial sum, then a second <code>groupBy(\"country\")</code> to combine the 10 buckets — spreading the hot key across 10 tasks.","related":["shuffle","broadcast-join","aqe","partitioning"]},
  "udf-catalyst": {"term":"UDFs vs Catalyst","group":"SPARK-TUNE","aliases":["udf","python udf","pandas udf","vectorized udf","catalyst black box","catalyst optimizer"],"eli5":"A custom UDF is a black box the optimizer can't see into or optimize through, and Python UDFs pay a per-row serialization tax.","memory":"Built-in beats Arrow beats row-by-row Python.","full":"<p>Catalyst optimizes the logical/physical plan and Tungsten generates fused JVM bytecode (<strong>whole-stage codegen</strong>) for built-in expressions. A <strong>UDF is opaque</strong>: Catalyst can't push filters or projections through it and can't reorder around it. A <strong>Python UDF</strong> is worse — each row is serialized from the JVM to a Python worker and back, one row at a time.</p><p>Order of preference: (1) <strong>built-in <code>pyspark.sql.functions</code></strong> / SQL expressions — stay in the JVM, fully optimized; (2) <strong>Pandas/vectorized UDFs</strong> — Arrow-based, process a batch at a time so the serialization cost amortizes; (3) plain Python UDF only as a last resort.</p>","trap":"\"Rewrite the slow Python UDF as a Pandas UDF and you're done.\" Better, but a Pandas UDF is still a Catalyst black box — no pushdown through it. If a built-in/SQL expression exists, it wins on both fronts.","worked":"Replacing <code>@udf def f(x): return x.upper()</code> with <code>F.upper(col)</code> keeps the work in the JVM, preserves codegen, and lets filters push past it — often several-fold faster with zero Python round-trips.","related":["predicate-pushdown","collect-oom"]},
  "collect-oom": {"term":"Driver OOM from collect()","group":"SPARK-TUNE","aliases":["collect","collect oom","driver oom","toPandas","maxResultSize"],"eli5":"collect() drags the whole dataset onto the single driver node, which runs out of memory and kills the app.","memory":"collect() = \"call the whole cluster home to one desk.\"","full":"<p>Actions like <code>collect()</code> and <code>toPandas()</code> pull <strong>every result row to the driver JVM heap</strong>. On anything beyond a small result this OOMs the driver — and unlike an executor failure, the driver dying takes the whole application with it. It also funnels all data through one node, erasing the parallelism you built the cluster for.</p><p>Safer patterns: inspect with <code>show()</code>/<code>take(n)</code>/<code>limit(n)</code>; aggregate or sample on the cluster first; and <strong>write results to storage</strong> (Parquet/Delta) rather than returning them. <code>spark.driver.maxResultSize</code> aborts oversized results early — a guardrail, not a fix. Size the driver heap for the plan/broadcasts, not for holding data.</p>","trap":"\"It worked in dev, so it's fine.\" Dev data was tiny; the same <code>collect()</code>/<code>toPandas()</code> OOMs the driver at production scale. Also note a large <code>broadcast</code> build side is collected to the driver too — same failure mode.","worked":"<code>big_df.toPandas()</code> on a 50GB result OOMs a 4GB driver instantly. Instead <code>big_df.write.parquet(out)</code>, or <code>big_df.groupBy(...).agg(...).toPandas()</code> after the aggregation has shrunk it to a few rows.","related":["broadcast-join","udf-catalyst","caching"]},
  "caching": {"term":"Caching / Persistence","group":"SPARK-TUNE","aliases":["cache","persist","unpersist","cache only reused","MEMORY_AND_DISK"],"eli5":"Cache a dataset only when you'll reuse it several times; otherwise it just wastes memory and evicts things you need.","memory":"Cache for reuse, not for comfort — and unpersist when done.","full":"<p>Spark recomputes a DataFrame's lineage on every action by default. <code>DataFrame.cache()</code> (= <code>persist(MEMORY_AND_DISK)</code>) materializes it so later actions reuse the stored blocks — valuable for <strong>iterative algorithms</strong> or a base reused by several downstream branches. Caching is <strong>lazy</strong>: nothing is stored until the first action triggers materialization.</p><p>Cache is not free: it consumes execution/storage memory and can <strong>evict</strong> blocks other tasks need, causing recompute churn and more GC. For a single linear pass it's pure overhead. Cache the smallest reused frame (filter/select first), pick the right storage level, and <code>unpersist()</code> as soon as you're done.</p>","trap":"\"<code>cache()</code> always means MEMORY_AND_DISK.\" True for DataFrames/Datasets, but <em>RDD</em> <code>cache()</code> defaults to <code>MEMORY_ONLY</code> (no disk fallback — blocks that don't fit are recomputed). Also, <code>cache()</code> is lazy: the first action still pays full compute.","worked":"In an ML loop scoring the same features 50x, <code>features.cache(); features.count()</code> (force materialize) avoids 50 re-reads. For a one-shot ETL pass, dropping the <code>cache()</code> frees memory for shuffles and runs faster.","related":["partitioning","collect-oom","shuffle"]}
});
