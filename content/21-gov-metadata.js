/* Metadata Management (GOV-METADATA) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"GOV-METADATA","front":"The three types of metadata (technical / business / operational)","back":"<ul><li><strong>Technical</strong>: physical reality of data — schema, column types, file/table location, partitions, lineage, formats.</li><li><strong>Business</strong>: human meaning and policy — glossary terms, definitions, ownership/stewardship, classification and sensitivity (PII/PHI), allowed use.</li><li><strong>Operational</strong>: runtime behavior — freshness, last-run status, row volume/deltas, quality scores, cost, and usage/access frequency.</li></ul>An interview answer should map each type to who consumes it: technical→engineers/tools, business→stewards/analysts, operational→on-call/SRE-for-data."},
  {"sec":"GOV-METADATA","front":"Active metadata vs. passive cataloging","back":"A <strong>passive catalog</strong> is a searchable inventory you read — a system of record you query when you remember to. <strong>Active metadata</strong> is bidirectional and event-driven: it's continuously emitted, and it is <em>pushed back out</em> to drive automation — auto-classifying PII, triggering policy enforcement, firing freshness alerts, and powering recommendations. The litmus test: does metadata <em>do</em> something on its own, or just sit there waiting to be looked up?"},
  {"sec":"GOV-METADATA","front":"Why is the metadata layer called the 'control plane' of a data platform?","back":"Borrowed from networking: the <strong>data plane</strong> moves and transforms records (warehouse, lake, pipelines); the <strong>control plane</strong> decides policy and routes behavior. The catalog/metadata layer is the control plane because masking rules, access grants, retention, quality gates, and alerting are all <em>driven by metadata</em> (classification tags, ownership, freshness SLAs) rather than hardcoded in each engine. Change a sensitivity tag in one place and enforcement propagates everywhere."},
  {"sec":"GOV-METADATA","front":"Concretely, what does 'metadata-driven governance' replace?","back":"It replaces governance hardcoded per-system (a hand-maintained list of PII columns in each pipeline, per-table grant scripts). Instead policy is expressed against <strong>metadata attributes</strong>: <code>mask WHERE classification = 'PII'</code>, <code>grant WHERE domain = 'finance' AND role = 'analyst'</code>. New tables inherit policy automatically once tagged, so enforcement scales without touching every consumer — the gotcha is it only works if classification coverage is near-complete."},
  {"sec":"GOV-METADATA","front":"How does active metadata auto-classify and tag PII at scale?","back":"A profiling/scanner job samples column values and names, applies <strong>classifiers</strong> (regex/dictionary for SSN/email, name/address NER, statistical fingerprints, and ML models), and writes back <code>classification</code> / <code>sensitivity</code> tags as metadata. The active part: that tag then <em>triggers</em> downstream enforcement (dynamic masking, row filters, access policy) without a human in the loop. Because sampling only reads a subset of rows, deep-scan free-text/JSON columns and route low-confidence detections to a steward queue — the dangerous failure is a missed tag (false negative), since unmasked PII leaks silently."},
  {"sec":"GOV-METADATA","front":"What operational metadata signals power data freshness alerting?","back":"The core trio: <strong>last successful load timestamp</strong> (is the table stale vs. its SLA?), <strong>row count / volume delta</strong> (did a load land empty or 10x?), and <strong>last-run status</strong> of the producing job. Active metadata turns these into alerts by comparing against an expected cadence (e.g., \"hourly table older than 90 min\") rather than waiting for a consumer to notice. This is the data-platform analog of infra observability but keyed on dataset SLAs."},
  {"sec":"GOV-METADATA","front":"Three mechanisms for harvesting technical metadata into a catalog","back":"<ul><li><strong>Crawlers/scanners</strong>: poll source systems (information_schema, file listings) on a schedule — simple, but lossy and lagged.</li><li><strong>Query-log parsing</strong>: parse warehouse SQL history to reconstruct column-level lineage and usage frequency.</li><li><strong>Push/event hooks (OpenLineage)</strong>: pipelines emit run events with inputs/outputs at execution time — most accurate and timely, and the basis for active metadata.</li></ul>Senior point: push-based emission is what makes lineage real-time and trustworthy; crawling alone always drifts."},
  {"sec":"GOV-METADATA","front":"Column-level vs. table-level lineage — why the distinction matters","back":"<strong>Table-level</strong> lineage says \"table B was built from table A.\" <strong>Column-level</strong> lineage says \"<code>B.revenue</code> = <code>A.price * A.qty</code>.\" Column-level is what makes lineage operationally useful: precise <em>impact analysis</em> (which dashboards break if I drop this column), targeted PII propagation (sensitivity inherited only by derived columns), and root-cause for a single bad metric. It's far harder to capture — it requires parsing SQL/transform logic, not just job I/O."},
  {"sec":"GOV-METADATA","front":"What is OpenLineage and why does it matter for active metadata?","back":"<strong>OpenLineage</strong> is an open standard + spec for emitting lineage as <code>RunEvent</code>s (START/COMPLETE/FAIL/ABORT) carrying job, run, and dataset facets — including input/output datasets and optional column-level and data-quality facets. It standardizes how orchestrators (Airflow, Spark, dbt) <em>push</em> lineage to any backend (Marquez, commercial catalogs). It's load-bearing for active metadata because it makes lineage and run status event-driven and vendor-neutral rather than scraped after the fact."},
  {"sec":"GOV-METADATA","front":"How do data quality scores become active metadata?","back":"A quality engine (e.g., Great Expectations, dbt tests, Soda) runs assertions and writes per-dataset results — pass rate, failing expectations, a rolled-up <strong>quality score</strong> — back to the catalog as operational metadata. Active use: the score <em>gates</em> behavior — circuit-break a downstream pipeline, demote the dataset's trust badge in the catalog UI, or route an alert to the owner. Passive use would just display the score and hope someone reads it."},
  {"sec":"GOV-METADATA","front":"What is a business glossary and how does it bind to physical assets?","back":"A <strong>business glossary</strong> is the curated set of agreed-upon terms and definitions (e.g., \"Active Customer = purchased in trailing 90 days\") with an owner per term. Its value comes from <strong>binding</strong>: linking each term to the specific columns/metrics that implement it, so a definition isn't just prose — it's traceable to the physical asset and reused in semantic-layer metrics. Unbound glossaries rot into a wiki nobody trusts; the binding is the whole point."},
  {"sec":"GOV-METADATA","front":"Ownership vs. stewardship in metadata — and why both are tracked","back":"<strong>Ownership</strong> is accountability for a data asset's existence and SLAs (usually the producing team/engineer); <strong>stewardship</strong> is accountability for its <em>meaning and quality</em> (definitions, classification, fitness — often a domain/business steward). Catalogs track both as first-class metadata so alerts and access requests route to the right human. A common interview trap: conflating the two — the on-call owner who fixes a broken pipeline is rarely the steward who decides what the metric should mean."},
  {"sec":"GOV-METADATA","front":"How does metadata coverage and 'metadata debt' undermine governance?","back":"Metadata-driven controls fire only when the keying attribute is present, so <strong>coverage gaps are security/quality holes</strong>: an unclassified column escapes masking; a table with no owner has no alert route; missing freshness SLA means staleness goes unnoticed. <strong>Metadata debt</strong> is the accumulating set of untagged, unowned, undocumented assets. Treat coverage (% assets classified/owned) as a tracked KPI, and prefer <em>fail-closed</em> defaults (deny/mask unknown-sensitivity) over fail-open."},
  {"sec":"GOV-METADATA","front":"Why prefer an open metadata standard/API over a single vendor catalog?","back":"Metadata is a cross-cutting control plane — every engine, BI tool, quality framework, and access-control system needs to read/write it. A closed catalog becomes a silo that re-creates the integration problem it was meant to solve. Standards like <strong>OpenLineage</strong> (lineage) and projects like <strong>OpenMetadata</strong>/DataHub (entity model + ingestion + emit APIs) let you push once and integrate many. Staff-level framing: choose the metadata model and API surface first; the UI/catalog is replaceable, the schema and integrations are not."},
  {"sec":"GOV-METADATA","front":"Reverse-pushing metadata back to source systems — what and why?","back":"Active metadata closes the loop by writing enriched attributes <em>back</em> to operational systems: applying <strong>tag-based access policies</strong> in the warehouse (e.g., Snowflake tag-based masking, BigQuery policy tags), syncing glossary descriptions onto columns, or pushing classifications into a DLP/access tool. This is what separates a control plane from a passive inventory — enrichment computed centrally is enforced at the source, so one tag change re-secures every consumer."}
);

Object.assign(GLOSSARY_DATA, {
  "active-metadata": {"term":"Active Metadata","group":"GOV-METADATA","aliases":["active metadata","passive metadata","passive cataloging"],"eli5":"Metadata that doesn't just sit in a catalog to be searched — it gets pushed back out to automatically trigger actions like masking PII or firing freshness alerts.","memory":"Passive = a library card catalog you walk up to. Active = a smoke detector that acts the moment it senses something.","full":"<p><strong>Active metadata</strong> is metadata treated as a continuously-emitted, bidirectional event stream rather than a static inventory. The defining shift from <em>passive cataloging</em> is directionality: a passive catalog is read-mostly (you query it when you remember), while active metadata is both ingested in real time and <em>reverse-pushed</em> to drive automation — auto-classifying and tagging sensitive columns, triggering policy/masking enforcement, gating pipelines on quality scores, and powering recommendations and alerting.</p><p>It is the conceptual backbone of the catalog-as-control-plane idea: enrichment (classification, ownership, SLAs) is computed centrally and then enforced at the source. The practical requirement is event-driven plumbing (push-based lineage via OpenLineage, run events, webhooks) and near-complete coverage, since automation keyed on a tag is only as good as the tag's presence.</p>","trap":"Interviewers probe whether you understand it's <em>bidirectional</em>. \"We have a searchable catalog\" is passive. The active part is metadata flowing OUT to change system behavior automatically — if no action is triggered, it's not active.","worked":"A scanner tags <code>users.ssn</code> as <code>PII</code>; that tag (active metadata) immediately drives a tag-based masking policy in the warehouse and adds the column to a DLP scope — no human ticket, no per-table SQL.","related":["metadata-control-plane","auto-classification","metadata-driven-governance","operational-metadata"]},
  "metadata-control-plane": {"term":"Metadata Layer as Control Plane","group":"GOV-METADATA","aliases":["control plane","catalog as control plane","data plane"],"eli5":"The catalog is where policy and routing decisions live; the warehouses and pipelines (the data plane) just execute what the metadata tells them to do.","memory":"Control plane decides, data plane carries. Metadata = the dispatcher, not the truck.","full":"<p>Borrowing the networking distinction, the <strong>data plane</strong> stores and transforms records while the <strong>control plane</strong> decides policy. In a modern data platform the metadata layer/catalog is the control plane: masking, access grants, retention, quality gating, and alerting are expressed against metadata attributes (classification, ownership, freshness SLA, domain) and propagated to every engine, rather than hardcoded per system.</p><p>This is why the metadata model and its read/write APIs are the architecturally load-bearing choice — change a sensitivity tag once and enforcement re-derives everywhere. The catalog UI is replaceable; the entity model, the tags, and the integration surface that push policy to sources are not.</p>","trap":"Don't describe the catalog as merely \"a place to find datasets.\" The senior framing is that it's where governance <em>decisions</em> are made and from which enforcement is driven — a passive search index is the opposite of a control plane.","worked":"A retention policy <code>delete WHERE classification='PII' AND age > 30d</code> lives in the metadata layer; the warehouse and lake each enforce it against their own data, both reading the same control-plane rule.","related":["active-metadata","metadata-driven-governance","data-classification","openlineage"]},
  "metadata-driven-governance": {"term":"Metadata-Driven Governance","group":"GOV-METADATA","aliases":["metadata-driven governance","attribute-based governance","tag-based policy"],"eli5":"Write governance rules against tags/attributes (\"mask anything classified PII\") instead of maintaining a hand-listed set of columns in every system.","memory":"Govern the <em>attribute</em>, not the <em>asset</em> — tag once, enforce everywhere.","full":"<p><strong>Metadata-driven governance</strong> expresses policy declaratively against metadata attributes rather than enumerating assets. Instead of a per-pipeline list of PII columns or per-table grant scripts, you write <code>mask WHERE classification='PII'</code> or <code>grant WHERE domain='finance' AND role='analyst'</code>. Newly created assets inherit policy automatically the moment they're tagged, so enforcement scales without editing every consumer.</p><p>The hard dependency is classification/ownership <em>coverage</em>: a rule keyed on a tag silently skips anything untagged. This is why senior practitioners pair metadata-driven governance with coverage KPIs and fail-closed defaults (treat unknown sensitivity as sensitive) so gaps don't become open holes.</p>","trap":"The gotcha is the coverage assumption: attribute-based policy is only as strong as tagging completeness. \"What happens to a brand-new unclassified table?\" should be answered with fail-closed, not \"it's fine.\"","worked":"Tag-based masking in Snowflake or BigQuery policy tags: attach a masking policy to the <code>PII</code> tag once; every column that ever carries that tag is masked for unauthorized roles automatically.","related":["data-classification","active-metadata","metadata-control-plane"]},
  "auto-classification": {"term":"Auto-Classification / PII Detection","group":"GOV-METADATA","aliases":["auto-classification","PII detection","data profiling","automated tagging"],"eli5":"A scanning job that profiles columns and automatically guesses which ones hold sensitive data, then tags them.","memory":"Profile, classify, tag, enforce — a smoke detector for PII that wires straight into the sprinklers.","full":"<p><strong>Auto-classification</strong> is the active-metadata pipeline that discovers sensitivity without manual labeling: it samples column values and names and applies a stack of classifiers — regex/dictionary matchers (SSN, email, credit card with Luhn check), NER for names/addresses, statistical fingerprints, and ML models — then writes <code>classification</code> tags back to the catalog. The \"active\" payoff is that the tag immediately triggers enforcement (dynamic masking, access policy, DLP scope).</p><p>Practitioner concerns are sampling depth vs. cost, confidence thresholds, and the false-negative problem in free-text. Mature setups gate low-confidence detections to human review and re-scan on schema change, because a missed tag is a silent compliance gap.</p>","trap":"\"How do you avoid false negatives?\" Sampling only reads a subset of rows, so rare or free-text PII can be missed — never claim 100% recall; pair scanning with deeper unstructured-column scans, lineage propagation of known tags, and human review.","worked":"A nightly profiler reads 10k sampled rows from <code>support_tickets.body</code>, NER flags embedded phone numbers at 0.7 confidence → routed to a steward queue rather than auto-tagged.","related":["data-classification","active-metadata","operational-metadata"]},
  "operational-metadata": {"term":"Operational Metadata","group":"GOV-METADATA","aliases":["operational metadata","freshness","data observability metadata","usage metadata"],"eli5":"The runtime stats about a dataset — when it last loaded, how many rows, whether the job passed, its quality score, and who uses it.","memory":"Three questions: Is it fresh? Is it full? Did the job pass? — plus who's actually using it.","full":"<p><strong>Operational metadata</strong> captures a dataset's runtime behavior: <strong>freshness</strong> (last successful load timestamp vs. its SLA), <strong>volume</strong> (row counts and deltas), <strong>last-run status</strong> of the producing job, <strong>quality scores</strong>, cost, and <strong>usage</strong>/access frequency. It is the fuel for data observability and freshness alerting — the data-platform analog of infra metrics, but keyed on dataset SLAs rather than host health.</p><p>As active metadata it drives behavior: a stale-table or empty-load signal fires an alert to the owner, a low quality score circuit-breaks downstream consumers, and usage stats inform deprecation and certification decisions. Usage also feeds the catalog's trust/popularity ranking so consumers find the canonical asset.</p>","trap":"Operational metadata is distinct from infra observability (covered elsewhere): it's about <em>dataset</em> SLAs (freshness/volume/quality) not CPU/latency. Don't answer a data-freshness question with server metrics.","worked":"An hourly table whose last-load timestamp is 95 minutes old breaches its 90-min freshness SLA → active metadata fires a freshness alert and demotes the table's trust badge in the catalog.","related":["active-metadata","openlineage","metadata-control-plane","metadata-driven-governance"]}
});
