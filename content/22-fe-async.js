/* Async JS & the Event Loop (FE-ASYNC) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"FE-ASYNC","front":"Why is JavaScript called \"single-threaded\" if it can do async work?","back":"JS runs on <strong>one call stack</strong> — only one piece of code executes at any instant, so two functions never run truly in parallel on the main thread. Async work isn't done <em>by JS</em>; it's delegated to the <strong>host environment</strong> (browser/Node C++ APIs, libuv, OS threads): timers, network, I/O, DOM events. When that work completes, the host <strong>enqueues a callback</strong>. The <strong>event loop</strong> then moves callbacks onto the empty stack one at a time.<ul><li><strong>Call stack</strong>: synchronous, LIFO; blocking it (e.g. a long <code>while</code> loop) freezes the UI and blocks every queued callback.</li><li><strong>Concurrency, not parallelism</strong>: interleaving of tasks, not simultaneous execution. (Real parallelism needs <code>Web Workers</code> / Node worker threads, which have separate stacks/heaps and message-pass.)</li></ul>"},
  {"sec":"FE-ASYNC","front":"Walk through one full iteration (\"tick\") of the browser event loop.","back":"<ol><li>Pop <strong>one macrotask</strong> from a task queue and run it to completion (<strong>run-to-completion</strong>: it isn't preempted).</li><li><strong>Drain the entire microtask queue</strong> — run all microtasks, including ones <em>enqueued by other microtasks</em>, until empty (the \"microtask checkpoint\").</li><li>If it's time to render: run <code>requestAnimationFrame</code> callbacks, then style → layout → paint.</li><li>If no task is ready, the loop idles until one arrives.</li></ol><strong>Key invariant</strong>: microtasks always fully drain <em>before</em> the next macrotask and before each render. So a Promise chain resolves before a <code>setTimeout(0)</code> queued earlier, and before the browser repaints. (Microtask checkpoints also occur whenever the stack empties, not only between macrotasks.)"},
  {"sec":"FE-ASYNC","front":"Macrotask vs microtask: what goes in each queue, and which wins?","back":"<strong>Microtasks always run first</strong> and the queue is fully drained between every macrotask.<ul><li><strong>Microtasks</strong>: <code>Promise.then/catch/finally</code> reactions, <code>await</code> continuations, <code>queueMicrotask()</code>, <code>MutationObserver</code> callbacks.</li><li><strong>Macrotasks (tasks)</strong>: <code>setTimeout</code>/<code>setInterval</code>, <code>setImmediate</code> (Node), I/O completions, UI events, <code>MessageChannel</code>/<code>postMessage</code>, initial script evaluation.</li></ul><strong>Danger</strong>: a microtask that keeps scheduling microtasks <em>starves</em> the macrotask queue and rendering indefinitely — the page hangs. Macrotasks can't do that to each other because the loop takes only one per tick and yields to rendering between them."},
  {"sec":"FE-ASYNC","front":"Predict the output: synchronous, setTimeout(0), and Promise.resolve interleaved.","back":"<pre><code>console.log('A');\nsetTimeout(() => console.log('B'), 0);\nPromise.resolve().then(() => console.log('C'));\nconsole.log('D');</code></pre>Output: <strong>A, D, C, B</strong>.<ul><li><code>A</code>, <code>D</code> are <strong>synchronous</strong> — run now on the stack.</li><li><code>C</code> is a <strong>microtask</strong> — drains after the current script (a macrotask) finishes, before any timer.</li><li><code>B</code> is a <strong>macrotask</strong> — runs on a later tick, after the microtask queue is empty.</li></ul><em>Trap</em>: <code>setTimeout(fn, 0)</code> does NOT mean \"run immediately\" — it means \"run as a future macrotask, after the minimum delay AND after all pending microtasks.\""},
  {"sec":"FE-ASYNC","front":"What are the three Promise states and the rules governing transitions?","back":"A Promise is in exactly one state:<ul><li><strong>pending</strong> — initial; not yet settled.</li><li><strong>fulfilled</strong> — has a value (<code>resolve(v)</code> with a non-thenable).</li><li><strong>rejected</strong> — has a reason (<code>reject(e)</code> or a thrown error in the executor/handler).</li></ul><strong>Settled = fulfilled or rejected</strong>. Transitions are <strong>one-way and permanent</strong>: once settled, a Promise can never change state or value. Subsequent <code>resolve</code>/<code>reject</code> calls are no-ops. Reactions registered via <code>.then</code> run as <strong>microtasks</strong>, and run <em>even if attached after</em> the Promise already settled — they're always scheduled, never synchronous. <em>Note</em>: <code>resolve()</code> with a thenable doesn't settle immediately — it <strong>adopts</strong> that thenable's eventual state."},
  {"sec":"FE-ASYNC","front":"How does .then chaining work, and what does it mean that promises \"flatten\"?","back":"Each <code>.then</code> returns a <strong>new promise</strong>, enabling chains. The new promise settles based on the callback's return:<ul><li>Return a <strong>plain value</strong> → next promise fulfills with it.</li><li><strong>Throw</strong> → next promise rejects.</li><li>Return a <strong>thenable/promise</strong> → the chain <em>adopts</em> (\"flattens\") it: the outer waits for the inner to settle, so you never get a nested <code>Promise&lt;Promise&lt;T&gt;&gt;</code>. This is the <strong>assimilation</strong> rule.</li></ul><pre><code>fetch(url)\n  .then(r =&gt; r.json())   // returns a promise — chain waits for it\n  .then(data =&gt; data.id) // plain value\n  .catch(err =&gt; ...);    // one catch handles any upstream reject</code></pre>"},
  {"sec":"FE-ASYNC","front":"How does error propagation work across a Promise chain (.then/.catch/.finally)?","back":"A rejection <strong>skips all subsequent <code>.then</code> fulfillment handlers</strong> and jumps to the next <code>.catch</code> (or the rejection handler of a <code>.then(onFulfilled, onRejected)</code>). After a <code>.catch</code> handles the error, the chain <strong>resumes in the fulfilled state</strong> (the catch's return becomes a value) — unless the catch itself throws/rejects.<ul><li><code>.catch(fn)</code> is sugar for <code>.then(undefined, fn)</code>.</li><li><code>.finally(fn)</code> runs on <em>either</em> outcome, receives <strong>no argument</strong>, and is <strong>transparent</strong>: it passes the original value/rejection through — unless <em>it</em> throws or returns a rejecting promise.</li><li><strong>Trap</strong>: in <code>.then(a, b)</code>, handler <code>b</code> does NOT catch an error thrown <em>by</em> <code>a</code> — both are attached to the same upstream promise. A trailing <code>.catch</code> would.</li></ul>"},
  {"sec":"FE-ASYNC","front":"async/await — what is it really, and what does await actually do?","back":"<code>async</code>/<code>await</code> is <strong>syntactic sugar over promises</strong>. An <code>async</code> function <em>always</em> returns a promise: <code>return v</code> fulfills it, <code>throw</code> rejects it.<br><code>await p</code> does NOT block the thread. It <strong>pauses the async function, registers a continuation as a microtask</strong>, and yields control back to the caller. When <code>p</code> settles, the continuation resumes: fulfilled → expression evaluates to the value; rejected → <code>await</code> <strong>throws</strong>, catchable with <code>try/catch</code>.<ul><li>Even <code>await Promise.resolve(1)</code> defers the rest of the function to a microtask — code after the first <code>await</code> never runs synchronously.</li><li><code>await</code> on a non-thenable wraps it (<code>await 5</code> resolves in one microtask turn, like <code>await Promise.resolve(5)</code>).</li></ul>"},
  {"sec":"FE-ASYNC","front":"Sequential vs parallel async: why is awaiting in a loop a performance anti-pattern?","back":"Each <code>await</code> <strong>waits for the prior one to settle</strong> before starting the next, so independent operations run <em>sequentially</em> and latencies add up.<pre><code>// SLOW — sum of all latencies\nfor (const url of urls) { results.push(await fetch(url)); }\n\n// FAST — overlap; total ≈ slowest request\nconst results = await Promise.all(urls.map(u =&gt; fetch(u)));</code></pre><ul><li>Use sequential awaits only when each step <strong>depends on</strong> the previous result, or you must throttle concurrency.</li><li><strong>Kick off first, await later</strong>: <code>const a = fetch(x), b = fetch(y); await a; await b;</code> overlaps even without <code>Promise.all</code> — the requests start at promise <em>creation</em>, not at <code>await</code>.</li></ul>"},
  {"sec":"FE-ASYNC","front":"Compare Promise.all, allSettled, race, and any.","back":"<ul><li><code>Promise.all([...])</code> — fulfills with an <strong>array of values</strong> when <em>all</em> fulfill; <strong>rejects fast</strong> on the <em>first</em> rejection (siblings keep running — there's no cancellation — but their results are discarded).</li><li><code>Promise.allSettled([...])</code> — <strong>never rejects</strong>; fulfills with <code>{status:'fulfilled', value}</code> / <code>{status:'rejected', reason}</code> for <em>every</em> input. Use when you want all outcomes.</li><li><code>Promise.race([...])</code> — adopts the outcome of the <em>first</em> promise to <strong>settle</strong> (fulfill OR reject). Common for timeouts.</li><li><code>Promise.any([...])</code> — fulfills with the <strong>first fulfillment</strong>; ignores rejections; rejects only if <em>all</em> reject, with an <strong><code>AggregateError</code></strong>.</li></ul><em>Empty array</em>: <code>all</code>/<code>allSettled</code> fulfill immediately; <code>any</code> rejects (<code>AggregateError</code>); <code>race</code> stays pending forever."},
  {"sec":"FE-ASYNC","front":"When does requestAnimationFrame fire relative to microtasks and setTimeout?","back":"<code>requestAnimationFrame(cb)</code> schedules <code>cb</code> to run <strong>just before the next repaint</strong>, in the event loop's <strong>render step</strong> — after the current task and its drained microtasks, aligned to the display refresh (~16.7ms at 60Hz; ~8.3ms at 120Hz).<ul><li>Ordering within a tick: <strong>task → microtasks drain → rAF callbacks → style/layout/paint</strong>.</li><li><strong>vs <code>setTimeout(0)</code></strong>: rAF is synced to the paint cadence and <em>pauses in hidden/background tabs</em>; <code>setTimeout</code> isn't paint-aligned and keeps a throttled cadence when backgrounded.</li><li>The callback receives a <code>DOMHighResTimeStamp</code> and fires <strong>once</strong> — re-schedule inside it for an animation loop.</li><li>rAF runs <em>before</em> paint, so for <strong>after-paint</strong> work, nest a second rAF (<code>rAF(() =&gt; rAF(measure))</code>) — or use the post-render task scheduler where supported.</li></ul>"},
  {"sec":"FE-ASYNC","front":"Implement debounce and throttle — and explain when to use each.","back":"<strong>Debounce</strong>: wait until activity <em>stops</em> for <code>delay</code> ms, then fire once (each call resets the timer). <strong>Throttle</strong>: fire at most once per <code>limit</code> ms during continuous activity.<pre><code>function debounce(fn, delay) {\n  let t;\n  return (...args) =&gt; {\n    clearTimeout(t);\n    t = setTimeout(() =&gt; fn(...args), delay);\n  };\n}\nfunction throttle(fn, limit) {\n  let wait = false;\n  return (...args) =&gt; {\n    if (wait) return;\n    fn(...args); wait = true;\n    setTimeout(() =&gt; { wait = false; }, limit);\n  };\n}</code></pre><ul><li><strong>Debounce</strong>: search-as-you-type, resize-end, autosave — you care only about the <em>final</em> state.</li><li><strong>Throttle</strong>: scroll/mousemove/pointer handlers — you want <em>steady</em>, rate-limited updates.</li></ul>"},
  {"sec":"FE-ASYNC","front":"How do you cancel an in-flight fetch with AbortController?","back":"<code>AbortController</code> exposes a <code>.signal</code> you pass to <code>fetch</code> (and many other signal-aware APIs); calling <code>.abort(reason)</code> rejects the fetch promise with an <strong><code>AbortError</code></strong> (a <code>DOMException</code>) and flips <code>signal.aborted</code> to true.<pre><code>const ac = new AbortController();\nconst t = setTimeout(() =&gt; ac.abort(), 5000);\ntry {\n  const res = await fetch(url, { signal: ac.signal });\n  const data = await res.json();\n} catch (e) {\n  if (e.name === 'AbortError') { /* expected — ignore */ }\n  else throw e;\n} finally { clearTimeout(t); }</code></pre><ul><li>One controller can abort <strong>many</strong> requests sharing its signal.</li><li><strong>2026</strong>: use <code>AbortSignal.timeout(ms)</code> for timeouts and <code>AbortSignal.any([...])</code> to combine signals (e.g. user-cancel + deadline).</li><li>In React, abort in <code>useEffect</code> cleanup to drop stale responses.</li></ul>"},
  {"sec":"FE-ASYNC","front":"What are generators/iterators and how do they relate to async?","back":"A <strong>generator</strong> (<code>function*</code>) returns an <strong>iterator</strong>; <code>yield</code> <em>pauses</em> execution and hands a value out, resuming on the next <code>.next(v)</code> — the only native way in JS to <strong>suspend and resume</strong> a function. <code>.next()</code> returns <code>{value, done}</code>.<ul><li>Any object whose <code>[Symbol.iterator]()</code> returns such an iterator is <strong>iterable</strong> — works in <code>for...of</code> and spread.</li><li><code>async function*</code> + <code>for await...of</code> consume <strong>async iterators</strong> (streaming/paginated data).</li><li>Historically <code>async/await</code> was modeled as generators yielding promises (e.g. the <em>co</em> library); today it's native.</li></ul><pre><code>function* ids() { let i = 0; while (true) yield i++; }\nconst g = ids();\ng.next().value; // 0\ng.next().value; // 1 — lazy, infinite, no array allocated</code></pre>"},
  {"sec":"FE-ASYNC","front":"What does queueMicrotask do and when would you reach for it over setTimeout(0)?","back":"<code>queueMicrotask(fn)</code> schedules <code>fn</code> on the <strong>microtask queue</strong> — it runs after the current synchronous code but <strong>before</strong> the next macrotask, render, or any <code>setTimeout</code>. It's the same queue Promise reactions use, <em>without</em> allocating a promise.<ul><li>Use it to <strong>defer to the end of the current operation</strong> while still completing before the browser yields/paints — e.g. batching notifications, or forcing a callback to <em>always</em> run async (avoid the \"sometimes sync, sometimes async\" footgun).</li><li><strong>vs <code>setTimeout(0)</code></strong>: queueMicrotask is earlier and not clamped, but can <strong>starve rendering</strong> if it recursively re-queues; <code>setTimeout</code> always yields to the loop and lets paint happen.</li><li><strong>vs <code>Promise.resolve().then</code></strong>: equivalent timing, clearer intent, and no spurious unhandled-rejection edge cases.</li></ul>"},
  {"sec":"FE-ASYNC","front":"Why does an async callback inside array.forEach silently break, and what's the fix?","back":"<code>forEach</code> <strong>ignores the promise returned</strong> by an async callback — it awaits nothing. So the loop returns <em>before</em> the async work finishes, and a surrounding <code>await</code> doesn't wait.<pre><code>// BROKEN — \"done\" logs before any save resolves\n[1,2,3].forEach(async id =&gt; { await save(id); });\nconsole.log('done');\n\n// Sequential\nfor (const id of [1,2,3]) await save(id);\n\n// Parallel\nawait Promise.all([1,2,3].map(id =&gt; save(id)));</code></pre><ul><li>Worse: errors thrown in the forEach callback become <strong>unhandled rejections</strong>, invisible to a <code>try/catch</code> around the loop.</li><li>Rule: <code>for...of</code> for sequential awaiting, <code>map</code> + <code>Promise.all</code> for parallel. Never <code>forEach</code> with <code>async</code>.</li></ul>"},
  {"sec":"FE-ASYNC","front":"What is an unhandled promise rejection and how do you guard against it in 2026?","back":"A promise that <strong>rejects with no rejection handler attached</strong> by the time the microtask queue drains triggers an <strong>unhandled rejection</strong>. Browsers fire the <code>window</code> <code>unhandledrejection</code> event; in <strong>Node, the default since v15 is to crash the process</strong>.<ul><li>Common causes: a forgotten <code>.catch</code>, an <code>await</code> with no <code>try/catch</code>, a fire-and-forget promise, or returning a rejecting promise from <code>.finally</code>.</li><li><strong>Float vs await</strong>: <code>doAsync()</code> alone floats the promise — either <code>await</code> it (in try/catch), attach <code>.catch</code>, or signal intent with <code>void doAsync().catch(report)</code>.</li><li>Add a global net for telemetry: <code>window.addEventListener('unhandledrejection', e =&gt; report(e.reason))</code> / Node <code>process.on('unhandledRejection', ...)</code> — and call <code>e.preventDefault()</code> to suppress the default. This is a safety net, not primary handling.</li><li><strong>Subtle</strong>: attaching <code>.catch</code> within the same microtask checkpoint avoids the warning; attaching it a tick later may be too late (Node may already have reported it).</li></ul>"}
);

Object.assign(GLOSSARY_DATA, {
  "event-loop": {"term":"Event Loop","group":"FE-ASYNC","eli5":"A dispatcher that keeps moving waiting callbacks onto the one available worker (the call stack) whenever it's free.","memory":"\"One stack, two queues, repeat forever.\"","full":"<p>The <strong>event loop</strong> is the coordination mechanism that lets single-threaded JS handle async work. Each iteration (\"tick\") runs <em>one macrotask</em> to completion, then <strong>fully drains the microtask queue</strong>, then renders if due. Because there is one call stack, JS never preempts running code — everything is <strong>run-to-completion</strong>.</p><p>The loop lives in the <strong>host</strong> (browser/Node), not the JS engine itself. Long synchronous work blocks the loop and freezes the UI, which is why heavy CPU work belongs in a Web Worker.</p>","trap":"Thinking the event loop gives parallelism. It gives <em>concurrency</em> (interleaving); only Workers run truly in parallel.","worked":"<code>console.log(1); setTimeout(()=&gt;console.log(3)); Promise.resolve().then(()=&gt;console.log(2));</code> → prints 1, 2, 3: sync, then microtask, then macrotask.","related":["microtask","macrotask","call-stack"]},
  "call-stack": {"term":"Call Stack","group":"FE-ASYNC","eli5":"The single to-do list of functions currently running, stacked so the newest finishes first.","memory":"\"LIFO: last in, first out — and there's only one.\"","full":"<p>The <strong>call stack</strong> tracks synchronous execution: calling a function pushes a frame, returning pops it. JS has <strong>exactly one</strong> stack per realm, so two functions never execute at the same instant on the main thread. The event loop places a queued callback on the stack only when it is <strong>empty</strong>.</p><p>Deep recursion overflows it (<code>RangeError: Maximum call stack size exceeded</code>); a long synchronous loop monopolizes it and blocks all async callbacks and rendering.</p>","trap":"Believing async callbacks can \"interrupt\" running sync code. They can't — they wait until the stack is clear.","worked":"<code>function a(){b();} function b(){c();} a();</code> → stack grows a→b→c then unwinds c→b→a.","related":["event-loop","single-threaded"]},
  "microtask": {"term":"Microtask","group":"FE-ASYNC","eli5":"A high-priority follow-up job that runs right after the current code, before anything else.","memory":"\"Micro = immediate; drains completely before the next task or paint.\"","full":"<p><strong>Microtasks</strong> are queued by Promise reactions (<code>.then/.catch/.finally</code>), <code>await</code> continuations, <code>queueMicrotask()</code>, and <code>MutationObserver</code>. At each microtask checkpoint the loop <strong>drains the entire queue</strong> — including microtasks scheduled by other microtasks — before taking the next task or rendering.</p><p>This guarantees Promise chains settle promptly, but a self-scheduling microtask can <strong>starve</strong> rendering and timers, hanging the page.</p>","trap":"Assuming a <code>setTimeout(0)</code> queued before a Promise runs first. The Promise (microtask) always wins.","worked":"<code>queueMicrotask(()=&gt;console.log('mt')); console.log('sync');</code> → 'sync' then 'mt', before any <code>setTimeout</code>.","related":["macrotask","event-loop","queue-microtask","promise"]},
  "macrotask": {"term":"Macrotask (Task)","group":"FE-ASYNC","eli5":"A regular queued job — like a timer or click handler — that runs one-per-loop-tick.","memory":"\"One macrotask per tick, then microtasks flush.\"","full":"<p><strong>Macrotasks</strong> include <code>setTimeout</code>/<code>setInterval</code>, I/O completions, UI events, <code>MessageChannel</code>, and script evaluation. The event loop processes <strong>one macrotask per iteration</strong>, then fully drains microtasks and possibly renders.</p><p>Because only one runs per tick, macrotasks naturally yield to rendering between them — making them safer than recursive microtasks for chunking heavy work across frames.</p>","trap":"Thinking <code>setTimeout(fn, 0)</code> runs immediately. Nested timers are clamped (≥ ~4ms after 5 levels) and it always waits for microtasks first.","worked":"Chunking work: schedule the next batch with <code>setTimeout(processNext, 0)</code> so the browser can paint between batches.","related":["microtask","event-loop","set-timeout-zero"]},
  "promise": {"term":"Promise","group":"FE-ASYNC","eli5":"A placeholder for a value that will arrive later, that you can attach follow-up steps to.","memory":"\"Pending → settled (fulfilled/rejected), and never back.\"","full":"<p>A <strong>Promise</strong> represents the eventual result of an async operation. It is <em>pending</em>, then transitions <strong>once and permanently</strong> to <em>fulfilled</em> (value) or <em>rejected</em> (reason). Handlers attached via <code>.then/.catch/.finally</code> run as <strong>microtasks</strong>, even if attached after the promise settled.</p><p>Each <code>.then</code> returns a new promise, enabling chaining; returning a thenable causes the chain to <strong>flatten</strong> (adopt) it, so you never get a nested <code>Promise&lt;Promise&gt;</code>.</p>","trap":"Expecting <code>.then</code> callbacks to run synchronously when the promise is already resolved — they're always deferred to a microtask.","worked":"<code>Promise.resolve(1).then(v=&gt;v+1).then(console.log)</code> → logs 2, asynchronously.","related":["async-await","microtask","promise-combinators"]},
  "async-await": {"term":"async / await","group":"FE-ASYNC","eli5":"A cleaner way to write promise code that reads like normal step-by-step code.","memory":"\"await = pause-and-resume-as-a-microtask; async fn always returns a promise.\"","full":"<p><code>async</code>/<code>await</code> is <strong>sugar over promises</strong>. An <code>async</code> function returns a promise; <code>await p</code> pauses the function, schedules its continuation as a <strong>microtask</strong>, and yields to the caller — it does not block the thread. On settle, a fulfilled value is returned and a rejection is <strong>thrown</strong> (catch with <code>try/catch</code>).</p><p>Independent awaits run sequentially, so use <code>Promise.all</code> (or start promises before awaiting) to overlap independent work.</p>","trap":"Thinking code after <code>await</code> runs synchronously. The first <code>await</code> always defers the rest to a microtask, even for <code>await Promise.resolve()</code>.","worked":"<code>const [a,b] = await Promise.all([f(), g()]);</code> overlaps f and g; <code>await f(); await g();</code> serializes them.","related":["promise","microtask","promise-combinators","unhandled-rejection"]},
  "promise-combinators": {"term":"Promise Combinators","group":"FE-ASYNC","eli5":"Helpers to wait on many promises at once with different rules for success and failure.","memory":"\"all=every value or first fail; allSettled=every outcome; race=first to settle; any=first to succeed.\"","full":"<p><strong>Combinators</strong> coordinate multiple promises. <code>Promise.all</code> fulfills with all values but rejects on the first rejection (siblings keep running — no cancellation). <code>Promise.allSettled</code> never rejects, returning a status object per input. <code>Promise.race</code> adopts the first <em>settlement</em> (success or failure). <code>Promise.any</code> resolves with the first <em>fulfillment</em>, rejecting with an <code>AggregateError</code> only if all reject.</p><p>Edge cases on an empty array: <code>all</code>/<code>allSettled</code> fulfill, <code>any</code> rejects, <code>race</code> never settles.</p>","trap":"Using <code>Promise.all</code> when you need every result — one rejection discards the others. Use <code>allSettled</code> instead.","worked":"Timeout: <code>Promise.race([fetch(url), timeout(5000)])</code>. Fastest mirror: <code>Promise.any(mirrors.map(fetch))</code>.","related":["promise","async-await","abort-controller"]},
  "set-timeout-zero": {"term":"setTimeout(fn, 0)","group":"FE-ASYNC","eli5":"A way to say \"run this later, after the current work and all promise jobs finish\" — not actually instantly.","memory":"\"Zero means 'next macrotask', not 'now'.\"","full":"<p><code>setTimeout(fn, 0)</code> schedules <code>fn</code> as a <strong>macrotask</strong>. It runs after the current synchronous script <em>and</em> after the microtask queue drains, on a later loop tick. Browsers <strong>clamp</strong> nested timeouts to a minimum (~4ms after 5 levels of nesting) and throttle timers in background tabs.</p><p>Use it to yield to the browser (let it paint, break up long tasks) — not for precise timing or to beat promises.</p>","trap":"Expecting it to run before queued microtasks/Promise callbacks. Microtasks always drain first.","worked":"<code>setTimeout(()=&gt;console.log('B')); Promise.resolve().then(()=&gt;console.log('A'));</code> → A then B.","related":["macrotask","microtask","request-animation-frame"]},
  "request-animation-frame": {"term":"requestAnimationFrame","group":"FE-ASYNC","eli5":"Schedules a callback to run right before the screen's next repaint, synced to the display.","memory":"\"rAF rides the paint cadence; after microtasks, before paint.\"","full":"<p><code>requestAnimationFrame(cb)</code> runs <code>cb</code> in the event loop's <strong>render step</strong>, just before the next repaint, aligned to the refresh rate (~16.7ms at 60Hz, ~8.3ms at 120Hz). The callback receives a high-resolution timestamp and fires <strong>once</strong> — re-schedule for a loop.</p><p>It <strong>pauses in hidden/background tabs</strong>, saving CPU, and batches visual updates to avoid layout thrashing. Order within a tick: task → microtasks → rAF → style/layout/paint.</p>","trap":"Using <code>setInterval</code> for animation. It drifts and isn't paint-synced; rAF is, and pauses when hidden.","worked":"<code>function loop(t){ move(t); requestAnimationFrame(loop);} requestAnimationFrame(loop);</code> — smooth, refresh-synced animation.","related":["macrotask","set-timeout-zero","throttle-debounce"]},
  "throttle-debounce": {"term":"Debounce vs Throttle","group":"FE-ASYNC","eli5":"Debounce waits for the user to stop before acting; throttle acts at a steady max rate during activity.","memory":"\"Debounce = elevator door (resets); throttle = metronome (fixed rate).\"","full":"<p><strong>Debounce</strong> delays the call until <code>delay</code> ms of <em>inactivity</em> — every new event resets the timer, so only the final event fires. <strong>Throttle</strong> guarantees at most one call per <code>limit</code> ms during continuous activity.</p><p>Debounce suits search-as-you-type, resize-end, and autosave (you want the settled state). Throttle suits scroll, mousemove, and pointer tracking (you want steady, rate-limited updates).</p>","trap":"Swapping them — debouncing a scroll handler means it may never fire while scrolling continuously.","worked":"Search box: <code>input.addEventListener('input', debounce(query, 300))</code> — one request after typing stops.","related":["request-animation-frame","macrotask"]},
  "abort-controller": {"term":"AbortController / AbortSignal","group":"FE-ASYNC","eli5":"A remote control to cancel an in-flight fetch or other async operation.","memory":"\"Pass the signal, call abort(), catch AbortError.\"","full":"<p><code>AbortController</code> produces a <code>.signal</code> you hand to <code>fetch</code> (and other signal-aware APIs). Calling <code>.abort(reason)</code> rejects the operation with an <strong><code>AbortError</code></strong> <code>DOMException</code> and flips <code>signal.aborted</code> to true. One signal can cancel many operations.</p><p>As of 2026, prefer <code>AbortSignal.timeout(ms)</code> for deadline cancellation and <code>AbortSignal.any([...])</code> to combine signals (e.g. user-cancel + timeout). In React, abort in effect cleanup to drop stale responses.</p>","trap":"Letting the resulting AbortError bubble as a real error. Detect <code>e.name === 'AbortError'</code> and treat it as expected.","worked":"<code>const ac=new AbortController(); fetch(url,{signal:ac.signal}); ac.abort();</code> → fetch promise rejects with AbortError.","related":["promise-combinators","async-await","unhandled-rejection"]},
  "queue-microtask": {"term":"queueMicrotask","group":"FE-ASYNC","eli5":"Directly schedule a job to run right after the current code but before timers or painting.","memory":"\"Same queue as promises, no promise allocated.\"","full":"<p><code>queueMicrotask(fn)</code> enqueues <code>fn</code> onto the <strong>microtask queue</strong> — running after the current synchronous code and before the next macrotask or render. It's equivalent in timing to <code>Promise.resolve().then(fn)</code> but clearer and without creating a promise.</p><p>Use it to batch work or to guarantee a callback is <em>always</em> async (avoiding the sometimes-sync/sometimes-async bug class), while staying ahead of paint.</p>","trap":"Recursively re-queuing microtasks — this starves rendering and timers, hanging the page. Use <code>setTimeout</code> when you must yield.","worked":"<code>queueMicrotask(()=&gt;flushBatchedUpdates());</code> after collecting synchronous mutations in one operation.","related":["microtask","macrotask","promise"]},
  "unhandled-rejection": {"term":"Unhandled Promise Rejection","group":"FE-ASYNC","eli5":"A promise that failed with nobody listening for the error.","memory":"\"No .catch, no await/try — Node crashes, browser fires unhandledrejection.\"","full":"<p>A rejected promise with <strong>no rejection handler</strong> by the microtask checkpoint becomes an <strong>unhandled rejection</strong>. Browsers fire the <code>unhandledrejection</code> event; <strong>Node terminates the process by default</strong> (since v15). Causes: forgotten <code>.catch</code>, un-try/catch'd <code>await</code>, floated fire-and-forget promises, or a rejecting <code>.finally</code>.</p><p>Add a global handler for telemetry (and <code>preventDefault()</code> to suppress the default), but always handle rejections at the source. Mark intentional floats with <code>void p.catch(report)</code>.</p>","trap":"Assuming a try/catch around a loop catches errors from an async <code>forEach</code> callback. It doesn't — those become unhandled rejections.","worked":"<code>window.addEventListener('unhandledrejection', e =&gt; log(e.reason));</code> as a safety net, plus per-call <code>.catch</code>.","related":["async-await","promise","generator-iterator"]},
  "generator-iterator": {"term":"Generators & Iterators","group":"FE-ASYNC","eli5":"Functions that can pause at yield and hand back values one at a time, on demand.","memory":"\"function* yields; .next() resumes; lazy and pausable.\"","full":"<p>A <strong>generator</strong> (<code>function*</code>) returns an <strong>iterator</strong> whose <code>.next()</code> runs until the next <code>yield</code>, returning <code>{value, done}</code>. It's the only native way to <strong>suspend and resume</strong> a function, enabling lazy/infinite sequences without allocating arrays.</p><p>Objects implementing <code>[Symbol.iterator]</code> are <strong>iterable</strong> (work in <code>for...of</code>/spread). <code>async function*</code> with <code>for await...of</code> drives <strong>async iterators</strong> for streamed or paginated data.</p>","trap":"Thinking generators run eagerly. Nothing executes until you call <code>.next()</code> (or iterate) — they're pull-based and lazy.","worked":"<code>function* range(n){for(let i=0;i&lt;n;i++) yield i;} [...range(3)] // [0,1,2]</code>.","related":["async-await","event-loop"]},
  "single-threaded": {"term":"Single-Threaded Model","group":"FE-ASYNC","eli5":"JavaScript runs your code on one worker at a time, delegating waiting to the system.","memory":"\"One thread runs JS; the host does the waiting.\"","full":"<p>JS executes on a <strong>single main thread</strong> with one call stack and heap per realm. It stays responsive via the event loop: slow operations (timers, network, I/O) are handed to the <strong>host environment</strong>, which signals completion by queuing callbacks. This is <strong>concurrency, not parallelism</strong>.</p><p>True parallelism requires <strong>Web Workers</strong> (browser) or worker threads (Node), which have isolated state and communicate via message passing / <code>SharedArrayBuffer</code>.</p>","trap":"Blaming async APIs for blocking. A frozen UI is almost always synchronous CPU work hogging the single thread, not the async layer.","worked":"A tight <code>while(Date.now() &lt; end){}</code> blocks clicks, timers, and rendering until it finishes — proof of one thread.","related":["call-stack","event-loop"]}
});
