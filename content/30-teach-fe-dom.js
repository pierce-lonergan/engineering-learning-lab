/* Teach me from the ground up: FE-DOM (auto-assembled) */
enh({
  "node-vs-element": {"teach":"<p>At its core, a <strong>node</strong> is just \"any single piece of the page tree,\" and an <strong>element</strong> is the special kind of node that comes from an HTML tag. Every element is a node, but not every node is an element.</p><h4>The family tree of page pieces</h4><p>The browser turns your HTML into a tree of objects. The base type for all of them is <code>Node</code> — it's abstract, meaning you never create a bare <code>Node</code>, you create one of its more specific subtypes. The important subtypes are:</p><ul><li><code>Element</code> — a tag, like <code>&lt;li&gt;</code> or <code>&lt;div&gt;</code> (<code>nodeType</code> 1)</li><li><code>Text</code> — the literal characters between tags, <em>including the spaces and newlines you typed for readability</em> (<code>nodeType</code> 3)</li><li><code>Comment</code> — an HTML <code>&lt;!-- ... --&gt;</code> (<code>nodeType</code> 8)</li><li><code>Document</code> — the root of the whole page (<code>nodeType</code> 9)</li></ul><p>The <code>nodeType</code> number is just a quick integer label so you can tell which subtype you're holding without string comparisons.</p><div class=\"analogy\">Think of a paragraph in a printed book. The <em>words</em> are elements — the meaningful content. But the page also contains blank spaces, line breaks, and the typesetter's margin notes. Those exist physically on the page even though they aren't \"words.\" The DOM is the same: your indentation and line breaks become real <strong>Text nodes</strong> that genuinely live in the tree.</p><h4>Why two parallel sets of APIs exist</h4><p>Because there are two questions you might ask — \"give me all the child <em>pieces</em>\" versus \"give me all the child <em>tags</em>\" — the DOM gives you two parallel families:</p><ul><li><strong>Node-level</strong>: <code>childNodes</code>, <code>firstChild</code>, <code>nextSibling</code> — these see <em>everything</em>, including whitespace text and comments.</li><li><strong>Element-level</strong>: <code>children</code>, <code>firstElementChild</code>, <code>nextElementSibling</code> — these skip past text and comments, returning only tags.</li></ul><p>This is exactly where beginners get bitten. Consider <code>&lt;ul&gt; &lt;li&gt;a&lt;/li&gt; &lt;/ul&gt;</code>. Because you put spaces around the <code>&lt;li&gt;</code>, the <code>&lt;ul&gt;</code> actually has three children: a Text node (the space before), the <code>&lt;li&gt;</code> element, and a Text node (the space after). So <code>ul.childNodes.length</code> is <strong>3</strong>, while <code>ul.children.length</code> is <strong>1</strong>. If you wrote <code>ul.firstChild</code> expecting the <code>&lt;li&gt;</code>, you'd instead get a whitespace text node — and calls like <code>.classList</code> on it would throw.</p><p>In practice: whenever you mean \"the actual tags,\" reach for the element-level APIs (<code>children</code>, <code>firstElementChild</code>). Reserve <code>childNodes</code> for when you genuinely need to inspect or manipulate text and comment content.</p><div class=\"key\">A node is any piece of the page tree; an element is the tag-shaped subtype. <code>childNodes</code> includes whitespace text and comment nodes from your formatting — use <code>children</code> / <code>firstElementChild</code> when you only want elements.</div>"},
  "live-vs-static": {"teach":"<p>At its core, some DOM collections are <strong>live</strong> — they keep watching the page and silently update themselves — while others are <strong>static</strong>: a frozen photograph of the matches that existed the instant you asked.</p><h4>Which is which</h4><ul><li><strong>Live</strong>: <code>getElementsByTagName</code>, <code>getElementsByClassName</code>, and the <code>.children</code> property return live <code>HTMLCollection</code>s. <code>childNodes</code> is a live <code>NodeList</code>.</li><li><strong>Static</strong>: <code>querySelectorAll</code> returns a static <code>NodeList</code> — a snapshot taken at call time.</li></ul><p>Notice the subtlety: <code>NodeList</code> isn't always one or the other. <code>childNodes</code>'s NodeList is live, but the one from <code>querySelectorAll</code> is static. So you can't judge live-ness by the type name alone — you have to know which method produced it.</p><div class=\"analogy\">A live collection is a <strong>security camera feed</strong>: whatever walks into frame appears instantly, whatever leaves vanishes. A static collection is a <strong>printed photo</strong> you took a moment ago: people may have come and gone since, but the photo never changes. Both are useful — you just have to know which one you're holding.</div><h4>Why live collections can bite</h4><p>The danger is mutating the DOM <em>while iterating by index</em>. Because the collection re-counts itself after every change, the indices shift under your feet. Imagine a classic loop removing items:</p><ul><li>You're at index <code>0</code>, you remove it.</li><li>Everything slides down: the old item <code>1</code> is now item <code>0</code>.</li><li>Your loop moves to index <code>1</code> — which is now the old item <code>2</code>. The old item <code>1</code> was <strong>skipped entirely</strong>.</li></ul><p>Run that over a list and you remove only every other element, or worse, spin in a confusing loop because <code>length</code> keeps changing.</p><h4>Turning the trap into a tool</h4><p>That same liveness can be exploited deliberately. The idiom <code>const items = document.getElementsByClassName('x'); while (items.length) items[0].remove();</code> works <em>precisely because</em> it's live: you always grab index <code>0</code>, remove it, the list shrinks, and the next survivor falls into slot <code>0</code>. The loop drains the collection cleanly. A plain indexed <code>for</code> loop over the same live collection would skip half the elements.</p><p>When you don't want that behavior — which is most of the time — take a snapshot before mutating: spread it into a real array with <code>[...coll]</code> (or <code>Array.from(coll)</code>). The array won't change as you remove DOM nodes, so your indices stay stable. This is also why many people default to <code>querySelectorAll</code>: its static result is immune to the shifting-index problem out of the box.</p><div class=\"key\">Live collections (<code>getElementsBy*</code>, <code>.children</code>, <code>childNodes</code>) reflect the DOM in real time and will skip elements if you remove-while-iterating by index. <code>querySelectorAll</code> is a static snapshot. Spread to <code>[...coll]</code> before mutating unless you want the liveness.</div>"},
  "query-selector": {"teach":"<p>At its core, <code>querySelector</code> lets you find elements on the page using the <strong>exact same selector syntax you already write in CSS</strong> — no special DOM query language to learn.</p><h4>The two methods</h4><ul><li><code>querySelector(sel)</code> returns the <strong>first</strong> element that matches the CSS selector, or <code>null</code> if nothing matches.</li><li><code>querySelectorAll(sel)</code> returns a <strong>static</strong> <code>NodeList</code> of <em>all</em> matches (an empty NodeList if none).</li></ul><p>Both live on <code>document</code> <em>and</em> on any individual element. When you call them on an element, the search is scoped to that element's subtree — so <code>card.querySelectorAll('button')</code> finds only the buttons inside that one card, not the whole page. That scoping is what makes them so ergonomic in component code.</p><div class=\"analogy\">Think of CSS selectors as the address format your stylesheet already speaks fluently. <code>querySelector</code> is like handing that same address to a courier and saying \"go fetch me the thing at this address\" instead of \"go paint the thing at this address.\" Same address language, different verb — find instead of style.</div><h4>What the selector can express</h4><p>Anything CSS can target: <code>#id</code>, <code>.class</code>, <code>tag</code>, attribute matchers like <code>a[href^=\"/\"]</code> (href <em>starts with</em> <code>/</code>), combinators (<code>nav a</code>, <code>ul &gt; li</code>), and pseudo-classes. For example, <code>document.querySelectorAll('nav a[href^=\"/\"]')</code> grabs every internal navigation link in one expression — try writing that with the older <code>getElementsBy*</code> family and you'll appreciate the power.</p><h4>Performance and pitfalls</h4><p>Flexibility has a cost. For a hot path where you repeatedly look up a single known id, <code>getElementById('x')</code> is faster than <code>querySelector('#x')</code> because the browser has a dedicated id index and doesn't have to parse a selector. Reach for <code>querySelector</code> when you need expressiveness, not in tight loops over a single id.</p><p>The classic trap: the result is <strong>static</strong>, not live. If you add matching nodes after you query, they won't magically appear in your existing NodeList — you must re-run the query. Don't confuse this NodeList with the live ones from <code>getElementsByClassName</code>.</p><p>One more ergonomic wrinkle: only the <code>NodeList</code> from <code>querySelectorAll</code> ships a native <code>forEach</code>. It does <em>not</em> have <code>map</code>, <code>filter</code>, or <code>reduce</code>. To use those, spread it into a real array first: <code>[...nodeList].map(a =&gt; a.href)</code>.</p><div class=\"key\"><code>querySelector</code>/<code>querySelectorAll</code> find elements with CSS selectors and scope to whatever you call them on; the result is a <em>static</em> snapshot with only <code>forEach</code> — spread to <code>[...]</code> for <code>map</code>/<code>filter</code>, and prefer <code>getElementById</code> for hot single-id lookups.</div>"},
  "event-flow": {"teach":"<p>At its core, when an event happens on the page it doesn't just fire on the thing you clicked — it takes a <strong>round trip</strong>. It dives down from the top of the document to the exact element, then ripples back up to the top again.</p><h4>The three phases</h4><p>Every dispatched event travels in this order:</p><ul><li><strong>Capture phase</strong>: the event travels <em>down</em> the tree, from <code>window</code> through every ancestor toward the target. Think \"the event is approaching.\"</li><li><strong>Target phase</strong>: the event arrives at the actual element it happened on.</li><li><strong>Bubble phase</strong>: the event travels back <em>up</em>, from the target through every ancestor to <code>window</code>. Think \"the event is leaving.\"</li></ul><div class=\"analogy\">Imagine a stone dropped into a well. On the way <strong>down</strong> it passes each ledge (capture), it <strong>hits the water</strong> at the bottom (target), and the splash <strong>ripples back up</strong> the walls (bubble). Anyone standing on a ledge can react either as the stone falls past them or as the ripple comes back up — and that choice is exactly what the capture flag controls.</div><h4>Where your listeners sit</h4><p>By default, <code>addEventListener</code> attaches your handler to the <strong>bubble</strong> phase. If you want to intercept earlier — as the event descends toward the target — you pass <code>{ capture: true }</code>. A capturing listener on an ancestor runs <em>before</em> the target's own handler, which makes it ideal for global guards: <code>document.addEventListener('click', fn, true)</code> fires before any bubbling button handler, so you can inspect or even block clicks page-wide before they're acted on.</p><h4>Why this matters</h4><p>This flow is the foundation of <strong>event delegation</strong> (a parent listening for events from many children) and explains how a parent can intercept, modify, or stop a child's event — either on the way down (capture) or on the way up (bubble).</p><h4>The trap: not everything bubbles</h4><p>It's tempting to assume every event makes the full round trip, but several common ones do <strong>not bubble</strong>: <code>focus</code>, <code>blur</code>, and most <code>scroll</code>/<code>load</code> events. If you try to catch a child's <code>focus</code> by listening on a parent's bubble phase, the event never reaches you. The fixes are either to listen in the <strong>capture</strong> phase (which still sees them on the way down) or to use the bubbling cousins <code>focusin</code>/<code>focusout</code>, which were designed precisely to support delegation of focus changes.</p><div class=\"key\">Events go capture (down) → target → bubble (up); listeners default to bubble, and <code>{capture:true}</code> intercepts on the way down. Remember that <code>focus</code>/<code>blur</code> don't bubble — use capture or <code>focusin</code>/<code>focusout</code>.</div>"},
  "event-delegation": {"teach":"<p>At its core, <strong>event delegation</strong> means: instead of putting a listener on every child, you put <em>one</em> listener on a parent and let the children's events float up to it. Then you ask \"what was actually clicked?\" and respond accordingly.</p><h4>Why this works</h4><p>It rides on <strong>bubbling</strong>. When you click a child, the event travels up through every ancestor. So a single listener on a stable ancestor will receive clicks that originated on any descendant. The listener uses <code>event.target.closest(selector)</code> to walk up from the clicked node to the nearest matching element and decide what was hit.</p><div class=\"analogy\">Imagine a busy office with one receptionist at the front desk instead of a personal assistant glued to every employee. Any visitor (event) walks toward the desk (bubbles up), and the receptionist looks at who they came to see (<code>target.closest</code>) and routes them. Hiring one receptionist scales effortlessly — when a new employee joins, you don't hire a new assistant. Same here: add new rows to a list and you bind <em>nothing</em> new.</div><h4>The concrete pattern</h4><p>For a list, you write something like:</p><ul><li><code>ul.addEventListener('click', e =&gt; { const li = e.target.closest('li'); if (li &amp;&amp; ul.contains(li)) select(li); });</code></li></ul><p>Let's read it facet by facet. <code>e.target</code> is the deepest element the click landed on — maybe a <code>&lt;span&gt;</code> inside the <code>&lt;li&gt;</code>. <code>closest('li')</code> climbs upward until it finds the enclosing <code>&lt;li&gt;</code>. The <code>ul.contains(li)</code> check guards against edge cases where <code>closest</code> matched something outside your container. Only then do you act.</p><h4>Why it matters in practice</h4><ul><li><strong>Scales to many children</strong> with a single listener — far less memory than thousands of individual handlers.</li><li><strong>Handles dynamic content for free</strong>: rows added later by the server, by a render, or by virtualization just work, because the listener lives on the unchanging parent. No re-binding.</li></ul><p>This is the default pattern for lists, tables, menus, and any server-rendered or virtualized UI.</p><h4>The trap</h4><p>Delegation depends on bubbling, so you <strong>cannot</strong> delegate non-bubbling events like <code>focus</code> and <code>blur</code> on the bubble phase — they never reach the ancestor. Use their bubbling cousins <code>focusin</code>/<code>focusout</code>, or attach the listener in the <strong>capture</strong> phase, which still sees them on the way down.</p><div class=\"key\">One listener on a stable ancestor + <code>event.target.closest(sel)</code> handles unlimited and dynamically-added children with no re-binding — but only for events that bubble, so use <code>focusin</code>/<code>focusout</code> (or capture) for focus.</div>"},
  "target-vs-currenttarget": {"teach":"<p>At its core: <code>event.target</code> is <strong>what you clicked</strong>, and <code>event.currentTarget</code> is <strong>whose listener is running right now</strong>. They're often the same element — but in delegation they're deliberately different, and confusing them is one of the most common DOM bugs.</p><h4>Defining each precisely</h4><ul><li><code>event.target</code> — the element where the event <em>originated</em>. As the event captures down and bubbles up through many ancestors, <code>target</code> never changes. It's the splash point.</li><li><code>event.currentTarget</code> — the element whose listener is <em>currently executing</em>. As the event propagates and fires handler after handler on different ancestors, <code>currentTarget</code> updates to point at each one in turn. In a normal-function handler it also equals <code>this</code>.</li></ul><div class=\"analogy\">Picture a relay race where a baton (the event) is passed up a line of runners. The <strong>target</strong> is the runner who first grabbed the baton off the ground — fixed, historical, \"where it started.\" The <strong>currentTarget</strong> is whoever is holding the baton at this exact moment — it changes hand to hand as the baton moves up the line. When you read these inside a handler, you're asking \"who started this?\" versus \"whose hands is it in <em>now</em>?\"</p><h4>Why delegation makes them diverge</h4><p>In a delegated handler bound to a <code>&lt;ul&gt;</code>, when someone clicks a <code>&lt;span&gt;</code> inside an <code>&lt;li&gt;</code>:</p><ul><li><code>currentTarget</code> = the <code>&lt;ul&gt;</code> (the element you attached the listener to)</li><li><code>target</code> = the <code>&lt;span&gt;</code> (the deepest thing actually clicked)</li></ul><p>This is precisely why delegation needs <code>target.closest('li')</code>: <code>target</code> tells you the exact descendant, and you climb from there to the logical row.</p><h4>The trap: currentTarget gets reset</h4><p>Here's the subtle bug. <code>currentTarget</code> is only valid <em>during</em> dispatch. Once your handler returns and the event finishes propagating, the browser resets <code>currentTarget</code> to <code>null</code>. So if you stash the event and read <code>e.currentTarget</code> later — say inside a <code>setTimeout</code> or a resolved promise — you'll get <code>null</code> and a crash. <code>target</code> survives, but <code>currentTarget</code> does not.</p><p>The fix is simple: capture it synchronously into a local variable while the handler runs — <code>const el = e.currentTarget;</code> — and use <code>el</code> in any deferred code.</p><div class=\"key\"><code>target</code> = where the event started (constant); <code>currentTarget</code> = the element whose listener is running now (changes during propagation, equals <code>this</code>). In delegation they differ, and <code>currentTarget</code> becomes <code>null</code> after dispatch — save it to a local before any async work.</div>"},
  "stop-propagation": {"teach":"<p>At its core these three methods answer three <em>different</em> questions, and the biggest mistake is treating them as interchangeable. One cancels the browser's built-in reaction; the other two stop the event from <em>spreading</em> to more listeners.</p><h4>The three, dissected</h4><ul><li><code>preventDefault()</code> — cancels the browser's <strong>default action</strong> (following a link, submitting a form, checking a checkbox). It does <em>not</em> stop propagation; the event keeps capturing and bubbling normally.</li><li><code>stopPropagation()</code> — halts further <strong>travel</strong> of the event (no more capture or bubble to other nodes), but other listeners <em>on the same element</em> still run. The default action is <em>not</em> cancelled.</li><li><code>stopImmediatePropagation()</code> — does everything <code>stopPropagation</code> does <em>and</em> blocks any remaining listeners attached to the same node.</li></ul><div class=\"analogy\">Imagine a rumor spreading through a building. <code>preventDefault()</code> is telling the <em>subject</em> of the rumor not to react — the rumor still travels, but nothing happens because of it. <code>stopPropagation()</code> is sealing the door of the current room so the rumor can't reach <em>other</em> rooms — but people already in this room still hear the rest of it out. <code>stopImmediatePropagation()</code> is shouting \"everyone stop talking, now\" — even the people in this room who hadn't spoken yet stay silent.</p><h4>They are orthogonal</h4><p>Because cancelling the default and stopping the spread are independent axes, you mix and match. A very common combo: <strong>cancel the default but let it keep bubbling</strong>. In a custom dropdown built on a link, you call <code>e.preventDefault()</code> so the link doesn't navigate, yet you deliberately <em>let</em> the event bubble up to a delegation root whose job is to close any other open menus. Conversely you might <code>stopPropagation()</code> to keep a click from reaching a document-level \"close on outside click\" handler while still allowing the default action.</p><h4>The trap</h4><p>If you registered a listener as <code>{ passive: true }</code> (common on <code>scroll</code>/<code>touchstart</code>/<code>wheel</code> for performance), calling <code>preventDefault()</code> inside it does <strong>nothing</strong> — the browser ignores it and logs a console warning. \"Passive\" is a promise to the browser that you will <em>not</em> prevent the default, which lets it scroll without waiting on your code. If you genuinely need to cancel scrolling, you must register that listener as non-passive.</p><div class=\"key\"><code>preventDefault</code> cancels the browser's default action; <code>stopPropagation</code> stops travel to other nodes; <code>stopImmediatePropagation</code> also blocks remaining same-node listeners. They're orthogonal — and <code>preventDefault</code> is silently ignored in a <code>passive:true</code> listener.</div>"},
  "addeventlistener-options": {"teach":"<p>At its core, the third argument to <code>addEventListener</code> can be an <strong>options object</strong> that fine-tunes <em>when</em> a listener fires, <em>how often</em>, whether it may block the browser, and <em>how</em> it gets removed.</p><h4>The four knobs</h4><ul><li><code>capture</code> — run the listener during the capture (downward) phase instead of the default bubble (upward) phase.</li><li><code>once</code> — auto-remove the listener after it fires a single time. No manual cleanup needed; perfect for one-shot \"wait for first interaction\" handlers.</li><li><code>passive</code> — a <strong>promise to the browser that you will not call <code>preventDefault()</code></strong>. Because the browser knows you won't cancel scrolling, it can scroll immediately without waiting for your handler to finish.</li><li><code>signal</code> — pass an <code>AbortController</code>'s signal; calling <code>controller.abort()</code> later removes the listener (and any others sharing that signal).</li></ul><div class=\"analogy\"><code>passive</code> is like telling airport security \"I have nothing to declare\" so they wave you straight through instead of stopping to inspect every bag. The browser doesn't have to pause the scroll to ask your handler \"are you going to cancel this?\" — you already promised you won't, so the page stays buttery smooth. <code>signal</code> is like a master kill-switch wired to many lights: one flip of <code>abort()</code> turns them all off at once.</p><h4>Why passive and signal matter most</h4><p><code>passive</code> is critical for scroll and touch performance — a non-passive <code>touchstart</code> handler can stall scrolling while the browser waits to see if you'll prevent it. For this reason browsers make <code>wheel</code> and <code>touchstart</code> listeners on <code>window</code>/<code>document</code> passive <em>by default</em>. <code>signal</code> is the cleanest teardown story in modern code: instead of holding onto every function reference to remove individually, you give them all one signal and call <code>abort()</code> once — ideal in a component's unmount/cleanup.</p><p>Example: <code>const ac = new AbortController(); el.addEventListener('scroll', fn, { passive: true, signal: ac.signal });</code> and later just <code>ac.abort();</code>.</p><h4>The trap</h4><p>The legacy third argument was a <strong>boolean</strong> meaning <code>capture</code>, not an object. Passing a boolean where you meant an options object (or vice-versa) silently misconfigures the listener. And crucially, <code>removeEventListener</code> only removes a listener if you pass the <strong>same function reference</strong> <em>and</em> the matching <code>capture</code> value — an inline arrow function or a mismatched capture flag won't remove anything. (This mismatch headache is exactly what <code>signal</code> sidesteps.)</p><div class=\"key\">The options object tunes a listener via <code>capture</code>/<code>once</code>/<code>passive</code>/<code>signal</code>; <code>passive:true</code> keeps scrolling smooth (and is the default for <code>wheel</code>/<code>touchstart</code>), and an <code>AbortController</code> <code>signal</code> is the cleanest way to tear down many listeners at once.</div>"},
  "observers": {"teach":"<p>At its core, observers are <strong>browser-provided watchers</strong> that efficiently notify you when something on the page changes — when an element scrolls into view, when the DOM is mutated, or when an element resizes — without you writing a constantly-firing scroll or polling loop.</p><h4>The three observers</h4><ul><li><code>IntersectionObserver</code> — watches whether an element <em>overlaps</em> a root (the viewport by default). Powers lazy-loading images, infinite scroll, and \"is this ad visible\" tracking.</li><li><code>MutationObserver</code> — watches <em>changes to the DOM tree</em>: added/removed nodes, attribute changes, text changes. Its records are delivered as a <strong>microtask</strong>, so they batch up after the current script before the next paint.</li><li><code>ResizeObserver</code> — watches an element's <em>box size</em>, letting components respond to their own dimensions (true container-responsive UIs, not just viewport media queries).</li></ul><div class=\"analogy\">The old way was hiring a guard to run a lap of the building every few milliseconds checking every door — exhausting and wasteful (that's a <code>scroll</code> handler polling <code>getBoundingClientRect</code> on every frame). An observer is instead a motion sensor wired to each door that only pings you <em>when something actually happens</em>, and even bundles several pings together. Far less work, far less jank.</p><h4>Why they beat hand-written handlers</h4><p>The key word is <strong>batched and async</strong>. Observers collect what changed and hand you an array of <em>records</em> in a callback that runs <em>off</em> the synchronous event path. A naive <code>scroll</code> listener runs your code on every single scroll tick, often forcing layout reads mid-frame and producing visible stutter. Observers let the browser pick efficient moments and coalesce work, dramatically reducing jank.</p><p>A typical lazy-load reads cleanly:</p><ul><li><code>const obs = new IntersectionObserver((es) =&gt; es.forEach(e =&gt; { if (e.isIntersecting) { load(e.target); obs.unobserve(e.target); } }), { rootMargin: '200px' });</code></li></ul><p>Here <code>isIntersecting</code> tells you the element entered the viewing area, <code>rootMargin: '200px'</code> starts loading 200px <em>before</em> it's visible (so it's ready by the time the user arrives), and <code>unobserve</code> stops watching once loaded — clean and self-terminating.</p><h4>The trap</h4><p>Inside a <code>ResizeObserver</code> callback, if you change styles that themselves alter layout, you can trigger a feedback loop and the browser warns: <em>\"ResizeObserver loop completed with undelivered notifications.\"</em> The fix is to defer your layout-affecting writes to the next frame with <code>requestAnimationFrame</code>, breaking the synchronous measure→mutate→re-measure cycle.</p><div class=\"key\">Intersection/Mutation/Resize observers replace expensive scroll/poll loops with efficient, batched, async callbacks for visibility, DOM changes, and size changes — just avoid layout-mutating writes inside a <code>ResizeObserver</code> callback (defer them with <code>requestAnimationFrame</code>).</div>"},
  "layout-thrashing": {"teach":"<p>At its core, <strong>layout thrashing</strong> is forcing the browser to recompute the page's geometry over and over by interleaving \"measure\" and \"change\" operations in a loop — when doing all the measuring first and all the changing second would have been far cheaper.</p><h4>Why a single read can be expensive</h4><p>The browser is lazy on purpose: when you change styles, it doesn't immediately recompute positions — it queues the work, hoping to batch many changes into one layout pass. But the moment you <em>read</em> a geometry property like <code>offsetWidth</code>, <code>getBoundingClientRect()</code>, or <code>scrollTop</code> <em>after</em> a write, the browser is cornered. To give you an accurate number, it must flush that queue and run a <strong>synchronous reflow</strong> right now.</p><p>One forced reflow is fine. But put it in a loop — write, read, write, read — and you trigger a reflow on <em>every iteration</em>. That's $O(n)$ reflows for $n$ elements instead of one, and reflow is one of the most expensive things the browser does. That cascade is layout thrashing.</p><div class=\"analogy\">Imagine a librarian re-alphabetizing a shelf. The smart way: collect <em>all</em> the new books, then re-sort the shelf once. Thrashing is inserting one book, then immediately asking \"what's the exact position of every book now?\" — forcing a full re-sort — then inserting the next book and asking again. Each question forces the librarian to redo the entire arrangement before answering. Ask $n$ times, re-sort $n$ times.</p><h4>The bad pattern and the fix</h4><p>Here's the trap in code:</p><ul><li><strong>Bad:</strong> <code>for (el of els) { el.style.width = el.offsetWidth + 10 + 'px'; }</code></li></ul><p>Each iteration <em>writes</em> a width, then the next iteration's <code>offsetWidth</code> <em>reads</em> geometry — forcing a reflow every single pass.</p><p>The cure is <strong>read/write batching</strong>: separate the phases. First loop: measure every <code>offsetWidth</code> into an array (all reads, one reflow). Second loop: assign every new width (all writes, batched). For animations, push the writes inside <code>requestAnimationFrame</code> so they land at the optimal moment in the frame. Libraries like <em>fastdom</em> formalize this by queuing reads and writes into separate phases for you.</p><h4>The subtle gotcha</h4><p>Don't assume the browser will quietly coalesce reads and writes for you. <em>Any</em> forced geometry read flushes the pending style/layout queue <strong>immediately</strong> — so even a single stray <code>getBoundingClientRect()</code> sitting in the middle of your writes defeats all your careful batching. Keep reads and writes strictly separated.</p><div class=\"key\">Reading geometry (<code>offsetWidth</code>, <code>getBoundingClientRect</code>, <code>scrollTop</code>) after a write forces a synchronous reflow; doing it in a loop is layout thrashing. Batch all reads first, then all writes (ideally in <code>requestAnimationFrame</code>) — one stray read flushes the queue and ruins the batch.</div>"},
  "document-fragment": {"teach":"<p>At its core, a <code>DocumentFragment</code> is an <strong>invisible holding bag</strong>: a lightweight container you assemble nodes inside, off to the side, and then drop into the real page all at once.</p><h4>Why the bag is useful</h4><p>Every time you append a node directly to the live DOM, the browser may recompute layout. Do that in a loop building 500 list items and you can trigger many costly updates. A <code>DocumentFragment</code> is <em>parentless and not part of the document</em>, so appending nodes to it is essentially free — there's no layout to recompute because it isn't on the page. When you finally append the fragment to the live DOM, the browser moves all its children in as a <strong>single operation</strong>, paying the layout cost just once.</p><div class=\"analogy\">Think of packing a moving truck. You could carry each plate one at a time from the kitchen to the new house, re-arranging the new kitchen after every single plate (appending to the live DOM in a loop). Or you load everything into the truck (the fragment) in your driveway where rearranging costs nothing, then make <em>one</em> trip and unload it all at once. Same plates arrive, but you only \"set up the kitchen\" a single time.</p><h4>The key behavior to internalize</h4><p>When you append a fragment, the <strong>fragment itself does not end up in the DOM</strong> — only its children are inserted, and the fragment is left <em>empty</em>. This trips people up: they expect to find a <code>&lt;documentfragment&gt;</code> wrapper in the page and there is none. It's purely a transport mechanism that empties itself on delivery.</p><p>The canonical pattern:</p><ul><li><code>const f = document.createDocumentFragment();</code></li><li><code>for (const t of items) { const li = document.createElement('li'); li.textContent = t; f.append(li); }</code></li><li><code>list.append(f);</code> — all the <code>&lt;li&gt;</code>s land in <code>list</code> at once; <code>f</code> is now empty.</li></ul><h4>The template cousin</h4><p>The <code>content</code> property of a <code>&lt;template&gt;</code> element is itself an <strong>inert</strong> <code>DocumentFragment</code>. \"Inert\" means its scripts don't run and its images don't download <em>until</em> you clone its content into the live document. That makes <code>&lt;template&gt;</code> the ideal place to define repeatable markup once and stamp out copies with <code>templateEl.content.cloneNode(true)</code> (the <code>true</code> means a deep clone, copying all descendants).</p><div class=\"key\">A <code>DocumentFragment</code> is an off-document bag for assembling nodes cheaply, then inserting them in one layout pass — and only its <em>children</em> are inserted (the fragment empties itself). <code>&lt;template&gt;.content</code> is an inert fragment perfect for cloning repeatable markup.</div>"},
  "request-idle-callback": {"teach":"<p>At its core, <code>requestIdleCallback</code> is a way to say to the browser: <em>\"run this work, but only when you're not busy — when there's spare time left over after painting a frame.\"</em> It's the polite scheduler for non-urgent jobs.</p><h4>How it hands you time</h4><p>You call <code>requestIdleCallback(cb, { timeout })</code>. When the browser finds an idle gap, it invokes your callback with an <code>IdleDeadline</code> object. The crucial method is <code>deadline.timeRemaining()</code>, which tells you how many milliseconds of idle time you have left (capped at roughly 50ms). Your job is to do a little work, keep checking <code>timeRemaining()</code>, and <strong>yield before it hits 0</strong> — re-scheduling whatever's left for the next idle period. That cooperative yielding is what keeps frames smooth.</p><div class=\"analogy\">Imagine a restaurant kitchen during a dinner rush. The head chef (the main thread) is slammed plating orders (rendering frames). You're a prep cook with a pile of non-urgent chopping. A smart prep cook doesn't grab the chef's station mid-order — you wait for the lull between tickets, chop for a few seconds, and the instant a new order comes in you step back. <code>timeRemaining()</code> is you glancing up to see how much lull is left before the next ticket.</p><h4>The right and wrong jobs for it</h4><p>Good fits: analytics beacons, prefetching data the user might need next, and background processing of a queue. <strong>Never</strong> use it for animation — that's what <code>requestAnimationFrame</code> is for, which is synced to the frame cadence. And avoid heavy <em>layout-affecting</em> DOM work inside it: idle time has no reflow budget, so mutating geometry there can blow your frame.</p><p>The draining pattern:</p><ul><li><code>function flush(d) { while (d.timeRemaining() &gt; 0 &amp;&amp; queue.length) process(queue.shift()); if (queue.length) requestIdleCallback(flush, { timeout: 2000 }); }</code></li><li><code>requestIdleCallback(flush, { timeout: 2000 });</code></li></ul><p>It chews through the queue a slice at a time, re-scheduling itself until empty.</p><h4>The traps</h4><p>First, it can be <strong>starved</strong>: on a perpetually busy main thread an idle moment may never come, and without a <code>timeout</code> your callback could wait indefinitely. The <code>timeout</code> option forces it to run once that deadline passes, even if the browser is busy — your safety net. Second, and important as of 2026: <strong>Safari still ships <code>requestIdleCallback</code> behind a feature flag</strong>, so it's effectively absent in the default browser. Feature-detect it and fall back to <code>setTimeout</code> when it's missing.</p><div class=\"key\"><code>requestIdleCallback</code> runs low-priority work in spare time after paint, giving you an <code>IdleDeadline</code> to cooperatively yield before <code>timeRemaining()</code> hits 0 — always pass a <code>timeout</code> so it can't be starved, and feature-detect because default Safari still lacks it in 2026.</div>"}
});
