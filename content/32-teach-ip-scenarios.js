/* Teach me from the ground up: IP-SCENARIOS (auto-assembled) */
enh({
  "trade-reconciliation": {"teach":"<p>At its core, trade reconciliation is just <strong>checking your own list against someone else's official list and chasing down anything that disagrees</strong>. You think you bought 500 shares for $10,000; the clearing firm says you bought 500 shares for $10,001.50. Reconciliation is the discipline of noticing that gap, deciding whether it matters, and resolving it.</p><h4>The two records</h4><p>On one side you have your <strong>internal records</strong> — what your own systems booked. On the other side you have an <strong>external source of truth</strong>: a clearing house or custodian Start-of-Day file, an exchange settlement file, or on-chain blockchain data. The external side is treated as authoritative because that is what money actually moves against.</p><div class=\"analogy\"><p>Think of balancing your checkbook against the bank statement. Your check register is your internal record; the bank statement is the external truth. You go line by line: this deposit matches, this one matches, and — wait — there's a $1.50 charge the bank applied that you forgot to write down. That charge isn't a mistake; it's a <em>fee</em> you simply tolerate.</p></div><h4>How matching actually works</h4><p>You <strong>match on stable identifiers</strong> — things that don't drift: trade id, amount, currency, symbol, settlement date. Then you <strong>tolerate expected discrepancies</strong>. Fees, rounding, timing lags, and partial fills will make the two sides differ in small, predictable ways. A row that still won't match after tolerances is called a <strong>break</strong>, and it flows into an exception queue for a human or a rule to resolve.</p><h4>Why idempotency matters</h4><p>A good engine is <strong>idempotent and re-runnable</strong>: feed it the same inputs twice and you get the exact same breaks, never a different answer. That sounds academic until 4 PM when an analyst re-runs the batch and needs to trust that nothing silently changed. Pair that with a full <strong>audit trail</strong> and you can always answer 'why did this break, and what did we do about it?'</p><h4>The trap that bites</h4><p>The classic failure is <strong>zero-tolerance exact matching</strong>. If you demand the penny match perfectly, every fee, every rounding difference, every timing lag becomes a 'break.' The queue floods with hundreds of false breaks, and analysts learn to rubber-stamp them — which is exactly when a <em>real</em> break slips through unnoticed. Tolerances aren't sloppiness; they are what keeps the signal visible.</p><p>Concretely: your internal record shows a fill of <code>$10,000.00</code>; the external file is gross of a <code>$1.50</code> fee and shows <code>$10,001.50</code>. With a <code>$2</code> fee tolerance, those match cleanly. With zero tolerance, you've manufactured a false break.</p><div class=\"key\"><p>Reconciliation is not about forcing the two records to be identical — it's about auto-clearing the <em>expected</em> differences so the genuinely <em>unexpected</em> ones stand out and get fixed.</p></div>"},
  "break-exception": {"teach":"<p>At its core, a break is just <strong>a trade that didn't line up between your two records and now needs fixing</strong>. Reconciliation produces breaks the way a spell-checker produces red squiggles: each one is a flag saying 'a human or a rule should look here.'</p><h4>The flavors of break</h4><p>After you've applied tolerances and timing windows, anything left over falls into a handful of buckets:</p><ul><li><strong>Amount break</strong> — same trade, different value.</li><li><strong>Date break</strong> — settlement dates disagree.</li><li><strong>Missing-on-internal</strong> — the external side has a trade you don't.</li><li><strong>Missing-on-external</strong> — you have a trade they don't (yet).</li><li><strong>Duplicate</strong> — the same fill counted twice.</li></ul><p>Each break lands in an <strong>exception queue</strong> carrying its status, owner, age, and crucially the <strong>dollars-at-risk</strong>.</p><div class=\"analogy\"><p>Picture an airport's lost-luggage desk. Every mismatched bag is an 'exception' with a tag: how long it's been missing (age), who's handling it (owner), and how valuable/urgent it is (dollars-at-risk). The desk doesn't work bags in random order — it triages by what's most expensive and most time-critical. A break queue works the same way.</p></div><h4>Why the clock matters: T+1</h4><p>Under <strong>T+1 settlement</strong>, trades settle one business day after execution, which compresses your intraday window to roughly <strong>2–5 hours</strong>. That's brutally short. So breaks are prioritized by <strong>dollar value and time-to-cutoff</strong>: a $2M amount break with 90 minutes left jumps the queue over a $500 break with hours to spare. Any break you fail to resolve in time can curdle into a <strong>settlement fail</strong> — a trade that doesn't settle, which carries cost, risk, and regulatory attention.</p><h4>The design goal</h4><p>You want the system to <strong>auto-resolve the deterministic majority</strong> — the boring, rule-shaped breaks — and escalate only the genuinely ambiguous ones to a human analyst. Analyst attention is your scarcest resource; spend it on judgment calls, not on clerical matches a rule could close.</p><h4>The trap that bites</h4><p>The subtle mistake is treating a <strong>timing difference as a real break</strong>. If a trade is present on one side but simply hasn't arrived on the other yet, that's not an economic problem — it's a clock problem. You should <strong>age it through the timing window first</strong> and only escalate if it's still unmatched after the next file lands.</p><p>For example: a custodian file arrives missing a 3:55 PM fill that's sitting in your blotter. That's a <strong>timing break</strong> — it clears the moment the next file lands. Escalating it to an analyst as an 'economic break' wastes attention on something that was always going to resolve itself.</p><div class=\"key\"><p>A break is a question, not an accusation — and the first question to ask is 'is this real, or is the other side just running late?' Age timing differences out before you spend a human on them.</p></div>"},
  "canonical-schema": {"teach":"<p>At its core, a canonical schema is just <strong>one standard internal shape that every messy outside feed gets translated into before your system touches it</strong>. Instead of teaching every part of your platform to speak 'equities-clearing-file,' 'crypto-exchange-API,' and 'prediction-market-resolution,' you translate all of them, once, into a single common vocabulary.</p><h4>What it looks like</h4><p>The canonical record is a fixed set of fields that every trade, from every venue, gets mapped onto during <strong>normalization</strong>. Typically something like:</p><ul><li><code>trade_id</code>, <code>symbol</code>, <code>side</code>, <code>qty</code>, <code>price</code></li><li><code>currency</code>, <code>settle_date</code>, <code>account</code></li></ul><p>Everything downstream — matching, position-keeping, reporting — reads <em>only</em> this shape and never the raw venue format.</p><div class=\"analogy\"><p>Think of a power adapter when you travel. Wall sockets differ wildly country to country, but your laptop only knows one plug. The travel adapter is the canonical layer: it absorbs the chaos of every socket on Earth so your device sees one consistent interface. The canonical schema is that adapter for trade data — venues are the foreign sockets, your downstream logic is the laptop.</p></div><h4>Why this decoupling is the whole point</h4><p>It <strong>decouples downstream logic from venue-specific quirks</strong>. Your matching engine doesn't need to know that equities carry a <code>settle_date</code> while crypto carries a chain/tx hash — by the time matching runs, both already look like the same canonical row. It also <strong>centralizes symbology mapping</strong>: the venue's symbol gets translated to your internal instrument id in exactly one place, so the rest of the system never juggles a dozen naming conventions.</p><h4>The trap that bites</h4><p>The tempting shortcut is to <strong>match raw venue records directly</strong>, joining one venue's format against another's on the fly. It feels faster for the first two venues. But with N venues you end up writing $N^2$ pairwise special-cases, and every new venue or format tweak ripples through brittle, venue-specific joins. Normalize-first turns that $N^2$ explosion into N simple mappers feeding one stable core.</p><h4>The example that makes it click</h4><p>Look at how different the raw inputs are, and how they collapse:</p><ul><li>An <strong>equity</strong> row carries a <code>settle_date</code>.</li><li>A <strong>crypto</strong> fill carries a chain/tx reference and a 24/7 timestamp.</li><li>A <strong>prediction-market</strong> contract carries an <code>event_id</code> plus a <code>resolution_date</code>.</li></ul><p>Three completely different worlds — yet all three map onto the same canonical columns. After normalization, your matching and tolerance rules can't even tell which venue a row came from, and that's exactly the property you want.</p><div class=\"key\"><p>Normalize first, reason once. The canonical schema lets you write matching, P&L, and reporting a single time against one shape — instead of re-implementing them for every venue's quirks.</p></div>"},
  "position-keeper": {"teach":"<p>At its core, a position keeper is just <strong>a running tally of everything you currently hold and what it's worth, updated every time a trade fills</strong>. Buy 100 shares, sell 30, buy 50 — the position keeper always knows you're net long 120, and it knows your running profit-and-loss as the market moves.</p><h4>Event sourcing: the position is a fold</h4><p>The clever design is to build it <strong>event-sourced</strong>. Rather than storing a single number you keep overwriting, you keep an <strong>append-only event log</strong> of every confirmed fill, and the position is a <em>fold</em> over that log — you replay the events and accumulate them into the current state.</p><div class=\"analogy\"><p>Think of a bank statement versus a sticky note with your balance scrawled on it. The sticky note (mutable counter) tells you the number now, but if it's wrong you have no idea why, and you can't reconstruct last Tuesday's balance. The statement (event log) lists every deposit and withdrawal; your balance is just the running sum. Want last Tuesday's balance? Sum the events up to Tuesday. That 'sum the events up to a point' operation is the <strong>fold</strong>, and it's why event sourcing is <strong>auditable</strong> and can be recomputed <strong>as-of</strong> any timestamp.</p></div><h4>Tying out to truth</h4><p>The position keeper doesn't operate in a vacuum — it <strong>reconciles to the clearing/custodian Start-of-Day position</strong>. Your replayed position must tie back to the external anchor each day. When a venue sends a correction or a bust (a <code>150=G</code> correction or <code>150=H</code> cancel), you <strong>append a reversing event</strong> rather than mutating history — the log stays a true record of everything that happened, including the mistakes and their fixes.</p><h4>The P&L math, in plain terms</h4><p>Two kinds of profit live here:</p><ul><li><strong>Unrealized P&L</strong> = <code>net_qty &times; (mark &minus; avg_cost)</code> — what you'd make if you closed everything right now at the current mark.</li><li><strong>Realized P&L</strong> accrues on <strong>offsetting fills</strong> — the moment a sell offsets a prior buy, that slice of profit/loss becomes locked in.</li></ul><h4>The trap that bites</h4><p>The seductive shortcut is to keep a <strong>mutable position counter and patch it in place</strong> when a correction arrives — just subtract 200 and move on. The problem: you've now destroyed your ability to reproduce yesterday's <em>reported</em> position, and you've severed the audit trail. When compliance or a counterparty asks 'what did your books show at 2 PM yesterday?', a mutated counter can't answer. An event log can, exactly.</p><p>Concretely: a <code>150=H</code> busts a 200-share buy. You don't reach in and edit the original — you <strong>append a &minus;200 reversal event referencing the original ExecID</strong> and re-fold. The position self-corrects, and the history shows both the original fill and its reversal.</p><div class=\"key\"><p>Never mutate the position — append events and fold. That single discipline buys you auditability, as-of reconstruction, and clean handling of every correction and bust.</p></div>"},
  "trade-correction": {"teach":"<p>At its core, this is just <strong>what happens when a venue tells you 'that trade I sent you earlier? It was wrong — here's the fix' or 'cancel it entirely'</strong> — and how you keep your books clean when it does.</p><h4>The FIX tag that carries the news</h4><p>In a FIX <strong>ExecutionReport</strong>, tag <code>150</code> is the <strong>ExecType</strong>, and its value tells you what kind of event you're looking at:</p><ul><li><code>150=F</code> — a normal <strong>Trade</strong> (a fill).</li><li><code>150=G</code> — a <strong>Trade Correction</strong> (amend a prior fill).</li><li><code>150=H</code> — a <strong>Trade Cancel</strong> (bust a prior fill).</li></ul><p>A correction or cancel has to point at <em>which</em> earlier fill it's modifying. It does that by referencing the original fill's <code>ExecID</code> via <code>ExecRefID</code> (tag <code>19</code>). So <code>150=G</code> + <code>ExecRefID</code> means 'amend the fill whose ExecID is X.'</p><div class=\"analogy\"><p>Think of a restaurant kitchen ticket rail. A correction is the waiter clipping a note onto an existing ticket — 'no onions, and make it medium-rare' — that points at the original order. A cancel is voiding the ticket entirely. What a good kitchen never does is erase and rewrite the original ticket, because then there's no record of what was first ordered or why it changed. Your trade log follows the same rule.</p></div><h4>How to handle it: append, never edit</h4><p>In an event-sourced position keeper, you process a correction or cancel by <strong>appending a reversal</strong> of the original — and, for a correction, also appending the <strong>new corrected event</strong> — then re-folding the log. You <em>never</em> reach back and edit the original event. The original fill, its reversal, and the corrected fill all coexist in the log, so the full story is reproducible.</p><h4>When it crosses a settlement boundary</h4><p>Timing adds a wrinkle. If a bust arrives <strong>after the trade has crossed a settlement boundary</strong> — meaning the clearing file already reflected the original — then your reversal no longer ties out against that file, and it surfaces as a <strong>break</strong> to be reconciled. The correction is clean inside your own log, but it now has to be squared against the external truth too.</p><h4>The trap that bites</h4><p>The mistake is <strong>editing the original fill row in place</strong> — just changing the price from $50 to $50.10. It looks tidy, but it destroys auditability: prior reported positions become unreproducible, and you can't show what your books said before the correction. Always append.</p><p>Worked example: an original <code>150=F</code> books 100 @ <code>$50</code>. A <code>150=G</code> corrects the price to <code>$50.10</code>. You append a <strong>reversal of the $50 fill</strong> plus a <strong>new corrected $50.10 fill</strong>, then re-fold. The net effect is 100 @ $50.10, and the log honestly shows all three events.</p><div class=\"key\"><p>A correction or cancel is a new event that references the old one — handle it by appending a reversal (and corrected fill), never by mutating history.</p></div>"},
  "sod-file": {"teach":"<p>At its core, the Start-of-Day file is just <strong>the clearing firm or custodian's official statement of what you own when the day begins</strong> — your fixed anchor of truth that everything else gets checked against.</p><h4>What's in it and why it's authoritative</h4><p>The <strong>SOD file</strong> is the clearing/custodian's snapshot of positions at day open, often including cash balances and <strong>SSIs</strong> (standing settlement instructions — the pre-agreed 'where the money and securities go' details). It's authoritative because it comes from the entity that actually holds your assets and settles your trades. Your own systems can drift or bug out; the custodian's record is what the rest of the financial system treats as real.</p><div class=\"analogy\"><p>Think of the opening count in a cash register at the start of a shift. Before a single sale, the manager counts the till and writes down the starting amount. Every transaction during the day builds on that number, and at close you reconcile back to it. The SOD file is that opening till count — the agreed starting point that makes the day's activity auditable.</p></div><h4>How the position keeper uses it</h4><p>This is the <strong>external truth your position keeper reconciles against</strong>. Your event-sourced replay accumulates the day's fills on top of the SOD position, and at tie-out your computed position must <strong>tie back to SOD</strong>. If your replay says you started flat but SOD says you held 500 shares, something is wrong on your side and needs investigating before you trust any intraday number.</p><h4>The trap that bites: not every asset has one</h4><p>The dangerous assumption is that <strong>every asset class hands you a clean daily SOD batch</strong>. Equities do — there's a central depository and a daily custodian/clearing file. But <strong>crypto is 24/7 with no central depository</strong>. There's no universal 'start of day' because the market never closes and no single custodian holds everything. So for crypto you don't get one daily truth file; instead you <strong>reconcile continuously against exchange REST trade history and on-chain data</strong>. The whole mental model of 'one batch at dawn' quietly breaks, and a multi-asset system has to handle both regimes.</p><h4>The example that makes it click</h4><p>Your event-sourced replay computes <strong>+500 XYZ</strong>. The custodian SOD also says <strong>+500 XYZ</strong> — the tie-out passes, and you proceed with confidence. But if your replay said +500 and SOD said +480, that <strong>20-share mismatch opens a position break</strong> that someone has to chase: a missed fill, an unprocessed correction, or a bookkeeping error on your side.</p><div class=\"key\"><p>The SOD file is your daily anchor — but it's an equities-shaped anchor. In a multi-asset world, crypto has no single start-of-day truth, so you reconcile it continuously instead.</p></div>"},
  "dtcc-pipeline": {"teach":"<p>At its core, this is just <strong>the three machines inside DTCC that take a stock trade you've agreed to and turn it into shares actually sitting in your account</strong>. Each does one job, and they hand off in sequence.</p><h4>The three pieces, in order</h4><ul><li><strong>ITP (Institutional Trade Processing)</strong> handles the <strong>pre-settlement matching and affirmation</strong> — the 'match-to-instruct' step where buyer and seller confirm they agree on the trade's details before anything settles.</li><li><strong>NSCC (National Securities Clearing Corporation)</strong> is the <strong>central counterparty (CCP)</strong>. It performs <strong>multilateral netting, novation, and guarantee</strong>, mostly through <strong>Continuous Net Settlement (CNS)</strong>.</li><li><strong>DTC (The Depository Trust Company)</strong> is the <strong>central securities depository</strong>. It does the actual <strong>book-entry settlement and custody</strong> — the shares change hands as ledger entries.</li></ul><h4>Decoding the scary words</h4><p>Let's dissect NSCC's job, because it's the dense one. <strong>Multilateral netting</strong>: instead of settling every individual buy and sell, NSCC collapses all your trades in a name down to one net number. <strong>Novation</strong>: NSCC legally steps into the middle, becoming buyer to every seller and seller to every buyer — so you no longer face your original counterparty, you face NSCC. <strong>Guarantee</strong>: because NSCC stands in the middle, it guarantees settlement even if your counterparty defaults. <strong>CNS</strong> is the mechanism that runs this continuously.</p><div class=\"analogy\"><p>Imagine ten roommates who each owe and are owed money after a month of covering each other's tabs. Rather than 90 separate Venmo payments, one friend (the CCP) collects what everyone net owes and pays out what everyone is net owed — each person makes or receives a single transfer. That netting friend also promises 'if someone flakes, I cover it.' NSCC is that friend for the entire US equity market. Then the actual moving of money in everyone's accounts — that final ledger update — is DTC.</p></div><h4>Why affirmation is the gate</h4><p><strong>Affirmation gates NSCC/DTC settlement.</strong> A trade that isn't affirmed by the <strong>9:00 PM ET cutoff under T+1</strong> may not settle on time. This is the entire reason recon and exception tooling exists in equities: to get every trade affirmed and matched <em>before</em> that gate slams shut. Miss it, and you risk a settlement fail.</p><h4>The trap that bites</h4><p>The common confusion is <strong>conflating NSCC and DTC</strong>. They are not the same: <strong>NSCC is the CCP that nets and guarantees</strong> the obligations; <strong>DTC is the depository that actually settles and custodies</strong> the securities. Netting happens at NSCC; the shares move at DTC. Get this backwards in an interview and it signals you don't understand the pipeline.</p><p>Worked example: affirmed buys and sells in a single name get <strong>netted by NSCC's CNS into one net obligation</strong>, which then <strong>settles book-entry at DTC</strong> — one clean ledger movement instead of dozens of gross settlements.</p><div class=\"key\"><p>ITP affirms, NSCC nets and guarantees as the CCP, DTC settles and custodies — and affirmation by the 9 PM ET cutoff is the gate that lets the whole thing settle on time.</p></div>"},
  "websocket-rest-ingest": {"teach":"<p>At its core, this is just <strong>how you reliably grab your fills from crypto and prediction-market venues that talk over plain web APIs instead of the FIX protocol</strong>. Wall Street equities use FIX, with its built-in reliability machinery. Newer venues hand you a <strong>REST + WebSocket</strong> pair instead, and you have to rebuild that reliability yourself.</p><h4>The division of labor</h4><p>The pattern splits the work between two channels:</p><ul><li>Use the <strong>WebSocket</strong> for the <strong>live fill/trade stream</strong> — a persistent connection that pushes events to you the instant they happen.</li><li>Use <strong>REST</strong> for <strong>snapshots, backfill on reconnect, and end-of-period truth checks</strong> — request-response calls you make when you need an authoritative picture.</li></ul><div class=\"analogy\"><p>Think of a live sports broadcast versus the official box score. The WebSocket is the live commentary — fast, real-time, but if your signal drops for ten seconds you might miss a goal and never know. REST is the official box score you can pull up anytime; it's the record of record. You watch the stream for speed, but you trust the box score for truth. Smart systems use both: stream for immediacy, then check the box score to confirm you didn't miss anything.</p></div><h4>Rebuilding what FIX gives you for free</h4><p>FIX bakes in sequencing, dedup, and idempotency. Over REST + WebSocket you reconstruct those yourself:</p><ul><li><strong>Track sequence numbers</strong> so you can detect a gap in the stream.</li><li><strong>Dedupe on the venue trade id</strong> so a re-sent message doesn't double-book a fill.</li><li><strong>Supply client order ids</strong> for <strong>idempotency</strong> — so retrying an order can't create two.</li></ul><h4>Staying resilient</h4><p>Web connections drop, and venues throttle you. Resilience requires <strong>auto-reconnect with backoff + jitter</strong> (retry with growing, randomized delays so you don't hammer the venue or stampede in sync with everyone else), <strong>heartbeats</strong> to detect a dead-but-not-closed socket, <strong>rate-limit handling</strong> via a token bucket (you spend tokens per request and refill over time), and a <strong>periodic REST reconciliation</strong> so anything the socket quietly dropped still gets caught.</p><h4>The trap that bites</h4><p>The cardinal mistake is <strong>treating the WebSocket as your source of truth</strong>. It isn't — it can silently <strong>drop messages on reconnect</strong>, and you'll never get a 'sorry, you missed one' notice. The <strong>REST trade-history endpoint is the authoritative check</strong>. The stream is for speed; REST is for truth.</p><p>Worked example: after a 5-second socket drop, you don't just resume streaming and hope. You <strong>re-snapshot 'my trades' over REST</strong>, diff that authoritative list against what you actually streamed, and <strong>backfill the missed fill</strong> before you let it touch your positions. That diff-and-backfill step is what keeps a dropped message from quietly corrupting your book.</p><div class=\"key\"><p>Stream over WebSocket for speed, but reconcile against REST for truth — because the socket can drop messages on reconnect and never tell you, while the REST trade-history endpoint is authoritative.</p></div>"}
});
