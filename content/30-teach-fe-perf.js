/* Teach me from the ground up: FE-PERF (auto-assembled) */
enh({
  "lcp": {"teach":"<p>At its core, LCP just answers one question your visitor cares about: <strong>\"How long until the big, important thing I came here to see actually shows up?\"</strong> Not the spinner, not a sliver of header — the largest meaningful element on the first screenful.</p><h4>What \"largest contentful\" really means</h4><p>The browser watches the visible area (the <em>viewport</em>) and keeps nominating the biggest piece of rendered <em>content</em> — usually a hero image, a video's poster frame, or a chunky block of text. As more loads, the nominee can change; the final answer is the render time of whatever ended up largest.</p><div class=\"analogy\"><p>Think of a stage curtain rising on a play. You don't feel the show has \"started\" when a stagehand's hand pokes out — you feel it when the <strong>lead actor</strong> is fully lit and visible. LCP times that moment for the lead actor of your page.</p></div><h4>Dissecting the budget: TTFB + delay + load + render</h4><p>LCP isn't one thing — it's a chain you can break into four pieces:</p><ul><li><strong>TTFB</strong> — time until the first byte of HTML arrives (server + network).</li><li><strong>Resource load delay</strong> — the gap between TTFB and when the browser <em>starts</em> fetching the LCP resource. This is the lever you usually pull.</li><li><strong>Resource load time</strong> — how long the image itself takes to download.</li><li><strong>Element render delay</strong> — time from \"bytes arrived\" to \"pixels on screen\" (often blocked by CSS/JS).</li></ul><p>Notice the biggest waste is usually <strong>load delay</strong>: the browser knows about the image late, so it starts late. The fix is to make the image <em>discoverable early</em>.</p><h4>Why discoverability is everything</h4><p>Browsers run a <strong>preload scanner</strong> that races ahead through the raw HTML looking for things to fetch. If your hero image lives in static <code>&lt;img&gt;</code> markup, the scanner finds it instantly. But if it's injected by JavaScript or set as a CSS <code>background-image</code>, the scanner can't see it — fetching only begins after the JS runs or the CSS is parsed. That delay directly inflates LCP.</p><p>So your three load-delay weapons are: keep the LCP resource in the initial HTML, mark it <code>fetchpriority=\"high\"</code> so it jumps the queue, and <strong>never</strong> put <code>loading=\"lazy\"</code> on it.</p><h4>Why it matters (and bites)</h4><p>LCP is judged at the <strong>p75 of real field data</strong>, and <strong>good means ≤ 2.5s</strong>. You can \"optimize\" a hero image to a tiny AVIF and still fail LCP if you lazy-load it — because the gotcha here is invisible: the image is small, but it was <em>discovered late</em>.</p><p>A worked fix preloads it explicitly: <code>&lt;link rel=\"preload\" as=\"image\" href=\"hero.avif\" imagesrcset=\"hero-800.avif 800w, hero-1600.avif 1600w\" fetchpriority=\"high\"&gt;</code> — now the scanner starts the fetch immediately, at high priority, with the right responsive variant.</p><div class=\"key\">LCP is won or lost on <strong>when the browser starts fetching the biggest element</strong>, not just how small that element is — keep it in static HTML, mark it high-priority, and never lazy-load the hero.</div>"},
  "inp": {"teach":"<p>At its core, INP just measures one feeling: <strong>after you click, tap, or press a key, how long before the screen visibly does something?</strong> And instead of grading a single click, it grades a near-worst-case across your <em>whole visit</em>.</p><h4>The three parts of an interaction</h4><p>Every interaction has a measurable journey from your finger to fresh pixels:</p><ul><li><strong>Input delay</strong> — the wait before your handler even runs, usually because the main thread is busy finishing some other task.</li><li><strong>Processing time</strong> — your event handler actually executing (state updates, calculations).</li><li><strong>Presentation delay</strong> — the time to render and paint the resulting visual change.</li></ul><p>INP is the <em>sum</em> of those three. <strong>Good is ≤ 200ms</strong> at the p75 of field data.</p><div class=\"analogy\"><p>Imagine a restaurant. Input delay is how long before a waiter notices you raising your hand. Processing time is the kitchen cooking your order. Presentation delay is plating and walking it to your table. A fast waiter (low input delay) means nothing if the kitchen stalls. INP times the <em>whole</em> trip, not just the wave.</p></div><h4>Why \"across the visit\" and not just the first click?</h4><p>INP <strong>replaced FID as a Core Web Vital on March 12, 2024</strong> precisely because FID was too forgiving: FID only measured the <em>input delay</em> of the <em>first</em> interaction. A page could ace FID and still feel awful on every later tap. INP fixes both blind spots — it counts processing and paint too, and it samples all interactions.</p><p>The scoring is cleverly robust to flukes: with under 50 interactions, INP is simply your single slowest one. Above that, it drops roughly one outlier per 50 interactions — effectively about the p98 — so one freak hiccup doesn't sink your score. (Scroll and zoom aren't counted as interactions.)</p><h4>The trap: a fast click can still be a slow INP</h4><p>The dangerous assumption is \"my input delay is tiny, so I'm fine.\" But INP also includes the <strong>handler work and the next paint</strong> — and that's exactly where a big synchronous React re-render or a layout-thrashing loop hides. Your click registered instantly; the page just took 400ms to repaint.</p><h4>How to actually fix it</h4><ul><li><strong>Break up long tasks</strong> — yield to the main thread with <code>scheduler.yield()</code> so the browser can paint between chunks.</li><li><strong>Defer non-urgent updates</strong> — React's <code>useTransition</code> lets the urgent visual response paint first.</li><li><strong>Offload heavy work</strong> to a Web Worker.</li><li><strong>Avoid synchronous layout thrash</strong> (reading then writing layout repeatedly) inside handlers.</li></ul><p>The pattern in one line: <code>button.onclick = () =&gt; { startTransition(() =&gt; setExpensiveState(next)); };</code> — the click's next paint stays fast because the expensive re-render is marked non-urgent and happens after.</p><div class=\"key\">INP grades the <strong>full round-trip from interaction to next paint</strong>, near-worst-case across the whole visit — so the win is keeping the main thread free to paint <em>quickly</em>, not just to acknowledge the click.</div>"},
  "cls": {"teach":"<p>At its core, CLS just measures one annoyance: <strong>how much does stuff jump around on the page while it's loading?</strong> You go to tap a button, an image loads above it, everything shifts down, and you tap an ad instead. CLS puts a number on that frustration.</p><h4>How a single shift is scored</h4><p>When a visible element moves from one rendered position to another without you asking it to, that's an <em>unexpected layout shift</em>. Its score is:</p><p>$\\text{score} = \\text{impact fraction} \\times \\text{distance fraction}$</p><ul><li><strong>Impact fraction</strong> — how much of the viewport the moving content covered (a big banner shifting hurts more than a tiny icon).</li><li><strong>Distance fraction</strong> — how far it moved, relative to the viewport's dimension.</li></ul><p>So a large element jumping a long way is the worst case; a small nudge barely registers.</p><h4>Why \"cumulative\" uses session windows</h4><p>CLS doesn't just add up every shift across a long visit — that would unfairly punish long sessions. Instead it finds the worst <strong>session window</strong>: a burst of shifts <strong>≤5s long with gaps no bigger than 1s</strong>, sums the scores inside it, and reports the <em>largest</em> such window. <strong>Good is ≤ 0.1.</strong></p><div class=\"analogy\"><p>Imagine grading a figure skater not on every wobble across the whole program, but on their worst <em>5-second stretch</em>. One clean recovery later doesn't erase the moment everything went sideways — but a single brief wobble in an otherwise stable routine won't dominate either.</p></div><h4>The \"expected\" exclusion — and its sharp edge</h4><p>Shifts you <em>caused</em> shouldn't count. So a shift within <strong>500ms of a discrete input</strong> — a click, tap, or keypress — is flagged <code>hadRecentInput</code> and excluded as expected (you clicked \"show more,\" of course content moved).</p><p>Here's the gotcha that trips people: <strong>scroll and pinch-zoom do NOT earn that exclusion.</strong> They're continuous gestures, not discrete inputs. If your page reflows during a scroll, that shift still counts against you.</p><h4>The other trap: it's not just images</h4><p>People assume CLS is purely an image problem. But the common culprits are subtler: a <strong>late-loading webfont</strong> swapping in and reflowing a paragraph, an <strong>injected banner or cookie bar</strong> pushing content down, and <strong>ad slots</strong> popping in without reserved space.</p><h4>How to prevent it: reserve the space</h4><p>The cure is always the same idea — tell the browser how big something will be <em>before</em> it loads:</p><ul><li>Set <code>width</code>/<code>height</code> or <code>aspect-ratio</code> on images and video.</li><li>Reserve placeholder boxes for ads and embeds.</li><li>Use font metric overrides (<code>size-adjust</code>, <code>ascent-override</code>) so a webfont swap doesn't change the text's height.</li></ul><p>One attribute does a lot: <code>&lt;img src=\"a.avif\" width=\"800\" height=\"450\"&gt;</code> — the browser computes the 16:9 box and holds that space before the bytes arrive, so loading causes <strong>zero shift</strong>.</p><div class=\"key\">CLS is fixed by <strong>reserving space up front</strong> for anything that loads late — and remember the 500ms \"expected\" pass applies to clicks and keypresses, never to scrolling.</div>"},
  "core-web-vitals": {"teach":"<p>At its core, Core Web Vitals are just <strong>Google's three headline numbers</strong> for whether a page feels fast, responsive, and stable to real people. Out of all the things you could measure, Google picked three that map to distinct human frustrations.</p><h4>The three, and the feeling each captures</h4><ul><li><strong>LCP — Largest Contentful Paint</strong> (loading): \"Has the main content shown up yet?\" <em>Good ≤ 2.5s.</em></li><li><strong>INP — Interaction to Next Paint</strong> (responsiveness): \"When I tap, does it react quickly?\" <em>Good ≤ 200ms.</em></li><li><strong>CLS — Cumulative Layout Shift</strong> (visual stability): \"Does stuff jump around under my finger?\" <em>Good ≤ 0.1.</em></li></ul><div class=\"analogy\"><p>Think of a restaurant review boiled down to three scores: <strong>how fast the food arrives</strong> (LCP), <strong>how quickly the waiter responds when you wave</strong> (INP), and <strong>whether the table wobbles while you eat</strong> (CLS). No single \"overall\" star — each axis stands on its own, and a great kitchen with a wobbly table still disappoints.</p></div><h4>The two ideas people miss</h4><p>First, <strong>each metric is judged independently at the 75th percentile of real-user field data.</strong> Let's dissect that:</p><ul><li><strong>Independently</strong> — there is <em>no single combined CWV score</em>. You don't average them; you must pass all three thresholds separately.</li><li><strong>75th percentile (p75)</strong> — Google sorts all your real visitors' values and looks at the one where 75% are faster. So you must be good for roughly three out of four real users — your fast laptop doesn't get a vote, but the visitor on a mid-tier phone over flaky 4G very much does.</li><li><strong>Field data</strong> — measured from <em>actual users</em> (via the Chrome UX Report), not a lab machine.</li></ul><p>Second, and this is the trap: <strong>lab tools don't decide pass/fail.</strong> Lighthouse and WebPageTest are fantastic <em>diagnostic</em> tools — they help you find <em>why</em> a metric is bad — but only field/RUM data determines the actual Core Web Vitals verdict and the page-experience signal.</p><h4>Why the lab-score trap bites so hard</h4><p>It's seductive to chase a green Lighthouse score and declare victory. But Lighthouse runs once, on a fixed simulated device, and <strong>it can't even measure INP</strong> (it reports a proxy). Real users on slow devices, with third-party scripts and cold caches, routinely diverge. A perfect lab run with failing field data means you're passing nobody.</p><h4>How you actually measure the truth</h4><p>To see what real users experience, instrument your own RUM with the <code>web-vitals</code> library and beam the numbers home: <code>import {onLCP, onINP, onCLS} from 'web-vitals'; onINP(m =&gt; navigator.sendBeacon('/rum', JSON.stringify(m)));</code>. Now you're collecting the same kind of field data Google grades you on.</p><div class=\"key\">Core Web Vitals are <strong>three independent metrics judged at p75 of real field data</strong> — a green Lighthouse score is a diagnosis, never the verdict.</div>"},
  "critical-rendering-path": {"teach":"<p>At its core, the critical rendering path is just <strong>the recipe the browser follows to turn your text files (HTML, CSS, JS) into actual colored pixels</strong>. It's a fixed sequence of steps, and knowing the steps tells you exactly where you can speed things up.</p><h4>The steps, in order</h4><ul><li><strong>Build the DOM</strong> — parse HTML into a tree of nodes (the Document Object Model).</li><li><strong>Build the CSSOM</strong> — parse CSS into a tree of style rules (the CSS Object Model). This happens <em>in parallel</em> with DOM building.</li><li><strong>Render tree</strong> — combine DOM + CSSOM into only the things that will actually be drawn (skipping, e.g., <code>display:none</code> nodes).</li><li><strong>Layout (reflow)</strong> — compute every element's exact geometry: position and size.</li><li><strong>Paint</strong> — fill in pixels: colors, text, borders, shadows.</li><li><strong>Composite</strong> — assemble the painted layers on the GPU into the final image.</li></ul><div class=\"analogy\"><p>It's like building a house. The DOM is the framing (structure), the CSSOM is the design spec (colors, sizes). The render tree is the approved blueprint. Layout is measuring where every wall goes, paint is applying the actual finishes, and composite is the drone photo that stacks every floor into one picture. You can't paint walls you haven't measured, and you can't measure without an approved blueprint.</p></div><h4>The two blockers that decide your speed</h4><p>Two things gate this pipeline, and both relate to that parallel DOM/CSSOM build:</p><ul><li><strong>CSS is render-blocking</strong> — the browser won't paint until the CSSOM is complete, because a half-built CSSOM could paint wrongly-styled content.</li><li><strong>Synchronous JS is parser-blocking</strong> — a plain <code>&lt;script&gt;</code> stops HTML parsing while it downloads and runs.</li></ul><p>So your goal is to <strong>minimize critical bytes</strong>: inline the small slice of CSS needed for the first screen, load the rest non-blocking, and add <code>async</code>/<code>defer</code> to scripts so they don't freeze parsing.</p><h4>The animation insight that trips engineers</h4><p>Here's the gotcha that bites in interviews and in production: <strong>not all CSS animations cost the same.</strong> The pipeline shows why. Animating <code>width</code>, <code>top</code>, or <code>margin</code> changes geometry — so the browser must redo <strong>layout AND paint every single frame</strong>, which is brutally expensive. But animating <code>transform</code> or <code>opacity</code> only changes how an already-painted layer is positioned/blended — that's the <strong>composite step only</strong>, handled cheaply by the GPU.</p><p>Concretely: <code>.x{transform:translateX(100px)}</code> is composited and smooth, while <code>.y{left:100px}</code> triggers a full relayout. Same visual motion, wildly different cost.</p><div class=\"key\">The critical rendering path is <strong>DOM + CSSOM → render tree → layout → paint → composite</strong>; the wins are shrinking render-blocking CSS, deferring JS, and animating only <code>transform</code>/<code>opacity</code> to stay on the cheap composite step.</div>"},
  "render-blocking": {"teach":"<p>At its core, a render-blocking resource is just <strong>a file the browser insists on finishing before it will paint anything on screen</strong>. Until that file is done, your visitor stares at a blank page — even if all the content is ready in the HTML.</p><h4>Why CSS blocks rendering by default</h4><p>The browser <em>could</em> paint your HTML the instant it parses it. It deliberately doesn't, because painting before the stylesheet arrives would show a flash of unstyled, ugly content (FOUC) that then snaps into its real look. To avoid that jarring flash, the browser waits for the <strong>CSSOM (CSS Object Model)</strong> to be complete before the first paint. That's what \"CSS is render-blocking\" means.</p><div class=\"analogy\"><p>Imagine a theater that refuses to raise the curtain until <em>all</em> the stage lighting is rigged. The actors are ready, the set is built — but the director won't show a half-lit stage to the audience. The lighting rig is your CSS: necessary before the reveal, and every extra fixture delays the curtain.</p></div><h4>Why synchronous scripts block the parser too</h4><p>A plain <code>&lt;script&gt;</code> is <strong>parser-blocking</strong>: when the parser hits it, HTML parsing stops until the script downloads and runs. Worse, because scripts can ask questions like \"what's the computed width of this element?\", the script must also <strong>wait for any pending CSSOM</strong> before executing — otherwise it'd read styles that aren't finalized. So one synchronous script in the head can stall on CSS <em>and</em> stall the parser. That's a double penalty.</p><h4>How to stop the blocking</h4><p>Two main moves:</p><ul><li><strong>Inline critical CSS</strong> — put the handful of styles needed for the first screenful directly in the HTML so there's no separate fetch to wait on, then load the rest of your CSS non-blocking.</li><li><strong>Use <code>defer</code> or <code>async</code> on scripts</strong> so they download off the parser thread instead of freezing it.</li></ul><p>A reliable trick to make a stylesheet non-blocking is the media-swap: <code>&lt;link rel=\"stylesheet\" href=\"non-critical.css\" media=\"print\" onload=\"this.media='all'\"&gt;</code>. Because the browser doesn't render-block on <code>media=\"print\"</code> styles, it fetches the file without blocking, then <code>onload</code> flips it to <code>all</code> to apply it.</p><h4>The trap that catches people</h4><p>First, the everyday version: adding \"just one stylesheet for one component\" in the <code>&lt;head&gt;</code> — that single link now blocks <em>the entire page's</em> first paint until it loads. Scope and defer non-critical CSS deliberately.</p><p>Second, the subtle one: <strong><code>&lt;link rel=preload&gt;</code> does NOT make CSS non-blocking.</strong> Preload only tells the browser to <em>fetch it early</em>; it still applies as a normal render-blocking stylesheet once it lands. Preload is about <em>timing the fetch</em>, not about removing the block.</p><div class=\"key\">CSS render-blocks first paint and synchronous scripts parser-block (and wait on CSS); make non-critical CSS non-blocking with the <code>media=\"print\"</code> swap and <code>defer</code> your scripts — and remember <code>preload</code> fetches early but does <strong>not</strong> unblock.</div>"},
  "async-defer": {"teach":"<p>At its core, <code>async</code> and <code>defer</code> are just <strong>two ways to load an external script without freezing the page while it parses</strong>. Without either, a <code>&lt;script&gt;</code> tag stops HTML parsing cold until the script downloads and runs. Both attributes let the download happen off to the side — they differ in <em>when</em> and <em>in what order</em> the script executes.</p><h4>What they share</h4><p>Both <code>async</code> and <code>defer</code> download the script <strong>off the parser thread</strong>, so fetching never blocks HTML parsing. The divergence is purely about execution timing.</p><h4><code>async</code>: run the instant it lands</h4><p>An <code>async</code> script executes <strong>the moment it finishes downloading</strong>, whenever that is. That means:</p><ul><li><strong>No guaranteed order</strong> — whichever arrives first runs first. Two <code>async</code> scripts are a race.</li><li>It <strong>can still interrupt parsing</strong> — if it lands mid-parse, the parser pauses to run it.</li></ul><p>This is perfect for <strong>independent</strong> scripts that don't depend on anything and that nothing depends on — analytics, ads, error trackers.</p><h4><code>defer</code>: wait, then run in order</h4><p>A <code>defer</code> script waits until <strong>the document is fully parsed</strong>, then runs <strong>in document order</strong>, immediately <strong>before <code>DOMContentLoaded</code></strong> fires. So deferred scripts behave like a tidy, ordered queue that the DOM is guaranteed to be ready for. This is what you want for your <strong>ordered application code</strong>.</p><div class=\"analogy\"><p><code>async</code> is like ordering food via several delivery apps — whatever shows up first, you eat first; arrival order is chaos. <code>defer</code> is a sit-down dinner with courses served in a fixed sequence, only after every guest has arrived (the DOM is fully set). One optimizes for \"as soon as possible,\" the other for \"correctly ordered, after setup.\"</p></div><h4>Two quiet rules worth knowing</h4><ul><li><strong><code>type=\"module\"</code> scripts are deferred by default</strong> — you get defer behavior for free, no attribute needed.</li><li><strong>Both attributes are ignored on inline (non-module) scripts</strong> — they only apply to scripts with a <code>src</code>. An inline <code>&lt;script async&gt;</code> just runs immediately.</li></ul><h4>The trap that causes real bugs</h4><p>Using <code>async</code> on <strong>interdependent</strong> scripts is a classic footgun. Say <code>app.js</code> needs a library that loads in another <code>async</code> tag — because execution order is nondeterministic, <code>app.js</code> sometimes runs first and you get a flaky <code>\"X is not defined\"</code> that only shows up under certain network timings. The fix is simple: use <code>defer</code>, which guarantees document order.</p><p>So the idiomatic split is: <code>&lt;script src=\"analytics.js\" async&gt;&lt;/script&gt;</code> for the independent tracker, and <code>&lt;script src=\"app.js\" defer&gt;&lt;/script&gt;</code> for ordered app code.</p><div class=\"key\">Both load off the parser thread; <strong><code>async</code> runs ASAP in unpredictable order</strong> (use only for independent scripts), while <strong><code>defer</code> runs after parse in document order</strong> (use for anything with dependencies).</div>"},
  "code-splitting": {"teach":"<p>At its core, these are two ways to <strong>ship less JavaScript</strong>: code splitting means \"only send the code this page needs <em>right now</em>,\" and tree shaking means \"drop any code <em>nobody imports</em>.\" Less JS means a smaller download and — just as importantly — less work for the browser's main thread to parse and run.</p><h4>Code splitting: load on demand</h4><p>Normally a bundler crams your whole app into one giant <code>bundle.js</code>. The user downloads everything up front, even features they'll never touch. <strong>Code splitting</strong> uses dynamic <code>import()</code> to break that bundle into <em>chunks</em> that load only when needed — typically per route, or when a specific interaction happens.</p><p>For example: <code>const Chart = lazy(() =&gt; import('./Chart'));</code> creates a separate chunk for the chart, fetched only when that component actually renders. The user who never opens the analytics page never pays for the charting library.</p><div class=\"analogy\"><p>Code splitting is ordering food à la carte instead of being forced to take the entire buffet to your table. Tree shaking is the kitchen refusing to plate the garnish you'll obviously never eat. One controls <em>when</em> things arrive; the other removes things that have no reason to come at all.</p></div><h4>Tree shaking: static dead-code elimination</h4><p>Let's dissect that term. \"Tree shaking\" pictures your imports as a tree — you shake it and the dead leaves (unused code) fall off. Technically it's <strong>static dead-code elimination over ES module imports/exports</strong>: the bundler reads which exports are actually imported and reachable, and drops everything else. \"Static\" is the key word — it figures this out <em>without running your code</em>, just by analyzing the import graph.</p><h4>Why tree shaking is fragile</h4><p>Static analysis only works when the bundler can <em>prove</em> code is safe to remove, and two things break that proof:</p><ul><li><strong>It needs real ES modules.</strong> CommonJS (<code>require()</code>) is dynamic — you could <code>require()</code> based on a runtime variable — so the bundler can't statically prove what's unused and keeps it all.</li><li><strong>It needs correct <code>\"sideEffects\"</code> metadata.</strong> If a module might do something just by being imported (e.g., register a polyfill, mutate a global), removing it could break the app. Marking <code>\"sideEffects\": false</code> in <code>package.json</code> tells the bundler \"importing my modules is safe to prune.\"</li></ul><p>This is exactly the trap: expecting tree shaking to prune a <strong>CommonJS dependency</strong> or a <strong>barrel file with side effects</strong> — it simply can't statically prove they're safe to drop, so the dead code rides along.</p><h4>Don't over-split</h4><p>Splitting too aggressively creates <strong>request waterfalls</strong>: chunk A loads, then discovers it needs chunk B, then C — each round-trip adding latency. <strong>Route-level splitting</strong> is usually the sweet spot: meaningful chunks without a cascade of tiny dependent requests.</p><div class=\"key\">Code splitting defers code with dynamic <code>import()</code> (split at routes, not everywhere); tree shaking removes unimported code but <strong>only over real ES modules with correct <code>sideEffects</code> metadata</strong> — CommonJS and side-effectful barrels defeat it.</div>"},
  "resource-hints": {"teach":"<p>At its core, resource hints are just <strong>you telling the browser \"start on this early\"</strong> — fetching a file or opening a connection <em>before</em> the browser would naturally discover it needs to. You're front-running the browser's own discovery process to save time later.</p><h4>The four hints, from heaviest to lightest</h4><ul><li><strong><code>preload</code></strong> — \"fetch this <em>now</em>, at high priority, I need it on <em>this</em> page.\" Requires an <code>as</code> attribute (<code>as=\"image\"</code>, <code>as=\"font\"</code>, etc.) so the browser knows the type and priority.</li><li><strong><code>prefetch</code></strong> — \"fetch this at <em>lowest</em> priority for the <em>next</em> navigation.\" It speculatively warms the cache for where the user is likely to go next.</li><li><strong><code>preconnect</code></strong> — \"open the full connection (DNS + TCP + TLS) to this cross-origin host early,\" so when a real request comes, the expensive handshake is already done.</li><li><strong><code>dns-prefetch</code></strong> — \"just resolve the DNS for this host.\" The cheapest hint, a good lightweight complement when you have many third-party origins.</li></ul><div class=\"analogy\"><p>Think of hosting dinner. <code>preconnect</code> is unlocking the front door and turning on the porch light before guests arrive — the slow setup is done so they walk straight in. <code>dns-prefetch</code> is just looking up the address. <code>preload</code> is pre-plating a dish you know everyone will order tonight. <code>prefetch</code> is prepping ingredients for <em>tomorrow's</em> dinner, quietly, only if there's spare time.</p></div><h4>Why use them sparingly</h4><p>Hints aren't free. Every <code>preload</code> competes for your limited bandwidth, and every <code>preconnect</code> consumes a connection slot. Over-hinting can <em>delay</em> the very resources that matter by crowding them out. Hint only the handful of things genuinely on the critical path.</p><h4>The font trap — why it downloads twice</h4><p>Here's a subtle, classic gotcha. Fonts are <strong>always fetched in CORS mode</strong> (anonymous, crossorigin). If you preload a font but forget <code>crossorigin</code>:</p><ul><li>Your preload fetches it in <em>non-CORS</em> mode and stores it under one cache key.</li><li>The browser's real font fetch uses <em>CORS</em> mode, a <em>different</em> cache key.</li><li>The two don't match, so the preload is wasted and the font downloads <strong>twice</strong>.</li></ul><p>The fix is to match modes by adding <code>crossorigin</code> to the preload. The same attribute matters for preconnecting to font/CDN origins that serve CORS resources: <code>&lt;link rel=\"preconnect\" href=\"https://cdn.example.com\" crossorigin&gt;</code> ensures the warmed connection is the one the real CORS request will reuse.</p><div class=\"key\">Resource hints front-run discovery — <code>preload</code> (this page), <code>prefetch</code> (next page), <code>preconnect</code>/<code>dns-prefetch</code> (warm the host) — but use them sparingly, and <strong>always add <code>crossorigin</code> when preloading fonts</strong> or you'll fetch them twice.</div>"},
  "font-display": {"teach":"<p>At its core, <code>font-display</code> just answers one question: <strong>while your fancy webfont is still downloading, what should the text do — hide, or show in a backup font?</strong> That single choice trades off two evils: invisible text and a visible font swap.</p><h4>The two evils, named</h4><ul><li><strong>FOIT — Flash of Invisible Text.</strong> The browser hides the text entirely during a \"block period,\" waiting for the webfont. The reader sees blank space.</li><li><strong>FOUT — Flash of Unstyled Text.</strong> The browser shows a fallback font immediately, then <em>swaps</em> in the webfont when it arrives. The reader sees text the whole time, but it visibly changes appearance.</li></ul><div class=\"analogy\"><p>Imagine waiting for a guest speaker. <strong>FOIT</strong> is leaving the stage empty and dark until the star arrives — no one's bored-proof, but the room sits in awkward silence. <strong>FOUT</strong> is having an understudy start talking immediately, then swapping in the star mid-sentence — the talk never stops, but the voice suddenly changes. <code>font-display</code> lets you pick the policy, and even tune how long you'll tolerate the empty stage.</p></div><h4>The values, by their block/swap periods</h4><p>Each value sets two windows — a <em>block period</em> (text invisible) and a <em>swap period</em> (fallback shown, swap still allowed):</p><ul><li><strong><code>swap</code></strong> — ~0 block, infinite swap. Text shows instantly in fallback, swaps whenever the webfont lands. Great for LCP (text is visible early) but the late swap risks a reflow → CLS.</li><li><strong><code>optional</code></strong> — tiny block, <em>no</em> swap. If the font isn't ready almost immediately (or cached), the browser just keeps the fallback for this page load. This <strong>eliminates layout shift</strong> because there's no swap.</li><li><strong><code>block</code></strong> — ~3s invisible (FOIT), then swap. Prioritizes \"correct font from the first pixel\" at the cost of blank text.</li><li><strong><code>fallback</code></strong> — tiny block + a short swap window, a compromise between <code>swap</code> and <code>optional</code>.</li></ul><h4>Why <code>swap</code> + metric overrides is the modern best practice</h4><p>The problem with <code>swap</code> is the reflow: the fallback and webfont have different letter widths/heights, so the swap shoves text around (CLS). The fix is to make the fallback <em>occupy the same space</em> as the webfont using <strong>metric overrides</strong> on <code>@font-face</code>: <code>size-adjust</code> scales the fallback's glyphs, and <code>ascent-override</code>/<code>descent-override</code> match line heights. Tune them so the swap is invisible — text appears early (good LCP) <em>and</em> doesn't jump (good CLS).</p><p>For example: <code>@font-face{font-family:Inter;src:url(inter.woff2);font-display:swap;size-adjust:97%}</code> shows fallback text immediately and swaps with no reflow.</p><h4>The trap</h4><p>Defaulting to <code>font-display:block</code> \"so text doesn't jump\" feels safe but backfires: it's FOIT, hiding your text for up to ~3 seconds on slow networks — which directly <strong>delays LCP</strong> if that text is your largest element. You traded a tiny visual swap for invisible content, the worse outcome.</p><div class=\"key\">Prefer <code>font-display:swap</code> paired with <code>size-adjust</code>/metric overrides — text shows early (LCP) and swaps without reflow (CLS); never reach for <code>block</code> to \"avoid jumping,\" since it hides text for seconds.</div>"},
  "caching-immutable": {"teach":"<p>At its core, immutable caching is a simple bargain: <strong>give every file a name based on its contents, and the browser can keep it forever</strong> — because if the contents ever change, the name changes too, so there's no risk of serving something stale.</p><h4>Content hashing: the fingerprint in the filename</h4><p>Your build tool computes a <strong>hash</strong> of each asset's bytes and bakes it into the filename: <code>app.4f2a9c.js</code>. That hex string is a fingerprint. Change one character of source code, the hash changes, the filename changes. So a given URL <em>always</em> maps to exactly one version of the bytes — the URL is effectively immutable.</p><div class=\"analogy\"><p>Think of it like a book's ISBN. Edit the book and it gets a brand-new ISBN — so \"give me ISBN X\" always returns the exact same edition, and a library can shelve it permanently knowing it'll never silently become a different book. Content hashes are ISBNs for your JS and CSS.</p></div><h4>Why this unlocks the strongest cache header</h4><p>Because the URL can never point at different bytes, you can serve these assets with the most aggressive cache directive there is: <code>Cache-Control: public, max-age=31536000, immutable</code>. Let's unpack it:</p><ul><li><strong><code>public</code></strong> — shared caches (CDNs, proxies) may store it.</li><li><strong><code>max-age=31536000</code></strong> — cache for one year (in seconds).</li><li><strong><code>immutable</code></strong> — the killer feature: don't even <em>revalidate</em> on reload. Normally a hard refresh sends a conditional request to check freshness; <code>immutable</code> tells the browser \"don't bother, it can't have changed.\"</li></ul><h4>The HTML's special role: atomic deploys</h4><p>If everything is cached forever, how do users ever get a new version? The answer is the <strong>HTML entry document</strong>. It's served with a <em>short TTL or no-cache</em>, so the browser re-fetches it often. That fresh HTML references the <em>new</em> hashed filenames (<code>app.9b3e1f.js</code>). The instant a user gets new HTML, they're pointed at the new assets — and since those have new URLs, they're fetched fresh while old ones stay cached harmlessly. The deploy is <strong>atomic</strong>: HTML and assets switch together, no half-updated state.</p><h4>Layering the caches</h4><p>A <strong>CDN</strong> stores these immutable assets on servers near your users, cutting latency. A <strong>service worker</strong> can go further, serving them from local Cache Storage for instant repeat loads — even offline.</p><h4>The trap</h4><p>The whole scheme inverts if you long-cache the wrong file. <strong>Never long-cache the HTML entry, or the service worker file itself.</strong> If the HTML is cached for a year, users get pinned to a stale build — its old HTML keeps pointing at old asset URLs, and there's no way for them to discover the new hashes. The fingerprint trick only works when the <em>pointer</em> (HTML) stays fresh.</p><div class=\"key\">Content-hashed filenames make assets safe to serve <code>immutable</code> for a year; keep the <strong>HTML entry (and the service worker) short-TTL</strong> so users always pick up the new hashes — that's what makes deploys atomic.</div>"},
  "service-worker": {"teach":"<p>At its core, a service worker is just <strong>a little proxy that lives in the background of the browser and can answer your app's network requests itself</strong> — often from a local cache. That's what lets a web app load instantly on repeat visits and even work offline.</p><h4>Where it sits and what it intercepts</h4><p>Once registered, the service worker sits <em>between</em> your page and the network. Every request your page makes fires a <code>fetch</code> event in the worker, and the worker decides how to respond: hand back something from <strong>Cache Storage</strong> (a persistent local store it controls), go to the network, or some mix. It's a programmable interception layer.</p><div class=\"analogy\"><p>Think of a personal assistant who screens your mail. When a letter (request) comes in, they can answer it instantly from a filing cabinet of copies they keep (cache), or go out to fetch a fresh one, or hand you the cached copy now and quietly update the cabinet for next time. You — the page — just get a fast answer.</p></div><h4>The three caching strategies</h4><ul><li><strong>Cache-first</strong> — check the cache; only hit the network if it's missing. <em>Fastest, but risks serving stale content.</em> Best for content-hashed, immutable assets.</li><li><strong>Network-first</strong> — try the network; fall back to cache. <em>Fresh when online</em>, but you need a timeout so an offline/slow network doesn't hang before falling back.</li><li><strong>Stale-while-revalidate</strong> — serve the cached copy immediately (fast), then fetch a fresh one in the background to update the cache for next time. A great balance for things that can be slightly stale.</li></ul><p>The cache-first shape in one line: <code>self.addEventListener('fetch', e =&gt; e.respondWith(caches.match(e.request).then(c =&gt; c || fetch(e.request))));</code> — return the cached response if present, otherwise fetch.</p><h4>Versioning and the kill switch</h4><p>A new service worker activates only when <code>sw.js</code> changes <strong>byte-wise</strong> — the browser compares the file and installs the new one if it differs. Two consequences follow:</p><ul><li><strong>Never long-cache <code>sw.js</code> itself.</strong> If it's cached, the browser never sees a new version and your users are frozen on old code.</li><li><strong>Always ship a <code>skipWaiting</code> / kill-switch path</strong> — a way to force the new worker to take over (or to unregister entirely). This is your escape hatch when a bad worker has cached a broken build.</li></ul><h4>The trap, and a key limit</h4><p>The classic disaster is <strong>cache-first on your app HTML/JS without versioning</strong>. The worker keeps serving the old, possibly broken build forever, and users have no way out. (Pair cache-first only with content-hashed URLs, and use network-first or stale-while-revalidate for HTML.) Finally, remember a service worker <strong>cannot speed up the very first visit</strong> — nothing is cached yet, and the worker isn't even controlling the page until the next load.</p><div class=\"key\">A service worker proxies <code>fetch</code> from Cache Storage for offline + instant repeat loads — but <strong>never cache-first your HTML/JS without versioning, never long-cache <code>sw.js</code>, and always ship a kill switch</strong>, because it can trap users on a broken build (and it can't help the first visit).</div>"},
  "lab-vs-field": {"teach":"<p>At its core, this is the difference between <strong>one careful test in a controlled room versus what your actual visitors live through</strong>. Lab data is a single reproducible measurement; field data is the messy reality aggregated across real people on real devices and networks.</p><h4>Lab data: the controlled experiment</h4><p>Lab tools — <strong>Lighthouse, WebPageTest</strong> — load your page <em>synthetically</em> on a fixed, simulated device and network. Because everything is held constant, the result is <strong>reproducible</strong>, which makes lab data perfect for two jobs:</p><ul><li><strong>CI regression gates</strong> — fail the build if a metric worsens, because run-to-run variance is low.</li><li><strong>Debugging</strong> — detailed traces show <em>why</em> something is slow (long tasks, blocking resources, etc.).</li></ul><p>But it has real limits. It reflects <em>one</em> environment, not your audience's diversity. And critically, <strong>lab tools can't measure INP</strong> — INP needs real user interactions, which don't happen in a synthetic run. Lighthouse reports <strong>Total Blocking Time (TBT)</strong> as a <em>proxy</em> for responsiveness instead.</p><div class=\"analogy\"><p>Lab data is a car's MPG rating from a controlled dynamometer test — clean, repeatable, great for comparing models. Field data is the mileage thousands of real drivers actually get in traffic, hills, and cold starts. The sticker number is useful, but it's the real-world fleet number that tells you what owners experience.</p></div><h4>Field data (RUM): the real audience</h4><p><strong>Field</strong> or <strong>RUM (Real User Monitoring)</strong> data comes from actual visits. Two main sources:</p><ul><li><strong>Chrome UX Report (CrUX)</strong> — Google's aggregated field dataset, the source of truth for Core Web Vitals.</li><li><strong>The <code>web-vitals</code> library</strong> — which uses the browser's <code>PerformanceObserver</code> API to capture each real user's metrics and send them to your own backend.</li></ul><p>RUM aggregates these at the <strong>p75</strong> — the value 75% of your users beat — so you're held to the experience of roughly three out of four real visitors, not your fast dev machine.</p><h4>Which one actually decides your grade</h4><p>This is the crux: <strong>field data determines Core Web Vitals pass/fail and the page-experience signal. Lab data is diagnostic only.</strong> Lab tells you <em>why</em>; field tells you <em>whether you passed</em>.</p><h4>The trap</h4><p>Treating a <strong>green Lighthouse score as \"passing Core Web Vitals.\"</strong> It isn't — only p75 field data counts. Slow real devices, flaky networks, and third-party scripts loaded in the wild routinely make field numbers diverge sharply from a pristine lab run. To know the truth, instrument RUM: <code>import {onINP} from 'web-vitals'; onINP(({value,id}) =&gt; beacon('/rum', {value,id}));</code>.</p><div class=\"key\">Lab data is reproducible and diagnostic (great for CI, but can't measure INP — it uses TBT as a proxy); <strong>only p75 field/RUM data determines Core Web Vitals pass/fail</strong>, so a green Lighthouse score is a clue, not a verdict.</div>"}
});
