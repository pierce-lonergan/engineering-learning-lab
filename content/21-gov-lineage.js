/* Data Lineage & Provenance (GOV-LINEAGE) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"GOV-LINEAGE","front":"Data lineage vs. provenance — what's the distinction?","back":"<strong>Provenance</strong> is the origin/derivation history of a data item (where it came from, who/what produced it). <strong>Lineage</strong> is the broader map of how data <em>flows and transforms</em> across systems over time. In practice the terms are used interchangeably, but provenance emphasizes the per-record audit trail and lineage emphasizes the structural dependency graph between datasets, tables, and columns."},
  {"sec":"GOV-LINEAGE","front":"Table-level vs. column-level lineage — what's the difference and why does column-level matter?","back":"<strong>Table-level</strong> records that table B was derived from table A. <strong>Column-level</strong> (field-level) records that <code>B.revenue</code> was computed from <code>A.price</code> and <code>A.qty</code>. Column-level enables <em>precise</em> impact analysis (which exact downstream columns break if you drop a source column), accurate PII/sensitivity propagation, and tighter root-cause debugging — at the cost of much harder capture (you must parse the transformation logic, not just the job's read/write sets)."},
  {"sec":"GOV-LINEAGE","front":"What are the four canonical reasons a data platform invests in lineage?","back":"<ul><li><strong>Impact analysis</strong> — 'if I change/drop this column or table, what downstream breaks?'</li><li><strong>Root-cause debugging</strong> — trace a bad number upstream to the offending source/transform.</li><li><strong>Regulatory/audit proof</strong> — demonstrate how a reported figure was derived (e.g. BCBS 239, SOX, GDPR data-flow accountability).</li><li><strong>Trust</strong> — let consumers see a dataset's origins and freshness so they know whether to rely on it.</li></ul>"},
  {"sec":"GOV-LINEAGE","front":"Name the three main mechanisms for capturing lineage automatically.","back":"<ul><li><strong>SQL/query-log parsing</strong> — parse executed SQL (e.g. warehouse query history) to infer source→target column mappings.</li><li><strong>Orchestrator hooks</strong> — emit run/job events from Airflow, Dagster, etc. as tasks execute.</li><li><strong>Transformation-framework metadata</strong> — read the DAG a tool already builds (dbt's <code>manifest.json</code>, Spark logical plans) for free, accurate lineage.</li></ul>"},
  {"sec":"GOV-LINEAGE","front":"What is OpenLineage and what problem does it solve?","back":"<strong>OpenLineage</strong> is an open standard and spec for collecting lineage metadata. It defines a JSON event model — <code>RUN</code> events (<code>START</code>/<code>COMPLETE</code>/<code>FAIL</code>/<code>ABORT</code>) referencing a <code>JOB</code> and its input/output <code>DATASET</code>s — emitted by producers (Airflow, Spark, dbt, Flink) to any compatible backend (Marquez, DataHub, etc.). It solves the N×M integration problem so every tool doesn't need a bespoke lineage connector for every catalog."},
  {"sec":"GOV-LINEAGE","front":"How does lineage fit into the metadata graph as a 'core layer'?","back":"Lineage is one layer of the unified metadata graph alongside <strong>technical</strong> metadata (schemas, types), <strong>operational</strong> metadata (run history, freshness), and <strong>social/business</strong> metadata (owners, tags, glossary terms). Lineage supplies the <em>edges</em> — the directed dependencies between dataset/column nodes — that let the catalog answer reachability questions like impact and root-cause across the entire graph."},
  {"sec":"GOV-LINEAGE","front":"Why is SQL/query-log parsing both attractive and fragile as a lineage source?","back":"Attractive because it's <strong>passive</strong> — you reconstruct lineage from query history with no pipeline instrumentation. Fragile because: dynamic SQL and stored procs are opaque, <code>SELECT *</code> and wildcard expansion hide column mappings, UDFs/UDTFs are black boxes, and dialect coverage in the SQL parser is never 100%. It also can't see transforms done outside the warehouse (Python, Spark UDFs)."},
  {"sec":"GOV-LINEAGE","front":"What makes dbt a high-quality lineage source compared to query-log parsing?","back":"dbt resolves <code>ref()</code> and <code>source()</code> calls into an explicit DAG and serializes it to <code>manifest.json</code> — so model-level lineage is <em>declared, not inferred</em>, and is exact. Column-level lineage still requires parsing each model's compiled SQL (dbt's column-level lineage and tools like <code>sqlglot</code> do this), so it's table-level for free and column-level with extra parsing."},
  {"sec":"GOV-LINEAGE","front":"What is 'logical' vs. 'physical' lineage and why does the distinction bite?","back":"<strong>Physical</strong> lineage tracks actual table/file dependencies as data moves. <strong>Logical</strong> lineage tracks business-level dataset relationships independent of storage (e.g. a 'Customer' entity spanning many physical tables). Interview trap: views, CTAS rebuilds, and table swaps can make physical lineage churn while the logical relationship is stable — good catalogs reconcile the two so consumers aren't confused by physical refactors."},
  {"sec":"GOV-LINEAGE","front":"How does column-level lineage strengthen PII and sensitivity governance?","back":"Sensitivity is a <em>propagating</em> property: if a column is tagged PII, every downstream column derived from it should inherit (or at least be flagged for) that classification. Column-level lineage is the propagation graph that lets a catalog auto-tag derived columns, drive masking/policy enforcement on copies, and prove to auditors that PII didn't silently leak into an 'analytics' table via a join."},
  {"sec":"GOV-LINEAGE","front":"Why can't lineage be fully reconstructed after the fact, and what's the design implication?","back":"Once a job runs without emitting events, the source→target mapping for that run is lost — query logs are retained only for a window, transient/ephemeral compute leaves no trace, and reverse-engineering deleted intermediates is impossible. Implication: lineage must be captured <strong>at execution time</strong> (push events from the runtime), not bolted on by periodic scraping, if you want completeness and per-run granularity."},
  {"sec":"GOV-LINEAGE","front":"What's the difference between static (design-time) and runtime (observed) lineage?","back":"<strong>Static</strong> lineage is parsed from code/SQL/DAG definitions before execution — complete but may include branches that never run. <strong>Runtime</strong> lineage is emitted from actual executions (OpenLineage run events) — reflects what truly happened, captures dynamic targets, and carries operational facts (row counts, timing, success/fail) but only for paths that actually ran. Mature platforms fuse both."},
  {"sec":"GOV-LINEAGE","front":"How do you use lineage for impact analysis before a breaking change?","back":"Treat lineage as a directed graph and compute the <strong>downstream transitive closure</strong> from the node you're changing (the column/table) to find every dependent dataset, dashboard, and ML feature. Rank by criticality/owner, notify, and gate the change. The same closure <em>upstream</em> answers root-cause ('what feeds this bad value?'). Column-level edges make the blast radius precise instead of over-broad."},
  {"sec":"GOV-LINEAGE","front":"What are common failure modes that make a lineage graph untrustworthy?","back":"<ul><li><strong>Gaps</strong> — un-instrumented hops (a Python script, a hand-run export) break the chain silently.</li><li><strong>Staleness</strong> — graph reflects old pipeline versions after a refactor.</li><li><strong>Coarseness</strong> — only table-level, so impact analysis over-alarms.</li><li><strong>Ghost edges</strong> — parser infers links that don't reflect real runtime behavior.</li></ul> A lineage graph that's 80% complete is dangerous because users trust the missing 20% as 'no dependency.'"},
  {"sec":"GOV-LINEAGE","front":"Where does Marquez fit relative to OpenLineage?","back":"<strong>OpenLineage</strong> is the <em>specification + client libraries</em> that emit lineage events; <strong>Marquez</strong> is the reference <em>backend/metadata store</em> that ingests those events, persists the run/job/dataset model, and serves the lineage graph + APIs. OpenLineage was spun out of the Marquez project (Marquez came first; OpenLineage was extracted as a vendor-neutral standard). Other backends (DataHub, OpenMetadata, commercial catalogs) also consume OpenLineage events."}
);

Object.assign(GLOSSARY_DATA, {
  "data-lineage": {"term":"Data Lineage","group":"GOV-LINEAGE","aliases":["lineage","data lineage","lineage graph"],"eli5":"A map showing where each piece of data came from and every step it passed through to get to its current form.","memory":"Lineage = the data's family tree: ancestors (where it came from) and descendants (what breaks if you change it).","full":"<p>Data lineage is the directed graph of dependencies that records how data flows and transforms from sources, through pipelines, into derived datasets, dashboards, and ML features. Nodes are datasets (and optionally columns); edges are the produce/consume relationships created by jobs. It is the structural backbone for impact analysis, root-cause debugging, audit, and consumer trust.</p><p>Lineage exists at multiple granularities (table vs. column) and from multiple sources (static code parsing vs. runtime events). Its value is gated by <em>completeness</em>: a single un-instrumented hop creates a silent gap, and consumers wrongly read 'no edge' as 'no dependency.'</p>","trap":"Candidates pitch lineage as a 'nice visualization.' Senior framing: it's a queryable dependency graph whose primary product is the transitive closure (downstream = blast radius, upstream = root cause), not a picture.","worked":"A revenue dashboard shows a wrong total. You walk lineage upstream: dashboard ← <code>fct_orders</code> ← <code>stg_orders</code> ← raw <code>orders</code> source, and discover <code>stg_orders</code> filtered a currency incorrectly — root cause found in minutes, not days.","related":["data-provenance","column-level-lineage","openlineage","impact-analysis","lineage-capture"]},
  "data-provenance": {"term":"Data Provenance","group":"GOV-LINEAGE","aliases":["provenance","data provenance"],"eli5":"The documented origin story and chain of custody for a specific piece of data.","memory":"Provenance = a painting's certificate of authenticity; lineage = the museum's map of how every painting got to every room.","full":"<p>Provenance is the recorded origin and derivation history of a data item — which source produced it, by which process, when, and through what transformations. Where lineage emphasizes the system-level dependency structure, provenance emphasizes the <em>per-record audit trail</em> and chain of custody used to prove how a specific reported value came to be.</p><p>In regulated contexts (BCBS 239 for banks, SOX financial-reporting controls, GDPR's data-flow accountability and Article 15 right of access, clinical/scientific reproducibility) provenance is the evidence that a figure is correct and reproducible. In practice the two terms overlap heavily and many catalogs use 'lineage' as the umbrella label.</p>","trap":"Don't treat them as synonyms in an audit conversation: provenance must answer 'prove how THIS number was derived,' which often demands per-run, record-level detail that coarse table-level lineage cannot supply.","related":["data-lineage"]},
  "column-level-lineage": {"term":"Column-Level Lineage","group":"GOV-LINEAGE","aliases":["column-level lineage","field-level lineage","column lineage"],"eli5":"Lineage that tracks which exact source columns feed each exact output column, not just which tables feed which tables.","memory":"Table-level = 'this building connects to that building'; column-level = 'this wire connects to that outlet.'","full":"<p>Column-level (field-level) lineage maps each output column to the specific input columns and expressions that produce it (e.g. <code>margin = (price - cost) / price</code>). It is strictly more powerful than table-level lineage: it makes impact analysis precise (only the truly affected downstream columns are flagged), propagates sensitivity/PII tags accurately, and pinpoints root cause to a single field.</p><p>The cost is capture difficulty: you must parse and resolve the transformation logic (SQL ASTs via tools like <code>sqlglot</code>, Spark logical plans, dbt compiled SQL), often resolving column references against table schemas, and handle <code>SELECT *</code> expansion, joins, window functions, and UDFs. Coverage is rarely 100%, so most platforms ship table-level everywhere and column-level where parsers succeed.</p>","trap":"Interviewers probe the failure modes: <code>SELECT *</code>, UDFs, dynamic SQL, and cross-language hops (SQL → Python) are where column-level lineage silently degrades to table-level or breaks entirely.","worked":"Dropping <code>users.email</code>: table-level lineage flags all 40 tables that read <code>users</code>; column-level lineage flags only the 3 columns actually derived from <code>email</code>, so you avoid 37 false alarms.","related":["data-lineage","table-level-lineage","impact-analysis","lineage-capture"]},
  "table-level-lineage": {"term":"Table-Level Lineage","group":"GOV-LINEAGE","aliases":["table-level lineage","dataset-level lineage"],"eli5":"Coarse lineage that records dataset-to-dataset dependencies without knowing which columns map to which.","memory":"Table-level is the highway map; column-level is the street-by-street GPS route.","full":"<p>Table-level (dataset-level) lineage records that one table/file/topic was produced from others, but not the intra-column mapping. It is cheap to capture — you only need a job's read-set and write-set, which orchestrator hooks and even crude log parsing can supply — and it answers many questions (refresh ordering, broad blast radius, basic root-cause scoping).</p><p>Its limitation is precision: an impact query over table-level lineage over-alarms because it can't tell which downstream columns actually depend on the changed source column. It's the pragmatic default layer; column-level is layered on top where parsing allows.</p>","trap":"Claiming table-level lineage suffices for PII governance is a red flag — sensitivity propagation and precise impact need column-level edges.","related":["column-level-lineage","data-lineage","lineage-capture"]},
  "openlineage": {"term":"OpenLineage","group":"GOV-LINEAGE","aliases":["openlineage","open lineage","ol"],"eli5":"An open standard for emitting lineage events so any pipeline tool can report to any catalog without custom glue.","memory":"OpenLineage is the 'USB-C of lineage' — one connector shape so producers and catalogs interoperate.","full":"<p>OpenLineage is a vendor-neutral specification (an LF AI &amp; Data Foundation graduate project) defining a JSON event model for lineage. Producers emit <code>RUN</code> events (<code>START</code>, <code>COMPLETE</code>, <code>FAIL</code>, <code>ABORT</code>) that reference a <code>JOB</code> and its input/output <code>DATASET</code>s, with extensible <em>facets</em> carrying schema, column-lineage, data-quality, and source-code details. Integrations exist for Airflow, Spark, dbt, Flink, and more.</p><p>It solves the N×M problem: instead of every orchestrator building a bespoke connector for every catalog, producers emit a common event that any compatible backend (Marquez, DataHub, OpenMetadata, commercial catalogs) ingests. Runtime emission also gives per-run, operational lineage rather than after-the-fact guesses.</p>","trap":"Know the layering: OpenLineage is the <em>spec + clients that emit events</em>; Marquez (its reference implementation) and DataHub are <em>backends that store and serve</em> them. Conflating the standard with a specific store signals shallow knowledge.","worked":"Enabling the Airflow OpenLineage provider makes each task emit START/COMPLETE events with input/output datasets; Marquez ingests them and the lineage graph populates automatically with no per-DAG wiring.","related":["lineage-capture","metadata-graph"]},
  "lineage-capture": {"term":"Lineage Capture Mechanisms","group":"GOV-LINEAGE","aliases":["lineage capture","capturing lineage","lineage collection"],"eli5":"The different ways a platform discovers lineage: parse the SQL, hook the orchestrator, or read the transform tool's own metadata.","memory":"Three taps: read the SQL (parse), watch the runs (hooks), or ask the tool (framework metadata).","full":"<p>Three dominant capture strategies, often combined: (1) <strong>SQL/query-log parsing</strong> — reconstruct source→target mappings from executed SQL in warehouse query history; passive but fragile to dynamic SQL, UDFs, and <code>SELECT *</code>. (2) <strong>Orchestrator/runtime hooks</strong> — emit events as jobs run (Airflow/Dagster/Spark via OpenLineage); accurate and per-run but requires instrumenting every executor. (3) <strong>Transformation-framework metadata</strong> — read the DAG the tool already builds (dbt <code>manifest.json</code>, Spark logical plan); high-fidelity and free where the framework owns the transform.</p><p>Each has blind spots, so completeness comes from layering them and accepting that the weakest-covered hop sets the trust ceiling for the whole graph.</p>","trap":"A graph stitched only from query-log parsing has invisible gaps wherever transforms happen outside the warehouse (Python jobs, external APIs) — and those gaps look identical to 'no dependency.'","worked":"A platform uses dbt manifest for warehouse models (exact), OpenLineage Spark for the lake jobs, and query-log parsing for ad-hoc BI extracts — three sources merged into one catalog graph.","related":["openlineage","data-lineage","column-level-lineage","metadata-graph"]},
  "impact-analysis": {"term":"Impact Analysis","group":"GOV-LINEAGE","aliases":["impact analysis","blast radius","downstream impact"],"eli5":"Using lineage to answer 'if I change or drop this, what downstream breaks?' before you do it.","memory":"Walk the arrows forward = blast radius; walk them backward = root cause.","full":"<p>Impact analysis computes the <strong>downstream transitive closure</strong> over the lineage graph from a node you intend to change (deprecate a table, drop/rename a column, alter a type) to enumerate every dependent dataset, dashboard, alert, and ML feature. It converts a risky change into a reviewed, notified, gated one, and the inverse closure (upstream) drives root-cause debugging.</p><p>Granularity controls quality: table-level closure over-alarms (flags every consumer of the table); column-level closure flags only true dependents. It's the single most cited business justification for lineage in platform interviews.</p>","trap":"Over-broad impact analysis (from table-level-only lineage) trains teams to ignore the alerts — false positives are as corrosive to trust as missing dependencies.","worked":"Before renaming <code>orders.amount</code> → <code>orders.gross_amount</code>, a column-level closure surfaces 6 downstream models and 2 dashboards; you ship a compatibility view and notify those 4 owners instead of breaking prod.","related":["data-lineage","column-level-lineage","table-level-lineage"]},
  "metadata-graph": {"term":"Lineage as a Metadata-Graph Layer","group":"GOV-LINEAGE","aliases":["metadata graph","metadata layer","active metadata"],"eli5":"Lineage is the 'edges' layer of the catalog's knowledge graph that connects all the other metadata about your data.","memory":"Other metadata describes the nodes; lineage IS the edges that make it a graph you can traverse.","full":"<p>A modern catalog models metadata as a graph: nodes for datasets, columns, jobs, dashboards, users, and glossary terms; layers for <em>technical</em> (schema/type), <em>operational</em> (runs, freshness, quality), <em>social/business</em> (owners, tags, definitions), and <em>lineage</em> metadata. Lineage supplies the directed produce/consume <strong>edges</strong> that make the graph traversable.</p><p>Those edges are what turn a passive inventory into an active system: reachability queries power impact analysis and root cause, sensitivity tags propagate along edges, and freshness/quality signals flow downstream to warn consumers. Lineage is therefore not a standalone feature but the connective tissue of the whole governance graph.</p>","trap":"Treating lineage as a separate siloed product (its own tool, disconnected from the catalog) is the anti-pattern — its value comes from being joined to ownership, quality, and classification in one graph.","related":["data-lineage","openlineage","impact-analysis"]}
});
