/* Teach me from the ground up: FE-A11Y (auto-assembled) */
enh({
  "pour": {"teach":"<p>At its core, <strong>POUR</strong> is just a way of organizing the question \"can everyone actually use this?\" into four buckets, so that a giant pile of accessibility rules stops feeling random.</p><h4>The four buckets</h4><p>WCAG (the Web Content Accessibility Guidelines) contains dozens of specific checks called <em>success criteria</em>. Instead of memorizing them as a flat list, POUR groups every one under four principles:</p><ul><li><strong>P — Perceivable:</strong> can users <em>sense</em> the content with whatever sense they have available? Text alternatives for images, captions for video, enough color contrast. If a blind user can't perceive your image and there's no alt text, the content simply does not exist for them.</li><li><strong>O — Operable:</strong> can users <em>drive</em> the interface? Everything must work by keyboard, you must give enough time, and you must not flash content that could trigger seizures.</li><li><strong>U — Understandable:</strong> is it <em>predictable and readable</em>? Pages behave consistently, language is clear, and forms tell you how to fix mistakes.</li><li><strong>R — Robust:</strong> can the content survive contact with <em>different and future</em> assistive technologies? It must expose a name, role, and value that screen readers and other tools can read.</li></ul><div class=\"analogy\"><p>Think of a public building. <strong>Perceivable</strong> = the signs are large and in Braille. <strong>Operable</strong> = there's a ramp and the doors are wide enough for a wheelchair. <strong>Understandable</strong> = the floor plan is logical and the room labels make sense. <strong>Robust</strong> = the building works whether you arrive on foot, by wheelchair, or in some new mobility device invented next year. Miss any one and someone gets stranded.</p></div><h4>The grading: A / AA / AAA</h4><p>Each criterion is also tagged with a conformance level. <strong>A</strong> is the bare minimum, <strong>AA</strong> is the real-world target, and <strong>AAA</strong> is enhanced. Here is the part most people get wrong: AAA is <em>not</em> the goal. WCAG itself explicitly says you should not require AAA across a whole site, because some AAA criteria are impossible for certain content. <strong>AA is the de-facto legal and contractual standard worldwide</strong> — it's what ADA case law, US Section 508, and Europe's EN 301 549 point to.</p><p>Why does this matter in practice? When a contract or lawsuit says \"must be accessible,\" it almost always means \"WCAG 2.x level AA.\" Chasing AAA everywhere wastes effort and can even be unachievable; missing AA can be a legal liability.</p><p>To see the buckets concretely: criterion 1.1.1 (alt text) is <em>Perceivable</em>; 2.1.1 (keyboard operation) is <em>Operable</em>; 3.3.1 (error identification) is <em>Understandable</em>; 4.1.2 (name/role/value) is <em>Robust</em>.</p><div class=\"key\">POUR is the mental filing cabinet for all of WCAG — Perceivable, Operable, Understandable, Robust — and the target everyone actually ships to and is held to legally is level AA, not AAA.</div>"},
  "wcag-levels": {"teach":"<p>At its core, the WCAG levels are just three strictness dials — <strong>A</strong>, <strong>AA</strong>, and <strong>AAA</strong> — and the version numbers (2.0, 2.1, 2.2) are just editions that added newer rules over time.</p><h4>The three levels</h4><p>Every individual rule in WCAG (a \"success criterion\") carries a level tag:</p><ul><li><strong>A</strong> — the minimum, blocking floor. Fail one of these and the page has a serious, fundamental barrier.</li><li><strong>AA</strong> — the standard, real-world target. This is the level that laws and contracts reference. When someone says \"we need to be WCAG compliant,\" they mean AA.</li><li><strong>AAA</strong> — enhanced and situational. Nice-to-have, applied selectively, never required wholesale.</li></ul><div class=\"analogy\"><p>Think of cleanliness standards for a restaurant kitchen. <strong>A</strong> = \"no rats, food isn't poisonous\" (you'll be shut down otherwise). <strong>AA</strong> = the health-code grade you must post on the door to legally operate. <strong>AAA</strong> = a Michelin-level spotless kitchen — admirable, but no inspector will close you for not reaching it everywhere.</p></div><h4>The versions add criteria over time</h4><p>WCAG evolves. Each new version is backward-compatible — it <em>adds</em> criteria rather than rewriting old ones:</p><ul><li><strong>WCAG 2.1 (2018)</strong> added criteria for mobile devices, low vision, and cognitive disabilities — things the 2008 original didn't anticipate.</li><li><strong>WCAG 2.2 (October 2023)</strong> added several practical ones: <em>Focus Not Obscured</em> (a sticky header mustn't hide the focused element), <em>Dragging Movements</em> (provide a non-drag alternative), <em>Target Size (Minimum)</em>, and <em>Accessible Authentication</em> (don't force users to memorize or transcribe to log in).</li></ul><h4>The trap: 4.1.1 Parsing is gone</h4><p>Here's the fact that catches out even experienced engineers. WCAG 2.2 <strong>removed</strong> success criterion 4.1.1 \"Parsing\" (the old rule about perfectly valid HTML — no duplicate IDs, properly nested tags). It was removed because modern browsers and assistive technologies now handle malformed markup robustly, so the rule no longer mapped to a real-world barrier. If someone tells you valid HTML is still a WCAG conformance requirement, they're working from outdated knowledge — as of 2.2 it is not.</p><h4>A concrete new rule</h4><p><strong>2.5.8 Target Size (Minimum)</strong> is a new AA criterion in 2.2: interactive targets must be at least <code>24×24</code> CSS pixels, or have adequate spacing around them (with some exceptions like inline links). This protects people with motor tremors or fat-finger taps on phones.</p><div class=\"key\">A/AA/AAA are strictness tiers with AA as the legal target; versions 2.1 and 2.2 only add criteria — and crucially, 2.2 deleted the old 4.1.1 \"valid HTML\" parsing rule, so it is no longer a conformance requirement.</div>"},
  "aria-rules": {"teach":"<p>At its core, the <strong>5 Rules of ARIA</strong> are a single piece of hard-won wisdom: use real HTML whenever you can, and remember that ARIA only changes what a screen reader <em>says</em> — it never changes what an element <em>does</em>.</p><h4>What ARIA even is</h4><p>ARIA (Accessible Rich Internet Applications) is a set of HTML attributes like <code>role</code>, <code>aria-expanded</code>, and <code>aria-label</code>. They feed extra information to the browser's accessibility tree, which screen readers read aloud. That's it. They are labels and annotations, not behavior.</p><h4>The five rules</h4><ul><li><strong>1. Prefer a native element.</strong> If a <code>&lt;button&gt;</code> or <code>&lt;select&gt;</code> does the job, use it — you get focus, keyboard handling, and semantics for free.</li><li><strong>2. Don't override native semantics.</strong> Don't put <code>role=\"heading\"</code> on a <code>&lt;button&gt;</code>. You confuse the accessibility tree.</li><li><strong>3. All interactive ARIA must be keyboard-operable.</strong> If you build a custom widget, it must work without a mouse.</li><li><strong>4. Don't hide focusable elements.</strong> Don't slap <code>role=\"presentation\"</code> or <code>aria-hidden=\"true\"</code> on something the user can still Tab to — they'll land on an invisible-to-the-screen-reader control.</li><li><strong>5. Every interactive element needs an accessible name.</strong> An unnamed button is announced as just \"button,\" which is useless.</li></ul><h4>The deepest point: ARIA adds zero behavior</h4><p>This is the lesson that separates juniors from seniors. Writing <code>&lt;div role=\"button\"&gt;</code> changes <em>only</em> the announced role to \"button.\" It does <strong>not</strong> make the div focusable, does not make Enter or Space activate it, and does not give it a click behavior. You must supply every bit of that yourself in JavaScript and with <code>tabindex</code>.</p><div class=\"analogy\"><p>ARIA is like a name tag at a conference that reads \"Dr. Smith, Surgeon.\" Pinning that tag on someone does not give them medical skills or let them perform surgery — it only changes what people <em>expect</em>. If the wearer can't actually operate, you've created a dangerous mismatch: people trust the label and get hurt. That's exactly why \"<strong>no ARIA is better than bad ARIA</strong>\" — a button that announces itself as a button but doesn't respond to the keyboard is worse than a plain, honest div.</p></div><h4>Why it bites</h4><p>Misused ARIA actively lies to users. A screen-reader user hears \"button,\" presses Space expecting activation, and nothing happens — they're stuck with no way to know why. The native <code>&lt;button&gt;</code> would have just worked.</p><p>So when you do go custom: <code>&lt;div role=\"button\" tabindex=\"0\"&gt;</code> still needs a <code>keydown</code> handler that fires the action on Enter <em>and</em> Space, plus a click handler — you are re-implementing everything the native element gave you for free.</p><div class=\"key\">ARIA describes, it never behaves: adding a role or state attribute changes only what assistive tech announces, so always reach for native HTML first and wire up every behavior yourself when you don't.</div>"},
  "accessible-name": {"teach":"<p>At its core, the <strong>accessible name</strong> is just the one short label a screen reader speaks when it lands on a control — like hearing \"Search, button\" — and there's a strict, well-defined recipe for which text wins.</p><h4>Why a recipe is needed</h4><p>A single button might have an icon, a tooltip, a wrapping label, and an aria attribute all at once. The browser can't read all of them, so the <strong>accname</strong> specification defines a precise priority order to pick exactly one name. Knowing this order lets you predict what users will actually hear.</p><h4>The precedence order (highest wins)</h4><ul><li><strong>1. <code>aria-labelledby</code></strong> — points to the IDs of other elements; their text becomes the name. Highest priority. It can reference multiple elements (even hidden ones) and concatenates them.</li><li><strong>2. <code>aria-label</code></strong> — a string you type directly on the element.</li><li><strong>3. Host-language labeling</strong> — the native HTML way: <code>&lt;label for&gt;</code> on inputs, <code>alt</code> on images, <code>&lt;legend&gt;</code> on fieldsets, <code>&lt;caption&gt;</code> on tables.</li><li><strong>4. Element text content / subtree</strong> — the visible text inside, e.g. the word \"Save\" inside a <code>&lt;button&gt;</code>.</li><li><strong>5. <code>title</code></strong> — the tooltip attribute, used only as a last-resort fallback.</li></ul><div class=\"analogy\"><p>Imagine a parcel arriving with several address labels stuck on it. The postal worker is told: \"If there's a forwarding sticker (<code>aria-labelledby</code>), obey that first. No sticker? Use the hand-written address (<code>aria-label</code>). None? Use the printed shipping label (native HTML labeling). Still nothing? Read whatever's scrawled on the box itself (text content). Truly desperate? Squint at the faint return-address stamp (<code>title</code>).\" The worker reads exactly one address — the first one found going down the list — and ignores the rest.</p></div><h4>Powerful, but a trap</h4><p>Because <code>aria-labelledby</code> sits at the top, it can <em>override the visible text</em>. That's powerful — you can compose a name from several visible labels — but it creates a real danger: the name a screen-reader user hears can silently diverge from what a sighted user reads. If your button visibly says \"Submit\" but <code>aria-label</code> says \"Send form,\" voice-control users who say \"click Submit\" may fail. Keep the accessible name and visible text aligned.</p><h4>The classic mistake</h4><p>Putting <code>aria-label</code> on a plain <code>&lt;span&gt;</code> or <code>&lt;div&gt;</code> with no role and expecting it to be announced. On a generic, non-interactive element, the name computation generally ignores it — there's no role for the name to attach to. <code>aria-label</code> belongs on interactive or roled elements.</p><p>And note: the <em>description</em> (<code>aria-describedby</code>) is a separate string, announced <em>after</em> the name, not part of it.</p><p>A clean example: <code>&lt;button aria-label=\"Close\"&gt;&lt;svg…/&gt;&lt;/button&gt;</code> — the icon has no text, so <code>aria-label</code> supplies the name \"Close.\"</p><div class=\"key\">A control's spoken name is resolved by one strict priority chain — aria-labelledby → aria-label → native labeling → text content → title — and the first match wins, so know the order and keep that name matching the visible text.</div>"},
  "name-role-value": {"teach":"<p>At its core, WCAG <strong>4.1.2 Name, Role, Value</strong> says every control on your page must be able to answer three questions for assistive tech: <em>what am I called, what kind of thing am I, and what's my current state?</em></p><h4>The three things every control must expose</h4><ul><li><strong>Name</strong> — the accessible name, the label a screen reader announces (e.g. \"Subscribe\").</li><li><strong>Role</strong> — what kind of control it is: button, checkbox, slider, tab, menu.</li><li><strong>Value / State</strong> — its current condition: is the checkbox checked? is the disclosure expanded? what's in the text field? Expressed through attributes like <code>aria-checked</code>, <code>aria-expanded</code>, <code>aria-selected</code>, or <code>value</code>.</li></ul><p>And there's a fourth, often-forgotten requirement baked in: when any of these <strong>change</strong>, you must notify assistive tech — meaning you update the attribute so the accessibility tree reflects reality.</p><div class=\"analogy\"><p>Picture a blind person navigating a control panel by touch and a helper who describes each control aloud. For every switch the helper must say three things: \"This is the <em>Cabin Lights</em> switch (name), it's a <em>toggle</em> (role), and it's currently <em>off</em> (value).\" If the helper names and types it but never mentions on/off — or worse, says \"off\" after you've already flipped it on — the person flips it blindly and gets the opposite of what they wanted. The screen reader <em>is</em> that helper, and your ARIA attributes are the script it reads from.</p></div><h4>Native elements give you all three for free</h4><p>A real <code>&lt;input type=\"checkbox\"&gt;</code> automatically reports role \"checkbox,\" reflects its checked state, and takes its name from a <code>&lt;label&gt;</code> — the browser handles everything. This is the strongest argument for native HTML: 4.1.2 is satisfied for free.</p><p>The moment you go custom — building a toggle out of a <code>&lt;div&gt;</code> or styled <code>&lt;button&gt;</code> — you take on the full burden. You must add the role, supply the name, set the initial state, <em>and</em> keep the state attribute in sync with the visual state on every single interaction.</p><h4>The gotcha that bites</h4><p>The most common failure is setting state once and forgetting it. You write <code>aria-expanded=\"false\"</code> on a disclosure button, the user clicks, the panel visually opens — but your JavaScript never flips the attribute to <code>\"true\"</code>. Now the screen reader insists it's collapsed while it's plainly open. <strong>Stale state is worse than no state</strong>, because it confidently misleads. Every toggle handler must update both the pixels and the attribute together.</p><p>Concrete example of a custom switch done right: start with <code>&lt;button role=\"switch\" aria-checked=\"false\"&gt;</code>, and inside the click handler, flip <code>aria-checked</code> to <code>\"true\"</code> the instant you flip the visual state.</p><div class=\"key\">Every interactive control must announce its name, its role, and its live value/state — native elements do this automatically, but custom widgets must replicate all three and, above all, keep the state attribute synced with reality on every interaction.</div>"},
  "focus-management": {"teach":"<p>At its core, <strong>focus management</strong> is just deliberately moving the keyboard's \"you are here\" marker to the right place when the screen changes — and putting it back afterward — so keyboard and screen-reader users never get lost.</p><h4>The problem it solves</h4><p>Sighted mouse users see a modal pop up and look at it. But a keyboard user's focus is an invisible cursor sitting on whatever element they last touched. If you open a dialog and <em>don't</em> move focus, that cursor stays behind the modal — often falling back to <code>&lt;body&gt;</code> — and the user is stranded, tabbing through hidden page content they can't even see has appeared.</p><p>So whenever UI appears or disappears — modals, route changes, expanding panels — you must <strong>move focus</strong> into the new content and later <strong>restore</strong> it.</p><h4>The focus trap</h4><p>A <strong>focus trap</strong> keeps the Tab key cycling <em>within</em> an open modal, so pressing Tab at the last element loops back to the first instead of escaping into the dimmed page behind. The rest of the page is marked <code>inert</code> so it can't be reached or read.</p><div class=\"analogy\"><p>Think of a revolving door at the entrance of a building. While you're in the modal \"room,\" the revolving door (the trap) keeps you circulating among the chairs in that room no matter how many times you push — you can't accidentally wander into the locked offices behind. But there's always a clearly marked EXIT button (the Esc key). Build a revolving door with <em>no</em> exit and you've literally trapped someone — which is exactly the WCAG 2.1.2 \"keyboard trap\" failure. The trap must always have an escape.</p></div><h4>The full lifecycle</h4><ol><li><strong>On open:</strong> save the trigger element — <code>const previouslyFocused = document.activeElement</code> — so you remember where the user came from.</li><li>Move focus into the dialog (the first control, or the dialog itself).</li><li>Loop Tab and Shift+Tab at the edges; mark the background <code>inert</code>.</li><li>Close on <strong>Esc</strong> (mandatory — no escape path is a keyboard-trap failure).</li><li><strong>On close:</strong> return focus to the saved trigger: <code>previouslyFocused?.focus()</code>.</li></ol><h4>The native shortcut</h4><p>The HTML <code>&lt;dialog&gt;</code> element with <code>.showModal()</code> handles most of this for you — focus moves in, the rest of the page becomes inert, and Esc closes it. Reach for it before hand-rolling a trap.</p><h4>The bite</h4><p>The single most common mistake is trapping and moving focus correctly but <strong>forgetting to restore</strong> it on close. The modal closes and the user's invisible cursor is back at the top of the page, forcing them to tab all the way down again to where they were. Saving <code>document.activeElement</code> on open and calling <code>.focus()</code> on it at close is what fixes it.</p><div class=\"key\">When UI appears or vanishes, move focus into it, trap Tab inside modals with a guaranteed Esc exit, and — the part everyone forgets — restore focus to the element that opened it when you're done.</div>"},
  "focus-visible": {"teach":"<p>At its core, <code>:focus-visible</code> is just a smarter way to draw the \"you are here\" outline — showing it for people navigating by keyboard, but not flashing it when someone simply clicks with a mouse.</p><h4>Why the focus ring exists</h4><p>When you Tab through a page, the browser draws an outline around the focused element so you can see where you are. WCAG <strong>2.4.7 Focus Visible (AA)</strong> requires this indicator. Without it, a keyboard user is navigating blind — they have no idea which button Enter will press.</p><h4>The old bad habit</h4><p>For years, designers hated that the outline also appeared when you clicked a button with a mouse — it looked like a stray box. So they wrote the infamous <code>*:focus { outline: none }</code> to \"clean up\" the design. This silently destroyed keyboard navigation for <em>everyone</em>: the page still moved focus, but now invisibly. It's one of the most damaging one-liners in CSS.</p><h4>The fix: <code>:focus-visible</code></h4><p><code>:focus-visible</code> is a CSS pseudo-class that matches only when the browser's heuristic decides the focus deserves a visible ring — essentially, when focus arrived via keyboard rather than a mouse click. So you get the best of both worlds: a crisp ring for keyboard users, no stray box for mouse users.</p><div class=\"analogy\"><p>Think of a museum spotlight that follows visitors. The old approach was either \"spotlight always on\" (ugly for casual mouse strollers) or \"spotlight ripped out\" (now the blind-cane visitors can't tell where they are). <code>:focus-visible</code> is a motion-aware spotlight: it lights up for the visitor tapping along with a cane (keyboard) who genuinely needs to see the boundary, and stays off for the visitor who walked straight up and touched the exhibit (mouse click) and already knows where they are.</p></div><h4>WCAG 2.2 raised the bar further</h4><ul><li><strong>2.4.11 Focus Not Obscured (Minimum, AA):</strong> a sticky header or cookie banner must not <em>completely cover</em> the focused element. The ring is useless if it's hidden behind a fixed bar.</li><li><strong>2.4.13 Focus Appearance (AA):</strong> sets a minimum on the indicator's <em>size and contrast</em> — a faint 1px hairline that barely contrasts isn't good enough.</li></ul><h4>The pattern to use</h4><p>Provide a strong ring for keyboard focus and suppress it only for mouse focus:</p><ul><li><code>:focus-visible { outline: 2px solid #1a73e8; outline-offset: 2px; }</code></li><li><code>:focus:not(:focus-visible) { outline: none; }</code> — this removes the outline <em>only</em> in the mouse case, leaving keyboard users protected.</li></ul><p>The trap to avoid is the blanket <code>*:focus { outline: none }</code> with no replacement — it satisfies a designer's eye and breaks accessibility for every keyboard user at once.</p><div class=\"key\">Never strip the focus outline globally; use <code>:focus-visible</code> to show a strong, high-contrast ring for keyboard users while keeping it off for mouse clicks — and in 2.2, also make sure nothing covers or shrinks that ring.</div>"},
  "tabindex": {"teach":"<p>At its core, <code>tabindex</code> is just one attribute with three behaviors that decide <strong>whether</strong> an element can receive keyboard focus and <strong>how</strong> it fits into the Tab order.</p><h4>The three values</h4><ul><li><strong><code>tabindex=\"0\"</code></strong> — \"join the natural tab order.\" Use this to make a normally non-focusable element (like a <code>&lt;div&gt;</code> you've turned into a custom widget) reachable by Tab, in its normal DOM position.</li><li><strong><code>tabindex=\"-1\"</code></strong> — \"focusable by script, but not by Tab.\" The element is skipped when tabbing, but you can move focus to it programmatically with <code>.focus()</code>. This is essential for things you want to jump focus <em>to</em> without adding them to the tab sequence.</li><li><strong>positive values (<code>1</code>, <code>2</code>, …)</strong> — \"jump the queue.\" These create a separate, higher-priority tab order. This is an <strong>anti-pattern</strong> — avoid it.</li></ul><div class=\"analogy\"><p>Picture a line of people waiting to be served. <code>tabindex=\"0\"</code> means \"stand in line where you naturally are.\" <code>tabindex=\"-1\"</code> means \"step out of the line, but the manager can still personally call you over.\" A positive <code>tabindex</code> is like handing someone a VIP pass that jumps them ahead of the whole queue — and the moment you give out <em>one</em> VIP pass, you've thrown the entire line's order into chaos, because all VIPs go first regardless of where they're standing.</p></div><h4>Why positive values are toxic</h4><p>A single positive <code>tabindex</code> doesn't just affect one element — it reorders the <em>whole page</em>. The browser tabs through all positive values first (in numeric order), <em>then</em> everything with <code>tabindex=\"0\"</code> in DOM order. So one stray <code>tabindex=\"1\"</code> can yank focus to a random spot before the rest of the page, and the tab order no longer matches what users see. It's unmaintainable: add a new field and you must renumber everything.</p><p><strong>The right way to fix tab order is to fix the DOM order</strong> — move the element in the markup (or with CSS that doesn't change source order semantics) — never with positive <code>tabindex</code>.</p><h4>Where <code>tabindex=\"-1\"</code> shines</h4><ul><li><strong>Skip-link targets</strong> and headings you move focus to after navigation — they need <code>-1</code> so <code>.focus()</code> works.</li><li><strong>Error summaries</strong> you focus after a failed form submit.</li><li><strong>Roving tabindex</strong> — the key pattern for composite widgets like toolbars, radio groups, and menus.</li></ul><h4>Roving tabindex in practice</h4><p>In a toolbar you don't want Tab to stop on all ten buttons. Instead, the <em>active</em> button gets <code>tabindex=\"0\"</code> and every other button gets <code>tabindex=\"-1\"</code>. Tab enters and exits the toolbar as a single stop; <strong>arrow keys</strong> move <em>within</em> it by shifting which button holds the <code>0</code> and calling <code>.focus()</code> on it. This matches how native widgets behave and keeps the Tab sequence short.</p><div class=\"key\">Use <code>tabindex=\"0\"</code> to add something to the natural order and <code>-1</code> to make it script-focusable only; never use positive values — fix tab order by fixing DOM order instead.</div>"},
  "live-region": {"teach":"<p>At its core, an <strong>ARIA live region</strong> is just a patch of the page that, when its text changes, gets read aloud by a screen reader <em>automatically</em> — without stealing the user's focus.</p><h4>The problem it solves</h4><p>A sighted user sees a \"3 results found\" message or a \"Saved\" toast appear and instantly knows. But a screen-reader user, whose attention is wherever their cursor is, would <em>never know</em> that text quietly appeared somewhere else — unless you mark that area as \"live.\" Live regions are how you announce updates without yanking the user away from what they're doing.</p><h4>Polite vs assertive</h4><ul><li><strong><code>aria-live=\"polite\"</code></strong> (or the shorthand <code>role=\"status\"</code>) — \"tell me when you get a chance.\" The announcement waits until the screen reader finishes whatever it's currently saying. Use this for status messages, search result counts, autosave confirmations — the vast majority of cases.</li><li><strong><code>aria-live=\"assertive\"</code></strong> (or <code>role=\"alert\"</code>) — \"interrupt me right now.\" It cuts off whatever's being read. <strong>Reserve this for genuine errors</strong> and time-critical warnings; overusing it is jarring and hostile.</li></ul><div class=\"analogy\"><p>Picture an office where you're focused on a document. A <strong>polite</strong> update is a colleague who waits until you pause typing, then quietly says \"by the way, your build finished.\" An <strong>assertive</strong> update is someone bursting in shouting \"THE BUILDING IS ON FIRE\" — appropriate for a fire, deeply annoying for \"your build finished.\" Choose assertive only when interrupting is genuinely justified.</p></div><h4>Two helper attributes</h4><ul><li><code>aria-atomic=\"true\"</code> — read the <em>whole</em> region as one unit, not just the changed word. (Both <code>role=\"status\"</code> and <code>role=\"alert\"</code> default to atomic.)</li><li><code>aria-relevant</code> — filter which kinds of mutations (additions, removals, text changes) trigger an announcement.</li></ul><h4>The gotcha that silences your messages</h4><p>Here is the subtlety that trips up nearly everyone. The screen reader must <strong>register the live region before content arrives</strong>. The browser's accessibility engine watches existing live regions for changes. If you create the region <em>and</em> inject its text in the same render tick — element and message born together — the region often isn't registered yet, so the <em>first</em> message is frequently dropped silently.</p><p>The fix: keep a <strong>persistent, empty</strong> live region sitting in the DOM from page load, and later just set its text:</p><ul><li>At load: <code>&lt;div aria-live=\"polite\" class=\"sr-only\"&gt;&lt;/div&gt;</code> (visually hidden but present).</li><li>Later: <code>region.textContent = '3 results found'</code> — now the change is reliably announced.</li></ul><p>Note <code>sr-only</code> hides it visually while keeping it in the accessibility tree — don't use <code>display:none</code>, which removes it entirely.</p><div class=\"key\">A live region announces text changes without moving focus — use <code>polite</code> for almost everything and <code>assertive</code> only for errors, and always keep the region in the DOM <em>before</em> injecting text, or the first message gets dropped.</div>"},
  "aria-describedby": {"teach":"<p>At its core, <code>aria-describedby</code> is just a wire that connects a form field to its hint or error text, so a screen reader reads them together — \"Email, edit, enter a valid email\" — instead of the user discovering the hint by accident or never at all.</p><h4>Name vs description</h4><p>Every control has a <strong>name</strong> (its main label, e.g. \"Email\") and can also have a <strong>description</strong> — supplementary text like a format hint or an error message. <code>aria-describedby</code> supplies that description by pointing at the <code>id</code>(s) of other elements. The key detail: the description is announced <em>after</em> the name, as extra context, not as a replacement for it.</p><div class=\"analogy\"><p>Think of filling out a paper form with a helpful clerk reading it to you. The <strong>label</strong> is the bold field title: \"Email.\" The <strong>description</strong> is the small grey print beneath it: \"we'll send your receipt here\" — or, after a mistake, the red note: \"that doesn't look like a valid email.\" <code>aria-describedby</code> is you drawing an arrow from the field to that small print so the clerk knows to read it aloud right after the title. Without the arrow, the clerk reads only \"Email\" and the small print goes unspoken.</p></div><h4>Pairing it with error states</h4><p>For validation errors, <code>aria-describedby</code> alone isn't enough — you also flag the field itself as broken with <code>aria-invalid=\"true\"</code>. Together they make the screen reader announce the field as <em>invalid</em> and then speak the linked error message. So a user who submits a bad email hears \"Email, invalid, enter a valid email\" and knows exactly what's wrong and where.</p><p>A complete error pattern:</p><ul><li><code>&lt;input aria-invalid=\"true\" aria-describedby=\"email-err\"&gt;</code></li><li><code>&lt;span id=\"email-err\"&gt;Enter a valid email&lt;/span&gt;</code></li></ul><h4>The bigger accessible-forms picture</h4><p><code>aria-describedby</code> is one piece of a recoverable form. Combine it with:</p><ul><li>A proper <code>&lt;label&gt;</code> associated to each input (for the <em>name</em>).</li><li><code>required</code> / <code>aria-required=\"true\"</code> so mandatory fields announce as such.</li><li><code>&lt;fieldset&gt;&lt;legend&gt;</code> to group related controls (like a set of radio buttons) under a shared heading.</li></ul><h4>The trap: placeholders are not labels</h4><p>The most common mistake is using the <code>placeholder</code> attribute as a field's label. It fails on three fronts: it <strong>disappears the moment the user types</strong> (so they forget what the field was), its grey-on-white styling <strong>usually fails contrast</strong>, and it is <strong>not a reliable accessible name</strong> across assistive tech. A placeholder is a faint hint at best — never the label. Use a real <code>&lt;label&gt;</code> for the name and <code>aria-describedby</code> for the supplementary hint.</p><div class=\"key\">Use <code>aria-describedby</code> to link a field to its hint or error (paired with <code>aria-invalid</code> for errors), built on top of a real <code>&lt;label&gt;</code> — and never substitute a disappearing <code>placeholder</code> for an actual label.</div>"},
  "skip-link": {"teach":"<p>At its core, a <strong>skip link</strong> is just a \"skip to main content\" shortcut at the very top of the page, and <strong>landmarks</strong> are HTML regions that let keyboard and screen-reader users jump straight to the part they want instead of wading through the same menus on every page.</p><h4>Why skip links exist</h4><p>Imagine every page on a site starts with a 30-link navigation bar. A mouse user just clicks past it. But a keyboard user has to press Tab 30 times — on <em>every single page</em> — just to reach the actual content. That repetition is the \"block\" that WCAG <strong>2.4.1 Bypass Blocks (A)</strong> requires you to let users skip.</p><p>The skip link is the solution: it's the <strong>first focusable element</strong> on the page, visually hidden until it receives focus, at which point it appears so a sighted keyboard user can see it. Tab once, see \"Skip to content,\" press Enter, and you're past the nav.</p><div class=\"analogy\"><p>Think of a long hotel corridor you must walk past every time to reach your room. A skip link is an express elevator right by the entrance with a button labeled \"Go straight to the rooms.\" Most guests stroll the corridor (and never notice the elevator), but the guest who's tired of the walk presses the button and arrives instantly. The elevator is normally tucked out of sight, only lighting up when someone reaches for it.</p></div><h4>The target needs <code>tabindex=\"-1\"</code></h4><p>A subtle but critical detail: the skip link's target — usually <code>&lt;main&gt;</code> — needs <code>tabindex=\"-1\"</code>. Without it, clicking the link scrolls the page but focus doesn't actually <em>land</em> there, so the screen reader keeps reading from the top. <code>tabindex=\"-1\"</code> makes the target programmatically focusable so focus moves into it and reading resumes from the content.</p><h4>Landmarks: the structural map</h4><p>Landmarks are semantic regions screen-reader users can jump between with a single keystroke:</p><ul><li><code>&lt;header&gt;</code> → <strong>banner</strong></li><li><code>&lt;nav&gt;</code> → <strong>navigation</strong></li><li><code>&lt;main&gt;</code> → <strong>main</strong></li><li><code>&lt;aside&gt;</code> → <strong>complementary</strong></li><li><code>&lt;footer&gt;</code> → <strong>contentinfo</strong></li></ul><p>Two nuances seniors should know: <code>&lt;header&gt;</code> and <code>&lt;footer&gt;</code> only map to banner/contentinfo when they're <em>page-level</em> (not nested inside an <code>&lt;article&gt;</code>). And if you have <em>multiple</em> landmarks of the same type — say two <code>&lt;nav&gt;</code>s — you must give each a distinguishing <code>aria-label</code> (\"Primary,\" \"Footer\") so users can tell them apart.</p><h4>The trap</h4><p>Hiding the skip link with <code>display:none</code> removes it from the tab order entirely — now nobody can reach it, defeating the purpose. Use an <strong>off-screen technique</strong> instead (position it far off-screen) that brings it <em>back on screen on <code>:focus</code></em>. That way it's hidden visually, present in the tab order, and appears the moment a keyboard user tabs to it.</p><p>The pattern: <code>&lt;a href=\"#main\" class=\"skip\"&gt;Skip to content&lt;/a&gt; … &lt;main id=\"main\" tabindex=\"-1\"&gt;</code>.</p><div class=\"key\">A skip link is the first focusable element that lets keyboard users bypass repeated nav (WCAG 2.4.1), and landmarks let them jump between page regions — hide the skip link off-screen (never <code>display:none</code>) and give its target <code>tabindex=\"-1\"</code> so focus truly lands.</div>"}
});
