/* Testing & Build Tooling (FE-TEST) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"FE-TEST","front":"Testing pyramid vs. testing trophy — what's the difference and which does Kent C. Dodds advocate for frontend?","back":"<p>The <strong>testing pyramid</strong> (Mike Cohn) prescribes <em>many unit tests</em>, fewer integration, fewest e2e — optimizing for speed/isolation. The <strong>testing trophy</strong> (Kent C. Dodds) reshapes this for UI: a small static base (TS, ESLint), a thin unit layer, and a <em>fat integration middle</em>, with a slim e2e cap.</p><ul><li><strong>Why the trophy bulges at integration:</strong> integration tests give the best <em>confidence-per-dollar</em> — they exercise real component interactions the way users do, while staying faster/cheaper than e2e.</li><li><strong>Pyramid's flaw for UI:</strong> heavy mocking at the unit level tests <em>implementation</em>, not behavior, and passes even when the wired-up app is broken.</li></ul><p>Guiding maxim: <em>\"The more your tests resemble the way your software is used, the more confidence they can give you.\"</em></p>"},
  {"sec":"FE-TEST","front":"Jest vs. Vitest — when and why would you pick Vitest in 2026?","back":"<ul><li><strong>Vitest</strong> reuses your <strong>Vite config + transform pipeline</strong> (esbuild/SWC plugins), so ESM/TS/JSX and aliases \"just work\" with no separate Babel/ts-jest setup. Jest-compatible API (<code>describe/it/expect</code>, <code>vi</code> ≈ <code>jest</code>).</li><li><strong>Speed:</strong> Vitest uses native ESM + a worker pool and <strong>smart watch mode</strong> (re-runs only tests affected by a change); Jest historically ran CJS through Babel (slower), though Jest now supports SWC/babel-jest and experimental ESM.</li><li><strong>Pick Jest</strong> when you're on a mature CRA/Next/webpack stack with a huge existing Jest suite and a battle-tested ecosystem.</li><li><strong>Pick Vitest</strong> for any Vite project, or to unify dev + test on one config.</li></ul><p><em>Gotcha:</em> both need <strong>jsdom/happy-dom</strong> for a DOM; neither runs a real browser by default (use <code>@vitest/browser</code> or Playwright for that).</p>"},
  {"sec":"FE-TEST","front":"React Testing Library query priority — what's the recommended order and why role-first?","back":"<p>RTL ranks queries by how closely they mirror real user/assistive-tech access:</p><ol><li><strong>Accessible to everyone:</strong> <code>getByRole</code> (best — also asserts a11y), then <code>getByLabelText</code> (forms), <code>getByPlaceholderText</code>, <code>getByText</code>, <code>getByDisplayValue</code>.</li><li><strong>Semantic:</strong> <code>getByAltText</code>, <code>getByTitle</code>.</li><li><strong>Last resort:</strong> <code>getByTestId</code>.</li></ol><p><strong>Role-first</strong> because querying by <code>role</code>+accessible name is exactly how a screen-reader user finds an element — passing tests double as a11y checks (e.g. <code>getByRole('button', {name: /save/i})</code>). Reaching for <code>data-testid</code> means the element wasn't reachable semantically — usually a real a11y smell, not just a test-style choice.</p>"},
  {"sec":"FE-TEST","front":"getBy vs. queryBy vs. findBy in RTL — when does each one throw, return null, or wait?","back":"<ul><li><strong><code>getBy*</code></strong>: <em>synchronous</em>, <strong>throws</strong> if 0 or &gt;1 match. Use for elements that must already be present.</li><li><strong><code>queryBy*</code></strong>: <em>synchronous</em>, returns <strong><code>null</code></strong> if absent (still <strong>throws</strong> on multiple matches). The <em>only</em> way to assert <strong>non-existence</strong>: <code>expect(queryByText(...)).not.toBeInTheDocument()</code>.</li><li><strong><code>findBy*</code></strong>: returns a <strong>Promise</strong>; it's literally <code>getBy</code> wrapped in <code>waitFor</code>, retrying (default <strong>1000ms</strong>, 50ms interval) until the element appears. Use for <em>async</em> UI: <code>await findByText('Loaded')</code>.</li></ul><p><strong>Gotcha:</strong> never use <code>getBy</code> for something that appears later — it throws immediately. Don't hand-roll <code>await waitFor(() =&gt; getBy...)</code> when <code>findBy</code> already does exactly that. <code>*AllBy</code> variants return arrays and don't throw on multiples (but <code>getAllBy</code>/<code>findAllBy</code> still throw on zero).</p>"},
  {"sec":"FE-TEST","front":"user-event vs. fireEvent — why does Testing Library push user-event?","back":"<p><strong><code>fireEvent</code></strong> dispatches a <em>single synthetic DOM event</em> (e.g. <code>fireEvent.click(el)</code>) — it doesn't replicate the full interaction.</p><p><strong><code>@testing-library/user-event</code></strong> simulates a <em>real user gesture</em> as a full sequence: a click first fires hover events (<code>pointerover/mouseover/pointermove/mousemove</code>), then <code>pointerdown → mousedown → focus → pointerup → mouseup → click</code>; typing fires per-key <code>keydown/keypress/input/keyup</code> and respects <code>disabled</code>, <code>maxLength</code>, selection, etc.</p><ul><li><strong>v14+ is async</strong> and requires a setup call: <code>const user = userEvent.setup(); await user.click(btn)</code>.</li><li><strong>Gotcha:</strong> <code>fireEvent.change</code> sets a value without focus/keystrokes, so it can pass when real typing would fail (e.g. masked inputs). Prefer user-event to catch those.</li></ul>"},
  {"sec":"FE-TEST","front":"\"Test behavior, not implementation\" — what does this forbid, and why avoid shallow rendering?","back":"<p>Test the <strong>observable output</strong> (rendered DOM, text, roles, what the user sees/does), <em>not</em> internals like state variables, hook call counts, private methods, or CSS class names. Implementation-coupled tests <strong>break on refactors that don't change behavior</strong> (false negatives) and pass when behavior breaks (false confidence).</p><ul><li><strong>Avoid shallow rendering</strong> (old Enzyme <code>shallow()</code>): it stubs out children, so you assert against a component tree that never runs — testing the blueprint, not the building. It can't catch broken integration between parent and child.</li><li>RTL has <em>no</em> shallow API by design; it renders the real tree into jsdom. (Enzyme itself is effectively dead — no official React 18+ adapter.)</li><li><strong>Heuristic:</strong> if a green refactor turns a test red, the test was coupled to implementation.</li></ul>"},
  {"sec":"FE-TEST","front":"Mocking network in tests — why MSW over jest.mock(fetch) or mocking axios?","back":"<p><strong>MSW (Mock Service Worker)</strong> intercepts at the <em>network layer</em> — a Service Worker in the browser, and a request interceptor in Node — so your code makes <strong>real <code>fetch</code>/XHR calls</strong> and MSW replies with stubbed responses.</p><ul><li><strong>vs. mocking the client</strong> (<code>jest.mock('axios')</code>): client mocks couple tests to <em>how</em> you fetch; swap axios→fetch and every test breaks. MSW is agnostic.</li><li><strong>Realism:</strong> requests hit real serialization, status codes, headers, and error paths — you can test loading/error/retry states honestly.</li><li><strong>Reuse:</strong> the <em>same handlers</em> serve unit tests, Storybook, and local dev.</li></ul><p><strong>Setup:</strong> <code>setupServer(...handlers)</code> in Node; <code>server.use()</code> to override per-test; <code>server.resetHandlers()</code> in <code>afterEach</code> to avoid bleed. (MSW v2 uses the <code>http</code>/<code>HttpResponse</code> API instead of v1's <code>rest</code>.)</p>"},
  {"sec":"FE-TEST","front":"Fake timers — what breaks without them and what's the modern gotcha with user-event?","back":"<p>Code using <code>setTimeout</code>, <code>setInterval</code>, <code>Date.now</code>, debounce/throttle, or polling would force real waits and flakiness. <strong><code>jest.useFakeTimers()</code> / <code>vi.useFakeTimers()</code></strong> replace timer APIs so you advance time deterministically: <code>jest.advanceTimersByTime(500)</code> or <code>runAllTimers()</code>.</p><ul><li>Always <strong>restore</strong> (<code>useRealTimers()</code> in <code>afterEach</code>) to avoid leaking into other tests.</li><li><strong>Modern gotcha:</strong> <code>user-event</code> v14 schedules its own delays on <em>real</em> timers; with fake timers you must pass <code>userEvent.setup({ advanceTimers: jest.advanceTimersByTime })</code> or interactions hang.</li><li>Wrap timer advances that cause React state updates in <code>act()</code> (RTL usually does this for you).</li></ul>"},
  {"sec":"FE-TEST","front":"Snapshot tests — what are they good for and why do teams ban large ones?","back":"<p>A snapshot serializes rendered output to a <code>.snap</code> file; later runs diff against it. <strong>Good for:</strong> catching <em>unintended</em> changes in stable serializable output (error messages, small components, config/AST output).</p><ul><li><strong>Brittle:</strong> any legit markup tweak fails the test — devs reflexively run <code>--updateSnapshot</code> (<code>-u</code>), rubber-stamping diffs they didn't read. Snapshots become <em>change detectors with no assertions</em>.</li><li><strong>Opaque:</strong> a 500-line snapshot encodes no intent; a reviewer can't tell what \"correct\" is.</li><li><strong>Better:</strong> small <strong>inline snapshots</strong> (<code>toMatchInlineSnapshot</code>) keep the expected value in the test file, and explicit assertions on specific roles/text express intent.</li></ul><p><em>Rule of thumb:</em> snapshot tiny, stable, meaningful output — never whole pages.</p>"},
  {"sec":"FE-TEST","front":"Playwright vs. Cypress for e2e in 2026 — architecture differences that matter?","back":"<ul><li><strong>Execution model:</strong> Cypress runs <em>inside the browser</em> alongside your app (great DX, time-travel debugger) but is fundamentally <strong>single-tab</strong> and single browser-context — new tabs/windows aren't supported, and cross-origin needs the <code>cy.origin()</code> escape hatch. Playwright drives browsers <em>out-of-process</em> via CDP/protocol, so multi-tab, multi-origin, and parallel <strong>browser contexts</strong> are first-class.</li><li><strong>Engines:</strong> Playwright ships Chromium, Firefox, <strong>and WebKit</strong> (Safari engine) as supported; Cypress runs Chrome-family + Firefox, with WebKit still <strong>experimental</strong> (<code>experimentalWebkitSupport</code>).</li><li><strong>Auto-wait:</strong> both auto-wait; Playwright's <strong>web-first assertions</strong> (<code>await expect(locator).toBeVisible()</code>) retry, and <strong>locators</strong> are lazy/auto-retrying.</li><li><strong>Speed/parallelism:</strong> Playwright parallelizes across workers natively and free; Cypress parallelization leans on its paid Cloud (or third-party orchestration).</li></ul>"},
  {"sec":"FE-TEST","front":"Why is 100% code coverage a bad target, and what does coverage actually measure?","back":"<p>Coverage measures which <em>lines/branches/functions were executed</em> during tests — <strong>not</strong> whether outcomes were <em>asserted</em>. You can run every line with zero <code>expect()</code> and hit 100%.</p><ul><li><strong>Diminishing returns:</strong> the last 10–15% is usually defensive/error branches and glue that cost more to test than they protect.</li><li><strong>Perverse incentives:</strong> a hard 100% gate breeds assertion-free \"coverage tests\" and snapshot rubber-stamping.</li><li><strong>Blind spots:</strong> coverage can't see missing <em>cases</em> (the input you never tried), race conditions, or wrong assertions.</li></ul><p><strong>Senior take:</strong> use coverage as a <em>discovery tool</em> for untested code and to gate <em>new</em> code (diff coverage), not as a vanity ceiling. <strong>Mutation testing</strong> (Stryker) measures assertion quality far better — it mutates code and checks whether a test fails.</p>"},
  {"sec":"FE-TEST","front":"Flaky tests — top root causes and how to eliminate them?","back":"<p>A flaky test passes/fails non-deterministically on the same code. Causes & fixes:</p><ul><li><strong>Race conditions / arbitrary waits:</strong> replace <code>sleep(500)</code> with <em>condition-based</em> waits (<code>findBy*</code>, <code>waitFor</code>, Playwright auto-wait + web-first assertions).</li><li><strong>Test interdependence / shared state:</strong> leaked globals, DB rows, or MSW handlers from a prior test — reset state in <code>afterEach</code>; ensure tests pass in random order.</li><li><strong>Time/locale/timezone:</strong> mock <code>Date</code> with fake timers; pin TZ.</li><li><strong>Network/animation nondeterminism:</strong> stub network (MSW), disable animations.</li></ul><p>Quarantine + retry-with-tracking masks but doesn't fix flakes; track flake rate and fix root cause — a tolerated flaky suite erodes trust until red builds get ignored.</p>"},
  {"sec":"FE-TEST","front":"webpack core model — loaders vs. plugins vs. splitChunks?","back":"<ul><li><strong>Loaders</strong> transform <em>individual files</em> as they're imported into modules — they're per-file, chainable transformers applied right-to-left (e.g. <code>sass-loader → css-loader → style-loader</code>, <code>babel-loader</code> for JS). Think: \"how to read this file type.\"</li><li><strong>Plugins</strong> tap into the <em>whole build lifecycle</em> via the compiler/compilation hooks — broader power (emit assets, inject env, generate HTML): <code>DefinePlugin</code>, <code>HtmlWebpackPlugin</code>, <code>MiniCssExtractPlugin</code>.</li><li><strong><code>optimization.splitChunks</code></strong> deduplicates shared modules into separate chunks (e.g. a <code>vendors</code> bundle) so common deps are cached across pages and not re-downloaded.</li></ul><p><em>Mnemonic:</em> loaders = <strong>per file</strong>, plugins = <strong>per build</strong>.</p>"},
  {"sec":"FE-TEST","front":"Vite's two-engine architecture — why esbuild in dev but Rollup for production?","back":"<p>Vite splits dev and build intentionally:</p><ul><li><strong>Dev:</strong> serves <strong>native ESM</strong> over the browser — no app bundling. The browser requests modules on demand; <strong>esbuild</strong> (Go, ~10–100× faster than JS tools) pre-bundles only <em>dependencies</em> (CJS→ESM, collapsing many internal files into fewer requests) and transpiles TS/JSX. Result: near-instant server start + true HMR regardless of app size.</li><li><strong>Build:</strong> ships <strong>Rollup</strong>, because production needs aggressive <em>tree-shaking</em>, code-splitting, chunk control, and a mature plugin ecosystem — and shipping thousands of unbundled ESM requests would be slow over the network.</li></ul><p><strong>Gotcha:</strong> esbuild and Rollup can differ subtly, so a dev-only bug or build-only bug is possible — <em>always test the production build</em>. (Vite is migrating bundling toward <strong>Rolldown</strong>, a Rust, Rollup-compatible bundler, to unify the two.)</p>"},
  {"sec":"FE-TEST","front":"Babel vs. SWC — what do transpilers do and why is SWC winning?","back":"<p>A transpiler converts modern/non-standard source (TS, JSX, ES2023+) into JS the target browsers understand, lowering newer syntax. <strong>Babel</strong> is JS-based with a vast plugin/preset ecosystem (<code>@babel/preset-env</code>, macros, custom AST transforms). <strong>SWC</strong> is a <em>Rust</em> compiler — same job, ~20× faster, with drop-in Jest/webpack/Next integration.</p><ul><li><strong>SWC wins on speed</strong> for the common path (TS/JSX strip + target lowering); Next.js defaults to SWC, and Vitest defaults to esbuild.</li><li><strong>Babel still wins</strong> when you need its plugin ecosystem (custom transforms, <code>babel-plugin-macros</code>, niche proposals) SWC doesn't implement.</li></ul><p><em>Note:</em> esbuild/SWC <strong>strip</strong> TS types but don't <em>type-check</em> — run <code>tsc --noEmit</code> separately.</p>"},
  {"sec":"FE-TEST","front":"Tree shaking — what enables it and why does sideEffects break it?","back":"<p>Tree shaking is dead-code elimination of unused exports, relying on <strong>static ESM</strong> (<code>import/export</code> are statically analyzable; CommonJS <code>require</code> is dynamic and <em>not</em> shakeable).</p><ul><li><strong>Killer #1 — side effects:</strong> if a module runs code on import (polyfills, CSS imports, global registration), the bundler must keep it even if no export is used. Declare <code>\"sideEffects\": false</code> in <code>package.json</code> (or list the side-effectful files) so the bundler can drop unused modules. Lying here <em>removes</em> needed CSS/polyfills.</li><li><strong>Killer #2:</strong> transpiling ESM to CJS before bundling (e.g. Babel <code>modules: 'commonjs'</code>) — set <code>modules: false</code>.</li><li><strong>Killer #3:</strong> re-export barrels + side effects, or importing a whole lib instead of deep paths / its ESM build.</li></ul>"},
  {"sec":"FE-TEST","front":"browserslist & polyfills — how do they interact, and what's the modern best practice?","back":"<p><strong>browserslist</strong> is a single shared config (in <code>package.json</code> or <code>.browserslistrc</code>) declaring your target browsers (e.g. <code>defaults</code>, <code>&gt; 0.5%</code>, <code>not dead</code>). Babel, autoprefixer, Lightning CSS, and others read it as the <em>source of truth</em> for how much to down-level.</p><ul><li><strong>Syntax</strong> (arrow fns, optional chaining) is handled by the <em>transpiler</em>; <strong>runtime APIs</strong> (<code>Promise</code>, <code>fetch</code>, <code>Array.flat</code>) need <strong>polyfills</strong> (<code>core-js</code>).</li><li><strong>Best practice:</strong> <code>@babel/preset-env</code> with <code>useBuiltIns: 'usage'</code> + a pinned <code>corejs</code> version injects <em>only</em> the polyfills your code touches, scoped to your targets — avoiding the bloat of importing all of core-js.</li><li><strong>Differential serving</strong> (modern + legacy bundles via module/nomodule, e.g. <code>@vitejs/plugin-legacy</code>) ships tiny modern code to capable browsers and a transpiled fallback to old ones.</li></ul>"},
  {"sec":"FE-TEST","front":"Source maps — what are the production-deployment tradeoffs (devtool options)?","back":"<p>A source map (<code>.map</code>) maps minified/transpiled output back to original source for debugging. webpack's <code>devtool</code> trades fidelity vs. build speed:</p><ul><li><strong>Dev:</strong> <code>eval-cheap-module-source-map</code> — fast rebuilds, line-accurate.</li><li><strong>Prod:</strong> <code>source-map</code> — full, separate file.</li></ul><p><strong>Security tradeoff:</strong> publicly serving full source maps exposes your original source. Best practice: <strong>generate</strong> source maps but <strong>don't deploy them publicly</strong> — upload them to your error monitor (Sentry/Datadog) so stack traces de-minify privately, and strip or restrict the map. <code>hidden-source-map</code> emits the map but omits the <code>//# sourceMappingURL</code> comment, so browsers don't auto-fetch it — exactly for this upload-and-hide workflow.</p>"}
);

Object.assign(GLOSSARY_DATA, {
  "testing-trophy": {"term":"Testing Trophy","group":"FE-TEST","eli5":"A guide that says write the most integration tests because they catch real bugs without being slow.","memory":"Trophy is fat in the MIDDLE — integration wins.","full":"<p>The <strong>testing trophy</strong> (Kent C. Dodds) is a frontend-tuned alternative to the testing pyramid. From bottom to top: a <em>static</em> base (TypeScript, ESLint), a thin <em>unit</em> layer, a deliberately <strong>large integration</strong> layer, and a small <em>e2e</em> cap. The shape reflects <em>confidence-per-cost</em>: integration tests render real component trees and exercise actual interactions, catching wiring bugs that heavily-mocked unit tests miss, while running far cheaper than full e2e.</p><p>It pairs with the maxim <em>\"the more your tests resemble the way your software is used, the more confidence they give you,\"</em> which is also why React Testing Library avoids shallow rendering and implementation details.</p>","trap":"Thinking it means 'skip unit tests.' Pure logic (reducers, utils, money math) still belongs in fast unit tests — the trophy just rebalances where the bulk goes.","worked":"A form component: instead of testing the onChange handler in isolation, render the form, type into fields with user-event, submit, and assert the success message — one integration test covers state, validation, and submission wiring.","related":["testing-pyramid","react-testing-library","user-event"]},
  "testing-pyramid": {"term":"Testing Pyramid","group":"FE-TEST","eli5":"Write tons of tiny fast tests, fewer medium ones, and very few slow end-to-end ones.","memory":"Pyramid: wide (unit) at the bottom, pointy (e2e) at the top.","full":"<p>The <strong>testing pyramid</strong> (Mike Cohn) prescribes a large base of fast, isolated <strong>unit tests</strong>, fewer <strong>integration/service</strong> tests, and a thin tip of slow, brittle <strong>e2e/UI</strong> tests. The logic: push tests down to the cheapest, fastest, most deterministic layer that can catch the bug.</p><p>It's excellent guidance for backend/business logic. For UI it's often <em>over-applied</em>: maximizing unit tests forces heavy mocking of the DOM and dependencies, which tests implementation and can stay green while the integrated app is broken — the motivation for the testing trophy's fatter integration middle.</p>","trap":"Treating it as universal law. For component-heavy frontends, an all-unit suite gives false confidence; integration is where UI bugs actually live.","worked":"Backend: 200 unit tests on pricing rules, 30 integration tests on the API + DB, 5 e2e smoke tests on checkout — classic well-shaped pyramid.","related":["testing-trophy","react-testing-library"]},
  "react-testing-library": {"term":"React Testing Library (RTL)","group":"FE-TEST","eli5":"A tool that tests your components the way a user sees and clicks them, not by peeking at internal state.","memory":"Query like a user: Role → Label → Text → (last resort) TestId.","full":"<p><strong>RTL</strong> renders components into a real DOM (jsdom) and queries them through <em>user-facing</em> handles — accessible roles, labels, and visible text — deliberately offering <em>no</em> access to component state or instance methods. This enforces \"test behavior, not implementation,\" so refactors that preserve behavior don't break tests.</p><p>Query <strong>priority</strong> (role/label/text first, <code>data-testid</code> last) doubles as an accessibility audit: if you can't reach an element by role+name, real assistive tech can't either. Variants <code>getBy</code> (throws), <code>queryBy</code> (null on absence / throws on multiple), and <code>findBy</code> (async, getBy+waitFor) cover sync vs. async UI.</p>","trap":"Reaching for getByTestId or container.querySelector by default — that bypasses the whole philosophy and hides a11y gaps. Also: using getBy for elements that render after async work (use findBy).","worked":"<code>const user = userEvent.setup(); await user.click(screen.getByRole('button', {name: /submit/i})); expect(await screen.findByRole('alert')).toHaveTextContent('Saved');</code>","related":["user-event","testing-trophy","msw"]},
  "user-event": {"term":"user-event","group":"FE-TEST","eli5":"Simulates a real person clicking and typing, firing all the events a browser would.","memory":"fireEvent = one fake event; user-event = the whole human gesture.","full":"<p><code>@testing-library/user-event</code> simulates realistic interactions by dispatching the <em>full event sequence</em> a browser produces. A click first fires hover events (pointerover/mouseover/pointermove/mousemove), then pointerdown → mousedown → focus → pointerup → mouseup → click; typing fires per-keystroke keydown/keypress/input/keyup and honors <code>disabled</code>, <code>maxLength</code>, and selection — unlike <code>fireEvent</code>, which dispatches a single synthetic event and can pass where real input would fail.</p><p>Since v14 it's <strong>async</strong> and requires <code>userEvent.setup()</code> once per test, returning a <code>user</code> instance whose methods you <code>await</code>.</p>","trap":"Forgetting it's async in v14 (missing await → assertions run before the interaction completes), or combining it with fake timers without passing { advanceTimers }, which makes interactions hang.","worked":"<code>const user = userEvent.setup(); await user.type(screen.getByLabelText('Email'), 'a@b.com'); await user.keyboard('{Enter}');</code>","related":["react-testing-library","fake-timers"]},
  "msw": {"term":"MSW (Mock Service Worker)","group":"FE-TEST","eli5":"Fakes the server by intercepting real network calls, so your code thinks it's talking to a real API.","memory":"Mock the network, not the fetch function.","full":"<p><strong>MSW</strong> intercepts requests at the <em>network boundary</em> — via a Service Worker in the browser and a request interceptor in Node — letting your code issue genuine <code>fetch</code>/XHR calls that MSW answers with declarative handlers. Because it's client-agnostic, swapping axios↔fetch doesn't touch your mocks.</p><p>The same handler set powers unit/integration tests, Storybook, and local dev, and lets you faithfully test loading, error, and retry states by returning real status codes and headers. <code>setupServer()</code> runs it in Node; <code>server.use()</code> overrides per-test; <code>server.resetHandlers()</code> clears overrides between tests. MSW v2 uses the <code>http</code>/<code>HttpResponse</code> API.</p>","trap":"Forgetting to reset handlers in afterEach, so an override leaks into later tests and causes order-dependent flakiness. Also: mocking the HTTP client instead (jest.mock('axios')) couples tests to your transport.","worked":"<code>const server = setupServer(http.get('/api/user', () => HttpResponse.json({name:'Ada'}))); // in a test: server.use(http.get('/api/user', () => new HttpResponse(null,{status:500})));</code>","related":["react-testing-library","flaky-test"]},
  "vite": {"term":"Vite","group":"FE-TEST","eli5":"A dev server that's instant because the browser loads modules directly, plus a Rollup build for production.","memory":"Vite = esbuild dev (no bundle) + Rollup build.","full":"<p><strong>Vite</strong> runs two engines. In <em>dev</em> it serves <strong>native ESM</strong> with no app bundling — the browser fetches modules on demand — while <strong>esbuild</strong> (Go) pre-bundles dependencies and transpiles TS/JSX, giving near-instant startup and true HMR independent of app size. In <em>build</em> it uses <strong>Rollup</strong> for tree-shaking, code-splitting, and a mature plugin ecosystem.</p><p>Because dev (esbuild) and prod (Rollup) differ, you must test the production build. Vite is moving bundling to <strong>Rolldown</strong> (a Rust, Rollup-compatible bundler) to unify both paths.</p>","trap":"Assuming dev and prod behave identically — esbuild doesn't type-check (only strips types) and can differ from Rollup, so a build-only bug or unchecked type error can slip through. Run tsc --noEmit and test the prod build.","worked":"<code>vite</code> (dev: instant ESM server) vs <code>vite build</code> (Rollup: minified, tree-shaken, code-split chunks).","related":["esbuild","tree-shaking","hmr"]},
  "esbuild": {"term":"esbuild","group":"FE-TEST","eli5":"A super-fast bundler/transpiler written in Go that strips types and lowers syntax in milliseconds.","memory":"Go-fast: esbuild transpiles, it doesn't type-check.","full":"<p><strong>esbuild</strong> is a Go-based bundler and transpiler that's roughly 10–100× faster than JS-based tools by exploiting native code and parallelism. It powers Vite's dev pre-bundling and dependency optimization, and is used standalone for fast TS/JSX transpilation.</p><p>Crucially, it <em>strips</em> TypeScript types rather than type-checking them and implements only a subset of advanced transforms, so teams pair it with <code>tsc --noEmit</code> for type safety and use Rollup for production bundling in Vite.</p>","trap":"Believing 'esbuild handles my TypeScript' means types are checked — it only erases them. A type error compiles cleanly; only tsc catches it.","worked":"Vite uses esbuild to convert a CommonJS dependency to ESM and transpile your <code>.tsx</code> on the fly during dev, with no separate Babel step.","related":["vite","swc"]},
  "swc": {"term":"SWC","group":"FE-TEST","eli5":"A Rust-based replacement for Babel that compiles JS/TS way faster.","memory":"SWC = Speedy Web Compiler, Rust-fast Babel.","full":"<p><strong>SWC</strong> (Speedy Web Compiler) is a Rust-based platform for transpilation and minification that does Babel's core job — TS/JSX stripping, syntax lowering to browserslist targets — at roughly an order of magnitude more speed. Next.js, Vitest, and others default to SWC (or esbuild) for transforms, and SWC ships a drop-in Jest transformer (<code>@swc/jest</code>).</p><p>Babel still wins when you depend on its rich plugin ecosystem (custom AST transforms, <code>babel-plugin-macros</code>, niche proposals) that SWC hasn't implemented. Like esbuild, SWC strips types without type-checking them.</p>","trap":"Switching from Babel to SWC blindly when your build relies on a custom Babel plugin SWC doesn't support — the transform silently no-ops and behavior changes.","worked":"Next.js compiles your app with SWC by default; a <code>.swcrc</code> sets jsc.target and parser options for TS+JSX.","related":["esbuild"]},
  "tree-shaking": {"term":"Tree Shaking","group":"FE-TEST","eli5":"The bundler deletes code you import but never actually use.","memory":"Shake the tree, dead leaves (unused exports) fall — but only ESM trees, and side effects glue leaves on.","full":"<p><strong>Tree shaking</strong> is dead-code elimination of unused exports, made possible by <strong>static ESM</strong> — <code>import/export</code> can be analyzed at build time, whereas dynamic CommonJS <code>require</code> cannot. The bundler marks reachable exports and drops the rest.</p><p>Its biggest blocker is <strong>side effects</strong>: a module that runs code on import (polyfills, CSS, global registration) must be kept even if unused, so bundlers honor <code>package.json</code> <code>\"sideEffects\"</code>. Setting it to <code>false</code> (or listing side-effectful files) unlocks aggressive shaking — but lying removes needed CSS/polyfills. Transpiling ESM→CJS before bundling also defeats it.</p>","trap":"Setting \"sideEffects\": false on a package that has import-time side effects (a global CSS import, a polyfill), so the bundler drops them and the app breaks in subtle ways.","worked":"<code>import { debounce } from 'lodash-es'</code> shakes to just debounce; <code>import { debounce } from 'lodash'</code> (CJS build) pulls the whole library because it isn't statically shakeable.","related":["vite"]},
  "hmr": {"term":"Hot Module Replacement (HMR)","group":"FE-TEST","eli5":"Updates just the changed module in the running app without a full page reload, keeping your state.","memory":"Hot-swap the module, keep the app's state alive.","full":"<p><strong>HMR</strong> swaps edited modules into a running application <em>without a full reload</em>, preserving app state (form inputs, route, store). The dev server pushes an update over a WebSocket; the HMR runtime accepts the new module and re-renders the affected subtree. Frameworks add fast-refresh layers (React Fast Refresh) that preserve component state across edits.</p><p>Vite's native-ESM dev model makes HMR essentially constant-time — it invalidates only the changed module's URL — whereas bundler-based HMR (older webpack) had to recompute more of the graph as apps grew.</p>","trap":"Confusing HMR with live-reload. Live-reload refreshes the whole page (losing state); HMR replaces only the changed module and keeps state.","worked":"Editing a button's color in a Vite + React app: the component updates instantly, the open modal and its form values stay intact — no page refresh.","related":["vite","esbuild"]},
  "flaky-test": {"term":"Flaky Test","group":"FE-TEST","eli5":"A test that randomly passes or fails on the exact same code.","memory":"Same code, different result = flake. Hunt the nondeterminism.","full":"<p>A <strong>flaky test</strong> yields non-deterministic results without code changes, usually from hidden nondeterminism: race conditions and arbitrary <code>sleep()</code> waits, shared state leaking between tests, time/timezone/locale assumptions, animations, or live network.</p><p>Fixes target the source: condition-based waiting (<code>findBy*</code>, <code>waitFor</code>, Playwright auto-wait + retrying web-first assertions), full state reset in <code>afterEach</code> plus random test ordering, fake timers and pinned TZ, and network stubbing via MSW. Retrying flaky tests hides the rot — a suite of tolerated flakes trains the team to ignore red, defeating the suite.</p>","trap":"Slapping a retry/sleep on it and calling it fixed. Retries mask flakes and let the underlying race ship to production.","worked":"A test does <code>await sleep(300)</code> then asserts a toast — flaky on slow CI. Fix: <code>expect(await screen.findByRole('status')).toBeVisible()</code>, which retries until the toast appears.","related":["msw","fake-timers","user-event"]},
  "fake-timers": {"term":"Fake Timers","group":"FE-TEST","eli5":"Lets tests fast-forward time instead of really waiting for timeouts and intervals.","memory":"Fake the clock, advance() time, then put the real clock back.","full":"<p><strong>Fake timers</strong> (<code>jest.useFakeTimers()</code> / <code>vi.useFakeTimers()</code>, both backed by @sinonjs/fake-timers) replace <code>setTimeout</code>, <code>setInterval</code>, and optionally <code>Date</code> with controllable stubs, so tests <em>advance time deterministically</em> (<code>advanceTimersByTime</code>, <code>runAllTimers</code>) instead of waiting — essential for debounce/throttle, polling, and timeouts.</p><p>Always restore real timers in <code>afterEach</code> to prevent leakage. With <code>user-event</code> v14 (which schedules on real timers), pass <code>userEvent.setup({ advanceTimers: jest.advanceTimersByTime })</code> or interactions hang. React state updates from advanced timers should be wrapped in <code>act()</code>.</p>","trap":"Enabling fake timers but forgetting to wire them into user-event, so awaited interactions never resolve and the test times out.","worked":"<code>vi.useFakeTimers(); render(<Debounced/>); fireEvent.change(input,{target:{value:'a'}}); vi.advanceTimersByTime(300); expect(onSearch).toHaveBeenCalled();</code>","related":["flaky-test","user-event"]}
});
