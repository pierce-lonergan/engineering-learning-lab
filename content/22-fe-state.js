/* State Management & Data Fetching (FE-STATE) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"FE-STATE","front":"Local vs lifted vs global state — how do you decide where state lives?","back":"<p>Push state to the <strong>narrowest scope that still works</strong>. Three tiers:</p><ul><li><strong>Local</strong> (<code>useState</code> in the component): owned by one component, nobody else reads it (an input's focus, a dropdown's open flag).</li><li><strong>Lifted</strong>: two siblings must agree, so you move state to their <em>lowest common ancestor</em> and pass it down — the canonical React \"lifting state up\".</li><li><strong>Global</strong>: truly cross-cutting (auth user, theme, feature flags) read by distant subtrees.</li></ul><p><em>Premature globalization is the most common architecture smell</em> — putting form-field state in Redux because \"we have Redux\". Co-locate first; promote only when a real second reader appears. And separate the axis of <strong>scope</strong> from the axis of <strong>kind</strong>: server-cache data is a different problem than client state regardless of how widely it's shared.</p>"},
  {"sec":"FE-STATE","front":"What is prop drilling, and what is the WRONG way to fix it?","back":"<p><strong>Prop drilling</strong> = threading a prop through many intermediate components that don't use it, just to reach a deep consumer. The pain is the <em>intermediate</em> layers, not depth itself.</p><p>The wrong fix is reaching for a <strong>global store immediately</strong>. Cheaper fixes first:</p><ul><li><strong>Component composition</strong> — pass JSX as <code>children</code> (or named-slot props) so the deep node is rendered where the data already lives, eliminating the middle layers entirely (React's own recommended cure).</li><li><strong>Context</strong> for a small, stable, app-wide value.</li></ul><p>Drilling 1–2 levels is fine and explicit. Only sustained, wide drilling justifies Context or a store.</p>"},
  {"sec":"FE-STATE","front":"Why does React Context cause re-renders, and what is context splitting?","back":"<p><strong>Every consumer of a Context re-renders whenever the Provider's <code>value</code> changes by <code>Object.is</code> identity</strong> — Context has no built-in selector mechanism, so a consumer that only reads <code>theme</code> still re-renders when <code>user</code> changes in the same value object. (Note: <code>React.memo</code> on an intermediate component does <em>not</em> stop a deeper consumer from re-rendering on context change.)</p><p>Two gotchas + fixes:</p><ul><li>Passing an inline object/array as <code>value</code> creates a new reference every render → memoize it with <code>useMemo</code>.</li><li>One fat context for everything → <strong>split into multiple contexts</strong> (ThemeContext, UserContext) or split state vs dispatch, so unrelated updates don't fan out.</li></ul><p>Context is a <em>dependency-injection</em> tool for low-frequency values, <em>not</em> a state manager for hot-path updates. For selector-style reads from one context, reach for a store (Zustand/Redux) or <code>useSyncExternalStore</code>.</p>"},
  {"sec":"FE-STATE","front":"Redux core: name the four non-negotiable rules.","back":"<ul><li><strong>Single source of truth</strong> — one immutable store tree for the whole app.</li><li><strong>State is read-only</strong> — the only way to change it is to <code>dispatch</code> a plain-object <strong>action</strong> (<code>{type, payload}</code>).</li><li><strong>Changes via pure reducers</strong> — <code>(state, action) =&gt; newState</code>, no mutation, no side effects, no async, deterministic.</li><li><strong>Immutable updates</strong> — return new references for changed slices so referential equality can power cheap change detection.</li></ul><p>These rules are what make time-travel debugging, replayable actions, and predictable testing possible. <strong>Redux Toolkit (RTK)</strong> is the official, recommended default for writing Redux today — hand-rolled action constants and switch-reducers are legacy boilerplate, not a separate library.</p>"},
  {"sec":"FE-STATE","front":"How does Redux Toolkit let you 'mutate' state, and why is that safe?","back":"<p><code>createSlice</code> reducers run inside <strong>Immer</strong>. You write apparently-mutative code:</p><p><code>state.todos.push(action.payload)</code></p><p>Immer wraps the current state in a <strong>Proxy</strong> that records your mutations on a <em>draft</em>, then produces a brand-new immutable object via <strong>structural sharing</strong> — untouched branches keep their old reference, only the changed path gets new references.</p><ul><li>You get immutable semantics without spread-operator pyramids.</li><li><em>Gotcha:</em> in one reducer either mutate the draft <strong>or</strong> <code>return</code> a new value — never do both. And never mutate state <em>outside</em> the Immer-powered producer (e.g. async callbacks).</li></ul><p>RTK also bundles <code>configureStore</code> (DevTools + <code>redux-thunk</code> + immutability/serializability dev checks on by default).</p>"},
  {"sec":"FE-STATE","front":"What problem do selectors and reselect solve?","back":"<p>A <strong>selector</strong> is a function <code>(state) =&gt; derivedValue</code> that <em>encapsulates the shape of the store</em> so components don't reach into raw paths (refactor the tree, fix one selector).</p><p>The problem: a selector that computes a <em>new</em> array/object (<code>.filter()</code>, <code>.map()</code>) returns a new reference every call → in <code>useSelector</code> (which compares with <code>===</code>/reference equality by default) that forces a re-render every dispatch.</p><p><strong>reselect's <code>createSelector</code></strong> fixes this with <strong>memoization</strong>: it caches the result and only recomputes when its <em>input</em> selectors' outputs change by identity.</p><ul><li>Keep input selectors cheap (plain field reads); do the expensive transform in the result function.</li><li><em>Trap:</em> reselect's default cache size is <strong>1</strong>, so one shared memoized selector called with different args thrashes the cache — use per-instance selector factories (or RTK's <code>createSelector</code> with a larger cache via <code>weakMapMemoize</code>).</li></ul>"},
  {"sec":"FE-STATE","front":"What is middleware in Redux, and how do thunks fit?","back":"<p><strong>Middleware</strong> sits between <code>dispatch</code> and the reducer as a chain of <code>store =&gt; next =&gt; action =&gt; …</code> functions. It's the <em>only</em> sanctioned place for side effects, because reducers must stay pure.</p><p>A <strong>thunk</strong> (via <code>redux-thunk</code>, included by <code>configureStore</code> in RTK) lets you dispatch a <em>function</em> instead of an action object:</p><p><code>const fetchUser = (id) =&gt; async (dispatch) =&gt; { dispatch(loading()); const u = await api(id); dispatch(loaded(u)); }</code></p><ul><li>Thunks receive <code>dispatch</code> + <code>getState</code>, so they orchestrate async + conditional dispatch.</li><li>For complex flows, <strong>RTK <code>createAsyncThunk</code></strong> auto-generates pending/fulfilled/rejected actions; <strong>redux-saga</strong> uses generators for cancellation/coordination-heavy logic; <strong>RTK's listener middleware</strong> is the modern lightweight alternative to saga.</li></ul>"},
  {"sec":"FE-STATE","front":"When do you NOT need Redux?","back":"<p>Redux earns its boilerplate only with <strong>genuinely global, frequently-updated, widely-shared client state</strong> that benefits from time-travel/auditing. Skip it when:</p><ul><li>Most of your \"state\" is actually <strong>server state</strong> — caching, refetching, and invalidation belong in <strong>React Query / SWR / RTK Query</strong>, not a hand-rolled reducer.</li><li>State is local or lifts cleanly to a shared parent.</li><li>A small global value (theme, auth) fits <strong>Context</strong> or a lightweight store (<strong>Zustand</strong>, <strong>Jotai</strong>).</li></ul><p><em>The 2026 default for a new app:</em> a server-cache library for server data + Context/Zustand for the little global client state, reaching for Redux Toolkit only when the client-state graph is large and cross-cutting.</p>"},
  {"sec":"FE-STATE","front":"Explain the Flux pattern and its one defining property.","back":"<p><strong>Flux</strong> (Facebook, 2014) is the architecture Redux descends from. Its defining property is <strong>strict unidirectional data flow</strong>:</p><p><code>Action → Dispatcher → Store → View → (Action)</code></p><ul><li><strong>Actions</strong> describe what happened; the <strong>Dispatcher</strong> is a single central hub that broadcasts every action to all stores; <strong>Stores</strong> hold state + logic and emit change; <strong>Views</strong> read stores and fire new actions.</li><li>It was a reaction against two-way data binding (MVC), where cascading updates made state changes impossible to trace.</li></ul><p>Redux compresses Flux: <em>one</em> store instead of many, and it <strong>removes the dispatcher entirely</strong>, replacing it with pure reducers composed into the store. The win — and the whole point — is that <strong>every change is traceable to an action</strong>.</p>"},
  {"sec":"FE-STATE","front":"Derived state: what's the rule and the classic bug it prevents?","back":"<p><strong>Don't store what you can compute.</strong> If a value is a pure function of existing state/props, compute it during render — don't mirror it into its own <code>useState</code>.</p><p>The classic bug: copying a prop into state and syncing with <code>useEffect</code>. The state goes <strong>stale</strong> the moment the source changes (the effect runs <em>after</em> render/paint), you get an extra render, and you create two sources of truth that drift.</p><ul><li><code>const fullName = first + ' ' + last;</code> — just compute it.</li><li>Need to reset state on prop change? Prefer a <code>key</code> to remount, or set state during render (React's <code>setX</code>-while-rendering pattern), not an effect.</li><li>Memoize with <code>useMemo</code> <em>only</em> if the computation is measurably expensive.</li></ul><p>React's own guidance: <em>\"you might not need an effect\"</em> — most derived-state effects are eliminable.</p>"},
  {"sec":"FE-STATE","front":"What is entity normalization and why normalize by id?","back":"<p><strong>Normalization</strong> stores entities in a flat lookup keyed by id instead of nesting/duplicating them:</p><p><code>{ users: { byId: {1:{…}, 2:{…}}, allIds: [1,2] } }</code></p><p>Why:</p><ul><li><strong>Single source of truth per entity</strong> — update user 1 in one place; nested copies can't drift.</li><li><strong>O(1) lookup and update</strong> by id instead of scanning arrays.</li><li>Avoids deep, fragile immutable updates into nested trees.</li></ul><p>RTK's <code>createEntityAdapter</code> gives you <code>entities</code> (byId) + <code>ids</code> + CRUD reducers + memoized <code>getSelectors</code> for free. <em>Trap:</em> over-normalizing tiny, never-shared view data adds ceremony for no gain — normalize relational, shared, frequently-updated data.</p>"},
  {"sec":"FE-STATE","front":"Server state vs client state — why is this the most important distinction in modern data fetching?","back":"<p>They have <em>opposite</em> properties, so they need different tools:</p><ul><li><strong>Client state</strong> — owned by your UI, synchronous, always current (form inputs, modal open, selected tab). Lives in <code>useState</code>/Context/Zustand/Redux.</li><li><strong>Server state</strong> — owned by a remote, fetched async, <em>shared by other users</em>, and <strong>goes stale the moment you receive it</strong>. It needs caching, deduping, background refetch, and invalidation.</li></ul><p>The 2010s mistake was dumping server data into Redux and manually managing loading/error/cache — thousands of lines of boilerplate. The modern answer: a <strong>server-cache library (React Query / SWR / RTK Query)</strong> treats the server as the source of truth and your store as a synchronized cache you don't hand-mutate.</p>"},
  {"sec":"FE-STATE","front":"Explain stale-while-revalidate and query keys in React Query / SWR.","back":"<p><strong>Stale-while-revalidate</strong>: serve the <em>cached</em> value instantly (no spinner), then refetch in the <em>background</em> and swap in fresh data — UI stays fast and eventually consistent. The name comes from the HTTP <code>Cache-Control</code> directive (RFC 5861). In React Query, <code>staleTime</code> controls how long data is considered fresh before a background refetch is allowed (default <code>0</code> = stale immediately).</p><p>The <strong>query key</strong> is the cache identity: <code>['todos', {status:'done'}]</code>. React Query <strong>dedupes</strong> concurrent requests with the same key into one fetch and stores results per key.</p><ul><li>Keys must be <em>serializable and complete</em> — every variable the fetch depends on belongs in the key, or you'll serve the wrong cached data.</li><li>Refetch triggers: window refocus, reconnect, key change, mount of a stale query, manual invalidation.</li></ul>"},
  {"sec":"FE-STATE","front":"Cache invalidation and optimistic updates with rollback — how does a mutation flow work?","back":"<p>A <strong>mutation</strong> is a write (POST/PUT/DELETE). After it succeeds you must reconcile the cache:</p><ul><li><strong>Invalidate</strong> (React Query v5): <code>queryClient.invalidateQueries({ queryKey: ['todos'] })</code> marks matching queries stale → they refetch. Simple and correct. (v5 takes a single options object; the old positional <code>invalidateQueries(['todos'])</code> was v4.)</li></ul><p><strong>Optimistic update</strong> for instant UX:</p><ol><li><code>onMutate</code>: <code>cancelQueries</code> to stop in-flight refetches, <em>snapshot</em> current cache (<code>getQueryData</code>), write the predicted result with <code>setQueryData</code>; return the snapshot as context.</li><li><code>onError</code>: <strong>roll back</strong> via <code>setQueryData</code> to the snapshot — this is why you snapshot.</li><li><code>onSettled</code>: invalidate to reconcile with the server's real answer.</li></ol><p><em>Gotcha:</em> skipping <code>cancelQueries</code> lets an in-flight refetch land <em>after</em> your optimistic write and clobber it. Optimistic UI is a bet — always have the rollback path.</p>"},
  {"sec":"FE-STATE","front":"When should state live in the URL / search params?","back":"<p>Put state in the URL when it should be <strong>shareable, bookmarkable, refresh-survivable, and back-button-aware</strong>: current page, active tab, filters, sort, search query, pagination.</p><p>The URL is a legitimate, <em>global, persistent</em> state store the platform gives you for free. Benefits:</p><ul><li>Deep-linkable — paste a link and land in the exact view.</li><li>Survives reload without extra persistence code.</li><li>The browser history stack is your undo/redo.</li></ul><p>Tools: <code>URLSearchParams</code>, React Router's <code>useSearchParams</code>, TanStack Router's typed search params, or framework router state. <em>Don't</em> URL-ify transient/sensitive UI state (a modal's open flag, passwords/tokens, huge blobs) — URLs are public, logged in server/proxy logs, browser history, and length-limited (~2k chars practical).</p>"},
  {"sec":"FE-STATE","front":"useReducer vs useState — when do you reach for useReducer?","back":"<p>Both are local state. Use <strong><code>useReducer</code></strong> when:</p><ul><li>The next state <strong>depends on the previous state in non-trivial ways</strong>, or multiple sub-values update <em>together</em> (a wizard, a form with validation, a cart).</li><li>Update logic is complex enough that centralizing it in a pure <code>(state, action)</code> reducer makes it testable and readable.</li><li>You want to <strong>pass <code>dispatch</code> down</strong> — React guarantees its identity is stable across renders, so deep children memoized on it don't re-render from new callback identities (unlike inline <code>setX</code>-wrapping closures).</li></ul><p><code>useState</code> wins for independent, simple values (a boolean, a string). <em>Mechanically</em> <code>useState</code> is implemented as a <code>useReducer</code> with a built-in reducer — same model, less ceremony.</p>"},
  {"sec":"FE-STATE","front":"Finite state machines (XState): what do they fix that a pile of booleans can't?","back":"<p>Boolean soup — <code>isLoading</code>, <code>isError</code>, <code>isSuccess</code>, <code>isEditing</code> — allows <strong>impossible states</strong> (loading <em>and</em> error at once) and forces defensive checks everywhere.</p><p>An <strong>FSM</strong> declares a finite set of named <strong>states</strong> and the <strong>transitions</strong> between them. You can only be in <em>one</em> state, and a transition only fires on an allowed <strong>event</strong> — impossible states become <em>unrepresentable</em>.</p><ul><li><strong>XState</strong> implements statecharts: guards, actions, <code>context</code> (extended quantitative state), hierarchical + parallel states, and a visualizable, serializable chart.</li><li>Best fit: complex, event-driven flows — checkout, multi-step auth, media players, drag interactions.</li></ul><p><em>Tradeoff:</em> overkill for a toggle; shines exactly where ad-hoc flags become unmanageable. The discriminated-union pattern (<code>{status:'loading'} | {status:'error', error}</code>) is the lightweight version of the same idea.</p>"},
  {"sec":"FE-STATE","front":"Why does immutability matter for React/Redux performance?","back":"<p>React and Redux detect change with <strong>referential equality</strong> (<code>Object.is</code> / <code>===</code>) — an O(1) pointer compare instead of a deep scan. That only works if updates create <em>new references</em> for what changed and keep old references for what didn't (<strong>structural sharing</strong>).</p><ul><li><code>React.memo</code>, <code>useMemo</code>, <code>useSelector</code>, and reducer change-detection all rely on this.</li><li>Mutating in place (<code>arr.push</code>, <code>obj.x = 1</code>) leaves the reference unchanged → React/Redux think <em>nothing changed</em> → <strong>missed re-renders / stale UI</strong>. One of the most insidious classes of state bug.</li></ul><p><em>Gotcha:</em> a shallow copy of the top level isn't enough — you must replace references all the way down the <em>changed path</em>. This is exactly the pyramid <strong>Immer</strong> automates (and what RTK's serializability/immutability dev checks catch).</p>"}
);

Object.assign(GLOSSARY_DATA, {
  "server-state": {"term":"Server State","group":"FE-STATE","eli5":"Data that really lives on a server, which your app just borrows a copy of and has to keep fresh.","memory":"Borrowed, not owned — and it spoils.","full":"<p><strong>Server state</strong> is application state whose source of truth is a remote backend: it is fetched asynchronously, potentially shared and mutated by other users, and becomes <em>stale</em> the instant you receive it. This is categorically different from <strong>client state</strong>, which your UI owns and which is always synchronously current.</p><p>Because server state needs caching, request deduplication, background revalidation, retries, and invalidation, it belongs in a dedicated server-cache library (React Query, SWR, RTK Query) rather than a hand-managed reducer. The defining shift in 2020s frontend state management was recognizing that most \"global state\" in a Redux store was actually mis-handled server state.</p>","trap":"Treating server data as if your client owns it — copying it into Redux and writing manual loading/error/cache logic, which drifts from the real backend and balloons into boilerplate.","worked":"useQuery({ queryKey:['user',id], queryFn:()=>fetch(`/api/users/${id}`).then(r=>r.json()) }) — the library owns the cache; you describe identity (key) + how to fetch.","related":["client-state","stale-while-revalidate","query-key","cache-invalidation"]},
  "client-state": {"term":"Client State","group":"FE-STATE","eli5":"State your UI fully owns and that nobody on a server cares about, like whether a menu is open.","memory":"You own it, it's instant, it's yours.","full":"<p><strong>Client state</strong> is UI-owned, synchronous state with no remote source of truth: form input values, the open/closed flag of a modal, the currently selected tab, toggles. It is always current because your code is the only writer.</p><p>It scales down to <code>useState</code> for local cases and up to Context, Zustand, Jotai, or Redux Toolkit when truly global. The key design move is to <em>not</em> mix it with server state — they have opposite freshness and ownership properties and want different tools.</p>","trap":"Reaching for a global store (Redux) to hold trivially local client state like a single input's value — premature globalization that adds indirection for nothing.","worked":"const [open, setOpen] = useState(false); — a dropdown's open flag is pure client state; it never needs caching, refetching, or a store.","related":["server-state","selector"]},
  "stale-while-revalidate": {"term":"Stale-While-Revalidate (SWR)","group":"FE-STATE","eli5":"Show the old cached data right away, then quietly fetch fresh data and swap it in.","memory":"Show stale, fetch fresh, swap silent.","full":"<p><strong>Stale-while-revalidate</strong> is a caching strategy (originating in the HTTP <code>Cache-Control</code> directive, RFC 5861) where a cached value is served immediately even if considered stale, while a background request revalidates it and replaces it if changed. The user sees no spinner on revisit; the UI is fast and eventually consistent.</p><p>In React Query and SWR, <code>staleTime</code> controls how long data is fresh (no background refetch; default 0 in React Query), and refetch is triggered on window focus, network reconnect, key change, mount of a stale query, or explicit invalidation.</p>","trap":"Confusing staleTime with gcTime (formerly cacheTime) — staleTime governs when a background refetch is allowed; gcTime governs when an unused/inactive cache entry is garbage-collected. They are independent.","worked":"useQuery({ queryKey:['todos'], queryFn, staleTime: 30_000 }) — for 30s the cached list is served with no refetch; after that, a revisit serves stale data then refetches.","related":["server-state","query-key","cache-invalidation"]},
  "query-key": {"term":"Query Key","group":"FE-STATE","eli5":"A label that uniquely names a piece of cached server data so the library knows what to store and reuse.","memory":"The cache's primary key — list every input.","full":"<p>A <strong>query key</strong> is the serializable identifier React Query uses to address an entry in its cache: <code>['todos', { status: 'done' }]</code>. Requests sharing a key are <strong>deduplicated</strong> into one in-flight fetch, and results are stored, refetched, and invalidated by key. Object members in a key are hashed deterministically (key order doesn't matter).</p><p>The rule is that the key must include <em>every</em> variable the query function depends on. An incomplete key causes cache collisions — serving data fetched for one set of params when another was requested.</p>","trap":"Leaving a dependency (a filter, a userId) out of the key, so two different requests share one cache slot and you render the wrong data.","worked":"['user', userId, { tab }] — changing userId or tab changes the key, which makes React Query treat it as a distinct cached resource and refetch.","related":["stale-while-revalidate","cache-invalidation","server-state"]},
  "optimistic-update": {"term":"Optimistic Update + Rollback","group":"FE-STATE","eli5":"Update the screen as if the save already worked, and undo it if the save actually fails.","memory":"Snapshot, predict, roll back on error.","full":"<p>An <strong>optimistic update</strong> writes the predicted result into the cache <em>before</em> the server confirms, so the UI feels instant. The mandatory companion is <strong>rollback</strong>: in <code>onMutate</code> you cancel in-flight queries and snapshot the current cache; in <code>onError</code> you restore that snapshot; in <code>onSettled</code> you invalidate to reconcile with the server's real answer.</p><p>It's a bet on success — correct UX for high-probability writes (like a button toggle), and dangerous without the rollback path.</p>","trap":"Skipping the cancelQueries step in onMutate — an in-flight background refetch can resolve after your optimistic write and overwrite it with stale data.","worked":"onMutate: async (next) => { await qc.cancelQueries({queryKey:k}); const prev = qc.getQueryData(k); qc.setQueryData(k, next); return {prev}; } / onError: (_e,_v,ctx)=>qc.setQueryData(k, ctx.prev)","related":["cache-invalidation","server-state","immutability"]},
  "pure-reducer": {"term":"Pure Reducer","group":"FE-STATE","eli5":"A function that takes the old state plus an action and returns the new state, with no surprises or side effects.","memory":"(state, action) → newState — same in, same out.","full":"<p>A <strong>reducer</strong> is a pure function <code>(state, action) =&gt; newState</code> at the heart of Redux and <code>useReducer</code>. <em>Pure</em> means: deterministic (same inputs → same output), no mutation of arguments, and no side effects (no fetches, no <code>Date.now()</code>, no dispatch).</p><p>Purity is what enables Redux's superpowers — replayable action logs, time-travel debugging, trivial unit testing, and safe React concurrent rendering. Side effects are pushed out to middleware/thunks; immutable returns let referential-equality change detection work. (Inside RTK's <code>createSlice</code>, Immer makes the mutating-looking body produce a pure immutable result.)</p>","trap":"Mutating the state argument (state.items.push(x)) instead of returning a new object — outside Immer this silently breaks change detection and time-travel.","worked":"function reducer(state, {type, payload}) { switch(type){ case 'add': return {...state, items:[...state.items, payload]}; default: return state; } }","related":["redux-toolkit","immutability"]},
  "redux-toolkit": {"term":"Redux Toolkit (RTK)","group":"FE-STATE","eli5":"The official, batteries-included way to write Redux with far less boilerplate.","memory":"Redux done right by default — createSlice + Immer.","full":"<p><strong>Redux Toolkit</strong> is the official, recommended Redux API. <code>createSlice</code> generates actions + a reducer together and runs reducer bodies inside <strong>Immer</strong>, so you write mutating-looking code that produces immutable updates. <code>configureStore</code> wires DevTools and thunk middleware automatically (plus dev-only immutability/serializability checks); <code>createAsyncThunk</code> handles async; <code>createEntityAdapter</code> gives normalized CRUD; <strong>RTK Query</strong> adds a full data-fetching/caching layer comparable to React Query.</p><p>As of 2026, hand-written Redux (manual action constants, switch reducers, spread pyramids) is considered legacy — new Redux code should be RTK.</p>","trap":"Thinking RTK is a different library you can skip 'because we use plain Redux' — RTK is the recommended way to use Redux itself, not an alternative to it.","worked":"const slice = createSlice({ name:'todos', initialState:[], reducers:{ add:(s,a)=>{ s.push(a.payload); } } }); — the push is Immer-safe.","related":["pure-reducer","immutability","normalization","selector"]}
});
