/* Streaming & Incremental Tables (LAKE-STREAM) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"LAKE-STREAM","front":"Incremental read vs. full table scan on a lakehouse table","back":"An <strong>incremental read</strong> returns only the rows added (and, depending on format, changed/deleted) between two table snapshots/versions, instead of re-scanning the whole table. The reader passes the last snapshot/version it processed and receives the delta. It is the lakehouse analog of CDC, but driven by the table format's own commit log rather than an external pipeline like Debezium."},
  {"sec":"LAKE-STREAM","front":"Iceberg incremental append scan: how it works and its key limitation","back":"Iceberg's incremental scan reads files added between a <code>start-snapshot-id</code> (exclusive) and <code>end-snapshot-id</code> (inclusive) using snapshot lineage in metadata. <strong>It is append-only</strong>: it surfaces new data files but does not emit deletes/updates, and <strong>throws if the range contains an overwrite or delete</strong> commit (a compaction counts as an overwrite). Use it for append-only streams (logs, events); use a change feed for mutated tables."},
  {"sec":"LAKE-STREAM","front":"Delta Change Data Feed (CDF): what it emits and how to enable it","back":"Enabled via <code>delta.enableChangeDataFeed = true</code> (per-table, in CREATE or ALTER). Once on, Delta records row-level changes so a reader can request changes between versions/timestamps and receive a <code>_change_type</code> column with values <code>insert</code>, <code>delete</code>, <code>update_preimage</code>, and <code>update_postimage</code>, plus <code>_commit_version</code> and <code>_commit_timestamp</code>. CDF only captures changes <em>after</em> it is enabled — prior history has no change data."},
  {"sec":"LAKE-STREAM","front":"Hudi incremental query and the difference between its two query types","back":"Hudi's <strong>incremental query</strong> returns records written after a begin instant (<code>hoodie.datasource.query.type=incremental</code> + <code>hoodie.datasource.read.begin.instanttime</code>). Default incremental returns the <em>latest image</em> of changed rows; <strong>CDC incremental</strong> adds <code>hoodie.datasource.query.incremental.format=cdc</code> to emit full before/after change records — and the table must have been written with <code>hoodie.table.cdc.enabled=true</code> so CDC log files exist. Hudi's record-key + commit-timeline model makes both efficient."},
  {"sec":"LAKE-STREAM","front":"Merge-on-Read (MOR) vs Copy-on-Write (COW) for upserts","back":"<ul><li><strong>COW</strong>: an upsert/delete rewrites the entire affected data file. Fast reads, expensive writes — good for read-heavy, low-mutation tables.</li><li><strong>MOR</strong>: writes lightweight delete files / log files and merges them with base files <em>at read time</em>; a background compaction periodically folds them into new base files. Cheap, low-latency writes; reads pay a merge cost until compaction. Good for streaming upserts.</li></ul>"},
  {"sec":"LAKE-STREAM","front":"How does Merge-on-Read achieve low write latency, and what's the read-side cost?","back":"MOR avoids rewriting base data on every change by appending <strong>delete files</strong> (Iceberg) or <strong>log/delta files</strong> (Hudi) that the reader merges on the fly. Writes are O(changes), not O(file size), enabling minute/second-level upsert latency. The cost is read amplification: queries must reconcile base + delta files, so latency and small-file count grow until <strong>compaction</strong> rewrites them into fresh base files."},
  {"sec":"LAKE-STREAM","front":"Iceberg position deletes vs equality deletes (MOR delete files)","back":"<ul><li><strong>Position deletes</strong> reference exact <code>(file_path, row_position)</code> tuples to suppress. Cheap to apply at read but require knowing the row's location, so they suit deletes within recently-written files.</li><li><strong>Equality deletes</strong> store predicate values (e.g. <code>id = 42</code>) and suppress matching rows. They let a streaming writer delete without first locating the row, but cost more at read since every candidate base row must be checked against the delete set. Iceberg v3 supersedes both with per-file <strong>deletion vectors</strong>.</li></ul>"},
  {"sec":"LAKE-STREAM","front":"What does \"exactly-once into the lake\" actually require?","back":"It requires <strong>idempotent commits</strong>: each micro-batch's commit is keyed by a stable identifier (typically the stream's checkpoint/offset or batch id) so that a retried/duplicated commit is recognized and skipped rather than re-applied. Combined with the engine's checkpointed source offsets and the table format's atomic commit, a replay after failure produces the same table state — no duplicates, no gaps."},
  {"sec":"LAKE-STREAM","front":"How Spark Structured Streaming gets exactly-once when writing Delta/Iceberg","back":"The streaming engine checkpoints source offsets and a monotonic <code>batchId</code>. The Delta sink records a <code>SetTransaction</code> action keyed by <code>txnAppId</code> + <code>txnVersion</code> (= batchId) in the commit log and <strong>atomically skips a commit whose (appId, version) was already applied</strong>; Iceberg sinks track the last batch in snapshot metadata. On replay, Spark re-runs the batch but the sink dedupes the commit — end-to-end exactly-once <em>into</em> the lake."},
  {"sec":"LAKE-STREAM","front":"Why is the lakehouse commit being atomic essential for streaming correctness?","back":"A streaming write may add many files; readers and the exactly-once dedup logic must never see a half-written batch. The format commits by atomically swapping a single pointer (Iceberg: new metadata file via catalog compare-and-swap; Delta: next <code>_delta_log</code> JSON via atomic put-if-absent). Either all files in the batch become visible or none do, which is what lets idempotent batchId-keyed commits work and gives readers a consistent snapshot."},
  {"sec":"LAKE-STREAM","front":"Streaming table / materialized view in a lakehouse (e.g. Databricks DLT, Snowflake)","back":"A <strong>streaming table</strong> incrementally ingests/append-processes new source rows (append semantics, each row processed once). A <strong>materialized view</strong> stores a query's precomputed result and is incrementally refreshed when bases change, serving reads from the materialization. Streaming tables suit append ingest and low-latency transforms; MVs suit expensive aggregations/joins that are queried far more than they change."},
  {"sec":"LAKE-STREAM","front":"Batch-and-streaming unification: what does \"one table, both read modes\" mean?","back":"Because a lakehouse table format keeps an ordered commit log, the <em>same physical table</em> can be read as a bounded batch (current snapshot) <strong>or</strong> as an unbounded stream (poll new snapshots/commits as they land). Producers don't maintain separate serving and streaming copies; one Iceberg/Delta table is simultaneously a batch source, a batch sink, a streaming source, and a streaming sink — collapsing the Lambda architecture's dual paths."},
  {"sec":"LAKE-STREAM","front":"Reading a Delta/Iceberg table as a streaming source: what is the unit of progress?","back":"The stream advances by <strong>table commit/snapshot</strong>, not by file. Spark's Delta source tracks the last processed <code>(version, index)</code>; Iceberg's streaming source tracks the last consumed snapshot id. Each new commit becomes one or more micro-batches. Options like <code>maxFilesPerTrigger</code> (Delta default 1000) / <code>maxBytesPerTrigger</code> bound batch size so a large backfill commit doesn't create one giant batch."},
  {"sec":"LAKE-STREAM","front":"Why incremental reads can break: non-append commits and expired snapshots","back":"Two common failures: (1) a commit in the requested range is an <strong>overwrite/compaction/delete</strong>, which append-only incremental scans reject (Delta streaming offers <code>skipChangeCommits</code>, deprecating <code>ignoreChanges</code>; Iceberg streaming offers <code>streaming-skip-overwrite-snapshots</code>/<code>streaming-skip-delete-snapshots</code>). (2) The <strong>start snapshot was expired</strong> by retention/vacuum, so the lineage needed to compute the delta is gone and the reader must fall back to a full read. Compaction is the usual culprit for both."},
  {"sec":"LAKE-STREAM","front":"What ongoing maintenance does a streaming MOR/upsert table demand?","back":"Streaming writes create many small data files and accumulate delete/log files, so you must run: <strong>compaction</strong> (rewrite small base + delete files into fewer large ones to cap read amplification), <strong>snapshot/version expiration</strong> (drop old snapshots so metadata and storage don't grow unbounded), and <strong>orphan-file / vacuum cleanup</strong>. Tuning compaction cadence trades read latency against write/compute cost — a core operability decision for streaming lakehouse tables."}
);

Object.assign(GLOSSARY_DATA, {
  "incremental-read": {"term":"Incremental Read","group":"LAKE-STREAM","aliases":["incremental scan","incremental query","incremental processing"],"eli5":"Ask the table \"what changed since I last looked?\" instead of re-reading everything.","memory":"Diff the manifests/log, not the data.","full":"<p>An incremental read uses the table format's own commit log to return only the rows added (and, format permitting, updated/deleted) between two snapshots/versions. The consumer persists the last snapshot id it processed and requests the delta from there, processing each commit once. This is the lakehouse-native equivalent of CDC: there is no separate Debezium/Kafka pipeline — the table <em>is</em> the change log.</p><p>The formats differ in expressiveness. Iceberg's incremental append scan is append-only and errors on overwrites/deletes in the range. Delta Change Data Feed and Hudi CDC incremental emit full row-level before/after images including updates and deletes. Picking between them is a real design decision: append-only is cheaper but cannot propagate mutations downstream.</p>","trap":"Interviewers probe whether your incremental read handles UPDATEs and DELETEs or only appends. Iceberg's plain incremental scan is append-only and throws on a range containing a delete/overwrite — you need a change feed (Delta CDF, Hudi CDC) for full mutation propagation.","worked":"Stream a silver table by reading Delta CDF from <code>_commit_version</code> 1042 to 1058: you get 12k <code>insert</code>, 3k <code>update_postimage</code> (with matching <code>update_preimage</code>), and 200 <code>delete</code> rows — apply them as upserts/deletes to gold.","related":["change-data-feed","iceberg-incremental-scan","hudi-incremental-query","batch-streaming-unification"]},
  "iceberg-incremental-scan": {"term":"Iceberg Incremental Scan","group":"LAKE-STREAM","aliases":["incremental append scan","appendsBetween","iceberg incremental read"],"eli5":"Iceberg hands you only the data files added between two snapshots — as long as nothing was deleted in between.","memory":"\"appendsBetween(A, B)\" — the name says it: appends only.","full":"<p>Iceberg models a table as a linear (or branched) history of <strong>snapshots</strong>, each pointing to manifest lists of data files. An incremental append scan computes the set of data files added between <code>start-snapshot-id</code> (exclusive) and <code>end-snapshot-id</code> (inclusive) by diffing snapshot manifests, then reads only those files. Because it works at file granularity off metadata, planning is cheap and no full scan is needed.</p><p>The hard constraint: the standard incremental-append scan is <strong>append-only</strong>. If the snapshot range contains an overwrite, delete, or replace (e.g. a compaction rewrite or a row-level delete), the scan raises an error rather than silently dropping or double-counting rows. For mutated tables you need a changelog scan or you must design the producer to be append-only.</p>","trap":"A scheduled compaction job is an overwrite commit. It will land in your incremental range and break an append-only incremental reader even though no logical data changed — candidates forget compaction counts as a non-append commit.","worked":"<code>spark.read.format(\"iceberg\").option(\"start-snapshot-id\", 8120).option(\"end-snapshot-id\", 8137).load(\"db.events\")</code> returns just the files added across those 17 snapshots.","related":["incremental-read","snapshot-isolation-commit","merge-on-read","compaction"]},
  "change-data-feed": {"term":"Delta Change Data Feed (CDF)","group":"LAKE-STREAM","aliases":["CDF","delta change feed","enableChangeDataFeed"],"eli5":"Delta tags every changed row with whether it was inserted, deleted, or the before/after of an update.","memory":"CDF = the four faces of a change: insert, delete, pre, post.","full":"<p>Change Data Feed makes a Delta table emit row-level change records between two versions or timestamps. Enable it with <code>delta.enableChangeDataFeed = true</code> (in CREATE or ALTER); thereafter Delta persists change metadata (and, for operations that don't already rewrite rows, dedicated <code>_change_data</code> files). A CDF read adds <code>_change_type</code> (<code>insert</code>, <code>delete</code>, <code>update_preimage</code>, <code>update_postimage</code>), <code>_commit_version</code>, and <code>_commit_timestamp</code>.</p><p>CDF is the backbone of incremental medallion pipelines (bronze→silver→gold) and incrementally-refreshed materialized views: a downstream job consumes only changed rows and applies them as a <code>MERGE</code>. Crucially CDF is <strong>not retroactive</strong> — it only captures changes committed after it was enabled, so enable it before you need history.</p>","trap":"CDF only records changes from the version where it was enabled onward. Asking for CDF over a range before it was turned on errors / returns nothing — you cannot retroactively reconstruct the feed from existing data files.","worked":"<code>spark.read.format(\"delta\").option(\"readChangeFeed\", \"true\").option(\"startingVersion\", 1042).table(\"silver.orders\")</code> yields rows carrying <code>_change_type</code> and <code>_commit_version</code>.","related":["incremental-read","hudi-incremental-query","materialized-view-streaming-table","exactly-once-idempotent-commit"]},
  "hudi-incremental-query": {"term":"Hudi Incremental Query","group":"LAKE-STREAM","aliases":["hudi incremental","hudi cdc query","incremental.format=cdc","hoodie incremental"],"eli5":"Read only the records Hudi wrote after a given commit instant — optionally with full before/after images.","memory":"Plain incremental = latest image; add <code>incremental.format=cdc</code> (table must be cdc.enabled) = before/after.","full":"<p>Hudi tracks every write as an instant on its <strong>commit timeline</strong> and keys records by a user-defined record key. An incremental query (<code>hoodie.datasource.query.type=incremental</code> with <code>hoodie.datasource.read.begin.instanttime</code>) returns records written after that instant. The default mode returns the <em>latest image</em> of each changed record (it cannot tell you what a row looked like before, nor surface hard deletes as change rows).</p><p>For full change semantics, Hudi's <strong>CDC incremental</strong> mode adds <code>hoodie.datasource.query.incremental.format=cdc</code>, which emits before/after change records (insert/update/delete). This only works if the table was written with the table property <code>hoodie.table.cdc.enabled=true</code>, so that CDC log files were produced alongside base/log files. The begin range is left-open, right-closed: <code>(begin, end]</code>.</p>","trap":"Candidates conflate the table-write flag with the query flag. <code>hoodie.table.cdc.enabled=true</code> is a <em>write-time table property</em> that produces CDC logs; you still must request <code>hoodie.datasource.query.incremental.format=cdc</code> at read time. Enabling neither gives only latest-image incremental, which silently drops before-images and hard deletes.","worked":"Read CDC from a Hudi table: <code>option(\"hoodie.datasource.query.type\",\"incremental\").option(\"hoodie.datasource.query.incremental.format\",\"cdc\").option(\"hoodie.datasource.read.begin.instanttime\",\"20260101120000\")</code> — returns before/after rows for everything committed after that instant.","related":["incremental-read","change-data-feed","merge-on-read","compaction"]},
  "iceberg-deletes": {"term":"Iceberg Delete Files (position vs equality)","group":"LAKE-STREAM","aliases":["position delete","equality delete","delete files","row-level delete","deletion vector"],"eli5":"Two ways to say \"ignore this row\": point at exactly where it lives, or describe it by its key.","memory":"Position = \"where it is\"; equality = \"what it is.\" Deletes only reach data older than themselves (lower sequence number).","full":"<p>In Iceberg's MOR mode, row-level deletes are encoded as separate delete files rather than by rewriting data. <strong>Position deletes</strong> store <code>(file_path, row_position)</code> tuples — they precisely target a row's physical location, are cheap to apply at read, but require the writer to know where the row sits (good for deleting recently-written rows). <strong>Equality deletes</strong> store predicate values (e.g. <code>id = 42</code>) and suppress base rows matching them — a streaming writer can issue them without locating the row first, at the cost of checking candidate base rows at read.</p><p>Deletes apply by <strong>sequence number</strong>: an equality delete applies only to data files whose data sequence number is <em>strictly less than</em> the delete's, so it never affects data written after it. Equality deletes make streaming upserts cheap (delete-by-key + insert) but are the expensive read side and the prime reason compaction matters. Iceberg v3 supersedes position deletes with <strong>deletion vectors</strong> (one compact deletes-bitmap per data file) to cut this overhead.</p>","trap":"Equality deletes apply ONLY to data with a sequence number lower than the delete — i.e. data written <em>before</em> the delete, never rows written later. (A new insert of the same key after the delete survives.) Candidates often state this backwards, and also assume deletes are as cheap as inserts; equality deletes are the main MOR read tax.","worked":"A streaming MERGE upsert keyed on <code>order_id</code> writes an equality-delete (<code>order_id = X</code>) plus a new insert at a higher sequence number; at read, Iceberg suppresses the older version (lower sequence) and surfaces the newer insert (higher sequence, unaffected by the delete).","related":["merge-on-read","compaction","iceberg-incremental-scan"]},
  "exactly-once-idempotent-commit": {"term":"Idempotent Commit (Exactly-Once into the Lake)","group":"LAKE-STREAM","aliases":["idempotent write","exactly-once sink","batchId dedup","transactional id","SetTransaction"],"eli5":"Stamp each micro-batch's commit with its batch id so a retry that re-sends the same batch is recognized and skipped.","memory":"Same (appId, batchId) in → same commit out; replays are no-ops.","full":"<p>Exactly-once <em>into</em> the lake is achieved by making the sink commit idempotent: each micro-batch carries a stable key — the stream's checkpointed <code>batchId</code>/offset — and the table format atomically refuses to apply a commit whose key was already committed. In Delta this uses a <code>SetTransaction</code> action storing <code>txnAppId</code> + <code>txnVersion</code> in the commit log; Iceberg tracks the last batch in snapshot metadata. On failure the engine replays the batch, but the dedup logic ensures the table state is identical — no duplicates, no gaps.</p><p>This relies on the format's <strong>atomic, all-or-nothing commit</strong>: a partially-written batch must never be visible, or the dedup key check would be unreliable. The combination — checkpointed source offsets + monotonic batchId + atomic format commit keyed by that id — is the whole exactly-once story for writes; end-to-end exactly-once additionally needs the source to be replayable.</p>","trap":"\"Exactly-once\" is exactly-once <em>effect</em> on the table via idempotent commits, not exactly-once <em>execution</em>. The batch may run twice; correctness comes from the dedup key, not from the job never retrying. Also note: reusing the same <code>txnAppId</code> with a fresh checkpoint (batchId restarts at 0) makes Delta silently skip new writes as duplicates.","worked":"Spark crashes after writing files but before recording the commit; on restart it re-runs <code>batchId=778</code>, but the sink sees <code>SetTransaction(appId, 778)</code> already committed and skips it — no double-insert.","related":["snapshot-isolation-commit","incremental-read","change-data-feed"]},
  "snapshot-isolation-commit": {"term":"Atomic Snapshot Commit","group":"LAKE-STREAM","aliases":["atomic commit","snapshot isolation","optimistic concurrency","metadata swap"],"eli5":"A write becomes visible by atomically swapping one pointer — readers see all of a batch or none of it.","memory":"Commit = swap one pointer; readers pin a snapshot.","full":"<p>Lakehouse formats give ACID by committing through an atomic pointer swap rather than mutating data files in place. Iceberg appends a new immutable metadata file and atomically updates the catalog's table pointer via a compare-and-swap (optimistic concurrency: retry if someone else advanced it). Delta writes the next numbered JSON to <code>_delta_log</code> using an atomic put-if-absent on the filename. Either way the entire set of files in a commit becomes visible at once, and any reader pins a consistent <strong>snapshot</strong>.</p><p>This atomicity is the foundation for everything streaming: micro-batch writers commit concurrently with optimistic retries, idempotent <code>batchId</code> dedup is reliable because there's no half-committed state, and a streaming source can advance commit-by-commit knowing each is a consistent unit. It's also why incremental reads can diff well-defined snapshot boundaries.</p>","trap":"Optimistic concurrency means conflicting concurrent writers <em>retry</em>, not block. Many parallel streaming writers into one table cause commit contention/retries and throughput collapse — partition the write space or serialize commits; don't assume infinite concurrent writers.","worked":"Two Spark jobs commit to one Iceberg table simultaneously; both attempt the catalog CAS, the loser detects its base snapshot moved, re-plans against the new snapshot, and retries.","related":["exactly-once-idempotent-commit","iceberg-incremental-scan","batch-streaming-unification"]},
  "batch-streaming-unification": {"term":"Batch/Streaming Unification","group":"LAKE-STREAM","aliases":["one table both modes","unified batch and streaming","kappa on lakehouse","table as stream"],"eli5":"The same lakehouse table is both a static dataset and a live stream — you choose how to read it.","memory":"One table, two lenses: snapshot (batch) or commit-feed (stream).","full":"<p>Because a lakehouse table maintains an ordered, append-only log of commits/snapshots, one physical table serves as a batch source, batch sink, streaming source, <em>and</em> streaming sink. A batch read returns the current snapshot; a streaming read polls for new commits and emits them as micro-batches, advancing a single stored offset (Delta <code>(version, index)</code>; Iceberg last snapshot id). No separate serving copy and streaming copy are maintained.</p><p>This collapses the classic <strong>Lambda architecture</strong> (parallel batch and speed layers that must be reconciled) into a single path: one table, one set of transformation code, reprocessable by replaying snapshots. It's the lakehouse realization of Kappa-style unification, and it's why streaming tables and incrementally-refreshed materialized views can sit directly on the same tables analysts query as batch.</p>","trap":"Unification is about the <em>storage</em> serving both modes — it does not give a streaming read access to UPDATEs/DELETEs for free. A vanilla streaming source over a mutated table needs <code>skipChangeCommits</code> (Delta) or a change feed; otherwise non-append commits break it.","worked":"Bronze Iceberg table is written by a Flink stream and simultaneously queried as a batch source by a nightly Spark job and as a streaming source by a silver micro-batch job — one table, three consumers.","related":["incremental-read","snapshot-isolation-commit","materialized-view-streaming-table","iceberg-incremental-scan"]},
  "materialized-view-streaming-table": {"term":"Streaming Table vs Materialized View","group":"LAKE-STREAM","aliases":["streaming table","materialized view","MV","incremental refresh","live table"],"eli5":"A streaming table incrementally appends new source rows; a materialized view incrementally re-derives a query's stored result.","memory":"Streaming table = append the inputs; MV = refresh the output.","full":"<p>A <strong>streaming table</strong> ingests/transforms new source rows with append semantics — each input row is processed once and progress is tracked by source offset (Databricks DLT streaming tables, Snowflake Dynamic Tables in append mode, Flink-written Iceberg). It's the right tool for low-latency ingest and row-wise transforms. A <strong>materialized view</strong> stores the precomputed result of a (possibly aggregating/joining) query and refreshes it when base tables change; reads hit the materialization instead of recomputing.</p><p>The engine's win is <strong>incremental refresh</strong>: rather than recompute the whole result, it reads only changed base rows (via a change feed / incremental scan) and applies the delta to the materialization. Choose a streaming table for append-heavy pipelines, an MV for expensive results queried far more often than their inputs change. Both are powered by the same incremental-read machinery.</p>","trap":"A materialized view is only as fresh as its last refresh, and incremental refresh falls back to a <em>full</em> recompute when the query uses constructs it can't incrementalize (e.g. Snowflake Dynamic Tables: UNION/INTERSECT/EXCEPT, outer joins with non-equality predicates, certain non-deterministic functions). Know your engine's incremental-eligibility rules or you'll budget for cheap refreshes and get full rebuilds.","worked":"A Snowflake Dynamic Table / Databricks MV defining a daily-revenue rollup refreshes by consuming only the order rows changed since last refresh (via change tracking) and updating the stored aggregate, not by re-scanning all orders.","related":["incremental-read","change-data-feed","batch-streaming-unification","compaction"]}
});
