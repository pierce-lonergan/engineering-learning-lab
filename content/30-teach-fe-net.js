/* Teach me from the ground up: FE-NET (auto-assembled) */
enh({
  "idempotent-method": {"teach":"<p>At its core, an idempotent method is one you can repeat as many times as you like and the world ends up in exactly the same state as if you'd done it once. Pressing a calculator's <code>=</code> key changes nothing if you mash it five times; that's the flavour we're after.</p><h4>What 'same server-side effect' really means</h4><p>The formal line is: <strong>N identical requests produce the same server-side <em>effect</em> as a single one.</strong> Let's dissect that. The key word is <em>effect</em> — what changed on the server, the durable state. It is emphatically <strong>not</strong> about the response you get back.</p><div class=\"analogy\"><p>Think of a light switch labelled \"ON\" and \"OFF\" (not a toggle). Flipping it to OFF once turns the light off. Flipping it to OFF again? Still off. Ten more times? Still off. The room's state is identical no matter how many times you act. A toggle switch, by contrast, is <em>not</em> idempotent — each flip flips the outcome.</p></div><h4>Which methods qualify</h4><p><code>GET</code>, <code>HEAD</code>, <code>PUT</code>, <code>DELETE</code>, and <code>OPTIONS</code> are idempotent. <code>PUT /user/5 {name:'Ada'}</code> sets the record to that value — run it twice and the record is still exactly that. <code>POST</code> is the odd one out: <code>POST /charges</code> twice usually creates <em>two</em> charges, so it is not idempotent. <code>PATCH</code> generally isn't either (a patch like \"increment balance by 10\" stacks up).</p><h4>Why it matters: safe retries</h4><p>Networks lose packets. When a client or proxy sends a request and the reply never arrives, it cannot tell whether the server processed it or not. If the method is idempotent, the answer is easy: <strong>just resend it</strong> — worst case, you redo a no-op. This is the bedrock of automatic retry logic in load balancers, HTTP clients, and service meshes.</p><p>For the non-idempotent ones, you bolt safety on yourself with an <strong>idempotency key</strong>: a unique token the server remembers, so a replay returns the original result instead of acting twice.</p><h4>The classic trap</h4><p>People assume idempotent means \"returns the same response.\" It doesn't. Call <code>DELETE /user/5</code> and you get <code>200 OK</code>; call it again and you get <code>404 Not Found</code> — different responses, but the server state (user 5 is gone) is identical both times. That is precisely the definition being satisfied.</p><div class=\"key\">Idempotency is about server <strong>state</strong>, not the response body. That property is exactly what makes blind retries on timeout safe — and what an idempotency key gives you for the methods that lack it.</div>"},
  "safe-method": {"teach":"<p>At its core, a safe HTTP method is a request that only <em>reads</em> — it looks but never touches. After a safe request, the server is in the same state it was before, by design.</p><h4>Which methods are safe</h4><p><code>GET</code>, <code>HEAD</code>, <code>OPTIONS</code>, and <code>TRACE</code> are defined as <strong>safe</strong>: they have no intended state-changing effect. The whole web's plumbing leans on this promise. Browsers prefetch links, search crawlers spider pages, and antivirus and email scanners follow URLs — all of them fire <code>GET</code> requests <em>speculatively</em>, confident that reading a page can't break anything.</p><div class=\"analogy\"><p>A safe method is like reading a sign on a door versus pushing the door open. A whole crowd can read the sign — \"Sale, 50% off\" — and the shop is unchanged. But if reading the sign secretly also rang up a purchase, you'd have chaos every time someone glanced at it. Safe methods promise: reading the sign rings up nothing.</p></div><h4>Safe vs idempotent</h4><p>These overlap but aren't the same. <strong>All safe methods are idempotent</strong> (reading nothing N times changes nothing). But <strong>not all idempotent methods are safe</strong>: <code>PUT</code> and <code>DELETE</code> definitely change state, yet repeating them lands you in the same place. So safe is the stricter promise: not just \"repeatable without extra harm\" but \"no harm at all.\"</p><h4>Why it bites: never mutate on GET</h4><p>The cardinal sin is hiding a mutation behind a safe method — an endpoint like <code>GET /deleteUser?id=5</code> or <code>GET /logout</code>. Because the whole ecosystem treats <code>GET</code> as harmless, something will eventually fire it without a human clicking: a browser's link prefetcher warming up a page, an antivirus scanning a link in an email, a chat app generating a preview thumbnail. Suddenly users get logged out or records vanish, and the bug is maddening to reproduce because no one consciously did it.</p><h4>The trap</h4><p>\"Safe\" sounds like \"secure,\" but it has <em>nothing</em> to do with authentication or authorization. A <code>GET</code> can require a password and still be safe (side-effect-free); an unauthenticated <code>POST</code> can be wide open and unsafe. Safe only means \"doesn't change server state.\"</p><div class=\"key\">Safe = read-only by contract. The entire web prefetches and scans <code>GET</code> URLs on your behalf, so any state change behind a safe method <em>will</em> eventually fire by accident — put mutations behind <code>POST</code>/<code>PUT</code>/<code>DELETE</code>.</div>"},
  "cors-preflight": {"teach":"<p>At its core, a CORS preflight is the browser pausing before a risky cross-site request to quietly ask the target server: \"Am I allowed to send this?\" It's a permission slip, sent automatically, before the real request goes out.</p><h4>Why the browser asks first</h4><p>The Same-Origin Policy stops JavaScript on <code>shop.com</code> from freely poking <code>bank.com</code>. CORS is the controlled relaxation of that rule. For requests the browser deems <strong>\"simple\"</strong> — basically a plain <code>GET</code>/<code>HEAD</code>/<code>POST</code> with ordinary form-ish headers — it just sends them and checks the response afterward. But for anything <strong>fancy</strong> — a custom header like <code>Authorization</code>, a <code>Content-Type: application/json</code>, or a method like <code>PUT</code>/<code>DELETE</code> — it sends a <strong>preflight</strong> first.</p><h4>What the preflight contains</h4><p>The preflight is an <code>OPTIONS</code> request carrying <code>Access-Control-Request-Method</code> (the method it wants to use) and <code>Access-Control-Request-Headers</code> (the custom headers it wants to send). The server must answer with matching <code>Access-Control-Allow-Origin</code>, <code>-Methods</code>, and <code>-Headers</code>. Only if those line up does the browser release the real request.</p><div class=\"analogy\"><p>It's like calling a restaurant ahead of a big group: \"We're 12 people, two need wheelchair access, can you handle that?\" If the host says yes to each item, you drive over. <code>Access-Control-Max-Age</code> is the host saying \"and you're pre-approved for the next two hours\" — so you skip the call on your next visit. Browsers cap that memory (Chromium ~2h, Firefox ~24h).</p></div><h4>The dangerous misconception</h4><p>Here's the trap that fools even senior engineers: people think CORS <em>protects their server</em> from malicious requests. It does not. The preflight and the real request both <strong>reach your server and execute</strong>. CORS is enforced <em>entirely by the browser</em>, and all it gates is whether the calling <em>JavaScript is allowed to read the response</em>. A malicious site can still trigger your endpoint; CORS just blinds its JS to the answer. That's why CORS is not CSRF protection — CSRF defenses (tokens, SameSite cookies) are a separate concern.</p><h4>Concrete flow</h4><p><code>fetch('https://api.x.com/v1', { headers: { Authorization: 'Bearer t', 'Content-Type': 'application/json' } })</code> → because of the custom header and JSON type, the browser first sends <code>OPTIONS</code>; the server replies with matching <code>Allow-*</code> headers; then the real request goes through.</p><div class=\"key\">A preflight asks permission to <em>read</em> a cross-origin response, not to <em>send</em> the request. The request still arrives and runs — so never mistake CORS for server-side security.</div>"},
  "cors-credentials": {"teach":"<p>At its core, this rule says: the moment a cross-site request carries the user's cookies, the server can no longer answer with a lazy \"anyone is allowed\" — it must name the <em>exact</em> caller it's blessing.</p><h4>The setup</h4><p>Normally a server can permit cross-origin reads with <code>Access-Control-Allow-Origin: *</code> — a wildcard meaning \"any site may read this.\" Fine for public, anonymous data. But when the request includes <strong>credentials</strong> (cookies or HTTP auth, triggered by <code>credentials: 'include'</code> in fetch), the browser tightens the rules dramatically.</p><h4>The two requirements</h4><p>For a credentialed request, the browser will only expose the response if the server returns <strong>both</strong>:</p><ul><li><code>Access-Control-Allow-Origin</code> set to the <em>literal exact origin</em> (e.g. <code>https://app.example.com</code>) — not <code>*</code>;</li><li><code>Access-Control-Allow-Credentials: true</code>.</li></ul><p>A literal <code>*</code> is rejected outright. And with credentials, even <code>*</code> in <code>Allow-Headers</code> or <code>Allow-Methods</code> stops being a wildcard — it's treated as the literal string \"asterisk,\" matching nothing useful.</p><div class=\"analogy\"><p>Think of a club. A sign saying \"anyone welcome\" (<code>*</code>) is fine for the public lobby. But the VIP room where your wallet is held requires the bouncer to check your name against the list and say it out loud: \"Yes, <em>you, app.example.com</em>, come in.\" A blanket \"anyone\" is forbidden precisely because the room holds something valuable (your authenticated session).</p></div><h4>Why <code>Vary: Origin</code> becomes mandatory</h4><p>Because the server now reflects a <em>different</em> <code>Allow-Origin</code> depending on who's calling, a shared cache (CDN, proxy) could cache the response for origin A and wrongly serve it — with A's allow-header — to origin B. Emitting <code>Vary: Origin</code> tells caches \"this response depends on the request's Origin header; key your cache on it.\" Skip it and you get cross-origin header leakage.</p><h4>The trap</h4><p>The single most common mistake is setting <code>Allow-Origin: *</code> together with <code>Allow-Credentials: true</code> and expecting it to work. The browser silently blocks the response — no error in the network tab that screams the cause — and you burn an afternoon. The fix is always a <strong>server-side allowlist that reflects the matched origin</strong>:</p><p><code>if (allowlist.has(req.origin)) { res.set('Access-Control-Allow-Origin', req.origin); res.set('Access-Control-Allow-Credentials','true'); res.set('Vary','Origin'); }</code></p><div class=\"key\">Credentials and wildcards are mutually exclusive: with cookies in play you must echo the exact origin, add <code>Allow-Credentials: true</code>, and emit <code>Vary: Origin</code> so caches don't cross the wires.</div>"},
  "samesite-cookie": {"teach":"<p>At its core, <code>SameSite</code> is a single cookie setting that answers one question: \"When a request to my site comes from <em>another</em> site, should the browser attach this cookie?\" It's your first line of defense against cross-site request forgery (CSRF).</p><h4>Why it exists</h4><p>Cookies are sent automatically by the browser on every request to their domain — including requests <em>triggered by other sites</em>. So if you're logged into <code>bank.com</code> and visit <code>evil.com</code>, a hidden form there can POST to <code>bank.com/transfer</code> and your session cookie rides along. That's CSRF. <code>SameSite</code> lets your cookie opt out of being sent in those cross-site contexts.</p><h4>The three values</h4><ul><li><strong><code>Lax</code></strong> (the modern browser default): sent on <em>top-level GET navigations</em> — clicking a link to your site — but withheld from cross-site subresources and cross-site <code>POST</code>s. A good balance: email links still log you in, but a forged background POST gets no cookie.</li><li><strong><code>Strict</code></strong>: withheld in <em>any</em> cross-site context, even a plain link click. Most secure, but a user clicking your link from elsewhere arrives logged out.</li><li><strong><code>None</code></strong>: always sent cross-site — required for legitimate third-party embedding — but only allowed alongside the <code>Secure</code> flag (HTTPS-only).</li></ul><div class=\"analogy\"><p>Think of the cookie as your house key and SameSite as a rule about when a stranger can use it on your behalf. <code>Strict</code>: only you, walking up to your own door, may use it. <code>Lax</code>: a friend handing you a note that leads you to <em>walk in yourself</em> (a top-level navigation) is okay, but no one can use it to quietly send a package while you're not looking (a background POST). <code>None</code>: anyone may use it anywhere — so it had better be over a secure channel.</p></div><h4>Stacking the defenses</h4><p>Pair SameSite with two siblings. <code>HttpOnly</code> hides the cookie from JavaScript (<code>document.cookie</code> can't read it), blunting XSS theft. <code>Secure</code> forbids sending it over plain HTTP. A solid session cookie: <code>Set-Cookie: session=abc; HttpOnly; Secure; SameSite=Lax</code>.</p><h4>The trap</h4><p>Don't lean on <code>SameSite=Lax</code> alone to stop CSRF on <em>GET-triggered</em> state changes. Lax still sends the cookie on top-level GET navigations — so if you (wrongly) mutate state on a <code>GET</code>, a top-level link to that URL fires the action <em>with</em> the cookie attached. The lesson loops back: don't mutate on GET. With third-party cookies now phased out, legitimate cross-site cookie use needs partitioned cookies (CHIPS, the <code>Partitioned</code> attribute).</p><div class=\"key\"><code>SameSite=Lax</code> is a strong, cheap CSRF baseline — but it still flows on top-level GETs, so it only works if you never put mutations behind GET, and you should always combine it with <code>HttpOnly</code> and <code>Secure</code>.</div>"},
  "http-cache-control": {"teach":"<p>At its core, <code>Cache-Control</code> is a set of instructions you stamp on a response telling browsers and CDNs two things: how long they may reuse this response without asking, and what to do when that time runs out.</p><h4>The freshness dial</h4><p><code>max-age=N</code> says \"reuse me freely for N seconds.\" <code>s-maxage=N</code> is the same but specifically for <em>shared</em> caches (CDNs, proxies), overriding <code>max-age</code> there. During that window the cache serves the stored copy instantly — no network trip at all.</p><h4>The revalidation directives — and the famous trap</h4><p>Here is the misconception that catches everyone: <code>no-cache</code> does <strong>not</strong> mean \"don't cache.\" It means \"you may store this, but you must <em>revalidate</em> with the server before every reuse\" (typically a cheap ETag check that returns a tiny <code>304</code>). The directive that actually forbids storage is <code>no-store</code> — nothing is written to disk or memory, used for sensitive data like bank statements.</p><div class=\"analogy\"><p>Think of leftovers in your fridge. <code>max-age</code> is a \"good until Friday\" label — eat freely, no checking. <code>no-cache</code> is \"keep it, but sniff it before every bite\" — you store it, but you verify freshness each time. <code>no-store</code> is \"throw it out immediately, never keep it.\" Confusing the sniff-test rule with the throw-it-out rule is the classic mistake.</p></div><h4>Audience and reloads</h4><p><code>private</code> limits caching to the end user's browser (good for personalized pages); <code>public</code> lets shared CDNs cache it too. <code>immutable</code> is a promise: \"this content will <em>never</em> change at this URL,\" so the browser shouldn't even revalidate on a manual reload — saving requests entirely.</p><h4>The canonical strategy</h4><p>This combination powers fast modern sites:</p><ul><li><strong>Static assets get content-hashed filenames</strong> like <code>app.4f9a.js</code> and <code>Cache-Control: public, max-age=31536000, immutable</code>. Because the hash changes whenever the content changes, the URL itself is the version — so you can cache for a year with zero revalidation, and a new build simply references a new filename.</li><li><strong>HTML is short-lived or revalidated</strong>: <code>Cache-Control: no-cache</code>, so the browser always rechecks <code>index.html</code> via ETag and immediately discovers the new hashed asset URLs.</li></ul><div class=\"key\">Remember the inverted naming: <code>no-cache</code> means \"cache but always revalidate,\" while <code>no-store</code> means \"never store.\" Hash your assets and cache them <code>immutable</code> for a year; keep HTML revalidated so new hashes are picked up instantly.</div>"},
  "etag-revalidation": {"teach":"<p>At its core, an ETag is a fingerprint the server attaches to a response so that later the browser can ask \"is this still the same?\" and, if it is, get a tiny \"yep, unchanged\" reply instead of re-downloading the whole thing.</p><h4>How the conversation goes</h4><p>When the server sends a response, it adds a header like <code>ETag: \"v7\"</code> — an opaque token (you don't parse it; only equality matters). The browser stores the body alongside that tag. Later, when the cached copy needs revalidating, the browser sends the request back with <code>If-None-Match: \"v7\"</code>, meaning \"only send me a fresh body if the fingerprint is no longer v7.\"</p><ul><li>If the resource still fingerprints to <code>\"v7\"</code>, the server replies <strong><code>304 Not Modified</code> with an empty body</strong>. The browser serves its cached copy instantly — you paid only for a few header bytes.</li><li>If it changed, the server sends a normal <code>200</code> with the new content and a new ETag (say <code>\"v8\"</code>).</li></ul><div class=\"analogy\"><p>It's like texting a friend a photo's caption hash instead of resending the photo: \"Still the one tagged v7?\" They reply \"yep\" (304) and you keep the copy you already have. Only if they say \"no, it's different now\" do they bother re-sending the megabytes.</p></div><h4>Strong vs weak</h4><p>A <strong>strong</strong> ETag asserts <em>byte-for-byte identity</em> — the resource is literally identical. A <strong>weak</strong> ETag, written <code>W/\"...\"</code>, asserts only <em>semantic equivalence</em> — \"the same as far as meaning goes,\" even if whitespace or a timestamp differs. Weak tags are fine for ordinary caching but unsafe for <strong>range requests</strong> (resuming a partial download), because there you genuinely need byte-exactness or you'd stitch together mismatched chunks.</p><h4>Why it pairs with <code>no-cache</code></h4><p>ETags shine alongside <code>Cache-Control: no-cache</code>: the browser stores the resource but revalidates every time — and that revalidation is cheap because a <code>304</code> carries no body. You get always-fresh content at near-zero bandwidth.</p><h4>The cluster trap</h4><p>A nasty production bug: generating ETags from a file's <em>inode or mtime</em>. In a multi-server cluster behind a load balancer, each node's filesystem reports different inode numbers, so the same file emits a <em>different</em> ETag per server. A client revalidating against a different node than served the original never matches — so you <em>never</em> get a 304, and revalidation silently degrades to full re-downloads. Fix: derive the ETag from a content hash so every node agrees.</p><div class=\"key\">An ETag turns \"is this stale?\" into a few header bytes and a body-less <code>304</code>. Generate it from content (a hash), not inode/mtime, or load-balanced clusters will never produce a cache hit.</div>"},
  "http3-quic": {"teach":"<p>At its core, HTTP/3 is the web's protocol rebuilt on top of UDP instead of TCP, so that one lost packet only stalls the <em>one</em> download it belongs to — not every download sharing the connection.</p><h4>The problem it fixes: head-of-line blocking</h4><p>To see why this matters, you need the bug it kills. HTTP/2 lets you multiplex many requests over one TCP connection — images, scripts, data, all interleaved. Brilliant at the application layer. But TCP underneath guarantees <em>in-order</em> delivery: if packet #5 is lost, TCP holds back packets #6, #7, #8 — even though they belong to <em>different</em> streams — until #5 is retransmitted. One dropped packet freezes <em>everything</em>. That's <strong>TCP-level head-of-line (HOL) blocking</strong>, and HTTP/2 could not escape it because the blockage lives below it, in TCP.</p><h4>How QUIC solves it</h4><p>HTTP/3 carries the same HTTP semantics over <strong>QUIC</strong>, a brand-new transport built on UDP. QUIC makes each stream <em>independent at the transport layer</em>. Lose a packet and only that stream waits; the others keep flowing. UDP gives no ordering guarantees of its own, so QUIC reimplements reliability and ordering <em>per-stream</em> rather than for the whole connection.</p><div class=\"analogy\"><p>TCP is a single-lane road: one stalled car blocks the entire line behind it, regardless of where each car is headed. QUIC is a multi-lane highway: a breakdown in lane 3 stalls only lane 3, while lanes 1, 2, and 4 cruise past. Same cars, same destinations — but one stall no longer freezes the whole road.</p></div><h4>What else QUIC buys you</h4><ul><li><strong>TLS 1.3 fused in:</strong> security isn't a layer on top — it's part of the transport handshake, giving <strong>0-RTT or 1-RTT</strong> connection setup (returning clients can send data almost immediately).</li><li><strong>Connection migration:</strong> connections are identified by a <em>connection ID</em>, not the IP/port tuple. So when your phone switches from Wi-Fi to cellular and its IP changes, the connection survives instead of dropping.</li><li><strong>QPACK</strong> header compression, designed to avoid reintroducing HOL blocking through the header table.</li></ul><h4>The trap</h4><p>Don't assume HTTP/2's multiplexing already \"solved\" head-of-line blocking. It solved it only at the <em>application</em> layer; the <em>transport</em>-level TCP HOL blocking remained the whole time, and it took HTTP/3's move to QUIC/UDP to truly eliminate it. By 2026 H3 is broadly deployed across major CDNs and browsers.</p><div class=\"key\">HTTP/3's superpower is independent per-stream transport over QUIC/UDP: a single lost packet stalls only its own stream, finally killing the TCP-level head-of-line blocking that HTTP/2 could never escape.</div>"},
  "tls13-handshake": {"teach":"<p>At its core, the TLS 1.3 handshake is the brief negotiation where client and server agree on encryption keys before any real data flows — and version 1.3 makes that negotiation faster and harder to get wrong than ever before.</p><h4>The speed win: 1-RTT</h4><p>Older TLS needed two round trips of back-and-forth before encrypted data could move: the client asks, the server proposes, the client confirms, and only then does business begin. TLS 1.3 cuts this to a single round trip (<strong>1-RTT</strong>) by having the client <em>guess</em> ahead. In its very first message (the ClientHello) it includes a <strong>key-share</strong> — its half of the key-exchange math — for the algorithms it expects the server to pick. Usually the guess is right, so the server can reply with its half and the keys are derived immediately, no extra trip.</p><div class=\"analogy\"><p>Imagine two people setting up a secret code over the phone. The slow way: \"What ciphers do you support?\" → \"These\" → \"Okay use this one\" → \"Agreed, here's my key.\" TLS 1.3 is like the caller opening with \"Hi — assuming you do AES, here's my key already, what's yours?\" If the assumption holds, you skip an entire round of chit-chat.</p></div><h4>Security made mandatory, not optional</h4><p>TLS 1.3 doesn't just trim fat; it <em>removes weak choices</em>. It mandates ephemeral <strong>(EC)DHE</strong> key exchange, which gives <strong>forward secrecy</strong>: each session uses throwaway keys, so even if the server's long-term private key leaks later, past recorded traffic stays unreadable. It rips out the broken primitives of the past — static RSA key exchange, CBC mode, RC4, SHA-1 — so there's simply no insecure option to negotiate down to.</p><h4>0-RTT and its sharp edge</h4><p>For <em>returning</em> clients, TLS 1.3 offers <strong>0-RTT</strong> resumption: using a pre-shared secret from a prior session, the client can send application data in its very first packet — effectively zero handshake delay. But this early data is <strong>replay-vulnerable</strong>: an attacker who captures it can re-send it. So 0-RTT must be restricted to <em>safe/idempotent</em> requests. The trap is using 0-RTT for a non-idempotent <code>POST</code> (say, a payment) — a replay could double-charge.</p><h4>Hostname privacy</h4><p><strong>SNI</strong> tells the server which certificate to serve (for shared hosting), and <strong>ECH</strong> (Encrypted Client Hello) encrypts that hostname so eavesdroppers can't see which site you're visiting. Over QUIC, this whole TLS 1.3 handshake is folded directly into the transport setup.</p><div class=\"key\">TLS 1.3 buys you a 1-RTT handshake with mandatory forward secrecy and no weak ciphers — but 0-RTT early data is replayable, so reserve it strictly for safe/idempotent requests.</div>"},
  "sse-vs-websocket": {"teach":"<p>At its core, the choice is about direction. <strong>SSE streams data one way — server to browser.</strong> <strong>WebSocket opens a two-way live channel</strong> where both sides talk at once. Pick the simplest tool that matches your data flow.</p><h4>Server-Sent Events: the one-way stream</h4><p>SSE (the browser's <code>EventSource</code> API) is a long-lived HTTP response that the server keeps writing to, pushing events down to the browser as they happen. Because it's <em>just HTTP</em>, it inherits a lot for free: it works through proxies, respects your existing auth and caching, and — crucially — <strong>auto-reconnects</strong> on its own. If the connection drops, the browser reopens it and sends the <code>Last-Event-ID</code> header so the server can resume from where it left off. That makes SSE perfect for notifications, live feeds, dashboards, and <strong>LLM token streaming</strong> (the server pushes each token delta as it's generated).</p><h4>WebSocket: the two-way channel</h4><p>WebSocket starts as an HTTP request that <em>upgrades</em> the connection (you'll see a <code>101 Switching Protocols</code> response) into a full-duplex frame-based protocol. After that, client and server send messages independently with very low latency. This is what you want when <em>both</em> ends produce data continuously: chat, multiplayer games, collaborative editing, live cursors.</p><div class=\"analogy\"><p>SSE is a radio broadcast: the station transmits, you listen, and if your signal cuts out the radio automatically re-tunes and catches the next bit. WebSocket is a phone call: both people can speak and listen at the same time — but you had to dial, and if the call drops, <em>you</em> have to redial and re-establish the conversation.</p></div><h4>The cost of WebSocket's power</h4><p>Because WebSocket leaves HTTP behind, you re-implement things SSE handed you: reconnection logic, re-authentication on reconnect, and backpressure handling. That's real work and a common source of bugs.</p><h4>The trap</h4><p>Engineers reflexively reach for WebSocket the instant they hear \"real-time.\" But if data only flows <em>server → client</em>, SSE is simpler, auto-reconnects, resumes cleanly, and rides plain HTTP. One caveat: on <strong>HTTP/1.1</strong>, SSE consumes one of the browser's ~6 connections-per-origin — a real constraint if you open several. On HTTP/2 and HTTP/3, multiplexing makes that a non-issue.</p><p>Concretely: an LLM chat UI streams tokens via SSE (server pushes deltas, browser appends them); a multiplayer cursor feature uses WebSocket because both peers send constantly.</p><div class=\"key\">Match the protocol to the data direction: server-only push → SSE (simpler, auto-reconnecting, plain HTTP); genuine two-way traffic → WebSocket (powerful, but you own reconnect, auth, and backpressure).</div>"},
  "fetch-abortsignal": {"teach":"<p>At its core, an <code>AbortSignal</code> is a kill switch you hand to <code>fetch</code> so you can cancel a request that's still in flight — when it's taking too long, or when the user has already moved on.</p><h4>The mechanism</h4><p>You create an <code>AbortController</code>, which exposes a <code>signal</code>. You pass that signal into fetch: <code>fetch(url, { signal })</code>. Then, whenever you want to stop the request, you call <code>controller.abort()</code>. Two things happen: the underlying network request is cancelled, and the fetch promise <strong>rejects with an <code>AbortError</code></strong> so your <code>catch</code> can react.</p><div class=\"analogy\"><p>Think of the controller as the remote and the signal as the wire to a toy car. <code>fetch</code> is the car driving off. Press the remote's stop button (<code>abort()</code>) and the wire yanks the car to a halt mid-drive. The same remote can be wired to several cars at once — abort once, and all of them stop.</p></div><h4>Handy shortcuts</h4><ul><li><code>AbortSignal.timeout(ms)</code> is a one-liner that auto-aborts after a deadline — instant request timeouts without manual <code>setTimeout</code> bookkeeping.</li><li><code>AbortSignal.any([...])</code> composes several signals into one that fires when <em>any</em> of them does — e.g. \"abort if the 5-second timeout hits <strong>or</strong> the user clicks cancel.\"</li></ul><h4>The trap everyone hits: fetch doesn't reject on HTTP errors</h4><p>This is the most important quirk. <code>fetch</code> only rejects its promise on a <em>network</em> failure or an abort. A perfectly delivered <code>404</code> or <code>500</code> response is, to fetch, a <strong>success</strong> — the request completed, it just carried an error status. So your <code>try/catch</code> will <em>not</em> fire on a 500. You must check <code>res.ok</code> (true for 200–299) yourself and throw if it's false. Treating a 500 as an automatic exception is a classic bug.</p><h4>One more: the body is single-use</h4><p>The response body is a stream you can read <em>once</em>. Call <code>res.json()</code> or <code>res.text()</code> a second time and it fails. If you genuinely need to read it twice (say, log it and parse it), call <code>res.clone()</code> first.</p><h4>The canonical React use</h4><p><code>const c = new AbortController(); fetch(url, { signal: c.signal }); /* cleanup */ return () =&gt; c.abort();</code> — when the component unmounts or its inputs change, the effect's cleanup aborts the now-stale request, preventing wasted work and \"set state on unmounted component\" races.</p><div class=\"key\">Abort gives you cancellable, timeout-able fetches — but remember fetch only rejects on network failure or abort, so a 4xx/5xx is <em>not</em> an error until you check <code>res.ok</code> yourself.</div>"},
  "rest-vs-graphql": {"teach":"<p>At its core, the difference is who decides the shape of the data. With <strong>REST</strong>, the server defines fixed endpoints and you take what they give. With <strong>GraphQL</strong>, the client writes a query asking for <em>exactly</em> the fields it wants — no more, no less.</p><h4>The two pains REST creates</h4><p>REST exposes resources at URLs like <code>/user/1</code>. Two friction points show up in real UIs:</p><ul><li><strong>Over-fetching:</strong> the endpoint returns 30 fields but your screen needs 3. You download the rest for nothing.</li><li><strong>Under-fetching / waterfalls:</strong> to render a user and their posts, you call <code>GET /user/1</code>, then <code>GET /user/1/posts</code>, then maybe each post's comments — a chain of round-trips, each waiting on the last.</li></ul><h4>How GraphQL responds</h4><p>GraphQL offers a single typed endpoint (usually <code>POST /graphql</code>) where you send a query describing the precise tree of data you want. The query <code>{ user(id:1){ name posts{ title } } }</code> returns exactly that — the user's name and each post's title — in <em>one</em> request. No over-fetch, no waterfall.</p><div class=\"analogy\"><p>REST is a fixed-menu restaurant: you order \"the combo plate\" and get whatever's on it, even the coleslaw you'll leave untouched — and if you want dessert you make a second trip to the counter. GraphQL is a build-your-own bowl: you point at exactly the ingredients you want and get one bowl, precisely portioned.</p></div><h4>What flexibility costs</h4><p>That power isn't free:</p><ul><li><strong>HTTP caching gets harder.</strong> REST's <code>GET /user/1</code> is trivially cached by browsers and CDNs by URL. A GraphQL <code>POST /graphql</code> with a body isn't — so teams adopt <em>persisted queries</em> (pre-registered query IDs) and <em>normalized client caches</em> (Apollo, Relay) to claw caching back.</li><li><strong>Expensive nested queries</strong> let a client request deeply recursive data; you must guard with depth/complexity limits.</li><li><strong>The N+1 resolver problem:</strong> naively, fetching 100 users' posts fires 100 separate post queries. The standard fix is <strong>DataLoader</strong>, which batches them into one.</li></ul><h4>The trap</h4><p>Don't believe GraphQL is universally \"better\" or \"faster.\" It trades easy HTTP/CDN caching and operational simplicity for query flexibility — a great deal when <em>many</em> diverse clients hit your API, but often overkill when one team owns both client and server. For first-party 2026 apps, REST with sparse fieldsets, tRPC, or React Server Components frequently win on simplicity.</p><div class=\"key\">GraphQL eliminates over- and under-fetching by letting the client name the exact response shape — but it sacrifices effortless HTTP caching and adds N+1 and complexity concerns, so reach for it when client diversity (not just \"real-time\") justifies the trade.</div>"}
});
