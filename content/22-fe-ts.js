/* TypeScript (FE-TS) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"FE-TS","front":"Is TypeScript's type system structural or nominal, and what's the one exception?","back":"<p>TypeScript is <strong>structurally typed</strong>: assignability is decided by shape, not by declared name. An object satisfies an interface if its members match — no <code>implements</code> needed. This is the opposite of Java/C# <em>nominal</em> typing.</p><p>The notable carve-out is <strong>excess property checking</strong>: a <em>fresh object literal</em> assigned/passed directly is rejected if it has properties not in the target type, catching typos that pure structural rules would allow. Assigning through an intermediate variable bypasses it. For true nominal guarantees, use <strong>branded types</strong> (<code>string & { __brand: 'X' }</code>).</p>"},
  {"sec":"FE-TS","front":"When should you reach for `interface` vs `type`, and what can each do that the other cannot?","back":"<p><code>interface</code> supports <strong>declaration merging</strong> (same-name declarations combine) — required for module/global augmentation — and is marginally faster for the checker on large <code>extends</code> hierarchies because instances are cached by name. <code>type</code> aliases are closed (redeclaration is a duplicate-identifier error) but can express <strong>unions, tuples, primitives, and mapped/conditional types</strong> that interfaces cannot.</p><p>Rule of thumb: <code>interface</code> for public, extensible object contracts; <code>type</code> for unions and computed types. They are <em>not</em> interchangeable — only one can merge, only one can be a union.</p>"},
  {"sec":"FE-TS","front":"How does a discriminated union give you compile-time exhaustiveness?","back":"<p>Each member shares a single <strong>literal discriminant</strong> (e.g. <code>kind: 'ok'</code> vs <code>kind: 'err'</code>). Switching on it narrows each branch to exactly one member. The discriminant must be a <em>literal</em> type — a plain <code>string</code> tag won't narrow.</p><p>In the <code>default</code> branch, assign the variable to <code>never</code>: <code>const _x: never = s;</code>. While all variants are handled, <code>s</code> has narrowed to <code>never</code> and the assignment compiles. Add a new variant without handling it and <code>s</code> is no longer <code>never</code> → <strong>compile error</strong>, forcing you to update the switch.</p>"},
  {"sec":"FE-TS","front":"What guard forms drive narrowing, and when does narrowing get lost?","back":"<p>Control-flow narrowing reacts to <code>typeof</code>, <code>instanceof</code>, <code>in</code>, truthiness, <code>===</code>/<code>!==</code> against literals, discriminant switches, user-defined predicates (<code>x is T</code>), and assertion functions (<code>asserts x is T</code>). Narrowing is <em>compiler-verified</em>, unlike <code>as</code>.</p><p>It can be <strong>invalidated inside closures/callbacks</strong>: if a <code>let</code> variable is captured by a function that could run later, the compiler can't prove it wasn't reassigned, so it widens back to the declared type. Narrow into a <code>const</code> (or read into a local) to make it stick.</p>"},
  {"sec":"FE-TS","front":"Type predicate (`x is T`) vs assertion signature (`asserts x is T`) — and the safety catch?","back":"<p>A function returning <code>arg is T</code> narrows callers in the <strong>true branch</strong> based on its boolean result. A function returning <code>asserts arg is T</code> throws on failure and narrows <strong>everything after the call</strong> (no branch needed).</p><p>Critical catch: TypeScript does <em>not</em> verify the body matches the claim — <code>(x): x is Cat =&gt; true</code> compiles and lies, silently corrupting type safety. Treat guard bodies as trusted, tested code. As of <strong>TS 5.5</strong>, the compiler can <em>infer</em> simple predicates automatically (e.g. <code>arr.filter(x =&gt; x != null)</code> now narrows out <code>null</code>).</p>"},
  {"sec":"FE-TS","front":"Inside `function f<T extends U>(x: T)`, what do you know about `T`, and what can't you do?","back":"<p>The constraint guarantees every <code>T</code> has at least <code>U</code>'s members, so you can <em>read</em> them. The canonical pattern is <code>K extends keyof T</code> for type-safe property access. Constraints also enable defaults (<code>&lt;T = string&gt;</code>) and guide inference.</p><p>What you can't do: <strong>produce an arbitrary value of <code>T</code></strong> from a value of <code>U</code>. A caller may pass a narrower subtype, so <code>const t: T = someU</code> is an error — you only know <code>T</code> is <em>some</em> subtype of <code>U</code>, not <code>U</code> itself.</p>"},
  {"sec":"FE-TS","front":"What does `keyof` return on an index-signature type, and what's the `T[number]` trick?","back":"<p><code>keyof T</code> is the union of property keys; <code>T[K]</code> (indexed access) is the type at key <code>K</code>. On a type with a <strong>string index signature</strong>, <code>keyof</code> collapses to <code>string | number</code> (numeric keys coerce to strings) — not the literal keys you may expect.</p><p><code>T[number]</code> on an array/tuple yields its <strong>element type</strong>. Combined with <code>as const</code>, <code>typeof ARR[number]</code> derives a literal union from runtime data: <code>const R = ['a','b'] as const; type X = typeof R[number]; // 'a' | 'b'</code>.</p>"},
  {"sec":"FE-TS","front":"What is a mapped type, and what does 'homomorphic' mean for it?","back":"<p><code>{ [K in keyof T]: F&lt;T[K]&gt; }</code> iterates a key union to rebuild an object type. The <code>+</code>/<code>-</code> modifiers add/remove <code>readonly</code> and <code>?</code> (<code>-readonly</code>, <code>-?</code>). <strong>TS 4.1</strong> added <strong>key remapping</strong> via <code>as</code>, letting you rename keys (<code>as \\`get${Capitalize&lt;string & K&gt;}\\`</code>) or drop them by mapping to <code>never</code>.</p><p>A mapped type written directly over <code>keyof T</code> is <strong>homomorphic</strong>: it preserves the original modifiers and array/tuple-ness. Map over an arbitrary union instead and those properties are <em>not</em> preserved. <code>Partial</code>, <code>Readonly</code>, and <code>Pick</code> are homomorphic; <code>Record</code> is not.</p>"},
  {"sec":"FE-TS","front":"How does `infer` work, and what is conditional-type distribution?","back":"<p><code>T extends U ? X : Y</code> branches on assignability at the type level. <code>infer R</code> inside the <code>extends</code> clause captures a matched portion — the engine behind <code>ReturnType</code>, <code>Parameters</code>, and <code>Awaited</code>.</p><p>When the checked type is a <strong>naked type parameter</strong> over a union, the conditional <strong>distributes</strong> across each member: <code>T extends U ? ... : ...</code> with <code>T = A | B</code> evaluates to <code>(A extends U?...) | (B extends U?...)</code>. Wrap both sides in a 1-tuple — <code>[T] extends [U] ? ...</code> — to <em>turn distribution off</em> and test the union atomically (also how you detect <code>never</code>, since distributing over <code>never</code> yields <code>never</code>).</p>"},
  {"sec":"FE-TS","front":"How are the built-in utility types implemented, and where do they bite you?","back":"<p>They're ordinary mapped/conditional types, not compiler magic: object shapers (<code>Partial</code>, <code>Required</code>, <code>Readonly</code>), selectors (<code>Pick</code>, <code>Omit</code>, <code>Record</code>), union filters (<code>Exclude</code>, <code>Extract</code>, <code>NonNullable</code>), extractors (<code>ReturnType</code>, <code>Parameters</code>, <code>InstanceType</code>, <code>Awaited</code>). <code>Omit&lt;T,K&gt;</code> is literally <code>Pick&lt;T, Exclude&lt;keyof T, K&gt;&gt;</code>.</p><p>The trap: <strong><code>Omit</code> does not constrain its keys to <code>keyof T</code></strong> (its key param is <code>keyof any</code>), so <code>Omit&lt;User, 'emial'&gt;</code> omits nothing and fails silently — unlike <code>Pick</code>, which errors on a bad key.</p>"},
  {"sec":"FE-TS","front":"`unknown` vs `any` — what's the precise difference?","back":"<p><code>unknown</code> is the type-safe <strong>top type</strong>: every value is assignable to it, but you can do nothing with it until you narrow. <code>any</code> also accepts everything but <em>disables</em> type checking on whatever it touches, propagating silently and defeating the checker far beyond one line.</p><p>Use <code>unknown</code> at untrusted boundaries — <code>JSON.parse</code>, network responses, and <code>catch (e: unknown)</code> (the default under <code>useUnknownInCatchVariables</code>) — then narrow with guards. Reserve <code>any</code> for genuine, isolated escape hatches.</p>"},
  {"sec":"FE-TS","front":"What makes `never` special, and how does it differ from `void`?","back":"<p><code>never</code> is the <strong>bottom type</strong>: no value inhabits it, it is assignable <em>to</em> every type, and nothing (except <code>never</code>) is assignable <em>to</em> it. It's the return type of functions that never return (throw / infinite loop), the type of impossible narrowings, and the value of mapped-type keys you filter out.</p><p>Don't confuse it with <code>void</code>: <code>void</code> means the function <em>returns</em> but you should ignore the value; <code>never</code> means it <strong>does not return at all</strong>. Its killer use is the discriminated-union exhaustiveness check.</p>"},
  {"sec":"FE-TS","front":"What do `as` and `!` actually do at runtime?","back":"<p><strong>Nothing.</strong> <code>x as T</code> reinterprets a value's static type at <em>compile time only</em> — no conversion, no check, bytes unchanged (unlike a C# cast). TS allows it only between overlapping types unless you double-assert through <code>unknown</code> (<code>x as unknown as T</code>, a code smell). The non-null assertion <code>x!</code> strips <code>null</code>/<code>undefined</code> with no runtime guard.</p><p>Both are <em>unverified promises</em>; being wrong surfaces as the very runtime error the type system was meant to prevent. Prefer narrowing or real validation; reserve assertions for things the compiler genuinely can't see (refs set later, framework-initialized fields).</p>"},
  {"sec":"FE-TS","front":"What does `strictNullChecks` change, and what does it NOT cover?","back":"<p>With it on, <code>null</code> and <code>undefined</code> leave every other type's domain and must be written explicitly (<code>string | undefined</code>), forcing you to narrow before dereferencing — it eliminates most null-reference bugs and is the highest-value <code>strict</code> flag.</p><p><code>strict: true</code> also bundles <code>noImplicitAny</code>, <code>strictFunctionTypes</code> (contravariant parameter checking), <code>strictBindCallApply</code>, <code>strictPropertyInitialization</code>, and <code>useUnknownInCatchVariables</code>. It does <strong>not</strong> add <code>| undefined</code> to index access — <code>arr[i]</code> stays typed as the element unless you separately enable <code>noUncheckedIndexedAccess</code>.</p>"},
  {"sec":"FE-TS","front":"What does `as const` infer, and why does it replace `enum`?","back":"<p><code>as const</code> infers the narrowest type: primitives stay literal, object properties become deeply <code>readonly</code>, and arrays become <code>readonly</code> tuples. It makes a runtime constant double as the single source of truth for a literal union via <code>typeof X[number]</code> (arrays) or <code>typeof X[keyof typeof X]</code> (objects).</p><p>It's the recommended <code>enum</code> replacement: you get a real value object <em>and</em> a precise type with no extra runtime emit. Trap: a <code>readonly</code> tuple is rejected by APIs expecting a mutable array — spread it or cast when needed.</p>"},
  {"sec":"FE-TS","front":"Why is `enum` discouraged in modern TS, and what breaks `const enum`?","back":"<p>A regular <code>enum</code> compiles to a runtime object (numeric enums even add reverse mappings), so it isn't erasable and adds bundle weight; numeric enums are also unsafe because any <code>number</code> is assignable to them. A <strong>literal union</strong> (or <code>as const</code> object + derived union) lives only in type space and vanishes after compilation.</p><p><code>const enum</code> inlines its values but <strong>breaks under <code>isolatedModules</code>, Babel, and esbuild</strong> (single-file transpilers can't see the enum's values). As of <strong>TS 5.8</strong>, the <code>--erasableSyntaxOnly</code> flag — aligned with Node's native type-stripping — errors on <em>all</em> enums (including <code>const enum</code>), namespaces with runtime code, and parameter properties.</p>"},
  {"sec":"FE-TS","front":"What is a `.d.ts` file for, and when is it global vs a module?","back":"<p><code>.d.ts</code> files hold <strong>ambient declarations</strong> and emit no JS. <code>declare module 'x'</code> types an untyped package; <code>declare module '*.svg'</code> types asset imports; <code>declare global { ... }</code> augments globals like <code>Window</code>; reopening a library's module (<strong>module augmentation</strong>) adds fields such as Express's <code>Request.user</code> — all powered by declaration merging, so the target must be an <code>interface</code>/<code>namespace</code>, not a <code>type</code> alias.</p><p>Scope rule: a <code>.d.ts</code> with <strong>no top-level <code>import</code>/<code>export</code></strong> is treated as <em>global</em>. Add any import and it becomes a module — then your <code>declare global</code> needs an explicit <code>export {}</code> to compile.</p>"}
);

Object.assign(GLOSSARY_DATA, {
  "structural-typing": {"term":"Structural Typing","group":"FE-TS","eli5":"If it has the right shape, TypeScript accepts it — the name on the box doesn't matter.","memory":"\"Walks like a duck\" — shape over name.","full":"<p>TypeScript's type compatibility is <strong>structural</strong>: a value is assignable to a target type when its members structurally match, regardless of the declared class/interface name. This is the opposite of <em>nominal</em> typing (Java/C#), where identity is by declaration. It makes TS ergonomic for plain objects but means two semantically different types with the same shape are interchangeable.</p><p>The one carve-out is <strong>excess property checking</strong> on fresh object literals, which catches typos that structural rules would otherwise allow. To get nominal guarantees back, engineers use <strong>branded types</strong> (intersecting a unique phantom property).</p>","trap":"Thinking you need <code>implements</code> for an object to satisfy an interface — you don't; matching shape is sufficient.","worked":"type UserId = string & { readonly __brand: 'UserId' };\nconst toUserId = (s: string) => s as UserId;\n// now a raw string can't be passed where UserId is expected","related":["type-vs-interface","discriminated-union"]},
  "type-vs-interface": {"term":"type vs interface","group":"FE-TS","eli5":"Two ways to name an object shape; interfaces can re-open and merge, type aliases can describe anything.","memory":"Interface = open & mergeable; type = closed & expressive.","full":"<p><code>interface</code> declarations with the same name in the same scope <strong>merge</strong> (declaration merging) — essential for augmenting global or library types. <code>type</code> aliases are closed: redeclaring is an error. Conversely, <code>type</code> can express unions, tuples, primitives, and mapped/conditional types that <code>interface</code> cannot.</p><p>For public, extendable object contracts prefer <code>interface</code>; for unions and computed types use <code>type</code>. The checker can handle <code>interface</code> slightly more efficiently for large extension hierarchies because interface instances are cached by name.</p>","trap":"Believing they're fully interchangeable — only interfaces support declaration merging, and only type aliases express unions/tuples.","worked":"interface W { a: number }\ninterface W { b: number } // merges -> { a; b }\ntype U = 'x' | 'y';       // impossible with interface","related":["structural-typing","declaration-merging","mapped-type"]},
  "discriminated-union": {"term":"Discriminated Union","group":"FE-TS","eli5":"A set of object shapes that each carry a label, so checking the label tells TypeScript exactly which shape you have.","memory":"Tag it, switch it, never-default it.","full":"<p>A union of object types sharing a single <strong>literal discriminant</strong> property (often <code>kind</code> or <code>type</code>). Switching on the discriminant narrows each branch to one exact member, giving full type-safe access to that member's fields. It's the backbone of typed reducers, state machines, and result/error modeling.</p><p>Pairing the <code>default</code> branch with an assignment to <code>never</code> creates a compile-time <strong>exhaustiveness check</strong>: adding a new variant breaks the build until it's handled.</p>","trap":"Using a non-literal discriminant (e.g. plain <code>string</code>) — narrowing only works when the tag is a literal type.","worked":"type S = { kind: 'ok'; data: string } | { kind: 'err'; msg: string };\nfunction h(s: S) {\n  switch (s.kind) {\n    case 'ok': return s.data;\n    case 'err': return s.msg;\n    default: const _e: never = s; return _e;\n  }\n}","related":["narrowing","never-type","type-guard"]},
  "narrowing": {"term":"Narrowing","group":"FE-TS","eli5":"Using runtime checks so TypeScript shrinks a broad type down to a precise one inside a branch.","memory":"Check at runtime, shrink at compile time.","full":"<p><strong>Narrowing</strong> is control-flow analysis that reduces a variable's type within a code path based on guards: <code>typeof</code>, <code>instanceof</code>, <code>in</code>, truthiness, equality, discriminant switches, user-defined predicates, and assertion functions. The compiler tracks the narrowed type per branch, so member access becomes safe without casts.</p><p>It's the primary alternative to <code>as</code> assertions — narrowing is <em>verified</em> by the compiler, whereas assertions are unverified promises.</p>","trap":"Expecting narrowing to survive a closure/callback — it can be invalidated when the compiler can't prove the variable wasn't reassigned (especially <code>let</code> captured in callbacks).","worked":"function f(x: string | number) {\n  if (typeof x === 'string') x.toUpperCase(); // x: string\n  else x.toFixed(2);                          // x: number\n}","related":["type-guard","discriminated-union","unknown-type"]},
  "type-guard": {"term":"User-Defined Type Guard","group":"FE-TS","eli5":"A function whose return type is a promise like 'p is Cat' that teaches TypeScript to narrow for you.","memory":"`is` narrows in the true branch; `asserts` narrows after the call.","full":"<p>A function returning a <strong>type predicate</strong> <code>arg is T</code> lets the compiler narrow callers based on its boolean result. An <strong>assertion signature</strong> <code>asserts arg is T</code> narrows everything after the call (it throws on failure). Both centralize reusable narrowing logic.</p><p>Crucially, TypeScript does <em>not</em> verify the body matches the predicate — it trusts you. A wrong guard silently corrupts type safety, so guard bodies should be treated as trusted, well-tested code. TS 5.5+ can infer simple predicates automatically (e.g. in <code>.filter</code>).</p>","trap":"Assuming the compiler checks your guard logic — it doesn't; <code>(x): x is Cat =&gt; true</code> compiles and lies.","worked":"function isCat(p: Pet): p is Cat {\n  return (p as Cat).meow !== undefined;\n}\nif (isCat(pet)) pet.meow();","related":["narrowing","unknown-type","type-assertion"]},
  "keyof": {"term":"keyof & Indexed Access","group":"FE-TS","eli5":"keyof gives the union of an object's property names; T[K] gives the type stored at a key.","memory":"keyof = the keys; T[K] = the value at a key.","full":"<p><code>keyof T</code> produces a union of <code>T</code>'s public property keys; <strong>indexed access</strong> <code>T[K]</code> retrieves the type at key <code>K</code>. Together they enable fully type-safe, generic property access and form the foundation of mapped types.</p><p><code>T[number]</code> on an array/tuple yields the element type — the trick behind deriving a union from <code>as const</code> data (<code>typeof ARR[number]</code>). On a type with a string index signature, <code>keyof</code> collapses to <code>string | number</code> rather than specific literals.</p>","trap":"Forgetting that <code>keyof</code> on a string-index-signature type gives <code>string | number</code>, not the concrete keys you wrote.","worked":"const ROLES = ['admin', 'user'] as const;\ntype Role = typeof ROLES[number]; // 'admin' | 'user'","related":["generic-constraint","mapped-type","as-const"]},
  "mapped-type": {"term":"Mapped Type","group":"FE-TS","eli5":"A type that loops over an object's keys and transforms each property to build a new type.","memory":"for-each-key in type space.","full":"<p><code>{ [K in keyof T]: ... }</code> iterates a key union and rebuilds an object type, optionally adding/removing <code>readonly</code> and <code>?</code> with the <code>+</code>/<code>-</code> modifiers. All the object-shaping utility types (<code>Partial</code>, <code>Readonly</code>, <code>Pick</code>, <code>Record</code>) are mapped types.</p><p>TS 4.1 added <strong>key remapping</strong> via <code>as</code>, letting you rename keys (e.g. build getter names with <code>Capitalize</code>) or filter them by returning <code>never</code>. A <em>homomorphic</em> mapped type (written directly over <code>keyof T</code>) preserves modifiers and array-ness; <code>Record</code> is not homomorphic.</p>","trap":"Expecting modifiers/array-ness to be preserved in a non-homomorphic map (over an arbitrary union instead of <code>keyof T</code>) — they aren't.","worked":"type Getters<T> = {\n  [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K];\n};","related":["keyof","conditional-type","utility-types"]},
  "conditional-type": {"term":"Conditional Type & infer","group":"FE-TS","eli5":"A type-level if/else that can also pull out a piece of another type into a variable.","memory":"T extends U ? then : else; `infer` plucks a type out.","full":"<p><code>T extends U ? X : Y</code> branches on assignability at the type level. The <code>infer R</code> keyword, used inside the <code>extends</code> clause, binds a matched portion of the type to a new variable — the mechanism behind <code>ReturnType</code>, <code>Parameters</code>, and <code>Awaited</code>.</p><p>When the checked type is a <em>naked</em> type parameter over a union, the conditional <strong>distributes</strong> across each member. Wrapping both sides in a 1-tuple (<code>[T] extends [U]</code>) turns distribution off, which is how you test a union atomically or detect <code>never</code>.</p>","trap":"Forgetting distribution — conditionals silently map over every union member unless you wrap in a tuple to stop it.","worked":"type ElementType<T> = T extends (infer E)[] ? E : T;\ntype A = ElementType<string[]>; // string","related":["mapped-type","utility-types","never-type"]},
  "utility-types": {"term":"Utility Types","group":"FE-TS","eli5":"Built-in type helpers like Partial, Pick, and ReturnType that transform existing types for you.","memory":"Partial/Required/Readonly shape it; Pick/Omit/Record select it; Exclude/Extract filter unions.","full":"<p>TypeScript ships a standard library of <strong>utility types</strong> built from mapped and conditional types: object shapers (<code>Partial</code>, <code>Required</code>, <code>Readonly</code>), selectors (<code>Pick</code>, <code>Omit</code>, <code>Record</code>), union filters (<code>Exclude</code>, <code>Extract</code>, <code>NonNullable</code>), and function/promise extractors (<code>ReturnType</code>, <code>Parameters</code>, <code>InstanceType</code>, <code>Awaited</code>).</p><p>They compose: <code>Omit</code> is literally <code>Pick&lt;T, Exclude&lt;keyof T, K&gt;&gt;</code>. Knowing they're not magic — just mapped/conditional types — lets you write your own (e.g. <code>DeepPartial</code>, <code>RequireOne</code>).</p>","trap":"Assuming <code>Omit</code> validates its keys like <code>Pick</code> does — its key param is <code>keyof any</code>, so a typo'd key omits nothing and fails silently.","worked":"type Patch = Partial<Pick<User, 'name' | 'email'>>;\ntype R = Awaited<Promise<Promise<number>>>; // number","related":["mapped-type","conditional-type","keyof"]},
  "unknown-type": {"term":"unknown vs any","group":"FE-TS","eli5":"unknown is the safe 'I don't know yet' type you must check before using; any just turns checking off.","memory":"unknown = top type with a seatbelt; any = no seatbelt.","full":"<p><code>unknown</code> is the type-safe <strong>top type</strong>: every value is assignable to it, but you can perform no operations until you narrow it. <code>any</code> also accepts everything but additionally <em>disables</em> type checking on anything it touches, silently propagating through your code.</p><p>Use <code>unknown</code> at every untrusted boundary — <code>JSON.parse</code>, network responses, <code>catch (e: unknown)</code> — then narrow with guards. Reserve <code>any</code> for genuine escape hatches and isolate it. The bottom type <code>never</code> is the complementary concept: no value inhabits it.</p>","trap":"Reaching for <code>any</code> to 'just make the error go away' — it spreads and silently defeats the type system far beyond the one line.","worked":"const data: unknown = JSON.parse(s);\nif (typeof data === 'object' && data && 'id' in data) { /* safe */ }","related":["never-type","narrowing","type-assertion"]},
  "never-type": {"term":"never","group":"FE-TS","eli5":"The type of a value that can't exist — used to prove a code path is impossible.","memory":"never = bottom; assign-to-never = 'this can't happen'.","full":"<p><code>never</code> is the <strong>bottom type</strong>: no value inhabits it, it's assignable <em>to</em> every type, and nothing (except <code>never</code>) is assignable to it. It's the type of functions that never return (throw or loop forever), of impossible narrowings, and of filtered-out mapped-type keys.</p><p>Its killer application is the <strong>exhaustiveness check</strong>: assigning a discriminated-union variable to <code>never</code> in a <code>default</code> branch turns any unhandled future variant into a compile error.</p>","trap":"Confusing <code>never</code> with <code>void</code> — <code>void</code> means 'returns, but ignore the value'; <code>never</code> means 'does not return at all'.","worked":"function fail(m: string): never { throw new Error(m); }\n// default: const _exhaustive: never = action;","related":["unknown-type","discriminated-union","conditional-type"]},
  "type-assertion": {"term":"Type Assertion & Non-null !","group":"FE-TS","eli5":"Telling the compiler 'trust me, it's this type' — with no runtime check behind it.","memory":"`as` and `!` are promises, not proofs.","full":"<p><code>x as T</code> reinterprets a value's type at <strong>compile time only</strong> — there is no runtime conversion or check. TS permits assertions only between overlapping types unless you double-assert through <code>unknown</code>. The non-null assertion <code>x!</code> tells the compiler a value isn't <code>null</code>/<code>undefined</code>, again with no runtime guard.</p><p>Both are unverified promises; being wrong yields runtime errors the type system was meant to prevent. Prefer narrowing or real runtime validation; reserve assertions for things the compiler genuinely can't see (framework-initialized values, refs set later).</p>","trap":"Thinking <code>as</code> performs a cast/conversion like in C# — it does nothing at runtime; the bytes are unchanged.","worked":"const el = document.getElementById('app')!; // assert non-null\nconst cfg = raw as unknown as Config;        // forced double-assert (smell)","related":["unknown-type","narrowing","strict-null-checks"]},
  "strict-null-checks": {"term":"strictNullChecks","group":"FE-TS","eli5":"Makes null and undefined separate types you must handle, instead of hiding in everything.","memory":"Null isn't free — you must opt it into the union.","full":"<p>With <code>strictNullChecks</code> enabled, <code>null</code> and <code>undefined</code> are removed from the domain of every other type and must be written explicitly (<code>string | undefined</code>). This forces you to narrow before dereferencing, eliminating the bulk of null-reference bugs at compile time. It's the highest-value flag in the <code>strict</code> family.</p><p><code>strict: true</code> bundles it with <code>noImplicitAny</code>, <code>strictFunctionTypes</code>, <code>strictPropertyInitialization</code>, <code>useUnknownInCatchVariables</code>, and more. Pair it with <code>noUncheckedIndexedAccess</code> for safe array/record reads.</p>","trap":"Assuming <code>strict</code> covers index access — it doesn't; <code>arr[i]</code> is typed without <code>| undefined</code> unless you also enable <code>noUncheckedIndexedAccess</code>.","worked":"function len(s: string | null) {\n  return s?.length ?? 0; // must handle null now\n}","related":["type-assertion","unknown-type","narrowing"]},
  "as-const": {"term":"const Assertion (as const)","group":"FE-TS","eli5":"Tells TypeScript to keep the most specific, read-only literal types instead of widening them.","memory":"as const = freeze + keep literals.","full":"<p><code>as const</code> infers the narrowest possible type: primitives stay literal, object properties become deeply <code>readonly</code>, and arrays become <code>readonly</code> tuples. It's the idiomatic way to make a runtime constant double as a single source of truth for a literal union (via <code>typeof X[number]</code> or <code>typeof X[keyof typeof X]</code>).</p><p>This is the recommended replacement for <code>enum</code>: you get a real value object <em>and</em> a precise literal type, with zero extra runtime emit beyond the data you already wrote.</p>","trap":"Passing an <code>as const</code> array to an API expecting a mutable array — the <code>readonly</code> tuple type is rejected.","worked":"const METHODS = ['GET', 'POST'] as const;\ntype Method = typeof METHODS[number]; // 'GET' | 'POST'","related":["keyof","structural-typing","enum-vs-union"]},
  "enum-vs-union": {"term":"enum vs Literal Union","group":"FE-TS","eli5":"Enums emit real JavaScript objects; a union of string literals lives only in types and disappears.","memory":"enum = runtime object; union = pure type, zero emit.","full":"<p>A TypeScript <code>enum</code> compiles to a runtime object (numeric enums even get reverse mappings), so it isn't erasable and adds bundle weight; numeric enums are also unsafe because any <code>number</code> is assignable. A <strong>literal union</strong> exists only in type space and vanishes after compilation.</p><p>Modern code favors <code>const X = {...} as const</code> plus a derived union, or a plain string-literal union. As of TS 5.8, the <code>--erasableSyntaxOnly</code> flag (aligned with Node's native type-stripping) errors on all enums — including <code>const enum</code> — plus namespaces with runtime code and parameter properties, accelerating the shift away from <code>enum</code>.</p>","trap":"Using <code>const enum</code> for 'free' inlining — it breaks under isolatedModules/Babel/esbuild and is rejected by <code>erasableSyntaxOnly</code>, causing surprising build failures.","worked":"const Status = { Active: 'active', Done: 'done' } as const;\ntype Status = typeof Status[keyof typeof Status];","related":["as-const","discriminated-union","structural-typing"]},
  "declaration-merging": {"term":"Declaration Merging","group":"FE-TS","eli5":"Multiple interface or namespace declarations with the same name combine into one.","memory":"Same name + interface = one merged shape.","full":"<p><strong>Declaration merging</strong> lets multiple <code>interface</code> (or <code>namespace</code>) declarations of the same name in the same scope be combined by the compiler into a single definition. It's the mechanism that powers <strong>module augmentation</strong> and <strong>global augmentation</strong> — you reopen a library's interface to add properties without forking it.</p><p><code>type</code> aliases cannot merge (duplicate identifier error), which is the decisive reason to choose <code>interface</code> for any contract you expect third parties to extend. Augmenting a module also requires the augmenting file to be a module (have a top-level <code>import</code>/<code>export</code>).</p>","trap":"Trying to augment a library type that was exported as a <code>type</code> alias — only <code>interface</code>/<code>namespace</code> declarations merge.","worked":"declare module 'express' {\n  interface Request { user?: { id: string } }\n}","related":["type-vs-interface"]},
  "generic-constraint": {"term":"Generic Constraint","group":"FE-TS","eli5":"A 'T extends X' bound that limits what a type parameter can be and unlocks safe operations on it.","memory":"extends = 'at least this much'.","full":"<p>Writing <code>&lt;T extends U&gt;</code> constrains the type parameter so callers must supply a subtype of <code>U</code>, and inside the function the compiler knows <code>T</code> has at least <code>U</code>'s members. The canonical use is <code>K extends keyof T</code> for type-safe property access. Constraints also enable generic defaults (<code>= ...</code>) and drive inference.</p><p>Inside the body, <code>T</code> is only known to be the constraint — not any concrete value of it — because a caller may pass a narrower subtype. This is why you can't assign a literal of the constraint to a <code>T</code>.</p>","trap":"Treating the constraint as the actual type inside the body — you can read constraint members but can't produce arbitrary values of <code>T</code>.","worked":"function pluck<T, K extends keyof T>(o: T, k: K): T[K] {\n  return o[k];\n}","related":["keyof","conditional-type","mapped-type"]}
});
