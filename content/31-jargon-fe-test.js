/* Frontend jargon & acronyms — clickable reference (FE-TEST, auto-assembled) */
Object.assign(GLOSSARY_DATA, {
  "rtl": {"term":"RTL","group":"FE-TEST","aliases":["React Testing Library","Testing Library"],"eli5":"A testing library that encourages you to query and assert on the DOM the way a real user (and assistive tech) would, rather than poking at component internals.","memory":"RTL = React Testing Library — test what the user sees, query by role/label, not by implementation details.","full":"<p><strong>RTL (React Testing Library)</strong> is a testing utility for React components built on a guiding philosophy: <em>\"the more your tests resemble the way your software is used, the more confidence they give you.\"</em> Instead of reaching into component state or instances, you render the component and interact with the rendered DOM through queries that mirror how users and assistive technology perceive the page.</p><p>RTL was created by <strong>Kent C. Dodds</strong> (first released around 2018) as a deliberate reaction against the then-dominant Enzyme, which encouraged testing implementation details like internal state and shallow rendering. RTL is the React binding of the broader <strong>Testing Library</strong> family (with siblings for Vue, Svelte, Angular, and a framework-agnostic DOM Testing Library core), and it became the de-facto standard for React component testing.</p><p>Its central contribution is a <em>priority order of queries</em> that ranks them by how closely they match real, accessible user access. The top tier is <code>getByRole</code> (best — it also asserts accessibility), then <code>getByLabelText</code> (forms), <code>getByPlaceholderText</code>, <code>getByText</code>, and so on, down to escape hatches like <code>getByTestId</code>. This nudges developers toward accessible markup as a side effect. RTL is still the recommended approach today precisely because querying by role/label makes tests resilient to refactors (they break only when user-facing behavior changes) and surfaces accessibility problems as test failures.</p>","worked":"<p>RTL queries the DOM the way a user would. Prefer <code>getByRole</code> — it also verifies accessibility:</p><p><code>render(&lt;LoginForm /&gt;);</code><br><code>// best: query by accessible role + name</code><br><code>const submit = screen.getByRole('button', { name: /log in/i });</code><br><code>// forms: query by label</code><br><code>const email = screen.getByLabelText(/email/i);</code><br><code>await userEvent.type(email, 'a@b.com');</code><br><code>await userEvent.click(submit);</code></p><p>If a button has no accessible name, <code>getByRole</code> can't find it — the test failing <em>is</em> the a11y signal.</p>","teach":"<p>At its core, RTL is just <em>a tool that makes you test your component the way a real person uses it</em> — find things on screen by what they say and what role they play, click and type like a user, then assert on what the user would see.</p><p>The old way (Enzyme) let you reach inside a component: inspect its state, call its methods, check that <code>this.state.isOpen === true</code>. The problem: those tests break the moment you refactor internals, even if the UI behaves identically — and they pass even if the UI is broken for users. RTL forbids that. You only get the rendered DOM.</p><div class=\"analogy\">Imagine testing a car. Enzyme-style testing pops the hood and checks that a specific wire is at 12 volts. RTL-style testing sits in the driver's seat, turns the key, and checks that the engine starts. The first test fails if an engineer reroutes the wiring — even though the car still drives perfectly. The second only fails if the car actually won't start, which is the thing you truly care about.</div><p>The heart of RTL is its <strong>query priority</strong>, ranked by how a real user — including someone using a screen reader — accesses the page:</p><ul><li><strong><code>getByRole</code></strong> — the best. Finds elements by their accessible role and name (button, heading, textbox). If this can't find your element, that's a sign the element isn't accessible — the test doubles as an a11y check.</li><li><strong><code>getByLabelText</code></strong> — for form fields, found by their associated label, exactly as a user reads them.</li><li><strong><code>getByPlaceholderText</code>, <code>getByText</code></strong> — visible text content.</li><li><strong><code>getByTestId</code></strong> — last resort, an escape hatch for things with no user-facing handle.</li></ul><p>Because you query the way users perceive the UI, tests survive refactors and break only when behavior genuinely changes — and they quietly push you toward accessible HTML.</p><div class=\"key\">RTL = React Testing Library: test from the user's perspective, not internals. Query by role first (it asserts accessibility too), then label, then text; avoid test IDs. Resilient to refactors, sensitive to real regressions, and an a11y nudge baked in.</div>","related":["msw"]},
  "e2e": {"term":"E2E","group":"FE-TEST","aliases":["End-to-End testing","End-to-End test","end to end"],"eli5":"A test that drives a real browser through a complete user journey, exercising the whole app stack like a real person would.","memory":"E2E = End-to-End: from the user's first click to the final result, the whole journey.","full":"<p><strong>E2E</strong> (End-to-End) testing verifies an application by simulating a real user's full journey through a real (or headless) browser — clicking, typing, navigating — and asserting on what the user actually sees. Unlike unit tests (which isolate a single function) or integration tests (which check a few pieces together), E2E exercises the entire stack: UI, routing, network, and often a real backend.</p><p>The term predates the web and comes from general software/systems testing, but in frontend it became prominent with browser-automation tools — Selenium (mid-2000s), then modern runners like <strong>Cypress</strong> and <strong>Playwright</strong> (Playwright created by Microsoft, released around 2020). In the classic \"testing pyramid\" (popularized by Mike Cohn), E2E tests sit at the narrow top: slow, expensive, and brittle, so you write very few of them.</p><p>Its purpose is to give the highest-confidence signal that critical flows (login, checkout, signup) actually work for a real user. It remains essential in 2026 because no amount of unit testing proves the assembled app works in a real browser — but the guidance is still \"a few full journeys, not hundreds,\" because E2E tests are the slowest and flakiest tier.</p>","worked":"<p>A Playwright E2E test of a login journey:</p><pre><code>import { test, expect } from '@playwright/test'\n\ntest('user can log in', async ({ page }) =&gt; {\n  await page.goto('https://app.example.com/login')\n  await page.getByLabel('Email').fill('ada@example.com')\n  await page.getByLabel('Password').fill('hunter2')\n  await page.getByRole('button', { name: 'Sign in' }).click()\n  await expect(page.getByText('Welcome, Ada')).toBeVisible()\n})</code></pre><p>This drives a real browser through the entire flow — exactly what a user would do.</p>","teach":"<p>At its core, an E2E test is just a <strong>robot pretending to be a user</strong>. It opens a real browser, goes to your site, clicks the buttons a human would click, types into the fields a human would type into, and then checks that the screen shows what it should. Nothing is mocked or faked — the test goes through the whole machine, end to end.</p><div class=\"analogy\">Think of testing a car. A <em>unit test</em> checks that one spark plug fires. An <em>integration test</em> checks that the engine and transmission work together on a bench. An <em>E2E test</em> is a test driver who gets in, turns the key, drives the car around the track, and confirms it actually goes. It's the most realistic check — and also the slowest and most expensive to run.</div><p>This is why E2E lives at the <strong>top of the testing pyramid</strong>: the tip is small on purpose. You might have thousands of fast unit tests, dozens of integration tests, and only a handful of E2E journeys — the few flows that would be catastrophic if broken (can a user sign up? can they pay?).</p><p>E2E tests are powerful because they catch problems no other test can: a broken route, a misconfigured API, a button that's visually covered by another element. But they're also <strong>flaky</strong> — a slow network or a one-second animation can make them fail randomly — and slow, because each one spins up a full browser.</p><div class=\"key\">The takeaway: <strong>E2E gives the most realistic confidence but costs the most to maintain</strong>. Write a few to cover your most critical user journeys, and push everything else down to faster, cheaper unit and integration tests.</div>","related":["swr","cra","cdp"]},
  "cdp": {"term":"CDP","group":"FE-TEST","aliases":["Chrome DevTools Protocol"],"eli5":"A low-level protocol that lets tools control and inspect a Chromium browser remotely — the engine behind modern browser automation.","memory":"CDP = Chrome DevTools Protocol: the wire that DevTools (and Playwright) use to puppet the browser.","full":"<p><strong>CDP</strong> (Chrome DevTools Protocol) is the JSON-over-WebSocket protocol that Chromium-based browsers expose to let external tools instrument, inspect, and control them. It's the same machinery Chrome's own DevTools panel uses to set breakpoints, read the DOM, capture network traffic, and emulate devices — but it's available to any program that speaks the protocol.</p><p>CDP was developed by the <strong>Chromium / Google Chrome team</strong> and grew alongside Chrome DevTools (the protocol was opened up to external automation in the mid-2010s). It became the foundation for Puppeteer (Google's Node automation library) and is one of the transports used by <strong>Playwright</strong> (Microsoft). Firefox and WebKit have partial or alternate support, which is why cross-browser tools layer their own protocol on top.</p><p>Its purpose is to give programmatic, out-of-process control of the browser: commands (\"navigate here,\" \"click,\" \"take a screenshot\") and events (\"a request was made,\" \"console logged this\"). Because tools drive the browser <em>out-of-process via CDP</em> rather than injecting scripts into the page (the older Selenium model), capabilities like multiple tabs, multiple origins, and parallel isolated browser contexts become first-class. It remains the backbone of modern browser tooling in 2026.</p>","worked":"<p>Connecting directly to a browser over CDP with Puppeteer:</p><pre><code># Launch Chrome with remote debugging exposed (a raw CDP endpoint):\nchrome --remote-debugging-port=9222\n\n// Then a tool speaks CDP to it:\nconst browser = await puppeteer.connect({\n  browserURL: 'http://localhost:9222'\n})\nconst page = await browser.newPage()\nawait page.goto('https://example.com')   // CDP: Page.navigate\nawait page.screenshot({ path: 'shot.png' }) // CDP: Page.captureScreenshot</code></pre><p>Each high-level call maps to CDP commands (e.g. <code>Page.navigate</code>, <code>Network.enable</code>) sent over a WebSocket.</p>","teach":"<p>At its core, CDP is just a <strong>remote-control language for the browser</strong>. The browser opens a special socket and agrees to accept commands like \"go to this URL,\" \"click at these coordinates,\" \"give me the DOM tree,\" and to emit events like \"a network request just fired.\" Any program that can speak this JSON protocol can puppet the browser from the outside.</p><div class=\"analogy\">Think of a TV with a hidden service port. The remote in your hand is one client (that's Chrome's own DevTools panel). But the same port lets a technician plug in a diagnostic console and run the TV from a laptop — change channels, read the internal temperature, capture the screen. CDP is that service port for Chromium: DevTools uses it, and so can your automation scripts.</div><p>Why does \"out-of-process\" matter? The old approach (Selenium/WebDriver) often injected a JavaScript agent <em>into the page</em> to do its work — which the page's own code could interfere with, and which made juggling multiple tabs or origins awkward. CDP instead controls the browser from <em>outside</em> the page, at the browser-process level. That's why Playwright can open multiple tabs, span multiple origins, and run several isolated <strong>browser contexts</strong> in parallel cleanly — each context is like a fresh, cookie-isolated incognito session.</p><p>The protocol is organized into <em>domains</em> — <code>Page</code>, <code>Network</code>, <code>DOM</code>, <code>Runtime</code> — each with commands and events. High-level libraries (Puppeteer, Playwright) wrap these so you write <code>page.click()</code> instead of hand-crafting a <code>DOM.querySelector</code> + <code>Input.dispatchMouseEvent</code> sequence.</p><div class=\"key\">The takeaway: <strong>CDP is the low-level engine under modern browser automation</strong> — driving the browser out-of-process is what makes multi-tab, multi-origin, and parallel testing first-class instead of a hack.</div>","related":["e2e"]}
});
