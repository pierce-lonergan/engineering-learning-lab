/* Open Table Formats (LAKE-FORMATS) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"LAKE-FORMATS","front":"What problem does a 'table format' solve that a directory of Parquet files does not?","back":"A directory-of-Parquet 'data lake' has no notion of a table: writers can't commit atomically (a partial write or concurrent overwrite leaves readers seeing torn/duplicated data), there's no schema enforcement, and discovering files requires an expensive recursive S3 <code>LIST</code>. An open table format adds a <strong>metadata layer</strong> that tracks exactly which immutable data files constitute the table at each version, giving you atomic commits (ACID), schema enforcement/evolution, and time travel over the same cheap object storage."},
  {"sec":"LAKE-FORMATS","front":"What is the core architectural pattern shared by Iceberg, Delta Lake, and Hudi?","back":"All three are <strong>metadata pointing to immutable data files</strong>. The actual rows live in immutable Parquet (or ORC/Avro) files that are never edited in place; a separate, versioned metadata structure records the set of files that make up each table snapshot. A commit produces a new metadata version pointing to a new file set, so readers always see a consistent point-in-time view (snapshot isolation) and you get time travel for free."},
  {"sec":"LAKE-FORMATS","front":"Why is snapshot-based commit the mechanism that gives lakehouse tables ACID on S3?","back":"Because data files are immutable, a write never mutates existing state — it writes new data files, then makes one <strong>atomic metadata swap</strong> to a new snapshot that references them. Readers pin a snapshot, so they never observe a half-finished write (isolation + atomicity). On object stores lacking multi-file transactions, the format reduces the whole commit to a single atomic operation (a catalog compare-and-swap, or in Delta an atomic creation of the next log file), which is the only primitive the store needs to provide."},
  {"sec":"LAKE-FORMATS","front":"How does Iceberg's metadata tree resolve a query without an S3 LIST?","back":"Iceberg keeps an explicit hierarchy: a <strong>metadata.json</strong> root points to a <strong>manifest list</strong> (one per snapshot), which points to <strong>manifest files</strong>, which list the actual data files plus per-file column min/max stats, row counts, and partition values. The engine reads metadata to get the exact file paths and prunes files via stats — it never lists directories, eliminating slow S3 <code>LIST</code> and making planning scale with metadata size, not raw file count."},
  {"sec":"LAKE-FORMATS","front":"What is Iceberg hidden partitioning and what query bug does it eliminate?","back":"Iceberg stores a <strong>partition spec</strong> (e.g. <code>days(ts)</code>, <code>bucket(16, id)</code>) as a transform in metadata, so users query the raw column (<code>WHERE ts &gt;= '2024-01-01'</code>) and the engine derives partition pruning automatically. This eliminates the classic Hive bug where a query on <code>ts</code> scans the whole table because the physical partition column was a separate derived field (<code>dt</code>) the user forgot to filter on. It also lets you evolve the partition spec later without rewriting old data."},
  {"sec":"LAKE-FORMATS","front":"How does Delta Lake's transaction log differ structurally from Iceberg's metadata tree?","back":"Delta uses an ordered <strong>transaction log</strong>: a <code>_delta_log/</code> directory of numbered JSON commit files (<code>00000.json</code>, <code>00001.json</code>, …), each recording <code>add</code>/<code>remove</code> file actions. Current table state is the <em>replay</em> of all commits since the last <strong>checkpoint</strong> (a Parquet snapshot of state). Iceberg instead materializes the full file set per commit via manifest lists — state is read directly, not replayed. Delta is log-of-deltas; Iceberg is tree-of-snapshots."},
  {"sec":"LAKE-FORMATS","front":"What is Hudi optimized for that distinguishes it from Iceberg and Delta?","back":"Hudi was built around <strong>record-level upserts and incremental processing</strong>: every record carries a key, and Hudi maintains an index (e.g. Bloom, bucket, or record-level) mapping keys to files so it can efficiently update/delete individual records and serve <em>incremental queries</em> that return only records changed since a commit. This makes it a natural sink for streaming CDC into the lake, where Iceberg/Delta historically leaned more toward append + batch <code>MERGE</code> workloads."},
  {"sec":"LAKE-FORMATS","front":"Hudi Copy-on-Write vs Merge-on-Read — what's the tradeoff?","back":"<strong>Copy-on-Write (CoW)</strong>: an update rewrites the whole Parquet file containing the record — slower writes, but reads hit clean columnar files (read-optimized). <strong>Merge-on-Read (MoR)</strong>: updates are appended to row-based (Avro) <strong>delta/log files</strong> and merged with base Parquet files at read time (or by async compaction) — fast, low-latency writes ideal for streaming, but reads pay a merge cost until compaction runs. CoW favors read-heavy; MoR favors write/ingest-heavy."},
  {"sec":"LAKE-FORMATS","front":"How do these formats handle row-level deletes/updates without mutating immutable files?","back":"They record deletes separately and apply them at read (<em>merge-on-read</em>). Iceberg v2 supports <em>position deletes</em> (file path + row position) and <em>equality deletes</em> (predicate on values); <strong>deletion vectors</strong> — a compact roaring-bitmap marking deleted row positions in a single data file, stored in Puffin files — are the newer mechanism, used by Delta and by Iceberg <strong>v3</strong> (not v2). The base Parquet stays immutable; readers subtract the marked rows, and later compaction rewrites surviving rows into new files."},
  {"sec":"LAKE-FORMATS","front":"What is time travel and what is the operational cost of relying on it?","back":"Because each commit creates a new snapshot/version pointing to a file set, you can read the table <strong>as of</strong> an old snapshot id or timestamp (<code>VERSION AS OF</code> / <code>TIMESTAMP AS OF</code>) for audits, reproducibility, or rollback. The cost: old data files and manifests can't be deleted while a snapshot references them, so storage grows. You bound it with <strong>retention/expiration</strong> (Iceberg <code>expire_snapshots</code>, Delta <code>VACUUM</code>) — which permanently deletes the unreferenced files and destroys the ability to time-travel past that point."},
  {"sec":"LAKE-FORMATS","front":"Why do lakehouse tables require regular compaction, and what does it fix?","back":"Streaming/frequent commits produce many <strong>small files</strong> and (for MoR) accumulating delete/log files, which inflate metadata, hurt read throughput, and slow planning. <strong>Compaction</strong> (Iceberg <code>rewrite_data_files</code> / <code>rewrite_manifests</code>, Delta <code>OPTIMIZE</code> + optional Z-ORDER, Hudi async compaction/clustering) rewrites many small files into fewer large ones and materializes pending deletes into the base data. It's the lakehouse equivalent of an LSM tree's background merge — necessary table maintenance, not optional."},
  {"sec":"LAKE-FORMATS","front":"What is the catalog's role, and why is it the linchpin of multi-engine ACID?","back":"The catalog maps a table name to the <strong>pointer to its current metadata</strong> (e.g. Iceberg's current <code>metadata.json</code> location). A commit is an atomic <strong>compare-and-swap</strong> on that pointer: \"swap from metadata vN to vN+1 only if it's still vN,\" which is how concurrent writers serialize and how every engine (Spark, Trino, Flink, Snowflake) agrees on what the table currently is. Without a transactional catalog you have no atomic-commit primitive and no single source of truth across engines."},
  {"sec":"LAKE-FORMATS","front":"What does the Iceberg REST catalog spec change versus a Hive Metastore or Glue catalog?","back":"Earlier catalogs (Hive Metastore, AWS Glue) required every engine to embed catalog-specific client logic and often relied on file-system rename or external locking for the commit. The <strong>Iceberg REST catalog</strong> standardizes commits behind an HTTP API, so the server owns the atomic commit, conflict detection, and increasingly credential vending / access control. Any client speaking the REST protocol interoperates, decoupling engines from the metastore implementation — the basis for Snowflake Open Catalog (Apache Polaris), Unity Catalog's Iceberg REST endpoint, and (acquired by Databricks) Tabular."},
  {"sec":"LAKE-FORMATS","front":"How does schema evolution work safely across these formats, and what makes it safe?","back":"They support add/drop/rename/reorder columns and (limited, widening-only) type promotion as <strong>metadata-only</strong> operations — no data rewrite. The key is tracking columns by a stable <strong>field id</strong>, not by name or position, so a rename can't resurrect an old column's data and a reorder can't misalign columns. Iceberg does this natively; Delta requires <strong>column mapping</strong> (<code>id</code>/<code>name</code> mode, off by default) to enable safe rename/drop. Reads project by id, so old files with the old layout still resolve under the new schema."},
  {"sec":"LAKE-FORMATS","front":"Why is concurrent-write conflict resolution harder on a lakehouse than in an OLTP database?","back":"There's no central lock manager holding row locks during a transaction; writers work optimistically against a snapshot and only collide at commit. The format uses <strong>optimistic concurrency control</strong>: at commit it validates that no conflicting change landed since its base snapshot (e.g. another writer touched the same partitions/files), and if so the loser must <strong>retry</strong> against the new snapshot. This works for low-to-moderate write concurrency but degrades under many writers hammering the same partition — the lakehouse is not an OLTP system."},
  {"sec":"LAKE-FORMATS","front":"Iceberg vs Delta vs Hudi — give the one-line positioning of each.","back":"<ul><li><strong>Iceberg</strong>: engine-neutral open spec, hidden partitioning, rich manifest metadata; the de-facto interop standard (Snowflake, Trino, Spark, Flink, BigQuery).</li><li><strong>Delta Lake</strong>: ordered transaction log + checkpoints; deepest Spark/Databricks integration, deletion vectors, Unity Catalog.</li><li><strong>Hudi</strong>: record-level index + upserts + incremental queries; strongest for streaming CDC ingestion and mutable workloads.</li></ul>Convergence is real (Delta UniForm / Apache XTable expose one table as multiple formats), so the practical choice is often driven by catalog/engine ecosystem, not raw features."}
);

Object.assign(GLOSSARY_DATA, {
  "lakehouse": {"term":"Lakehouse","group":"LAKE-FORMATS","aliases":["lakehouse architecture","data lakehouse"],"eli5":"Put warehouse-grade table management (ACID, schema, time travel) on top of cheap object storage so one system serves both BI and data science.","memory":"Lake (cheap storage) + house (warehouse rigor) = lakehouse. The table format is the foundation poured over the lake.","full":"<p>A lakehouse layers an <strong>open table format</strong> over open file formats (Parquet/ORC) sitting in cheap object storage (S3/GCS/ADLS), giving you the transactional guarantees and schema management of a warehouse without copying data into a proprietary system. Storage and compute are fully decoupled: many engines (Spark, Trino, Flink, Snowflake, DuckDB) read and write the same physical tables.</p><p>It replaces the two-tier pattern where a raw 'data lake' (directory of files) fed a separate warehouse via ETL. The pitch is one copy of data, open formats, no lock-in, and warehouse semantics — at object-storage cost.</p>","trap":"Interviewers probe 'so it replaces the warehouse?' — no. Lakehouse gives ACID and schema, but query latency/concurrency/optimizer maturity still lag a tuned MPP warehouse for many BI workloads, and it is emphatically not OLTP. The real win is one open copy that many engines share.","related":["table-format","open-file-format","catalog"]},
  "table-format": {"term":"Open Table Format","group":"LAKE-FORMATS","aliases":["table format","open table formats"],"eli5":"A metadata spec that turns a pile of immutable files in object storage into a real table with atomic commits, schema, and versions.","memory":"It's the 'table' in lakehouse: files are the rows, the format is the bookkeeping that makes them a table.","full":"<p>An open table format (Iceberg, Delta, Hudi) defines a <strong>metadata layer</strong> recording exactly which immutable data files belong to a table at each version, plus the table schema, partitioning, and statistics. A write produces new files and an atomic metadata commit referencing them, delivering ACID snapshot isolation, schema evolution, and time travel over storage that natively offers none of those.</p><p>Crucially the spec is open and engine-neutral: the contract lives in the metadata on disk (plus a catalog), so any compliant engine can read/write without a proprietary server in the path.</p>","trap":"Don't conflate the table format with the file format. Parquet stores columnar bytes; the table format (Iceberg/Delta) tracks <em>which Parquet files</em> form the table and at what version. A common gotcha question: 'Is Iceberg a file format?' — no, it sits above Parquet.","related":["open-file-format","snapshot","lakehouse","catalog"]},
  "open-file-format": {"term":"Open File Format (Parquet)","group":"LAKE-FORMATS","aliases":["parquet","file format","columnar format"],"eli5":"The on-disk columnar encoding of the actual rows — immutable once written.","memory":"Parquet = the bricks (immutable, columnar); the table format = the blueprint saying which bricks are the wall right now.","full":"<p>Parquet is the dominant columnar file format under all three table formats: column-chunked, compressed, with per-row-group min/max statistics and a footer the engine reads to skip irrelevant data. Files are <strong>immutable</strong> — the table format never edits them in place; updates write new files and supersede old ones via metadata.</p><p>The table format leans on Parquet's footer/row-group stats and copies file-level min/max into its own manifests so it can prune files <em>before</em> opening them, then prune row groups within a chosen file.</p>","trap":"Candidates credit Iceberg/Delta with column pruning — that's Parquet's job (row-group stats). The table format's contribution is <em>file</em> pruning via manifest stats so you never open irrelevant files at all. Know which layer does which.","related":["table-format","manifest","hidden-partitioning"]},
  "snapshot": {"term":"Snapshot / Time Travel","group":"LAKE-FORMATS","aliases":["snapshot isolation","time travel","table version"],"eli5":"Each commit freezes a complete, named view of the table's file set, so you can always read a consistent past version.","memory":"Snapshot = a git tag on the file set. Time travel = checking out an old tag. VACUUM/expire = git gc that deletes unreachable blobs.","full":"<p>Because data files are immutable, every commit creates a new <strong>snapshot</strong> (Iceberg snapshot id / Delta version) that references a specific set of files. Readers pin one snapshot for the duration of a query → <strong>snapshot isolation</strong>: no torn reads, no seeing another writer's half-commit. Reading an older snapshot is <strong>time travel</strong> (<code>VERSION AS OF</code> / <code>TIMESTAMP AS OF</code>), used for audits, rollback, and reproducible ML training sets.</p><p>The cost is retained history: files referenced by any live snapshot can't be deleted, so you run <code>expire_snapshots</code> (Iceberg) / <code>VACUUM</code> (Delta) to reclaim space, which also caps how far back you can travel.</p>","trap":"VACUUM/expire_snapshots permanently deletes files — running it with a short retention while someone is mid-query (or relying on rollback) breaks those reads. Also: time travel does NOT bring back data deleted past your retention window.","related":["table-format","occ","compaction"]},
  "hidden-partitioning": {"term":"Hidden Partitioning (Iceberg)","group":"LAKE-FORMATS","aliases":["partition spec","partition transform","partition evolution"],"eli5":"Iceberg derives the partition from the raw column via a stored transform, so queries on the real column get pruned automatically.","memory":"Hidden = the user never sees or types the partition key; the transform is the hidden machinery between query column and physical layout.","full":"<p>Iceberg records partitioning as <strong>transforms</strong> on source columns (<code>days(ts)</code>, <code>hours(ts)</code>, <code>bucket(N, id)</code>, <code>truncate(L, s)</code>) in the partition spec. Users filter the raw column and the engine computes which partitions to scan — no separate derived partition column to remember. The spec can <strong>evolve</strong>: change from daily to hourly partitioning and old data keeps its old layout, queried correctly under the new spec.</p><p>This fixes the Hive anti-pattern where the physical partition (<code>dt=2024-01-01</code>) was decoupled from the data column, so a filter on the timestamp column scanned everything.</p>","trap":"Classic Hive gotcha: <code>WHERE event_time = '...'</code> ignores the <code>dt</code> partition and full-scans. Iceberg's hidden partitioning eliminates this — but only Iceberg. Delta partitions on a literal column (like Hive) unless you use generated columns; both now favor clustering (Delta liquid clustering) for layout.","related":["table-format","manifest","compaction"]},
  "transaction-log": {"term":"Delta Transaction Log","group":"LAKE-FORMATS","aliases":["delta log","_delta_log","DeltaLog","checkpoint"],"eli5":"Delta's source of truth is an ordered log of add/remove-file actions you replay to get current state, periodically compacted into checkpoints.","memory":"It's a write-ahead log for a table: append commits, replay to rebuild, checkpoint to truncate. WAL semantics you already know, applied to a table on S3.","full":"<p>Delta Lake stores state in <code>_delta_log/</code>: numbered JSON commit files, each an atomic transaction listing <code>add</code> and <code>remove</code> file actions plus metadata/schema changes. Current table state = replay of all commits since the last <strong>checkpoint</strong> (a Parquet snapshot of state written every N commits to bound replay cost). The atomic commit primitive is creating commit file <code>N+1.json</code> exactly once.</p><p>Contrast Iceberg, which materializes the full file set per snapshot in manifest lists (read directly, no replay). Delta = log of deltas; Iceberg = tree of snapshots.</p>","trap":"The log gives ordering, but multi-writer atomicity needs a way to create commit file N+1 exactly once. S3 historically had no put-if-absent, so Delta used the <strong>DynamoDB LogStore</strong> for mutual exclusion; only since S3 added conditional writes (Aug 2024) can the put-if-absent be native. Without one of those, naive overwrite 'commits' can lose writes. Senior answer: ordering from the log + an atomic create-file/CAS from the catalog or store.","related":["snapshot","catalog","occ","deletion-vector"]},
  "manifest": {"term":"Iceberg Manifest / Manifest List","group":"LAKE-FORMATS","aliases":["manifest file","manifest list","metadata.json"],"eli5":"A layered index — metadata.json → manifest list → manifests → data files — that lets the engine plan a query from metadata alone, never listing S3.","memory":"Three-level pyramid: metadata.json (the table), manifest list (this snapshot's manifests), manifests (the files). Skip downward at each level using the stats.","full":"<p>Iceberg's metadata is a tree: the root <code>metadata.json</code> tracks schema/spec history and current snapshot; each snapshot has a <strong>manifest list</strong> (one entry per manifest, with partition-range summaries for fast skip); each <strong>manifest</strong> lists data files with column-level min/max, null counts, row counts, and partition values. Planning reads this tree and prunes files via stats — directory <code>LIST</code> never happens.</p><p>This makes query planning scale with metadata size, not raw file count, and is why Iceberg handles millions of files where Hive-style directory listing collapses.</p>","trap":"Many small commits bloat the manifest layer and slow planning even if data files are fine — that's why <code>rewrite_manifests</code> exists alongside <code>rewrite_data_files</code>. Interviewers test whether you know metadata itself needs compaction, not just data.","related":["table-format","hidden-partitioning","open-file-format","compaction"]},
  "catalog": {"term":"Catalog (REST / metastore)","group":"LAKE-FORMATS","aliases":["iceberg catalog","rest catalog","metastore","polaris","unity catalog","glue"],"eli5":"The transactional pointer from a table name to its current metadata; the atomic compare-and-swap on that pointer is what makes commits safe across engines.","memory":"Catalog = the single mutable pointer in an otherwise immutable world. One CAS on that pointer = the entire ACID commit.","full":"<p>The catalog resolves <code>db.table</code> to the location of the current metadata and provides the <strong>atomic commit</strong>: a compare-and-swap that advances the pointer from version N to N+1 only if it's still N, serializing concurrent writers and giving every engine one agreed-upon current table. Implementations: Hive Metastore, AWS Glue, JDBC, Nessie, Unity Catalog, and the standardized <strong>Iceberg REST catalog</strong>.</p><p>The REST spec moves commit logic server-side (conflict detection, credential vending, governance) so any client speaking HTTP interoperates — the basis for Snowflake Open Catalog (Apache Polaris), Databricks Unity Catalog's Iceberg REST endpoint, and Tabular. It decouples engines from the metastore.</p>","trap":"Without a transactional catalog, two writers can both think they committed (lost update), especially on eventually-consistent stores. 'Iceberg gives ACID' is incomplete — ACID across writers requires a catalog that does atomic CAS. Naming the REST catalog and CAS is the staff-level answer.","related":["table-format","occ","transaction-log","snapshot"]},
  "occ": {"term":"Optimistic Concurrency Control (lakehouse)","group":"LAKE-FORMATS","aliases":["optimistic concurrency","commit conflict","write conflict","retry"],"eli5":"Writers work against a snapshot and only check for conflicts at commit time; the loser retries instead of holding locks.","memory":"Same OCC you know from MVCC databases — read snapshot, validate-at-commit, retry on conflict — but the 'lock' is a catalog CAS on one pointer.","full":"<p>Lakehouse tables have no central row-lock manager. Each writer reads a base snapshot, stages new files, and at commit the catalog validates no conflicting change landed since the base (e.g. another writer modified the same files/partitions, or a delete invalidated read files). On conflict the commit fails and the writer <strong>retries</strong> against the new current snapshot. This is optimistic concurrency: cheap when writers rarely collide.</p><p>It scales fine for append-heavy and partition-disjoint workloads but degrades when many writers contend on the same partition — escalating retries. The cure is partitioning writes by writer, fewer/larger commits, or serialization upstream, not the format.</p>","trap":"Pitched as a streaming sink with dozens of concurrent writers into one hot partition? OCC turns into a retry storm. Senior answer: lakehouse commit is serialized per table at the pointer, so high single-table write concurrency is its weak spot — design to avoid hot-partition contention.","related":["catalog","snapshot","transaction-log","hudi-upsert"]},
  "deletion-vector": {"term":"Deletion Vectors / Merge-on-Read Deletes","group":"LAKE-FORMATS","aliases":["deletion vector","delete files","position delete","equality delete","merge-on-read"],"eli5":"Mark deleted rows in a side file (a bitmap or predicate) instead of rewriting the immutable data file, then merge at read time.","memory":"Tombstones, not edits: the row still sits in the immutable file, but a bitmap says 'skip me.' Compaction is the cleanup that finally removes the body.","full":"<p>Since data files are immutable, row-level deletes/updates are recorded separately and applied at read. Iceberg v2 has <strong>position deletes</strong> (file + row offset) and <strong>equality deletes</strong> (a predicate on column values); <strong>deletion vectors</strong> — a compact roaring-bitmap of deleted row positions per data file — are used by Delta and by Iceberg <strong>v3</strong> (stored in Puffin files), not v2. Readers subtract marked rows; this is <em>merge-on-read</em>. A later <strong>compaction</strong> rewrites surviving rows into clean files and drops the delete artifacts.</p><p>The tradeoff is the classic one: cheap writes, read-time merge cost that grows until compaction reconciles it.</p>","trap":"Equality deletes are powerful but expensive at read (predicate applied across many files) and can pile up under streaming CDC — unbounded delete files silently tank read latency until compaction. Know position vs equality deletes, and that deletion vectors (Iceberg v3 / Delta) are the newer, cheaper-to-apply mechanism — they don't exist in Iceberg v2.","related":["hudi-upsert","compaction","snapshot","open-file-format"]},
  "hudi-upsert": {"term":"Hudi Upsert / Incremental (CoW vs MoR)","group":"LAKE-FORMATS","aliases":["hudi","copy-on-write","merge-on-read","incremental query","record index"],"eli5":"Hudi keys every record and indexes it to a file, enabling efficient upserts/deletes and queries that return only what changed since a commit.","memory":"Hudi = the upsert-first format: key + index + MoR log files = an LSM-flavored table. CoW=read-optimized, MoR=write-optimized.","full":"<p>Hudi centers on <strong>record-level keys + an index</strong> (Bloom, bucket, or record-level index) mapping each key to its file, so it can locate and update/delete individual records — the foundation for ingesting CDC into the lake. <strong>Copy-on-Write</strong> rewrites the whole Parquet file on update (read-optimized, slower writes); <strong>Merge-on-Read</strong> appends changes to row-based (Avro) log files merged at read or by async compaction (write-optimized, low-latency).</p><p><strong>Incremental queries</strong> return only records changed after a given commit instant — pull-based downstream propagation without re-scanning the table, which is Hudi's signature capability versus append-oriented Iceberg/Delta.</p>","trap":"Don't say 'Iceberg/Delta can't upsert' — they do via <code>MERGE</code>. The distinction is Hudi's per-record <em>index</em> makes upserts and <em>incremental queries</em> first-class and efficient at high mutation rates, where MERGE on the others can be heavier. Also CoW≈read-optimized, MoR≈write-optimized — don't flip them.","related":["deletion-vector","compaction","table-format","occ"]}
});
