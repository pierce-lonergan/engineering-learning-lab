/* Trade-Ops System Design Scenarios (IP-SCENARIOS) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"IP-SCENARIOS","front":"Design a trade reconciliation system: internal records vs an external \"truth\" source. Walk through the end-to-end architecture.","back":"<strong>Goal:</strong> match <strong>internal trade records</strong> against an <strong>external truth</strong> (clearing/custodian SOD file, exchange/processor settlement file, on-chain data) and surface only true discrepancies as <strong>breaks</strong>.<ul><li><strong>1. Ingest</strong> both sides idempotently into raw/staging tables (file or API). Record source, batch id, ingest timestamp; never mutate raw.</li><li><strong>2. Normalize</strong> each side to a <strong>canonical schema</strong> (e.g. <code>trade_id, symbol, side, qty, price, currency, settle_date, account</code>). Map venue-specific symbology/IDs here.</li><li><strong>3. Match</strong> on <strong>stable keys</strong> (<code>trade_id</code> first; fall back to composite <code>symbol+qty+settle_date+account</code>) with <strong>tolerances</strong> for expected diffs (fees, rounding, timing).</li><li><strong>4. Classify</strong> outcomes: matched, <strong>amount break</strong>, <strong>date break</strong>, <strong>missing-on-internal</strong>, <strong>missing-on-external</strong>, duplicate.</li><li><strong>5. Exception queue + UI:</strong> breaks land in a work queue with status, assignee, age; analysts resolve via manual or <strong>bulk corrections</strong>.</li><li><strong>6. Alerting & SLA:</strong> alert on aging/high-$ breaks given the <strong>T+1</strong> intraday window.</li><li><strong>7. Idempotent & re-runnable:</strong> re-running the same batch produces the same result (dedupe on natural keys); full <strong>audit trail</strong>.</li></ul><em>Interview line:</em> \"Match on stable identifiers, tolerate expected discrepancies, escalate only the residue.\""},
  {"sec":"IP-SCENARIOS","front":"Why design the reconciliation engine to be idempotent and re-runnable, and how do you actually achieve it?","back":"Files arrive late, get re-sent, or you fix a normalization bug and must replay &mdash; a recon that double-counts on replay is worse than useless.<ul><li><strong>Dedupe on natural keys:</strong> a <code>UNIQUE</code> constraint on <code>(source, trade_id, version)</code> so re-ingesting the same row is a no-op (<code>INSERT ... ON CONFLICT DO NOTHING</code>).</li><li><strong>Deterministic match:</strong> matching is a pure function of (internal snapshot, external snapshot) &mdash; same inputs &rArr; same breaks. No reliance on \"what we saw last time.\"</li><li><strong>Versioned runs:</strong> each run writes results tagged with a <code>run_id</code> + <code>as_of</code>; you can recompute history without clobbering prior runs.</li><li><strong>Separate raw from derived:</strong> raw staging is append-only; the match layer is fully rebuildable from raw.</li></ul><em>This is the SAME primitive as payments/billing reconciliation</em> &mdash; an idempotency key over an external truth source. A senior billing/ledger engineer (e.g. JPMorgan-style Python/Java + Kafka/Snowflake/AWS background) maps ~1:1 onto this."},
  {"sec":"IP-SCENARIOS","front":"Ingest & reconcile exchange fills from a FIX drop-copy. What is a drop-copy and how do you process the feed?","back":"A <strong>FIX drop-copy</strong> is a read-only session that replays <code>35=8</code> <strong>ExecutionReports</strong> (your fills) for trade capture & recon &mdash; used by venues like Coinbase Derivatives and Zero Hash.<ul><li><strong>Session mgmt:</strong> FIX is <code>tag=value</code> with sequence numbers; on a gap send a <strong>ResendRequest</strong> (<code>35=2</code>) to replay missing messages &mdash; never silently skip a seqnum.</li><li><strong>Parse</strong> the ExecutionReport: <code>37</code> OrderID, <code>11</code> ClOrdID, <code>17</code> ExecID, <code>150</code> ExecType, <code>55</code> Symbol, <code>54</code> Side, <code>32</code> LastQty, <code>31</code> LastPx, <code>14</code> CumQty.</li><li><strong>Normalize</strong> to canonical fill rows; dedupe on <code>ExecID</code> (replays will repeat it).</li><li><strong>Match to internal orders</strong> by <strong><code>ClOrdID</code> (client order id)</strong> &mdash; that's why you stamp your own id on every order.</li><li><strong>Update positions</strong> from confirmed fills; <strong>flag mismatches</strong> (fill with no matching order, qty/price disagreement, unknown symbol).</li></ul><em>Interview line:</em> \"Drop-copy is the venue's authoritative trade tape; <code>ClOrdID</code> is my join key, <code>ExecID</code> is my dedupe key.\""},
  {"sec":"IP-SCENARIOS","front":"Crypto and prediction-market venues mostly don't offer FIX. How do you ingest fills over REST + WebSocket reliably?","back":"Most crypto/prediction venues are <strong>REST + WebSocket</strong>, not FIX &mdash; so you build the reliability FIX gives you for free.<ul><li><strong>WebSocket</strong> for the live fill/trade stream; <strong>REST</strong> for backfill/snapshot reconciliation on reconnect.</li><li><strong>Sequence/gap handling:</strong> track the venue's sequence number; on a gap, re-snapshot via REST rather than trusting a partial stream.</li><li><strong>Idempotency:</strong> dedupe on the venue's trade/fill id; use <strong>client order IDs</strong> on submission so you can join fills back to your orders.</li><li><strong>Resilience:</strong> auto-reconnect with <strong>backoff + jitter</strong>, heartbeat/ping, respect <strong>rate limits</strong> (token bucket), and a periodic REST recon to catch anything the socket dropped.</li><li><strong>Truth check:</strong> end-of-period, reconcile streamed fills against the venue's REST \"my trades\" endpoint &mdash; the WS stream is a convenience, the REST history is the truth.</li></ul><em>Trap:</em> treating the WebSocket as the source of truth. It can drop messages on reconnect; always have a REST reconciliation pass."},
  {"sec":"IP-SCENARIOS","front":"Design a position keeper sourced from fills. What's the core model and how do you get intraday position & P&L?","back":"<strong>Event-sourced:</strong> the position is a <em>fold</em> over an append-only stream of fill events &mdash; never a mutable counter you patch.<ul><li><strong>Events:</strong> each confirmed fill is an immutable event (<code>symbol, side, qty, price, fee, ts, exec_id</code>).</li><li><strong>State = fold:</strong> running <code>net_qty</code>, <code>avg_cost</code> (or lot-level), realized & unrealized P&L computed by replaying events in order.</li><li><strong>Intraday P&L:</strong> <code>unrealized = net_qty * (mark - avg_cost)</code>; <code>realized</code> accrues on offsetting fills; mark from live prices.</li><li><strong>Rebuildable:</strong> because state is a pure fold, you can recompute position as-of any timestamp &mdash; invaluable when a correction arrives.</li><li><strong>Reconcile to SOD:</strong> at start of day, your replayed position must equal the clearing/custodian <strong>Start-of-Day</strong> file; the SOD is the anchoring truth, intraday fills layer on top.</li></ul><em>Interview line:</em> \"Event sourcing makes the position auditable and time-travelable &mdash; corrections are just new events, not destructive edits.\""},
  {"sec":"IP-SCENARIOS","front":"How does a position keeper handle trade corrections and cancels &mdash; FIX <code>150=G</code> and <code>150=H</code>?","back":"Corrections are inevitable; the design must absorb them without corrupting history.<ul><li><code>150=F</code> = <strong>Trade</strong> (a normal new fill).</li><li><code>150=G</code> = <strong>Trade Correction</strong> &mdash; the venue amends a prior fill (e.g. wrong qty/price), referencing the original <code>ExecID</code> via <code>ExecRefID</code> (<code>19</code>).</li><li><code>150=H</code> = <strong>Trade Cancel</strong> &mdash; the venue busts a prior fill.</li></ul><strong>Event-sourced handling:</strong><ul><li>Never mutate the original event. Append a <strong>reversal</strong> (negating event referencing the original) and, for a correction, a new corrected event.</li><li>Re-fold the stream &rArr; position and P&L self-correct, and the audit trail shows the full lineage.</li><li>If the bust crosses a settlement boundary, it becomes a <strong>break</strong> against the clearing file and goes to the exception queue.</li></ul><em>Trap:</em> handling a correction by editing the original row in place &mdash; you lose auditability and can't reproduce yesterday's reported position."},
  {"sec":"IP-SCENARIOS","front":"Design the settlement-break resolution workflow under T+1. What's the time pressure and the pipeline?","back":"<strong>T+1</strong> (US equities since <strong>May 28, 2024</strong> &mdash; also corp/muni bonds, UITs, ADRs, ETFs) compresses the post-trade window to ~<strong>2&ndash;5 hours</strong>; the <strong>DTCC affirmation cutoff is 9:00 PM ET on trade date</strong>. Unresolved exceptions become <strong>settlement fails</strong>, so STP is non-negotiable.<ul><li><strong>Detect:</strong> recon flags mismatches vs the matching/affirmation feed (DTCC <strong>ITP</strong>) and vs the custodian file.</li><li><strong>Classify:</strong> economic break (price/qty), SSI/account break, missing affirmation, timing.</li><li><strong>Auto-resolve</strong> the deterministic cases (known tolerance, stale-then-arrived, standing reference-data fix) via rules; only escalate the genuinely ambiguous ones.</li><li><strong>Exception queue:</strong> prioritized by <strong>$ at risk + time-to-cutoff</strong>, with bulk-correct actions and a clear audit of who did what.</li><li><strong>Affirmation gate:</strong> affirmation feeds <strong>NSCC</strong> (the CCP &mdash; netting/novation/guarantee, CNS) and <strong>DTC</strong> (the depository &mdash; book-entry settlement). Miss it and you risk a fail.</li></ul><em>Interview line:</em> \"Automate the deterministic majority so humans spend the 2&ndash;5 hour window only on the genuinely ambiguous breaks.\""},
  {"sec":"IP-SCENARIOS","front":"Design idempotent order/settlement submission so retries never double-submit. What do you store and how do you respond?","back":"Use the <strong>idempotency-key</strong> pattern (popularized by Stripe; presented here as a community/IETF convention, not literal Stripe docs).<ul><li>Client sends an <strong>Idempotency-Key</strong> with each submit. Server stores <code>(caller, idempotency_key) &rarr; request_hash, response, status</code>.</li><li><strong>Same key + same payload</strong> &rArr; return the <em>original</em> stored response (no second submission).</li><li><strong>Same key + different payload</strong> &rArr; <code>409 Conflict</code> (the key was reused for a different request &mdash; a bug).</li><li><strong>In-flight:</strong> record an <em>in-progress</em> status under a lock so a concurrent retry waits/returns the same result rather than firing twice.</li></ul><strong>Why it matters in trade ops:</strong> network timeouts on order entry or settlement instructions are common; without idempotency a retry can place a duplicate order or send a duplicate settlement. On crypto/prediction REST APIs you supply your own <strong>client order id</strong> as the key.<br><em>Interview line:</em> \"Idempotency key + request-hash check = safe retries; reused-key-with-different-body is a 409, which catches caller bugs.\""},
  {"sec":"IP-SCENARIOS","front":"What's a canonical schema and why normalize every source into one before matching?","back":"A <strong>canonical schema</strong> is one internal representation every feed is mapped into before any matching/positioning happens &mdash; equities clearing files, crypto exchange APIs, prediction-market resolutions all become the same shape.<ul><li><strong>Decouples</strong> downstream logic from venue quirks: matching, position keeping, and reporting code never special-case a venue.</li><li><strong>Symbology mapping</strong> lives in one place (venue symbol &harr; internal instrument id), which is exactly where multi-asset firms bleed time.</li><li><strong>Stable keys</strong> become well-defined: <code>trade_id, symbol, side, qty, price, currency, settle_date, account</code>.</li></ul><strong>Example fields differ per asset</strong> but map to the same columns: an equity has a <code>settle_date</code>; a crypto fill has a chain/tx and a 24/7 timestamp; a prediction contract has an <code>event_id</code> + <code>resolution_date</code>. Normalize at the edge, reconcile in the middle.<br><em>Trap:</em> matching raw venue records directly &mdash; you end up with N&sup2; special cases and brittle joins."},
  {"sec":"IP-SCENARIOS","front":"How do you reconcile crypto post-trade with no central depository &mdash; multiple chains, on-chain settlement, 24/7?","back":"Crypto has <strong>no central depository (no DTC analog)</strong>, so \"truth\" is fragmented.<ul><li><strong>Truth sources:</strong> exchange REST trade history + <strong>on-chain data</strong> (the chain is the settlement ledger). Reconcile internal records against both.</li><li><strong>Finality vs confirmation:</strong> an on-chain transfer isn't done until enough confirmations; handle <strong>reorgs</strong> (a settled tx can be orphaned) &mdash; treat \"pending\" and \"final\" as distinct states.</li><li><strong>24/7:</strong> no SOD batch boundary &mdash; recon runs continuously, not once a day.</li><li><strong>Recon challenges:</strong> multiple chains, gas/fees, address management (hot vs cold custody), differing decimals.</li><li><strong>2026 cash leg:</strong> stablecoins / tokenized deposits act as the on-chain <strong>cash leg</strong>; under the <strong>GENIUS Act</strong> stablecoin framework (OCC implementation effective Mar 2, 2026) redemption of a payment stablecoin must occur <strong>no later than 2 business days</strong>.</li></ul><em>Interview line:</em> \"On-chain data is the depository &mdash; but finality is probabilistic, so I model confirmation state and reconcile pending vs final separately.\""},
  {"sec":"IP-SCENARIOS","front":"Prediction markets settle to event resolution, not a price. What does that change in your post-trade design?","back":"Prediction contracts are <strong>binary event contracts priced $0&ndash;$1</strong> (= implied probability) that settle to <strong>$1 if the event occurs, else $0</strong>.<ul><li><strong>Settlement trigger is resolution, not a clearing price:</strong> your settlement automation keys off an <strong>oracle/resolution event</strong> for an <code>event_id</code>, not a numeric mark.</li><li><strong>Resolution risk:</strong> disputed/ambiguous outcomes, delayed resolution, oracle errors &mdash; design for a <strong>disputed/pending-resolution</strong> state before booking final P&L.</li><li><strong>Multi-platform:</strong> the same event may trade on <strong>Kalshi</strong> (CFTC DCM, 2020) and <strong>Polymarket</strong> (crypto-native; became a CFTC-regulated US exchange via Amended Order Nov 25, 2025) &mdash; reconcile/arb across venues by mapping to a shared event identity.</li><li><strong>Surveillance context:</strong> the CFTC <strong>Prediction Markets Advisory (Feb 25, 2026)</strong> flags insider trading, wash/pre-arranged trades, spoofing, manipulation &mdash; your data may feed those checks.</li></ul><em>Interview line:</em> \"I treat resolution as an external truth event with its own pending/disputed/final lifecycle &mdash; settlement automation subscribes to it.\""},
  {"sec":"IP-SCENARIOS","front":"In reconciliation, what discrepancies do you tolerate vs treat as a break, and how do tolerances work?","back":"The art of recon is <strong>not</strong> flagging everything &mdash; you'd drown analysts in noise.<ul><li><strong>Match on stable identifiers:</strong> trade id, amount, currency, symbol, settlement date.</li><li><strong>Tolerate expected discrepancies:</strong><ul><li><strong>Fees/commissions:</strong> external may be gross, internal net &mdash; reconcile within a fee tolerance.</li><li><strong>Rounding:</strong> price/qty precision differs by venue.</li><li><strong>Timing:</strong> a fill present internally but not yet in the external file (or vice versa) is a <em>timing</em> difference, not a true break &mdash; age it before escalating.</li><li><strong>Partial fills:</strong> one order &rArr; many fills; aggregate before comparing.</li></ul></li><li><strong>Break</strong> = anything outside tolerance or unmatched after the timing window: amount break, missing on one side, duplicate, wrong settle date.</li></ul><em>Trap:</em> zero-tolerance exact matching. It produces thousands of false breaks from fees/rounding and trains analysts to ignore the queue."},
  {"sec":"IP-SCENARIOS","front":"Walk a SQL-based matching pass: how would you find breaks between an internal and external trade table?","back":"Match on the stable key, then bucket the residue. Sketch:<br><code>-- amount/date breaks among matched ids</code><br><code>SELECT i.trade_id, i.amount AS int_amt, e.amount AS ext_amt</code><br><code>FROM internal i JOIN external e USING (trade_id)</code><br><code>WHERE ABS(i.amount - e.amount) &gt; :tol_amt</code><br><code>&nbsp;&nbsp;&nbsp;OR i.settle_date &lt;&gt; e.settle_date;</code><br><br><code>-- missing on external (in internal, unmatched)</code><br><code>SELECT i.* FROM internal i</code><br><code>LEFT JOIN external e USING (trade_id)</code><br><code>WHERE e.trade_id IS NULL;</code><br><br><strong>Design notes:</strong><ul><li>Prefer a <strong>FULL OUTER JOIN</strong> on the canonical key to get matched + both-sided misses in one pass (<code>WHERE i.trade_id IS NULL OR e.trade_id IS NULL</code> isolates misses).</li><li>Apply tolerances in the predicate (<code>ABS(diff) &gt; :tol</code>), not after.</li><li>Aggregate partial fills (<code>GROUP BY order_id</code>) before joining.</li><li>Write break rows with a <code>break_type</code> and <code>run_id</code> so the run is auditable & idempotent.</li></ul>"},
  {"sec":"IP-SCENARIOS","front":"Sketch the idempotent-submit handler in Python. What's the minimal correct logic?","back":"<code>def submit(caller, key, payload, db, venue):</code><br><code>&nbsp;&nbsp;h = sha256(canonical(payload))</code><br><code>&nbsp;&nbsp;row = db.get(caller, key)&nbsp;&nbsp;# (caller, key) PK</code><br><code>&nbsp;&nbsp;if row:</code><br><code>&nbsp;&nbsp;&nbsp;&nbsp;if row.request_hash != h:</code><br><code>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;raise Conflict(409)&nbsp;&nbsp;# key reused, different body</code><br><code>&nbsp;&nbsp;&nbsp;&nbsp;return row.response&nbsp;&nbsp;# replay original</code><br><code>&nbsp;&nbsp;db.insert(caller, key, h, status=\"in_progress\")&nbsp;&nbsp;# UNIQUE PK guards race</code><br><code>&nbsp;&nbsp;resp = venue.place(payload, client_order_id=key)</code><br><code>&nbsp;&nbsp;db.update(caller, key, response=resp, status=\"done\")</code><br><code>&nbsp;&nbsp;return resp</code><br><br><strong>Key points:</strong><ul><li><code>(caller, key)</code> is the primary key; the <code>UNIQUE</code> insert is what makes a concurrent retry safe (loser catches the conflict and re-reads).</li><li><strong>Hash the payload</strong> so a reused key with a different body is a <code>409</code>, not a silent wrong action.</li><li>Pass <code>key</code> as the venue's <strong>client_order_id</strong> so the venue also dedupes.</li></ul><em>Trap:</em> storing only the key, not the request hash &mdash; you'd happily replay a stale response for a different order."},
  {"sec":"IP-SCENARIOS","front":"Why is a payments/billing/ledger engineer (Python/Java, Kafka/Snowflake/AWS) a strong fit for post-trade automation? How do you say it in the interview?","back":"The primitives are <strong>identical</strong> &mdash; only the vocabulary changes.<ul><li><strong>Idempotency keys:</strong> safe-retry of a charge &equiv; safe-retry of an order/settlement instruction.</li><li><strong>Reconciliation vs an external truth:</strong> processor settlement file &equiv; clearing/custodian SOD file &equiv; on-chain data. Match on stable ids, tolerate fees/rounding/timing, escalate breaks.</li><li><strong>Exception handling:</strong> payment disputes/chargebacks &equiv; settlement breaks &mdash; a queue, classification, bulk resolution, audit.</li><li><strong>Ledger:</strong> double-entry/event-sourced ledger &equiv; event-sourced position keeper.</li></ul><em>Interview line:</em> \"Post-trade reconciliation and exception management are the same engineering problem I solved in payments: an idempotent pipeline reconciling internal state against an external source of truth, with a break queue and a full audit trail. The asset class is new; the primitives aren't.\""},
  {"sec":"IP-SCENARIOS","front":"What failure modes should every trade-ops pipeline design explicitly handle? Give the checklist.","back":"Name these unprompted &mdash; it signals production maturity.<ul><li><strong>Duplicate / replayed messages:</strong> dedupe on <code>ExecID</code>/venue trade id; idempotent ingest.</li><li><strong>Out-of-order / gapped feeds:</strong> FIX seqnum gaps &rArr; <code>ResendRequest</code>; WS gaps &rArr; REST re-snapshot.</li><li><strong>Late files:</strong> the external truth arrives after your run &mdash; re-runnable recon + timing tolerance before escalating.</li><li><strong>Corrections/busts:</strong> <code>150=G</code>/<code>150=H</code> via append-only reversal, not in-place edit.</li><li><strong>Clock/timezone:</strong> normalize timestamps to a single zone (UTC, derive ET) &mdash; a mislabeled tz silently breaks date matching.</li><li><strong>Partial fills & fees:</strong> aggregate and tolerance before declaring breaks.</li><li><strong>Rate limits / disconnects</strong> (crypto/prediction REST+WS): backoff+jitter, heartbeats, token-bucket.</li><li><strong>Reorgs / settlement finality</strong> (crypto): pending vs final states.</li><li><strong>Cutoff risk</strong> (T+1, 9:00 PM ET affirmation): SLA alerting prioritized by $-at-risk &times; time-to-cutoff.</li></ul>"}
);

Object.assign(GLOSSARY_DATA, {
  "trade-reconciliation": {"term":"Trade Reconciliation","group":"IP-SCENARIOS","eli5":"Checking your own record of every trade against someone else's authoritative record and chasing down anything that doesn't match.","memory":"Two ledgers, one truth: match, tolerate, escalate.","full":"<p><strong>Reconciliation</strong> compares <strong>internal records</strong> against an <strong>external source of truth</strong> &mdash; a clearing/custodian Start-of-Day file, an exchange/processor settlement file, or on-chain data. You <strong>match on stable identifiers</strong> (trade id, amount, currency, symbol, settlement date), <strong>tolerate expected discrepancies</strong> (timing, partial fills, fees, rounding), and treat unmatched rows as <strong>breaks</strong>.</p><p>Breaks flow into an exception queue and are resolved via manual or bulk corrections. A good engine is <strong>idempotent and re-runnable</strong> (replaying the same inputs yields the same breaks) and keeps a full audit trail.</p>","trap":"Zero-tolerance exact matching floods the queue with false breaks from fees/rounding/timing and trains analysts to ignore real ones.","worked":"Internal shows fill $10,000.00; external (gross of a $1.50 fee) shows $10,001.50. With a $2 fee tolerance this matches; with zero tolerance it's a false break.","related":["canonical-schema","break-exception","sod-file","idempotency-key"]},
  "break-exception": {"term":"Break / Exception","group":"IP-SCENARIOS","eli5":"A trade that didn't match between the two records and now needs a human (or a rule) to fix it.","memory":"Unmatched after tolerance = break = work item.","full":"<p>A <strong>break</strong> (or exception) is any record that fails to reconcile after tolerances and timing windows are applied: an <strong>amount break</strong>, <strong>date break</strong>, <strong>missing-on-internal</strong>, <strong>missing-on-external</strong>, or duplicate. Breaks land in an <strong>exception queue</strong> with status, owner, age, and $-at-risk.</p><p>Under <strong>T+1</strong> the intraday window is ~2&ndash;5 hours, so breaks are prioritized by dollar value and time-to-cutoff; unresolved ones can become <strong>settlement fails</strong>. The design goal is to auto-resolve the deterministic majority and escalate only genuinely ambiguous breaks to analysts.</p>","trap":"Treating a timing difference (record present on one side, not yet on the other) as a real break &mdash; age it through the timing window first.","worked":"A custodian file arrives missing a 3:55 PM fill that's in your blotter; it's a timing break that clears when the next file lands, not an economic break.","related":["trade-reconciliation","settlement-fail","t-plus-one","position-keeper"]},
  "canonical-schema": {"term":"Canonical Schema","group":"IP-SCENARIOS","eli5":"One standard internal shape that every messy external feed gets translated into before you do anything with it.","memory":"Normalize at the edge, reconcile in the middle.","full":"<p>A <strong>canonical schema</strong> is the single internal representation that every venue feed &mdash; equities clearing files, crypto exchange APIs, prediction-market resolutions &mdash; is mapped into during normalization. Typical fields: <code>trade_id, symbol, side, qty, price, currency, settle_date, account</code>.</p><p>It decouples downstream logic (matching, position keeping, reporting) from venue-specific quirks and centralizes <strong>symbology mapping</strong> (venue symbol &harr; internal instrument id). Matching, tolerances, and break classification all operate on this one shape.</p>","trap":"Matching raw venue records directly instead of normalizing first &mdash; you get N&sup2; special cases and brittle, venue-specific joins.","worked":"An equity row carries <code>settle_date</code>; a crypto fill carries a chain/tx + 24/7 timestamp; a prediction contract carries <code>event_id</code> + <code>resolution_date</code> &mdash; all map to the same canonical columns.","related":["trade-reconciliation","fix-drop-copy","position-keeper"]},
  "position-keeper": {"term":"Position Keeper","group":"IP-SCENARIOS","eli5":"A system that adds up all your fills in real time to know what you currently hold and your running P&L.","memory":"Position = fold over fill events; corrections are new events.","full":"<p>A <strong>position keeper</strong> derives the running net position and intraday P&L from a stream of confirmed fills. Built <strong>event-sourced</strong>, the position is a <em>fold</em> over an append-only event log, so it's auditable and can be recomputed <strong>as-of</strong> any timestamp.</p><p>It <strong>reconciles to the clearing/custodian Start-of-Day position</strong> and absorbs corrections/busts (<code>150=G</code>/<code>150=H</code>) by appending reversing events rather than mutating history. Unrealized P&L = <code>net_qty &times; (mark &minus; avg_cost)</code>; realized accrues on offsetting fills.</p>","trap":"Patching a mutable position counter in place on a correction &mdash; you lose the ability to reproduce yesterday's reported position and the audit trail.","worked":"A <code>150=H</code> busts a 200-share buy; you append a &minus;200 reversal event referencing the original ExecID and re-fold, and the position self-corrects.","related":["fix-drop-copy","trade-correction","sod-file"]},
  "trade-correction": {"term":"Trade Correction / Cancel (150=G / 150=H)","group":"IP-SCENARIOS","eli5":"When a venue amends or busts a fill it already sent you, and you have to fix your records cleanly.","memory":"F=trade, G=correct, H=cancel - reverse, don't overwrite.","full":"<p>In a FIX ExecutionReport, <code>150</code> ExecType encodes <strong>Trade (150=F)</strong>, <strong>Trade Correction (150=G)</strong>, and <strong>Trade Cancel (150=H)</strong>. A correction amends a prior fill (referencing the original <code>ExecID</code> via <code>ExecRefID</code>, tag <code>19</code>); a cancel busts it.</p><p>In an event-sourced position keeper you handle these by <strong>appending a reversal</strong> (and, for corrections, a new corrected event) and re-folding &mdash; never by editing the original event. If the bust crosses a settlement boundary it becomes a <strong>break</strong> against the clearing file.</p>","trap":"Editing the original fill row in place, which destroys auditability and makes prior reported positions unreproducible.","worked":"Original <code>150=F</code> books 100 @ $50; a <code>150=G</code> corrects price to $50.10; you append a reversal of the $50 fill plus a corrected $50.10 fill and re-fold.","related":["position-keeper","fix-drop-copy","break-exception"]},
  "sod-file": {"term":"Start-of-Day (SOD) File","group":"IP-SCENARIOS","eli5":"The clearing firm or custodian's official record of your positions at the start of the day - your anchor of truth.","memory":"SOD anchors the position; intraday fills layer on top.","full":"<p>The <strong>Start-of-Day (SOD) file</strong> is the clearing/custodian's authoritative snapshot of positions (and often cash/SSIs) at day open. It's the <strong>external truth</strong> a position keeper reconciles against: replayed intraday position must tie back to SOD.</p><p>In a multi-asset shop, SOD-style truth varies by asset &mdash; equities get a daily custodian/clearing file, while crypto (24/7, no central depository) reconciles continuously against exchange REST history and on-chain data instead of a single daily batch.</p>","trap":"Assuming every asset class has a clean daily SOD batch &mdash; crypto is 24/7 with no central depository, so there's no single start-of-day truth file.","worked":"Your event-sourced replay says +500 XYZ; the custodian SOD says +500 XYZ &rArr; tie-out passes. A +480 mismatch opens a position break.","related":["position-keeper","trade-reconciliation","crypto-settlement"]},
  "dtcc-pipeline": {"term":"DTCC: ITP / NSCC / DTC","group":"IP-SCENARIOS","eli5":"The three DTCC pieces that take an affirmed equity trade and turn it into settled, custodied shares.","memory":"ITP matches/affirms -> NSCC nets & guarantees -> DTC settles & holds.","full":"<p>DTCC's post-trade chain: <strong>ITP</strong> handles pre-settlement matching/affirmation (match-to-instruct); <strong>NSCC</strong> is the central counterparty (CCP) doing <strong>multilateral netting, novation, and guarantee</strong> via <strong>Continuous Net Settlement (CNS)</strong>; <strong>DTC</strong> is the central securities depository performing <strong>book-entry settlement and custody</strong>.</p><p><strong>Affirmation gates NSCC/DTC settlement</strong> &mdash; if a trade isn't affirmed by the 9:00 PM ET cutoff under T+1, it may not settle on time. Recon/exception tooling exists largely to get every trade affirmed and matched before that gate.</p>","trap":"Conflating NSCC and DTC &mdash; NSCC is the CCP that nets and guarantees; DTC is the depository that actually settles and custodies.","worked":"Affirmed buys/sells in a name are netted by NSCC's CNS to a single net obligation, which then settles book-entry at DTC.","related":["t-plus-one","settlement-fail","trade-reconciliation"]},
  "websocket-rest-ingest": {"term":"WebSocket + REST Ingestion","group":"IP-SCENARIOS","eli5":"How you reliably pull fills from crypto/prediction venues that use web APIs instead of FIX.","memory":"WS for live, REST for truth/backfill, dedupe + backoff.","full":"<p>Crypto and prediction-market venues are predominantly <strong>REST + WebSocket</strong>, not FIX. The pattern: use the <strong>WebSocket</strong> for the live fill/trade stream and <strong>REST</strong> for snapshots, backfill on reconnect, and end-of-period truth checks. You rebuild the reliability FIX provides: track sequence numbers, dedupe on the venue trade id, and supply <strong>client order ids</strong> for idempotency.</p><p>Resilience requires <strong>auto-reconnect with backoff + jitter</strong>, heartbeats, <strong>rate-limit</strong> handling (token bucket), and a periodic REST reconciliation so nothing the socket dropped goes unnoticed.</p>","trap":"Treating the WebSocket as the source of truth &mdash; it can drop messages on reconnect; the REST trade-history endpoint is the authoritative check.","worked":"After a 5-second socket drop you re-snapshot 'my trades' over REST, diff against what you streamed, and backfill the missed fill before updating positions.","related":["fix-drop-copy","client-order-id","crypto-settlement","idempotency-key"]}
});
