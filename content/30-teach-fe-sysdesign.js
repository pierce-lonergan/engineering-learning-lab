/* Teach me from the ground up: FE-SYSDESIGN (auto-assembled) */
enh({
  "hydration": {"teach":"<p>At its core, hydration is the moment a page that <em>looks</em> alive actually <em>becomes</em> alive. The server hands the browser a finished-looking photograph of your UI (HTML), and hydration is the browser going back over that photograph and re-wiring every button, input, and dropdown so they actually respond to clicks.</p><h4>Why a photograph isn't enough</h4><p>When you do Server-Side Rendering, the server runs your components and produces plain HTML text. That HTML paints fast and looks complete — you see the product page, the price, the Add-to-Cart button. But HTML is just markup. It has no idea what should happen when you click that button. The <code>onClick</code> handler, the component's internal state, the event listeners — none of that survives the trip as text.</p><p>So the framework ships the <strong>same component JavaScript</strong> down to the browser, re-executes the whole component tree, builds its own virtual picture of what the DOM should be, and then walks the real, already-painted DOM and attaches the missing event listeners and state. That walking-and-wiring step is hydration.</p><div class=\"analogy\"><p>Imagine a model home staged for a tour. Every light fixture and faucet is mounted and looks ready (that's the HTML). But none of it is connected to the wiring or plumbing yet. Hydration is the electrician walking through every room connecting each switch to its circuit. Until they finish your room, flipping the switch does nothing — the bulb is there, it's just not wired.</p></div><h4>The hidden tax</h4><p>This is the catch with SSR: you don't get interactivity for free just because the page painted. There's a gap between \"I can see it\" and \"it works.\" During that gap, clicking Add-to-Cart does <strong>nothing</strong> — the listener isn't attached yet. The cost of hydration is roughly the cost of a full client-side render, because the framework is re-running all the same component code. This is why hydration is the classic enemy of <strong>INP</strong> (Interaction to Next Paint): the JS has to download, parse, and execute before the page feels responsive.</p><h4>The trap</h4><p>A common misconception is that \"SSR means no JavaScript is shipped.\" The opposite is usually true — SSR re-ships the same component JS precisely so it can hydrate. You pay for the HTML <em>and</em> the JS.</p><h4>How the industry fights back</h4><ul><li><strong>Selective hydration</strong> with streaming SSR — hydrate the parts the user is interacting with first.</li><li><strong>Partial hydration / islands</strong> — only hydrate the genuinely interactive widgets, leave the rest as static HTML.</li><li><strong>Resumability</strong> (Qwik) — serialize the listener info into the HTML so the client resumes instead of re-executing.</li><li><strong>RSC</strong> (React Server Components) — these components never hydrate at all because they have no client-side counterpart.</li></ul><p>Concretely in Next.js: the server sends product-page HTML, then React calls <code>hydrateRoot()</code>, walks the tree, and wires up the Add-to-Cart <code>onClick</code>.</p><div class=\"key\">Hydration is the bridge between a server-rendered <em>picture</em> and a working <em>app</em> — and that bridge costs roughly a full client render in downloaded, parsed, and executed JS. SSR doesn't eliminate JS; it defers the interactivity bill to hydration.</div>"},
  "rsc": {"teach":"<p>At its core, a React Server Component is a component whose code stays on the server forever and never gets sent to your browser. The browser receives the <em>result</em> of running it, but never the recipe.</p><h4>What actually crosses the wire</h4><p>A normal React component ships its JavaScript to the browser so the browser can run it. An RSC is different: it runs <strong>only</strong> on the server, and what it produces is serialized into something called the <strong>RSC payload</strong> — a compact description of the rendered tree (which elements, which props, where the interactive boundaries are). That payload is not HTML, and it's not hydrated JavaScript. The component's own code is excluded from the client bundle entirely.</p><p>Because it runs on the server, an RSC can <code>await</code> a database query or read the filesystem <em>directly inside the component</em>:</p><p><code>async function Page(){ const posts = await db.posts.find(); return &lt;List posts={posts}/&gt; }</code></p><p>Here <code>db</code> lives only on the server. It never appears in the browser bundle, so your database credentials and query logic stay secret, and there's no client-side fetch waterfall — the data is already there when the tree is built.</p><div class=\"analogy\"><p>Think of a restaurant kitchen. SSR is like the kitchen sending out a fully plated dish <em>and</em> a copy of the recipe card, in case you want to re-cook it at your table (that re-cooking is hydration). RSC is the kitchen that sends only the plated dish — the recipe, the raw ingredients, the secret sauce all stay in the back. You eat the result; you never see how it was made and you never need to.</p></div><h4>The crucial distinction from SSR</h4><p>This is the part everyone confuses. <strong>SSR renders components to HTML and then hydrates those same components</strong> in the browser. RSC components have <em>no client counterpart at all</em> — they are never hydrated, because their code never reaches the client. They are not two ways of doing the same thing; they are <strong>complementary</strong>. You can server-render the HTML (SSR) for fast first paint <em>and</em> have most of your tree be Server Components that never hydrate.</p><h4>The boundary</h4><p>Interactivity — state with <code>useState</code>, effects, event handlers — can only live in <strong>Client Components</strong>. You mark that boundary with the <code>'use client'</code> directive. Everything above it can be a Server Component; below it ships and hydrates as normal. So the directive is really saying \"from here down, send the JS to the browser.\"</p><p>RSC became stable in React 19 (December 2024) and is consumed in practice through frameworks like the Next.js App Router.</p><div class=\"key\">RSC and SSR are not alternatives. SSR makes HTML and hydrates it; RSC components run only on the server, ship a tree-description payload instead of code, and are never hydrated — slashing client bundle size and keeping data access and secrets server-side.</div>"},
  "ssr": {"teach":"<p>At its core, Server-Side Rendering means the server does the work of drawing your page into finished HTML <em>before</em> sending it, so you see real content immediately instead of staring at a blank screen waiting for JavaScript.</p><h4>What happens per request</h4><p>With SSR, every time a browser asks for a page, the server runs your component tree and produces complete HTML for <em>that specific request</em>. That HTML includes the actual data — the user's name, today's numbers, the personalized dashboard. The browser paints it right away (great First Contentful Paint), and search engines and link-preview bots can read the content directly (great SEO), because it's all there in the markup.</p><p>Then the server ships the JavaScript so the browser can <strong>hydrate</strong> that HTML into a fully interactive app. So SSR is a two-step story: server makes HTML, client makes it interactive.</p><div class=\"analogy\"><p>Imagine ordering a piece of furniture. CSR is like getting a flat box of parts you assemble at home — the truck arrives fast but you can't use anything until you build it. SSR is like the store assembling the furniture and delivering it standing up and usable, though the truck takes a bit longer to load. You can already sit on the chair the moment it arrives; a worker then comes by to tighten the last bolts (that's hydration) so it's fully sturdy.</p></div><h4>Where it shines</h4><p>SSR is the right tool for pages that are <strong>personalized, dynamic, and SEO-critical</strong> — content that changes per user or per request and must still be crawlable and paint fast. A logged-in dashboard is the canonical case.</p><h4>The costs you must respect</h4><ul><li><strong>TTFB (Time To First Byte)</strong> can suffer: the server must finish rendering and fetching data before it can send a single byte. Static files, by contrast, are instant.</li><li><strong>Server scaling</strong>: every request does real CPU work, so traffic spikes hit your servers, not just a CDN.</li><li><strong>You still pay hydration</strong>: the interactivity JS is shipped and executed exactly as in a client app.</li></ul><p>The trap: assuming SSR is \"always faster.\" It improves FCP and SEO, but it can actually <em>worsen</em> TTFB compared to static generation, and it does nothing to remove the hydration JS bill.</p><h4>Softening the costs: streaming</h4><p>Modern SSR uses <em>streaming</em> with Suspense via <code>renderToPipeableStream</code>. Instead of waiting for the whole page, the server flushes the HTML shell immediately, then streams in Suspense-deferred widgets as their data resolves. This also enables <strong>selective hydration</strong>, so the parts the user touches first become interactive first.</p><p>Concretely: a logged-in dashboard renders user-specific HTML on each request via <code>renderToPipeableStream</code>, streaming the shell first and the heavier Suspense-wrapped widgets after.</p><div class=\"key\">SSR buys you fast first paint and crawlable, personalized markup at the price of per-request server work (and a possibly higher TTFB) — and it never removes the hydration cost. Reach for it when content is dynamic and SEO matters; use streaming to keep TTFB honest.</div>"},
  "csr-spa": {"teach":"<p>At its core, Client-Side Rendering means the server sends an almost-empty page and lets your browser's JavaScript draw the entire UI from scratch.</p><h4>What the browser receives</h4><p>With CSR, the server hands over a tiny HTML shell — often just <code>&lt;div id=root&gt;&lt;/div&gt;</code> — plus a JavaScript bundle. Nothing meaningful is in that HTML. The browser must download the bundle, parse it, run it to render the UI, and then usually make API calls to fetch the actual data. Only after all of that does real content appear.</p><p>This is why the timing profile is distinctive: <strong>TTFB is excellent</strong> (you're just serving a static file, basically instant), but <strong>FCP and LCP lag</strong>, because nothing meaningful paints until the JS loads, parses, and the data resolves. The user experiences a spinner, then content — a two-stage wait.</p><div class=\"analogy\"><p>CSR is like receiving a flat-pack IKEA box. The delivery is lightning fast — it's just a flat box (the shell). But you can't sit on anything until you've assembled all the parts yourself (run the JS) and then gone out to buy the cushions (fetch the data). A passerby glancing in the window sees only a pile of boards, not a sofa — which is exactly why search crawlers and link-preview bots, glancing at your near-empty HTML, see nothing useful.</p></div><h4>The SEO and preview weakness</h4><p>Because the initial HTML is empty, naive crawlers and social link-unfurlers may see no content. That's the heart of the trap: <strong>using CSR for content or marketing pages that depend on SEO and fast LCP</strong> is using it exactly where it's weakest.</p><h4>Where it genuinely shines</h4><p>CSR is excellent for <strong>app-like, authenticated, highly interactive experiences</strong> — dashboards, editors, design tools, internal admin panels. In those cases:</p><ul><li>SEO doesn't matter (it's behind a login anyway).</li><li>The user stays for a long session, so the one-time JS cost <em>amortizes</em> — they pay the bundle once, then enjoy instant client-side navigation with no server round trips per page.</li><li>The interactivity is the whole point, so you'd ship that JS regardless.</li></ul><h4>Mitigations</h4><p>You soften the initial bundle cost with <strong>code-splitting</strong> (load only the JS for the current view) and <strong>prefetching</strong> (quietly fetch the next view's code before the user clicks).</p><p>Concretely: a Vite + React SPA serves <code>index.html</code> with a <code>&lt;div id=root&gt;</code>; the bundle renders nothing on the server and fetches <code>/api/me</code> only after the component mounts.</p><div class=\"key\">CSR trades fast TTFB for slow, JS-gated first paint and weak SEO. It's the right call for long-lived, authenticated, interaction-heavy apps where the bundle cost amortizes — and the wrong call for content pages that live and die by LCP and crawlability.</div>"},
  "ssg": {"teach":"<p>At its core, Static Site Generation means you build your pages into finished HTML files <em>once</em>, ahead of time, and then serve those frozen files from a CDN — so visitors get the fastest possible page because there's nothing left to compute.</p><h4>Why pre-building is so fast</h4><p>With SSR, every request does work. With SSG, the work already happened at <strong>build time</strong>. The output is plain HTML sitting on CDN edge caches close to your users. When someone visits, the nearest edge node just hands over the file — no server render, no database call. That gives you the best achievable <strong>TTFB and LCP</strong>.</p><div class=\"analogy\"><p>SSG is like a bakery that bakes all its bread at 5am and stocks the shelves. When you walk in, you grab a loaf instantly — no waiting for it to bake (that's the CDN serving a pre-built file). The downside is obvious: if the recipe changed at noon, you're still selling this morning's bread until the next baking. That's <strong>staleness</strong>.</p></div><h4>The two real tradeoffs</h4><ul><li><strong>Staleness</strong>: content is frozen at the moment of build. Until you rebuild, the page shows whatever data existed then.</li><li><strong>Build time scales with page count</strong>: 10 pages build instantly; 100,000 product pages can mean very long builds.</li></ul><p>The trap follows directly: believing \"static = always up to date.\" Without a revalidation strategy, an SSG page keeps showing old data until the next <em>full</em> build runs.</p><h4>ISR: fixing staleness without losing speed</h4><p>This is where <strong>Incremental Static Regeneration</strong> (incremental revalidation) comes in. Instead of choosing between \"fast but stale\" and \"fresh but slow,\" ISR gives you both. Each cached page carries a TTL, or can be invalidated on-demand via a webhook. When a request comes in for a page whose TTL has expired, the CDN <strong>serves the stale copy instantly</strong> to that user, and <em>in the background</em> regenerates a fresh version for the next visitors.</p><p>That's exactly the <code>stale-while-revalidate</code> pattern you may know from HTTP caching — but applied at the page level. Nobody waits for a rebuild; freshness catches up quietly.</p><h4>Concrete example</h4><p>A blog uses <code>generateStaticParams</code> (to enumerate which post pages to pre-build) plus <code>revalidate: 60</code>. After you edit a post, visitors may see the stale version for up to 60 seconds; the first request after that window triggers a background regeneration, and subsequent visitors get the updated page.</p><div class=\"key\">SSG gives unbeatable speed by serving pre-built HTML from the edge, at the cost of staleness and build-time scaling. ISR rescues freshness with page-level stale-while-revalidate: serve the old copy now, regenerate in the background — so you rarely have to choose between fast and current.</div>"},
  "design-tokens": {"teach":"<p>At its core, a design token is just a named variable for a design decision — instead of writing the hex code <code>#3B82F6</code> in fifty places, you write <code>color.brand.primary</code> once and let one source of truth feed it everywhere.</p><h4>Why a name beats a value</h4><p>Hard-coded values are scattered, untraceable, and platform-specific. A token stores the <em>decision</em> as platform-agnostic data — typically JSON, increasingly in the W3C <strong>DTCG</strong> format. Names like <code>color.brand.primary</code> or <code>space.4</code> are then compiled (by a tool like <strong>Style Dictionary</strong>) into whatever each platform needs: CSS custom properties for the web, Swift constants for iOS, XML for Android. One definition, many outputs.</p><div class=\"analogy\"><p>Think of tokens like the wiring diagram in a building. The light switch by the door is labeled \"living-room overhead light\" — it refers to the <em>purpose</em>, not to a specific bulb. If you swap a 60W bulb for an LED, the switch's meaning doesn't change. Tokens work the same way: a button refers to \"the action background color,\" not \"blue-500.\" Swap what that intent points to, and every button updates without anyone rewiring a single switch.</p></div><h4>The three tiers — the key idea</h4><p>The real power comes from layering tokens into a hierarchy:</p><ul><li><strong>Primitive tokens</strong> — the raw palette. <code>blue-500</code>, <code>gray-100</code>. These are just colors with no meaning attached.</li><li><strong>Semantic tokens</strong> — intent. <code>color.action.bg</code> means \"the background of an interactive action.\" It <em>points</em> to a primitive: <code>color.action.bg = blue-500</code>.</li><li><strong>Component tokens</strong> — the most specific, e.g. a particular component's override.</li></ul><p>Components reference <strong>intent</strong> (semantic tokens), never raw palette values. That's what makes retheming a single value swap with zero component edits.</p><h4>Why this enables effortless theming</h4><p>At runtime, you expose semantic tokens as <code>:root</code> CSS custom properties. Now switching themes — dark mode, a white-label client's brand — is just toggling a class or <code>data-attribute</code> that redefines those variables:</p><p><code>--color-action-bg: var(--blue-500);</code> in light mode, and <code>[data-theme=dark]{ --color-action-bg: var(--blue-400); }</code> in dark mode. Every button retheme happens without touching one line of button code, because the button only ever knew about <code>--color-action-bg</code>.</p><h4>The trap</h4><p>If you let components reference primitive tokens directly — a button styled with <code>blue-500</code> instead of <code>color.action.bg</code> — you've defeated the entire system. Now dark mode means hunting down every <code>blue-500</code> in your components. Always route through semantic aliases so the swap stays in one place.</p><div class=\"key\">Design tokens turn design decisions into named, platform-agnostic data; the magic is the semantic layer — components reference <em>intent</em>, not raw values, so retheming (dark mode, white-label) becomes a single value swap with no component changes.</div>"},
  "list-virtualization": {"teach":"<p>At its core, list virtualization means you only build the rows the user can actually see, and recycle those few DOM nodes as they scroll — so whether your list has 100 rows or 100,000, the browser only ever deals with a couple dozen.</p><h4>The problem it solves</h4><p>Rendering 50,000 rows means creating 50,000 DOM nodes. The browser must lay them all out, paint them, and hold them in memory — even though only ~30 fit on screen. Scrolling becomes janky, memory balloons, and initial render crawls. Virtualization (also called <strong>windowing</strong>) says: why build what nobody can see?</p><h4>How the illusion works</h4><p>Two tricks combine:</p><ul><li>A single full-height <strong>spacer</strong> element reserves the total scroll height, so the scrollbar looks and behaves exactly as if all 50,000 rows existed. The user feels a real, correctly-sized list.</li><li>Only the rows in or near the viewport are actually rendered, and they're positioned with <code>transform: translateY(...)</code> to sit at the right offset. As you scroll, the same handful of DOM nodes get <strong>recycled</strong> — their content and position updated to show different data.</li></ul><p>The result: memory, layout, and paint cost stay roughly <strong>constant</strong> no matter how big the dataset grows. Libraries like TanStack Virtual and react-window handle the bookkeeping.</p><div class=\"analogy\"><p>Picture a long train of identical seats, but the windows are painted over except for a small viewing slot. As the train moves, a crew quietly carries the seats from behind you around to the front and re-labels them. You'd swear there are thousands of seats stretching ahead (the scrollbar promises it), but the crew is endlessly reusing the same dozen. You only ever <em>see</em> a dozen, so a dozen is all that needs to physically exist.</p></div><h4>The gotchas that bite</h4><ul><li><strong>Variable row heights</strong>: the spacer math assumes it knows each row's height. If rows differ, you must measure or estimate them, or the scrollbar position jumps as real heights are discovered.</li><li><strong>Off-screen content isn't in the DOM</strong>: this is the big trap. Because un-rendered rows literally don't exist in the document, browser <strong>Ctrl+F find-in-page</strong> can't find them, and naive crawlers can't see them either. Virtualization does <em>not</em> help SEO — it hides content from anything that reads the DOM.</li><li><strong>Accessibility</strong>: recycled rows mean you must carefully manage focus (so it doesn't vanish when its row gets reused) and provide <code>aria-rowcount</code> / <code>aria-setsize</code> so assistive tech knows the true total, not just the rendered window.</li></ul><h4>Pairing with remote data</h4><p>Virtualization pairs naturally with <strong>cursor pagination</strong>: render a virtual window over data you fetch in pages, giving the effect of a virtually-infinite remote list without ever loading it all.</p><p>Concretely: a 50k-row table renders ~30 visible rows via <code>useVirtualizer({count:50000, estimateSize:()=&gt;40})</code>, which maps each visible row to its correct <code>translateY</code> offset.</p><div class=\"key\">Virtualization keeps render cost constant by only mounting visible rows over a full-height spacer — but because off-screen rows aren't in the DOM, it breaks Ctrl+F and SEO, and demands care with variable heights and accessibility.</div>"},
  "bff": {"teach":"<p>At its core, a Backend-for-Frontend is a small server, owned by the frontend team, that sits between your UI and your messy pile of backend services and hands the UI exactly the data it needs — no more, no less, in one trip.</p><h4>The problem it solves</h4><p>In a microservices world, rendering one screen might need data from <code>/user</code>, <code>/orders</code>, and <code>/recommendations</code> — three separate services. Without a BFF, the client makes three round trips (a <strong>request waterfall</strong>), gets back bloated payloads full of fields it doesn't use (<strong>over-fetching</strong>), or has to make yet more calls for missing pieces (<strong>under-fetching</strong>). On a phone with high latency, that's painful.</p><p>A BFF moves that aggregation server-side. It calls the three services, stitches and trims the result, and returns a single UI-tailored payload in <strong>one round trip</strong>. It also becomes the natural home for cross-cutting concerns: auth, token handling, caching, and retrying flaky downstream services — all done server-side where it's safer and faster.</p><div class=\"analogy\"><p>Think of a personal shopper. Instead of you running to the butcher, the baker, and the florist yourself (three trips, each handing you their full catalog), you tell one shopper what you need. They visit all three, pick exactly your items, and hand you a single neat bag. And crucially, the shopper for your tiny apartment packs a small bag, while the shopper for a restaurant packs a big one — same shops, different output sized to the client.</p></div><h4>Per-client is the whole point</h4><p>That sizing difference is why it's called Backend-<em>for-Frontend</em>: web, iOS, and Android each get their <strong>own</strong> BFF. The mobile app needs a lean payload; the web app can handle a richer shape. Each BFF tailors the response to its specific client's needs, so divergent requirements don't force an awkward one-size-fits-all API.</p><h4>BFF vs GraphQL gateway</h4><p>A <strong>GraphQL gateway</strong> is the main alternative: one schema serving many clients, where each client declaratively selects the fields it wants. That's elegant, but it brings caching difficulty, N+1 query risks, and query-cost/complexity concerns. A BFF is <strong>per-client and imperative</strong> — you write the aggregation logic explicitly. Prefer the BFF when web and mobile needs <strong>diverge sharply</strong>; the per-client tailoring is its superpower. They can even <strong>compose</strong>: a BFF might itself call a GraphQL gateway underneath.</p><h4>The trap</h4><p>The danger is letting the BFF quietly grow into a second monolith that owns business logic and becomes the system of record. It must stay an <strong>aggregator and adapter</strong> — reshaping and combining data — not the place where core business rules live.</p><p>Concretely: the mobile BFF merges <code>/user</code>, <code>/orders</code>, and <code>/recommendations</code> into one trimmed JSON sized for a phone, while the web BFF returns a richer shape from the same services.</p><div class=\"key\">A BFF is a per-client edge service that collapses many backend calls into one UI-shaped payload, killing over/under-fetching and waterfalls — but it must stay an aggregator, never the system of record. Choose it over a GraphQL gateway when client needs diverge sharply.</div>"},
  "optimistic-ui": {"teach":"<p>At its core, optimistic UI means you update the screen <em>immediately</em> as if the action already succeeded, instead of making the user wait for the server to say yes — and you only walk it back if the server disagrees.</p><h4>Why bother</h4><p>Network round trips take time. If you wait for the server before showing a \"like\" turning blue, the app feels sluggish even on a fast connection. Optimistic UI assumes the action will succeed (it usually does) and reflects that instantly, so the app feels snappy and alive.</p><h4>The disciplined pattern</h4><p>You can't just blindly update and hope. The correct pattern has three beats:</p><ul><li><strong>On mutate</strong> — <em>snapshot</em> the previous state (so you can undo), then apply the optimistic value to local state.</li><li><strong>On success</strong> — replace your guess with the server's <strong>canonical response</strong>. The server is the source of truth; your optimistic value was just a placeholder.</li><li><strong>On error</strong> — <em>roll back</em> to the snapshot you saved, and tell the user.</li></ul><p>This is exactly what TanStack Query's <code>onMutate / onError / onSettled</code> hooks formalize, and what React 19's <code>useOptimistic</code> gives you natively.</p><div class=\"analogy\"><p>Think of a good waiter taking your order. The moment you say \"the salmon,\" they write it down and tell you \"great choice\" — they don't sprint to the kitchen to confirm the salmon is in stock before responding. You feel served instantly. But they keep a mental note (the snapshot): if the kitchen says they're out of salmon, the waiter comes back, apologizes, and corrects the order. That graceful correction is your rollback.</p></div><h4>The trap that wrecks it</h4><p>The single most dangerous mistake is <strong>forgetting to snapshot and roll back</strong>. If a mutation fails and you never restore the previous state, your UI is now showing data the server <em>never accepted</em> — a phantom like, a saved comment that doesn't exist. The user trusts a lie. The snapshot is not optional; it's the safety net that makes optimism honest.</p><h4>Harder edge cases</h4><ul><li><strong>Out-of-order responses</strong>: fire two mutations quickly and the responses may return in the wrong order. Use <strong>client-generated temporary IDs</strong> so you can match each response to the right optimistic item.</li><li><strong>Irreversible actions</strong>: don't be optimistic about financial transfers, deletions, or anything you can't cleanly undo. Show the truth, not a guess.</li><li><strong>Real-time echo</strong>: if a WebSocket channel broadcasts your own mutation back to you, de-duplicate it so you don't double-count your own action.</li></ul><p>Concretely: liking a post, you increment the count instantly and stash the old count. If the <code>POST</code> returns a 500, you restore the old count and surface a retry toast.</p><div class=\"key\">Optimistic UI makes apps feel instant by showing the expected result before confirmation — but only works if you snapshot the old state and roll back on failure. Skip the rollback and your UI starts displaying data the server rejected.</div>"},
  "islands-architecture": {"teach":"<p>At its core, islands architecture means you ship a page that's almost entirely static HTML, and only the few genuinely interactive widgets — the \"islands\" — get any JavaScript at all.</p><h4>The insight</h4><p>Most pages are mostly static. A blog post is text, headings, and images; the only interactive bit might be a search box or a comment form. Yet traditional hydration re-runs JavaScript for the <em>entire</em> page, even the parts that will never respond to a click. That's enormous wasted work. Islands flip the model: render everything as static HTML, then hydrate <strong>only</strong> the interactive components — each one independently, and often lazily (on browser idle, or when scrolled into view).</p><p>Frameworks like <strong>Astro</strong> and <strong>Fresh</strong> are built around this. Because the static \"sea\" between islands never needs client JS, you ship dramatically less JavaScript and your <strong>INP</strong> improves sharply compared to full-page hydration.</p><div class=\"analogy\"><p>Picture a calm ocean dotted with a few small islands. The water is the static HTML — vast, peaceful, requiring no machinery. The islands are your interactive widgets — a search box here, a like button there. You only need to ship power and crew (JavaScript) to the islands; you don't electrify the entire ocean. And each island runs its own generator independently — you can power up the one the visitor is standing on first and leave distant islands dark until they sail over.</p></div><h4>Lazy, independent hydration</h4><p>Each island decides <em>when</em> to come alive. A search box marked <code>client:visible</code> doesn't download its JavaScript until the user scrolls it into view. One marked <code>client:idle</code> waits until the browser has nothing better to do. This per-island, on-demand loading is what makes the JS budget so small.</p><h4>The trap: islands are isolated</h4><p>Here's the catch people miss: each island is its own little app. You <strong>cannot</strong> freely share React state across the page the way you would in a single SPA tree — there's no shared component tree connecting them. If island A needs to know something island B changed, you must use an <strong>external store</strong> (like a shared signal store) or a global event bus. Treating islands as if they're part of one big React tree leads to confusion when state doesn't propagate.</p><h4>A cousin worth knowing: resumability</h4><p>Related is <strong>resumability</strong>, pioneered by <strong>Qwik</strong>. Instead of hydrating (re-executing the component tree on the client), Qwik serializes the listener and state information directly into the HTML. The client can then <em>resume</em> exactly where the server left off without re-running everything — pushing startup hydration cost toward zero. Islands cut <em>how much</em> hydrates; resumability attacks <em>whether you hydrate at all</em>.</p><p>Concretely: an Astro blog post is pure static HTML except for a <code>&lt;Search client:visible /&gt;</code> island, whose JavaScript only downloads once it scrolls into view.</p><div class=\"key\">Islands architecture ships a static page and hydrates only the interactive widgets, independently and lazily — slashing JS and boosting INP. The price: islands are isolated, so cross-island state needs an external store, not shared component state.</div>"}
});
