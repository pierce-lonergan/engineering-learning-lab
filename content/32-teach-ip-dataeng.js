/* Teach me from the ground up: IP-DATAENG (auto-assembled) */
enh({
  "kafka-trade-capture": {"teach":"<p>At its core, event-driven trade capture is just this: <strong>every time a trade fills, you write down one note about it and drop that note into a shared inbox that many different teams can read from at their own pace.</strong> Nobody calls anybody. You just post the note; whoever cares picks it up.</p><h4>The note and the inbox</h4><p>The note is an <strong>event</strong> &mdash; a small, immutable fact: \"100 shares of AAPL filled at 187.30 for account X at 10:04:12.\" The inbox is a Kafka <strong>topic</strong> (think of it as a named log of events). A <strong>producer</strong> writes events into the topic; one or more <strong>consumers</strong> read them.</p><p>The clever part is the <strong>key</strong>. Each event carries a key (say symbol or account). Kafka uses the key to decide which <strong>partition</strong> the event lands in, and within a single partition events stay in the exact order they were written. So all of AAPL's fills, or all of account X's fills, arrive in order &mdash; which matters enormously when you're applying buys and sells to a position.</p><div class=\"analogy\"><p>Picture a busy restaurant kitchen with a ticket rail. Each waiter clips an order ticket onto the rail (the producer). The grill cook, the salad station, and the cashier each walk over and read tickets independently (the consumers) &mdash; the grill cook books the cook, the cashier rings it up. Nobody waits on anybody else. And tickets for table 5 stay clipped in the order they arrived, so you don't fire the dessert before the main.</p></div><h4>Why this shape wins for trades</h4><p>One fill event feeds many jobs at once: a <strong>ledger</strong> consumer updates your positions, a <strong>recon</strong> consumer lands the raw fill for later reconciliation, and a <strong>warehouse</strong> sink copies it to analytics. These are fully <strong>decoupled</strong> &mdash; the warehouse can be slow or down and the ledger keeps booking. Kafka also <strong>buffers bursts</strong>: at the open, fills can spike, and the queue absorbs the surge instead of crushing a downstream DB. And because Kafka retains events by offset, you can <strong>replay</strong> &mdash; rebuild a broken consumer's state by re-reading from an earlier offset.</p><h4>The catch: at-least-once</h4><p>By default Kafka guarantees <strong>at-least-once</strong> delivery: it will never silently lose your fill, but on a crash-and-retry it may hand you the same fill <em>twice</em>. If your ledger naively adds the position again, you've now booked a phantom 200 shares. So every consumer must be <strong>idempotent</strong> &mdash; applying the same event twice has the same effect as applying it once, usually by upserting on a stable id.</p><p>Concretely: FIX drop-copy execution reports (<code>35=8</code>) get parsed and published to a <code>fills</code> topic; the ledger consumer upserts the position keyed on <code>provider_trade_id</code>, so a redelivered fill is harmless.</p><div class=\"key\"><p>Kafka gives you a durable, ordered, replayable stream that fans one fill out to many independent consumers &mdash; but it delivers <em>at-least-once</em>, so the consumer (not Kafka) must be idempotent to avoid double-booking.</p></div>"},
  "delivery-semantics": {"teach":"<p>At its core, delivery semantics are just the answer to one nervous question: <strong>\"when I send a message across a flaky network, can it get lost, can it arrive twice, or is it guaranteed to land exactly once?\"</strong></p><h4>The three promises</h4><ul><li><strong>At-most-once</strong>: send it and forget it. If it's lost, oh well &mdash; it's never duplicated, but it <em>can vanish</em>. Fast and cheap, because nobody retries.</li><li><strong>At-least-once</strong>: keep retrying until you get an acknowledgement. Nothing is ever lost, but a retry after a slow or missing ack means the same message can <em>arrive twice</em>.</li><li><strong>Exactly-once</strong>: the holy grail &mdash; each message takes effect precisely one time, no loss and no duplicate.</li></ul><p>Why doesn't everyone just pick exactly-once? Because true <strong>end-to-end</strong> exactly-once across an <em>external</em> system (like a database your streaming engine doesn't control) is genuinely hard and expensive. The moment your message leaves the streaming system and updates an outside ledger, you need that update and the \"I've consumed this\" bookkeeping to commit together, atomically. Networks crash <em>between</em> those two steps. So most streaming systems default to at-least-once.</p><div class=\"analogy\"><p>Think of mailing a signed contract. <strong>At-most-once</strong> is dropping it in the mailbox with no tracking &mdash; cheap, but if the post office loses it, it's gone. <strong>At-least-once</strong> is certified mail where you re-send if you don't get the signature card back &mdash; the contract always arrives, but if a signature card is delayed you might mail a second copy, and now your counterparty has two contracts on the desk. <strong>Exactly-once</strong> would be a magical courier who guarantees one and only one copy lands &mdash; wonderful, but you pay a premium and it's brittle.</p></div><h4>The trade-ops trick: effectively-once</h4><p>For fills hitting a ledger, losing a message is unacceptable &mdash; a dropped fill is a <strong>phantom or wrong position</strong>, real money mis-stated. So at-most-once is off the table immediately.</p><p>The practical recipe is: <strong>at-least-once delivery + idempotent consumers = effectively-once.</strong> You accept that duplicates <em>will</em> occasionally happen, and you make them <em>harmless</em>. The consumer keeps a dedupe table keyed on a stable id (a <code>trade_id</code>); before applying a fill it checks whether it's already seen that id, and if so it skips. You've gotten the safety of exactly-once without paying for end-to-end transactional exactly-once.</p><p>Concretely: a consumer crashes right after applying a fill but <em>before</em> committing its Kafka offset. On restart it re-reads that fill. But the <code>trade_id</code> is already in the dedupe table, so the second apply is a no-op &mdash; the position stays correct.</p><div class=\"key\"><p>Pick the guarantee by the cost of being wrong: for money-moving fills, never at-most-once. Use at-least-once delivery and make the consumer idempotent &mdash; that combination gives you effectively-once without the pain of real end-to-end exactly-once.</p></div>"},
  "cdc": {"teach":"<p>At its core, Change Data Capture is just this: <strong>instead of repeatedly asking a database \"what's new?\", you stand at the door and watch every change walk out the moment it happens.</strong> Every insert, update, and delete becomes its own event the instant the database commits it.</p><h4>Why not just poll?</h4><p>The obvious approach is <strong>polling</strong>: every few seconds, run <code>SELECT ... WHERE updated_at &gt; last_check</code> and copy whatever changed. This seems fine but quietly leaks information:</p><ul><li>If a row changes <em>twice</em> between polls, you only ever see the final state &mdash; the intermediate state is <strong>gone</strong>.</li><li><strong>Deletes are invisible</strong> &mdash; a deleted row simply isn't in the result set, so your downstream copy keeps a ghost.</li><li>It hammers the source DB with repeated scans.</li></ul><h4>How log-based CDC works</h4><p>Every serious database already keeps a <strong>transaction log</strong> &mdash; Postgres calls it the <strong>WAL</strong> (write-ahead log) &mdash; an ordered, append-only record of every change it commits, used internally for crash recovery and replication. CDC tools like <strong>Debezium</strong> simply <em>read that log</em> and turn each entry into an event: row inserted, row updated (with old and new values), row deleted. Because the log is the database's own authoritative, ordered history, CDC captures <strong>every</strong> change, in <strong>exact commit order</strong>, including deletes &mdash; and it barely touches the source because reading the log is much cheaper than running scans.</p><div class=\"analogy\"><p>Polling is like photographing a whiteboard every ten minutes &mdash; you catch the current scribbles but miss anything written and erased in between, and you never know what was rubbed out. Log-based CDC is like having a live transcript of every stroke and erasure of the marker, in order. You can reconstruct the whiteboard at any instant, and you know precisely what was deleted and when.</p></div><h4>Why it matters for trade ops</h4><p>Your trade-capture database is an <strong>OLTP</strong> system (fast transactional writes). Your reconciliation and analytics live in an <strong>OLAP</strong> warehouse like Snowflake. CDC is the conveyor belt that moves changes continuously from OLTP to OLAP, so the warehouse is <strong>seconds-fresh</strong> instead of waiting for a nightly batch extract. That freshness is exactly what you need under <strong>T+1</strong> settlement, where the window to reconcile and fix breaks is brutally compressed &mdash; you can't afford to discover a mismatch tomorrow morning.</p><p>Concretely: the instant a new fill row commits in Postgres, Debezium reads it off the WAL and publishes it to Kafka, keeping the Snowflake recon model seconds behind the source instead of a full day.</p><div class=\"key\"><p>CDC streams a database's actual commit log &mdash; not periodic snapshots &mdash; so you capture <em>every</em> change in order, including deletes and fast intermediate states, with low source load. That's what turns nightly recon into near-real-time recon.</p></div>"},
  "dtcc-itp-nscc-dtc": {"teach":"<p>At its core, after two parties agree on a trade, three different jobs still have to happen: somebody confirms both sides actually agree, somebody figures out who net owes what, and somebody moves the shares and the cash. In US equities, <strong>DTCC runs all three through three distinct pieces &mdash; ITP, NSCC, and DTC.</strong></p><h4>ITP &mdash; match and affirm</h4><p><strong>ITP</strong> (Institutional Trade Processing) is the front of the pipeline. When an institution trades, the broker and the asset manager must agree on every detail &mdash; price, quantity, account, settlement instructions. ITP does this <strong>matching and affirmation</strong>: \"match-to-instruct.\" An <strong>affirmed</strong> trade is one both sides have confirmed and locked. Crucially, affirmation is the <strong>gate</strong> &mdash; nothing flows to settlement until the trade is affirmed.</p><h4>NSCC &mdash; the clearer (CCP)</h4><p><strong>NSCC</strong> (National Securities Clearing Corporation) is the <strong>central counterparty (CCP)</strong>. Two powerful things happen here. First, <strong>novation</strong>: NSCC legally steps into the middle of every trade, becoming the buyer to every seller and the seller to every buyer &mdash; so you no longer face your original counterparty, you face NSCC, and it provides a <strong>settlement guarantee</strong>. Second, <strong>multilateral netting</strong> via <strong>CNS</strong> (Continuous Net Settlement): if you bought 1,000 AAPL and sold 700 across the day, NSCC nets you down to a single obligation of 300, drastically shrinking how much actually has to move.</p><h4>DTC &mdash; the depository</h4><p><strong>DTC</strong> (Depository Trust Company) is the <strong>central securities depository</strong>: it holds the securities in custody and performs <strong>book-entry settlement</strong> &mdash; the shares never physically move; ownership flips by updating ledger entries, exchanged against cash (delivery versus payment).</p><div class=\"analogy\"><p>Think of a giant after-school sports swap. <strong>ITP</strong> is the two kids and their parents signing off that yes, we agreed to trade <em>this</em> card for <em>that</em> price. <strong>NSCC</strong> is the trusted teacher who collects everyone's promises, cancels out all the back-and-forth so each kid only owes one net thing, and personally guarantees the swap even if a kid flakes. <strong>DTC</strong> is the locked trophy cabinet where the cards actually live &mdash; the teacher just re-labels which shelf belongs to whom; nothing leaves the cabinet.</p></div><h4>Why the distinction matters</h4><p>People conflate NSCC and DTC, but they're <strong>separate legal entities with different jobs</strong>: NSCC <em>clears</em> (nets, novates, guarantees); DTC <em>settles and custodies</em>. Under <strong>T+1</strong>, same-day affirmation through ITP is precisely what lets NSCC net and DTC settle the very next morning &mdash; the compressed timeline leaves no slack for late affirmations.</p><p>Concretely: an affirmed institutional trade flows <code>ITP &rarr; NSCC</code> (netted into a single CNS obligation) <code>&rarr; DTC</code> (book-entry delivery versus payment).</p><div class=\"key\"><p>Three roles, three entities: ITP matches/affirms (the gate), NSCC is the CCP that nets and guarantees (clearing), and DTC is the depository that custodies and settles (book-entry DvP). Don't merge the clearer and the depository &mdash; they are legally and functionally distinct.</p></div>"}
});
