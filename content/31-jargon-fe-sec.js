/* Frontend jargon & acronyms — clickable reference (FE-SEC, auto-assembled) */
Object.assign(GLOSSARY_DATA, {
  "sop": {"term":"SOP","group":"FE-SEC","aliases":["Same-Origin Policy","Same Origin Policy"],"eli5":"The browser's core security rule that scripts from one origin cannot read responses or data from a different origin.","memory":"Same-Origin Policy — code can only freely read from its own origin (scheme + host + port); everything else is fenced off.","full":"<p><strong>What it is.</strong> The <strong>Same-Origin Policy (SOP)</strong> is the foundational isolation mechanism of the web: scripts running on one <em>origin</em> are restricted from reading data or responses belonging to a different origin. An <strong>origin</strong> is the triple of <strong>scheme + host + port</strong> (e.g. <code>https://app.example.com:443</code>); change any one and it's a different origin. SOP is what keeps a random tab from reading your webmail or your bank's pages just because they're open in the same browser.</p><p><strong>History / origin.</strong> The Same-Origin Policy dates back to the early days of scriptable browsers — it originated at Netscape in the mid-1990s as JavaScript and cross-site scripting risks emerged. It has been refined and standardized over the decades as browsers grew more capable, but its core principle has remained remarkably stable and is now woven through specs like HTML, Fetch, and the cookie standards.</p><p><strong>Purpose &amp; why still used.</strong> SOP is the bedrock of browser security and is absolutely still in force. It governs <em>reads</em>, not all sends — which is why <strong>CSRF</strong> (a cross-origin <em>write</em>) is possible despite SOP, and why <strong>CORS</strong> exists to <em>selectively relax</em> SOP's read restrictions when a server opts in. Almost every other browser security feature (CORS, cookie <code>SameSite</code> attributes, postMessage, cross-origin isolation) is defined in relation to the origin concept SOP establishes.</p>","worked":"<p>Two tabs open: your bank at <code>https://bank.com</code> and a malicious page at <code>https://evil.com</code>.</p><ul><li>Script on <code>evil.com</code> calls <code>fetch('https://bank.com/balance')</code>. The request may even be <em>sent</em> with your cookies — but SOP blocks <code>evil.com</code>'s script from <em>reading</em> the response. That's the protection.</li><li>Origins that look similar but differ in any part are still cross-origin: <code>https://app.com</code> vs <code>http://app.com</code> (scheme), vs <code>https://api.app.com</code> (host), vs <code>https://app.com:8443</code> (port).</li></ul><p>Because SOP blocks the <em>read</em> but not the <em>send</em>, a forged write request (CSRF) can still succeed — hence <code>SameSite</code> cookies and CSRF tokens.</p>","teach":"<p>At its core, the Same-Origin Policy is just one rule: code from site A is not allowed to read what belongs to site B. Everything else in browser security builds on top of it.</p><p>Why does the browser need this rule? Because a single browser holds your logged-in sessions for dozens of sites at once, and the cookies that prove who you are get attached automatically to requests. Without isolation, any sketchy page you open could quietly call your bank, your email, your admin panel — using <em>your</em> credentials — and read everything back.</p><div class=\"analogy\">Think of each origin as an apartment in a building, and your cookies as a key the doorman copies onto every delivery you send. The Same-Origin Policy is the rule that, although a stranger in apartment 4B can shove a note under your door (send a request), they are <em>not allowed to walk in and read your mail back out</em> (read the response). The doorman will carry the note in — but the stranger never sees what comes back.</div><p>The precise definition of \"same\" matters: an origin is <strong>scheme + host + port</strong>. <code>https://shop.com</code> and <code>http://shop.com</code> are different origins (scheme differs). So are <code>shop.com</code> and <code>api.shop.com</code> (host), and <code>shop.com</code> and <code>shop.com:8080</code> (port). All three must match for two pages to be same-origin.</p><p>Now the two crucial consequences. First, SOP guards <em>reads</em>, not sends. A malicious page can fire off a request that <em>changes</em> something on another site — that's the <strong>CSRF</strong> attack — because the send still goes through; only the reply is hidden. That's why we need extra defenses like <code>SameSite</code> cookies. Second, sometimes you <em>want</em> cross-origin reads (calling a legitimate third-party API). SOP is deliberately strict, so <strong>CORS</strong> exists as the controlled way for a server to opt in and relax it.</p><div class=\"key\">The Same-Origin Policy is the browser's bedrock isolation: scripts can't read across origins, where origin = scheme + host + port. It protects reads but not sends — so CSRF survives it, and CORS exists to relax it on purpose.</div>","related":["cors","xhr","mime","jsonp"]},
  "coop": {"term":"COOP","group":"FE-SEC","aliases":["Cross-Origin-Opener-Policy","Cross Origin Opener Policy"],"eli5":"COOP is an HTTP response header that severs the JavaScript link between your page and any other-origin window that opened it (or that it opens), so they can't poke into each other's browsing context.","memory":"Cross-Origin-Opener-Policy — it controls your relationship with the window that 'opened' you (window.opener).","full":"<p><strong>COOP (Cross-Origin-Opener-Policy)</strong> is an HTTP response header that lets a document control whether it shares a browsing-context group with cross-origin windows that open it or that it opens. With <code>Cross-Origin-Opener-Policy: same-origin</code>, the browser breaks the <code>window.opener</code> relationship to cross-origin pages, putting your document in its own isolated context group so other origins can't reach into it via the opener reference.</p><p>COOP emerged from the web platform's response to speculative-execution attacks (Spectre/Meltdown, disclosed in 2018) and to cross-window scripting leaks. It was developed within the WHATWG HTML standard and the W3C web-security community, and shipped in major browsers around 2020. Together with COEP it forms the mechanism for opting a document into <em>cross-origin isolation</em>.</p><p>It is still used today for two main reasons. First, it hardens pages against cross-window attacks by cutting the synchronous-access channel that <code>window.opener</code> provides. Second — and the reason most developers reach for it — a page must send <code>COOP: same-origin</code> <em>and</em> <code>COEP: require-corp</code> to become cross-origin isolated, which is the prerequisite for powerful features like <code>SharedArrayBuffer</code> and high-resolution timers. Without that isolation, those APIs are unavailable.</p>","worked":"<p>Server response headers to achieve cross-origin isolation:</p><ul><li><code>Cross-Origin-Opener-Policy: same-origin</code></li><li><code>Cross-Origin-Embedder-Policy: require-corp</code></li></ul><p>With both set, JavaScript can verify isolation and unlock shared memory:</p><ul><li><code>self.crossOriginIsolated === true</code></li><li><code>const buf = new SharedArrayBuffer(1024); // now allowed</code></li></ul><p>A cross-origin page that opened this document will find <code>window.opener</code> is null, severing the back-channel.</p>","teach":"<p>At its core, COOP is just a switch that says \"don't let windows from other websites keep a live JavaScript handle on me.\"</p><p>When one page opens another with <code>window.open()</code> or a link, the new page normally gets a <code>window.opener</code> reference back to its parent, and they can share a browsing-context group. If those two pages are from different origins, that link is a tiny crack: it allows certain cross-window interactions and, combined with CPU side-channels, could be abused to leak data across origins.</p><div class=\"analogy\">Think of browser tabs as apartments. Normally, when your apartment is opened by a neighbor, you leave an intercom line connected back to theirs — handy, but it means they can buzz you and listen at the door. COOP with <code>same-origin</code> is you cutting that intercom wire to anyone who isn't a member of your own household. You get your own private building wing where outsiders simply can't reach in.</div><p>There are two payoffs. The defensive one: cutting the opener link closes a cross-window attack surface. The enabling one is the reason most teams actually set it — COOP is half of the password to unlock <strong>cross-origin isolation</strong>. The other half is COEP. Only when a page sends <code>COOP: same-origin</code> and <code>COEP: require-corp</code> does the browser flip <code>self.crossOriginIsolated</code> to true and re-enable powerful, previously-disabled APIs.</p><p>Those APIs — chiefly <code>SharedArrayBuffer</code> (shared memory between the main thread and Web Workers) and precise timers — were globally restricted after the 2018 Spectre disclosures because they made timing attacks easier. Cross-origin isolation is the platform's way of saying \"prove your page is sealed off, and we'll give the dangerous tools back.\"</p><div class=\"key\">COOP isolates your window from cross-origin openers (kills <code>window.opener</code> for other origins). Paired with COEP, it earns your page cross-origin isolation, which is the gate to SharedArrayBuffer and high-res timers.</div>","related":["coep","sni","sri"]},
  "coep": {"term":"COEP","group":"FE-SEC","aliases":["Cross-Origin-Embedder-Policy","Cross Origin Embedder Policy"],"eli5":"COEP is an HTTP response header that forces every cross-origin resource your page embeds to explicitly opt in to being loaded, which (with COOP) flips your page into cross-origin isolated mode.","memory":"Cross-Origin-Embedder-Policy — it governs what you're allowed to 'embed' from elsewhere; require-corp means every embed must say yes.","full":"<p><strong>COEP (Cross-Origin-Embedder-Policy)</strong> is an HTTP response header that controls whether a document can load cross-origin resources that haven't explicitly granted permission to be embedded. The common value <code>Cross-Origin-Embedder-Policy: require-corp</code> tells the browser to block any cross-origin subresource (image, script, font, fetch, etc.) unless that resource opts in — either via a <code>Cross-Origin-Resource-Policy</code> header or via CORS.</p><p>Like COOP, COEP was created as part of the web platform's post-Spectre security work and was standardized within the WHATWG/W3C web-security effort, shipping in major browsers around 2020. The two headers were designed to work as a pair: they are the joint mechanism for entering <em>cross-origin isolation</em>.</p><p>COEP remains in use because it is the other half (with COOP) of the gate that unlocks <code>SharedArrayBuffer</code>, precise timers, and other high-powered APIs that were restricted after Spectre/Meltdown. Its purpose is to guarantee that nothing in your document was loaded \"by accident\" from an origin that didn't consent — so the browser can trust that your page is a sealed memory boundary. A later value, <code>credentialless</code>, relaxes the requirement for some resources by loading them without credentials instead of demanding explicit opt-in, easing adoption.</p>","worked":"<p>To make a page cross-origin isolated, send both headers from the server:</p><ul><li><code>Cross-Origin-Opener-Policy: same-origin</code></li><li><code>Cross-Origin-Embedder-Policy: require-corp</code></li></ul><p>Now a cross-origin asset must opt in or it will be blocked. The asset's own server must send, e.g.:</p><ul><li><code>Cross-Origin-Resource-Policy: cross-origin</code></li></ul><p>If it doesn't, the resource fails to load. Once everything complies, <code>self.crossOriginIsolated === true</code> and <code>SharedArrayBuffer</code> becomes available.</p>","teach":"<p>At its core, COEP is just a rule that says \"my page will only embed outside resources that have explicitly agreed to be embedded — no freeloaders.\"</p><p>A normal web page pulls in dozens of cross-origin things: a font from a CDN, an analytics script, an image from a partner. The browser usually loads these without asking the resource's server whether it's okay to be embedded in <em>your</em> page. That permissiveness is fine most of the time, but it means your page's memory space can contain bytes from origins that never consented — a problem if the browser wants to give your page powerful, Spectre-sensitive APIs.</p><div class=\"analogy\">Imagine you're assembling a secure cleanroom and every component must come with a signed certificate saying \"approved for use in a sealed environment.\" COEP <code>require-corp</code> is the inspector at the door rejecting any part that lacks that certificate. Only once <em>every</em> embedded part is certified can the room be declared sterile — i.e., cross-origin isolated.</div><p>This is why COEP and COOP are always discussed together. COOP seals you off from other <em>windows</em>; COEP seals you off from un-consenting <em>subresources</em>. The browser requires both because cross-origin isolation only makes sense if neither channel can smuggle in foreign data. Set <code>COOP: same-origin</code> + <code>COEP: require-corp</code> and the browser flips <code>self.crossOriginIsolated</code> to true.</p><p>The reward is the same as with COOP: <code>SharedArrayBuffer</code> (real shared memory across threads, essential for WebAssembly threading, high-performance games, and media processing) and high-resolution timers come back online — features that were globally disabled after Spectre because they sharpened timing attacks. Because <code>require-corp</code> can be hard to roll out (every third-party asset must comply), the <code>credentialless</code> mode exists as a gentler path.</p><div class=\"key\">COEP makes every cross-origin embed opt in (require-corp). Together with COOP's <code>same-origin</code> it grants cross-origin isolation — the precondition for SharedArrayBuffer and precise timing APIs.</div>","related":["coop","sni","sri"]},
  "idn": {"term":"IDN","group":"FE-SEC","aliases":["Internationalized Domain Name","Internationalized Domain Names"],"eli5":"An IDN is a domain name that contains non-ASCII characters (like accented letters or non-Latin scripts), encoded under the hood into ASCII so the DNS system can route it.","memory":"Internationalized Domain Name — domains for the whole world, not just a-z; beware look-alike (homograph) tricks.","full":"<p><strong>IDN (Internationalized Domain Name)</strong> is a domain name that includes characters outside the basic ASCII letter/digit/hyphen set — for example accented Latin letters, or Cyrillic, Greek, Arabic, or CJK scripts. Because the DNS infrastructure only handles a restricted ASCII set, IDNs are transformed via a process called IDNA into an ASCII-compatible encoding (a <code>xn--</code> \"Punycode\" label) before resolution.</p><p>IDNs were standardized by the IETF: the original IDNA specification (RFC 3490 and related RFCs, around 2003) defined Punycode encoding and the ToASCII/ToUnicode operations, and a later revision, IDNA2008 (RFCs 5890–5894, around 2010), updated the character rules. The purpose was to make the web usable in the world's many writing systems rather than restricting domain names to English-friendly characters.</p><p>From a frontend <em>security</em> standpoint, IDNs matter because of <strong>homograph (homoglyph) attacks</strong>: an attacker can register a domain using look-alike characters from another script (e.g., a Cyrillic letter that renders identically to a Latin one) to impersonate a trusted site. This is why URL validation and open-redirect defenses must normalize and carefully inspect hostnames, and why browsers display Punycode in suspicious mixed-script cases. When allowlisting redirect targets or parsing user-supplied URLs, IDN/Punycode handling is part of preventing spoofing and bypasses.</p>","worked":"<p>An IDN and its Punycode (ASCII) form:</p><ul><li>Human-readable: <code>bücher.example</code></li><li>DNS/Punycode form: <code>xn--bcher-kva.example</code></li></ul><p>The homograph risk — two visually similar hostnames that are <em>different</em> strings:</p><ul><li><code>apple.com</code> (Latin 'a')</li><li><code>аpple.com</code> (Cyrillic 'а', encoded as a distinct <code>xn--</code> label)</li></ul><p>A redirect allowlist that compares display strings naively could be tricked; safe code normalizes to Punycode and does an exact-match check against the registered host.</p>","teach":"<p>At its core, an IDN is just a domain name written in your own alphabet — and a hidden ASCII translation that the internet's plumbing actually uses.</p><p>The Domain Name System was born in an English-centric era and only understands a narrow set of characters: a–z, 0–9, and the hyphen. That left out most of humanity's writing systems. IDNs fix this by letting people type a name like <code>münchen.de</code> while a standard transformation (Punycode, producing a label like <code>xn--mnchen-3ya.de</code>) converts it into something DNS can resolve.</p><div class=\"analogy\">Think of Punycode like a postal system that secretly rewrites every address into a universal barcode. You write the address in your native script; the sorting machines read only the barcode. The convenience is huge — but it means two envelopes that <em>look</em> identical to a human can carry <em>different</em> barcodes, and two with different-looking text could be confused if you're not careful.</div><p>That look-alike problem is the security crux for frontend developers. Many scripts contain glyphs that render almost identically to Latin letters — a Cyrillic \"а\" next to a Latin \"a\" is indistinguishable on screen. An attacker registers a homograph domain that visually mimics <code>paypal.com</code> and harvests credentials. Browsers mitigate this by showing the raw Punycode for suspicious mixed-script domains.</p><p>When you write code that handles URLs — validating an OAuth <code>redirect_uri</code>, building an open-redirect allowlist, or parsing user-supplied links — you must account for IDNs. Naively comparing the displayed string can be defeated by homoglyphs, trailing whitespace, or encoding tricks. Normalize to the canonical ASCII/Punycode form and do strict, exact matching against your registered hosts. The same vigilance that catches <code>//evil.com</code> and backslash bypasses must also catch look-alike Unicode hostnames.</p><div class=\"key\">IDN = a domain in non-ASCII script, encoded to a <code>xn--</code> Punycode label for DNS. Standardized by the IETF (IDNA 2003, IDNA2008). Its frontend danger is homograph spoofing — always normalize and exact-match hostnames in URL/redirect validation.</div>","related":["sso","rfc"]},
  "sso": {"term":"SSO","group":"FE-SEC","aliases":["Single Sign-On","Single Sign On"],"eli5":"SSO lets a user authenticate once with one trusted identity provider and then access many separate applications without logging in again to each one.","memory":"Single Sign-On — sign on a single time, get into everything.","full":"<p><strong>SSO (Single Sign-On)</strong> is an authentication arrangement in which a user logs in once to a central identity provider and then gains access to multiple independent applications or services without re-entering credentials for each. Instead of every app holding its own password, they delegate authentication to a shared, trusted authority that issues tokens or assertions proving the user's identity.</p><p>SSO predates the modern web — enterprise systems and protocols like Kerberos (developed at MIT in the 1980s) established the pattern internally. On the web, SSO is typically built today on standards such as <strong>SAML</strong> (an OASIS standard, with SAML 2.0 finalized in 2005) for enterprise federation, and <strong>OAuth 2.0</strong> (an IETF standard, RFC 6749, 2012) plus <strong>OpenID Connect</strong> (built on OAuth, 2014) for delegated authorization and identity. \"Log in with Google/Apple/Microsoft\" buttons are consumer-facing SSO.</p><p>SSO endures because it reduces password fatigue, centralizes security policy (one place to enforce MFA, revoke access, and audit), and lowers the attack surface of scattered credential stores. Its frontend security caveat is the redirect flow: protocols like OAuth/OIDC bounce the user to the identity provider and back via a <code>redirect_uri</code>. That URI must be validated against a strict, pre-registered allowlist with exact matching and no wildcards, because a lax check lets an attacker steal authorization codes or tokens by redirecting them to a malicious host.</p>","worked":"<p>An OAuth/OIDC authorization request the frontend initiates for SSO:</p><ul><li><code>GET https://idp.example.com/authorize?response_type=code&client_id=app123&redirect_uri=https://app.example.com/callback&scope=openid%20profile&state=xyz</code></li></ul><p>The identity provider authenticates the user once, then redirects back to the <em>exact</em> registered URI with a code:</p><ul><li><code>https://app.example.com/callback?code=AUTH_CODE&state=xyz</code></li></ul><p>The provider must reject any <code>redirect_uri</code> that isn't an exact match in its allowlist — no wildcards, no <code>//evil.com</code>, no trailing-path tricks — or the code can be hijacked.</p>","teach":"<p>At its core, SSO is just proving who you are <em>once</em> to a single trusted gatekeeper, then getting waved into many buildings on the strength of that one check.</p><p>Without SSO, every app you use keeps its own username/password. That's a nightmare: users reuse weak passwords, each app becomes a separate breach target, and there's no central place to enforce policy or revoke access. SSO flips the model — a dedicated <strong>identity provider</strong> handles login, and every application <em>trusts</em> the provider's word about who you are.</p><div class=\"analogy\">Imagine a theme park where, instead of buying a separate ticket at every ride, you show ID once at the front gate and get a wristband. Each ride just glances at the wristband. The identity provider is the front gate; the wristband is the token (a SAML assertion or an OAuth/OIDC token); the rides are the apps. One trusted check, many doors.</div><p>The machinery is built on standards. Enterprises lean on <strong>SAML 2.0</strong> (2005) for federation; consumer and API-driven apps use <strong>OAuth 2.0</strong> (RFC 6749, 2012) for authorization and <strong>OpenID Connect</strong> (2014) layered on top to convey identity. The familiar \"Sign in with Google\" button is exactly this flow.</p><p>For a frontend developer the dangerous moment is the <em>redirect</em>. These protocols send the user from your app to the identity provider and back, carrying a sensitive authorization code in the URL. The provider decides where to send the user using the <code>redirect_uri</code> your app supplied. If the provider accepts a sloppy or wildcarded redirect target, an attacker can craft a request that funnels the code to <code>evil.com</code> and impersonate the user.</p><div class=\"key\">SSO = authenticate once at a trusted identity provider, access many apps via tokens. Built on SAML, OAuth 2.0, and OpenID Connect. The frontend pitfall is the redirect: validate <code>redirect_uri</code> against a strict, exact-match, no-wildcard allowlist.</div>","related":["idn","rfc","sni"]},
  "hsts": {"term":"HSTS","group":"FE-SEC","aliases":["HTTP Strict Transport Security","Strict-Transport-Security"],"eli5":"A response header that tells a browser to only ever talk to your site over HTTPS for a set period, never plain HTTP.","memory":"HSTS = HTTP Strict Transport Security — 'strictly' HTTPS, no exceptions.","full":"<p><strong>HSTS</strong> (HTTP Strict Transport Security) is a security policy delivered via the <code>Strict-Transport-Security</code> HTTP response header. Once a browser sees it, it remembers (for the duration of <code>max-age</code>, in seconds) that the site must be accessed <em>only</em> over HTTPS. Any future attempt to load the origin over <code>http://</code> is rewritten by the browser to <code>https://</code> internally, before any network request leaves the machine — so there is no plaintext request for an attacker to intercept or downgrade.</p><p>HSTS was standardized as <strong>RFC 6797</strong> in November 2012 by the IETF, building on earlier work (notably the ForceHTTPS research by Collin Jackson and Adam Barth). The <code>includeSubDomains</code> directive extends the policy to every subdomain; the <code>preload</code> directive opts the domain into the browser-maintained <em>HSTS preload list</em> (run via hstspreload.org and shipped inside Chrome, Firefox, Safari, and Edge), so HTTPS is enforced even on the very first visit, before any header is ever seen.</p><p>Its purpose is to defeat <strong>SSL-stripping</strong> and protocol-downgrade attacks, where a man-in-the-middle intercepts the initial plain-HTTP request (or a redirect) and keeps the victim on an unencrypted connection. It remains a default hardening header today because that first-request gap is otherwise unclosable; a typical production value is <code>max-age=31536000; includeSubDomains; preload</code> (one year). It must be sent as a real HTTP header — a <code>&lt;meta&gt;</code> tag equivalent is ignored by browsers.</p>","worked":"<p>Server response header (set once over HTTPS, honored thereafter):</p><ul><li><code>Strict-Transport-Security: max-age=31536000; includeSubDomains; preload</code></li></ul><p>Express example:</p><p><code>res.setHeader('Strict-Transport-Security', 'max-age=31536000; includeSubDomains; preload')</code></p><p>After the first HTTPS visit, typing <code>http://example.com</code> never hits the network as plaintext — the browser silently upgrades it to <code>https://</code>.</p>","teach":"<p>At its core, HSTS is just a sticky note your server hands the browser that says: <em>'from now on, only ever talk to me over HTTPS — for the next N seconds, don't even try plain HTTP.'</em> The browser writes it down and obeys, rewriting any <code>http://</code> URL for your domain into <code>https://</code> before a single byte leaves the device.</p><div class=\"analogy\">Imagine a members-only club that, after your first verified entry through the secure front door, stamps your hand. From then on the bouncer won't let you sneak in the unlocked back alley door — even if a stranger points you toward it. The stamp (the HSTS policy) is what stops you being lured onto an insecure path.</div><p>Why does this matter? The classic attack is <strong>SSL stripping</strong>. You type <code>example.com</code> (no protocol), the browser tries <code>http://</code> first, and an attacker on the same Wi-Fi intercepts that plaintext request and quietly proxies an unencrypted version — you never reach real HTTPS, and they read everything. A redirect from HTTP to HTTPS doesn't save you, because the attacker controls that first hop.</p><p>HSTS closes the gap by moving the decision <em>inside</em> the browser: once the policy is cached, there is no plaintext request to hijack. The remaining weakness is the very first visit (before any header is seen) — which is why the <code>preload</code> list exists: browsers ship a hard-coded list of HSTS domains, so enforcement applies even on visit zero.</p><p>Key directives: <code>max-age</code> (how long to remember, in seconds), <code>includeSubDomains</code> (apply to <code>*.example.com</code> too), and <code>preload</code> (request inclusion in the baked-in browser list).</p><div class=\"key\">HSTS turns 'please use HTTPS' (a redirect an attacker can strip) into 'you may ONLY use HTTPS' (a browser-enforced rule). It must be a real HTTP header, and it only works once seen — so combine it with the preload list to cover the first visit.</div>","related":["xfo","clickjacking"]},
  "xfo": {"term":"X-Frame-Options","group":"FE-SEC","aliases":["XFO","X-Frame-Options header"],"eli5":"A response header that controls whether your page is allowed to be embedded inside an &lt;iframe&gt; on another site, preventing clickjacking.","memory":"XFO = X-Frame-Options — 'can you Frame me or not?'","full":"<p><strong>X-Frame-Options</strong> (abbreviated <strong>XFO</strong>) is an HTTP response header that tells the browser whether the current page may be rendered inside a framing context such as <code>&lt;iframe&gt;</code>, <code>&lt;frame&gt;</code>, <code>&lt;embed&gt;</code>, or <code>&lt;object&gt;</code>. Its accepted values are <code>DENY</code> (never allow framing) and <code>SAMEORIGIN</code> (allow only when the framing page shares the page's origin). An older <code>ALLOW-FROM uri</code> value existed but is obsolete and inconsistently supported.</p><p>XFO originated as a non-standardized, vendor-introduced header — first shipped in Internet Explorer 8 around 2009 and quickly adopted by other browsers — to combat clickjacking before any formal standard existed. It was later documented in <strong>RFC 7034</strong> (2013). The modern, standardized successor is the Content-Security-Policy <code>frame-ancestors</code> directive, which is more flexible (it accepts a source list, supports multiple allowed origins, and supersedes <code>ALLOW-FROM</code>). When both are present, browsers honor <code>frame-ancestors</code>.</p><p>Its purpose is anti-clickjacking defense: stopping a malicious site from invisibly embedding your page and tricking users into clicking. XFO is still widely shipped today for backward compatibility with older browsers, typically alongside a CSP <code>frame-ancestors</code> directive that does the same job more robustly. Critically, XFO only works as a real HTTP response header — it is ignored when set as a <code>&lt;meta&gt;</code> tag.</p>","worked":"<p>Send as a response header to forbid all framing:</p><ul><li><code>X-Frame-Options: DENY</code></li><li><code>X-Frame-Options: SAMEORIGIN</code></li></ul><p>Modern equivalent via CSP (preferred, and more granular):</p><p><code>Content-Security-Policy: frame-ancestors 'self' https://trusted-partner.example;</code></p><p>This <em>cannot</em> be reliably done in markup — <code>&lt;meta http-equiv=\"X-Frame-Options\" content=\"DENY\"&gt;</code> is ignored by browsers.</p>","teach":"<p>At its core, X-Frame-Options is just a yes/no answer your server gives to one question: <em>'is another website allowed to put my page inside a frame?'</em> If you say <code>DENY</code>, the browser refuses to render your page in any iframe at all.</p><div class=\"analogy\">Think of clickjacking like a fake storefront. An attacker builds an innocent-looking page — 'Click here to win!' — and stretches a transparent, invisible iframe of <em>your real banking site</em> over the button. The victim thinks they're clicking 'win', but they're actually clicking 'Transfer funds' on your site, with their own logged-in session. XFO is your site declaring 'you may not put me behind your fake glass.'</div><p>Why a header and not markup? Framing is decided <em>at load time</em>, by the browser, before your page's HTML even runs. A <code>&lt;meta&gt;</code> tag lives inside the page body — far too late, and easily stripped. The browser only trusts the HTTP response header, which arrives with the response itself.</p><p>The two values that matter: <code>DENY</code> (nobody frames me, not even me) and <code>SAMEORIGIN</code> (only pages from my own origin may frame me — useful if your own app embeds itself). The old <code>ALLOW-FROM</code> is dead; if you need to allow specific external partners, that's exactly what CSP <code>frame-ancestors</code> is for.</p><p>Today XFO is the legacy belt and CSP <code>frame-ancestors</code> is the modern suspenders. Seniors ship both: <code>frame-ancestors</code> for current browsers (granular, standardized) and <code>X-Frame-Options</code> for any older client that doesn't understand CSP. A sandboxed iframe on the attacker's side can't save you here — only <em>your</em> server header is authoritative.</p><div class=\"key\">XFO answers 'can you frame me?' as a real HTTP header. It defends against clickjacking; <code>frame-ancestors</code> is its more powerful modern replacement, and you usually ship both.</div>","related":["clickjacking","hsts"]},
  "cms": {"term":"CMS","group":"FE-SEC","aliases":["Content Management System"],"eli5":"A system that lets non-developers create, edit, and publish web content — often producing rich HTML that a front-end then has to render safely.","memory":"CMS = Content Management System: it 'manages content' so editors publish without touching code.","full":"<p>A <strong>CMS (Content Management System)</strong> is software that lets people create, organize, edit, and publish digital content — typically web pages and articles — without writing code for each piece. Editors work in a rich-text or structured interface, and the CMS stores the content and serves it to a front end. CMSs range from traditional all-in-one platforms (like WordPress) to <em>headless</em> CMSs that expose content purely through an API for a separate front end to render.</p><p>Content management systems emerged through the late 1990s and 2000s as the web grew beyond hand-coded pages and organizations needed non-technical staff to publish at scale. The category has since split: traditional coupled CMSs render their own pages, while modern <em>headless</em> CMSs (which rose to prominence in the 2010s alongside JAMstack and component frameworks) deliver content as JSON over an API to React, Vue, or other front ends.</p><p>Its purpose is to separate content authoring from engineering, empowering editors and enforcing workflow, versioning, and structure. CMSs remain ubiquitous because most of the web's content is editor-authored, not hard-coded. For front-end developers the key consequence is <em>rendering trust</em>: a CMS rich-text field arrives as real HTML markup, so displaying it means deliberately injecting markup (e.g. React's <code>dangerouslySetInnerHTML</code>) — which must be paired with sanitization, since unsanitized CMS or user HTML is a classic XSS vector.</p>","worked":"<p>Rendering a CMS rich-text field in React requires the deliberate markup opt-out — and sanitization first:</p><ul><li><code>import DOMPurify from 'dompurify';</code></li><li><code>const clean = DOMPurify.sanitize(cmsField.bodyHtml);</code></li><li><code>return &lt;div dangerouslySetInnerHTML={{ __html: clean }} /&gt;;</code></li></ul><p>The <code>dangerouslySetInnerHTML</code> prop is intentionally awkwardly named to flag the risk: you are injecting raw markup. Sanitizing the CMS output first strips any <code>&lt;script&gt;</code> or event-handler attributes that an attacker might have slipped into a content field.</p>","teach":"<p>At its core, a CMS is just <strong>a publishing tool for people who shouldn't have to write code.</strong></p><p>Imagine a news site with a hundred writers. You can't ask each of them to hand-edit HTML files and redeploy the site every time they fix a typo. Instead you give them a CMS: a friendly editor where they type, add images, bold things, and hit Publish. The CMS stores that content and hands it to the website. Modern setups often go 'headless' — the CMS becomes just an API that returns the content as data, and a separate front end (React, say) decides how to display it.</p><div class=\"analogy\">Think of a CMS like a print shop's order desk. Authors bring in their writing; the desk handles formatting, versioning, scheduling, and proofs; the press just prints what the desk approves. The writers never touch the printing machinery. But here's the catch — if the desk lets a prankster slip a booby-trapped instruction into the copy, the press will faithfully print the booby trap too. The desk must inspect submissions, not blindly forward them.</div><p>That catch is the whole front-end concern. When a CMS rich-text field comes back, it's not plain text — it's real HTML markup (bold tags, links, embeds). To show it you have to inject markup into the page, which frameworks make deliberately conspicuous: React calls it <code>dangerouslySetInnerHTML</code> precisely so you stop and think. The danger is XSS (cross-site scripting): if some content contains a <code>&lt;script&gt;</code> tag or a malicious <code>onerror</code> attribute and you inject it raw, that code runs in your users' browsers. The rule is to <em>sanitize</em> the HTML (with a library like DOMPurify) before injecting it, stripping scripts and event handlers while keeping the safe formatting.</p><div class=\"key\">A CMS lets non-developers publish content, often as rich HTML. Rendering that HTML means deliberately injecting markup (<code>dangerouslySetInnerHTML</code>) — always sanitize CMS/user HTML first, because raw injection is a textbook XSS hole.</div>","related":["html","api"]}
});
