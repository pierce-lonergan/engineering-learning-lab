/* Adaptive Query Execution (SPARK-AQE) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"SPARK-AQE","front":"What is Adaptive Query Execution (AQE) and when does it run?","back":"AQE is a Spark SQL framework that <strong>re-optimizes the physical plan at runtime</strong> using <em>actual</em> statistics gathered from completed shuffle stages, rather than relying only on compile-time estimates. It runs at <code>QueryStage</code> boundaries — after each shuffle (exchange) materializes its <code>MapStatus</code> metrics, AQE re-plans the remaining stages. Enabled by <code>spark.sql.adaptive.enabled</code>, which is <code>true</code> by default since Spark 3.2.0 (GA in 3.0 but off by default)."},
  {"sec":"SPARK-AQE","front":"Why does AQE wait for shuffle/exchange boundaries to re-optimize?","back":"An <strong>Exchange</strong> (shuffle) is a natural materialization point: map tasks write shuffle files and report <code>MapStatus</code> with per-partition byte sizes. AQE consumes these real sizes to make decisions the optimizer couldn't make accurately at compile time (estimates are often wrong by orders of magnitude on small/skewed data). The plan is split into <code>QueryStage</code>s at exchanges, executed bottom-up, and re-optimized between stages."},
  {"sec":"SPARK-AQE","front":"How does AQE coalesce shuffle partitions, and what config controls it?","back":"After a shuffle, AQE merges many small adjacent partitions into fewer, right-sized ones targeting roughly <code>spark.sql.adaptive.advisoryPartitionSizeInBytes</code> (default <code>64MB</code>). Controlled by <code>spark.sql.adaptive.coalescePartitions.enabled</code> (default true). The number of post-shuffle partitions becomes data-dependent instead of fixed at <code>spark.sql.shuffle.partitions</code> (default 200), so you can set that high and let AQE shrink it. Coalescing applies to any shuffle — joins, aggregations, and window functions alike."},
  {"sec":"SPARK-AQE","front":"Why does AQE reduce the need to hand-tune <code>spark.sql.shuffle.partitions</code>?","back":"Without AQE you must pick one static partition count that's a compromise across all stages and data sizes — too few causes spill/OOM, too many causes scheduler overhead and tiny tasks. With coalescing, you set <code>spark.sql.shuffle.partitions</code> to a high <em>upper bound</em> and AQE collapses it per-stage to hit the advisory size, so the same setting works across vastly different input sizes and skew profiles. Note AQE only merges <em>down</em> from the initial count — it can't add partitions back, so don't set it too low."},
  {"sec":"SPARK-AQE","front":"How does AQE convert a sort-merge join to a broadcast join at runtime?","back":"If a join's build side, after its shuffle materializes, is measured under <code>spark.sql.adaptive.autoBroadcastJoinThreshold</code> (defaults to the value of <code>spark.sql.autoBroadcastJoinThreshold</code>, i.e. <code>10MB</code>), AQE replaces the planned <code>SortMergeJoin</code> with a <code>BroadcastHashJoin</code>. This uses the <em>actual</em> shuffle output size, so it catches small sides the optimizer over-estimated (e.g., after a selective filter or aggregation) and couldn't broadcast at plan time. AQE can also demote to a shuffled-hash join when broadcast isn't viable."},
  {"sec":"SPARK-AQE","front":"What is the local shuffle reader and why does AQE add it after a join demotion?","back":"When AQE switches a sort-merge join to a broadcast hash join, the shuffle on the streamed (probe) side is no longer needed for co-partitioning. AQE inserts a <em>local shuffle reader</em> (config <code>spark.sql.adaptive.localShuffleReader.enabled</code>, default true) so each task reads shuffle blocks <strong>locally from the same mapper</strong> instead of fetching a full remote shuffle, avoiding the network cost the new plan made unnecessary. In Spark 3.2+ EXPLAIN it surfaces as an <code>AQEShuffleRead</code> node flagged <code>local</code>."},
  {"sec":"SPARK-AQE","front":"How does AQE handle a skew join, and what configs govern the split?","back":"AQE flags a skewed partition when its size exceeds BOTH <code>spark.sql.adaptive.skewJoin.skewedPartitionFactor</code> (default 5) times the <em>median</em> partition size AND <code>spark.sql.adaptive.skewJoin.skewedPartitionThresholdInBytes</code> (default <code>256MB</code>). It then <strong>splits</strong> that partition into sub-partitions (each targeting <code>advisoryPartitionSizeInBytes</code>) and replicates the matching partition on the other side, so the heavy key is processed by several tasks in parallel. Gated by <code>spark.sql.adaptive.skewJoin.enabled</code> (default true)."},
  {"sec":"SPARK-AQE","front":"Why can AQE skew-split only happen for shuffled joins, not broadcast joins?","back":"Skew splitting works by subdividing a shuffle partition's map outputs and replicating the other side's matching partition. This requires both sides to be <strong>shuffled and co-partitioned</strong>, which is the case for <code>SortMergeJoin</code> and shuffle-hash join. A broadcast join has no shuffle to split, so a hot key there is instead mitigated by broadcasting the small side entirely (no reduce-side concentration)."},
  {"sec":"SPARK-AQE","front":"What is Dynamic Partition Pruning (DPP) and how does it differ from AQE?","back":"DPP prunes partitions of a large <em>partitioned</em> fact table at runtime using values derived from the filtered <em>dimension</em> side of a join — typically a star-schema join where a filter on the small dimension becomes a dynamic <code>InSubquery</code> filter pushed to the fact scan. It is a <strong>separate</strong> feature (<code>spark.sql.optimizer.dynamicPartitionPruning.enabled</code>, default true) injected during logical/physical planning and applied at the fact-table scan — not AQE runtime re-planning at shuffle boundaries."},
  {"sec":"SPARK-AQE","front":"What are the preconditions for Dynamic Partition Pruning to fire?","back":"<ul><li>The large table must be <strong>partitioned</strong> on the join key.</li><li>It's an equi-join, and the other (filtered) side is broadcastable so its surviving keys can be computed cheaply and reused as a pruning filter (Spark reuses the join's <code>BroadcastExchange</code>).</li><li>There must be a selective filter on the dimension side to make pruning worthwhile.</li></ul>If the dimension side can't be broadcast there's no exchange to reuse, so DPP generally won't fire."},
  {"sec":"SPARK-AQE","front":"Does AQE work for non-shuffle (e.g. RDD or scan-only) plans?","back":"No. AQE re-optimization is driven by <strong>Exchange/shuffle boundaries</strong> — a query with no shuffle (e.g., a map-only scan-filter-project, or pure RDD operations) has no <code>QueryStage</code> boundaries to re-plan at, so AQE has nothing to adapt. AQE benefits joins, aggregations, and window functions that introduce shuffles."},
  {"sec":"SPARK-AQE","front":"What did AQE improve over the old <code>spark.sql.adaptive</code> from Spark 1.6/2.x?","back":"Spark 2.x had a limited experimental adaptive execution that only adjusted the post-shuffle reducer count via a single heuristic, was off by default, and was fragile. Spark 3.0's AQE is a re-architecture: it re-applies the physical optimizer (plus a few logical rules like demoting joins and propagating empty relations) to not-yet-run stages, enabling coalescing, dynamic join switching, and skew handling together — not just reducer counts."},
  {"sec":"SPARK-AQE","front":"How does AQE interact with reused exchanges and broadcast finalization?","back":"AQE materializes each <code>QueryStage</code> and can <strong>reuse</strong> identical exchanges (<code>ReusedExchange</code>) discovered during re-planning. Because it has real sizes, it makes better reuse and broadcast decisions — including <em>avoiding</em> a planned broadcast if the side turns out too large (preventing driver OOM). Plans are finalized per stage, so <code>EXPLAIN</code> shows <code>AdaptiveSparkPlan isFinalPlan=true</code> only after execution; pre-execution it shows placeholder query stages."},
  {"sec":"SPARK-AQE","front":"How do you confirm AQE actually changed the plan, and read its decisions?","back":"Run the query, then inspect the final plan: the root node is <code>AdaptiveSparkPlan</code> with <code>isFinalPlan=true</code>, and you'll see operators like <code>AQEShuffleRead coalesced</code> (or <code>local</code>), a flipped <code>BroadcastHashJoin</code> where a <code>SortMergeJoin</code> was planned, or skewed readers. (In Spark 3.0/3.1 this reader node was named <code>CustomShuffleReader</code>; it was renamed to <code>AQEShuffleRead</code> in 3.2.) The Spark UI SQL tab annotates these; <code>isFinalPlan=false</code> means it hasn't executed yet."},
  {"sec":"SPARK-AQE","front":"What are common reasons AQE doesn't kick in or doesn't help?","back":"<ul><li><code>spark.sql.adaptive.enabled=false</code> (or pre-3.2 default off).</li><li>No shuffle in the query — nothing to adapt.</li><li>Advisory size mis-set so coalescing makes partitions too large (spill) or too small.</li><li>Broadcast demotion blocked because the build side still exceeds the threshold, or the join type is unsupported.</li><li>Skew split unsupported for the join (e.g. broadcast join, or join types where a side can't be replicated).</li><li>Sources where even runtime metrics mislead after wide transformations.</li></ul>"}
);

Object.assign(GLOSSARY_DATA, {
  "query-stage": {"term":"QueryStage","group":"SPARK-AQE","aliases":["query stage","querystage","adaptive query stage"],"eli5":"A chunk of the plan up to the next shuffle that Spark runs, measures, then uses to decide what to do next.","memory":"Think of QueryStages as checkpoints on a relay — Spark can change the next runner after seeing the last leg's time.","full":"<p>AQE breaks the physical plan into <code>QueryStage</code>s delimited by <code>Exchange</code> (shuffle) and <code>BroadcastExchange</code> nodes. Stages are materialized bottom-up; the root operator is an <code>AdaptiveSparkPlan</code>. After a stage's shuffle writes complete, its statistics flow up and AQE re-optimizes the not-yet-materialized portion before launching the next stage.</p><p>This is why <code>EXPLAIN</code> on an un-run adaptive query shows placeholder stages and <code>isFinalPlan=false</code>; the plan literally isn't decided yet. Only after execution does <code>AdaptiveSparkPlan isFinalPlan=true</code> reflect the actual operators chosen.</p>","trap":"Candidates say AQE 'optimizes the whole plan at runtime.' It optimizes incrementally, one stage boundary at a time — it cannot un-execute a stage that already ran.","related":["adaptive-query-execution","coalesce-shuffle-partitions","dynamic-join-switching"]},
  "coalesce-shuffle-partitions": {"term":"Coalesce Shuffle Partitions","group":"SPARK-AQE","aliases":["partition coalescing","coalesce partitions","coalescePartitions"],"eli5":"After a shuffle, Spark globs together the many tiny output partitions into fewer chunks of a sensible size.","memory":"Coalesce = 'merge down to 64MB-ish bricks.' Set the partition count high; AQE shrinks it.","full":"<p>Governed by <code>spark.sql.adaptive.coalescePartitions.enabled</code> (default true). Spark starts with up to <code>spark.sql.shuffle.partitions</code> (default 200) reduce partitions, then merges contiguous ones so each combined partition approaches <code>spark.sql.adaptive.advisoryPartitionSizeInBytes</code> (default 64MB). The result is a data-dependent reducer count rather than a fixed one. It applies to any shuffle — joins, aggregations, window functions.</p><p>This eliminates the classic tuning headache where 200 partitions is far too many for a small post-filter result (hundreds of empty/tiny tasks) but too few for a huge one. You now set <code>spark.sql.shuffle.partitions</code> as a generous upper bound and let coalescing right-size each stage.</p>","trap":"Coalescing only <em>merges</em> adjacent partitions — it never <em>splits</em> a too-big one. Splitting is the job of skew handling. So a single huge skewed partition is not fixed by coalescing.","worked":"200 shuffle partitions, total ~300MB output → AQE coalesces to ~5 partitions of ~60MB each instead of 200 tiny ones, cutting scheduler overhead.","related":["advisory-partition-size","shuffle-partitions-tuning","adaptive-query-execution","skew-join-handling"]},
  "advisory-partition-size": {"term":"Advisory Partition Size","group":"SPARK-AQE","aliases":["advisoryPartitionSizeInBytes","advisory partition size","target partition size"],"eli5":"The 'how big should each partition be' target Spark aims for when merging or splitting shuffle partitions.","memory":"Advisory = 'aim for 64MB bricks.' It's a target, not a hard cap.","full":"<p><code>spark.sql.adaptive.advisoryPartitionSizeInBytes</code> (default 64MB) is the target size AQE uses when coalescing post-shuffle partitions, and also the granularity for splitting skewed partitions. It's <em>advisory</em>: AQE merges partitions until adding another would overshoot, so actual sizes hover around the target, not exactly on it.</p><p>Tuning it trades task count against task size: a larger advisory size means fewer, bigger tasks (less scheduling overhead, more memory/spill risk); a smaller one means more parallelism but more overhead. It interacts with <code>spark.sql.adaptive.coalescePartitions.minPartitionSize</code> and <code>minPartitionNum</code>, which floor how aggressively Spark can collapse.</p>","trap":"People assume advisory size guarantees partition size — it doesn't. It also doesn't apply to map-side reads; it governs post-shuffle (reduce-side) sizing.","related":["coalesce-shuffle-partitions","shuffle-partitions-tuning","skew-join-handling"]},
  "dynamic-join-switching": {"term":"Dynamic Join Switching","group":"SPARK-AQE","aliases":["join strategy switch","sort-merge to broadcast","runtime join conversion","demote join"],"eli5":"If a join side turns out small after shuffling, Spark swaps the heavy sort-merge join for a fast broadcast join.","memory":"'Measure, then maybe broadcast.' The shuffle already happened — the win is skipping the sort-merge, plus a local read.","full":"<p>At runtime, once a join input's shuffle materializes, AQE compares its real size to <code>spark.sql.adaptive.autoBroadcastJoinThreshold</code> (which defaults to the value of <code>spark.sql.autoBroadcastJoinThreshold</code>, 10MB). If under threshold, the planned <code>SortMergeJoin</code> is rewritten to a <code>BroadcastHashJoin</code>, and a local shuffle reader is inserted on the other side to avoid a now-pointless remote shuffle fetch. AQE may also pick a shuffled-hash join when broadcast isn't viable.</p><p>This catches sides the static optimizer mis-sized — e.g., a table that's large on disk but tiny after a selective filter or aggregation. Conversely, AQE can also avoid a planned broadcast if the side turns out too large, preventing driver OOM from an over-optimistic broadcast.</p>","trap":"The shuffle write on the demoted side has already occurred, so AQE saves the sort+merge and the remote fetch, not the original shuffle write. Demotion only happens at or after the shuffle boundary — not before any data moves.","worked":"Plan: SortMergeJoin of orders ⋈ filtered_customers. customers filtered to 4MB at runtime < 10MB → AQE flips to BroadcastHashJoin, broadcasting the 4MB side.","related":["adaptive-query-execution","local-shuffle-reader","autobroadcast-threshold","skew-join-handling"]},
  "local-shuffle-reader": {"term":"Local Shuffle Reader","group":"SPARK-AQE","aliases":["local shuffle reader","local reader","localShuffleReader","LocalShuffleReader"],"eli5":"When a join becomes a broadcast, each task reads shuffle blocks from its own machine instead of fetching them across the network.","memory":"Broadcast flip → 'read your own shuffle files, no network hop.'","full":"<p>When AQE demotes a sort-merge join to a broadcast hash join, the streamed (probe) side no longer needs to be re-partitioned to match the build side. A local shuffle reader (config <code>spark.sql.adaptive.localShuffleReader.enabled</code>, default true) lets each task read the shuffle output produced by mappers <strong>on the same executor/node locally</strong>, eliminating the cross-network shuffle fetch the original plan required.</p><p>In Spark 3.2+ it shows up in the final plan/UI as an <code>AQEShuffleRead</code> node flagged <code>local</code> (the standalone <code>CustomShuffleReader</code> name from 3.0/3.1 was renamed). Seeing it is a tell that AQE switched a join.</p>","trap":"A local shuffle reader is a consequence of join demotion, not an independent knob you flip on per-query. Seeing it in EXPLAIN signals AQE converted a join.","related":["dynamic-join-switching","adaptive-query-execution"]},
  "skew-join-handling": {"term":"Skew Join Handling","group":"SPARK-AQE","aliases":["skew join","skewed partition split","skewJoin","skew mitigation"],"eli5":"Spark spots one giant partition (a hot key), splits it into pieces, and copies the matching side so several tasks share the load.","memory":"'Split the fat partition, copy the other side.' Trigger = bigger than 5× median AND > 256MB.","full":"<p>Enabled by <code>spark.sql.adaptive.skewJoin.enabled</code> (default true). A partition is 'skewed' if its size exceeds both <code>skewedPartitionFactor</code> × the <em>median</em> partition size (factor default 5) and <code>skewedPartitionThresholdInBytes</code> (default 256MB). AQE splits such a partition into sub-partitions targeting <code>advisoryPartitionSizeInBytes</code> (≈64MB by default) and <strong>replicates</strong> the corresponding partition on the other join side so each split joins independently.</p><p>This only works for shuffled, co-partitioned joins — <code>SortMergeJoin</code> and shuffle-hash join — because splitting relies on the shuffle's partition layout. It directly attacks the 'one straggler task does 90% of the work' pathology from a hot join key.</p>","trap":"Both conditions (median-factor AND absolute byte threshold) must hold — a partition 10× the median but only 50MB is NOT treated as skewed. And it can't fix skew in a broadcast join (no shuffle to split).","worked":"Median reduce partition 40MB; one is 4GB (>5× median and >256MB). AQE splits it into roughly 4GB/64MB ≈ 60+ sub-partitions of ~64MB and replicates the matching right-side partition for each.","related":["coalesce-shuffle-partitions","advisory-partition-size","dynamic-join-switching","adaptive-query-execution"]},
  "dynamic-partition-pruning": {"term":"Dynamic Partition Pruning (DPP)","group":"SPARK-AQE","aliases":["dpp","dynamic partition pruning"],"eli5":"Before scanning a huge partitioned table, Spark uses the filtered values from the small joined table to skip irrelevant partitions.","memory":"DPP = 'let the small table tell the big table which partitions to skip.' Needs a partitioned fact + broadcastable dimension.","full":"<p>DPP (<code>spark.sql.optimizer.dynamicPartitionPruning.enabled</code>, default true) targets star-schema joins where a large <strong>partitioned</strong> fact table is joined to a filtered dimension table. The <code>PartitionPruning</code> rule injects a dynamic <code>InSubquery</code> filter from the dimension side; at physical planning <code>PlanDynamicPruningFilters</code> reuses the join's <code>BroadcastExchange</code> so the surviving keys are pushed into the fact scan, which reads only matching partitions.</p><p>It is distinct from AQE re-planning: DPP is a planner optimization applied at the scan, not at shuffle QueryStage boundaries. It can deliver order-of-magnitude scan reductions when a small, selective dimension filter prunes most fact partitions.</p>","trap":"Don't conflate DPP with AQE — they're separate features (both shipped in 3.0 but governed by different configs; DPP fires at the scan, not at shuffle boundaries). DPP needs the fact table physically partitioned on the join key, and typically a broadcastable dimension whose exchange it can reuse.","worked":"sales (partitioned by date) ⋈ date_dim WHERE date_dim.quarter='Q4'. DPP pushes the surviving date keys to prune the sales scan to only Q4 partitions.","related":["dynamic-join-switching","adaptive-query-execution"]},
  "shuffle-partitions-tuning": {"term":"spark.sql.shuffle.partitions Tuning","group":"SPARK-AQE","aliases":["spark.sql.shuffle.partitions","shuffle partitions","200 partitions default"],"eli5":"The old fixed knob for reducer count that you had to guess; AQE makes it just an upper bound.","memory":"Post-AQE mantra: 'Set it high, let AQE shrink it.' 200 is just the legacy default.","full":"<p><code>spark.sql.shuffle.partitions</code> (default 200) sets the number of reduce partitions for shuffles. Pre-AQE it was a single static value that had to compromise across every stage and data size in a job — a notorious tuning pain that caused either tiny-task overhead or spill/OOM.</p><p>With AQE coalescing on, you set it as a generous <strong>upper bound</strong> (often higher than 200 for big jobs) and let AQE collapse each stage to the advisory size. This decouples performance from getting one magic number right, which is the headline operational win of AQE.</p>","trap":"AQE does not change the <em>initial</em> shuffle partition count — it only coalesces downward from it. If you set it too low, AQE can't add partitions back, so you can still under-parallelize. Keep it high.","related":["coalesce-shuffle-partitions","advisory-partition-size","adaptive-query-execution"]},
  "aqe-shuffle-read": {"term":"AQEShuffleRead","group":"SPARK-AQE","aliases":["aqeshuffleread","aqe shuffle read","customshufflereader","custom shuffle reader"],"eli5":"The plan node that proves AQE touched a shuffle — it reports whether partitions were coalesced, read locally, or split for skew.","memory":"'AQEShuffleRead = AQE's fingerprint on a shuffle.' Old name (3.0/3.1) was CustomShuffleReader.","full":"<p><code>AQEShuffleRead</code> is the reduce-side reader node AQE inserts over a shuffle query stage. Its flags tell you which optimization fired: <code>coalesced</code> (small partitions merged), <code>local</code> (local shuffle reader after a broadcast conversion), or a skew indication (split partitions). It appears under the <code>AdaptiveSparkPlan</code> wrapper in <code>EXPLAIN</code> and the Spark UI SQL tab.</p><p>This node was named <code>CustomShuffleReader</code> in Spark 3.0 and 3.1 and renamed to <code>AQEShuffleRead</code> in Spark 3.2 (SPARK-36217) because the old name read like a public API. On a deck pinned to Spark 3.2+, expect <code>AQEShuffleRead</code> in plans.</p>","trap":"Citing <code>CustomShuffleReader</code> for current Spark is a version slip — it's <code>AQEShuffleRead</code> since 3.2. And it's a reader annotation, not a knob you enable directly.","related":["adaptive-query-execution","coalesce-shuffle-partitions","local-shuffle-reader"]}
});
