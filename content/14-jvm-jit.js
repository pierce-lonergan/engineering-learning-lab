/* JIT Compilation & Execution (JVM-JIT) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"JVM-JIT","front":"What is the full execution pipeline of a Java program, from source to running machine code?","back":"<code>javac</code> compiles <code>.java</code> source to portable <strong>bytecode</strong> (<code>.class</code> files). At runtime the JVM <strong>interprets</strong> that bytecode immediately. The interpreter also <em>profiles</em> execution; HotSpot's <strong>JIT</strong> then compiles frequently-run (\"hot\") methods to native machine code on a background compiler thread. So the same method may run interpreted, then C1-compiled, then C2-compiled over its lifetime."},
  {"sec":"JVM-JIT","front":"Why does the JVM ship bytecode instead of native code, and what does that cost at runtime?","back":"Bytecode is platform-independent (\"write once, run anywhere\") and compact, so one artifact runs on any JVM. The cost is that it must be <strong>interpreted</strong> or <strong>JIT-compiled</strong> at runtime rather than executing directly on the CPU — trading startup latency and warmup for portability plus the ability to optimize using <em>runtime</em> profile data a static AOT compiler never has."},
  {"sec":"JVM-JIT","front":"How does HotSpot decide a method is \"hot\" enough to JIT-compile? Which two counters drive it?","back":"Each method carries an <strong>invocation counter</strong> (how many times it's called) and a <strong>back-edge counter</strong> (incremented on each backward jump, i.e. per loop iteration). When a counter crosses a threshold the method is queued for compilation. Back-edge counters are why a method with one huge loop gets compiled (via OSR) even though it was invoked only once."},
  {"sec":"JVM-JIT","front":"What is On-Stack Replacement (OSR) and when does HotSpot use it?","back":"OSR compiles a method <em>while a long-running loop is still executing on the stack</em> and swaps the running frame for compiled code mid-flight. It's triggered by the <strong>back-edge counter</strong>, not invocation count, so a method called once but looping millions of times still gets compiled. OSR code is specialized to a loop back-edge entry point and is often slightly less optimal than a method compiled normally at its standard entry."},
  {"sec":"JVM-JIT","front":"What are the 5 levels of HotSpot tiered compilation (0–4)?","back":"<ul><li><strong>0</strong> — interpreter (with profiling counters)</li><li><strong>1</strong> — C1, <em>no</em> profiling; used for <em>trivial</em> methods (getters/setters/constant returns) that can't benefit from profiling</li><li><strong>2</strong> — C1 with light profiling; mainly a fallback used when the C2 queue is full, to get code compiled fast</li><li><strong>3</strong> — C1 with <em>full</em> profiling</li><li><strong>4</strong> — C2, aggressively optimized</li></ul>The typical hot path is 0→3→4."},
  {"sec":"JVM-JIT","front":"Why does tiered compilation use C1 (level 3) as a stepping stone before C2 (level 4) instead of jumping straight to C2?","back":"C2 needs a rich <strong>profile</strong> (branch frequencies, receiver types, null-ness) to optimize aggressively. Running level-3 C1 code gathers that profile while already delivering a large speedup over the interpreter. Going interpreter→C2 directly would mean either slow startup (interpret long enough to profile) or a blind C2 compile with no profile. Tiering gives <em>both</em> fast warmup and a good profile for peak."},
  {"sec":"JVM-JIT","front":"C1 vs C2: what is each optimized for, and how do they differ in cost and output quality?","back":"<strong>C1 (client)</strong> compiles fast with modest optimization — good for quick warmup and short-lived code; in tiered mode it also inserts profiling instrumentation. <strong>C2 (server)</strong> is the aggressive optimizing compiler (SSA-based; does inlining, escape analysis, loop transforms) producing the fastest code, but compiles slowly and uses more CPU/memory. Tiered compilation runs both."},
  {"sec":"JVM-JIT","front":"How does method inlining enable downstream JIT optimizations, and what limits it?","back":"Inlining replaces a call with the callee's body, removing call overhead but more importantly <em>merging optimization scopes</em> — escape analysis, constant folding, and dead-code elimination can now see across the old boundary. It's gated by method bytecode size (e.g. <code>-XX:MaxInlineSize</code>, <code>FreqInlineSize</code>), inline depth, and whether the call site is <strong>monomorphic</strong>/bimorphic enough to devirtualize. Megamorphic virtual calls usually can't be inlined."},
  {"sec":"JVM-JIT","front":"What is escape analysis and what optimizations does it unlock?","back":"Escape analysis (C2) proves an object's reference never \"escapes\" the compiling method/thread. If it doesn't escape, C2 can: <strong>scalar-replace</strong> it — represent its fields as registers/stack slots instead of a heap object (eliminating the allocation and GC pressure); and <strong>lock-elide</strong> — drop synchronization on a thread-confined object. (Stack allocation is often described conceptually; HotSpot mainly realizes the win via scalar replacement.) It's why small short-lived objects in a hot loop are often free after warmup."},
  {"sec":"JVM-JIT","front":"What is lock elision, and what makes it safe?","back":"If <strong>escape analysis</strong> proves a locked object never escapes to another thread, its <code>synchronized</code> blocks are <strong>elided</strong> — the monitor enter/exit are removed because no other thread can contend. This is why a thread-confined <code>StringBuffer</code> can cost nothing in hot JIT'd code. This is distinct from the older <em>biased locking</em>, which was disabled by default in JDK 15 and removed in JDK 18."},
  {"sec":"JVM-JIT","front":"What is deoptimization, and name some assumptions whose invalidation triggers it.","back":"<strong>Deoptimization</strong> discards speculatively-compiled code and falls back to the interpreter (or a lower tier) when a <em>speculative assumption</em> proves false. Triggers include: a <strong>monomorphic call site</strong> seeing a new receiver class (Class Hierarchy Analysis invalidated by class loading), an <strong>uncommon trap</strong> on a branch the profile said was never taken, a null where non-null was assumed, or a class being loaded/redefined. The JVM typically recompiles with the corrected information."},
  {"sec":"JVM-JIT","front":"Concretely, how does a monomorphic-call-site assumption get invalidated and cause deopt?","back":"If a virtual call site has only ever seen one receiver type, C2 <strong>devirtualizes and inlines</strong> it, guarding with a class check (or relying on <strong>Class Hierarchy Analysis</strong> that no overriding subclass is loaded). If a new subclass is later loaded — or a different receiver actually arrives — the guard fails (an <strong>uncommon trap</strong>), the compiled frame is <strong>deoptimized</strong>, and execution resumes in the interpreter; the method is later recompiled as bi-/megamorphic."},
  {"sec":"JVM-JIT","front":"Why do naive Java micro-benchmarks lie, and what does JMH do about it?","back":"Before <strong>warmup</strong>, code runs interpreted or as half-baked C1, so timing measures the wrong thing; the JIT may also <strong>dead-code-eliminate</strong> a result you never consume, or constant-fold a loop-invariant input. <strong>JMH</strong> fixes this by running warmup iterations until steady state, forking fresh JVMs, and providing <code>Blackhole</code> / <code>@State</code> to consume results and defeat constant-folding so the optimizer can't delete the work being measured."},
  {"sec":"JVM-JIT","front":"What is JIT warmup and why does it matter for latency-sensitive and serverless workloads?","back":"<strong>Warmup</strong> is the interval during which a process runs interpreted/C1 while profiling accumulates and C2 catches up — early requests are far slower with a long tail. For long-lived services this is amortized to near-nothing, but for <strong>serverless / short-lived / scale-to-zero</strong> workloads the process may die before reaching peak, paying warmup cost on every cold start without recouping it — a key motivation for AOT."},
  {"sec":"JVM-JIT","front":"JIT vs AOT / GraalVM native-image: state the core tradeoff and when to pick each.","back":"<strong>JIT (HotSpot)</strong>: slow startup + warmup, but typically the highest <em>peak</em> throughput because it optimizes on real runtime profiles and can deoptimize. <strong>AOT / GraalVM native-image</strong>: compiles ahead of time under a <strong>closed-world assumption</strong>, giving millisecond startup and low memory (great for serverless/CLI/scale-to-zero) but generally lower peak (the gap narrows with PGO) and restrictions on reflection/dynamic class loading. Long-running data services favor JIT; bursty/short-lived favor AOT."}
);

Object.assign(GLOSSARY_DATA, {
  "tiered-compilation": {"term":"Tiered Compilation","group":"JVM-JIT","aliases":["tiered compilation","tiers","compilation levels"],"eli5":"The JVM ramps a method up through cheap-but-fast compilers to expensive-but-optimal ones as it learns the method is hot.","memory":"0 interpret, 1 = C1 trivial (no profile), 2 = C1 light (C2-busy fallback), 3 = C1 full profile, 4 = C2. Hot path: 0→3→4.","full":"<p>Tiered compilation (default since JDK 8, <code>-XX:+TieredCompilation</code>) blends HotSpot's two JITs across five levels: <strong>0</strong> interpreter, <strong>1</strong> C1 no-profile (trivial methods), <strong>2</strong> C1 light-profile, <strong>3</strong> C1 full-profile, <strong>4</strong> C2. A hot method typically flows 0→3→4: level-3 C1 code runs fast <em>and</em> gathers the rich profile C2 needs, then C2 produces peak code and the level-3 version is retired.</p><p>Thresholds are tunable: on common HotSpot builds (e.g. JDK 17) <code>Tier3CompileThreshold≈2000</code> and <code>Tier4CompileThreshold≈15000</code> invocations (verify with <code>-XX:+PrintFlagsFinal</code>; defaults vary by version/build). Level 2 exists mainly as a fallback: when the C2 compile queue is long, the JVM may route methods through level 2 (or downgrade level-3 candidates) to get them compiled sooner with lighter profiling overhead.</p>","trap":"Interviewers ask 'why not interpret then go straight to C2?' — because C2 needs a profile; level-3 C1 produces that profile cheaply while already running fast. It's about feeding C2 good data, not just speed. Also: level 1 is for trivial methods, NOT 'the most optimized C1'.","worked":"A request handler called ~2,000x crosses Tier3 → C1 full-profile compiles it; after ~15,000 calls C2 compiles it using the branch/type profile C1 collected, and the C1 version is retired.","related":["c1-c2-compilers","jit-compiler","interpreter-profiling","deoptimization"]},
  "jit-compiler": {"term":"JIT Compiler (HotSpot)","group":"JVM-JIT","aliases":["jit","just-in-time","jit compilation"],"eli5":"A compiler that turns hot bytecode into native machine code while the program runs, using what it has learned about actual execution.","memory":"JIT = 'compile Just In Time, using info AOT could never have' — the runtime profile is the superpower.","full":"<p>The Just-In-Time compiler converts frequently executed bytecode to optimized native code <em>during</em> execution, on a background compiler thread. Its decisive advantage over an ahead-of-time compiler is <strong>profile-guided, speculative optimization</strong>: it can assume a branch is never taken, a call site is monomorphic, or a value is non-null, and emit fast code guarded by cheap checks — then <strong>deoptimize</strong> if an assumption breaks.</p><p>This is why long-running JVM workloads reach very high peak throughput. The cost is <strong>warmup</strong>: code starts interpreted and slow, and the process must run long enough for compilation to pay off. HotSpot's JIT is split into C1 and C2 and orchestrated by tiered compilation.</p>","trap":"Don't say the JIT 'compiles the whole program.' It compiles only hot methods/loops; cold code stays interpreted. Compilation is per-method and lazy.","related":["tiered-compilation","c1-c2-compilers","deoptimization","warmup","interpreter-profiling"]},
  "c1-c2-compilers": {"term":"C1 and C2 Compilers","group":"JVM-JIT","aliases":["c1","c2","client compiler","server compiler"],"eli5":"C1 is the fast-but-shallow compiler for quick warmup; C2 is the slow-but-aggressive one for peak speed.","memory":"C1 = Client = Cheap & Quick; C2 = Server = Smart & Slow.","full":"<p><strong>C1 (client)</strong> compiles quickly with modest optimization and, in tiered mode, inserts the profiling instrumentation tiered compilation depends on — ideal for fast warmup and short-lived methods. <strong>C2 (server)</strong> is the heavyweight optimizer: SSA-based, it performs inlining, escape analysis, loop unrolling, range-check elimination, and speculative devirtualization, producing the fastest code but consuming far more compile time and memory.</p><p>Historically you chose one via <code>-client</code>/<code>-server</code>; modern JVMs run both under tiered compilation. C2 is the compiler whose speculative assumptions can be invalidated, causing deoptimization. (GraalVM can replace C2 with the Graal JIT, written in Java.)</p>","trap":"C1 is not 'unoptimized.' It emits real native code and does real optimization — it's just less aggressive than C2 and (in tiered mode) adds profiling hooks. The interpreter (level 0), not C1, is the truly slow tier.","related":["tiered-compilation","jit-compiler","inlining","escape-analysis","deoptimization"]},
  "interpreter-profiling": {"term":"Interpreter & Execution Profiling","group":"JVM-JIT","aliases":["interpreter","profiling","invocation counter","back-edge counter","osr"],"eli5":"The JVM runs bytecode directly at first while counting calls and loop iterations to find the hot spots worth compiling.","memory":"Two counters: invocations (how often called) + back-edges (loop trips). Back-edges → OSR.","full":"<p>Execution begins in the <strong>bytecode interpreter</strong> (level 0). To find hot code, HotSpot maintains per-method counters: an <strong>invocation counter</strong> (calls) and a <strong>back-edge counter</strong> (incremented on every backward branch, i.e. loop iteration). Crossing a threshold queues the method for JIT compilation.</p><p>The back-edge counter enables <strong>On-Stack Replacement (OSR)</strong>: a method stuck in a long loop is compiled and the live interpreter frame is swapped for compiled code <em>mid-execution</em>, so a method invoked once but looping millions of times still gets optimized. Beyond counters, level-3 C1 collects type and branch profiles that C2 later consumes.</p>","trap":"A method called only once is NOT necessarily interpreted forever — if it contains a hot loop, the back-edge counter triggers OSR. OSR code is also typically slightly less optimal than a normal (standard-entry) compilation. Forgetting OSR is a classic gotcha.","worked":"<code>main()</code> runs a 10-million-iteration loop. Invocation count = 1, but back-edges blow past the threshold → OSR compiles the loop while it's still running.","related":["jit-compiler","tiered-compilation","warmup"]},
  "inlining": {"term":"Method Inlining","group":"JVM-JIT","aliases":["inlining","inline","devirtualization"],"eli5":"The compiler pastes a called method's body into the caller, removing the call and letting it optimize across the old boundary.","memory":"Inlining's real payoff isn't 'no call' — it's 'optimizer can now see through the wall.'","full":"<p>Inlining is the JIT's most valuable optimization — not mainly because it removes call overhead, but because it <strong>merges optimization scopes</strong>, exposing the callee's body to constant folding, escape analysis, and dead-code elimination in the caller's context. C2 inlines aggressively, bounded by bytecode size (<code>MaxInlineSize</code>, <code>FreqInlineSize</code>), inline depth, and the compile budget.</p><p>Virtual/interface calls must first be <strong>devirtualized</strong>: using the call-site profile or Class Hierarchy Analysis, a <strong>monomorphic</strong> (one receiver type) or bimorphic site is turned into a guarded direct call and then inlined. <strong>Megamorphic</strong> sites (many receiver types) generally can't be inlined and stay as virtual dispatch.</p>","trap":"'Java can't inline virtual methods' is wrong. The JIT devirtualizes monomorphic/bimorphic call sites (often via CHA) and inlines them — guarded so it can deopt if a new type appears. Only megamorphic sites resist inlining.","related":["c1-c2-compilers","escape-analysis","deoptimization","jit-compiler"]},
  "deoptimization": {"term":"Deoptimization","group":"JVM-JIT","aliases":["deopt","deoptimization","uncommon trap"],"eli5":"When a speculative compiled optimization turns out to be wrong, the JVM throws the fast code away and drops back to the interpreter.","memory":"Speculate boldly, guard cheaply, deopt safely. Deopt is the JIT's undo button.","full":"<p>C2 emits code under <strong>speculative assumptions</strong> drawn from the profile — a branch is never taken, a call site is monomorphic, a value is non-null, no overriding subclass exists (Class Hierarchy Analysis). Each assumption is protected by a cheap guard or an <strong>uncommon trap</strong>. When an assumption is violated — a new subclass is loaded, an 'impossible' branch fires, a class is redefined — the JVM <strong>deoptimizes</strong>: it reconstructs the interpreter state from the compiled frame, discards the compiled code, and resumes interpreting; the method is later recompiled with the corrected profile.</p><p>Deoptimization is what makes aggressive speculation <em>safe</em>, and it's the structural reason a JIT can out-optimize an AOT compiler that must be conservative. Excessive deopt churn, though, shows up as a performance problem (visible via <code>-XX:+PrintCompilation</code>).</p>","trap":"Deoptimization is a normal, healthy mechanism — not an error. The gotcha is calling it a failure; the real failure mode is a deopt <em>storm</em> (repeated invalidate/recompile), e.g. a call site oscillating between monomorphic and polymorphic.","worked":"A call site sees only <code>ArrayList</code>, so C2 inlines <code>ArrayList.add</code> with a type guard. Later a <code>LinkedList</code> arrives → guard fails → uncommon trap → deopt to interpreter → recompile as bimorphic.","related":["jit-compiler","c1-c2-compilers","inlining","tiered-compilation","jit-vs-aot"]},
  "warmup": {"term":"JIT Warmup & JMH","group":"JVM-JIT","aliases":["warmup","warm-up","jmh","microbenchmark"],"eli5":"Java code starts slow and gets fast only after the JIT has watched it run for a while — so you must let it warm up before timing it.","memory":"'Measure cold, measure a lie.' JMH = warm it up, fork it, and Blackhole the result so the JIT can't cheat.","full":"<p><strong>Warmup</strong> is the period during which a process runs interpreted then C1 while profiles accumulate and C2 catches up; early execution is much slower with a long tail. For long-lived services this cost is amortized to near-nothing, but it dominates short-lived and serverless workloads that die before reaching peak.</p><p>Warmup makes naive micro-benchmarks meaningless: you may measure interpreted code, or the JIT may <strong>dead-code-eliminate</strong> an unused result or constant-fold a loop-invariant input. <strong>JMH</strong> (Java Microbenchmark Harness) is the correct tool — it runs warmup iterations to steady state, forks fresh JVMs, and supplies <code>Blackhole</code>/<code>@State</code> so results are consumed and inputs aren't folded, preventing the optimizer from deleting the work you're trying to measure.</p>","trap":"A hand-rolled <code>System.nanoTime()</code> loop is the classic bad benchmark: dead-code elimination can delete the loop body entirely, reporting absurd nanosecond timings. Always cite JMH and dead-code elimination together.","related":["jit-compiler","tiered-compilation","interpreter-profiling","jit-vs-aot"]},
  "jit-vs-aot": {"term":"JIT vs AOT / GraalVM Native Image","group":"JVM-JIT","aliases":["aot","native image","graalvm","jit vs aot","closed-world"],"eli5":"JIT trades slow startup for the highest peak speed; AOT native images trade peak speed for near-instant startup and low memory.","memory":"JIT = high ceiling, slow climb. AOT = low(er) ceiling, instant launch. Serverless picks the instant launch.","full":"<p><strong>JIT (HotSpot)</strong> compiles at runtime on real profiles and can deoptimize, so it generally reaches the highest <em>peak</em> throughput — but pays startup + warmup. <strong>AOT / GraalVM native-image</strong> compiles everything ahead of time under a <strong>closed-world assumption</strong> (all reachable classes/methods known at build time), producing a standalone executable with millisecond startup and low memory footprint, but typically lower peak and no runtime reprofiling/deopt. Profile-guided optimization (PGO) for native image narrows the peak gap substantially and, in some web benchmarks, can match or edge out the JIT.</p><p>The closed-world assumption restricts reflection, dynamic proxies, JNI, and runtime class loading, which must be declared via configuration. Choose AOT for <strong>serverless, CLIs, and scale-to-zero</strong> where cold-start and memory dominate; choose JIT for <strong>long-running data services</strong> where steady-state throughput dominates. (Project Leyden and CRaC are emerging middle grounds.)</p>","trap":"Native image is NOT 'just a faster JVM.' It trades peak throughput and dynamic features (reflection/proxies need config) for startup and memory. Saying it strictly beats the JVM is a red flag — and so is claiming a fixed peak gap; with PGO the gap is workload-dependent and can vanish.","worked":"A Spring Boot service: multi-second JVM startup vs well under 100 ms as a native image, with far lower memory — but often a few percent lower sustained throughput under hours of load (less with PGO).","related":["jit-compiler","warmup","deoptimization"]}
});
