/* Event-Driven & Messaging Patterns (ARCH-EVENTS) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"ARCH-EVENTS","front":"Event vs command: what's the semantic and coupling difference?","back":"A <strong>command</strong> is imperative (<code>ReserveInventory</code>) — it names a single intended recipient and expects it to act; the sender knows who handles it and is coupled to that handler. An <strong>event</strong> is a past-tense fact (<code>OrderPlaced</code>) — it states something that happened, names no recipient, and the producer doesn't know or care who consumes it. A command can be <em>rejected</em> (the handler refuses the request); an event states a fact that already happened, so it can't be invalidated — a consumer can only ignore it (or dead-letter the message). The arrow of coupling flips: with commands the sender depends on the receiver; with events the receiver depends on the producer."},
  {"sec":"ARCH-EVENTS","front":"What are the three event styles, ordered by how much data they carry?","back":"<ul><li><strong>Event notification</strong> — thin; just \"<code>OrderPlaced</code> id=123\". Consumer calls back to the source for details.</li><li><strong>Event-carried state transfer</strong> — fat; the event carries the order payload so consumers never call back.</li><li><strong>Event sourcing</strong> — the event stream <em>is</em> the system of record; current state is a fold over all past events.</li></ul>They trade off coupling, payload size, and source load against consumer autonomy and replayability. (Fowler's taxonomy also lists CQRS as a fourth, related pattern.)"},
  {"sec":"ARCH-EVENTS","front":"Event notification: what's the hidden coupling cost despite its thin payload?","back":"The thin event minimizes payload and producer commitment, but it creates a <strong>temporal + behavioral coupling</strong>: every consumer that needs details must synchronously call back to the source to fetch them, so the producer's API becomes a runtime dependency on the hot path. You also lose the ability to reason about a flow from the message log alone — the notification says \"something changed\" but the actual data lived elsewhere and was fetched out-of-band at a later time, making debugging and replay hard."},
  {"sec":"ARCH-EVENTS","front":"Event-carried state transfer: what does it buy you and what's the main hazard?","back":"It buys <strong>consumer autonomy and read availability</strong> — consumers keep a local replica built from events and can serve queries even if the producer is down, eliminating callback chatter. The hazard is <strong>stale local copies and data duplication</strong>: every consumer now holds a fork of the data that's eventually consistent, fatter messages increase bandwidth, and you must handle reordering/duplicates idempotently so a late or replayed event doesn't overwrite newer state."},
  {"sec":"ARCH-EVENTS","front":"Event sourcing: what is the system of record, and how is current state obtained?","back":"The <strong>append-only log of events is the system of record</strong> — you never update rows in place, you append <code>MoneyDeposited</code>, <code>MoneyWithdrawn</code>, etc. Current state is derived by <strong>replaying (folding) events</strong> from the start, usually accelerated by periodic <strong>snapshots</strong> so you fold only events since the last snapshot. This gives a complete audit trail and temporal queries (\"what was the balance on date X\") at the cost of added complexity and a query-hostile store."},
  {"sec":"ARCH-EVENTS","front":"Why is event sourcing almost always paired with CQRS?","back":"The event log is optimized for <em>writes</em> (append-only, replayable), but it's terrible for ad-hoc <em>queries</em> — you can't <code>SELECT … WHERE balance > 100</code> against a stream of deltas. <strong>CQRS</strong> separates the write model (events) from one or more read models (<strong>projections</strong>) that are materialized views built by consuming the event stream. The read side is eventually consistent with the write side, and you can spin up brand-new projections by replaying history from the start."},
  {"sec":"ARCH-EVENTS","front":"What is eventual consistency the price of, and how do you make it acceptable to the business?","back":"It's the price of <strong>decoupling</strong> — once a producer fires an event and returns, downstream state converges only after consumers process it, so there's a window where replicas disagree. You make it acceptable by (1) keeping the <em>command</em> side strongly consistent within its own aggregate boundary, (2) surfacing the lag honestly in UX (\"pending\"/\"processing\"), and (3) choosing aggregate boundaries so anything needing read-your-writes lives inside one transactional consistency boundary, not across an event hop."},
  {"sec":"ARCH-EVENTS","front":"What is the transactional outbox pattern and which failure does it prevent?","back":"It prevents the <strong>dual-write problem</strong>: you cannot atomically commit to your DB <em>and</em> publish to a broker in two separate steps, so a crash between them either loses the event or publishes a phantom. The outbox writes the event row into an <code>outbox</code> table <em>in the same local DB transaction</em> as the business change; a separate relay (poller or CDC tail of the log, e.g. Debezium on the WAL) reads the table and publishes to the broker, marking rows sent. Atomicity is restored because both writes share one ACID transaction — no 2PC required."},
  {"sec":"ARCH-EVENTS","front":"Outbox relay delivers at-least-once — what's the architectural consequence for consumers?","back":"Because the relay can crash after publishing but before marking the outbox row sent, the same event can be published more than once. Therefore <strong>at-least-once delivery is the contract</strong>, and the architectural consequence is non-negotiable: <strong>every consumer must be idempotent</strong>. Exactly-once end-to-end is a fiction across process boundaries; you achieve effectively-once by combining at-least-once delivery with idempotent consumption."},
  {"sec":"ARCH-EVENTS","front":"Name three concrete techniques for building an idempotent consumer.","back":"<ul><li><strong>Dedup table / inbox / processed-message log</strong>: store the event/message id, reject on duplicate (insert under a unique constraint).</li><li><strong>Natural idempotency</strong>: design the operation as a set/upsert keyed by a business id, so reapplying it is a no-op (<code>SET status='PAID'</code> vs <code>balance += x</code>).</li><li><strong>Expected-version / optimistic check</strong>: the event carries the source aggregate version; apply only if it advances local state, otherwise drop as stale or duplicate.</li></ul>The dedup record and the side effect must commit in the <em>same</em> transaction, or you reopen a dual-write hole."},
  {"sec":"ARCH-EVENTS","front":"Why does a global total order across a topic not scale, and what guarantee do you actually get?","back":"A single total order requires a single serialization point (one partition / one consumer), which caps throughput and availability. Real systems give <strong>per-key (per-partition) ordering only</strong>: messages sharing a partition key are delivered in order relative to each other, but there's no ordering guarantee <em>across</em> keys. The design rule is to pick a partition key that matches your consistency unit — e.g. <code>accountId</code> — so all events for one aggregate stay ordered while different aggregates parallelize."},
  {"sec":"ARCH-EVENTS","front":"How do you evolve an event schema without breaking existing consumers?","back":"Prefer <strong>additive, compatible</strong> changes: add <em>optional</em> fields with defaults; avoid changing a field's type or semantics. Under a registry's <code>BACKWARD</code> mode you may add fields-with-defaults and delete fields; <code>FORWARD</code> allows adding fields and deleting optional ones; <code>FULL</code> is the intersection (add/remove optional fields only). The pragmatic team rule is \"add optional, don't remove or repurpose,\" enforced via a schema registry compatibility mode in CI. For genuinely breaking changes, publish a <strong>new event version/type</strong> (<code>OrderPlaced.v2</code>) and run both in parallel (or upcast v1→v2 on read) — you can't coordinate a synchronous cutover across independently deployed consumers."},
  {"sec":"ARCH-EVENTS","front":"What is an 'upcaster' and where does it sit in an event-sourced system?","back":"An <strong>upcaster</strong> transforms an old event version into the current shape <em>at read time</em>, as events are loaded from the store before being applied to the aggregate or projections. Because the event log is immutable, you don't migrate stored events in place — you keep the original bytes and chain upcasters (v1→v2→v3) during hydration. This lets the domain code only ever see the latest schema while old history stays untouched and auditable."},
  {"sec":"ARCH-EVENTS","front":"Why is a poison message / dead-letter strategy mandatory in event-driven systems?","back":"With ordered per-key delivery, a single un-processable message (bad schema, business invariant violation) <strong>blocks the entire partition behind it</strong> — head-of-line blocking stalls every later event for that key. You need a <strong>dead-letter queue (DLQ)</strong> plus a bounded retry/backoff policy: after N failed attempts, route the poison message to the DLQ so the consumer can advance. Without this, one bad event can freeze a whole aggregate's stream indefinitely."},
  {"sec":"ARCH-EVENTS","front":"When should you NOT go event-driven?","back":"Avoid it when the interaction is fundamentally <strong>request/response with a result the caller must have synchronously</strong> (e.g. \"is this card valid right now?\"), when you need <strong>strong read-your-writes consistency</strong> across the boundary, or when a <strong>simple monolith / direct call</strong> would do and the team can't absorb the operational tax (idempotency, ordering, schema registry, DLQs, distributed debugging). Event-driven trades local simplicity and consistency for decoupling and scale — if you don't need the decoupling, you're paying the tax for nothing."},
  {"sec":"ARCH-EVENTS","front":"In a Spring Modulith / hexagonal payment system, how do domain events differ from integration events?","back":"<strong>Domain events</strong> are in-process, fine-grained, and module-internal — published on the application's event bus (e.g. Spring's <code>ApplicationEventPublisher</code>) and consumed by other modules in the same deployable, typically within or just after the same transaction. <strong>Integration events</strong> cross the deployment boundary onto a broker, are coarser, versioned, and form a published contract with external services. The architectural discipline is to <em>not</em> leak internal domain events onto the wire — translate them to a stable integration-event schema at the edge."}
);

Object.assign(GLOSSARY_DATA, {
  "event-vs-command": {"term":"Event vs Command","group":"ARCH-EVENTS","aliases":["event","command","events vs commands"],"eli5":"A command tells one specific service to do something; an event announces that something already happened and lets anyone react.","memory":"Command = imperative mood (do X), aimed at one. Event = past tense (X happened), aimed at no one.","full":"<p>A <strong>command</strong> is an imperative request (<code>CapturePayment</code>) addressed to a known handler that may accept or reject it; the sender is coupled to the receiver and expects an outcome. An <strong>event</strong> is an immutable past-tense fact (<code>PaymentCaptured</code>) with no addressee — the producer broadcasts it and is indifferent to who, if anyone, consumes it.</p><p>The key architectural property is the <em>direction of coupling</em>: commands couple sender→receiver, events couple receiver→producer. An event states a fact that already happened, so it can't be invalidated — a consumer can only ignore it (or dead-letter the message). Choosing the right verb mood at each boundary is a design decision, not a transport detail.</p>","trap":"Calling a message an 'event' while it's really a disguised command (e.g. <code>ShouldChargeCustomer</code>). If exactly one consumer must act and the producer cares about the result, it's a command — naming it an event just hides the coupling and tempts a second consumer to hang off it and break invariants.","worked":"<code>OrderPlaced</code> (event) fans out to inventory, billing, and email independently. Compare <code>ReserveInventory</code> (command): order service knows inventory must succeed and waits on it.","related":["event-notification","event-carried-state-transfer"]},
  "event-notification": {"term":"Event Notification","group":"ARCH-EVENTS","aliases":["notification event","thin event"],"eli5":"A tiny 'something changed, go look it up' ping that carries only an id, not the data.","memory":"Notification = a doorbell: it tells you someone's there, you still have to open the door to see who.","full":"<p>The producer emits a minimal event — typically an aggregate id and event type — and consumers that care call back to the source's API to fetch the details they need. Payloads and producer commitment stay tiny, and the producer doesn't have to anticipate what data consumers want.</p><p>The cost is a <strong>callback dependency on the hot path</strong>: the source becomes a runtime dependency for every interested consumer, and the flow is no longer reconstructable from the message log alone, because the meaningful data was fetched out-of-band at a later time. Good when consumers rarely need details, or the data is large and seldom used.</p>","trap":"Interviewers probe the callback storm: a fan-out of N consumers each calling back turns one event into N synchronous reads against the producer, often reintroducing exactly the coupling and load you adopted events to avoid.","related":["event-vs-command","event-carried-state-transfer","event-sourcing"]},
  "event-carried-state-transfer": {"term":"Event-Carried State Transfer","group":"ARCH-EVENTS","aliases":["ECST","fat event","state transfer event"],"eli5":"The event carries a full copy of the data so consumers never have to call back — they just keep a local replica.","memory":"Notification ships the address; ECST ships the whole package. Fat event = no callbacks, but N stale copies.","full":"<p>The producer puts the relevant state <em>inside</em> the event (the order, the customer record), so each consumer maintains its own eventually-consistent local copy and serves queries without ever contacting the source. This maximizes consumer autonomy and read availability: a consumer can keep working while the producer is down.</p><p>The trade-offs are <strong>data duplication, fatter messages, and staleness</strong>. Every consumer holds a fork of the truth that lags the source, and out-of-order or duplicate delivery can clobber newer state unless you version events and apply idempotently. It's the natural fit when consumers query the data often and can tolerate lag.</p>","trap":"The 'who owns the truth?' gotcha: with N replicated copies, a senior interviewer wants to hear that the producer remains the single writer/system-of-record and consumers hold read-only derived copies — never let two services believe they own the same datum.","worked":"<code>CustomerUpdated</code> carries the full customer profile; the fraud service stores it locally and scores transactions even during a customer-service outage, accepting that it may be a few seconds stale.","related":["event-notification","eventual-consistency","idempotent-consumer","schema-evolution"]},
  "event-sourcing": {"term":"Event Sourcing","group":"ARCH-EVENTS","aliases":["event-sourced","event store"],"eli5":"Instead of storing current state, store every change as an event and rebuild state by replaying them.","memory":"Your bank statement is event sourcing: it stores deposits/withdrawals (events), not just today's balance — the balance is the fold.","full":"<p>The append-only event log is the <strong>system of record</strong>; state is never updated in place. Current state is a left-fold over the event history, accelerated by periodic <strong>snapshots</strong> so you only replay events since the last snapshot. You get a complete audit trail, temporal queries (state as-of any point), and the ability to derive brand-new read models by replaying from the beginning.</p><p>It's almost always paired with <strong>CQRS</strong> because the log is write-optimized and query-hostile — you materialize <em>projections</em> for reads. Costs are real: schema evolution via upcasters, eventual consistency on the read side, replay performance, and the cognitive load of a fundamentally different persistence model. Reserve it for domains where the audit trail and history <em>are</em> the value (ledgers, payments, compliance).</p>","trap":"Conflating event sourcing with 'we publish events to Kafka.' Publishing events ≠ sourcing from them. Event sourcing means the log is the authoritative store you rebuild state from — most systems that 'do events' still keep a mutable table as the truth.","worked":"An account aggregate replays <code>Opened, Deposited(100), Withdrew(30)</code> to compute balance=70; a snapshot at event 10,000 means a query folds only the ~50 events since, not all 10,050.","related":["cqrs-projection","schema-evolution","eventual-consistency","event-vs-command"]},
  "cqrs-projection": {"term":"CQRS & Projections","group":"ARCH-EVENTS","aliases":["cqrs","projection","read model","materialized view"],"eli5":"Split the write side (events) from the read side (query-shaped views) and keep the views in sync by consuming events.","memory":"Write model = the ledger; projections = whatever report you print from it. You can print a new report (projection) anytime by re-reading the ledger.","full":"<p><strong>CQRS</strong> (Command Query Responsibility Segregation) uses separate models for writes and reads. In event-driven/event-sourced systems the write model is the event stream, and one or more <strong>projections</strong> consume that stream to build read-optimized materialized views (a SQL table, a search index, a cache) each shaped for a specific query.</p><p>The read side is <em>eventually consistent</em> with the write side — there's a projection lag. The upside is independent scaling and the freedom to add or rebuild a projection at any time by replaying history. The discipline: projections are disposable derived data, never a second source of truth, and must be rebuildable deterministically from the log.</p>","trap":"Forgetting projection lag in the UX/API contract. After a command succeeds, the read model may not reflect it yet — interviewers want you to name read-your-writes handling (e.g. return the command result directly, or pin the user to the write side briefly).","related":["event-sourcing","eventual-consistency","ordering-per-key"]},
  "idempotent-consumer": {"term":"Idempotent Consumer","group":"ARCH-EVENTS","aliases":["idempotency","idempotent","dedup","deduplication"],"eli5":"Processing the same event twice produces the same result as processing it once.","memory":"At-least-once delivery + idempotent consumer = effectively once. You can't fix delivery; you fix the consumer.","full":"<p>Because reliable systems deliver <strong>at-least-once</strong> (outbox relays, broker redeliveries, consumer rebalances all replay messages), a consumer must produce the same outcome whether it sees an event once or five times. This is the load-bearing assumption that makes 'effectively once' achievable without distributed transactions.</p><p>Techniques: a <strong>dedup/inbox table</strong> with a unique constraint on the message id; <strong>natural idempotency</strong> via upserts keyed by a business id (set-style operations, not increments); or an <strong>expected-version check</strong> where the event carries the source version and you apply only if it advances local state. The dedup record and the side effect should commit together, or you reopen a dual-write hole.</p>","trap":"Idempotency that isn't atomic: marking the message processed in one transaction and doing the side effect in another. A crash between them either double-applies or silently drops — the dedup write and the effect must share one transaction.","worked":"On <code>PaymentCaptured</code>, insert <code>(message_id)</code> into <code>processed_events</code> with a PK constraint and update the ledger in the same tx; a duplicate hits the unique violation and the whole tx rolls back as a no-op.","related":["transactional-outbox","ordering-per-key","eventual-consistency"]},
  "ordering-per-key": {"term":"Per-Key Ordering","group":"ARCH-EVENTS","aliases":["ordering","partition key","ordering guarantee","per-partition ordering"],"eli5":"Messages with the same key arrive in order; messages with different keys have no order guarantee, so they can run in parallel.","memory":"Order is a per-key promise, never a global one. Partition key = the unit you need ordered = usually your aggregate id.","full":"<p>A global total order requires a single serialization point, which kills throughput and availability, so production systems offer <strong>ordering only within a partition/key</strong>. Events sharing a partition key are consumed in order relative to each other; events under different keys may interleave arbitrarily, which is exactly what lets you parallelize.</p><p>The design rule: choose a partition key equal to your consistency/aggregate unit (e.g. <code>accountId</code>) so all events for one aggregate stay ordered while distinct aggregates scale out. Beware <strong>head-of-line blocking</strong> — one stuck or poison message halts everything behind it on that key, which is why DLQs and bounded retries are mandatory companions.</p>","trap":"Assuming consumers see events in producer order. Across keys there is none; even within a key, a naive multi-threaded consumer that processes a partition concurrently destroys the ordering the broker handed you. Concurrency must be per-key, not per-message.","worked":"Key by <code>accountId</code> so <code>Deposited</code> then <code>Withdrawn</code> for one account stay ordered; two different accounts land on different partitions and process simultaneously with no cross-ordering needed.","related":["idempotent-consumer","cqrs-projection","eventual-consistency"]},
  "schema-evolution": {"term":"Event Schema Evolution","group":"ARCH-EVENTS","aliases":["schema versioning","event versioning","upcaster","schema registry"],"eli5":"Change event shapes over time without coordinating a synchronous upgrade of every producer and consumer.","memory":"BACKWARD (the Kafka default) lets new consumers replay old data. Breaking change = new version + parallel run, not an in-place migration of the log.","full":"<p>Events are an immutable, independently-deployed contract, so you can't do a synchronized cutover. The safest changes are <strong>additive</strong>: add optional fields with defaults and avoid retyping or repurposing fields. A <strong>schema registry</strong> compatibility mode enforces this in CI: <code>BACKWARD</code> (new-schema consumer reads old data — add fields-with-defaults, delete fields), <code>FORWARD</code> (old-schema consumer reads new data — add fields, delete optional ones), and <code>FULL</code> (the intersection: add/remove optional fields only). A bad schema fails the build, not production.</p><p>For genuinely breaking changes, version the event type (<code>OrderPlaced.v2</code>) and run old and new in parallel until consumers migrate, or use an <strong>upcaster</strong> that transforms old versions to the current shape <em>at read time</em> — essential in event sourcing where stored history is immutable and must never be rewritten.</p>","trap":"Treating a semantic change as compatible because the wire format still parses. Repurposing a field's <em>meaning</em> (e.g. <code>amount</code> from gross to net) passes registry checks but silently corrupts every consumer — compatibility is about bytes, not meaning, so meaning changes need a new version.","worked":"Event sourcing: store <code>OrderPlaced.v1</code> bytes forever; on load, an upcaster chain v1→v2 injects a new <code>currency</code> field defaulting to <code>USD</code>, so domain code only ever handles v2 while history stays untouched.","related":["event-sourcing","event-carried-state-transfer","idempotent-consumer"]}
});
