/* Streaming (Kinesis, MSK) (CLOUD-STREAM) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"CLOUD-STREAM","front":"Kinesis Data Streams: what is the unit of capacity and throughput?","back":"The <strong>shard</strong>. Each shard provides <strong>1 MB/s or 1,000 records/s ingest</strong> and <strong>2 MB/s egress</strong> (shared across standard consumers). Stream capacity = number of shards, so you scale by resharding (split/merge). In <code>on-demand</code> mode AWS manages shard count automatically; in <code>provisioned</code> mode you set and pay per shard-hour."},
  {"sec":"CLOUD-STREAM","front":"How does a record's partition key map to a shard in Kinesis Data Streams?","back":"Kinesis computes an <strong>MD5 hash of the partition key</strong>, producing a 128-bit value, and routes the record to the shard whose hash-key range contains it. Records sharing a partition key always land on the same shard and are therefore <strong>strictly ordered</strong> within that shard. A skewed key distribution creates a <strong>hot shard</strong> that throttles regardless of total stream capacity."},
  {"sec":"CLOUD-STREAM","front":"What ordering guarantee does Kinesis Data Streams provide — and at what granularity?","back":"Ordering is guaranteed <strong>per shard</strong> (equivalently, per partition key), not across the whole stream — identical to Kafka's per-partition ordering. Consumers read each shard via a <strong>shard iterator</strong> and get records in arrival order with monotonically increasing sequence numbers. There is no global total order across shards."},
  {"sec":"CLOUD-STREAM","front":"Standard consumers vs enhanced fan-out in Kinesis Data Streams","back":"<ul><li><strong>Standard</strong>: consumers <em>share</em> the shard's 2 MB/s egress via polling (<code>GetRecords</code>). More consumers = more contention and higher latency.</li><li><strong>Enhanced fan-out (EFO)</strong>: each registered consumer gets its own <strong>dedicated 2 MB/s per shard</strong> pushed via HTTP/2 (<code>SubscribeToShard</code>), ~70 ms latency. Costs extra per consumer-shard-hour plus per-GB data retrieval.</li></ul>"},
  {"sec":"CLOUD-STREAM","front":"What does the KCL (Kinesis Client Library) handle that raw GetRecords does not?","back":"The KCL manages <strong>shard discovery, lease assignment, load-balancing across worker instances, checkpointing, and resharding handoff</strong>. It stores leases and checkpoints in a <strong>DynamoDB table</strong> (one per app), so an under-provisioned lease table is a classic throttling source. KCL invokes your <code>processRecords</code> with an <strong>ordered batch of records per shard</strong> (not one at a time) and gives at-least-once delivery."},
  {"sec":"CLOUD-STREAM","front":"Kinesis Firehose: what is it and how does it differ from Data Streams?","back":"Firehose (Amazon Data Firehose) is a <strong>fully-managed delivery stream</strong> — you write records and it batches and delivers them to <strong>S3, Redshift, OpenSearch, Splunk, or HTTP endpoints</strong> with no consumer code to write or shards to manage. It is <strong>near-real-time (buffered), not real-time</strong>, and there is <strong>no consumer API and no replay</strong> — once delivered, data is gone from Firehose."},
  {"sec":"CLOUD-STREAM","front":"What controls Firehose delivery latency, and what are the buffering knobs?","back":"Delivery fires when <strong>either</strong> the buffer <strong>size</strong> or buffer <strong>interval</strong> threshold is hit. For S3 the size hint is <strong>1–128 MB</strong> (default 5) and the interval is <strong>0–900 s</strong> (default 300; 0 = zero-buffering). Smaller buffers = lower latency but more, smaller output objects (worse for downstream query cost). This buffering is why Firehose is near-real-time, not real-time."},
  {"sec":"CLOUD-STREAM","front":"What in-flight transforms can Firehose do before delivery?","back":"<ul><li><strong>Format conversion</strong>: JSON → <strong>Parquet or ORC</strong> using a Glue Data Catalog table for the schema (columnar = cheaper Athena/Redshift Spectrum scans).</li><li><strong>Record transformation</strong> via a <strong>Lambda</strong> function (enrich, filter, reformat).</li><li><strong>Dynamic partitioning</strong> into S3 prefixes (e.g. by date/tenant) using jq/Lambda-extracted keys.</li><li>GZIP/Snappy/Zip <strong>compression</strong>.</li></ul>"},
  {"sec":"CLOUD-STREAM","front":"Amazon MSK: what does 'managed' actually cover, and what stays your job?","back":"MSK runs <strong>real Apache Kafka</strong> — it provisions/patches brokers, handles AZ placement, and manages the control-plane metadata layer (<strong>KRaft</strong> on current versions; ZooKeeper on legacy clusters, removed in MSK 4.x+). <strong>You</strong> still own: topic/partition design, partition count, replication factor, consumer-group tuning, client configs, and (for provisioned) right-sizing broker instance types and EBS. Kafka APIs and the ecosystem (Connect, Streams, Schema Registry, MirrorMaker) work unchanged."},
  {"sec":"CLOUD-STREAM","front":"MSK Provisioned vs MSK Serverless — when do you pick each?","back":"<ul><li><strong>Provisioned</strong>: you choose broker count/type and storage; cheapest at steady high throughput; supports tiered storage and the full feature set.</li><li><strong>Serverless</strong>: no broker/capacity management, auto-scales, pay per throughput + storage; great for spiky or unknown load and dev. Trades cost-efficiency at scale and some feature limits (no tiered storage, per-partition throughput quotas) for zero ops.</li></ul>"},
  {"sec":"CLOUD-STREAM","front":"Kinesis Data Streams vs MSK: the core decision tradeoff","back":"<strong>Kinesis</strong> = managed simplicity, no servers, deep AWS-native integration (Lambda, Firehose, IAM), pay-per-shard — best when you want low ops and stay in AWS. <strong>MSK</strong> = real Kafka APIs, the Kafka ecosystem, higher/clustered throughput, and <strong>portability</strong> (avoid lock-in, lift-and-shift existing Kafka apps) — at the cost of running and tuning a cluster."},
  {"sec":"CLOUD-STREAM","front":"What are the data-retention limits for Kinesis Data Streams, and why does it matter for replay?","back":"Default retention is <strong>24 hours</strong>, extendable to <strong>365 days</strong> (extra cost beyond 24 h). Within that window consumers can replay by resetting the shard iterator (<code>AT_TIMESTAMP</code> / <code>TRIM_HORIZON</code>). <strong>Firehose has no retention/replay</strong>; MSK retention is configured per-topic (time and/or size based) just like open-source Kafka."},
  {"sec":"CLOUD-STREAM","front":"How does each AWS streaming service handle scaling/elasticity?","back":"<ul><li><strong>KDS provisioned</strong>: manual or API resharding (split/merge); shard count is the lever.</li><li><strong>KDS on-demand</strong>: auto-scales to ~double the prior 30-day peak write throughput (throttles if you exceed 2x within ~15 min of a new peak).</li><li><strong>Firehose</strong>: fully elastic, no capacity to manage.</li><li><strong>MSK provisioned</strong>: add brokers / grow EBS / change instance type (partitions don't auto-rebalance); <strong>Serverless</strong> auto-scales.</li></ul>"},
  {"sec":"CLOUD-STREAM","front":"Why can a Kinesis stream throttle even when total provisioned throughput looks sufficient?","back":"Because limits are <strong>per shard</strong>, not per stream. A <strong>hot partition key</strong> concentrates writes on one shard, hitting its 1 MB/s / 1,000 rec/s cap and returning <code>ProvisionedThroughputExceededException</code> while other shards sit idle. The fix is a higher-cardinality / better-distributed partition key (or on-demand mode), not just more shards."},
  {"sec":"CLOUD-STREAM","front":"Which AWS streaming choice for: 'land clickstream JSON into S3 as Parquet for Athena, no custom consumer'?","back":"<strong>Kinesis Data Firehose</strong>. It needs zero consumer code, buffers and writes directly to S3, does JSON→<strong>Parquet</strong> conversion via a Glue table, and supports dynamic partitioning by date — exactly the managed ELT-to-lake pattern. Reach for Data Streams or MSK only if you need real-time processing, replay, or multiple independent consumers off the same log."}
);

Object.assign(GLOSSARY_DATA, {
  "kinesis-data-streams": {"term":"Kinesis Data Streams (KDS)","group":"CLOUD-STREAM","aliases":["kinesis data streams","kds","kinesis stream","data streams"],"eli5":"A managed, sharded, append-only log — Kafka-lite that AWS runs for you.","memory":"Shard = Kafka partition. Stream throughput = (# shards) × (1 MB/s). Think 'Kafka with the cluster hidden.'","full":"<p>KDS is a durable, ordered, replayable record log. A stream is a set of <strong>shards</strong>; each shard is an ordered sub-log providing <strong>1 MB/s or 1,000 records/s in and 2 MB/s out</strong>. Producers attach a <strong>partition key</strong> per record; Kinesis MD5-hashes it to pick a shard, guaranteeing per-shard (per-key) ordering with monotonic sequence numbers. Capacity scales by changing shard count: <code>on-demand</code> (AWS auto-scales, pay per GB) or <code>provisioned</code> (you set shards, pay per shard-hour).</p><p>Consumers read via shard iterators — either DIY <code>GetRecords</code>, the <strong>KCL</strong> (which adds leasing/checkpointing in DynamoDB), or <strong>enhanced fan-out</strong> for dedicated per-consumer throughput. Records persist 24 h by default (up to 365 days), enabling replay. It's the right pick for real-time, multi-consumer, AWS-native pipelines where you don't want to run Kafka.</p>","trap":"Interviewers probe the hot-shard problem: limits are PER SHARD, so a skewed partition key throttles one shard (ProvisionedThroughputExceededException) no matter how much aggregate capacity you bought. Fix the key cardinality, not just the shard count.","worked":"1,000 records all keyed on the userId of one whale account → every record hashes to one shard → that shard caps at 1 MB/s while 50 others idle. Re-key on (userId, sessionId) to spread the load.","related":["shard","partition-key","enhanced-fan-out","kcl","kinesis-vs-msk"]},
  "shard": {"term":"Shard","group":"CLOUD-STREAM","aliases":["shard","shards","resharding","shard iterator"],"eli5":"One ordered slice of a Kinesis stream, with a fixed throughput budget — the atomic unit of capacity.","memory":"Shard math: in = 1 MB/s, out = 2 MB/s. The 2× egress exists so one consumer can read while another catches up.","full":"<p>A shard owns a contiguous range of the 128-bit MD5 hash space. It guarantees <strong>1 MB/s / 1,000 records/s ingest and 2 MB/s egress</strong>, ordered by sequence number. Total stream capacity is purely the number of shards, so you scale by <strong>resharding</strong>: <code>SplitShard</code> (one → two, more capacity) or <code>MergeShards</code> (two → one, less). Resharding creates new shard IDs and closes parents; the KCL handles the parent→child read handoff so ordering is preserved.</p><p>Each open shard is billed per shard-hour in provisioned mode, which makes over-provisioning a real cost trap. Enhanced fan-out multiplies egress by giving each consumer its own 2 MB/s per shard.</p>","trap":"Resharding isn't instantaneous or free of ordering nuance — parent shards stay readable until drained, and you pay for open shards. Don't assume you can double throughput for a microsecond burst; on-demand or pre-split is the answer for known spikes.","related":["kinesis-data-streams","partition-key","enhanced-fan-out"]},
  "enhanced-fan-out": {"term":"Enhanced Fan-Out (EFO)","group":"CLOUD-STREAM","aliases":["enhanced fan-out","efo","enhanced fanout","subscribetoshard"],"eli5":"Give each consumer its own private 2 MB/s pipe per shard, pushed over HTTP/2 instead of polling.","memory":"EFO = 'dedicated lane per consumer.' Standard = 'everyone shares the 2 MB/s off-ramp.'","full":"<p>By default, standard consumers <strong>share</strong> a shard's 2 MB/s egress and poll with <code>GetRecords</code>, so the 3rd or 4th consumer starves and latency climbs. EFO registers a consumer to receive a <strong>dedicated 2 MB/s per shard</strong> via <code>SubscribeToShard</code> (HTTP/2 server push), cutting latency to roughly <strong>70 ms</strong> and isolating consumers from each other.</p><p>It costs extra: per consumer-shard-hour plus per-GB data-retrieval charges. Use it when you have multiple independent real-time consumers on the same stream or need low, predictable latency; skip it for a single consumer or batch/Firehose-style sinks where polling is fine.</p>","trap":"EFO throughput is per-consumer-per-shard, but so is the BILL. Registering 5 EFO consumers on a 100-shard stream is 500 consumer-shard-hours of charges — easy to blow the budget for latency you may not need.","related":["shard","kinesis-data-streams","kcl"]},
  "kcl": {"term":"Kinesis Client Library (KCL)","group":"CLOUD-STREAM","aliases":["kcl","kinesis client library","lease table","checkpointing"],"eli5":"The AWS-built consumer framework that does shard leasing, load-balancing, and checkpointing for you so you don't hand-roll GetRecords.","memory":"KCL's brain lives in DynamoDB: one row per shard = (leaseOwner, checkpoint). Lose/throttle that table and consumption stalls.","full":"<p>The KCL turns raw <code>GetRecords</code> polling into a managed consumer: it does <strong>shard discovery</strong> (via <code>ListShards</code>/<code>DescribeStream</code>), assigns one <strong>lease</strong> per shard, load-balances leases across worker instances, and persists <strong>checkpoints</strong> — all in a per-application <strong>DynamoDB table</strong>. It hands your <code>processRecords</code> callback an <strong>ordered batch of records per shard</strong> and tracks the last checkpoint sequence number so a restarted worker resumes where it left off.</p><p>Delivery is <strong>at-least-once</strong> (records can re-deliver after a crash before checkpoint), so your processing must be idempotent. The lease/checkpoint table is itself a DynamoDB table you pay for and can under-provision — a classic hidden throttling source.</p>","trap":"At-least-once, not exactly-once — checkpoint happens after processing, so a crash mid-batch re-delivers. And the DynamoDB lease table needs its own capacity; an under-provisioned one throttles consumption even when the stream is healthy.","related":["kinesis-data-streams","shard","enhanced-fan-out"]},
  "kinesis-firehose": {"term":"Kinesis Data Firehose","group":"CLOUD-STREAM","aliases":["kinesis firehose","firehose","amazon data firehose","delivery stream"],"eli5":"A no-code pipe that buffers streaming records and dumps them into S3/Redshift/OpenSearch for you.","memory":"Firehose = 'fire and forget into S3.' If you ever need to re-read the data, you needed Data Streams, not Firehose.","full":"<p>Firehose is a <strong>fully-managed delivery stream</strong>: you <code>PutRecord</code> and it batches by <strong>size OR time</strong> then writes to S3, Redshift, OpenSearch, Splunk, or HTTP endpoints. There are <strong>no shards to manage and no consumer code</strong>. In flight it can convert JSON→<strong>Parquet/ORC</strong> (via a Glue schema), run a <strong>Lambda</strong> transform, do <strong>dynamic partitioning</strong> into S3 prefixes, and compress.</p><p>The catch: it is <strong>near-real-time, not real-time</strong> (buffer interval up to 900 s), and it has <strong>no retention, no replay, and no fan-out</strong> — delivered data leaves Firehose. It's the canonical 'stream-to-lake/warehouse ELT' button when you don't need multiple consumers or replay.</p>","trap":"Don't call Firehose real-time — the buffer interval (default 300 s, up to 900 s) means minutes of delay. And tiny buffers to chase low latency produce thousands of small S3 objects that wreck Athena scan cost.","worked":"Clickstream JSON → Firehose with 128 MB / 300 s buffer, JSON→Parquet via Glue, dynamic-partition by event_date → one tidy Parquet object per partition per ~5 min, Athena-ready.","related":["kinesis-data-streams","buffering-firehose","kinesis-vs-msk"]},
  "buffering-firehose": {"term":"Firehose Buffering (size/interval)","group":"CLOUD-STREAM","aliases":["firehose buffering","buffer hints","buffer size","buffer interval"],"eli5":"Firehose waits until it has enough data OR enough time, then flushes a batch — the dial that trades latency for file size.","memory":"'Whichever comes first' — size OR interval. Bigger buffer = bigger files = cheaper queries = slower data.","full":"<p>Each destination has two buffer hints: a <strong>size</strong> threshold (S3: 1–128 MB, default 5) and an <strong>interval</strong> threshold (0–900 s, default 300). Firehose flushes when <strong>either</strong> fires. Large buffers → fewer, bigger objects (cheap to query, higher latency); small buffers → low latency but many small files and more PUT/COPY operations.</p><p>This single tradeoff is why Firehose can't be 'real-time' and why it's a frequent cost trap: query engines like Athena and Redshift Spectrum hate small files. Tune buffers toward the largest your latency SLA allows, and let format conversion to Parquet plus compression shrink the footprint further.</p>","trap":"The buffer is a HINT, not a hard cap — Firehose raises it dynamically when delivery falls behind ingest, and format conversion or Lambda transforms change effective object sizes. Never assume your output file is exactly the configured size.","related":["kinesis-firehose","kinesis-data-streams"]},
  "msk": {"term":"Amazon MSK (Managed Streaming for Apache Kafka)","group":"CLOUD-STREAM","aliases":["msk","amazon msk","managed kafka","msk serverless","msk provisioned"],"eli5":"Real Apache Kafka with AWS running the brokers — full Kafka APIs and ecosystem, more knobs than Kinesis.","memory":"MSK = 'bring your Kafka skills to AWS.' Kinesis hides the cluster; MSK gives you the cluster (managed).","full":"<p>MSK runs genuine open-source Kafka, so every Kafka API, client, and ecosystem tool works unchanged: Kafka Connect, Streams, Schema Registry, MirrorMaker 2. AWS provisions and patches brokers, handles AZ spread, and runs the metadata layer — <strong>KRaft</strong> on current Kafka versions (ZooKeeper on legacy clusters; ZooKeeper-mode is removed from MSK 4.x onward). It comes in <strong>Provisioned</strong> (pick broker type/count + EBS, cheapest at steady scale, tiered storage available) and <strong>Serverless</strong> (auto-scaling, pay per throughput, zero capacity ops).</p><p>You still own topic/partition/replication design and client tuning. Choose MSK over Kinesis when you need Kafka portability (avoid lock-in / migrate existing apps), the Kafka ecosystem, or very high clustered throughput — accepting the extra operational surface.</p>","trap":"'Managed' ≠ 'serverless' for MSK Provisioned — you still right-size brokers and EBS, and partitions DON'T auto-rebalance when you add brokers (you run reassignment). Don't promise effortless elasticity unless it's MSK Serverless.","related":["kinesis-vs-msk","kinesis-data-streams"]},
  "kinesis-vs-msk": {"term":"Kinesis vs MSK (decision)","group":"CLOUD-STREAM","aliases":["kinesis vs msk","kinesis vs kafka","msk vs kinesis","streaming service choice"],"eli5":"Kinesis trades flexibility for zero-ops AWS-native simplicity; MSK trades ops for real Kafka and portability.","memory":"Three-way fork: need a consumer + replay + AWS-native → Kinesis; need Kafka ecosystem/portability → MSK; need just 'dump to S3' → Firehose.","full":"<p><strong>Kinesis Data Streams</strong>: no servers, shard-based capacity, native hooks into Lambda/Firehose/IAM, fastest to stand up, AWS-only API (lock-in). Best when the team wants minimal ops and lives entirely in AWS. <strong>MSK</strong>: real Kafka protocol + ecosystem, higher and more flexible throughput via partitions/brokers, and <strong>portability</strong> off AWS — at the cost of running, tuning, and right-sizing a cluster.</p><p>Rules of thumb: choose Kinesis for greenfield AWS-native, multi-consumer real-time with low ops; choose MSK to lift-and-shift existing Kafka, use Connect/Streams/Schema Registry, or need throughput/feature control. For pure deliver-to-S3-no-consumer, neither — use Firehose.</p>","trap":"The lazy answer 'Kafka is always faster/better' loses points. Senior answer weighs ops cost, lock-in vs portability, ecosystem need, and integration depth — and recognizes Firehose as the right tool when no real consumer logic exists.","worked":"Team already runs Kafka Connect + Schema Registry on-prem and wants cloud → MSK (lift-and-shift). New AWS-only team wants events → Lambda with replay → Kinesis Data Streams. Just archiving logs to S3 as Parquet → Firehose.","related":["kinesis-data-streams","msk","kinesis-firehose"]}
});
