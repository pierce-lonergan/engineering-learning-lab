/* Data Services (Glue, EMR, Athena) (CLOUD-DATA) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"CLOUD-DATA","front":"What problem does the Glue Data Catalog actually solve?","back":"It is a serverless, <strong>Hive-metastore-compatible</strong> central metadata store: table schemas, column types, partition lists, and S3 locations. It decouples <em>metadata</em> from <em>compute</em> so Athena, EMR (Spark/Hive/Presto), Redshift Spectrum, and Lake Formation can all query the same S3 data through one shared catalog instead of each tool maintaining its own metastore. The data stays in S3 (open formats); the Catalog just describes it."},
  {"sec":"CLOUD-DATA","front":"What does a Glue Crawler do, and what's the standard gotcha?","back":"A crawler scans an S3 prefix (or JDBC source), <strong>infers schema and partitions</strong>, and writes/updates table definitions in the Glue Catalog. Gotcha: crawlers are <em>slow, run on a schedule/trigger, and cost money per run</em> — they can mis-infer types, create a separate table per slightly-different file layout, and miss new partitions until re-run. In production, prefer registering partitions explicitly (<code>ALTER TABLE ADD PARTITION</code> / partition projection) over scheduling crawlers on hot paths."},
  {"sec":"CLOUD-DATA","front":"How is Athena billed, and what are the two levers that cut the bill?","back":"Athena (DDL excluded) is billed <strong>per TB of data scanned from S3</strong>, ~$5/TB, rounded up with a 10 MB minimum per query. The two big levers: <strong>(1) partition pruning</strong> — filter on partition columns so Athena reads fewer S3 prefixes; and <strong>(2) columnar Parquet/ORC + compression</strong> — so it reads only the needed columns, compressed. Together these can cut scan (and cost) by 10-100x versus raw row-oriented JSON/CSV."},
  {"sec":"CLOUD-DATA","front":"Athena vs Redshift: when do you reach for which?","back":"<ul><li><strong>Athena</strong>: serverless, no cluster, pay-per-scan. Best for <em>ad-hoc / infrequent</em> queries over S3, ELT exploration, and unpredictable workloads. No cluster tuning, and no persistent hot-data layer (it does offer opt-in query <em>result reuse</em>).</li><li><strong>Redshift</strong>: provisioned (or serverless) MPP warehouse with sort/dist keys, materialized views, result cache. Best for <em>high-concurrency BI dashboards</em> and repeated queries where you want consistent low latency and are willing to manage/pay for a cluster.</li></ul>Rule of thumb: occasional big scans → Athena; constant dashboard load → Redshift."},
  {"sec":"CLOUD-DATA","front":"Glue ETL job vs EMR: when is each the right tool?","back":"<strong>Glue ETL</strong> is serverless Spark billed per DPU-hour — great for moderate, scheduled ETL where you don't want to manage a cluster, and integrates natively with the Catalog. <strong>EMR</strong> gives you full control of a Hadoop/Spark cluster (instance types, Spark/JVM tuning, custom libs, other apps like Hive/Presto/HBase/Flink) — reach for it on <em>heavy / long-running / cost-sensitive batch</em> where Spot instances and tuning materially cut cost, or when you need the broader Hadoop ecosystem."},
  {"sec":"CLOUD-DATA","front":"Why run EMR clusters as 'transient', and what does that mean?","back":"A <strong>transient cluster</strong> spins up, runs a defined set of steps, and <strong>auto-terminates on completion</strong> (vs a long-running/persistent cluster). You pay only for the minutes the job runs. Combined with <strong>Spot</strong> for task nodes and reading/writing directly to S3 (via EMRFS), transient clusters are the cost-efficient pattern for scheduled batch — no idle cluster burning money overnight."},
  {"sec":"CLOUD-DATA","front":"What query engine does Athena run, and what does that imply?","back":"Athena is managed <strong>Trino/Presto</strong> (Athena engine v3 is Trino-based). Implications: it's an <em>MPP SQL engine</em>, not a storage system — it has no indexes and no data of its own; every query is a fresh distributed scan over S3. So performance/cost is dominated by <em>how the S3 data is laid out</em> (partitioning, file size, columnar format), not by the engine. Also: ANSI-ish Trino SQL dialect, federated connectors available for non-S3 sources."},
  {"sec":"CLOUD-DATA","front":"What is the Athena 'small files problem' and why does it hurt?","back":"Thousands of tiny S3 objects (e.g., one file per streaming micro-batch) force Athena/Spark to open a huge number of objects, each with per-request overhead and poor parallelism — queries get slow and listing dominates. Fix: <strong>compact</strong> into larger files (target ~128 MB-1 GB Parquet), e.g., via a Glue/EMR compaction job or table formats like Iceberg with automatic compaction. This is one of the most common real-world Athena performance killers."},
  {"sec":"CLOUD-DATA","front":"What does Lake Formation add on top of S3 + Glue Catalog?","back":"<strong>Fine-grained, centralized governance</strong> over the lake: database/table/<em>column</em>/<em>row</em>/cell-level permissions and tag-based access control, enforced for Athena, Redshift Spectrum, and EMR — instead of hand-managing coarse S3 bucket IAM policies. It governs access to Catalog-described data and provides cross-account sharing and audit. Think: 'GRANT/REVOKE on lake tables' rather than 'edit a bucket policy.'"},
  {"sec":"CLOUD-DATA","front":"Partition projection vs crawler-managed partitions in Athena — why use projection?","back":"<strong>Partition projection</strong> lets you declare partition values as a formula in table properties (e.g., a date range, integer range, or enum) so Athena <em>computes</em> which S3 prefixes to read instead of listing them from the Catalog. Benefits: no crawler, no millions of partition rows in the Catalog, and no slow Glue <code>GetPartitions</code> calls — big win for high-cardinality time-partitioned tables. Tradeoff: the layout must follow a predictable, declarable pattern."},
  {"sec":"CLOUD-DATA","front":"Why does CTAS (CREATE TABLE AS SELECT) matter in Athena?","back":"<code>CTAS</code> (and <code>INSERT INTO</code>) let Athena <strong>write results back to S3</strong> in a chosen format/partitioning — the standard way to do <em>in-place ELT</em>: convert raw JSON/CSV into partitioned, compressed Parquet, or materialize a curated table — without spinning up Spark. It also sidesteps the small-files problem by writing well-sized output and supports <code>bucketing</code>. Common pattern: land raw → CTAS into an optimized 'silver' table queried cheaply thereafter."},
  {"sec":"CLOUD-DATA","front":"EMRFS vs HDFS on an EMR cluster — what's the model?","back":"<strong>HDFS</strong> on EMR is local instance storage — fast but <em>ephemeral</em>: it dies with the cluster, so it's only for scratch/intermediate shuffle data. <strong>EMRFS</strong> is the connector that lets Hadoop/Spark read and write directly to <strong>S3</strong> as the durable system of record. The pattern: durable input/output in S3 via EMRFS, transient intermediate data in HDFS, which is exactly what makes transient clusters safe — nothing important lives on the cluster."},
  {"sec":"CLOUD-DATA","front":"Redshift Spectrum vs Athena — what's the relationship?","back":"Both run Trino/Presto-style SQL over S3 using the Glue Catalog. <strong>Redshift Spectrum</strong> runs that external-table scan from <em>inside a Redshift cluster</em>, so you can <code>JOIN</code> S3 'cold' data against 'hot' tables physically in Redshift in one query, using Redshift's compute. <strong>Athena</strong> is fully serverless with no warehouse. Choose Spectrum when you already have Redshift and want to extend queries onto the lake; choose Athena when you have no warehouse at all."},
  {"sec":"CLOUD-DATA","front":"What is the Glue DPU and how does it relate to cost?","back":"A <strong>DPU (Data Processing Unit)</strong> is Glue's billing/compute unit — roughly 4 vCPU + 16 GB RAM. Glue Spark ETL bills <strong>per DPU-hour</strong> (~$0.44/DPU-hour standard), billed by the second with a ~1-minute minimum per run. More DPUs = more parallelism = faster but more expensive; right-sizing DPUs (and using <em>auto-scaling</em> / Flex execution for non-urgent jobs) is the main Glue cost lever."},
  {"sec":"CLOUD-DATA","front":"Name three concrete ways to lower an Athena bill on a large table.","back":"<ul><li><strong>Convert to columnar Parquet/ORC + compress</strong> (Snappy/Zstd) — reads only needed columns.</li><li><strong>Partition on common filter columns</strong> (e.g., <code>dt</code>, <code>region</code>) and always filter on them so unscanned partitions are pruned.</li><li><strong>Compact small files</strong> to ~128 MB-1 GB and avoid <code>SELECT *</code>.</li></ul>Secondary: use workgroup <em>per-query data-scan limits</em> (<code>BytesScannedCutoffPerQuery</code>) to cap runaway queries, and CTAS to materialize a pre-optimized table."}
);

Object.assign(GLOSSARY_DATA, {
  "glue-crawler": {"term":"Glue Crawler","group":"CLOUD-DATA","aliases":["crawler","glue crawlers","schema inference"],"eli5":"A bot that walks an S3 prefix, guesses the schema and partitions, and writes table definitions into the Glue Catalog for you.","memory":"A crawler is an intern with a clipboard: fast to deploy, occasionally wrong, and you pay them every time they walk the warehouse.","full":"<p>A crawler connects to a data store (S3 prefix, or JDBC like RDS), samples files, <strong>infers schema and partition structure</strong>, and creates or updates Catalog tables. It's the no-code way to bootstrap a Catalog and to discover newly arrived partitions on a schedule or trigger.</p><p>In practice crawlers are convenient but blunt: they cost per run, can mis-infer types (everything becomes <code>string</code>, or a column flips type across files), and will split data into multiple tables if file layouts differ under one prefix. For hot, well-understood tables, explicit partition management (<code>ALTER TABLE ADD PARTITION</code>) or <strong>partition projection</strong> is faster, cheaper, and deterministic.</p>","trap":"'How do new partitions get discovered?' — saying 'the crawler, always' is a red flag. Senior answer: prefer partition projection or explicit ADD PARTITION on high-cardinality time tables; crawlers don't scale to millions of partitions and add latency/cost.","worked":"Pointing a crawler at <code>s3://lake/raw/</code> containing both <code>orders/</code> and <code>clicks/</code> with different schemas yields two tables; if <code>clicks/</code> has a file where <code>id</code> is sometimes quoted, the crawler may type it <code>string</code> in one partition and <code>bigint</code> in another, breaking queries.","related":["glue-data-catalog","partition-pruning","athena"]},
  "athena": {"term":"Athena","group":"CLOUD-DATA","aliases":["aws athena","athena query","serverless presto"],"eli5":"Serverless SQL over your S3 files — you just point it at data in the Catalog and pay for how much it scans.","memory":"Athena = a taxi (pay per trip/scan); Redshift = a leased car (pay to keep it parked, cheaper if you drive constantly).","full":"<p>Athena is managed <strong>Trino/Presto</strong> (engine v3 is Trino-based) that runs ANSI-style SQL directly over S3 using Glue Catalog table definitions. There is no cluster to manage and no storage of its own; every query is a fresh distributed scan. It's billed <strong>per TB scanned</strong> (~$5/TB, 10 MB minimum, DDL free), which makes data layout — not query tuning — the dominant performance and cost factor.</p><p>It shines for ad-hoc analysis, ELT (via <code>CTAS</code>/<code>INSERT INTO</code>), and unpredictable/infrequent workloads where a provisioned warehouse would sit idle. Its weakness is the flip side: no persistent hot-data layer or indexes (it does offer opt-in query <em>result reuse</em>), so high-concurrency repeated dashboard queries over huge data are often better served by Redshift.</p>","trap":"Interviewers probe the bill: Athena charges by <em>bytes scanned</em>, not rows returned or query time. <code>SELECT col FROM t LIMIT 10</code> on un-pruned row-oriented data still scans the whole table — the LIMIT doesn't reduce scan.","worked":"Same query on 1 TB of JSON costs ~$5; converted to partitioned Snappy Parquet and filtered to one day's partition reading 3 columns, it scans ~4 GB → ~$0.02 — a >200x cost cut from layout alone.","related":["athena-cost-model","partition-pruning","parquet-columnar","glue-data-catalog","redshift-spectrum"]},
  "athena-cost-model": {"term":"Athena Cost Model (per-TB-scanned)","group":"CLOUD-DATA","aliases":["bytes scanned","per tb scanned","athena pricing","scan cost"],"eli5":"Athena charges by how many bytes it reads from S3, so the cheapest query is the one that reads the least data.","memory":"You pay the meter for bytes read off the shelf, not for what you carry out the door — narrow + skip + compress.","full":"<p>Athena bills on <strong>bytes scanned from S3</strong> (~$5/TB, rounded up, 10 MB minimum per query; DDL is free). Note that failed/cancelled queries are still billed for the bytes scanned before they stopped. Because the engine is stateless, you can't 'index your way' out of cost — you reduce <em>what gets read</em>. The three knobs: <strong>partitioning</strong> (skip whole S3 prefixes), <strong>columnar formats</strong> (read only referenced columns), and <strong>compression</strong> (fewer bytes per column).</p><p>Operationally, enforce limits with <strong>workgroups</strong>: set per-query (<code>BytesScannedCutoffPerQuery</code>) and per-workgroup data-scan caps so a stray <code>SELECT *</code> can't burn thousands of dollars, and route teams to separate workgroups for cost attribution. Athena also supports an optional capacity-reservation (provisioned) pricing model for steady heavy use.</p>","trap":"Compression is double-edged: a single huge GZIP file isn't splittable, so Athena can't parallelize it — you cut bytes but lose parallelism. Prefer splittable Parquet+Snappy/Zstd over one giant gzipped CSV. (And note failed queries still cost you the bytes already scanned.)","worked":"A workgroup with a 100 GB per-query scan limit rejects an analyst's accidental cross-join over the raw lake before it scans 40 TB and bills ~$200.","related":["athena","partition-pruning","parquet-columnar","small-files-problem"]},
  "parquet-columnar": {"term":"Parquet / Columnar Format","group":"CLOUD-DATA","aliases":["parquet","orc","columnar","columnar storage"],"eli5":"Store data column-by-column instead of row-by-row, so a query reads only the columns it needs and skips the rest.","memory":"Row formats make you read the whole row to get one field; columnar lets you grab just that column's stripe — and the file's min/max stats let you skip stripes entirely.","full":"<p>Parquet (and ORC) are <strong>columnar, compressed, splittable</strong> file formats. Because columns are stored contiguously, an engine reads only the referenced columns (column pruning) and uses per-column compression plus min/max <strong>statistics</strong> and row-group metadata to skip blocks that can't match a predicate (predicate pushdown). Versus row-oriented JSON/CSV, this typically cuts bytes scanned — and Athena cost — by 5-50x.</p><p>Splittability matters for parallelism: Parquet row groups let multiple workers read one file in parallel, unlike a monolithic gzipped CSV. Converting raw landing data to partitioned Parquet (via Glue/EMR Spark or Athena <code>CTAS</code>) is the canonical first optimization in any S3 lake.</p>","trap":"Parquet helps column pruning automatically, but predicate pushdown only skips row groups if the data is <em>sorted/clustered</em> on the filtered column. Random-ordered Parquet still reads every row group whose min/max range overlaps the predicate — so sort on your common filter when writing.","worked":"A 200-column events table; a query needs 3 columns. CSV scans all 200 columns' bytes; Parquet reads ~3/200 of the data plus skips row groups outside the date range — often a 10-100x reduction.","related":["athena-cost-model","partition-pruning","small-files-problem","athena"]},
  "emr": {"term":"EMR (Elastic MapReduce)","group":"CLOUD-DATA","aliases":["aws emr","elastic mapreduce","managed hadoop","managed spark","transient cluster"],"eli5":"Managed Hadoop/Spark clusters you spin up for heavy batch jobs and (ideally) tear down when done.","memory":"EMR = rent a whole construction crew (cluster) for the big job, send them home after (transient + Spot); Glue = call a serverless contractor for the small job.","full":"<p>EMR provisions managed clusters running the Hadoop ecosystem — Spark, Hive, Presto/Trino, HBase, Flink — with full control over instance types, versions, libraries, and Spark/JVM tuning. You reach for it over serverless Glue when batch is <strong>heavy, long-running, or cost-sensitive</strong>, when you need ecosystem components beyond Spark, or when fine-grained tuning + <strong>Spot</strong> instances materially cut cost.</p><p>The cost-efficient pattern is the <strong>transient cluster</strong>: launch, run defined steps, auto-terminate — paying only for job runtime. Durable I/O goes to S3 via <strong>EMRFS</strong>; HDFS on the cluster is ephemeral scratch. Variants: EMR on EKS (run Spark on your Kubernetes) and EMR Serverless (no cluster management, pay per job) for teams wanting EMR's engines without operating clusters.</p>","trap":"'Why not always EMR?' — because idle long-running clusters bleed money and you own the ops. And persisting important data in HDFS is a footgun: it dies with the cluster. Durable state must be in S3 via EMRFS.","worked":"A nightly 8 TB join: a transient EMR cluster with on-demand core nodes + Spot task nodes reads from S3 (EMRFS), runs Spark for 40 min, writes partitioned Parquet back to S3, and self-terminates — far cheaper than a 24/7 cluster.","related":["emrfs-vs-hdfs","small-files-problem","parquet-columnar"]},
  "emrfs-vs-hdfs": {"term":"EMRFS vs HDFS","group":"CLOUD-DATA","aliases":["emrfs","hdfs","emr storage"],"eli5":"EMRFS reads/writes durable data in S3; HDFS is fast but temporary scratch that dies with the cluster.","memory":"EMRFS = the vault (S3, keep it); HDFS = the workbench (scratch, swept away when the crew leaves).","full":"<p>On EMR you have two file systems. <strong>HDFS</strong> uses the cluster's local/instance storage — low latency, ideal for intermediate shuffle/spill data — but it is <em>ephemeral</em> and vanishes when the cluster terminates. <strong>EMRFS</strong> is the S3 connector that makes <code>s3://</code> paths behave like a Hadoop filesystem, giving durable, decoupled, infinitely scalable storage as the system of record.</p><p>The standard pattern — durable input/output in S3 via EMRFS, transient intermediate data in HDFS — is precisely what makes transient clusters and storage/compute separation safe. (Historically EMRFS offered 'consistent view'; since S3 became strongly read-after-write consistent in Dec 2020 that workaround is obsolete and end-of-support for new EMR releases.)</p>","trap":"Writing final output to HDFS instead of S3 means the results die at cluster termination — a classic transient-cluster mistake. Source of truth always lands in S3 via EMRFS.","worked":"A Spark job reads <code>s3://lake/in/</code> (EMRFS), shuffles intermediate stages to HDFS for speed, then writes <code>s3://lake/out/</code> (EMRFS); the cluster terminates and only the S3 output survives — exactly as intended.","related":["emr","parquet-columnar"]},
  "lake-formation": {"term":"Lake Formation","group":"CLOUD-DATA","aliases":["aws lake formation","lf","lake governance","fine-grained access"],"eli5":"A governance layer that lets you GRANT/REVOKE access to lake tables down to the column and row, instead of editing raw S3 bucket policies.","memory":"Bucket IAM = a lock on the whole filing cabinet; Lake Formation = per-folder, per-page, even per-cell permissions with tags.","full":"<p>Lake Formation adds <strong>fine-grained, centralized access control</strong> over Catalog-described S3 data: permissions at database, table, <em>column</em>, <em>row</em>, and cell level, plus <strong>tag-based access control (LF-Tags)</strong> for scalable policy. Engines that integrate (Athena, Redshift Spectrum, EMR with LF integration) enforce these grants, so you manage access with database-style <code>GRANT</code>/<code>REVOKE</code> rather than sprawling bucket IAM policies.</p><p>It also handles secure <strong>cross-account data sharing</strong>, blueprints for ingestion, and centralized audit. The mental model: IAM/S3 policies are coarse and per-bucket; Lake Formation is fine-grained and per-table/column, and it's the only practical way to do column/row masking across the AWS analytics stack uniformly.</p>","trap":"Lake Formation only governs engines that integrate with it. A user (or role) with direct S3 <code>GetObject</code> on the underlying bucket can bypass LF entirely — you must remove direct S3 grants and revoke the default <code>IAMAllowedPrincipals</code> grant, routing access through LF-aware engines for the controls to mean anything. This is the single most common LF misconfiguration.","worked":"Analysts in the EU get an LF-Tag granting access to <code>orders</code> but a column-level deny on <code>ssn</code> and a row filter <code>region='EU'</code>; the same Athena query returns masked/filtered results per user, with no per-bucket policy changes.","related":["glue-data-catalog","athena","redshift-spectrum"]},
  "redshift-spectrum": {"term":"Redshift Spectrum","group":"CLOUD-DATA","aliases":["spectrum","redshift external tables","redshift s3 query"],"eli5":"Lets a Redshift cluster query S3 data through external tables, so you can join warehouse 'hot' tables to lake 'cold' data in one SQL statement.","memory":"Spectrum is Redshift's telescope pointed at S3 — hot tables in the warehouse, cold history on the lake, joined in one query.","full":"<p>Redshift Spectrum runs Trino/Presto-style scans over S3 (using the Glue Catalog) but from <em>within a Redshift cluster</em>, exposing S3 data as <strong>external tables</strong>. Its value is the <code>JOIN</code>: you keep recent/hot data physically in Redshift for fast BI and reach into years of cold history in S3 in the same query, billed roughly per-TB-scanned like Athena for the external portion.</p><p>Versus <strong>Athena</strong> (fully serverless, no warehouse), choose Spectrum when you already operate Redshift and want to extend it onto the lake; choose Athena when you have no warehouse and just want serverless SQL over S3. Both lean on the same partitioning/Parquet optimizations to control scan cost.</p>","trap":"Spectrum is not warehouse-free: the external scan still costs per-TB-scanned, and you still pay for/run the Redshift cluster to drive it. If you only want occasional S3 SQL with no cluster, that's Athena, not Spectrum.","worked":"A dashboard joins <code>fact_sales</code> (last 90 days, in Redshift) to <code>archive_sales</code> (5 years, external Parquet in S3) on <code>customer_id</code> in one query — recent rows are fast/local, history is scanned from S3 partitions on demand.","related":["athena","glue-data-catalog","partition-pruning","parquet-columnar"]}
});
