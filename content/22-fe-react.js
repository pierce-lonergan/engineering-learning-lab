/* React (FE-REACT) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"FE-REACT","front":"What does JSX compile to in React 19, and how does the automatic runtime differ from the classic one?","back":"<p>JSX is sugar that a compiler (Babel/SWC/esbuild) lowers to function calls returning <strong>immutable React element objects</strong> <code>{type, props, key}</code> — descriptions, not DOM.</p><ul><li><strong>Classic runtime:</strong> emits <code>React.createElement(type, props, ...children)</code>, so <code>React</code> had to be in scope.</li><li><strong>Automatic runtime</strong> (available since React 17, the default in modern toolchains): emits <code>jsx(type, props, key?)</code> / <code>jsxs(...)</code> for static children, auto-imported from <code>react/jsx-runtime</code> — <em>no manual <code>React</code> import</em>.</li></ul><p>Capitalized names → component references; lowercase → tag strings. <code>key</code> is hoisted out of props (passed as a separate <code>jsx</code> arg, not on <code>props</code>). <code>class</code>→<code>className</code>, <code>for</code>→<code>htmlFor</code>.</p>"},
  {"sec":"FE-REACT","front":"Why is React's state update model based on functional updaters, and when MUST you use one?","back":"<p>State updates are <strong>batched</strong> and the state variable is a <em>snapshot</em> captured at render time — it does <em>not</em> mutate mid-render. Calling <code>setN(n+1)</code> three times with a stale <code>n</code> increments once.</p><p>Use a <strong>functional updater</strong> <code>setN(prev =&gt; prev + 1)</code> whenever the next state depends on the previous, or inside async/closures (intervals, effects) to escape a stale closure. React 18+ batches <em>all</em> updates automatically, including those in promises, timeouts, and native event handlers (React 17 only batched inside React event handlers).</p><p><em>Lazy init:</em> <code>useState(() =&gt; expensive())</code> runs the initializer only on the first render.</p>"},
  {"sec":"FE-REACT","front":"Walk through React's render phase vs commit phase. Why does it matter where you put side effects?","back":"<ul><li><strong>Render phase:</strong> React calls your components to build the new element tree and diffs it. Must be <em>pure</em>; it is <strong>interruptible / restartable</strong> under concurrent rendering (one reason Strict Mode double-invokes components in dev).</li><li><strong>Commit phase:</strong> synchronously applies DOM mutations, sets refs, runs <code>useLayoutEffect</code> and its cleanup. <em>After the browser paints</em>, passive <code>useEffect</code>s fire (asynchronously).</li></ul><p>Side effects in render are a bug: the render output can be <strong>thrown away or run multiple times</strong>, causing duplicate subscriptions/requests or tearing. Effects and event handlers are the only safe places.</p>"},
  {"sec":"FE-REACT","front":"How does reconciliation decide whether to reuse or recreate a DOM node?","back":"<p>React diffs the new element tree against the prior one with an O(n) heuristic:</p><ul><li><strong>Different <code>type</code> at a position</strong> → tear down the old subtree entirely (unmount, lose its state), mount fresh.</li><li><strong>Same type</strong> → keep the node and its fiber, patch only changed props/attributes in place.</li><li><strong>Lists of siblings</strong> → matched by <code>key</code> to move/reuse fibers rather than recreate; without keys React falls back to index-position matching.</li></ul><p>It runs on the <strong>Fiber</strong> architecture, making the work interruptible. The result is a minimal set of mutations committed atomically — React patches, it does not rebuild the whole DOM.</p>"},
  {"sec":"FE-REACT","front":"Why do index keys break, and what's the failure mode?","back":"<p>Keys are React's <strong>identity</strong> hint among siblings, used to match elements across renders so it reuses the fiber, its DOM, and its <em>internal state</em>. They must be unique among siblings and <strong>stable</strong> across renders.</p><p>An array <strong>index</strong> tracks <em>position</em>, not the logical item. On insert/delete/reorder, index N now points at a different item, so React reuses the wrong fiber:</p><ul><li>Uncontrolled input text, focus, checkbox state bind to the <em>slot</em> and jump to the wrong row.</li><li>Component state and in-flight animations desync.</li></ul><p>Index keys are acceptable only for static, append-only, stateless lists that never reorder.</p>"},
  {"sec":"FE-REACT","front":"What is the virtual DOM actually for? Is it faster than direct DOM manipulation?","back":"<p>The \"virtual DOM\" is the in-memory tree of plain element objects your components return. React diffs new vs old and applies only necessary, <strong>batched</strong> DOM mutations.</p><p><em>Its real value is not raw speed</em> — the diff itself costs CPU, and hand-tuned imperative code can beat it. The value is a <strong>declarative model</strong>: you write UI as a pure function of state and React handles correct, minimal imperative updates. Note the React team de-emphasizes the term; the persistent diffable tree is really the <strong>Fiber</strong> tree. Saying \"virtual DOM is always faster than the real DOM\" is the classic misconception.</p>"},
  {"sec":"FE-REACT","front":"Contrast controlled vs uncontrolled inputs. When would you deliberately choose uncontrolled?","back":"<ul><li><strong>Controlled:</strong> value lives in React state — <code>value={x}</code> + <code>onChange</code>. React is the single source of truth; enables validation, formatting, conditional disabling on every keystroke. Cost: a render per keystroke.</li><li><strong>Uncontrolled:</strong> the DOM owns the value; you set an initial via <code>defaultValue</code>/<code>defaultChecked</code> and read it via a <code>ref</code> when needed. Less code, no per-keystroke render.</li></ul><p>Choose uncontrolled for simple/large forms, file inputs (<code>&lt;input type=\"file\"&gt;</code> is <em>always</em> uncontrolled — its value can't be set programmatically), or perf-sensitive fields. <em>Trap:</em> passing <code>value</code> without <code>onChange</code> (or <code>readOnly</code>) makes a stuck field and logs a warning.</p>"},
  {"sec":"FE-REACT","front":"useEffect vs useLayoutEffect — timing, blocking, and the right default?","back":"<ul><li><strong><code>useEffect</code></strong>: runs <em>asynchronously after paint</em>. Never blocks the visual update. Default for data fetching, subscriptions, logging.</li><li><strong><code>useLayoutEffect</code></strong>: runs <em>synchronously after commit but before the browser paints</em>. Use only to <strong>measure layout and synchronously re-mutate the DOM</strong> to avoid a visible flicker.</li></ul><p>Both: deps compared with <code>Object.is</code> drive re-runs; cleanup runs before the next run and on unmount. <code>useLayoutEffect</code> <strong>blocks paint</strong> (can hurt INP) and has no effect during SSR (warns if used), so reach for it only when synchronous measurement is required — otherwise <code>useEffect</code>.</p>"},
  {"sec":"FE-REACT","front":"What is a stale closure, and why does lying to exhaustive-deps cause it?","back":"<p>An effect/callback captures the variables from the render that created it. If you omit a reactive value from the dependency array, the function keeps reading that render's <em>frozen</em> snapshot — a <strong>stale closure</strong>.</p><p><code>exhaustive-deps</code> requires every reactive value used inside to be listed; an empty <code>[]</code> is a promise that nothing inside is reactive. <em>Correct</em> fixes preserve correctness, not just silence the lint:</p><ul><li>Functional updaters (<code>setN(n =&gt; n+1)</code>) to drop a state dep.</li><li><code>useCallback</code>/<code>useMemo</code> to stabilize function/object deps.</li><li>A <code>ref</code> to read the latest value without re-subscribing.</li></ul>"},
  {"sec":"FE-REACT","front":"useRef: what are its two distinct jobs, and how does it differ from state?","back":"<p><code>useRef(initial)</code> returns a <strong>stable mutable container</strong> <code>{current}</code> that persists across renders (same object identity every render). Two jobs:</p><ul><li><strong>DOM access:</strong> attach via <code>ref={node}</code> to read/focus/measure an element after commit.</li><li><strong>Mutable instance value:</strong> timer IDs, previous values, latest-prop trackers — anything that shouldn't trigger re-render.</li></ul><p>Unlike state, <strong>mutating <code>.current</code> does NOT re-render</strong> and the change is visible immediately (no snapshot). Don't read/write refs during render (impure — breaks concurrent safety). In React 19, <code>ref</code> is a regular prop on function components, so <code>forwardRef</code> is no longer required (and is now deprecated).</p>"},
  {"sec":"FE-REACT","front":"useMemo / useCallback: what problem do they solve, and when is using them a mistake?","back":"<p>React compares props/deps by <strong>identity</strong> (<code>Object.is</code>). A fresh inline object/array/function each render breaks <code>React.memo</code> bailouts and re-fires effects. <code>useMemo</code> (caches a value) and <code>useCallback(fn, deps)</code> (caches a function — equivalent to <code>useMemo(() =&gt; fn, deps)</code>) preserve <strong>referential stability</strong> given stable deps.</p><p>They are <strong>perf hints, not guarantees</strong> — React may discard the cache (e.g. to free memory), so never rely on identity for correctness. <em>Don't</em> memoize when there's no memoized consumer or expensive computation: it adds dep-array overhead and noise. <strong>React Compiler</strong> (separate build tool, v1.0 stable Oct 2025) auto-memoizes, eliminating most manual usage.</p>"},
  {"sec":"FE-REACT","front":"Why does putting an object literal in a Context Provider's value re-render the whole tree?","back":"<p>Context propagation compares the Provider's <code>value</code> with <code>Object.is</code>. On change, <strong>every consumer re-renders</strong> — there are <em>no selectors</em> and no per-field bailout (a consumer reading one field re-renders even if only another field changed).</p><p>An <strong>inline object</strong> <code>value={{user, setUser}}</code> is a new reference each render, so all consumers re-render whenever the provider renders. Fixes:</p><ul><li><code>useMemo</code> the value object.</li><li><strong>Split contexts</strong> by update cadence (stable <code>dispatch</code> vs volatile state).</li><li>Use an external store + <code>useSyncExternalStore</code> with selectors for fine-grained reads.</li></ul>"},
  {"sec":"FE-REACT","front":"When do you reach for useReducer over useState, and what's special about dispatch?","back":"<p>Prefer <code>useReducer</code> when state transitions are <strong>interdependent, multi-field, or worth testing in isolation</strong>. Logic lives in a <strong>pure</strong> <code>(state, action) =&gt; newState</code> reducer; you get <code>[state, dispatch]</code>.</p><ul><li><code>dispatch</code> has a <strong>stable identity</strong> for the component's life — safe to pass through context without re-rendering consumers (the reducer+context store pattern).</li><li>Lazy init via the third <code>init</code> argument: <code>useReducer(reducer, arg, init)</code> computes initial state as <code>init(arg)</code>.</li></ul><p><em>Trap:</em> reducers must be pure — no fetches/mutations/randomness. Strict Mode double-invokes them in dev to surface impurity.</p>"},
  {"sec":"FE-REACT","front":"State the Rules of Hooks and the mechanism that makes them non-negotiable.","back":"<p><strong>Call hooks unconditionally, at the top level, only from function components or custom hooks.</strong> No hooks inside conditions, loops, nested functions, or after an early <code>return</code>.</p><p><em>Why:</em> React tracks hook state by <strong>call order</strong> on the fiber, not by name. Skipping a hook on one render shifts every later hook to the wrong state slot — state desyncs catastrophically. The <code>use</code> prefix lets <code>eslint-plugin-react-hooks</code> enforce ordering and exhaustive-deps.</p><p><em>Exception:</em> React 19's <code>use()</code> is an API, not a hook — it tracks promises by reference and context by tree position, not call order, so it <em>may</em> be called inside conditions and loops.</p>"},
  {"sec":"FE-REACT","front":"How does React.memo work, and what silently defeats it?","back":"<p><code>React.memo(Component)</code> skips re-rendering when the component's <strong>props are shallow-equal</strong> (<code>Object.is</code> per prop) to the previous render. It's a bailout, not a deep guarantee, and only blocks re-renders caused by the <em>parent</em> — state/context changes inside still re-render it.</p><p>What defeats it:</p><ul><li><strong>Unstable prop identities</strong> — inline objects/arrays/functions or non-memoized callbacks create new references each render → memo always re-renders.</li><li>Passing <code>children</code> as fresh JSX each render.</li></ul><p>Pair it with <code>useCallback</code>/<code>useMemo</code> on the parent, or pass an optional custom comparator as the 2nd arg. <em>Note:</em> React Compiler makes most manual <code>memo</code> unnecessary. Don't wrap cheap components — the comparison isn't free.</p>"},
  {"sec":"FE-REACT","front":"How do Suspense, lazy, and error boundaries divide the loading/error responsibilities?","back":"<ul><li><strong><code>&lt;Suspense fallback&gt;</code></strong> handles the <em>pending</em> state of any suspending descendant: a <code>React.lazy</code> chunk, a streamed SSR segment, or a <code>use(promise)</code>/RSC data read. Shows the fallback until all suspended reads in its subtree resolve.</li><li><strong>Error boundaries</strong> (class with <code>getDerivedStateFromError</code> and/or <code>componentDidCatch</code>) catch <em>render-phase</em> throws — the failed state.</li></ul><p><em>Traps:</em> neither catches errors in <strong>event handlers or async code</strong> (use try/catch there); and any single suspending child triggers the <em>whole</em> boundary's fallback, so scope boundaries to avoid large fallback flashes.</p>"},
  {"sec":"FE-REACT","front":"React Server Components: what runs where, and what crosses the 'use client' boundary?","back":"<p>In the RSC model (React 19, used by frameworks like Next.js App Router), components default to <strong>Server Components</strong>: they run on the server, can be <code>async</code> and <code>await</code> data inline, render to a serialized payload, and ship <strong>zero JS</strong> for themselves. No state, effects, or browser APIs.</p><p><strong><code>'use client'</code></strong> marks the boundary into the interactive client bundle (hooks/handlers allowed). Server components can render client components and pass <strong>serializable props</strong> + <code>children</code> — never functions (except Server Actions) or class instances. <strong>Server Actions</strong> (<code>'use server'</code>) expose async server functions to the client for form/mutation handling.</p>"},
  {"sec":"FE-REACT","front":"What do useTransition and useDeferredValue do, and how do they differ?","back":"<p>Both are concurrent features that keep the UI responsive by marking work <strong>low-priority / interruptible</strong>, so urgent input (typing, clicks) renders first. Neither makes rendering faster — they re-order it for perceived responsiveness.</p><ul><li><strong><code>useTransition</code></strong>: gives <code>[isPending, startTransition]</code>. You wrap the <em>state update</em> you're causing in <code>startTransition</code> — you control the source.</li><li><strong><code>useDeferredValue</code></strong>: wraps a <em>value you receive</em> (e.g. a prop), returning a lagging copy used to render the expensive subtree while the input stays snappy.</li></ul><p>Use the first when you own the setter, the second when you only have the value. Both rely on Fiber's interruptible rendering.</p>"}
);

Object.assign(GLOSSARY_DATA, {
  "reconciliation": {"term":"Reconciliation","group":"FE-REACT","eli5":"React's process of figuring out the minimum DOM changes by comparing the new element tree to the old one.","memory":"\"Re-concile = re-align two trees.\"","full":"<p><strong>Reconciliation</strong> is the render-phase diffing algorithm that compares the newly produced React element tree against the previous one to decide what to mutate in the real DOM. It's heuristic and roughly O(n): React assumes elements of <em>different type</em> at a position are entirely different (tear down + rebuild), and uses <strong>keys</strong> to match siblings of the same type across renders so it can reorder/reuse rather than recreate.</p><p>The output is a set of effects committed atomically in the commit phase. The modern implementation runs on the <strong>Fiber</strong> architecture, which makes the work interruptible for concurrent rendering.</p>","trap":"Thinking reconciliation re-renders the whole DOM. It computes a <em>minimal</em> patch; only changed nodes are touched. Also, it diffs React's element/fiber trees, not the real DOM against itself.","worked":"Swapping <code>&lt;a/&gt;</code> for <code>&lt;span/&gt;</code> at the same slot unmounts the <code>a</code> and its subtree entirely; same type with changed props patches in place.","related":["virtual-dom","keys","fiber"]},
  "virtual-dom": {"term":"Virtual DOM","group":"FE-REACT","eli5":"A lightweight JS description of the UI that React diffs in memory before touching the slower real DOM.","memory":"\"Blueprint before the bricks.\"","full":"<p>The <strong>virtual DOM</strong> is the in-memory tree of plain <em>React element</em> objects (<code>{type, props, key}</code>) that your components return. React diffs the new tree against the prior one (reconciliation) and applies only the necessary mutations to the real DOM, batching writes to avoid layout thrash.</p><p>The value isn't that it's magically faster than hand-written DOM code — it's that it gives a <strong>declarative</strong> programming model: you describe UI as a function of state and React handles the imperative updates correctly. The React team now de-emphasizes the term; the persistent structure React actually diffs/keeps state on is the <strong>Fiber</strong> tree.</p>","trap":"\"The virtual DOM is always faster than direct DOM manipulation.\" False — it's an abstraction for correctness/DX; the diff itself has cost. Hand-tuned imperative code can beat it.","worked":"<code>jsx('h1', {children: count})</code> returns a tiny object, not an HTML element. React compares it to last render's object and only updates the text node if <code>count</code> changed.","related":["reconciliation","jsx-runtime","render-commit"]},
  "jsx-runtime": {"term":"JSX (automatic runtime)","group":"FE-REACT","eli5":"The HTML-like syntax that a compiler turns into function calls producing React element objects.","memory":"\"JSX in, jsx() out.\"","full":"<p><strong>JSX</strong> is compiled away by Babel/SWC/esbuild. Under the <strong>automatic runtime</strong> (available since React 17, default in modern setups), elements become <code>jsx</code>/<code>jsxs</code> calls auto-imported from <code>react/jsx-runtime</code>, so you no longer import <code>React</code> just to use JSX. The legacy/classic runtime emitted <code>React.createElement</code>.</p><p>Each call returns an <strong>immutable element</strong>: a description of what to render, not actual DOM. Capitalized identifiers resolve to component references; lowercase become tag-name strings; <code>key</code> is extracted as a separate argument.</p>","trap":"Thinking JSX <em>is</em> HTML or that it renders immediately. It's just function-call sugar; the element is an inert object until React renders it. Also <code>class</code>→<code>className</code>, <code>for</code>→<code>htmlFor</code>.","worked":"<code>&lt;Btn type=\"x\"&gt;Go&lt;/Btn&gt;</code> → <code>jsx(Btn, {type: 'x', children: 'Go'})</code>.","related":["virtual-dom","render-commit"]},
  "keys": {"term":"Keys","group":"FE-REACT","eli5":"Stable IDs that tell React which list item is which across re-renders.","memory":"\"Key = identity, not position.\"","full":"<p><strong>Keys</strong> are React's identity hint during reconciliation, used to match elements among <em>siblings</em> across renders so React can move and reuse fibers (and their state/DOM) instead of destroying and recreating them. They must be unique per sibling list and <em>stable</em> across renders.</p><p>A key is not readable as a prop (it's consumed by React itself). Using array <strong>index</strong> as key is unsafe for lists that can reorder, insert, or hold per-row state, because indices track position, not the logical item.</p>","trap":"Using the array index as key on a dynamic/stateful list. Reordering then preserves the wrong DOM/state (e.g. input text jumps to the wrong row).","worked":"<code>items.map(i =&gt; &lt;Row key={i.id} .../&gt;)</code> survives sorting; <code>key={index}</code> would mis-bind state after a sort.","related":["reconciliation","key-as-reset"]},
  "render-commit": {"term":"Render & Commit phases","group":"FE-REACT","eli5":"React first computes what changed (pure), then applies it to the DOM (with side effects).","memory":"\"Render = think, Commit = act.\"","full":"<p>An update runs in two phases. The <strong>render phase</strong> calls components to build the new tree and diffs it; it must be <em>pure</em> and is <strong>interruptible</strong>/restartable under concurrent rendering (hence Strict Mode's dev double-invoke). The <strong>commit phase</strong> synchronously applies DOM mutations, sets refs, and runs <code>useLayoutEffect</code>; after the browser paints, passive <code>useEffect</code>s fire.</p><p>This split is why side effects belong in effects/handlers, never in render — render output may be thrown away or repeated.</p>","trap":"Doing side effects (mutating refs, subscribing, network calls) during render. Under concurrency the render can run multiple times or be discarded, causing duplicates/tearing.","worked":"Reading <code>node.offsetHeight</code> to reposition before paint belongs in <code>useLayoutEffect</code> (after commit, before paint), not in the component body.","related":["use-effect-vs-layout","fiber","virtual-dom"]},
  "use-effect-vs-layout": {"term":"useEffect vs useLayoutEffect","group":"FE-REACT","eli5":"Both run effects after render, but layout effects run before the screen paints and block it.","memory":"\"Layout = before paint (sync); Effect = after paint (async).\"","full":"<p><strong><code>useEffect</code></strong> runs asynchronously after paint and is the default for subscriptions, fetching, and logging — it never blocks the visual update. <strong><code>useLayoutEffect</code></strong> runs synchronously after commit but <em>before</em> paint, so it's the tool for measuring layout and synchronously mutating the DOM to prevent a visible flicker.</p><p>Both accept a dependency array (<code>Object.is</code> comparison drives re-runs) and both run cleanup before the next run and on unmount. Layout effects block paint and don't run during SSR (warn if used there).</p>","trap":"Reaching for <code>useLayoutEffect</code> by default. It blocks paint and can hurt INP; use it only for synchronous DOM measurement/mutation, otherwise <code>useEffect</code>.","worked":"Tooltip positioning that reads <code>getBoundingClientRect()</code> and sets <code>top/left</code> uses <code>useLayoutEffect</code> so the user never sees it flash at (0,0).","related":["deps-array","render-commit"]},
  "deps-array": {"term":"Dependency array / exhaustive-deps","group":"FE-REACT","eli5":"The list that decides when an effect/memo re-runs; the linter makes sure it's complete.","memory":"\"List everything you read, or read stale.\"","full":"<p>The <strong>dependency array</strong> is compared element-wise with <code>Object.is</code> each render; any change re-runs the effect (after cleanup) or recomputes the memo. <code>[]</code> means run once on mount; omitting it means run every render. The <strong>exhaustive-deps</strong> lint rule requires every reactive value used inside to be listed.</p><p>Omitting a dep produces a <strong>stale closure</strong>: the callback captures values from the render it was created in. Correct fixes preserve correctness — functional updaters, <code>useCallback</code>-stabilized functions, moving logic inside the effect, or <code>ref</code>s for read-only-latest values — never lying to the linter.</p>","trap":"Disabling the lint to \"run only on mount\" while reading props/state inside — you silently read stale values. Empty deps is a claim that nothing inside is reactive.","worked":"<code>useEffect(() =&gt; { const id = setInterval(() =&gt; setN(n =&gt; n+1), 1000); return () =&gt; clearInterval(id); }, [])</code> — functional updater avoids needing <code>n</code> in deps.","related":["use-effect-vs-layout","referential-stability","rules-of-hooks"]},
  "referential-stability": {"term":"Referential stability","group":"FE-REACT","eli5":"Keeping the same object/function identity across renders so memoized children and effects don't needlessly re-run.","memory":"\"Same reference = skip the work.\"","full":"<p>React compares props and deps by <strong>identity</strong> (<code>Object.is</code>), so a freshly-created inline object/array/function breaks bailouts in <code>React.memo</code> and re-triggers effects even when the <em>logical</em> value is unchanged. <strong><code>useMemo</code></strong> (values) and <strong><code>useCallback</code></strong> (functions) preserve identity across renders given stable deps.</p><p>Critically these are <strong>performance hints, not guarantees</strong> — React may evict the cache — so never rely on identity for correctness. The <strong>React Compiler</strong> (a separate build-time tool, v1.0 stable Oct 2025) auto-memoizes, removing most manual usage.</p>","trap":"Memoizing everything \"to be safe.\" For values with no memoized consumer it's pure overhead. Also relying on a memoized identity persisting (it can be dropped).","worked":"<code>const onClick = useCallback(() =&gt; act(id), [id])</code> lets a <code>React.memo</code>'d button skip re-render until <code>id</code> changes.","related":["context-rerender","deps-array","keys"]},
  "context-rerender": {"term":"Context re-render pitfall","group":"FE-REACT","eli5":"All consumers of a context re-render whenever the provider's value reference changes, even if they only read one field.","memory":"\"New value reference = everyone re-renders.\"","full":"<p>Context propagates by comparing the Provider's <code>value</code> with <code>Object.is</code>; on change <em>every</em> consumer re-renders, with no built-in selector or per-field bailout. The common bug is an <strong>inline object literal</strong> as value, which is a new reference each render and re-renders all consumers unnecessarily.</p><p>Mitigations: <code>useMemo</code> the value; <strong>split contexts</strong> by update cadence (stable dispatch vs volatile state); or expose a stable external store consumed with <code>useSyncExternalStore</code> + selectors (the pattern libraries like Zustand and Redux use).</p>","trap":"Treating context like a performance-optimized store. It has no selectors — a single hot field can re-render an entire subtree of consumers.","worked":"<code>&lt;Ctx.Provider value={useMemo(() =&gt; ({user, setUser}), [user])}&gt;</code> stops re-renders caused by a new object each render.","related":["referential-stability","use-reducer-pattern"]},
  "rules-of-hooks": {"term":"Rules of Hooks","group":"FE-REACT","eli5":"Always call hooks in the same order at the top level, only from components/custom hooks.","memory":"\"Same order, every render — no ifs.\"","full":"<p>Hooks must be called <strong>unconditionally at the top level</strong> and only from <strong>React function components or custom hooks</strong>. React tracks hook state by <strong>call order</strong> on the fiber, not by name, so a conditional or early-return that skips a hook shifts every later hook to the wrong state slot.</p><p><code>eslint-plugin-react-hooks</code> enforces both the ordering rule and exhaustive-deps. The <code>use</code> naming prefix is how tooling identifies hooks and custom hooks.</p>","trap":"Calling a hook inside an <code>if</code>, loop, or after an early <code>return</code>. It \"works\" until the branch flips, then state desyncs. Note: React 19's <code>use()</code> is an API, not a hook — it's the documented exception that <em>can</em> be conditional.","worked":"Bad: <code>if (open) useEffect(...)</code>. Good: <code>useEffect(() =&gt; { if (open) {...} }, [open])</code>.","related":["deps-array","custom-hook"]},
  "use-reducer-pattern": {"term":"useReducer","group":"FE-REACT","eli5":"Manage complex state with a pure (state, action) function instead of scattered setState calls.","memory":"\"Reducer = state machine in a function.\"","full":"<p><code>useReducer(reducer, initialArg, init?)</code> centralizes state transitions in a <strong>pure</strong> <code>(state, action) =&gt; newState</code> function, returning <code>[state, dispatch]</code>. It's preferred over <code>useState</code> when transitions are interdependent, span multiple fields, or warrant isolated testing.</p><p><code>dispatch</code> has a <strong>stable identity</strong> for the component's lifetime, so it can be passed through context without causing consumer re-renders — the backbone of the lightweight reducer+context store pattern. Lazy init: when <code>init</code> is given, initial state is <code>init(initialArg)</code>.</p>","trap":"Putting impure logic (fetches, randoms, mutations) inside the reducer. Reducers must be pure — Strict Mode double-invokes them in dev to surface impurity.","worked":"<code>dispatch({type:'increment'})</code> hits <code>(s,a) =&gt; a.type==='increment' ? {...s, n:s.n+1} : s</code>.","related":["context-rerender","referential-stability"]},
  "custom-hook": {"term":"Custom hook","group":"FE-REACT","eli5":"A use-prefixed function that bundles reusable stateful logic by calling other hooks.","memory":"\"Reuse logic, not state.\"","full":"<p>A <strong>custom hook</strong> is any function named <code>useX</code> that calls built-in or other custom hooks. It extracts <em>stateful logic</em> (subscriptions, timers, fetching, form handling) for reuse while keeping each call site's state fully independent — hooks aren't singletons.</p><p>It returns whatever shape is useful (values, setters, refs, tuples). The <code>use</code> prefix lets the linter apply the Rules of Hooks inside it. Use custom hooks for logic, components for markup, and plain functions for stateless helpers.</p>","trap":"Expecting two components using the same custom hook to <em>share</em> state. They don't — each call gets its own independent state. Shared state needs context or an external store.","worked":"<code>function useToggle(init=false){ const [on,set]=useState(init); return [on, ()=&gt;set(o=&gt;!o)] }</code> — reusable, isolated per component.","related":["rules-of-hooks","deps-array"]},
  "server-components": {"term":"React Server Components","group":"FE-REACT","eli5":"Components that run only on the server, fetch data directly, and ship zero JS to the browser.","memory":"\"Server by default, 'use client' to opt out.\"","full":"<p>In the RSC model (React 19, used by frameworks like Next.js App Router), components are <strong>Server Components by default</strong>: they execute on the server, may be <code>async</code> and <code>await</code> data inline, render to a serialized payload, and add <strong>no JavaScript</strong> for themselves to the client bundle. They can't use state, effects, or browser APIs.</p><p>The <strong><code>'use client'</code></strong> directive marks the boundary into the interactive client bundle (hooks allowed). Server components can render client components and pass <strong>serializable</strong> props plus <code>children</code>. <strong>Server Actions</strong> (<code>'use server'</code>) expose async server functions to the client for mutations.</p>","trap":"Using <code>useState</code>/<code>useEffect</code>/<code>onClick</code> in a Server Component, or passing a non-serializable prop (a plain function — other than a Server Action — or a class instance) across the boundary.","worked":"<code>async function Page(){ const data = await db.query(); return &lt;List rows={data}/&gt; }</code> — no client JS for the fetch.","related":["suspense","jsx-runtime"]},
  "suspense": {"term":"Suspense","group":"FE-REACT","eli5":"A boundary that shows a fallback while something inside is still loading.","memory":"\"Suspense waits, error boundary catches.\"","full":"<p><strong><code>&lt;Suspense fallback&gt;</code></strong> declaratively handles the <em>pending</em> state of any suspending descendant — a lazy chunk (<code>React.lazy</code>), a streamed SSR segment, or a data read via <code>use(promise)</code>/RSC. It shows the fallback until all suspended reads in its subtree resolve, then reveals the content.</p><p>It pairs with <strong>error boundaries</strong>, which catch render-phase throws (the <em>failed</em> state), and with concurrent features for streaming and transitions. Suspense is about <em>loading orchestration</em>, not error handling.</p>","trap":"Expecting Suspense (or error boundaries) to catch errors in event handlers/async callbacks — they don't; use try/catch there. And Suspense shows fallback on <em>any</em> suspending child, so place boundaries thoughtfully to avoid large fallback flashes.","worked":"<code>&lt;Suspense fallback={&lt;Spinner/&gt;}&gt;&lt;LazyChart/&gt;&lt;/Suspense&gt;</code> shows the spinner until the code-split chunk loads.","related":["server-components","render-commit","concurrent-transition"]},
  "concurrent-transition": {"term":"Concurrent rendering (useTransition / useDeferredValue)","group":"FE-REACT","eli5":"Mark some updates as low-priority so React can interrupt them to keep typing and clicks responsive.","memory":"\"Transition = mark the update; DeferredValue = lag the value.\"","full":"<p>Concurrent rendering lets React treat updates as <strong>interruptible, prioritized</strong> work on the Fiber scheduler. <strong><code>useTransition</code></strong> returns <code>[isPending, startTransition]</code>; wrapping a state update in <code>startTransition</code> marks it non-urgent so urgent input renders first. <strong><code>useDeferredValue</code></strong> wraps a <em>value you receive</em>, returning a lagging copy to render an expensive subtree while the input stays snappy.</p><p>Use <code>useTransition</code> when you own the setter, <code>useDeferredValue</code> when you only have the value. Neither makes work faster — it reorders it for perceived responsiveness.</p>","trap":"Thinking transitions speed up rendering. They don't — they re-prioritize and allow interruption. Also don't wrap a controlled-input's own <code>value</code> update in a transition (the input would lag).","worked":"<code>const [isPending, start] = useTransition(); start(() =&gt; setQuery(input));</code> keeps the textbox responsive while a big results list re-renders.","related":["fiber","render-commit"]},
  "key-as-reset": {"term":"Key as reset","group":"FE-REACT","eli5":"Change a component's key to force React to throw away the old instance and mount a fresh one.","memory":"\"New key = new component = clean state.\"","full":"<p>Because reconciliation treats a changed <strong>key</strong> as a different instance, assigning a new key makes React <strong>unmount the old component and mount a new one</strong>, discarding all internal state/refs and re-running mount effects. It's the idiomatic, declarative way to reset state when a logical <em>identity</em> changes.</p><p>React's docs prefer this over the anti-pattern of syncing props into state with effects. The cost is a full remount (lost focus/animation, mount work), so apply it intentionally.</p>","trap":"Using it as a generic re-render trigger. It's a full remount, not a refresh — you lose focus, in-flight animations, and pay mount cost.","worked":"<code>&lt;EditForm key={selectedId} record={record}/&gt;</code> wipes draft state when the user picks a different record.","related":["keys","reconciliation","render-commit"]},
  "fiber": {"term":"Fiber","group":"FE-REACT","eli5":"React's internal unit of work that lets rendering pause, resume, and prioritize.","memory":"\"Fiber = a thread of render work React can interrupt.\"","full":"<p><strong>Fiber</strong> is React's reconciler architecture (since v16): each element gets a fiber node holding its type, state, effects, and links to parent/child/sibling. Representing work as a linked list of fibers lets React perform rendering <strong>incrementally</strong> — splitting work into units it can pause, abort, resume, and prioritize.</p><p>This is the foundation of <strong>concurrent rendering</strong>: <code>useTransition</code>/<code>useDeferredValue</code>, Suspense streaming, and interruptible renders all rely on the fiber scheduler keeping urgent input ahead of low-priority updates.</p>","trap":"Conflating fiber (the persistent internal node/scheduler) with the element tree (the immutable description recreated each render). Fibers persist across renders and carry hook state by call order; elements are recreated each render.","worked":"A long list render started inside <code>startTransition</code> can be paused mid-fiber-tree so a keystroke renders first, then resumed — only possible because work is fiber-by-fiber.","related":["reconciliation","render-commit","virtual-dom","concurrent-transition"]}
});
