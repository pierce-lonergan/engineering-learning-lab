/* Functions, Closures & Decorators (PY-FUNCTIONS) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"PY-FUNCTIONS","front":"What does \"functions are first-class objects\" mean in Python?","back":"Functions are ordinary objects you can <strong>assign to variables, pass as arguments, return from other functions, and store in data structures</strong>. They have attributes (<code>__name__</code>, <code>__doc__</code>, <code>__dict__</code>) and a runtime type (<code>types.FunctionType</code>). This is the foundation that makes higher-order functions, callbacks, and decorators possible without any special syntax."},
  {"sec":"PY-FUNCTIONS","front":"What exactly does a Python closure capture — the value or the variable?","back":"It captures the <strong>variable (a cell), by reference, not the value at definition time</strong>. The inner function holds <code>__closure__</code> cells pointing at the enclosing locals, so it reads their <em>current</em> value each time it runs. This is why closures see later mutations of the captured variable — and the root cause of the late-binding loop gotcha."},
  {"sec":"PY-FUNCTIONS","front":"Why do all closures created in a `for` loop return the same (last) value?","back":"All the lambdas share <strong>one</strong> loop-variable cell, and the loop leaves that cell holding its final value; every closure reads it at call time, not at creation time. <code>fns=[lambda: i for i in range(3)]</code> then <code>[f() for f in fns]</code> gives <code>[2,2,2]</code>. Fix by binding per-iteration: <code>lambda i=i: i</code> (default-arg snapshot) or a factory function returning the closure."},
  {"sec":"PY-FUNCTIONS","front":"Write the standard fix for the late-binding closure-in-a-loop bug.","back":"Snapshot the value into a per-iteration default argument, which is evaluated immediately at each <code>lambda</code> creation: <code>fns = [lambda i=i: i for i in range(3)]</code> now yields <code>[0,1,2]</code>. Equivalent fix: a factory <code>def make(i): return lambda: i</code>. Note: a comprehension does <em>not</em> give each iteration a fresh cell for the loop variable — <code>[lambda: i for i in range(3)]</code> still returns <code>[2,2,2]</code> (all lambdas share one cell); the Py3 change is only that the loop name doesn't leak to the outer scope."},
  {"sec":"PY-FUNCTIONS","front":"Desugar `@decorator` — what is `@d` above `def f(): ...` equivalent to?","back":"It is pure syntactic sugar for <strong>rebinding the name to the wrapped result</strong>: <code>f = d(f)</code>, evaluated right after the function object is created. <code>d</code> is any callable that takes the function and returns a replacement (usually a wrapper). The replacement is what the name <code>f</code> now points to."},
  {"sec":"PY-FUNCTIONS","front":"Why use `functools.wraps` when writing a decorator?","back":"Without it the wrapper <strong>shadows the original's identity</strong>: <code>f.__name__</code> becomes <code>'wrapper'</code>, and <code>__doc__</code>, <code>__module__</code>, <code>__qualname__</code>, <code>__dict__</code>, and <code>__wrapped__</code> are lost — breaking introspection, debuggers, and doctests. <code>@functools.wraps(func)</code> on the inner wrapper copies that metadata over and sets <code>__wrapped__</code> so the original is still reachable."},
  {"sec":"PY-FUNCTIONS","front":"How many nested functions does a decorator that TAKES ARGUMENTS need, and why?","back":"<strong>Three levels.</strong> The outermost takes the decorator's arguments and returns the actual decorator; the middle takes the function and returns the wrapper; the inner is the wrapper that takes <code>*args, **kwargs</code>. Because <code>@deco(x)</code> first <em>calls</em> <code>deco(x)</code> and then applies <em>its return value</em> to <code>f</code> — i.e. <code>f = deco(x)(f)</code>."},
  {"sec":"PY-FUNCTIONS","front":"What is the difference between `*args` and `**kwargs` in a function signature?","back":"<code>*args</code> collects extra <strong>positional</strong> arguments into a tuple; <code>**kwargs</code> collects extra <strong>keyword</strong> arguments into a dict. In a <em>call</em> the same stars mean <em>unpack</em>: <code>f(*seq, **mapping)</code> spreads a sequence into positionals and a mapping into keywords. Generic wrappers use <code>def wrapper(*args, **kwargs): return f(*args, **kwargs)</code> to forward any signature."},
  {"sec":"PY-FUNCTIONS","front":"What does a bare `*` or a `/` in a parameter list do (Py3)?","back":"<code>*</code> marks the start of <strong>keyword-only</strong> parameters: anything after it must be passed by name (<code>def f(a, *, b)</code> forbids <code>f(1,2)</code>). <code>/</code> (Py3.8+) marks the end of <strong>positional-only</strong> parameters: anything before it cannot be passed by name (<code>def f(a, /, b)</code> forbids <code>f(a=1,...)</code>). Used to lock APIs and free up names for <code>**kwargs</code>."},
  {"sec":"PY-FUNCTIONS","front":"Why is `def append(x, lst=[]): lst.append(x); return lst` a bug?","back":"The default <code>[]</code> is evaluated <strong>once, at <code>def</code> time</strong>, and the single list object is reused across every call that omits <code>lst</code> — so it accumulates state between calls. <code>append(1)</code> → <code>[1]</code>, <code>append(2)</code> → <code>[1,2]</code>. Canonical fix: default to <code>None</code> and build inside: <code>def append(x, lst=None): lst = [] if lst is None else lst</code>."},
  {"sec":"PY-FUNCTIONS","front":"State the LEGB scope-resolution rule.","back":"Name lookup proceeds <strong>Local → Enclosing → Global → Built-in</strong>, stopping at the first match. <em>Local</em> = the current function; <em>Enclosing</em> = any outer functions' scopes (the closure path); <em>Global</em> = the module namespace; <em>Built-in</em> = the <code>builtins</code> module. Note: comprehensions and generator expressions get their own scope; class bodies do <em>not</em> participate in enclosing lookup for methods."},
  {"sec":"PY-FUNCTIONS","front":"`global` vs `nonlocal` — when do you need each?","back":"You only need them to <strong>rebind</strong> (assign to) a name from an outer scope; reading is always allowed via LEGB. <code>global x</code> binds at module level; <code>nonlocal x</code> binds in the nearest <em>enclosing function</em> scope (and errors if no such variable exists). Without them, any assignment inside a function creates a new <em>local</em>, which also triggers <code>UnboundLocalError</code> if you read it before that assignment."},
  {"sec":"PY-FUNCTIONS","front":"What are the practical limits of `lambda` versus `def`?","back":"<code>lambda</code> is an <strong>expression</strong> producing an anonymous function whose body is a <em>single expression</em> (implicit return) — no statements, no annotations, and its <code>__name__</code> is <code>'&lt;lambda&gt;'</code>. Prefer <code>def</code> whenever you want a name (better tracebacks), a docstring, multiple statements, or type hints. Reserve <code>lambda</code> for tiny inline callables like <code>key=</code> functions."},
  {"sec":"PY-FUNCTIONS","front":"What does `functools.partial(f, *a, **kw)` do that a `lambda` does not?","back":"It returns a callable with some arguments <strong>pre-bound</strong>, but unlike a closure-over-lambda it <em>evaluates and freezes those arguments now</em>, is picklable, and exposes <code>.func</code>, <code>.args</code>, and <code>.keywords</code> for introspection. <code>partial(int, base=2)</code> makes a binary parser. Keyword args set by <code>partial</code> can still be overridden at call time."},
  {"sec":"PY-FUNCTIONS","front":"What does `functools.lru_cache` do, and what is its key correctness constraint?","back":"It memoizes a function's return values keyed by its call arguments, with an LRU eviction bound (<code>maxsize=128</code> default; <code>None</code> = unbounded). The function should be <strong>pure</strong>, and all arguments must be <strong>hashable</strong> (lists/dicts raise <code>TypeError</code>). It exposes <code>.cache_info()</code> and <code>.cache_clear()</code>; beware unbounded growth and holding references that block GC. <code>functools.cache</code> (3.9+) is <code>lru_cache(maxsize=None)</code>."},
  {"sec":"PY-FUNCTIONS","front":"What is the contract of `functools.reduce(f, iterable[, initializer])`?","back":"It folds the iterable left-to-right, applying the binary <code>f(acc, item)</code> and threading the accumulator: <code>reduce(op.add, [1,2,3], 0)</code> → <code>6</code>. With no <em>initializer</em> the first element seeds <code>acc</code> and an empty iterable raises <code>TypeError</code>; with one, an empty iterable returns the initializer. It lives in <code>functools</code> in Py3 (removed from builtins) and is usually less readable than a plain loop or <code>sum</code>/<code>any</code>."}
);

Object.assign(GLOSSARY_DATA, {
  "closure": {"term":"Closure","group":"PY-FUNCTIONS","aliases":["closure","closures","__closure__","cell"],"eli5":"A nested function that remembers the outer variables it used, by pointing at them rather than copying them.","memory":"Closures capture the *box* (cell), not the *ball* (value) inside it.","full":"<p>A <strong>closure</strong> is a nested function plus the references to the free variables it uses from its enclosing scope. Python implements this with <em>cells</em>: each captured variable becomes a <code>cell</code> object, and the inner function stores a tuple of these in <code>fn.__closure__</code>. Because the cell is shared, the closure reads the variable's <em>current</em> value at call time, not its value when the closure was defined.</p><p>This by-reference capture is what makes decorators, callbacks, and stateful function factories work — but it is also the mechanism behind the late-binding loop bug. To inspect: <code>fn.__code__.co_freevars</code> lists the captured names and <code>fn.__closure__[i].cell_contents</code> shows the value.</p>","trap":"Interviewers ask what <code>[lambda: i for i in range(3)]</code> returns when all called. Answer <code>[2,2,2]</code> — every lambda shares one cell and reads its final value. Per-iteration default args (<code>lambda i=i: i</code>) fix it.","worked":"<code>def counter():\n    n = 0\n    def inc():\n        nonlocal n; n += 1; return n\n    return inc</code> — the returned <code>inc</code> keeps incrementing its own private <code>n</code> across calls.","related":["legb-rule","nonlocal-global","late-binding-loop","decorator"]},
  "mutable-default-arg": {"term":"Mutable Default Argument Trap","group":"PY-FUNCTIONS","aliases":["mutable default","default argument","def f(x=[])","shared default"],"eli5":"A default like [] or {} is created once when the function is defined and then secretly reused on every call.","memory":"Defaults are baked at the *def*, not the *call* — one oven, one loaf, reused forever.","full":"<p>Default argument values are evaluated <strong>once, at function-definition time</strong>, and stored on the function object (<code>f.__defaults__</code>). If the default is mutable, every call that omits the argument shares and mutates the <em>same</em> object, so state leaks across calls — a classic source of \"why is this list growing\" bugs.</p><p>The fix is the <code>None</code> sentinel: <code>def f(x=None): x = [] if x is None else x</code>, which builds a fresh object per call. The same trap applies to dicts, sets, and any expression evaluated at def time (including <code>datetime.now()</code> as a default, which freezes the definition timestamp).</p>","trap":"<code>append(1)</code> then <code>append(2)</code> on <code>def append(x, lst=[])</code> returns <code>[1,2]</code>, not <code>[2]</code>. State the sentinel-<code>None</code> fix immediately.","worked":"<code>def add(x, acc=None):\n    acc = [] if acc is None else acc\n    acc.append(x); return acc</code> — now each defaulted call starts empty.","related":["first-class-functions","args-kwargs","closure"]},
  "legb-rule": {"term":"LEGB Scope Rule","group":"PY-FUNCTIONS","aliases":["legb","scope","scoping","name resolution","unboundlocalerror"],"eli5":"Python looks up a name in Local, then Enclosing, then Global, then Built-in scope, in that order.","memory":"\"Legb\" = Local, Enclosing, Global, Built-in — inside-out.","full":"<p><strong>LEGB</strong> is the order Python resolves a name read: <em>Local</em> (current function), <em>Enclosing</em> (outer function scopes — the closure path), <em>Global</em> (module), <em>Built-in</em> (<code>builtins</code>). The first match wins. Whether a name is local is decided <strong>statically at compile time</strong>: if a function assigns to a name <em>anywhere</em> in its body, that name is local throughout, so reading it before assignment raises <code>UnboundLocalError</code>.</p><p>Two surprises for interviews: comprehensions and generator expressions run in their <em>own</em> scope (their loop variable does not leak in Py3), and a <em>class body</em> is not an enclosing scope for its methods — methods can't see class-level names via LEGB, only via <code>self</code>/<code>ClassName</code>.</p>","trap":"<code>x=1\\ndef f():\\n    print(x)\\n    x=2</code> raises <code>UnboundLocalError</code>, not <code>1</code> — the later <code>x=2</code> makes <code>x</code> local for the whole function.","worked":"Shadowing a builtin: defining a local <code>list = [...]</code> then calling <code>list(...)</code> later in the same function fails because Local now wins over Built-in.","related":["closure","nonlocal-global","late-binding-loop"]},
  "nonlocal-global": {"term":"nonlocal vs global","group":"PY-FUNCTIONS","aliases":["nonlocal","global","rebind outer scope"],"eli5":"Keywords that let a function assign to a variable in an outer scope instead of making a new local.","memory":"No keyword ⇒ read-only on outer scopes; add <code>nonlocal</code>/<code>global</code> only to *reassign*.","full":"<p>By default any assignment inside a function creates a <em>local</em> name. To <strong>rebind</strong> a name living in an outer scope you must declare intent: <code>global x</code> targets the <em>module</em> namespace; <code>nonlocal x</code> targets the nearest <em>enclosing function</em> scope. Reading outer names never needs a declaration (LEGB handles it) — only assignment does.</p><p><code>nonlocal</code> requires the name to already exist in an enclosing function (else <code>SyntaxError</code>) and is the idiomatic way to maintain mutable state in a closure without a class. <code>global</code> at module top level is a no-op. Both are about <em>binding</em>, not mutation: you can always mutate a captured mutable object (e.g. <code>lst.append</code>) without either keyword.</p>","trap":"A counter closure that does <code>n += 1</code> without <code>nonlocal n</code> raises <code>UnboundLocalError</code> — <code>+=</code> is an assignment, which forced <code>n</code> local.","worked":"<code>def make():\n    n=0\n    def inc():\n        nonlocal n; n+=1; return n\n    return inc</code>","related":["legb-rule","closure"]},
  "args-kwargs": {"term":"*args / **kwargs","group":"PY-FUNCTIONS","aliases":["args","kwargs","varargs","star args","unpacking","keyword-only"],"eli5":"Stars that collect leftover positional/keyword arguments in a definition, or spread them out in a call.","memory":"Star in a *def* = vacuum (collect); star in a *call* = leaf blower (spread).","full":"<p>In a <em>definition</em>, <code>*args</code> gathers surplus positional arguments into a tuple and <code>**kwargs</code> gathers surplus keyword arguments into a dict. In a <em>call</em>, the same stars do the inverse — <em>unpacking</em> a sequence into positionals (<code>f(*seq)</code>) and a mapping into keywords (<code>f(**d)</code>). This is what lets a decorator wrapper accept and forward <em>any</em> signature with <code>def w(*a, **k): return f(*a, **k)</code>.</p><p>Ordering is fixed: positional, <code>*args</code>, keyword-only, <code>**kwargs</code>. A bare <code>*</code> introduces <strong>keyword-only</strong> parameters; a <code>/</code> (Py3.8+) ends <strong>positional-only</strong> parameters. Duplicate keys when unpacking multiple mappings, or passing a keyword that collides with a positional, raise <code>TypeError</code>.</p>","trap":"<code>def f(a, *, b)</code> rejects <code>f(1, 2)</code> — <code>b</code> is keyword-only. And <code>f(**{'a':1}, a=2)</code> raises <code>TypeError</code> for duplicate <code>a</code>.","worked":"<code>def trace(*a, **k): print(a, k)\ntrace(1, 2, x=3)  # (1, 2) {'x': 3}</code>","related":["decorator","first-class-functions"]},
  "functools-toolbox": {"term":"functools (partial / reduce / lru_cache)","group":"PY-FUNCTIONS","aliases":["functools","partial","reduce","lru_cache","cache","memoize"],"eli5":"Standard-library helpers for partially applying, folding, and caching functions.","memory":"partial = pre-fill, reduce = fold, lru_cache = remember (hashable args only).","full":"<p><code>functools.partial(f, *a, **kw)</code> freezes some arguments now and returns a picklable callable with introspectable <code>.func/.args/.keywords</code> — preferable to a lambda when you need serialization or to avoid late-binding. <code>functools.reduce(f, it[, init])</code> left-folds an iterable with a binary function; without an initializer an empty iterable raises <code>TypeError</code>. It lives in <code>functools</code> in Py3 (not a builtin).</p><p><code>functools.lru_cache(maxsize=128)</code> memoizes a <em>pure</em> function keyed by its arguments, which must be <strong>hashable</strong>; <code>maxsize=None</code> (or <code>functools.cache</code>, 3.9+) is unbounded. It exposes <code>.cache_info()</code> and <code>.cache_clear()</code>. Watch for unbounded memory, cached references blocking GC, and caching methods on instances (the cache pins <code>self</code> alive — prefer <code>cached_property</code> or per-instance caches).</p>","trap":"<code>@lru_cache</code> on a function taking a <code>list</code> argument raises <code>TypeError: unhashable type</code> the first call — and memoizing an impure/IO function silently serves stale results.","worked":"<code>from functools import lru_cache\n@lru_cache(maxsize=None)\ndef fib(n): return n if n<2 else fib(n-1)+fib(n-2)</code> turns exponential into linear.","related":["first-class-functions","decorator"]},
  "late-binding-loop": {"term":"Late-Binding-in-a-Loop","group":"PY-FUNCTIONS","aliases":["late binding","loop closure bug","all return last value"],"eli5":"Closures made in a loop all see the loop variable's final value because they share one cell.","memory":"\"They all phoned the same variable after the loop hung up on its last value.\"","full":"<p>Closures capture variables by reference, and a loop reuses the <em>same</em> loop variable. Every closure created inside the loop points at that one cell, and the loop leaves it holding its last value — so calling them later all returns that final value. <code>fns=[lambda: i for i in range(3)]; [f() for f in fns]</code> → <code>[2,2,2]</code>.</p><p>The fix is to bind a fresh value per iteration: a default-argument snapshot <code>lambda i=i: i</code> (evaluated immediately) or a factory function <code>def make(i): return lambda: i</code>. Note: a comprehension does <em>not</em> give each iteration a fresh cell for the loop variable — all the comprehension's lambdas share one cell (you can verify they are the same <code>__closure__</code> object), so the comprehension form exhibits the bug just like a plain <code>for</code> loop. The only Py3 change is that the comprehension's loop name no longer leaks into the enclosing scope.</p>","trap":"This is the single most common closure interview question. Know both the symptom (<code>[2,2,2]</code>) and the two fixes (default-arg capture / factory).","worked":"<code>handlers = [lambda i=i: i for i in range(3)]\n[h() for h in handlers]  # [0, 1, 2]</code>","related":["closure","legb-rule","nonlocal-global"]},
  "first-class-functions": {"term":"First-Class Functions","group":"PY-FUNCTIONS","aliases":["first-class","higher-order","function object","callable"],"eli5":"Functions are values you can pass around, return, and store just like ints or strings.","memory":"If you can do it to an int, you can do it to a function — assign, pass, return, store.","full":"<p>In Python a function is an ordinary object of type <code>types.FunctionType</code> with attributes (<code>__name__</code>, <code>__doc__</code>, <code>__defaults__</code>, <code>__closure__</code>, <code>__dict__</code>) and can be assigned, passed, returned, and stored in lists/dicts. Anything implementing <code>__call__</code> is a <em>callable</em>, so classes, instances, <code>partial</code> objects, and bound methods all qualify; <code>callable(x)</code> tests this.</p><p>First-class status is the prerequisite for higher-order functions (<code>map</code>, <code>sorted(key=...)</code>, decorators, callbacks). Practically, this enables dispatch tables (<code>{name: handler}</code>) instead of long <code>if/elif</code> chains and lets you build/return specialized functions at runtime via closures and <code>partial</code>.</p>","trap":"<code>f</code> is the function object; <code>f()</code> calls it. Passing <code>f()</code> where a callback wants <code>f</code> registers the <em>result</em>, not the function — a frequent decorator/callback mix-up.","worked":"<code>dispatch = {'add': operator.add, 'sub': operator.sub}\ndispatch['add'](2, 3)  # 5</code> — functions as dict values.","related":["decorator","closure","args-kwargs","functools-toolbox"]}
});
