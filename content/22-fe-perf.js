/* Web Performance & Core Web Vitals (FE-PERF) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"FE-PERF","front":"What are the three Core Web Vitals as of 2026, with their \"good\" thresholds?","back":"<ul><li><strong>LCP (Largest Contentful Paint)</strong> — loading: render time of the largest in-viewport element (image / video poster frame / block-level text). <em>Good ≤ 2.5s</em>.</li><li><strong>INP (Interaction to Next Paint)</strong> — responsiveness: near-worst-case latency from a user interaction to the <em>next paint</em>, across the whole page lifecycle. <em>Good ≤ 200ms</em>. It <strong>replaced FID as a Core Web Vital on March 12, 2024</strong>.</li><li><strong>CLS (Cumulative Layout Shift)</strong> — visual stability: largest <em>session window</em> of unexpected layout-shift scores. <em>Good ≤ 0.1</em> (unitless).</li></ul><p>All three \"good/needs-improvement/poor\" verdicts are assessed at the <strong>p75 of field (RUM) data</strong>, not lab numbers. (Note: each individual metric's p75 is taken; they are not combined into one score.)</p>"},
  {"sec":"FE-PERF","front":"Why did INP replace FID, and what does INP actually measure that FID did not?","back":"<p><strong>FID (First Input Delay)</strong> only measured <em>input delay</em> (time from interaction until the handler <em>starts</em>) and only for the <em>first</em> interaction. It systematically scored well because the delay-only window is small and first interactions are often trivial.</p><p><strong>INP</strong> captures the <em>full interaction latency</em>: <code>input delay + processing time (event handlers) + presentation delay (next paint)</code>, and reports a near-worst-case across <em>all</em> interactions: the single worst interaction below 50, dropping ~1 outlier per 50 above that (effectively ~p98). So INP exposes janky handlers, expensive React re-renders, and forced reflows that FID hid. <em>Gotcha:</em> heavy <code>onClick</code> work that triggers a large synchronous re-render shows up in INP's processing/presentation segments even when input delay is near zero. Scroll and zoom are not counted as interactions.</p>"},
  {"sec":"FE-PERF","front":"Walk through the Critical Rendering Path. Where do CSS and JS block?","back":"<p><code>HTML → DOM</code> and <code>CSS → CSSOM</code> are built in parallel, then combined into the <strong>render tree</strong> → <strong>layout</strong> (geometry/reflow) → <strong>paint</strong> (raster) → <strong>composite</strong> (GPU layers).</p><ul><li><strong>CSS is render-blocking</strong>: the browser won't paint until the CSSOM is ready (avoids a flash of unstyled content), so inline critical CSS and load the rest non-blocking.</li><li><strong>Synchronous <code>&lt;script&gt;</code> is parser-blocking</strong>: it pauses DOM construction and, because scripts can read computed styles, also waits on pending CSSOM before it can execute.</li></ul><p><em>Senior point:</em> <code>layout</code> and <code>paint</code> are the expensive phases; animating only <code>transform</code>/<code>opacity</code> stays on the <strong>composite</strong> step and skips layout+paint entirely.</p>"},
  {"sec":"FE-PERF","front":"<code>async</code> vs <code>defer</code> vs module scripts — when does each execute?","back":"<ul><li><code>&lt;script&gt;</code> (no attr): downloads + executes <strong>immediately</strong>, blocking the parser.</li><li><code>async</code>: downloads off the parser thread, executes <strong>as soon as it arrives</strong> (order NOT guaranteed), and can still interrupt parsing. Best for independent scripts (analytics).</li><li><code>defer</code>: downloads in parallel, executes <strong>after the document is fully parsed</strong>, in <em>document order</em>, immediately before <code>DOMContentLoaded</code> fires. Best for app code with dependencies.</li><li><code>type=\"module\"</code>: <strong>deferred by default</strong> (even inline); adding <code>async</code> to a module makes it execute ASAP, out of order.</li></ul><p><em>Trap:</em> <code>async</code> on multiple interdependent scripts causes nondeterministic ordering bugs — use <code>defer</code>. Note both <code>async</code>/<code>defer</code> are ignored on inline (non-module) scripts.</p>"},
  {"sec":"FE-PERF","front":"How do you optimize LCP for a hero image?","back":"<ul><li><strong>Don't lazy-load it.</strong> <code>loading=\"lazy\"</code> on the LCP image delays discovery and is a classic LCP regression. Use <code>fetchpriority=\"high\"</code> instead.</li><li><strong>Preload</strong> it (or rely on the preload scanner finding it in the static HTML): <code>&lt;link rel=\"preload\" as=\"image\" fetchpriority=\"high\"&gt;</code>, with <code>imagesrcset</code>/<code>imagesizes</code> so the right candidate is fetched.</li><li>Avoid injecting it via JS or via CSS <code>background-image</code> — the <strong>preload scanner can't see those</strong>, delaying discovery until the relevant CSS/JS runs.</li><li>Serve modern formats (AVIF/WebP), correctly sized via <code>srcset</code>/<code>sizes</code>; set explicit <code>width</code>/<code>height</code>.</li></ul><p><em>Mechanism:</em> LCP = TTFB + resource load delay + resource load time + element render delay. Most wins come from cutting <strong>load delay</strong> (early discovery + high priority).</p>"},
  {"sec":"FE-PERF","front":"What causes Cumulative Layout Shift and how do you eliminate it?","back":"<p>CLS comes from <strong>inserting or resizing content above existing content</strong> without reserving space (an <em>unexpected</em> shift = layout shift not preceded by recent user input).</p><ul><li><strong>Size all media:</strong> set <code>width</code>/<code>height</code> attributes (or CSS <code>aspect-ratio</code>) so the box is reserved before the image loads.</li><li><strong>Reserve space for ads/embeds/banners</strong> with min-height placeholders.</li><li><strong>Fonts:</strong> use <code>font-display: optional</code> or fallback <code>@font-face</code> metric overrides (<code>size-adjust</code>/<code>ascent-override</code>) to prevent reflow when the webfont swaps in.</li><li>Animate with <code>transform</code>, not properties that trigger layout.</li></ul><p><em>Key nuance:</em> shifts within <strong>500ms of a <u>discrete</u> input</strong> (click/tap/keypress) get the <code>hadRecentInput</code> flag and are excluded — so opening an accordion doesn't hurt CLS. This exclusion does <em>not</em> apply to scroll, drag, or pinch-zoom.</p>"},
  {"sec":"FE-PERF","front":"Code splitting & dynamic <code>import()</code> — what problem does it solve and what's the cost?","back":"<p>It splits the bundle so the browser only downloads/parses/executes the JS needed for the current route or interaction, shrinking the <strong>initial bundle</strong> and improving TBT/INP and LCP (less main-thread contention and parse cost).</p><p><code>const Modal = lazy(() =&gt; import('./Modal'))</code> creates a separate chunk fetched on demand. Bundlers (Vite/Rollup/esbuild, webpack) split at dynamic-import boundaries.</p><ul><li><em>Tradeoff:</em> a chunk requested at interaction time adds a network round-trip → use <code>&lt;link rel=\"prefetch\"&gt;</code> or import-on-hover/idle to hide that latency, and provide a <code>Suspense</code> fallback.</li><li><em>Gotcha:</em> over-splitting causes request waterfalls and per-request overhead; <strong>route-level</strong> splitting is usually the sweet spot.</li></ul>"},
  {"sec":"FE-PERF","front":"What is tree shaking and what silently breaks it?","back":"<p><strong>Tree shaking</strong> is dead-code elimination based on <strong>static ESM <code>import</code>/<code>export</code></strong> analysis — the bundler keeps only reachable exports.</p><p>It breaks or is disabled when:</p><ul><li>Modules have <strong>side effects</strong> and aren't marked <code>\"sideEffects\": false</code> (or a file-glob list) in <code>package.json</code> — the bundler conservatively keeps them.</li><li>You use <strong>CommonJS</strong> (<code>require</code>) — dynamic, not statically analyzable.</li><li>A <strong>barrel/namespace</strong> import (<code>import * as x</code>, or <code>index.js</code> re-exports) defeats pruning if the analyzer can't prove members are unused.</li></ul><p><em>Senior tip:</em> a <code>/*#__PURE__*/</code> annotation tells the minifier a call has no side effects so an unused result can be dropped. Tree shaking is bundler-time only — it can't remove code the runtime might reach dynamically.</p>"},
  {"sec":"FE-PERF","front":"Compare image formats and responsive image delivery for 2026.","back":"<ul><li><strong>AVIF</strong> — best compression (especially photos), now broadly supported across evergreen browsers; encode is slower/CPU-heavier. <strong>WebP</strong> — solid, near-universal fallback. Serve via <code>&lt;picture&gt;</code> with ordered <code>&lt;source type&gt;</code> elements for graceful fallback to JPEG/PNG.</li><li><strong>Responsive resolution:</strong> <code>srcset</code> (width descriptors) + <code>sizes</code> lets the browser pick the right candidate — avoids shipping a 2000px image to a 360px phone.</li><li><strong>Art direction</strong> (different crops per breakpoint) needs <code>&lt;picture&gt;</code> + <code>media</code>, not <code>srcset</code> alone.</li><li><strong>Always set <code>width</code>/<code>height</code></strong> (or <code>aspect-ratio</code>) to prevent CLS.</li><li><code>loading=\"lazy\"</code> below the fold; <strong>never</strong> on the LCP image.</li></ul>"},
  {"sec":"FE-PERF","front":"Resource hints: <code>preload</code> vs <code>prefetch</code> vs <code>preconnect</code> vs <code>dns-prefetch</code>.","back":"<ul><li><strong><code>preload</code></strong> — high-priority fetch for a resource needed on <em>this</em> page (LCP image, critical font). Requires <code>as</code>. Over-preloading steals bandwidth from other critical requests.</li><li><strong><code>prefetch</code></strong> — lowest-priority fetch for a resource likely needed on the <em>next</em> navigation; lands in the HTTP cache.</li><li><strong><code>preconnect</code></strong> — warm up a cross-origin connection early (DNS + TCP + <strong>TLS</strong>), e.g. a font/CDN host. Costly; limit to a few critical origins.</li><li><strong><code>dns-prefetch</code></strong> — DNS resolution only; cheap, a lighter complement to preconnect for many third-party origins.</li></ul><p><em>Gotcha:</em> a preloaded font needs <code>crossorigin</code> (fonts are fetched in CORS mode) or the preload and the real request use different cache keys and the font downloads twice.</p>"},
  {"sec":"FE-PERF","front":"Explain FOUT vs FOIT and how <code>font-display</code> controls them.","back":"<ul><li><strong>FOIT (Flash of Invisible Text)</strong> — text is invisible during the font's <em>block period</em> while the webfont loads. Hurts LCP/perceived load if the font is slow.</li><li><strong>FOUT (Flash of Unstyled Text)</strong> — fallback font shows immediately, then swaps to the webfont (possible reflow → CLS).</li></ul><p><code>font-display</code> values:</p><ul><li><code>swap</code> — ~0 block, infinite swap period: fallback shown until the webfont loads, then swaps (FOUT; good for LCP, risk CLS).</li><li><code>optional</code> — tiny ~100ms block, <em>no</em> swap period; if the font isn't already cached it won't swap this load → <strong>no layout shift</strong>, best for CLS.</li><li><code>block</code> — ~3s invisible block then swap. <code>fallback</code> — tiny block + short (~3s) swap window.</li></ul><p><em>Pro move:</em> pair <code>swap</code> with <code>size-adjust</code>/<code>ascent-override</code> on a fallback <code>@font-face</code> to match metrics and eliminate the reflow.</p>"},
  {"sec":"FE-PERF","front":"How does content hashing enable aggressive immutable caching?","back":"<p>Build tools emit filenames with a <strong>content hash</strong> (<code>app.4f2a9c.js</code>). Because the URL changes whenever the bytes change, you can serve the file with <code>Cache-Control: public, max-age=31536000, immutable</code> — the browser <em>never revalidates</em> for a year.</p><ul><li>The <strong>HTML entry</strong> must stay <code>no-cache</code>/short-TTL (it references the hashed assets), so a deploy is atomic: new HTML points at new hashes.</li><li><code>immutable</code> additionally skips the conditional revalidation request even on an explicit reload (which would otherwise send <code>If-None-Match</code>).</li></ul><p><em>Gotcha:</em> per-file hashing means a one-line change shouldn't bust the whole bundle — split stable vendor chunks separately so their hash (and cache) survives app-code edits.</p>"},
  {"sec":"FE-PERF","front":"What can a service worker do for performance, and what's the cache-strategy tradeoff?","back":"<p>A <strong>service worker</strong> is a programmable proxy that intercepts <code>fetch</code> events, enabling offline support and instant repeat loads from <strong>Cache Storage</strong>. (It runs off the main thread, but its first install can't speed up the very first visit.)</p><ul><li><strong>Cache-first</strong> — fastest, but risks serving stale assets (safe for content-hashed immutable files).</li><li><strong>Network-first</strong> — fresh, but no win when offline/slow (needs a timeout fallback).</li><li><strong>Stale-while-revalidate</strong> — serve cache instantly, update in background; best UX/freshness balance for many assets.</li></ul><p><em>Gotchas:</em> <code>sw.js</code> is itself checked for byte changes on navigation, so don't long-cache it; a bad cache-first SW can pin users to an old broken build — always ship a <code>skipWaiting</code>/kill-switch path.</p>"},
  {"sec":"FE-PERF","front":"Why is list virtualization (windowing) a major INP and memory win?","back":"<p>Rendering 10,000 rows creates 10,000 DOM nodes → huge layout/paint cost, memory bloat, and slow re-renders that wreck INP. <strong>Virtualization</strong> renders only the rows in (and a small buffer around) the viewport, recycling nodes as you scroll.</p><ul><li>Total DOM stays ~constant (e.g. ~30 nodes) regardless of dataset size.</li><li>Libraries: <code>@tanstack/react-virtual</code>, <code>react-window</code>.</li></ul><p><em>Gotchas:</em> variable row heights need measurement/estimation; native <code>Ctrl+F</code> find and screen-reader traversal miss off-screen rows (use a large <code>overscan</code> or pair with semantics); absolute positioning can fight sticky headers. The CSS <code>content-visibility: auto</code> (with <code>contain-intrinsic-size</code>) is a lighter alternative that skips rendering off-screen content <em>without</em> unmounting it.</p>"},
  {"sec":"FE-PERF","front":"When does memoization help and when is it pure overhead in React?","back":"<p>Memoization (<code>useMemo</code>, <code>useCallback</code>, <code>React.memo</code>) caches a value/component to skip recompute or re-render when inputs are referentially equal.</p><ul><li><strong>Helps:</strong> genuinely expensive computations, or stabilizing object/array/function props passed to a <code>memo</code>'d child or used in a dependency array.</li><li><strong>Hurts:</strong> trivial values — you pay for cache storage + the comparison every render for no gain; and a new inline object/array/function as a dep silently invalidates the cache, making it useless.</li></ul><p><em>2026 note:</em> the <strong>React Compiler</strong> auto-memoizes at build time, so manual <code>useMemo</code>/<code>useCallback</code> become largely unnecessary in compiled codebases. Remember memoization fixes <em>re-render</em> cost, not initial <em>mount</em> cost.</p>"},
  {"sec":"FE-PERF","front":"Web Workers: what do they solve for INP, and what's the hard constraint?","back":"<p>A <strong>Web Worker</strong> runs JS on a <em>separate thread</em>, keeping the main thread free for input and rendering — ideal for parsing, crypto, image processing, or large data transforms that would otherwise block and inflate INP/TBT.</p><ul><li><strong>No DOM access</strong> and no shared scope — communicate via <code>postMessage</code> (structured clone) or <code>SharedArrayBuffer</code> (which needs cross-origin isolation via COOP/COEP headers).</li><li>Large payloads should use <strong>transferables</strong> (<code>postMessage(buf, [buf])</code>) to move (zero-copy) rather than clone memory.</li></ul><p><em>Gotcha:</em> structured-clone serialization cost can exceed the compute saved for small/chatty workloads — workers pay off for <em>chunky</em> CPU-bound work, not frequent tiny messages.</p>"},
  {"sec":"FE-PERF","front":"Debounce vs throttle — definitions and the right use for each.","back":"<ul><li><strong>Debounce</strong> — delay execution until <em>N ms after the last</em> call; collapses a burst into one (typically trailing) run. Use for <strong>search-as-you-type</strong>, autosave, resize-settle.</li><li><strong>Throttle</strong> — run <em>at most once per N ms</em> during a continuous stream. Use for <strong>scroll/mousemove/drag</strong> handlers where you want periodic updates.</li></ul><p><em>Senior nuances:</em> for visibility/size, prefer <code>IntersectionObserver</code>/<code>ResizeObserver</code> over throttled listeners (event-driven, off the main-thread polling path). For animation at frame cadence, throttle with <code>requestAnimationFrame</code> rather than a timer. Both patterns cut handler invocations → directly lower INP processing time.</p>"},
  {"sec":"FE-PERF","front":"Lighthouse lab data vs RUM field data — why can they disagree, and which gates Core Web Vitals?","back":"<p><strong>Lab (Lighthouse)</strong> is a single synthetic run on a fixed device/network — reproducible, great for debugging and CI regression gates, but it can't measure INP (no real interactions) and reflects one environment. (Lighthouse reports TBT as its responsiveness proxy instead.)</p><p><strong>Field/RUM</strong> (Chrome UX Report, the <code>web-vitals</code> JS lib) aggregates <em>real users'</em> devices, networks, and interactions at the <strong>p75</strong>.</p><ul><li><strong>Field data is what determines the Core Web Vitals pass/fail</strong> and the page-experience signal — lab is diagnostic only.</li><li>They diverge because of slow real devices, third-party scripts, cache state, and real interaction patterns lab can't replicate.</li></ul><p>Capture field metrics with the <code>web-vitals</code> library (<code>onINP</code>, <code>onLCP</code>, <code>onCLS</code>), which wraps the underlying <strong>PerformanceObserver</strong> entries, and beacon them with <code>navigator.sendBeacon</code> (or <code>fetch(..., {keepalive:true})</code>).</p>"}
);

Object.assign(GLOSSARY_DATA, {
  "lcp": {"term":"Largest Contentful Paint (LCP)","group":"FE-PERF","eli5":"How long until the biggest thing on screen finishes showing up.","memory":"LCP = the Largest Content to Paint — your hero moment.","full":"<p><strong>LCP</strong> measures loading performance: the render time of the largest image, video poster frame, or block-level text visible in the viewport, judged at the <strong>p75 of field data</strong>. <em>Good ≤ 2.5s.</em> It decomposes into TTFB + resource load delay + resource load time + element render delay.</p><p>The biggest lever is usually cutting <strong>load delay</strong> — make sure the LCP resource is discovered early (present in static HTML for the preload scanner, not injected by JS or a CSS background-image), fetched at high priority (<code>fetchpriority=\"high\"</code>), and never <code>loading=\"lazy\"</code>.</p>","trap":"Lazy-loading or CSS-background-loading the hero image — it hides the resource from the preload scanner and regresses LCP even though the image itself is 'optimized'.","worked":"<link rel=\"preload\" as=\"image\" href=\"hero.avif\" imagesrcset=\"hero-800.avif 800w, hero-1600.avif 1600w\" fetchpriority=\"high\">","related":["core-web-vitals","critical-rendering-path","resource-hints","responsive-images"]},
  "inp": {"term":"Interaction to Next Paint (INP)","group":"FE-PERF","eli5":"After you click or tap, how long until the screen visibly responds — measured across the whole visit.","memory":"INP = Is the page Now Painting? It replaced FID on March 12, 2024.","full":"<p><strong>INP</strong> reports a near-worst-case interaction latency across all click/tap/keypress interactions on the page: <code>input delay + processing time + presentation delay</code>. <em>Good ≤ 200ms</em> at p75. With under 50 interactions it's the single slowest; above that it drops ~1 outlier per 50 (effectively ~p98). It <strong>replaced FID as a Core Web Vital on March 12, 2024</strong> because FID only measured the input delay of the first interaction.</p><p>To improve INP: break up long tasks (yield to the main thread, e.g. <code>scheduler.yield()</code>), defer non-urgent updates (React <code>useTransition</code>), move heavy work to Web Workers, and avoid synchronous layout thrash in handlers. Scroll/zoom are not counted.</p>","trap":"Assuming a fast input delay means good INP — INP also counts handler processing and the next paint, where big synchronous re-renders hide.","worked":"button.onclick = () => { startTransition(() => setExpensiveState(next)); }; // keeps the click's next paint fast","related":["core-web-vitals"]},
  "cls": {"term":"Cumulative Layout Shift (CLS)","group":"FE-PERF","eli5":"How much stuff jumps around unexpectedly while the page loads.","memory":"CLS = Content Leaping Suddenly — reserve the space so it can't.","full":"<p><strong>CLS</strong> is the largest <em>session window</em> (≤5s long, ≤1s gaps) of summed unexpected layout-shift scores, where each score = impact fraction × distance fraction. <em>Good ≤ 0.1.</em> Shifts within 500ms of a <em>discrete</em> input (click/tap/keypress) carry the <code>hadRecentInput</code> flag and are excluded as 'expected'; scroll and pinch-zoom do NOT earn that exclusion.</p><p>Prevent it by reserving space: <code>width</code>/<code>height</code> or <code>aspect-ratio</code> on media, placeholders for ads/embeds, and font metric overrides so a webfont swap doesn't reflow text.</p>","trap":"Thinking CLS is only about images — late-loading webfonts, injected banners, and ad slots are common culprits; and the 500ms exclusion is for clicks/keys, not scroll.","worked":"<img src=\"a.avif\" width=\"800\" height=\"450\"> /* browser reserves the 16:9 box before load → zero shift */","related":["core-web-vitals","font-display","responsive-images"]},
  "core-web-vitals": {"term":"Core Web Vitals","group":"FE-PERF","eli5":"Google's three headline numbers for whether a page feels fast and stable.","memory":"LIC: Loading (LCP), Interactivity (INP), Cumulative shift (CLS).","full":"<p><strong>Core Web Vitals</strong> are the three user-centric metrics Google uses to grade page experience: <strong>LCP</strong> (loading ≤2.5s), <strong>INP</strong> (responsiveness ≤200ms), and <strong>CLS</strong> (stability ≤0.1). Each metric is judged independently at the <strong>75th percentile of real-user (field) data</strong> — there is no single combined CWV score.</p><p>Lab tools like Lighthouse help diagnose them, but only field/RUM data (Chrome UX Report) determines the pass/fail and page-experience signal.</p>","trap":"Optimizing for Lighthouse scores and assuming the page passes CWV — only p75 field data counts, and Lighthouse can't even measure INP.","worked":"import {onLCP, onINP, onCLS} from 'web-vitals'; onINP(m => navigator.sendBeacon('/rum', JSON.stringify(m)));","related":["lcp","inp","cls","lab-vs-field"]},
  "critical-rendering-path": {"term":"Critical Rendering Path","group":"FE-PERF","eli5":"The exact sequence the browser follows to turn HTML/CSS/JS into pixels.","memory":"DOM + CSSOM → Render tree → Layout → Paint → Composite.","full":"<p>The <strong>critical rendering path</strong> is: build the <strong>DOM</strong> from HTML and the <strong>CSSOM</strong> from CSS (in parallel), combine into the <strong>render tree</strong>, compute geometry in <strong>layout</strong> (reflow), rasterize in <strong>paint</strong>, and assemble GPU layers in <strong>composite</strong>.</p><p>CSS is render-blocking and synchronous JS is parser-blocking, so the goal is to minimize critical bytes: inline critical CSS, load the rest non-blocking, and use <code>async</code>/<code>defer</code> on scripts. Animating only <code>transform</code>/<code>opacity</code> stays on the composite step, skipping layout and paint.</p>","trap":"Believing all CSS-property animations are equal — animating <code>width</code>/<code>top</code>/<code>margin</code> triggers layout+paint every frame, while <code>transform</code> is composite-only.","worked":"/* cheap: composited */ .x{transform:translateX(100px)} /* expensive: relayout */ .y{left:100px}","related":["render-blocking","async-defer","core-web-vitals"]},
  "render-blocking": {"term":"Render-Blocking Resources","group":"FE-PERF","eli5":"Files the browser must finish before it can paint anything.","memory":"CSS blocks the paint; sync JS blocks the parser (and waits on CSS).","full":"<p><strong>Render-blocking</strong> resources delay first paint. <strong>CSS is render-blocking</strong> by default (the browser won't paint with an incomplete CSSOM, to avoid a flash of unstyled content). <strong>Synchronous <code>&lt;script&gt;</code> is parser-blocking</strong> and, because scripts can query computed styles, must also wait on any pending CSSOM before executing.</p><p>Mitigations: inline critical CSS and load the rest non-blocking (e.g. the <code>media</code>-swap trick), and use <code>defer</code>/<code>async</code> on scripts. <code>&lt;link rel=preload&gt;</code> alone does NOT make CSS non-blocking — it just fetches it early.</p>","trap":"Adding a stylesheet in the <head> 'just for one component' — it blocks the entire page's first paint until it loads.","worked":"<link rel=\"stylesheet\" href=\"non-critical.css\" media=\"print\" onload=\"this.media='all'\"> /* loads non-blocking */","related":["critical-rendering-path","async-defer","lcp"]},
  "async-defer": {"term":"async vs defer","group":"FE-PERF","eli5":"Two ways to load scripts without freezing the page parse.","memory":"async = run ASAP, any order; defer = wait for DOM, keep order.","full":"<p>Both download external scripts off the parser thread. <strong><code>async</code></strong> executes the moment the script arrives, in <em>no guaranteed order</em>, and can still interrupt parsing — good for independent scripts like analytics. <strong><code>defer</code></strong> executes after the document is fully parsed, in <em>document order</em>, immediately before <code>DOMContentLoaded</code> — good for ordered app code.</p><p><code>type=\"module\"</code> scripts are deferred by default; both attributes are ignored on inline non-module scripts.</p>","trap":"Using async on multiple interdependent scripts — nondeterministic execution order causes 'X is not defined' race bugs. Use defer.","worked":"&lt;script src=\"analytics.js\" async&gt;&lt;/script&gt; &lt;script src=\"app.js\" defer&gt;&lt;/script&gt;","related":["render-blocking","critical-rendering-path","code-splitting"]},
  "code-splitting": {"term":"Code Splitting & Tree Shaking","group":"FE-PERF","eli5":"Ship only the JS a page needs right now, and drop code nobody imports.","memory":"Split by route (load less now), shake the dead exports (keep less total).","full":"<p><strong>Code splitting</strong> uses dynamic <code>import()</code> to break the bundle into chunks loaded on demand (per route/interaction), shrinking the initial download and main-thread parse cost. <strong>Tree shaking</strong> is static dead-code elimination over ESM imports/exports, dropping unreachable code.</p><p>Tree shaking requires real ES modules and correct <code>\"sideEffects\"</code> metadata; CommonJS and unmarked side-effectful modules defeat it. Over-splitting creates request waterfalls, so route-level splitting is usually optimal.</p>","trap":"Expecting tree shaking to prune a CommonJS dependency or a barrel file with side effects — it can't statically prove they're safe to drop.","worked":"const Chart = lazy(() => import('./Chart')); // separate chunk, fetched when rendered","related":["async-defer","caching-immutable","lcp"]},
  "resource-hints": {"term":"Resource Hints","group":"FE-PERF","eli5":"Tell the browser to start fetching or connecting early so it's ready when needed.","memory":"preload=now, prefetch=next page, preconnect=warm the socket, dns-prefetch=just DNS.","full":"<p><strong>Resource hints</strong> let you front-run the browser's discovery. <code>preload</code> = high-priority fetch for something needed on this page (needs <code>as</code>); <code>prefetch</code> = lowest-priority fetch for the next navigation; <code>preconnect</code> = open DNS+TCP+TLS to a cross-origin host early; <code>dns-prefetch</code> = resolve DNS only (cheap complement for many third-party origins).</p><p>Use sparingly — each preload/preconnect competes for bandwidth and connection slots.</p>","trap":"Forgetting crossorigin on a preloaded font — fonts fetch in CORS mode, so the preload and the real fetch use different cache keys and the font downloads twice.","worked":"<link rel=\"preconnect\" href=\"https://cdn.example.com\" crossorigin>","related":["lcp","font-display","caching-immutable"]},
  "font-display": {"term":"font-display (FOUT/FOIT)","group":"FE-PERF","eli5":"Controls whether text is hidden or shown in a fallback font while a webfont loads.","memory":"swap = show fallback then swap (FOUT); optional = no swap, no shift.","full":"<p><code>font-display</code> governs the webfont swap behavior. <strong>FOIT</strong> hides text during the block period; <strong>FOUT</strong> shows a fallback then swaps in. <code>swap</code> (~0 block, infinite swap) minimizes invisible text — good for LCP but risks a reflow (CLS); <code>optional</code> (tiny block, no swap) avoids the swap on slow/uncached loads, eliminating layout shift; <code>block</code> (~3s invisible) and <code>fallback</code> (tiny block + short swap window) tune the periods.</p><p>Best practice pairs <code>swap</code> with fallback <code>@font-face</code> metric overrides (<code>size-adjust</code>, <code>ascent-override</code>) so the swap causes no reflow.</p>","trap":"Defaulting to font-display:block 'so text doesn't jump' — it causes invisible text (FOIT) for up to ~3s that delays LCP on slow networks.","worked":"@font-face{font-family:Inter;src:url(inter.woff2);font-display:swap;size-adjust:97%}","related":["cls","lcp","resource-hints"]},
  "caching-immutable": {"term":"Immutable Caching & Content Hashing","group":"FE-PERF","eli5":"Give files a fingerprinted name so the browser can cache them forever and still get updates.","memory":"Hash the name → cache forever immutable; keep the HTML fresh.","full":"<p>Build tools emit assets with a <strong>content hash</strong> in the filename (<code>app.4f2a9c.js</code>). Since the URL changes only when bytes change, these can be served <code>Cache-Control: public, max-age=31536000, immutable</code> — never revalidated, even on reload. The <strong>HTML entry stays short-TTL/no-cache</strong> and points at the new hashes, making deploys atomic.</p><p>A <strong>CDN</strong> stores these near users; a <strong>service worker</strong> can additionally serve them from Cache Storage for instant repeat loads.</p>","trap":"Long-caching the HTML or the service worker file itself — users get pinned to a stale build with no way to receive the new asset URLs.","worked":"Cache-Control: public, max-age=31536000, immutable   # only for content-hashed assets","related":["service-worker","code-splitting","resource-hints"]},
  "service-worker": {"term":"Service Worker","group":"FE-PERF","eli5":"A background proxy that can serve your app from cache, even offline.","memory":"SW = the fetch middleman; pick cache-first vs network-first vs SWR.","full":"<p>A <strong>service worker</strong> intercepts network requests via the <code>fetch</code> event and can respond from <strong>Cache Storage</strong>, enabling offline use and instant repeat loads. Common strategies: <strong>cache-first</strong> (fast, risks staleness), <strong>network-first</strong> (fresh, needs a timeout for offline), and <strong>stale-while-revalidate</strong> (serve cache, update in background).</p><p>It activates a new version when <code>sw.js</code> changes byte-wise, so that file must not be long-cached, and you should ship a <code>skipWaiting</code>/kill-switch path to escape a broken cached build. It can't speed up the very first visit (nothing is cached yet).</p>","trap":"Cache-first on app HTML/JS without versioning — users get stuck on an old broken build that the SW keeps serving.","worked":"self.addEventListener('fetch', e => e.respondWith(caches.match(e.request).then(c => c || fetch(e.request))));","related":["caching-immutable","core-web-vitals"]},
  "lab-vs-field": {"term":"Lab vs Field (RUM) Measurement","group":"FE-PERF","eli5":"Lab is one controlled test run; field is what real users actually experience.","memory":"Lab = lab coat (synthetic, repeatable); Field = real people at p75.","full":"<p><strong>Lab data</strong> (Lighthouse, WebPageTest) runs synthetically on a fixed device/network — reproducible and great for CI regression gates and debugging, but it can't measure INP (it reports TBT as a proxy) and reflects one environment. <strong>Field/RUM data</strong> (Chrome UX Report, the <code>web-vitals</code> library over <code>PerformanceObserver</code>) aggregates real users at the <strong>p75</strong>.</p><p><em>Field data determines Core Web Vitals pass/fail and the page-experience signal; lab is diagnostic.</em></p>","trap":"Treating a green Lighthouse score as 'passing Core Web Vitals' — only p75 field data counts, and slow real devices/3rd-party scripts often diverge sharply.","worked":"import {onINP} from 'web-vitals'; onINP(({value,id}) => beacon('/rum', {value,id}));","related":["core-web-vitals","inp","lcp"]}
});
