/* Teach me from the ground up: IP-PYTHON (auto-assembled) */
enh({
  "decimal-not-float": {"teach":"<p>At its core, this is about a simple, painful truth: <strong>computers store regular decimal numbers as approximations, and approximations are unacceptable for money.</strong></p><h4>Why your computer can't count cents</h4><p>A normal Python number like <code>0.1</code> is a <em>float</em> — stored in <strong>binary</strong> (base-2). The trouble is that one-tenth has no exact representation in base-2, just as one-third (<code>0.3333...</code>) has no exact representation in base-10. So the machine stores the <em>closest binary value it can</em>, which is microscopically off. Run <code>0.1 + 0.2</code> and you get <code>0.30000000000000004</code>. That trailing <code>4</code> is the leftover error leaking into view.</p><div class=\"analogy\"><p>Imagine measuring ingredients with a ruler marked only in halves, quarters, and eighths of an inch. You can nail $\\tfrac{1}{2}$ and $\\tfrac{1}{4}$ exactly, but \"one-tenth of an inch\" forces you to eyeball the nearest tick. Every recipe inherits a tiny smudge. Binary floats are that ruler; base-10 amounts like a dime are the tenth-inch you can never land on cleanly.</p></div><h4>What goes wrong in the real world</h4><p>In post-trade <strong>reconciliation</strong>, you compare your computed numbers against an external <strong>truth file</strong>. A penny-sized float smudge shows up as a <strong>break</strong> — a phantom mismatch that isn't a real discrepancy, just rounding noise. Multiply across thousands of positions and netting goes subtly wrong. That is a real bug, not a cosmetic one.</p><h4>The fix: Decimal</h4><p><code>decimal.Decimal</code> stores numbers in <strong>base-10</strong>, exactly the way humans write money, so <code>0.10</code> really is one-tenth. You get three powers:</p><ul><li><strong>Exactness</strong> — no hidden binary drift.</li><li><strong>Precision control</strong> via <code>getcontext().prec</code> — how many significant digits to keep.</li><li><strong>Explicit rounding</strong> via <code>quantize(Decimal('0.01'), rounding=ROUND_HALF_EVEN)</code> — snap to cents using banker's rounding, which rounds ties to the nearest even digit to avoid systematic upward bias.</li></ul><h4>The one rule you must never break</h4><p>Construct from a <strong>string</strong>: <code>Decimal('0.1')</code>. If you write <code>Decimal(0.1)</code>, Python first builds the <em>flawed float</em> <code>0.1</code>, then faithfully copies that flaw into your Decimal. The error is already baked in before Decimal ever sees it — the type can't rescue you from poison you fed it. In the worked example, fee and gross are built from strings, multiplied, then quantized to <code>Decimal('998.50')</code> — exact to the cent.</p><p>The same discipline extends to storage: in SQL use <code>NUMERIC</code>/<code>DECIMAL</code> columns, never <code>FLOAT</code>, so the database doesn't reintroduce the error you worked to avoid.</p><div class=\"key\"><p>Money is base-10 and must be exact, so represent it with <code>Decimal</code> built from <strong>strings</strong> (never floats) and round explicitly — anything else lets binary rounding noise masquerade as a real reconciliation break.</p></div>"},
  "backoff-jitter": {"teach":"<p>At its core, this is good manners for a struggling server: <strong>when a call fails, don't pound on the door — wait, wait longer each time, and stagger your knocks so you're not banging in unison with everyone else.</strong></p><h4>Part one: exponential backoff</h4><p>When a request fails for a <em>temporary</em> reason, retrying instantly is the worst thing you can do — the venue is already overwhelmed, and your immediate retry adds to the pile. <strong>Exponential backoff</strong> grows the wait geometrically: <code>base * 2**attempt</code>. With <code>base = 1s</code> you wait roughly 1s, 2s, 4s, 8s… The exponent means each failure buys the server dramatically more breathing room than the last, while a <strong>cap</strong> stops the delay from ballooning to absurd lengths.</p><h4>Part two: why you also need jitter</h4><p>Pure backoff has a hidden flaw. If an outage knocks out a thousand clients at the same instant, they all compute the <em>same</em> 1s, then 2s, then 4s delays — and retry in perfect lockstep. Each synchronized wave re-triggers the rate limit and re-fails together. That self-reinforcing stampede is the <strong>thundering herd</strong>.</p><p><strong>Full jitter</strong> breaks the synchrony by picking a <em>random</em> delay in <code>[0, computed]</code>: <code>random.uniform(0, min(cap, base * 2 ** attempt))</code>. Now those thousand clients smear their retries across the whole window instead of slamming the same instant.</p><div class=\"analogy\"><p>Picture a crowded restaurant turning people away. Without backoff, everyone keeps re-asking every five seconds — chaos. With plain backoff, the whole crowd agrees to come back in 1, then 2, then 4 minutes — but they all arrive together and overwhelm the host again. With <em>jitter</em>, each person flips a coin and shows up at a random moment inside that window. The crowd dissolves into a manageable trickle.</p></div><h4>When backoff bites</h4><p>Only retry <strong>transient</strong> failures — timeouts, <code>429 Too Many Requests</code>, and <code>5xx</code> server errors. These might succeed next time. A <code>400</code>/<code>422</code> (malformed request) or <code>409</code> (idempotency conflict) is <strong>deterministic</strong>: it will fail identically forever. Retrying it wastes effort and can do harm. When the server sends a <code>Retry-After</code> header, honor it — that's the venue telling you exactly how long to wait.</p><h4>The dangerous interaction with writes</h4><p>Retrying a read is safe. Retrying a <em>write</em> — placing an order — is risky: maybe the first attempt actually succeeded and only the response got lost, so your retry would double-submit. Backoff is only safe on writes when paired with an <strong>idempotency key</strong>, so the server recognizes the repeat and applies the operation exactly once.</p><div class=\"key\"><p>Back off <strong>exponentially</strong> to give a struggling venue room, add <strong>full jitter</strong> to prevent synchronized retry stampedes, retry <em>only</em> transient errors — and never retry a write without an idempotency key.</p></div>"},
  "dtcc-chain": {"teach":"<p>At its core, the DTCC chain is the <strong>invisible plumbing that turns \"I agreed to buy your shares\" into \"the shares and cash have actually changed hands\"</strong> — safely, and without each firm settling thousands of trades one by one.</p><h4>The three stages</h4><p>When you trade US equities, the trade flows through three distinct services, each with a different job:</p><ul><li><strong>ITP (pre-settlement matching)</strong> — right after the trade, both sides confirm the economic details agree: same security, quantity, price. This is <strong>match-to-instruct</strong> / <em>affirmation</em>. Think of it as both parties signing off that they recorded the same deal before any money moves.</li><li><strong>NSCC (the central counterparty, or CCP)</strong> — steps into the middle of every trade through <strong>novation</strong>: it becomes the buyer to every seller and the seller to every buyer. It then performs <strong>multilateral netting</strong> — collapsing all your buys and sells in a security into a single net obligation — and <strong>guarantees</strong> the trade even if your counterparty defaults. This runs through <strong>Continuous Net Settlement (CNS)</strong>.</li><li><strong>DTC (the central securities depository)</strong> — actually moves the shares and provides <strong>custody</strong>, all via <strong>book-entry</strong>: nobody ships paper certificates; ownership just changes in DTC's electronic records.</li></ul><div class=\"analogy\"><p>Picture a giant office where everyone owes everyone coffees. Without netting, hundreds of cups crisscross the room all day. NSCC is the office manager who tallies it all up and tells you the one truth: \"You owe three coffees, net.\" DTC is the shared cupboard where the mugs physically live — settlement is just the manager re-labeling whose shelf each mug sits on. No mug ever leaves the room.</p></div><h4>Why affirmation gates everything</h4><p>Affirmation in ITP is the <strong>gate</strong>: an <em>unaffirmed</em> trade can't proceed to NSCC/DTC settlement on time and becomes a <strong>fail</strong> — a missed settlement with cost and risk. This is precisely why reconciliation tooling exists: to match and affirm trades <em>inside the <strong>T+1</strong> window</em> (settlement one business day after trade) so everything flows cleanly down the chain.</p><h4>The classic interview trap</h4><p><strong>NSCC and DTC are different entities doing different things.</strong> NSCC is the CCP that <em>nets and guarantees</em> obligations; DTC is the depository that <em>custodies and settles</em> the actual securities. Conflating them — saying \"DTC nets the trades\" or \"NSCC holds the shares\" — is a common slip that signals you don't really understand the flow.</p><p>Put it together: an affirmed trade goes to NSCC, which nets all your activity in a security into one CNS obligation, then DTC moves the shares book-entry between accounts. Match → net/guarantee → settle.</p><div class=\"key\"><p>Three entities, three jobs: <strong>ITP matches/affirms</strong>, <strong>NSCC nets and guarantees</strong> as the CCP, <strong>DTC custodies and settles</strong> book-entry — and affirmation is the gate that lets a trade reach settlement inside T+1.</p></div>"},
  "rest-websocket": {"teach":"<p>At its core, this is how modern crypto and prediction-market exchanges talk to you: <strong>ordinary web requests to <em>do</em> things, plus an always-open pipe to <em>hear</em> things as they happen.</strong></p><h4>Two channels, two jobs</h4><p>Traditional equity venues speak <strong>FIX</strong>, a specialized financial messaging protocol. But crypto and prediction markets like <strong>Kalshi</strong> and <strong>Polymarket</strong> are predominantly <strong>REST + WebSocket</strong> — the same web technologies that power ordinary websites:</p><ul><li><strong>REST</strong> (HTTP request/response) is <em>pull</em>: you <code>POST</code> to place an order, <code>GET</code> to query its status. One ask, one answer.</li><li><strong>WebSocket (WS)</strong> is a persistent two-way <em>push</em> channel: you subscribe once, and the venue streams fills and order-book updates to you the moment they occur — no polling.</li></ul><div class=\"analogy\"><p>REST is texting a question and waiting for the reply — fine for \"place this order\" or \"what's my status?\". WebSocket is leaving a phone line open: the exchange whispers every fill and price tick into your ear in real time. You wouldn't text \"any updates?\" a thousand times a second — you'd keep the line open.</p></div><h4>What production clients must handle</h4><p>A toy script ignores failure; a production client treats it as the norm:</p><ul><li><strong>Idempotency via client order IDs</strong> — attach a <code>cloid</code> so a retried order isn't placed twice.</li><li><strong>Rate-limit handling</strong> — respect <code>429</code> responses and the <code>Retry-After</code> header.</li><li><strong>Retries with exponential backoff + jitter</strong> — so transient failures recover without stampeding the venue.</li><li><strong>Signing/nonce auth</strong> — cryptographically sign requests; a <em>nonce</em> (a one-time increasing number) stops an attacker from replaying a captured message.</li></ul><h4>The WebSocket trap: snapshot then deltas</h4><p>An order book stream sends a full <strong>snapshot</strong> once, then a flood of small <strong>deltas</strong> (\"add 10 here, remove 5 there\"). Deltas are meaningless without that anchor — and dangerous on their own. If you apply deltas <em>without</em> first anchoring to a snapshot and tracking <strong>sequence numbers</strong>, a single dropped message silently corrupts your book: you keep trading off a picture that's quietly wrong, with no error to warn you.</p><p>So the correct <strong>reconnect-and-resync</strong> dance after any disconnect is: re-fetch a fresh snapshot, then resume applying deltas, verifying sequence numbers have no gaps.</p><h4>Why normalization matters</h4><p>A multi-asset desk trades equities (FIX) <em>and</em> crypto (REST/WS). The winning move is to <strong>normalize</strong> every venue's messages into a single internal trade model first, so all the downstream logic — and the final reconciliation against the external settlement truth — speaks one language regardless of where the trade came from.</p><div class=\"key\"><p>Crypto/prediction venues are REST (do) + WebSocket (hear); production-grade clients need client-order-id idempotency, backoff, and signed auth — and you must <strong>anchor the order book to a snapshot and track sequence numbers</strong>, or a dropped delta silently corrupts your book.</p></div>"},
  "datetime-tz": {"teach":"<p>At its core, this is one disciplined habit: <strong>store every timestamp in UTC with its timezone attached, and only convert to a local market time at the very last moment — for a cutoff check or for display.</strong></p><h4>Aware vs naive — the foundational split</h4><p>A Python datetime is either <strong>aware</strong> (it knows its timezone) or <strong>naive</strong> (just digits, no context). A naive datetime is a trap: it <em>looks</em> fine but secretly assumes \"local time\" — and whose local? Always work aware: <code>datetime.now(timezone.utc)</code> gives you an unambiguous moment that means the same thing everywhere on Earth.</p><div class=\"analogy\"><p>UTC is like writing every entry in a shared logbook in one master clock, then translating to the reader's wall clock only when you hand them the page. If instead everyone scribbles in their own local time without saying which zone, the logbook becomes a pile of numbers nobody can safely compare — was \"3:00\" in New York or Tokyo?</p></div><h4>Why UTC, and why convert only at the edges</h4><p>UTC has no Daylight Saving Time, so arithmetic on it is clean and monotonic. You convert to a market zone with <code>zoneinfo.ZoneInfo('America/New_York')</code> <em>only</em> for things that genuinely depend on local wall-clock time: the DTCC <strong>9:00 PM ET affirmation</strong> cutoff and <strong>T+1</strong> settlement-date math. Because <code>ZoneInfo</code> knows the full DST history, it returns the right offset automatically — sometimes -05:00, sometimes -04:00.</p><h4>The two traps</h4><p><strong>Trap one — a hardcoded offset.</strong> Writing <code>-05:00</code> for \"Eastern\" is <em>wrong half the year</em>. From spring to fall, New York is on Daylight Time at -04:00. A fixed offset silently shifts your cutoff by an hour for months — exactly long enough to push a trade past 9 PM ET when it actually wasn't.</p><p><strong>Trap two — mislabeled timestamps.</strong> If a UTC timestamp gets <em>labeled</em> as ET (or a naive value is assumed local), every downstream date shifts. That can move a trade or settlement onto the wrong calendar day and quietly break reconciliation. The defense is a hard rule: <strong>always derive ET from the raw source timestamp</strong>, never trust a pre-labeled column that someone upstream may have mislabeled.</p><h4>Business days vs 24/7</h4><p>Settlement-date arithmetic for equities counts <strong>business days</strong> off a trading calendar — you must skip weekends and market holidays, because exchanges and the DTCC chain don't settle then. Crypto, by contrast, runs <strong>24/7</strong> with no holidays, so its date math is simpler but never sleeps.</p><p>The worked example shows the whole discipline: capture in UTC, <code>astimezone</code> to New York, then compare <code>et.time() &lt; time(21, 0)</code> for the 9 PM cutoff — DST handled for free.</p><div class=\"key\"><p>Compute in <strong>UTC-aware</strong> datetimes and convert to a named market zone (<code>ZoneInfo</code>, never a hardcoded offset) only for cutoffs and display — and always derive ET from the <em>raw</em> timestamp, because a mislabeled or naive time silently shifts the settlement date and breaks recon.</p></div>"}
});
