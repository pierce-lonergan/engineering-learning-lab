/* Teach me from the ground up: FE-TEST (auto-assembled) */
enh({
  "testing-trophy": {"teach":"<p>At its core, the testing trophy is just a piece of advice about <em>where to spend your testing effort</em> so you catch the most real bugs for the least pain.</p><h4>The shape, decoded</h4><p>Picture an actual trophy. From the ground up it has four bands:</p><ul><li><strong>Static</strong> (the base) — TypeScript and ESLint. These catch typos, undefined variables, and type mismatches <em>before you even run a test</em>.</li><li><strong>Unit</strong> (thin stem) — small tests on isolated pieces of pure logic.</li><li><strong>Integration</strong> (the big fat cup) — the bulk. Render real component trees, click real buttons, assert real output.</li><li><strong>E2E</strong> (the small lid) — a few full browser journeys.</li></ul><p>The cup is biggest on purpose. That width represents <strong>confidence-per-cost</strong>: integration tests render actual components wired together, so they catch the bugs that live <em>between</em> units — the wiring — while still running far faster and more reliably than spinning up a whole browser for e2e.</p><div class=\"analogy\"><p>Think of checking that a car works. A unit test confirms the spark plug fires. An e2e test drives the car across the country. The integration test starts the engine in the driveway and listens to it idle — cheap, fast, and it catches the things that actually break: the fuel line that doesn't connect to the engine. Most of your time belongs there.</p></div><h4>Why heavily-mocked unit tests lie</h4><p>When you test one function in total isolation, you have to fake everything it talks to. Those fakes can drift from reality, so the test stays green while the assembled app is broken. The trophy pairs with the maxim: <em>\"the more your tests resemble the way your software is used, the more confidence they give you.\"</em> That's also why React Testing Library refuses to peek at internal state — it pushes you toward the user's vantage point.</p><h4>The trap</h4><p>The trophy does <strong>not</strong> mean \"skip unit tests.\" Pure logic — reducers, date math, money calculations — still belongs in fast, focused unit tests. The trophy just <em>rebalances the bulk</em> toward integration; it doesn't delete the stem.</p><h4>In practice</h4><p>Take a form. Instead of unit-testing the <code>onChange</code> handler alone, you render the whole form, type into the fields with <code>user-event</code>, click submit, and assert the success message appears. That single integration test exercises state updates, validation, and submission wiring all at once — exactly the seams where UI bugs hide.</p><div class=\"key\">Spend most of your effort on integration tests because they verify real wiring at a fraction of e2e's cost — but keep fast unit tests for pure logic and lean on static analysis as your free first line of defense.</div>"},
  "testing-pyramid": {"teach":"<p>At its core, the testing pyramid is just one rule: <strong>catch each bug at the cheapest, fastest layer that can possibly catch it.</strong></p><h4>The three tiers</h4><p>Picture a pyramid, widest at the bottom:</p><ul><li><strong>Unit tests</strong> (wide base) — many, tiny, fast, isolated. Test one function or class with everything else faked.</li><li><strong>Integration / service tests</strong> (middle) — fewer. Test that pieces work together, e.g. the API plus the database.</li><li><strong>E2E / UI tests</strong> (narrow tip) — very few. Drive the whole system like a user. Slow and brittle.</li></ul><p>The widths encode a tradeoff. As you climb, tests get slower, flakier, and harder to debug — when an e2e test fails, the bug could be <em>anywhere</em>. A failing unit test points right at the culprit. So you push tests <em>down</em> to the most deterministic layer.</p><div class=\"analogy\"><p>It's like quality control in a factory. You inspect each component as it's made (unit) — cheap and pinpoints the faulty part. You spot-check assembled sub-modules (integration). And you only fully road-test a handful of finished cars (e2e), because that's expensive and slow. You'd never road-test every car to discover a bad bolt.</p></div><h4>Where it shines, where it bites</h4><p>For backend and business logic the pyramid is excellent — pricing rules, validation, and algorithms are pure functions that unit-test beautifully.</p><p>For <strong>UI</strong> it's often <em>over-applied</em>. Maximizing unit tests forces you to heavily mock the DOM and dependencies, so you end up testing implementation details. The suite stays green while the integrated app is visibly broken — false confidence. That gap is exactly why the testing trophy fattens the integration middle for component-heavy frontends.</p><h4>The trap</h4><p>Treating the pyramid as universal law. It's a heuristic, not physics. For a React app, an all-unit suite gives a dangerous illusion of safety because UI bugs live in the <em>integration</em> of components, routing, and data — not inside a single isolated function.</p><h4>In practice</h4><p>A well-shaped backend service might have 200 unit tests on its pricing rules, 30 integration tests hitting the real API and database, and 5 e2e smoke tests on the checkout flow. Lots of cheap certainty at the bottom, a thin and deliberate layer of expensive certainty at the top.</p><div class=\"key\">Push every test to the lowest layer that can catch the bug — but recognize the pyramid was built for business logic; for UIs, the bugs migrate upward into integration, so the shape should too.</div>"},
  "react-testing-library": {"teach":"<p>At its core, React Testing Library is just a tool that <strong>looks at your component the way a human user would</strong> — by the text, labels, and buttons on screen — instead of poking at its private internals.</p><h4>What it actually does</h4><p>RTL renders your component into a real DOM (provided by <code>jsdom</code> in Node), then gives you queries to <em>find</em> things the way a person finds them: by accessible role, by label, by visible text. Crucially, it offers <strong>no</strong> way to read component state or call instance methods. That restriction is the whole philosophy: <em>test behavior, not implementation.</em></p><div class=\"analogy\"><p>Imagine reviewing a restaurant. The implementation-detail approach would inspect the chef's exact knife grip and pan brand. RTL reviews the <em>plate that arrives at your table</em> — does it taste right and look right? If the chef switches knives but the dish is identical, your review shouldn't change. So when you refactor a component's internals but the rendered output is the same, your tests stay green.</p></div><h4>Query priority is a free accessibility audit</h4><p>RTL ranks how you should find elements: by <strong>role</strong> and accessible name first, then label, then text, with <code>data-testid</code> dead last. This isn't arbitrary. If you <em>can't</em> reach a button with <code>getByRole('button', {name: /submit/i})</code>, then a screen reader can't reach it either. Following the priority order quietly forces your UI to be accessible.</p><h4>The three query flavors</h4><ul><li><code>getBy*</code> — returns the element, <strong>throws</strong> if it's missing. Use when it should exist now.</li><li><code>queryBy*</code> — returns <code>null</code> if missing (throws if multiple). Use to assert something is <em>absent</em>.</li><li><code>findBy*</code> — <strong>async</strong>; it's <code>getBy</code> wrapped in <code>waitFor</code>, retrying until the element appears. Use for elements that render after async work.</li></ul><h4>The trap</h4><p>Two common slips. First, reaching for <code>getByTestId</code> or <code>container.querySelector</code> by default — that bypasses the entire philosophy and hides accessibility gaps. Second, using <code>getByText</code> for something that only appears after a fetch resolves; it throws immediately because the element isn't there yet. Use <code>findBy*</code> for async UI.</p><h4>In practice</h4><p><code>const user = userEvent.setup(); await user.click(screen.getByRole('button', {name: /submit/i})); expect(await screen.findByRole('alert')).toHaveTextContent('Saved');</code> — click the submit button by its accessible name, then wait for the success alert.</p><div class=\"key\">RTL deliberately hides component internals so you test what the user experiences; querying by role and accessible name keeps your tests refactor-proof and your UI accessible in one move.</div>"},
  "user-event": {"teach":"<p>At its core, <code>user-event</code> is just a library that <strong>pretends to be a real person</strong> interacting with your page — and a real person triggers a whole cascade of browser events with every click and keystroke.</p><h4>Why a single event isn't enough</h4><p>When a user clicks a button, the browser doesn't fire one tidy \"click\" event. It fires a sequence: the pointer moves over the element (<code>pointerover</code>, <code>mouseover</code>, <code>pointermove</code>, <code>mousemove</code>), then presses down (<code>pointerdown</code>, <code>mousedown</code>), the element receives <code>focus</code>, then it releases (<code>pointerup</code>, <code>mouseup</code>), and only <em>then</em> the <code>click</code>. Typing is similar: each character fires <code>keydown</code>, <code>keypress</code>, <code>input</code>, and <code>keyup</code>, and the library respects <code>disabled</code>, <code>maxLength</code>, and the current text selection.</p><p>The older <code>fireEvent</code> dispatches just one synthetic event. That can <em>pass where a real user would fail</em> — for example, firing a raw <code>click</code> on a disabled button, or a <code>change</code> that skips the keystroke-by-keystroke logic your input depends on.</p><div class=\"analogy\"><p><code>fireEvent</code> is like a stagehand reaching in and flipping a switch backstage. <code>user-event</code> is an actor who walks across the stage, reaches out, and presses the switch the way the audience would see it happen. If a tripwire (a disabled prop, a focus handler) sits in the path, the actor trips on it — exactly as a real user would. The stagehand teleports past it and you never learn the tripwire was there.</p></div><h4>It's async now (v14)</h4><p>Since version 14, <code>user-event</code> is asynchronous and you set it up once per test: <code>const user = userEvent.setup();</code>. Every interaction method returns a promise you must <code>await</code>.</p><h4>The trap</h4><p>Two ways to get burned. First, forgetting the <code>await</code> in v14 — your assertions run <em>before</em> the interaction finishes, so they check stale state and fail mysteriously. Second, combining it with fake timers without telling it: pass <code>{ advanceTimers }</code> to <code>setup()</code>, or the interaction's internal delays never resolve and the test hangs forever.</p><h4>In practice</h4><p><code>const user = userEvent.setup(); await user.type(screen.getByLabelText('Email'), 'a@b.com'); await user.keyboard('{Enter}');</code> — this fires every keystroke event for the email, then a real Enter press, just as a person would submit the form.</p><div class=\"key\">Always <code>await user.setup()</code> interactions in v14 — <code>user-event</code> reproduces the browser's full event cascade so your tests fail on the same things a real user would hit, unlike <code>fireEvent</code>'s single synthetic shortcut.</div>"},
  "msw": {"teach":"<p>At its core, MSW is just a fake server that <strong>intercepts your app's real network calls and answers them itself</strong> — so your code genuinely believes it's talking to a live API.</p><h4>Intercepting at the network boundary</h4><p>The clever part is <em>where</em> MSW sits. Instead of replacing your HTTP client, it intercepts requests at the network layer: via a <strong>Service Worker</strong> in the browser, and via a request interceptor in Node. Your code still calls real <code>fetch</code> or <code>XHR</code>; the request leaves your code exactly as in production, and MSW catches it at the edge and replies with a handler you wrote.</p><div class=\"analogy\"><p>Think of mocking the HTTP client (like <code>jest.mock('axios')</code>) as bribing the mail-room clerk to hand you fake letters — if you switch clerks (axios to fetch), the whole scheme breaks. MSW instead is a stand-in post office at the edge of town: every letter your app sends still goes through the real mailbox and down the real road, but it's intercepted at the boundary. Swap clerks all you like — the post office still answers.</p></div><p>Because MSW is client-agnostic, switching axios↔fetch doesn't touch your mocks at all.</p><h4>One handler set, many uses</h4><p>The same handlers power your unit tests, integration tests, Storybook, and local development. And because MSW returns <em>real</em> status codes and headers, you can faithfully test the states people forget: loading spinners, error banners, and retry logic — just return a <code>500</code> or a slow response.</p><h4>The key APIs (v2)</h4><ul><li><code>setupServer(...handlers)</code> — runs MSW in Node for tests.</li><li><code>server.use(...)</code> — override a handler for a single test.</li><li><code>server.resetHandlers()</code> — clear those overrides between tests.</li><li>v2 uses the <code>http</code> / <code>HttpResponse</code> API.</li></ul><h4>The trap</h4><p>Forgetting <code>server.resetHandlers()</code> in <code>afterEach</code>. A per-test override then <em>leaks</em> into later tests, so a test that error-mocks <code>/api/user</code> poisons every test after it — and your suite fails or passes depending on <em>order</em>, the classic flaky nightmare. (The other trap is mocking the HTTP client directly, which couples your tests to your transport.)</p><h4>In practice</h4><p><code>const server = setupServer(http.get('/api/user', () => HttpResponse.json({name:'Ada'})));</code> sets the happy path. Inside one test: <code>server.use(http.get('/api/user', () => new HttpResponse(null,{status:500})));</code> simulates a server error so you can assert your error UI appears.</p><div class=\"key\">MSW mocks at the network boundary, not the HTTP client — so your code runs unchanged from production; just remember to <code>resetHandlers()</code> after each test or overrides will leak and make your suite order-dependent.</div>"},
  "vite": {"teach":"<p>At its core, Vite is just a dev server that feels <strong>instant</strong> because it lets the browser load your modules directly — plus a separate, optimized bundler for the production build.</p><h4>Two engines, one tool</h4><p>The thing that confuses newcomers is that Vite behaves differently in development versus production. It runs two engines:</p><ul><li><strong>Dev:</strong> Vite serves your source as <strong>native ESM</strong> with <em>no app bundling</em>. The browser asks for modules one at a time as it needs them. Meanwhile <strong>esbuild</strong> (written in Go) pre-bundles your <code>node_modules</code> dependencies and transpiles TypeScript/JSX. Startup is near-instant and stays fast no matter how big your app grows.</li><li><strong>Build:</strong> Vite switches to <strong>Rollup</strong>, which does proper tree-shaking, code-splitting, and has a mature plugin ecosystem to produce optimized production bundles.</li></ul><div class=\"analogy\"><p>Old bundler-based dev servers are like a restaurant that cooks <em>every dish on the menu</em> before opening, so you can't serve until it's all done — and the bigger the menu, the longer you wait. Vite's dev server is a made-to-order kitchen: it cooks only the dish a customer actually requests, the moment they ask. The menu can be enormous and the kitchen still opens instantly.</p></div><h4>Why two engines is a footgun</h4><p>Because dev uses esbuild and prod uses Rollup, <strong>they are not guaranteed to behave identically.</strong> The most important gap: esbuild <em>does not type-check</em> — it only strips TypeScript types and moves on. So a real type error compiles cleanly in dev and you never notice. Subtle differences between esbuild and Rollup can also let a build-only bug slip through.</p><p>The defense is twofold: run <code>tsc --noEmit</code> in CI for actual type safety, and always test the <em>production build</em>, not just the dev server, before you ship.</p><h4>Where it's heading</h4><p>To eliminate the dev/prod engine split, Vite is moving its bundling to <strong>Rolldown</strong> — a Rust-based, Rollup-compatible bundler — so both paths can eventually share one engine.</p><h4>In practice</h4><p><code>vite</code> launches the instant ESM dev server. <code>vite build</code> runs Rollup to emit minified, tree-shaken, code-split chunks ready for production.</p><div class=\"key\">Vite is fast in dev because the browser loads native ESM with no bundling — but dev (esbuild, no type-checking) and prod (Rollup) differ, so run <code>tsc --noEmit</code> and test the real production build before shipping.</div>"},
  "esbuild": {"teach":"<p>At its core, esbuild is just a <strong>blazingly fast tool that turns your modern TypeScript/JSX into plain JavaScript</strong> the browser can run — and bundles it — in milliseconds.</p><h4>Why it's so fast</h4><p>esbuild is written in <strong>Go</strong>, a compiled language that runs as native machine code and parallelizes easily across CPU cores. Traditional tools like Babel are written in JavaScript and run single-threaded on Node. That difference makes esbuild roughly <strong>10–100× faster</strong>. \"Transpiling\" means rewriting newer syntax (or TS/JSX) into a form older environments understand; \"bundling\" means stitching many module files into fewer output files.</p><div class=\"analogy\"><p>If Babel is a meticulous human translator working through a document page by page, esbuild is a room of native-speaker translators each taking a chapter simultaneously — and they read at superhuman speed. Same job, finished in a fraction of the time.</p></div><h4>Where you meet it</h4><p>You often use esbuild without naming it: it powers <strong>Vite's dev pre-bundling</strong>, converting CommonJS dependencies to ESM and transpiling your <code>.tsx</code> on the fly. You can also use it standalone for fast transpilation.</p><h4>The critical catch: it strips types, it doesn't check them</h4><p>Here's the trap that bites teams. esbuild <em>erases</em> TypeScript types — it deletes the annotations and keeps the JavaScript — but it does <strong>not</strong> verify that your types are correct. A genuine type error compiles cleanly and silently. esbuild also implements only a subset of advanced transforms.</p><p>So \"esbuild handles my TypeScript\" is dangerously incomplete. It handles the <em>syntax</em>, not the <em>safety</em>. The fix is to pair it with <code>tsc --noEmit</code>, which does the real type-checking, and to use Rollup for production bundling within Vite where you need its maturity.</p><div class=\"analogy\"><p>esbuild reading your types is like a copy editor who deletes all the footnotes to make a clean reading copy but never reads them for errors. The footnotes (types) vanish from the output, but nobody confirmed they were correct. That's <code>tsc</code>'s job.</p></div><h4>In practice</h4><p>During Vite dev, esbuild converts a CommonJS dependency into ESM and transpiles your <code>.tsx</code> components on the fly — no separate Babel step, no perceptible delay.</p><div class=\"key\">esbuild gives you near-instant TS/JSX transpilation and bundling by running native Go code in parallel — but it only <em>strips</em> types, so you still need <code>tsc --noEmit</code> to actually catch type errors.</div>"},
  "swc": {"teach":"<p>At its core, SWC is just a <strong>much faster replacement for Babel</strong> — it does the same job of turning modern and TypeScript code into widely-runnable JavaScript, but in a fraction of the time.</p><h4>What it does and why it's quick</h4><p>SWC stands for <strong>Speedy Web Compiler</strong>, and it's built in <strong>Rust</strong>. Like Babel, it strips TS/JSX and \"lowers\" newer syntax down to the targets in your <code>browserslist</code> (the set of browsers you support). Because Rust compiles to fast native code and parallelizes well, SWC runs roughly an <strong>order of magnitude faster</strong> than JavaScript-based Babel.</p><div class=\"analogy\"><p>Babel and SWC are two translators producing the same output document. Babel is a seasoned generalist who has, over decades, accumulated a vast library of specialist dictionaries (plugins) for obscure dialects. SWC is a prodigy who translates the common languages ten times faster — but hasn't yet bought every rare dictionary. For everyday work, pick the prodigy. For one obscure dialect, you still need the generalist's shelf.</p></div><h4>Where it shows up</h4><p>SWC has become a default in many toolchains: <strong>Next.js</strong> compiles your app with it, and <strong>Vitest</strong> and others use SWC or esbuild for transforms. It even ships a drop-in Jest transformer, <code>@swc/jest</code>, to speed up your test suite.</p><h4>When Babel still wins</h4><p>Babel's edge is its enormous <strong>plugin ecosystem</strong> — custom AST transforms, <code>babel-plugin-macros</code>, and bleeding-edge proposals that SWC simply hasn't implemented. If your build depends on one of those, Babel stays.</p><h4>The trap</h4><p>Two things to watch. First, like esbuild, SWC <em>strips</em> types without type-checking them — a type error compiles cleanly, so you still need <code>tsc</code> for safety. Second, and nastier: if you switch from Babel to SWC <em>blindly</em> while your build relies on a custom Babel plugin SWC doesn't support, the transform <strong>silently no-ops</strong>. There's no loud error — your code's behavior just quietly changes, which is the worst kind of bug to track down.</p><h4>In practice</h4><p>Next.js compiles your app with SWC by default. A <code>.swcrc</code> file lets you set <code>jsc.target</code> (the syntax level) and parser options for TypeScript plus JSX.</p><div class=\"key\">SWC is a Rust-based, ~10× faster Babel alternative now default in tools like Next.js — but it doesn't type-check, and a missing-plugin migration can silently change behavior, so verify your Babel plugins are covered before switching.</div>"},
  "tree-shaking": {"teach":"<p>At its core, tree shaking is just the bundler <strong>deleting the code you imported but never actually used</strong>, so it doesn't bloat what you ship.</p><h4>Why \"shaking\"?</h4><p>Picture your code as a tree where each export is a branch. The bundler grabs the trunk (your entry point), traces which branches are actually reachable, and <em>shakes</em> — everything that isn't connected to a real use falls off. The technical name is <strong>dead-code elimination</strong> of unused exports.</p><div class=\"analogy\"><p>It's like packing for a trip from your whole wardrobe. You lay out your itinerary (the entry point), trace what you'll actually wear, and leave the rest in the closet. You don't lug the entire wardrobe to the airport just because you <em>own</em> it.</p></div><h4>Why ESM makes it possible</h4><p>Tree shaking only works because of <strong>static ES modules</strong>. With <code>import</code> and <code>export</code>, the bundler can determine <em>at build time</em>, just by reading the code, exactly what's used. CommonJS <code>require</code> is <em>dynamic</em> — you can call it conditionally, with computed paths, at runtime — so the bundler can't safely prove an export is unused and must keep it. This is why transpiling ESM→CJS <em>before</em> bundling defeats tree shaking entirely.</p><h4>The big blocker: side effects</h4><p>A <strong>side effect</strong> is code that <em>does something</em> just by being imported — registers a global, injects CSS, installs a polyfill. The bundler can't drop such a module even if its exports look unused, because removing it would change how the app runs. To know which modules are safe to shake, bundlers read the <code>\"sideEffects\"</code> field in <code>package.json</code>. Setting it to <code>false</code> tells the bundler \"nothing here runs on import — shake aggressively,\" or you can list the specific files that <em>do</em> have effects.</p><h4>The trap</h4><p>Lying about side effects. If you set <code>\"sideEffects\": false</code> on a package that actually <em>does</em> have import-time effects — a global CSS import, a polyfill — the bundler happily drops them. Your app then breaks in subtle, hard-to-trace ways: missing styles, a polyfill that should have been there but vanished.</p><h4>In practice</h4><p><code>import { debounce } from 'lodash-es'</code> shakes down to just the <code>debounce</code> code, because <code>lodash-es</code> is statically analyzable ESM. But <code>import { debounce } from 'lodash'</code> pulls in the <em>entire</em> library, because the classic <code>lodash</code> is a CommonJS build the bundler can't shake.</p><div class=\"key\">Tree shaking removes unused exports — but only for static ESM, and only when side effects are declared honestly; an incorrect <code>\"sideEffects\": false</code> will silently drop CSS or polyfills your app needs.</div>"},
  "hmr": {"teach":"<p>At its core, Hot Module Replacement is just the dev server <strong>swapping the one file you edited into the already-running app</strong> — without reloading the page, so you keep all your current state.</p><h4>What \"keeping state\" means</h4><p>When you tweak code, the naive approach refreshes the whole browser tab. That throws away everything: the form you half-filled, the route you navigated to, the data in your store. HMR instead replaces <em>only</em> the changed module in place, so all that live state survives the edit.</p><div class=\"analogy\"><p>Imagine you're cooking an elaborate meal and realize the sauce needs more salt. A full page reload is like throwing out the entire meal and starting over from raw ingredients just to fix the sauce. HMR is reaching over and stirring salt into the one pot that needs it — everything else keeps simmering, untouched.</p></div><h4>How it works under the hood</h4><p>The dev server watches your files. When one changes, it pushes an update to the browser over a <strong>WebSocket</strong> (a persistent two-way connection). The HMR runtime in the browser <em>accepts</em> the new version of that module and re-renders just the affected part of the UI. Frameworks layer on extras — <strong>React Fast Refresh</strong>, for instance, is smart enough to preserve a component's internal state across edits, not just the surrounding app's.</p><h4>Why Vite makes it near-instant</h4><p>Because Vite serves native ESM where each module has its own URL, an edit only needs to <em>invalidate that one module's URL</em> — the update is essentially constant-time regardless of app size. Older bundler-based HMR (early webpack) had to recompute larger portions of the module graph, so it got slower as the app grew.</p><h4>The trap</h4><p>Confusing HMR with <strong>live-reload</strong>. They sound similar but differ crucially: live-reload refreshes the <em>entire page</em>, wiping your state. HMR replaces only the changed module and <em>keeps</em> your state. If you find your form clearing on every save, you've got live-reload, not true HMR.</p><h4>In practice</h4><p>You're styling a button in a Vite + React app with a modal open and a form half-filled. You change the button's color and hit save. The button updates instantly, and the modal stays open with every form value intact — no refresh, no lost work.</p><div class=\"key\">HMR swaps only the edited module into the live app over a WebSocket, preserving state — unlike live-reload, which refreshes the whole page; Vite's per-module ESM makes this near-instant no matter how large the app grows.</div>"},
  "flaky-test": {"teach":"<p>At its core, a flaky test is just a test that <strong>passes sometimes and fails other times on the exact same code</strong> — with nothing changed between runs.</p><h4>Why flakiness is so corrosive</h4><p>A test's entire value is being a trustworthy signal: green means safe, red means broken. A flaky test poisons that. When people see random reds, they learn to shrug and re-run — and the day a <em>real</em> bug turns the suite red, nobody believes it. A suite full of tolerated flakes trains the whole team to ignore failures, which defeats the point of having tests at all.</p><div class=\"analogy\"><p>A flaky test is a car alarm that goes off when a leaf lands on the hood. The first few times you rush out to check. After the twentieth false alarm, you stop listening — and that's exactly when someone actually breaks in. A test you've learned to ignore is worse than no test, because it cost you effort and bought you false comfort.</p></div><h4>Where the randomness comes from</h4><p>Flakiness is always hidden <strong>nondeterminism</strong> sneaking into your test:</p><ul><li><strong>Race conditions / arbitrary sleeps</strong> — you <code>sleep(300)</code> hoping something's ready; on a slow CI machine, it isn't.</li><li><strong>Shared state leaking</strong> — one test mutates a global the next test reads.</li><li><strong>Time/timezone/locale</strong> — a test that passes in one timezone fails in another.</li><li><strong>Animations or live network</strong> — timing and real servers are inherently unpredictable.</li></ul><h4>Fixing the source, not the symptom</h4><p>Each cause has a real fix: replace fixed sleeps with <strong>condition-based waiting</strong> (<code>findBy*</code>, <code>waitFor</code>, Playwright's auto-wait and retrying web-first assertions) that polls until the thing is actually ready; <strong>reset all state in <code>afterEach</code></strong> and even randomize test order to surface leaks; use <strong>fake timers</strong> and pin the timezone; and stub the network with <strong>MSW</strong>.</p><h4>The trap</h4><p>Slapping a retry or a longer <code>sleep</code> on a flaky test and declaring victory. That doesn't fix anything — it <em>masks</em> the flake. Worse, the underlying race condition is still real, and it can ship to production where there's no retry button.</p><h4>In practice</h4><p>A test does <code>await sleep(300)</code> then asserts a toast appeared — flaky whenever CI is slow. The proper fix: <code>expect(await screen.findByRole('status')).toBeVisible()</code>, which retries until the toast genuinely shows up, no matter the machine's speed.</p><div class=\"key\">Flaky tests come from hidden nondeterminism — fix the source with condition-based waiting, state resets, fake timers, and network stubs; never paper over it with retries or sleeps, which hide the very race that can ship to production.</div>"},
  "fake-timers": {"teach":"<p>At its core, fake timers let your tests <strong>fast-forward through time</strong> instead of really sitting and waiting for a timeout or interval to elapse.</p><h4>The problem they solve</h4><p>Lots of code is time-based: a debounced search waits 300ms after you stop typing, a poller fires every 5 seconds, a session times out after 30 minutes. If your test had to <em>actually wait</em> 30 minutes, the suite would be unusable. Even 300ms across hundreds of tests adds up and makes things flaky on slow machines.</p><h4>How they work</h4><p>Calling <code>jest.useFakeTimers()</code> or <code>vi.useFakeTimers()</code> (both built on <code>@sinonjs/fake-timers</code>) <em>replaces</em> the real <code>setTimeout</code>, <code>setInterval</code>, and optionally <code>Date</code> with controllable stand-ins. Now time doesn't pass on its own — <em>you</em> drive it. <code>advanceTimersByTime(300)</code> jumps the clock forward 300ms instantly; <code>runAllTimers()</code> fires everything pending. The waiting is deterministic and takes zero real seconds.</p><div class=\"analogy\"><p>It's like having a remote control for a movie. Normally you'd watch in real time and wait two hours for the ending. Fake timers let you hit fast-forward and jump straight to any scene. The story plays out in the exact same order — you just control <em>when</em> each beat happens instead of waiting for it.</p></div><h4>Three things you must get right</h4><ul><li><strong>Restore real timers</strong> in <code>afterEach</code> (<code>useRealTimers()</code>), or the fakes leak into later tests and cause bizarre failures.</li><li><strong>Wire them into <code>user-event</code> v14</strong>, which schedules its delays on <em>real</em> timers. Pass <code>userEvent.setup({ advanceTimers: jest.advanceTimersByTime })</code> — otherwise awaited interactions never resolve.</li><li><strong>Wrap React updates</strong> that result from advanced timers in <code>act()</code>, so React flushes them properly.</li></ul><h4>The trap</h4><p>Enabling fake timers but forgetting to connect them to <code>user-event</code>. Your awaited interaction schedules a delay on the <em>real</em> clock, which you've frozen, so it never fires — and the test just hangs until it times out. The cause is invisible unless you know to look for it.</p><h4>In practice</h4><p><code>vi.useFakeTimers(); render(<Debounced/>); fireEvent.change(input,{target:{value:'a'}}); vi.advanceTimersByTime(300); expect(onSearch).toHaveBeenCalled();</code> — type into the input, jump the debounce clock forward 300ms, and assert the search fired, all in an instant.</p><div class=\"key\">Fake timers swap real <code>setTimeout</code>/<code>setInterval</code> for a clock you control, making time-based tests instant and deterministic — but always restore real timers afterward, and wire them into <code>user-event</code> or your awaited interactions will hang.</div>"}
});
