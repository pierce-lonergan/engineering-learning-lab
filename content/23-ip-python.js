/* Python for Automation (IP-PYTHON) — fact-checked content wave (auto-assembled) */
CARDS_DATA.push(
  {"sec":"IP-PYTHON","front":"Why must you never use <code>float</code> for money in post-trade automation, and what do you use instead?","back":"<strong>Binary floats can't represent most decimal fractions exactly</strong>, so <code>0.1 + 0.2 == 0.30000000000000004</code>. In a recon engine that compares internal amounts against an external truth file (clearing SOD, settlement file, on-chain), float drift creates <strong>phantom breaks</strong> and mis-nets positions.<ul><li>Use <code>decimal.Decimal</code> constructed <em>from strings</em>: <code>Decimal('0.1')</code>, never <code>Decimal(0.1)</code> (the latter inherits the float error).</li><li>Set an explicit <code>getcontext().prec</code> and round with an explicit mode, e.g. <code>amount.quantize(Decimal('0.01'), rounding=ROUND_HALF_EVEN)</code>.</li><li>Compare with a <strong>tolerance</strong> for known fee/rounding differences, but the carried ledger value stays exact Decimal.</li></ul><em>In SQL store money as <code>NUMERIC/DECIMAL</code>, never <code>FLOAT</code>/<code>DOUBLE</code>.</em>"},
  {"sec":"IP-PYTHON","front":"Implement exponential backoff with jitter for retrying a flaky exchange REST call. Why jitter?","back":"<strong>Retry only transient failures</strong> (timeouts, 429, 5xx), never a 400/422 (bad request) or 409 (idempotency conflict).<pre><code>for attempt in range(max_retries):\n    try:\n        return client.post(url, json=payload,\n                           headers={'Idempotency-Key': key})\n    except (Timeout, RateLimited):\n        if attempt == max_retries - 1:\n            raise\n        cap_delay = min(cap, base * 2 ** attempt)\n        time.sleep(random.uniform(0, cap_delay))  # full jitter</code></pre><ul><li><strong>Jitter de-synchronizes</strong> retries so many clients hit by the same outage don't all retry on the same tick (the <em>thundering herd</em>), which would re-trigger the rate limit.</li><li><strong>Honor the <code>Retry-After</code></strong> header when present instead of the computed backoff.</li><li>Retries are only safe because every order/instruction carries an <strong>idempotency key</strong> — backoff + idempotency are a pair.</li></ul>"},
  {"sec":"IP-PYTHON","front":"Describe the idempotency-key pattern for safe retries of order entry / settlement instructions. What does a 409 mean?","back":"Server stores <code>(caller, idempotency_key) -&gt; (request_hash, response, status)</code>.<ul><li><strong>Same key + same payload</strong> (matching <code>request_hash</code>) -&gt; return the <em>original stored response</em>, do NOT re-execute. The network retry is now safe — you won't double-submit an order or double-instruct a settlement.</li><li><strong>Same key + different payload</strong> -&gt; <code>409 Conflict</code> — the key was reused for a semantically different request; reject loudly rather than silently doing the wrong thing.</li><li><strong>New key</strong> -&gt; execute, persist result keyed by the pair, return.</li></ul>This is the <strong>Stripe-style</strong> pattern (treat the 409 behavior as the community/IETF idempotency convention, not literal Stripe docs). The client generates a stable <code>client_order_id</code> per logical action. <em>It's the same primitive as payments/billing dedup — directly transferable from a payments/ledger background to trade ops.</em>"},
  {"sec":"IP-PYTHON","front":"What's wrong with <code>except Exception:</code> (or bare <code>except:</code>) in a settlement-automation script?","back":"<strong>It swallows too much.</strong> A bare <code>except:</code> even catches <code>KeyboardInterrupt</code>/<code>SystemExit</code> (they subclass <code>BaseException</code>, not <code>Exception</code>); a broad <code>except Exception:</code> hides bugs like <code>NameError</code> and <em>the one error you actually needed to act on</em>.<ul><li>Catch <strong>specific exceptions</strong>: <code>except (ConnectionError, Timeout):</code> — retry. <code>except ValidationError:</code> — break/quarantine the record. Let unexpected ones propagate.</li><li>Never <code>except: pass</code> — a settlement instruction that silently fails becomes a <strong>fail-to-deliver</strong> you discover next morning.</li><li>Log with context (<code>logger.exception()</code> captures the traceback) and <strong>re-raise or route to a dead-letter/break queue</strong>.</li><li>Use <code>raise ... from e</code> to preserve the cause chain.</li></ul><em>Fail fast and visibly on the unexpected; only the expected, recoverable error is caught narrowly.</em>"},
  {"sec":"IP-PYTHON","front":"Parse a fixed-width Start-of-Day (SOD) position file robustly in Python.","back":"Fixed-width = fields at byte offsets, no delimiter (legacy clearing/custodian format).<ul><li>Define a schema of slices: <code>FIELDS = {'account': (0,10), 'symbol': (10,22), 'qty': (22,37), 'price': (37,52)}</code>.</li><li><code>row = {k: line[s:e].strip() for k,(s,e) in FIELDS.items()}</code> — <strong>always <code>.strip()</code></strong> the padded fields.</li><li>Convert money/qty via <code>Decimal(row['price'])</code>, never float.</li><li>Validate <strong>header/trailer control records</strong> (record count, hash-total of amounts) before trusting the body — a truncated file should raise, not silently under-recon.</li><li>Pandas alternative: <code>pd.read_fwf(path, colspecs=[(0,10),...], dtype=str)</code>, then cast deliberately (keep IDs as <code>str</code> so leading zeros / 0x addresses survive).</li></ul><em>Treat the external file as the truth source; assume it can be malformed, short, or duplicated.</em>"},
  {"sec":"IP-PYTHON","front":"How do you correctly handle timestamps when reconciling trades across equities, crypto (24/7), and prediction-market venues?","back":"<strong>Store and compute in UTC; convert to a market timezone only for display/cutoffs.</strong><ul><li>Always use <strong>timezone-aware</strong> datetimes: <code>datetime.now(timezone.utc)</code>, never naive <code>datetime.now()</code>.</li><li>Use <code>zoneinfo.ZoneInfo('America/New_York')</code> for DTCC/exchange cutoffs (e.g. the <strong>9:00 PM ET affirmation</strong>, T+1 settlement-date math) — DST is handled correctly, a fixed <code>-05:00</code> is not.</li><li><strong>Settlement-date arithmetic uses business days</strong>, not calendar days (a trading-calendar/holiday lib), while crypto settles 24/7 with no holidays.</li><li>A classic bug: a UTC timestamp <em>mislabeled</em> as ET (or vice-versa) silently shifts the trade date and breaks recon — derive ET from the raw source timestamp, don't trust a pre-labeled column.</li></ul>"},
  {"sec":"IP-PYTHON","front":"What is T+1, when did it start, and why does it raise the stakes for Python automation in post-trade?","back":"<strong>T+1 = settlement one business day after trade date.</strong> The US moved <strong>T+2 -&gt; T+1 on May 28, 2024</strong> (equities, corporate/muni bonds, UITs, ADRs, ETFs); EU/UK align <strong>Oct 11, 2027</strong>.<ul><li>The DTCC <strong>affirmation cutoff is 9:00 PM ET on trade date</strong> — the intraday window to catch/fix breaks collapsed to roughly 2-5 hours.</li><li>Unresolved exceptions become <strong>settlement fails</strong> (financial + regulatory cost), so <strong>STP (straight-through processing)</strong> and automated exception handling are mandatory, not nice-to-have.</li><li>This is exactly why the role exists: Python+SQL systems that auto-reconcile internal records vs the external truth and surface breaks fast enough to fix same-day.</li></ul>"},
  {"sec":"IP-PYTHON","front":"Walk through the core reconciliation algorithm: internal records vs an external truth file.","back":"<ul><li><strong>Match on stable identifiers</strong>: trade id, then a composite key (symbol, amount, currency, settlement date) when no shared id exists.</li><li><strong>Tolerate expected discrepancies</strong>: timing lag, partial fills, fees, rounding — apply tolerances rather than flagging them as breaks.</li><li><strong>Anything unmatched = a BREAK</strong> (exception): internal-not-external, external-not-internal, or matched-but-amount-differs.</li><li>Breaks route to tooling for <strong>manual or bulk correction</strong>; everything else auto-clears.</li></ul>Pandas sketch: <code>merge(internal, external, on='trade_id', how='outer', indicator=True)</code>, then <code>_merge=='left_only'</code> / <code>'right_only'</code> are missing-side breaks, and matched rows with <code>abs(amt_int - amt_ext) &gt; tol</code> are amount breaks. <em>The external source (clearing/custodian SOD, exchange/processor settlement file, on-chain data) is the truth; internal is what you reconcile to it.</em>"},
  {"sec":"IP-PYTHON","front":"Why are type hints + <code>@dataclass</code> worth it in production trade-ops code?","back":"<strong>They turn implicit dict-shaped data into a checked contract.</strong><ul><li><code>@dataclass(frozen=True)</code> gives an immutable, self-documenting record with <code>__init__</code>, <code>__eq__</code>, <code>__repr__</code> for free — immutability prevents accidental mutation of a booked record mid-recon.</li><li>Hints like <code>price: Decimal</code>, <code>settle_date: date</code>, <code>qty: int</code> let <strong>mypy</strong> catch a float-where-Decimal or naive-datetime mistake at CI time, before it reaches a settlement instruction.</li><li>For validated external input (SOD rows, API payloads), <strong>Pydantic</strong> adds runtime coercion/validation on top of the type annotations.</li></ul><pre><code>@dataclass(frozen=True)\nclass Fill:\n    order_id: str\n    price: Decimal\n    qty: int\n    ts: datetime</code></pre>"},
  {"sec":"IP-PYTHON","front":"How do you unit-test code that calls an external exchange API, without hitting the network?","back":"<strong>Mock the boundary; never call the real venue in a unit test.</strong><ul><li><code>unittest.mock</code> / pytest <code>monkeypatch</code> to replace the HTTP client, or <code>responses</code>/<code>respx</code> to stub specific endpoints with canned JSON.</li><li>Assert <strong>behavior</strong>: a 429 triggers backoff-then-retry, a 409 does NOT retry, the <code>Idempotency-Key</code> header is sent, a malformed payload raises.</li><li>Use <strong>pytest fixtures</strong> for reusable test data (a sample SOD file, a settlement record) and <code>@pytest.mark.parametrize</code> for tolerance/edge cases (zero qty, partial fill, negative price).</li><li>Use <code>freezegun</code> or an injected clock to make timezone &amp; cutoff logic deterministic.</li><li>Lock the <strong>break-detection</strong> path with golden internal+external fixtures so recon logic is regression-protected.</li></ul><em>Goal: fast, deterministic, offline tests that pin the exact failure-handling semantics money code depends on.</em>"},
  {"sec":"IP-PYTHON","front":"FIX drop-copy: what is it, which messages matter, and how would you consume it in Python for trade capture?","back":"<strong>A drop-copy is a read-only FIX feed of your own fills</strong> for independent trade-capture / real-time recon (offered by e.g. Coinbase Derivatives, Zero Hash).<ul><li>It delivers <strong><code>35=8</code> ExecutionReports</strong>. Key tag <code>150</code> (ExecType): <strong><code>150=F</code> Trade</strong>, <strong><code>150=G</code> Trade Correct</strong>, <strong><code>150=H</code> Trade Cancel</strong>.</li><li>FIX is <code>tag=value</code> pairs over a stateful session with <strong>sequence numbers</strong>; gaps trigger a <strong>ResendRequest</strong> to replay missed messages — your consumer must track and persist <code>MsgSeqNum</code>, not just read a socket.</li><li>In Python you'd use a FIX engine (e.g. <code>quickfix</code>) implementing <code>onMessage</code>, parse the exec reports, normalize to your <code>Fill</code> dataclass, and book idempotently keyed on <code>ExecID</code> (tag 17) / <code>ClOrdID</code> (tag 11).</li></ul><em>Order entry is <code>NewOrderSingle 35=D</code>; crypto/prediction venues are instead predominantly REST + WebSocket.</em>"},
  {"sec":"IP-PYTHON","front":"Contrast connectivity: traditional equities (FIX) vs crypto/prediction-market venues (REST + WebSocket). What does each demand of your Python?","back":"<ul><li><strong>Equities / institutional: FIX</strong> — <code>tag=value</code>, persistent sessions, sequence numbers, <code>35=D</code> NewOrderSingle, <code>35=8</code> ExecutionReport, drop-copy for recon. Your code maintains session state, heartbeats, and resend logic.</li><li><strong>Crypto &amp; prediction markets (Kalshi, Polymarket): REST + WebSocket</strong> — REST for order placement/queries, WS for streaming fills/order-book. Demands: <strong>idempotency via client order IDs</strong>, <strong>rate-limit handling</strong> (429 + <code>Retry-After</code>), <strong>retries with exponential backoff + jitter</strong>, WS reconnect-and-resync (snapshot then deltas), and signing/nonce auth.</li></ul>Both feed the same downstream: normalize heterogeneous venue messages into one internal trade model, then reconcile against the external settlement truth. <em>Multi-asset desks juggle all three transports at once.</em>"},
  {"sec":"IP-PYTHON","front":"How do prediction-market contracts settle, and what new reconciliation-risk dimension does that add vs equities?","back":"<strong>Binary event contracts priced $0-$1</strong> (= implied probability); settle to <strong>$1 if the event occurs, else $0</strong>. Kalshi is a <strong>CFTC DCM</strong> (2020); Polymarket (crypto-native, Polygon/USDC, 2020) became a CFTC-regulated US exchange via the <strong>Amended Order of Nov 25, 2025</strong>.<ul><li>Settlement is to <strong>EVENT RESOLUTION</strong>, not a clearing-house price — so the new risk is <strong>oracle / resolution risk</strong>: disputed or delayed outcomes, and the same contract resolving differently across platforms (<strong>multi-platform arb</strong>).</li><li>The <strong>CFTC Prediction Markets Advisory (Feb 25, 2026)</strong> flags insider trading, wash/pre-arranged trades, spoofing, and manipulation — your automation must surveil for these.</li><li>Recon must track <em>which resolution source</em> applies and handle a contract sitting unresolved past its event date.</li></ul>"},
  {"sec":"IP-PYTHON","front":"What's different about crypto post-trade reconciliation, and what role do stablecoins play in 2026 settlement?","back":"<ul><li><strong>No central depository</strong> (unlike DTC), <strong>multiple chains</strong>, <strong>24/7</strong>, and breaks driven by <strong>gas fees, address mapping, and on-chain finality</strong>.</li><li><strong>Finality risk:</strong> a confirmed transfer can be undone by a <strong>reorg</strong>, so recon waits for N confirmations / probabilistic finality before booking it settled.</li><li>Custody splits <strong>hot (online, fast) vs cold (offline, secure)</strong> wallets — you reconcile balances across both.</li><li><strong>2026 cash leg:</strong> stablecoins / tokenized deposits act as the <strong>on-chain CASH LEG</strong> of settlement. The <strong>GENIUS Act</strong> stablecoin regime (per the Mar 2, 2026 OCC guidance) requires <strong>timely redemption no later than 2 business days</strong> — your automation tracks those redemption SLAs.</li></ul><em>Your Python normalizes on-chain data (the external truth) against internal trade records, tolerating gas/timing diffs and flagging reorg-reverted or unconfirmed transfers.</em>"},
  {"sec":"IP-PYTHON","front":"Explain the DTCC settlement chain (ITP, NSCC, DTC) and where affirmation fits — the context your recon tooling feeds.","back":"<ul><li><strong>ITP (Institutional Trade Processing)</strong>: pre-settlement <strong>matching / affirmation</strong> — confirm trade economics and <em>match-to-instruct</em>. The <strong>9:00 PM ET trade-date affirmation cutoff gates everything downstream.</strong></li><li><strong>NSCC — the CCP</strong>: <strong>multilateral netting</strong>, <strong>novation</strong> (NSCC becomes counterparty to both sides), and the <strong>guarantee</strong>; settlement runs through <strong>Continuous Net Settlement (CNS)</strong>, netting many trades into a single per-security obligation.</li><li><strong>DTC — the central securities depository</strong>: actual <strong>settlement &amp; custody</strong> via <strong>book-entry</strong> (no physical certificates).</li></ul><strong>Affirmation gates NSCC/DTC settlement</strong> — an unaffirmed trade can't settle on time and becomes a fail. Your automation's job is to get internal vs external matched and affirmed inside the T+1 window so the trade flows cleanly through this chain."},
  {"sec":"IP-PYTHON","front":"How do you schedule and operate these jobs in production (cron vs Airflow), and what makes a scheduled recon job reliable?","back":"<ul><li><strong>cron</strong>: fine for a single, independent, time-triggered script (e.g. pull the SOD file at 6:00 AM ET). Simple, but no dependency graph, retries, backfill, or visibility.</li><li><strong>Airflow</strong> (or similar): a <strong>DAG</strong> expresses <em>dependencies</em> — wait-for-file sensor -&gt; parse -&gt; reconcile -&gt; publish breaks -&gt; alert — with built-in <strong>retries, SLAs, backfill, and a UI</strong>. Right when steps must run in order and failures must be visible.</li></ul>Reliability essentials regardless of scheduler:<ul><li><strong>Idempotent runs</strong> (re-running the 6 AM job doesn't double-book) and <strong>partition by business date</strong>.</li><li><strong>Structured logging + alerting</strong> on failure — a missed recon = an undetected break.</li><li><strong>Timezone-correct triggers</strong> tied to market cutoffs, and <strong>file-arrival sensing</strong> rather than assuming the SOD file is on time.</li></ul>"},
  {"sec":"IP-PYTHON","front":"Build a small CLI to run a recon for a given date. argparse vs click, and what flags matter?","back":"<strong>argparse</strong> (stdlib, zero deps) vs <strong>click</strong> (decorator-based, nicer for multi-command tools, type coercion, prompts). For an internal one-shot, argparse is fine; for a growing toolkit, click.<pre><code>p = argparse.ArgumentParser()\np.add_argument('--date', type=date.fromisoformat, required=True)\np.add_argument('--asset', choices=['equities','crypto','prediction'])\np.add_argument('--dry-run', action='store_true')\np.add_argument('--log-level', default='INFO')</code></pre><ul><li><strong><code>--dry-run</code></strong> is essential — preview breaks/corrections without writing instructions.</li><li><strong>Validate/coerce at the boundary</strong> (<code>date.fromisoformat</code>, <code>choices=</code>) so bad input dies at parse time, not mid-settlement.</li><li><strong>Exit codes</strong>: return non-zero when breaks remain unresolved so a scheduler/monitor can alert.</li><li>Keep the CLI a thin shell over importable, unit-tested functions.</li></ul>"}
);

Object.assign(GLOSSARY_DATA, {
  "decimal-not-float": {"term":"Decimal vs float for money","group":"IP-PYTHON","eli5":"Use Python's exact Decimal type for currency because regular floats can't store amounts like $0.10 precisely.","memory":"Money is exact; floats are approximate. Decimal('0.10'), never 0.10.","full":"<p>Binary floating point cannot exactly represent most base-10 fractions, so <code>0.1 + 0.2</code> yields <code>0.30000000000000004</code>. In post-trade recon this manifests as tiny phantom differences that look like <strong>breaks</strong> against the external truth file, or as mis-netted positions. <code>decimal.Decimal</code> stores base-10 values exactly and lets you control precision and rounding.</p><p>Construct from strings (<code>Decimal('0.10')</code>) so you don't import a float's error; set <code>getcontext().prec</code>; round explicitly with <code>quantize(Decimal('0.01'), rounding=ROUND_HALF_EVEN)</code>. In SQL, store money as <code>NUMERIC/DECIMAL</code>, never <code>FLOAT</code>.</p>","trap":"<code>Decimal(0.1)</code> passes a float in and silently keeps the float error — you MUST pass a string: <code>Decimal('0.1')</code>.","worked":"<pre><code>from decimal import Decimal, ROUND_HALF_EVEN\nfee = Decimal('0.0015')\ngross = Decimal('1000.00')\nnet = (gross * (1 - fee)).quantize(\n    Decimal('0.01'), rounding=ROUND_HALF_EVEN)  # Decimal('998.50')</code></pre>","related":["reconciliation"]},
  "backoff-jitter": {"term":"Exponential backoff with jitter","group":"IP-PYTHON","eli5":"When a call fails transiently, wait longer each retry plus a random amount so clients don't all retry at once.","memory":"delay = random(0, min(cap, base * 2**attempt)). Jitter breaks the herd.","full":"<p>Exponential backoff increases the wait between retries geometrically (<code>base * 2**attempt</code>, capped) to give a struggling venue time to recover. <strong>Full jitter</strong> picks a random delay in <code>[0, computed]</code> so many clients hit by the same outage don't synchronize their retries (the thundering herd) and re-trigger the rate limit.</p><p>Only retry transient errors (timeouts, 429, 5xx); honor a <code>Retry-After</code> header when present. Backoff is only safe to apply to writes when paired with an <strong>idempotency key</strong>.</p>","trap":"Retrying a 400/422 (bad request) or 409 (idempotency conflict) is pointless and harmful — those are deterministic; back off only on transient failures.","worked":"<pre><code>sleep = random.uniform(0, min(cap, base * 2 ** attempt))\ntime.sleep(sleep)</code></pre>","related":["idempotency-key","rest-websocket"]},
  "dtcc-chain": {"term":"DTCC chain (ITP / NSCC / DTC)","group":"IP-PYTHON","eli5":"The US plumbing that matches, nets, guarantees, and finally settles equity trades.","memory":"ITP affirms, NSCC nets+guarantees (CNS), DTC settles+custodies (book-entry).","full":"<p><strong>ITP</strong> handles pre-settlement matching/affirmation (match-to-instruct). <strong>NSCC</strong> is the central counterparty: <strong>multilateral netting</strong>, <strong>novation</strong>, and the trade <strong>guarantee</strong>, running through <strong>Continuous Net Settlement (CNS)</strong>. <strong>DTC</strong> is the central securities depository providing settlement and custody via <strong>book-entry</strong>.</p><p>Affirmation gates NSCC/DTC settlement: an unaffirmed trade can't settle on time and becomes a fail. Recon tooling's purpose is to match and affirm inside the T+1 window so trades flow cleanly through this chain.</p>","trap":"NSCC (the CCP that nets/guarantees) and DTC (the depository that custodies/settles) are different entities — conflating them is a common interview slip.","worked":"Affirmed trade -> NSCC nets your buys/sells per security into one CNS obligation -> DTC moves shares book-entry between accounts.","related":["t-plus-one","reconciliation"]},
  "rest-websocket": {"term":"REST + WebSocket (crypto/prediction venues)","group":"IP-PYTHON","eli5":"Crypto and prediction-market exchanges use HTTP requests plus a streaming socket instead of FIX.","memory":"REST to place/query, WS to stream; need client order IDs + rate-limit + reconnect.","full":"<p>Unlike FIX-based equity venues, crypto and prediction markets (Kalshi, Polymarket) are <strong>predominantly REST + WebSocket</strong>: REST for placing/querying orders, WebSocket for streaming fills and order-book updates. Production clients need <strong>idempotency via client order IDs</strong>, <strong>rate-limit handling</strong> (429 + <code>Retry-After</code>), <strong>retries with exponential backoff + jitter</strong>, and WS <strong>reconnect-and-resync</strong> (snapshot then deltas), plus signing/nonce auth.</p><p>Multi-asset desks normalize FIX and REST/WS messages into a single internal trade model before reconciling against the external settlement truth.</p>","trap":"WebSocket order books drift if you apply deltas without first anchoring to a snapshot (and tracking sequence numbers) — a dropped message silently corrupts your book.","worked":"<pre><code>ws.subscribe('fills')  # stream\nclient.post('/orders', json=o,\n            headers={'Idempotency-Key': cloid})  # place</code></pre>","related":["fix-drop-copy","backoff-jitter","idempotency-key","prediction-markets"]},
  "datetime-tz": {"term":"datetime & timezone correctness","group":"IP-PYTHON","eli5":"Always use timezone-aware times in UTC, converting to a market timezone only for cutoffs and display.","memory":"Aware UTC everywhere; ZoneInfo for ET cutoffs; never naive datetime.now().","full":"<p>Store and compute in <strong>UTC with timezone-aware</strong> datetimes (<code>datetime.now(timezone.utc)</code>); convert with <code>zoneinfo.ZoneInfo('America/New_York')</code> only for market cutoffs and display so DST is handled correctly. The DTCC <strong>9:00 PM ET affirmation</strong> and T+1 date math depend on this.</p><p>Settlement-date arithmetic uses <strong>business days</strong> (trading calendar/holidays) for equities, while crypto runs 24/7 with no holidays. Always derive ET from the raw source timestamp rather than trusting a pre-labeled column.</p>","trap":"A UTC timestamp mislabeled as ET (or a naive datetime assumed local) silently shifts the trade/settlement date and breaks recon — and a fixed <code>-05:00</code> offset is wrong half the year.","worked":"<pre><code>from zoneinfo import ZoneInfo\nutc_ts = datetime.now(timezone.utc)\net = utc_ts.astimezone(ZoneInfo('America/New_York'))\nbefore_cutoff = et.time() &lt; time(21, 0)  # 9 PM ET</code></pre>","related":["t-plus-one","reconciliation"]}
});
